/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EntitlementsView,
  MutateReq,
  ProjectionView,
} from '../models/index.js';
import {
    EntitlementsViewFromJSON,
    EntitlementsViewToJSON,
    MutateReqFromJSON,
    MutateReqToJSON,
    ProjectionViewFromJSON,
    ProjectionViewToJSON,
} from '../models/index.js';

export interface EntitlementApiGetEntitlementOrgsByOrgRequest {
    org: string;
}

export interface EntitlementApiPostEntitlementOrgsByOrgRequest {
    org: string;
    mutateReq: MutateReq;
}

/**
 * 
 */
export class EntitlementApi extends runtime.BaseAPI {

    /**
     * Projection reports which console apps the CALLER\'s org may open, and the plan slug that decides it. It is the READ side of the unified paywall: the org\'s plan tier resolved from commerce, which is a different authority from the enablement store behind GET /v1/entitlement/orgs/{org} (that one is the org\'s own on/off intent).  It fails SAFE-TO-LOCKED, never 500: an unvalidated principal is a 403, but a commerce outage reports every app locked at 200 rather than breaking the shell. The ENFORCEMENT path still fails open, so functionality survives the same outage even while the UI conservatively shows locked.
     * Projection reports which console apps the CALLER\'s org may open, and the plan slug that decides it.
     */
    async getEntitlementRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectionView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/entitlement`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectionViewFromJSON(jsonValue));
    }

    /**
     * Projection reports which console apps the CALLER\'s org may open, and the plan slug that decides it. It is the READ side of the unified paywall: the org\'s plan tier resolved from commerce, which is a different authority from the enablement store behind GET /v1/entitlement/orgs/{org} (that one is the org\'s own on/off intent).  It fails SAFE-TO-LOCKED, never 500: an unvalidated principal is a 403, but a commerce outage reports every app locked at 200 rather than breaking the shell. The ENFORCEMENT path still fails open, so functionality survives the same outage even while the UI conservatively shows locked.
     * Projection reports which console apps the CALLER\'s org may open, and the plan slug that decides it.
     */
    async getEntitlement(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectionView> {
        const response = await this.getEntitlementRaw(initOverrides);
        return await response.value();
    }

    /**
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show. It is distinct from what the org\'s plan ENTITLES it to (that is GET /v1/entitlement, resolved from commerce).  A caller may only read its OWN org\'s row; a platform super admin may read any.
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show.
     */
    async getEntitlementOrgsByOrgRaw(requestParameters: EntitlementApiGetEntitlementOrgsByOrgRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntitlementsView>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling getEntitlementOrgsByOrg().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/entitlement/orgs/{org}`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntitlementsViewFromJSON(jsonValue));
    }

    /**
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show. It is distinct from what the org\'s plan ENTITLES it to (that is GET /v1/entitlement, resolved from commerce).  A caller may only read its OWN org\'s row; a platform super admin may read any.
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show.
     */
    async getEntitlementOrgsByOrg(requestParameters: EntitlementApiGetEntitlementOrgsByOrgRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntitlementsView> {
        const response = await this.getEntitlementOrgsByOrgRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Post turns products on or off for an org and returns the enabled set afterwards.  A product may only be ENABLED if the org\'s plan already ENTITLES it, so enabling never spends new money — a product the plan does not grant answers 402 and the console routes that to an upgrade prompt. DISABLING is never gated. A platform super admin bypasses the plan check (operator comp/grant) and may target any org; everyone else may only change their own. Commerce unreachable is a 503, never an implicit yes.
     * Post turns products on or off for an org and returns the enabled set afterwards.
     */
    async postEntitlementOrgsByOrgRaw(requestParameters: EntitlementApiPostEntitlementOrgsByOrgRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntitlementsView>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling postEntitlementOrgsByOrg().'
            );
        }

        if (requestParameters['mutateReq'] == null) {
            throw new runtime.RequiredError(
                'mutateReq',
                'Required parameter "mutateReq" was null or undefined when calling postEntitlementOrgsByOrg().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/entitlement/orgs/{org}`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MutateReqToJSON(requestParameters['mutateReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntitlementsViewFromJSON(jsonValue));
    }

    /**
     * Post turns products on or off for an org and returns the enabled set afterwards.  A product may only be ENABLED if the org\'s plan already ENTITLES it, so enabling never spends new money — a product the plan does not grant answers 402 and the console routes that to an upgrade prompt. DISABLING is never gated. A platform super admin bypasses the plan check (operator comp/grant) and may target any org; everyone else may only change their own. Commerce unreachable is a 503, never an implicit yes.
     * Post turns products on or off for an org and returns the enabled set afterwards.
     */
    async postEntitlementOrgsByOrg(requestParameters: EntitlementApiPostEntitlementOrgsByOrgRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntitlementsView> {
        const response = await this.postEntitlementOrgsByOrgRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
