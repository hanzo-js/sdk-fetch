/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  InstallReq,
  InstallState,
  Listing,
  ListingPage,
  MarketCatalog,
  PublishReq,
} from '../models/index.js';
import {
    InstallReqFromJSON,
    InstallReqToJSON,
    InstallStateFromJSON,
    InstallStateToJSON,
    ListingFromJSON,
    ListingToJSON,
    ListingPageFromJSON,
    ListingPageToJSON,
    MarketCatalogFromJSON,
    MarketCatalogToJSON,
    PublishReqFromJSON,
    PublishReqToJSON,
} from '../models/index.js';

export interface MarketplaceApiDeleteMarketplaceListingsByIdRequest {
    id: string;
}

export interface MarketplaceApiPostMarketplaceInstallRequest {
    installReq: InstallReq;
}

export interface MarketplaceApiPostMarketplaceListingsRequest {
    publishReq: PublishReq;
}

export interface MarketplaceApiPostMarketplaceUninstallRequest {
    installReq: InstallReq;
}

/**
 * 
 */
export class MarketplaceApi extends runtime.BaseAPI {

    /**
     * Unpublish withdraws one of the caller org\'s listings from the marketplace and answers 204. Only the publishing org can remove its own listing; an id that is unknown, or belongs to another org, is the same 404, so a probe learns nothing about what exists. Removing a listing removes its price from per-call enforcement; it does not uninstall the tool for anyone who already installed it.
     * Unpublish withdraws one of the caller org\'s listings from the marketplace and answers 204.
     */
    async deleteMarketplaceListingsByIdRaw(requestParameters: MarketplaceApiDeleteMarketplaceListingsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteMarketplaceListingsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketplace/listings/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Unpublish withdraws one of the caller org\'s listings from the marketplace and answers 204. Only the publishing org can remove its own listing; an id that is unknown, or belongs to another org, is the same 404, so a probe learns nothing about what exists. Removing a listing removes its price from per-call enforcement; it does not uninstall the tool for anyone who already installed it.
     * Unpublish withdraws one of the caller org\'s listings from the marketplace and answers 204.
     */
    async deleteMarketplaceListingsById(requestParameters: MarketplaceApiDeleteMarketplaceListingsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMarketplaceListingsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Discover lists every tool and agent the caller can reach in their own org and project, enriched with any public listing\'s title, category and price, and with installed=true on the ones already activated for that scope. It is the shop window: one read that answers what exists, what it costs and what is already on.
     * Discover lists every tool and agent the caller can reach in their own org and project, enriched with any public listing\'s title, category and price, and with installed=true on the ones already activated for that scope.
     */
    async getMarketplaceRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MarketCatalog>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketplace`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MarketCatalogFromJSON(jsonValue));
    }

    /**
     * Discover lists every tool and agent the caller can reach in their own org and project, enriched with any public listing\'s title, category and price, and with installed=true on the ones already activated for that scope. It is the shop window: one read that answers what exists, what it costs and what is already on.
     * Discover lists every tool and agent the caller can reach in their own org and project, enriched with any public listing\'s title, category and price, and with installed=true on the ones already activated for that scope.
     */
    async getMarketplace(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MarketCatalog> {
        const response = await this.getMarketplaceRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the listings the caller\'s own org has published — what this org is offering, not what it can buy. A publisher only ever sees its own rows.
     * Returns the listings the caller\'s own org has published — what this org is offering, not what it can buy.
     */
    async getMarketplaceListingsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListingPage>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketplace/listings`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListingPageFromJSON(jsonValue));
    }

    /**
     * Returns the listings the caller\'s own org has published — what this org is offering, not what it can buy. A publisher only ever sees its own rows.
     * Returns the listings the caller\'s own org has published — what this org is offering, not what it can buy.
     */
    async getMarketplaceListings(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListingPage> {
        const response = await this.getMarketplaceListingsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Install activates one tool for the caller\'s own org and project. A marketplace install IS the tool plane\'s activation write — one store, one truth — so an installed capability is immediately dispatchable and a monetized one is priced from its listing at every call. The tool must resolve in the caller\'s scope, so installing something that does not exist is refused rather than recorded.
     * Install activates one tool for the caller\'s own org and project.
     */
    async postMarketplaceInstallRaw(requestParameters: MarketplaceApiPostMarketplaceInstallRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InstallState>> {
        if (requestParameters['installReq'] == null) {
            throw new runtime.RequiredError(
                'installReq',
                'Required parameter "installReq" was null or undefined when calling postMarketplaceInstall().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketplace/install`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: InstallReqToJSON(requestParameters['installReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstallStateFromJSON(jsonValue));
    }

    /**
     * Install activates one tool for the caller\'s own org and project. A marketplace install IS the tool plane\'s activation write — one store, one truth — so an installed capability is immediately dispatchable and a monetized one is priced from its listing at every call. The tool must resolve in the caller\'s scope, so installing something that does not exist is refused rather than recorded.
     * Install activates one tool for the caller\'s own org and project.
     */
    async postMarketplaceInstall(requestParameters: MarketplaceApiPostMarketplaceInstallRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InstallState> {
        const response = await this.postMarketplaceInstallRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publish offers one tool on the marketplace, optionally monetized. The tool must already resolve in the publisher\'s own scope, so a listing can never advertise a capability that does not exist; a listing with a price must name the payout wallet the x402 seam settles to, so a monetized offer is never unpayable. The price is exact to 18 decimal places, so a per-call price below a cent is a real price and not a rounded-away zero. The listing is owned by the publishing org, paid into a wallet of that same org, and answers 201 with the created row.
     * Publish offers one tool on the marketplace, optionally monetized.
     */
    async postMarketplaceListingsRaw(requestParameters: MarketplaceApiPostMarketplaceListingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Listing>> {
        if (requestParameters['publishReq'] == null) {
            throw new runtime.RequiredError(
                'publishReq',
                'Required parameter "publishReq" was null or undefined when calling postMarketplaceListings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketplace/listings`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PublishReqToJSON(requestParameters['publishReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListingFromJSON(jsonValue));
    }

    /**
     * Publish offers one tool on the marketplace, optionally monetized. The tool must already resolve in the publisher\'s own scope, so a listing can never advertise a capability that does not exist; a listing with a price must name the payout wallet the x402 seam settles to, so a monetized offer is never unpayable. The price is exact to 18 decimal places, so a per-call price below a cent is a real price and not a rounded-away zero. The listing is owned by the publishing org, paid into a wallet of that same org, and answers 201 with the created row.
     * Publish offers one tool on the marketplace, optionally monetized.
     */
    async postMarketplaceListings(requestParameters: MarketplaceApiPostMarketplaceListingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Listing> {
        const response = await this.postMarketplaceListingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Uninstall deactivates one tool for the caller\'s own org and project, so it stops being dispatchable there. It is the exact inverse of install and touches the same activation record; deactivating something that was never active is not an error. The listing itself is untouched — this withdraws the caller\'s use of a capability, not anyone\'s offer of it.
     * Uninstall deactivates one tool for the caller\'s own org and project, so it stops being dispatchable there.
     */
    async postMarketplaceUninstallRaw(requestParameters: MarketplaceApiPostMarketplaceUninstallRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InstallState>> {
        if (requestParameters['installReq'] == null) {
            throw new runtime.RequiredError(
                'installReq',
                'Required parameter "installReq" was null or undefined when calling postMarketplaceUninstall().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketplace/uninstall`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: InstallReqToJSON(requestParameters['installReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstallStateFromJSON(jsonValue));
    }

    /**
     * Uninstall deactivates one tool for the caller\'s own org and project, so it stops being dispatchable there. It is the exact inverse of install and touches the same activation record; deactivating something that was never active is not an error. The listing itself is untouched — this withdraws the caller\'s use of a capability, not anyone\'s offer of it.
     * Uninstall deactivates one tool for the caller\'s own org and project, so it stops being dispatchable there.
     */
    async postMarketplaceUninstall(requestParameters: MarketplaceApiPostMarketplaceUninstallRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InstallState> {
        const response = await this.postMarketplaceUninstallRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
