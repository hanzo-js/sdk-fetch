/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AddDomainReq,
  AppView,
  BuildBoard,
  CDApp,
  CdResp,
  CreateAppReq,
  Declaration,
  DeclaredResp,
  DeployLogs,
  DeployReq,
  DeploymentView,
  DomainView,
  DriftBoard,
  EnvironmentBoard,
  PipelineBoard,
  PreviewReq,
  PreviewView,
  ProjectView,
  PromoteReq,
  Push,
  Readiness,
  ReleaseBoard,
  RestartRef,
  Restarted,
  RollbackReq,
  RunReq,
  RunView,
  RunnerBuildReq,
  RunnerBuildResp,
  SetEnvReq,
  Verdict,
} from '../models/index.js';
import {
    AddDomainReqFromJSON,
    AddDomainReqToJSON,
    AppViewFromJSON,
    AppViewToJSON,
    BuildBoardFromJSON,
    BuildBoardToJSON,
    CDAppFromJSON,
    CDAppToJSON,
    CdRespFromJSON,
    CdRespToJSON,
    CreateAppReqFromJSON,
    CreateAppReqToJSON,
    DeclarationFromJSON,
    DeclarationToJSON,
    DeclaredRespFromJSON,
    DeclaredRespToJSON,
    DeployLogsFromJSON,
    DeployLogsToJSON,
    DeployReqFromJSON,
    DeployReqToJSON,
    DeploymentViewFromJSON,
    DeploymentViewToJSON,
    DomainViewFromJSON,
    DomainViewToJSON,
    DriftBoardFromJSON,
    DriftBoardToJSON,
    EnvironmentBoardFromJSON,
    EnvironmentBoardToJSON,
    PipelineBoardFromJSON,
    PipelineBoardToJSON,
    PreviewReqFromJSON,
    PreviewReqToJSON,
    PreviewViewFromJSON,
    PreviewViewToJSON,
    ProjectViewFromJSON,
    ProjectViewToJSON,
    PromoteReqFromJSON,
    PromoteReqToJSON,
    PushFromJSON,
    PushToJSON,
    ReadinessFromJSON,
    ReadinessToJSON,
    ReleaseBoardFromJSON,
    ReleaseBoardToJSON,
    RestartRefFromJSON,
    RestartRefToJSON,
    RestartedFromJSON,
    RestartedToJSON,
    RollbackReqFromJSON,
    RollbackReqToJSON,
    RunReqFromJSON,
    RunReqToJSON,
    RunViewFromJSON,
    RunViewToJSON,
    RunnerBuildReqFromJSON,
    RunnerBuildReqToJSON,
    RunnerBuildRespFromJSON,
    RunnerBuildRespToJSON,
    SetEnvReqFromJSON,
    SetEnvReqToJSON,
    VerdictFromJSON,
    VerdictToJSON,
} from '../models/index.js';

export interface PlatformApiDeletePlatformProjectsByProjectAppsByAppRequest {
    project: string;
    app: string;
}

export interface PlatformApiDeletePlatformProjectsByProjectAppsByAppDomainsByHostRequest {
    project: string;
    app: string;
    host: string;
}

export interface PlatformApiGetPlatformAppsRequest {
    org?: string;
}

export interface PlatformApiGetPlatformAppsByAppRequest {
    app: string;
    org?: string;
}

export interface PlatformApiGetPlatformAppsByAppCdRequest {
    app: string;
    org?: string;
}

export interface PlatformApiGetPlatformFleetRequest {
    env?: string;
    health?: string;
    org?: string;
    drift?: string;
}

export interface PlatformApiGetPlatformFleetByAppRequest {
    app: string;
    env?: string;
}

export interface PlatformApiGetPlatformProjectsByProjectRequest {
    project: string;
}

export interface PlatformApiGetPlatformProjectsByProjectAppsRequest {
    project: string;
}

export interface PlatformApiGetPlatformProjectsByProjectAppsByAppRequest {
    project: string;
    app: string;
}

export interface PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsRequest {
    project: string;
    app: string;
}

export interface PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsByIdRequest {
    project: string;
    app: string;
    id: string;
}

export interface PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRequest {
    project: string;
    app: string;
    id: string;
}

export interface PlatformApiGetPlatformProjectsByProjectAppsByAppDomainsRequest {
    project: string;
    app: string;
}

export interface PlatformApiPostPlatformFleetByAppDeployRequest {
    app: string;
    restartRef: RestartRef;
}

export interface PlatformApiPostPlatformHookRequest {
    push?: Push;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsRequest {
    project: string;
    createAppReq: CreateAppReq;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppDeployRequest {
    project: string;
    app: string;
    deployReq: DeployReq;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppDomainsRequest {
    project: string;
    app: string;
    addDomainReq: AddDomainReq;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRequest {
    project: string;
    app: string;
    host: string;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppPreviewRequest {
    project: string;
    app: string;
    previewReq: PreviewReq;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppPromoteRequest {
    project: string;
    app: string;
    promoteReq: PromoteReq;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppRollbackRequest {
    project: string;
    app: string;
    rollbackReq: RollbackReq;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppStartRequest {
    project: string;
    app: string;
}

export interface PlatformApiPostPlatformProjectsByProjectAppsByAppStopRequest {
    project: string;
    app: string;
}

export interface PlatformApiPostPlatformRunRequest {
    runReq: RunReq;
}

export interface PlatformApiPostPlatformRunnerRequest {
    runnerBuildReq: RunnerBuildReq;
}

export interface PlatformApiPutPlatformProjectsByProjectAppsByAppEnvRequest {
    project: string;
    app: string;
    setEnvReq: SetEnvReq;
}

/**
 * 
 */
export class PlatformApi extends runtime.BaseAPI {

    /**
     * Deletes an application and tears down what it runs.  It removes the application record and tears down what it owns in the org\'s tenant namespace — its operator Service CR and its KMSSecret — then answers 204. An app this org and project do not have is 404, never a silent success.  Teardown is best-effort by design: a cluster that refuses or is unreachable does not block the delete, so the record cannot be left orphaned behind a broken cluster; the failure is logged for operators and the orphan reaper reconciles it. Requires a validated principal; 403 without one.
     * Deletes an application and tears down what it runs.
     */
    async deletePlatformProjectsByProjectAppsByAppRaw(requestParameters: PlatformApiDeletePlatformProjectsByProjectAppsByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling deletePlatformProjectsByProjectAppsByApp().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling deletePlatformProjectsByProjectAppsByApp().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes an application and tears down what it runs.  It removes the application record and tears down what it owns in the org\'s tenant namespace — its operator Service CR and its KMSSecret — then answers 204. An app this org and project do not have is 404, never a silent success.  Teardown is best-effort by design: a cluster that refuses or is unreachable does not block the delete, so the record cannot be left orphaned behind a broken cluster; the failure is logged for operators and the orphan reaper reconciles it. Requires a validated principal; 403 without one.
     * Deletes an application and tears down what it runs.
     */
    async deletePlatformProjectsByProjectAppsByApp(requestParameters: PlatformApiDeletePlatformProjectsByProjectAppsByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePlatformProjectsByProjectAppsByAppRaw(requestParameters, initOverrides);
    }

    /**
     * Detaches a hostname and releases the claim.  It drops the host from the app\'s ingress and releases any custom claim on it, so the name becomes claimable again — by this org or any other. Answers 204.  The default host is permanent and cannot be removed: that is 400, not 404. A host that is neither attached nor claimed here is 404. Requires a validated principal; 403 without one.
     * Detaches a hostname and releases the claim.
     */
    async deletePlatformProjectsByProjectAppsByAppDomainsByHostRaw(requestParameters: PlatformApiDeletePlatformProjectsByProjectAppsByAppDomainsByHostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling deletePlatformProjectsByProjectAppsByAppDomainsByHost().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling deletePlatformProjectsByProjectAppsByAppDomainsByHost().'
            );
        }

        if (requestParameters['host'] == null) {
            throw new runtime.RequiredError(
                'host',
                'Required parameter "host" was null or undefined when calling deletePlatformProjectsByProjectAppsByAppDomainsByHost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/domains/{host}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));
        urlPath = urlPath.replace(`{${"host"}}`, encodeURIComponent(String(requestParameters['host'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Detaches a hostname and releases the claim.  It drops the host from the app\'s ingress and releases any custom claim on it, so the name becomes claimable again — by this org or any other. Answers 204.  The default host is permanent and cannot be removed: that is 400, not 404. A host that is neither attached nor claimed here is 404. Requires a validated principal; 403 without one.
     * Detaches a hostname and releases the claim.
     */
    async deletePlatformProjectsByProjectAppsByAppDomainsByHost(requestParameters: PlatformApiDeletePlatformProjectsByProjectAppsByAppDomainsByHostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePlatformProjectsByProjectAppsByAppDomainsByHostRaw(requestParameters, initOverrides);
    }

    /**
     * Answers what this organisation has declared, joined with what the delivery plane has done about it.  The join is best-effort BY DESIGN and says so when it is missing: the declarations ARE the answer to \"what have I deployed\", so refusing the whole board because the cluster is unreadable would lose the half that is readable. What must never happen is a silent null — an unreadable plane is reported as `cd.unavailable` carrying the reason, never as an app with no reconciliation.
     * Answers what this organisation has declared, joined with what the delivery plane has done about it.
     */
    async getPlatformAppsRaw(requestParameters: PlatformApiGetPlatformAppsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeclaredResp>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/apps`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeclaredRespFromJSON(jsonValue));
    }

    /**
     * Answers what this organisation has declared, joined with what the delivery plane has done about it.  The join is best-effort BY DESIGN and says so when it is missing: the declarations ARE the answer to \"what have I deployed\", so refusing the whole board because the cluster is unreadable would lose the half that is readable. What must never happen is a silent null — an unreadable plane is reported as `cd.unavailable` carrying the reason, never as an app with no reconciliation.
     * Answers what this organisation has declared, joined with what the delivery plane has done about it.
     */
    async getPlatformApps(requestParameters: PlatformApiGetPlatformAppsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeclaredResp> {
        const response = await this.getPlatformAppsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers ONE declaration — what git says this app is, before the delivery plane has had any say in it.
     * Answers ONE declaration — what git says this app is, before the delivery plane has had any say in it.
     */
    async getPlatformAppsByAppRaw(requestParameters: PlatformApiGetPlatformAppsByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Declaration>> {
        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformAppsByApp().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/apps/{app}`;
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeclarationFromJSON(jsonValue));
    }

    /**
     * Answers ONE declaration — what git says this app is, before the delivery plane has had any say in it.
     * Answers ONE declaration — what git says this app is, before the delivery plane has had any say in it.
     */
    async getPlatformAppsByApp(requestParameters: PlatformApiGetPlatformAppsByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Declaration> {
        const response = await this.getPlatformAppsByAppRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers ONE app\'s reconciliation alone — the poll a deploy console makes while it waits, without re-reading the whole inventory each time.
     * Answers ONE app\'s reconciliation alone — the poll a deploy console makes while it waits, without re-reading the whole inventory each time.
     */
    async getPlatformAppsByAppCdRaw(requestParameters: PlatformApiGetPlatformAppsByAppCdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CDApp>> {
        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformAppsByAppCd().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/apps/{app}/cd`;
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CDAppFromJSON(jsonValue));
    }

    /**
     * Answers ONE app\'s reconciliation alone — the poll a deploy console makes while it waits, without re-reading the whole inventory each time.
     * Answers ONE app\'s reconciliation alone — the poll a deploy console makes while it waits, without re-reading the whole inventory each time.
     */
    async getPlatformAppsByAppCd(requestParameters: PlatformApiGetPlatformAppsByAppCdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CDApp> {
        const response = await this.getPlatformAppsByAppCdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns real build records for your org.  It lists the org\'s BuildKit build records — the git build step behind a deploy — each with the repo it built, the short commit, its status, when it started and how long it took. These are real records or an honest empty list; a build appears here because one ran, never because a page needed a row. Builds are created only by /deploy and the push-to-deploy hook. Requires a validated principal; 403 without one.
     * Returns real build records for your org.
     */
    async getPlatformBuildsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BuildBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/builds`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BuildBoardFromJSON(jsonValue));
    }

    /**
     * Returns real build records for your org.  It lists the org\'s BuildKit build records — the git build step behind a deploy — each with the repo it built, the short commit, its status, when it started and how long it took. These are real records or an honest empty list; a build appears here because one ran, never because a page needed a row. Builds are created only by /deploy and the push-to-deploy hook. Requires a validated principal; 403 without one.
     * Returns real build records for your org.
     */
    async getPlatformBuilds(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BuildBoard> {
        const response = await this.getPlatformBuildsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Answers every Application the delivery plane holds.  Scoped to the namespaces the caller\'s own validated org owns: the ROLE admits the caller and the tenant boundary is applied inside, so an admin of one org never observes another\'s.
     * Answers every Application the delivery plane holds.
     */
    async getPlatformCdRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CdResp>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/cd`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CdRespFromJSON(jsonValue));
    }

    /**
     * Answers every Application the delivery plane holds.  Scoped to the namespaces the caller\'s own validated org owns: the ROLE admits the caller and the tenant boundary is applied inside, so an admin of one org never observes another\'s.
     * Answers every Application the delivery plane holds.
     */
    async getPlatformCd(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CdResp> {
        const response = await this.getPlatformCdRaw(initOverrides);
        return await response.value();
    }

    /**
     * Answers 501. The forge\'s Actions runs need a Forgejo API client and this deployment has none; an empty run list would be indistinguishable from a forge with no runs.
     * Continuous integration (not wired)
     */
    async getPlatformCiRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/ci`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers 501. The forge\'s Actions runs need a Forgejo API client and this deployment has none; an empty run list would be indistinguishable from a forge with no runs.
     * Continuous integration (not wired)
     */
    async getPlatformCi(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getPlatformCiRaw(initOverrides);
    }

    /**
     * Returns your deploy targets, and what is running on each.  It returns the org\'s environments — the distinct deploy targets its applications name, `production` for anything that names none — each aggregating the apps that target it, a rolled-up status and when it last changed.  An environment is DERIVED, not stored: there is nothing to create or delete here, and an environment exists exactly as long as an app points at it. Requires a validated principal; 403 without one.
     * Returns your deploy targets, and what is running on each.
     */
    async getPlatformEnvironmentsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EnvironmentBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/environments`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EnvironmentBoardFromJSON(jsonValue));
    }

    /**
     * Returns your deploy targets, and what is running on each.  It returns the org\'s environments — the distinct deploy targets its applications name, `production` for anything that names none — each aggregating the apps that target it, a rolled-up status and when it last changed.  An environment is DERIVED, not stored: there is nothing to create or delete here, and an environment exists exactly as long as an app points at it. Requires a validated principal; 403 without one.
     * Returns your deploy targets, and what is running on each.
     */
    async getPlatformEnvironments(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EnvironmentBoard> {
        const response = await this.getPlatformEnvironmentsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the platform\'s own service tier, and where it has drifted.  It returns the board for the services the PLATFORM itself runs — iam, kms, gateway and the rest — as `{apps, summary}`: per service its environment, health, phase, the image tag its CR DECLARES, the tag actually running, and the drift between them, plus a summary counting the board green, yellow and red.  This is not a customer surface. `/v1/platform/projects/:project/apps` is a tenant\'s apps; this is the tier those tenants run ON, which is why the two are named differently rather than sharing a prefix.  Admission is scoped at the SCAN, before any CR is read: a platform SuperAdmin observes the whole fleet, an org admin observes only their own org\'s namespaces, and an org that owns none gets an empty board — a non-super caller never even lists another org\'s services. Narrow further with `env`, `health`, `org`, or `drift=1` for only what has drifted.  It degrades honestly rather than failing whole: a namespace that does not exist is skipped, and a running-state read the caller cannot make leaves the running tag empty — an unknown, never a guess — while the declared, health and phase columns still render.
     * Returns the platform\'s own service tier, and where it has drifted.
     */
    async getPlatformFleetRaw(requestParameters: PlatformApiGetPlatformFleetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriftBoard>> {
        const queryParameters: any = {};

        if (requestParameters['env'] != null) {
            queryParameters['env'] = requestParameters['env'];
        }

        if (requestParameters['health'] != null) {
            queryParameters['health'] = requestParameters['health'];
        }

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['drift'] != null) {
            queryParameters['drift'] = requestParameters['drift'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/fleet`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriftBoardFromJSON(jsonValue));
    }

    /**
     * Returns the platform\'s own service tier, and where it has drifted.  It returns the board for the services the PLATFORM itself runs — iam, kms, gateway and the rest — as `{apps, summary}`: per service its environment, health, phase, the image tag its CR DECLARES, the tag actually running, and the drift between them, plus a summary counting the board green, yellow and red.  This is not a customer surface. `/v1/platform/projects/:project/apps` is a tenant\'s apps; this is the tier those tenants run ON, which is why the two are named differently rather than sharing a prefix.  Admission is scoped at the SCAN, before any CR is read: a platform SuperAdmin observes the whole fleet, an org admin observes only their own org\'s namespaces, and an org that owns none gets an empty board — a non-super caller never even lists another org\'s services. Narrow further with `env`, `health`, `org`, or `drift=1` for only what has drifted.  It degrades honestly rather than failing whole: a namespace that does not exist is skipped, and a running-state read the caller cannot make leaves the running tag empty — an unknown, never a guess — while the declared, health and phase columns still render.
     * Returns the platform\'s own service tier, and where it has drifted.
     */
    async getPlatformFleet(requestParameters: PlatformApiGetPlatformFleetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriftBoard> {
        const response = await this.getPlatformFleetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one platform service, resolved to production by default.  It returns a single platform service by its CR name, with the same declared-versus-running and drift facts the board carries. The name must be a DNS-1123 label; anything else is 400.  Namespaces are scanned in lifecycle order — main, then test, then dev — and the first match wins, so a bare name resolves to PRODUCTION. The scan covers only the namespaces the caller is authorized for, so an org admin can never read a service outside their own org, and a name found in none of them is 404 rather than a leak.
     * Returns one platform service, resolved to production by default.
     */
    async getPlatformFleetByAppRaw(requestParameters: PlatformApiGetPlatformFleetByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AppView>> {
        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformFleetByApp().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['env'] != null) {
            queryParameters['env'] = requestParameters['env'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/fleet/{app}`;
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppViewFromJSON(jsonValue));
    }

    /**
     * Returns one platform service, resolved to production by default.  It returns a single platform service by its CR name, with the same declared-versus-running and drift facts the board carries. The name must be a DNS-1123 label; anything else is 400.  Namespaces are scanned in lifecycle order — main, then test, then dev — and the first match wins, so a bare name resolves to PRODUCTION. The scan covers only the namespaces the caller is authorized for, so an org admin can never read a service outside their own org, and a name found in none of them is 404 rather than a leak.
     * Returns one platform service, resolved to production by default.
     */
    async getPlatformFleetByApp(requestParameters: PlatformApiGetPlatformFleetByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AppView> {
        const response = await this.getPlatformFleetByAppRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports whether this control plane can actually deploy anything.  A real probe, not a status page. It answers 200 only when the metadata store is open AND the cluster is genuinely reachable — proved by LISTING the operator App CRD, which settles reachability and CRD presence in one bounded call, and which is the exact question every deploy depends on. Anything else is 503 carrying the real reason and whether the CRD was found.  A constructed cluster client proves nothing — it is built from a kubeconfig, not from a reachable apiserver — so this deliberately spends a round trip rather than reporting `ok` while every deploy fails. Not admin-gated: liveness has to be probe-able without a credential.
     * Reports whether this control plane can actually deploy anything.
     */
    async getPlatformHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Readiness>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReadinessFromJSON(jsonValue));
    }

    /**
     * Reports whether this control plane can actually deploy anything.  A real probe, not a status page. It answers 200 only when the metadata store is open AND the cluster is genuinely reachable — proved by LISTING the operator App CRD, which settles reachability and CRD presence in one bounded call, and which is the exact question every deploy depends on. Anything else is 503 carrying the real reason and whether the CRD was found.  A constructed cluster client proves nothing — it is built from a kubeconfig, not from a reachable apiserver — so this deliberately spends a round trip rather than reporting `ok` while every deploy fails. Not admin-gated: liveness has to be probe-able without a credential.
     * Reports whether this control plane can actually deploy anything.
     */
    async getPlatformHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Readiness> {
        const response = await this.getPlatformHealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one build-and-deploy pipeline per app, with its latest run.  It returns one pipeline per application in the caller\'s org — its repo or image source, its current status, and when its most recent deployment ran and how long it took. A pipeline is a PROJECTION of an app plus its newest deployment, not a separate record: it comes into existence with the app and is triggered only through /deploy, never here. Requires a validated principal; 403 without one.
     * Returns one build-and-deploy pipeline per app, with its latest run.
     */
    async getPlatformPipelinesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PipelineBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/pipelines`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PipelineBoardFromJSON(jsonValue));
    }

    /**
     * Returns one build-and-deploy pipeline per app, with its latest run.  It returns one pipeline per application in the caller\'s org — its repo or image source, its current status, and when its most recent deployment ran and how long it took. A pipeline is a PROJECTION of an app plus its newest deployment, not a separate record: it comes into existence with the app and is triggered only through /deploy, never here. Requires a validated principal; 403 without one.
     * Returns one build-and-deploy pipeline per app, with its latest run.
     */
    async getPlatformPipelines(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PipelineBoard> {
        const response = await this.getPlatformPipelinesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns your org\'s projects, each with how many apps live under it.  It lists the caller org\'s projects with the number of platform applications in each. A project is IAM\'s resource — it is created and deleted at /v1/iam/projects, never here — so this is the ONE projection IAM cannot serve: the project plus what the platform has put under it.  Requires a validated principal; 403 without one, and the org comes from that validated identity rather than a request header. This is the console\'s first authenticated read, so a project store that is not yet initialised degrades to an EMPTY list rather than a 500 — a new org genuinely has zero projects — and the real cause is surfaced to operators instead of to the caller.
     * Returns your org\'s projects, each with how many apps live under it.
     */
    async getPlatformProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProjectView>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProjectViewFromJSON));
    }

    /**
     * Returns your org\'s projects, each with how many apps live under it.  It lists the caller org\'s projects with the number of platform applications in each. A project is IAM\'s resource — it is created and deleted at /v1/iam/projects, never here — so this is the ONE projection IAM cannot serve: the project plus what the platform has put under it.  Requires a validated principal; 403 without one, and the org comes from that validated identity rather than a request header. This is the console\'s first authenticated read, so a project store that is not yet initialised degrades to an EMPTY list rather than a 500 — a new org genuinely has zero projects — and the real cause is surfaced to operators instead of to the caller.
     * Returns your org\'s projects, each with how many apps live under it.
     */
    async getPlatformProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProjectView>> {
        const response = await this.getPlatformProjectsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one project and its app count.  It returns a single project of the caller\'s org with the number of platform applications under it. A project this org does not have is 404, which is also what another tenant\'s project looks like from here. Requires a validated principal; 403 without one.
     * Returns one project and its app count.
     */
    async getPlatformProjectsByProjectRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProject().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectViewFromJSON(jsonValue));
    }

    /**
     * Returns one project and its app count.  It returns a single project of the caller\'s org with the number of platform applications under it. A project this org does not have is 404, which is also what another tenant\'s project looks like from here. Requires a validated principal; 403 without one.
     * Returns one project and its app count.
     */
    async getPlatformProjectsByProject(requestParameters: PlatformApiGetPlatformProjectsByProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectView> {
        const response = await this.getPlatformProjectsByProjectRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the applications in one project, with what the cluster says about them.  It lists the caller org\'s applications under one project. Each row carries the stored record and, for an app that is live or deploying, the LIVE phase and health read from its operator Service CR; an app with sealed env also carries its secret-sync state. Those cluster reads are best-effort — an unreachable cluster leaves those fields empty and never blocks the listing.  The project must exist in IAM for this org, or the answer is 404; the `default` project is implicit and always accepted, because it is part of what an org IS. Requires a validated principal; 403 without one.
     * Returns the applications in one project, with what the cluster says about them.
     */
    async getPlatformProjectsByProjectAppsRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<AppView>>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProjectApps().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(AppViewFromJSON));
    }

    /**
     * Returns the applications in one project, with what the cluster says about them.  It lists the caller org\'s applications under one project. Each row carries the stored record and, for an app that is live or deploying, the LIVE phase and health read from its operator Service CR; an app with sealed env also carries its secret-sync state. Those cluster reads are best-effort — an unreachable cluster leaves those fields empty and never blocks the listing.  The project must exist in IAM for this org, or the answer is 404; the `default` project is implicit and always accepted, because it is part of what an org IS. Requires a validated principal; 403 without one.
     * Returns the applications in one project, with what the cluster says about them.
     */
    async getPlatformProjectsByProjectApps(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<AppView>> {
        const response = await this.getPlatformProjectsByProjectAppsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one application, with its live phase, health and secret sync.  It returns a single application of the caller\'s org together with what the cluster currently reports for it: the operator Service CR\'s phase and health, and whether its sealed env has synced. An app this org and project do not have is 404. Requires a validated principal; 403 without one.
     * Returns one application, with its live phase, health and secret sync.
     */
    async getPlatformProjectsByProjectAppsByAppRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AppView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProjectAppsByApp().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformProjectsByProjectAppsByApp().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppViewFromJSON(jsonValue));
    }

    /**
     * Returns one application, with its live phase, health and secret sync.  It returns a single application of the caller\'s org together with what the cluster currently reports for it: the operator Service CR\'s phase and health, and whether its sealed env has synced. An app this org and project do not have is 404. Requires a validated principal; 403 without one.
     * Returns one application, with its live phase, health and secret sync.
     */
    async getPlatformProjectsByProjectAppsByApp(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AppView> {
        const response = await this.getPlatformProjectsByProjectAppsByAppRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns an app\'s deployment history.  It lists every deployment recorded for one of the caller org\'s applications, newest version first, each with its version, status, source, commit and image. Failed and superseded attempts are included — that is the point of a history. Requires a validated principal; 403 without one.
     * Returns an app\'s deployment history.
     */
    async getPlatformProjectsByProjectAppsByAppDeploymentsRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DeploymentView>>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeployments().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeployments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/deployments`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DeploymentViewFromJSON));
    }

    /**
     * Returns an app\'s deployment history.  It lists every deployment recorded for one of the caller org\'s applications, newest version first, each with its version, status, source, commit and image. Failed and superseded attempts are included — that is the point of a history. Requires a validated principal; 403 without one.
     * Returns an app\'s deployment history.
     */
    async getPlatformProjectsByProjectAppsByAppDeployments(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DeploymentView>> {
        const response = await this.getPlatformProjectsByProjectAppsByAppDeploymentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one deployment of one app.  It returns a single deployment by id, scoped to the named application of the caller\'s org — so an id belonging to another app or another tenant is 404, not a read. Requires a validated principal; 403 without one.
     * Returns one deployment of one app.
     */
    async getPlatformProjectsByProjectAppsByAppDeploymentsByIdRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeploymentView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeploymentsById().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeploymentsById().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeploymentsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/deployments/{id}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeploymentViewFromJSON(jsonValue));
    }

    /**
     * Returns one deployment of one app.  It returns a single deployment by id, scoped to the named application of the caller\'s org — so an id belonging to another app or another tenant is 404, not a read. Requires a validated principal; 403 without one.
     * Returns one deployment of one app.
     */
    async getPlatformProjectsByProjectAppsByAppDeploymentsById(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeploymentView> {
        const response = await this.getPlatformProjectsByProjectAppsByAppDeploymentsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns real logs for a deployment — the build\'s, then the app\'s.  It returns the deployment\'s recorded status timeline together with LIVE pod logs pulled from the cluster: the build pod\'s output while a git build is running, and the running app\'s output once it is deployed. The `source` field says which of the two the body is — `build`, `app` or `none` — so a console can label the pane honestly.  It never fabricates log content. When no pod exists yet, or the cluster is unreachable, it degrades to the recorded timeline and says so. Every cluster read is confined to the caller org\'s own namespaces and time-boxed. Requires a validated principal; 403 without one.
     * Returns real logs for a deployment — the build\'s, then the app\'s.
     */
    async getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeployLogs>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogs().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogs().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/deployments/{id}/logs`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeployLogsFromJSON(jsonValue));
    }

    /**
     * Returns real logs for a deployment — the build\'s, then the app\'s.  It returns the deployment\'s recorded status timeline together with LIVE pod logs pulled from the cluster: the build pod\'s output while a git build is running, and the running app\'s output once it is deployed. The `source` field says which of the two the body is — `build`, `app` or `none` — so a console can label the pane honestly.  It never fabricates log content. When no pod exists yet, or the cluster is unreachable, it degrades to the recorded timeline and says so. Every cluster read is confined to the caller org\'s own namespaces and time-boxed. Requires a validated principal; 403 without one.
     * Returns real logs for a deployment — the build\'s, then the app\'s.
     */
    async getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogs(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeployLogs> {
        const response = await this.getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every hostname this app answers on.  It lists the app\'s hosts: the permanent default host it was born with, any org-subtree hosts attached to it, and every custom host claimed for it with its verification state and, while pending, the DNS challenge records to publish. Live endpoint status for each host is observed from the cluster. Requires a validated principal; 403 without one.
     * Returns every hostname this app answers on.
     */
    async getPlatformProjectsByProjectAppsByAppDomainsRaw(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDomainsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DomainView>>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDomains().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling getPlatformProjectsByProjectAppsByAppDomains().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/domains`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DomainViewFromJSON));
    }

    /**
     * Returns every hostname this app answers on.  It lists the app\'s hosts: the permanent default host it was born with, any org-subtree hosts attached to it, and every custom host claimed for it with its verification state and, while pending, the DNS challenge records to publish. Live endpoint status for each host is observed from the cluster. Requires a validated principal; 403 without one.
     * Returns every hostname this app answers on.
     */
    async getPlatformProjectsByProjectAppsByAppDomains(requestParameters: PlatformApiGetPlatformProjectsByProjectAppsByAppDomainsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DomainView>> {
        const response = await this.getPlatformProjectsByProjectAppsByAppDomainsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the versions that actually reached the cluster.  It lists the org\'s releases: the deployments that were genuinely applied to the cluster, with the app they belong to, their version, environment, status and when they were released. A deployment that failed or is still building is NOT a release and is excluded — reaching the cluster is what makes one. Requires a validated principal; 403 without one.
     * Returns the versions that actually reached the cluster.
     */
    async getPlatformReleasesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReleaseBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/releases`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReleaseBoardFromJSON(jsonValue));
    }

    /**
     * Returns the versions that actually reached the cluster.  It lists the org\'s releases: the deployments that were genuinely applied to the cluster, with the app they belong to, their version, environment, status and when they were released. A deployment that failed or is still building is NOT a release and is excluded — reaching the cluster is what makes one. Requires a validated principal; 403 without one.
     * Returns the versions that actually reached the cluster.
     */
    async getPlatformReleases(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReleaseBoard> {
        const response = await this.getPlatformReleasesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Builds a git repository into an image and writes the declaration that names it — a values file in `hanzoai/universe` under `charts/app/values/<namespace>/<name>.yaml`, which the `fleet` ApplicationSet renders as one Application. That file IS the deployment: nothing else has to be applied.  `mode` decides whether anything can go live. The default, `branch`, pushes to `deploy/<namespace>/<name>/<tag>` and returns a review URL; the generator reads main, so a branch declaration deploys NOTHING and merging the review is the deliberate act. `commit` writes main, and proves the image is pullable first — a declaration naming an image the registry cannot serve is an ImagePullBackOff with no rollback path.  Omit `tag` to build; give it to declare an image an earlier call already built, which is how a green build is released without rebuilding it.  An org is its name: the values DIRECTORY, the destination NAMESPACE and the AppProject FENCE are all `<org>`, and the image is `<registry>/<org>/<app>`. None of them is a request field — the directory decides what CD admits the sync under and the repository decides what the cluster pulls, so a caller who could name either could reach outside its own org.  `org` is an ACT-AS, not a placement field: it defaults to the caller\'s own, and naming another requires SuperAdmin. So does naming a RESERVED org — the platform\'s own namespace family (the brands and their environments, the control and delivery planes, `admin`) — even when it is the caller\'s own, because an IAM org named `kube-system` does not own Kubernetes. Both refuse rather than downgrade, so an escape attempt is never indistinguishable from a normal request.  A host outside the caller\'s org subtree is refused: claim and verify a custom domain first.
     * Deploy an app through cd.hanzo.ai
     */
    async postPlatformAppsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/apps`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Builds a git repository into an image and writes the declaration that names it — a values file in `hanzoai/universe` under `charts/app/values/<namespace>/<name>.yaml`, which the `fleet` ApplicationSet renders as one Application. That file IS the deployment: nothing else has to be applied.  `mode` decides whether anything can go live. The default, `branch`, pushes to `deploy/<namespace>/<name>/<tag>` and returns a review URL; the generator reads main, so a branch declaration deploys NOTHING and merging the review is the deliberate act. `commit` writes main, and proves the image is pullable first — a declaration naming an image the registry cannot serve is an ImagePullBackOff with no rollback path.  Omit `tag` to build; give it to declare an image an earlier call already built, which is how a green build is released without rebuilding it.  An org is its name: the values DIRECTORY, the destination NAMESPACE and the AppProject FENCE are all `<org>`, and the image is `<registry>/<org>/<app>`. None of them is a request field — the directory decides what CD admits the sync under and the repository decides what the cluster pulls, so a caller who could name either could reach outside its own org.  `org` is an ACT-AS, not a placement field: it defaults to the caller\'s own, and naming another requires SuperAdmin. So does naming a RESERVED org — the platform\'s own namespace family (the brands and their environments, the control and delivery planes, `admin`) — even when it is the caller\'s own, because an IAM org named `kube-system` does not own Kubernetes. Both refuse rather than downgrade, so an escape attempt is never indistinguishable from a normal request.  A host outside the caller\'s org subtree is refused: claim and verify a custom domain first.
     * Deploy an app through cd.hanzo.ai
     */
    async postPlatformApps(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postPlatformAppsRaw(initOverrides);
    }

    /**
     * Rolls a platform service\'s pods, in a named environment.  It triggers a rolling restart of one platform service\'s Deployment by stamping a fresh restart annotation, and answers 202 with the app, the namespace, the environment and the timestamp. It restarts pods; it does NOT change the image — a version change is the release path, not this.  SuperAdmin ONLY, and deliberately narrower than the read gate beside it. The only namespaces this board touches are the platform\'s own tier, so a restart here recycles a SHARED service every tenant depends on. A brand-org admin is a customer-org admin, not a platform operator: observing the board is bounded and audited, and restarting production identity is not.  `?env=main|test|dev` is REQUIRED — a bare call does not default to production, which is what closes the fat-finger and confused-deputy hazard — and any other value is 400. A service with no Deployment to restart in that environment is 404.
     * Rolls a platform service\'s pods, in a named environment.
     */
    async postPlatformFleetByAppDeployRaw(requestParameters: PlatformApiPostPlatformFleetByAppDeployRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Restarted>> {
        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformFleetByAppDeploy().'
            );
        }

        if (requestParameters['restartRef'] == null) {
            throw new runtime.RequiredError(
                'restartRef',
                'Required parameter "restartRef" was null or undefined when calling postPlatformFleetByAppDeploy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/fleet/{app}/deploy`;
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RestartRefToJSON(requestParameters['restartRef']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RestartedFromJSON(jsonValue));
    }

    /**
     * Rolls a platform service\'s pods, in a named environment.  It triggers a rolling restart of one platform service\'s Deployment by stamping a fresh restart annotation, and answers 202 with the app, the namespace, the environment and the timestamp. It restarts pods; it does NOT change the image — a version change is the release path, not this.  SuperAdmin ONLY, and deliberately narrower than the read gate beside it. The only namespaces this board touches are the platform\'s own tier, so a restart here recycles a SHARED service every tenant depends on. A brand-org admin is a customer-org admin, not a platform operator: observing the board is bounded and audited, and restarting production identity is not.  `?env=main|test|dev` is REQUIRED — a bare call does not default to production, which is what closes the fat-finger and confused-deputy hazard — and any other value is 400. A service with no Deployment to restart in that environment is 404.
     * Rolls a platform service\'s pods, in a named environment.
     */
    async postPlatformFleetByAppDeploy(requestParameters: PlatformApiPostPlatformFleetByAppDeployRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Restarted> {
        const response = await this.postPlatformFleetByAppDeployRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The forge\'s push-to-deploy endpoint. git.hanzo.ai runs as a separate server, so its pushes never reach this fleet\'s own receive-pack; without this a push to the host we call canonical builds nothing. A verified push is handed to the SAME two clients a native push travels — the single-registrant deploy trigger, and the many-subscriber lifecycle stream that notifies and indexes — and the build decision itself stays downstream in the one place that knows what a push means.  PUBLIC at the JWT layer, because the forge carries no Hanzo session: AUTHENTICATION IS THE SIGNATURE. The HMAC covers the raw bytes and is verified BEFORE the payload is parsed, so an unauthenticated body is never decoded. The secret is read from KMS; a deployment that cannot read it answers 503 and processes nothing, rather than trusting a delivery it could not check. The body is read UNCOMPRESSED — a request declaring a Content-Encoding is refused 415 before it is touched, because decoding one is unbounded work bought with a few bytes and no credential. A bad signature is 401, a payload over 8 MiB is 413, and a malformed one 400.  A verified push that reaches both clients answers 200 with fired true and the NUMBER OF BUILDS it launched — zero is ordinary, since most pushes track no application, and it is the answer \'fired\' cannot give. A push that could not be dispatched answers 500: the delivery page shows it red, and the Replay that prompts reaches a fresh attempt rather than being declined as already landed.  The deliveries deliberately ignored answer 200 with a reason and nothing else: a payload that is not a push, a ref DELETE (a zero `after` has no commit to build), a BOT-authored push (release automation pushes as the forge\'s own Actions user, and a release must never rebuild itself), a push from a forge namespace that maps to no org, and a redelivery of a push already fired. Branches and tags both reach the build trigger, because releases are cut by tag and filtering here would silently stop publishing.
     * Receive a push from the forge and trigger its build
     */
    async postPlatformHookRaw(requestParameters: PlatformApiPostPlatformHookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Verdict>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/hook`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PushToJSON(requestParameters['push']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VerdictFromJSON(jsonValue));
    }

    /**
     * The forge\'s push-to-deploy endpoint. git.hanzo.ai runs as a separate server, so its pushes never reach this fleet\'s own receive-pack; without this a push to the host we call canonical builds nothing. A verified push is handed to the SAME two clients a native push travels — the single-registrant deploy trigger, and the many-subscriber lifecycle stream that notifies and indexes — and the build decision itself stays downstream in the one place that knows what a push means.  PUBLIC at the JWT layer, because the forge carries no Hanzo session: AUTHENTICATION IS THE SIGNATURE. The HMAC covers the raw bytes and is verified BEFORE the payload is parsed, so an unauthenticated body is never decoded. The secret is read from KMS; a deployment that cannot read it answers 503 and processes nothing, rather than trusting a delivery it could not check. The body is read UNCOMPRESSED — a request declaring a Content-Encoding is refused 415 before it is touched, because decoding one is unbounded work bought with a few bytes and no credential. A bad signature is 401, a payload over 8 MiB is 413, and a malformed one 400.  A verified push that reaches both clients answers 200 with fired true and the NUMBER OF BUILDS it launched — zero is ordinary, since most pushes track no application, and it is the answer \'fired\' cannot give. A push that could not be dispatched answers 500: the delivery page shows it red, and the Replay that prompts reaches a fresh attempt rather than being declined as already landed.  The deliveries deliberately ignored answer 200 with a reason and nothing else: a payload that is not a push, a ref DELETE (a zero `after` has no commit to build), a BOT-authored push (release automation pushes as the forge\'s own Actions user, and a release must never rebuild itself), a push from a forge namespace that maps to no org, and a redelivery of a push already fired. Branches and tags both reach the build trigger, because releases are cut by tag and filtering here would silently stop publishing.
     * Receive a push from the forge and trigger its build
     */
    async postPlatformHook(requestParameters: PlatformApiPostPlatformHookRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Verdict> {
        const response = await this.postPlatformHookRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an application from a git repo or a container image.  It registers a new application under one of the caller org\'s projects and answers 201 with it. Creating does NOT deploy: the app lands in `draft` and nothing reaches the cluster until /deploy.  `source` is `git` — which requires `repo.url` — or `image`, which requires `image.repository`; anything else is 400. A git app builds with zero-config `pack` by default and may opt into `dockerfile`; an image app never builds. The repo URL and Dockerfile path are validated here against the SAME allowlist the privileged build enforces, so an unsafe source is refused before it is ever persisted.  The `slug` is the app\'s identity in the cluster: given or derived from `name`, it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and a slug already used in this project is 409. `replicas` and `storageGb` are clamped to the deployment\'s limits rather than refused.  Env keys must match `^[A-Za-z_][A-Za-z0-9_]*$`. A variable marked `secret: true` is SEALED into KMS and its plaintext is never written to the database — and if KMS is unavailable the create fails 503 rather than falling back to storing a secret in the clear.  The app is seeded with its canonical default host, so it has a working HTTPS URL the moment it deploys. A bare custom domain cannot be attached here — it has to go through add-domain and DNS verification first. Requires a validated principal; 403 without one, and every cluster object it will later create lands in that org\'s own `tenant-<org>` namespace.
     * Creates an application from a git repo or a container image.
     */
    async postPlatformProjectsByProjectAppsRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AppView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectApps().'
            );
        }

        if (requestParameters['createAppReq'] == null) {
            throw new runtime.RequiredError(
                'createAppReq',
                'Required parameter "createAppReq" was null or undefined when calling postPlatformProjectsByProjectApps().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateAppReqToJSON(requestParameters['createAppReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppViewFromJSON(jsonValue));
    }

    /**
     * Creates an application from a git repo or a container image.  It registers a new application under one of the caller org\'s projects and answers 201 with it. Creating does NOT deploy: the app lands in `draft` and nothing reaches the cluster until /deploy.  `source` is `git` — which requires `repo.url` — or `image`, which requires `image.repository`; anything else is 400. A git app builds with zero-config `pack` by default and may opt into `dockerfile`; an image app never builds. The repo URL and Dockerfile path are validated here against the SAME allowlist the privileged build enforces, so an unsafe source is refused before it is ever persisted.  The `slug` is the app\'s identity in the cluster: given or derived from `name`, it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and a slug already used in this project is 409. `replicas` and `storageGb` are clamped to the deployment\'s limits rather than refused.  Env keys must match `^[A-Za-z_][A-Za-z0-9_]*$`. A variable marked `secret: true` is SEALED into KMS and its plaintext is never written to the database — and if KMS is unavailable the create fails 503 rather than falling back to storing a secret in the clear.  The app is seeded with its canonical default host, so it has a working HTTPS URL the moment it deploys. A bare custom domain cannot be attached here — it has to go through add-domain and DNS verification first. Requires a validated principal; 403 without one, and every cluster object it will later create lands in that org\'s own `tenant-<org>` namespace.
     * Creates an application from a git repo or a container image.
     */
    async postPlatformProjectsByProjectApps(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AppView> {
        const response = await this.postPlatformProjectsByProjectAppsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deploys the app — building it first if it comes from git.  It starts a new, monotonically versioned deployment of the app and answers 202 with the deployment record. A 202 is an ACCEPTED deployment, not a live one.  An IMAGE app deploys the tag you name (falling back to the app\'s tag, then `latest`) by writing its operator Service CR; the operator reconciles it to running. A GIT app launches an in-cluster BuildKit Job at `commit` — or the app\'s branch — and comes back in `building`; the Service CR is applied later, by the reconciler, once the Job succeeds. The reconciler is restart-safe, so a build in flight survives a cloud restart.  Deploys are bounded per org: over the concurrent-deploy cap is 429 and NOTHING is recorded, so a rejected deploy leaves no phantom in the history. An unreachable cluster is 503 but still records an honest `error` deployment, because a deploy that was attempted and failed must not be indistinguishable from one never made. Every other failure is likewise recorded in its real terminal state.  This is metered work: a git build is billed to the org\'s ledger in wall-clock build minutes once the Job finishes, and the running deployment is billed for its compute per tick for as long as it stays live. Requires a validated principal; 403 without one, and everything is written into that org\'s own `tenant-<org>` namespace.
     * Deploys the app — building it first if it comes from git.
     */
    async postPlatformProjectsByProjectAppsByAppDeployRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppDeployRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeploymentView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDeploy().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDeploy().'
            );
        }

        if (requestParameters['deployReq'] == null) {
            throw new runtime.RequiredError(
                'deployReq',
                'Required parameter "deployReq" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDeploy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/deploy`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DeployReqToJSON(requestParameters['deployReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeploymentViewFromJSON(jsonValue));
    }

    /**
     * Deploys the app — building it first if it comes from git.  It starts a new, monotonically versioned deployment of the app and answers 202 with the deployment record. A 202 is an ACCEPTED deployment, not a live one.  An IMAGE app deploys the tag you name (falling back to the app\'s tag, then `latest`) by writing its operator Service CR; the operator reconciles it to running. A GIT app launches an in-cluster BuildKit Job at `commit` — or the app\'s branch — and comes back in `building`; the Service CR is applied later, by the reconciler, once the Job succeeds. The reconciler is restart-safe, so a build in flight survives a cloud restart.  Deploys are bounded per org: over the concurrent-deploy cap is 429 and NOTHING is recorded, so a rejected deploy leaves no phantom in the history. An unreachable cluster is 503 but still records an honest `error` deployment, because a deploy that was attempted and failed must not be indistinguishable from one never made. Every other failure is likewise recorded in its real terminal state.  This is metered work: a git build is billed to the org\'s ledger in wall-clock build minutes once the Job finishes, and the running deployment is billed for its compute per tick for as long as it stays live. Requires a validated principal; 403 without one, and everything is written into that org\'s own `tenant-<org>` namespace.
     * Deploys the app — building it first if it comes from git.
     */
    async postPlatformProjectsByProjectAppsByAppDeploy(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppDeployRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeploymentView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppDeployRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.  It attaches `host` to the app, and which of two things happens depends on who owns the name. A host inside the caller org\'s own subtree is structurally owned, so it goes ACTIVE immediately and answers 201. A bring-your-own host is claimed as PENDING and answers the DNS challenge records to publish; it is NOT rendered into the app\'s ingress until /verify passes.  Claims are globally unique. A host already claimed by another organization is 409, and so is one claimed by a different app in your own; re-adding this app\'s OWN claim is idempotent and answers its current state at 200. The default host is always attached and re-adding it is 409. A host under the platform\'s shared apex that is not the caller\'s own subtree is 403 — it belongs to whoever owns that subtree and can never be grabbed through the custom path.  `host` must be a valid DNS hostname; anything else is 400. Requires a validated principal; 403 without one.
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.
     */
    async postPlatformProjectsByProjectAppsByAppDomainsRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppDomainsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DomainView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDomains().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDomains().'
            );
        }

        if (requestParameters['addDomainReq'] == null) {
            throw new runtime.RequiredError(
                'addDomainReq',
                'Required parameter "addDomainReq" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDomains().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/domains`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AddDomainReqToJSON(requestParameters['addDomainReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DomainViewFromJSON(jsonValue));
    }

    /**
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.  It attaches `host` to the app, and which of two things happens depends on who owns the name. A host inside the caller org\'s own subtree is structurally owned, so it goes ACTIVE immediately and answers 201. A bring-your-own host is claimed as PENDING and answers the DNS challenge records to publish; it is NOT rendered into the app\'s ingress until /verify passes.  Claims are globally unique. A host already claimed by another organization is 409, and so is one claimed by a different app in your own; re-adding this app\'s OWN claim is idempotent and answers its current state at 200. The default host is always attached and re-adding it is 409. A host under the platform\'s shared apex that is not the caller\'s own subtree is 403 — it belongs to whoever owns that subtree and can never be grabbed through the custom path.  `host` must be a valid DNS hostname; anything else is 400. Requires a validated principal; 403 without one.
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.
     */
    async postPlatformProjectsByProjectAppsByAppDomains(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppDomainsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DomainView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppDomainsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Checks a custom domain\'s DNS and turns it on if it passes.  It runs the DNS challenge check for a pending custom host and, when it passes, marks the host verified and renders it into the app\'s ingress so it starts serving.  A check that RAN and did not pass is not an error: it answers 200 with the host still pending and the reason in `detail`, so a console can show the operator what DNS is actually returning. An already-verified host answers as-is without re-checking. A host not claimed by this app is 404. Requires a validated principal; 403 without one.
     * Checks a custom domain\'s DNS and turns it on if it passes.
     */
    async postPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DomainView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDomainsByHostVerify().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDomainsByHostVerify().'
            );
        }

        if (requestParameters['host'] == null) {
            throw new runtime.RequiredError(
                'host',
                'Required parameter "host" was null or undefined when calling postPlatformProjectsByProjectAppsByAppDomainsByHostVerify().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/domains/{host}/verify`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));
        urlPath = urlPath.replace(`{${"host"}}`, encodeURIComponent(String(requestParameters['host'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DomainViewFromJSON(jsonValue));
    }

    /**
     * Checks a custom domain\'s DNS and turns it on if it passes.  It runs the DNS challenge check for a pending custom host and, when it passes, marks the host verified and renders it into the app\'s ingress so it starts serving.  A check that RAN and did not pass is not an error: it answers 200 with the host still pending and the reason in `detail`, so a console can show the operator what DNS is actually returning. An already-verified host answers as-is without re-checking. A host not claimed by this app is 404. Requires a validated principal; 403 without one.
     * Checks a custom domain\'s DNS and turns it on if it passes.
     */
    async postPlatformProjectsByProjectAppsByAppDomainsByHostVerify(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DomainView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Puts a branch on its own URL.  It deploys an already-built `image` to a per-branch preview and answers its URL, the branch, the preview\'s slug and the deployment. The preview is a FIRST-CLASS application named `<app>-<branch>` in the same project and tenant namespace, with its own default host — so it is completely isolated from production while reusing the same deploy mechanic. Re-previewing a branch converges that same target in place rather than stacking another one.  It carries NO environment variables, deliberately: a preview never inherits production\'s secrets. It also does not build — `image` is required and must already exist, and `branch` defaults to the parent app\'s. A branch that does not resolve to a valid slug distinct from the parent\'s is 400. Requires a validated principal; 403 without one.
     * Puts a branch on its own URL.
     */
    async postPlatformProjectsByProjectAppsByAppPreviewRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PreviewView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppPreview().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppPreview().'
            );
        }

        if (requestParameters['previewReq'] == null) {
            throw new runtime.RequiredError(
                'previewReq',
                'Required parameter "previewReq" was null or undefined when calling postPlatformProjectsByProjectAppsByAppPreview().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/preview`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PreviewReqToJSON(requestParameters['previewReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PreviewViewFromJSON(jsonValue));
    }

    /**
     * Puts a branch on its own URL.  It deploys an already-built `image` to a per-branch preview and answers its URL, the branch, the preview\'s slug and the deployment. The preview is a FIRST-CLASS application named `<app>-<branch>` in the same project and tenant namespace, with its own default host — so it is completely isolated from production while reusing the same deploy mechanic. Re-previewing a branch converges that same target in place rather than stacking another one.  It carries NO environment variables, deliberately: a preview never inherits production\'s secrets. It also does not build — `image` is required and must already exist, and `branch` defaults to the parent app\'s. A branch that does not resolve to a valid slug distinct from the parent\'s is 400. Requires a validated principal; 403 without one.
     * Puts a branch on its own URL.
     */
    async postPlatformProjectsByProjectAppsByAppPreview(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PreviewView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppPreviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Promotes an already-built release to the app.  It redeploys an image that already exists — named either by `deploymentId`, which promotes that deployment\'s exact built image, or by `tag`, resolved the same way a deploy resolves one. One of the two is required; neither is 400.  Promotion never builds. A deployment that carries no built image cannot be promoted and is 400, and a deployment id outside this app is 404. It runs through the same deploy core as everything else, so it takes a NEW version number and is subject to the same per-org concurrency cap. Requires a validated principal; 403 without one.
     * Promotes an already-built release to the app.
     */
    async postPlatformProjectsByProjectAppsByAppPromoteRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppPromoteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeploymentView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppPromote().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppPromote().'
            );
        }

        if (requestParameters['promoteReq'] == null) {
            throw new runtime.RequiredError(
                'promoteReq',
                'Required parameter "promoteReq" was null or undefined when calling postPlatformProjectsByProjectAppsByAppPromote().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/promote`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PromoteReqToJSON(requestParameters['promoteReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeploymentViewFromJSON(jsonValue));
    }

    /**
     * Promotes an already-built release to the app.  It redeploys an image that already exists — named either by `deploymentId`, which promotes that deployment\'s exact built image, or by `tag`, resolved the same way a deploy resolves one. One of the two is required; neither is 400.  Promotion never builds. A deployment that carries no built image cannot be promoted and is 400, and a deployment id outside this app is 404. It runs through the same deploy core as everything else, so it takes a NEW version number and is subject to the same per-org concurrency cap. Requires a validated principal; 403 without one.
     * Promotes an already-built release to the app.
     */
    async postPlatformProjectsByProjectAppsByAppPromote(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppPromoteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeploymentView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppPromoteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Goes back to the previous release.  It redeploys a prior image: the one named by `deploymentId`, or — with no body — the newest earlier deployment that carries a real built image and did not error, skipping the release currently live. An app with nothing earlier to return to is 400.  A rollback is a deploy of an old image, not a rewind: it takes a NEW version number and appends to the history rather than erasing what came after. Both lookups are scoped to this app and org, so another tenant\'s image can never be rolled in. Requires a validated principal; 403 without one.
     * Goes back to the previous release.
     */
    async postPlatformProjectsByProjectAppsByAppRollbackRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppRollbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeploymentView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppRollback().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppRollback().'
            );
        }

        if (requestParameters['rollbackReq'] == null) {
            throw new runtime.RequiredError(
                'rollbackReq',
                'Required parameter "rollbackReq" was null or undefined when calling postPlatformProjectsByProjectAppsByAppRollback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/rollback`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RollbackReqToJSON(requestParameters['rollbackReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeploymentViewFromJSON(jsonValue));
    }

    /**
     * Goes back to the previous release.  It redeploys a prior image: the one named by `deploymentId`, or — with no body — the newest earlier deployment that carries a real built image and did not error, skipping the release currently live. An app with nothing earlier to return to is 400.  A rollback is a deploy of an old image, not a rewind: it takes a NEW version number and appends to the history rather than erasing what came after. Both lookups are scoped to this app and org, so another tenant\'s image can never be rolled in. Requires a validated principal; 403 without one.
     * Goes back to the previous release.
     */
    async postPlatformProjectsByProjectAppsByAppRollback(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppRollbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeploymentView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppRollbackRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Starts a stopped app back up.  It scales the app\'s Service back to its configured replica count and marks it live, answering the updated application. It does not redeploy: the image already on the Service CR is what comes back.  The billing watermark is reset to now as part of starting, so the org is charged for THIS live span and never for the gap the app spent stopped. An app with no Service CR is 404, an unreachable cluster is 503, and a cluster that refuses the scale is 502. Requires a validated principal; 403 without one.
     * Starts a stopped app back up.
     */
    async postPlatformProjectsByProjectAppsByAppStartRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppStartRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AppView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppStart().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppStart().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/start`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppViewFromJSON(jsonValue));
    }

    /**
     * Starts a stopped app back up.  It scales the app\'s Service back to its configured replica count and marks it live, answering the updated application. It does not redeploy: the image already on the Service CR is what comes back.  The billing watermark is reset to now as part of starting, so the org is charged for THIS live span and never for the gap the app spent stopped. An app with no Service CR is 404, an unreachable cluster is 503, and a cluster that refuses the scale is 502. Requires a validated principal; 403 without one.
     * Starts a stopped app back up.
     */
    async postPlatformProjectsByProjectAppsByAppStart(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppStartRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AppView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppStartRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stops an app without deleting it.  It scales the app\'s Service to zero replicas and marks it stopped, answering the updated application. Nothing else is removed — the record, its env, its domains and its deployment history all survive, and /start brings it back at the same replica count.  An app that is not deployed has no Service CR to scale and is 404. An unreachable cluster is 503 and a cluster that refuses the scale is 502. Because the pods stop, so does the compute metering. Requires a validated principal; 403 without one.
     * Stops an app without deleting it.
     */
    async postPlatformProjectsByProjectAppsByAppStopRaw(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppStopRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AppView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postPlatformProjectsByProjectAppsByAppStop().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling postPlatformProjectsByProjectAppsByAppStop().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/stop`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppViewFromJSON(jsonValue));
    }

    /**
     * Stops an app without deleting it.  It scales the app\'s Service to zero replicas and marks it stopped, answering the updated application. Nothing else is removed — the record, its env, its domains and its deployment history all survive, and /start brings it back at the same replica count.  An app that is not deployed has no Service CR to scale and is 404. An unreachable cluster is 503 and a cluster that refuses the scale is 502. Because the pods stop, so does the compute metering. Requires a validated principal; 403 without one.
     * Stops an app without deleting it.
     */
    async postPlatformProjectsByProjectAppsByAppStop(requestParameters: PlatformApiPostPlatformProjectsByProjectAppsByAppStopRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AppView> {
        const response = await this.postPlatformProjectsByProjectAppsByAppStopRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs a container image and gives back a URL.  The one-call shortcut over project → app → deploy: give it a `name` and an `image` and it creates or updates an image-source application in your org\'s DEFAULT project, deploys it through the same operator Service-CR writer everything else uses, and answers its id, name, live URL, status and shape. Re-running the same name UPDATES it in place, so the call is idempotent by name.  What it produces is a first-class application, not a special object: it is listable, stoppable and redeployable through the /v1/platform routes like any other app.  `minScale` is the replica floor. `maxScale` above it declares an autoscaling ceiling; `maxScale: 0` means no autoscaler at all — a fixed run at the floor. Both are clamped to the deployment\'s limits. `runtime` and `shape` are accepted for the client contract and echoed back: the image is the runtime unit and sizing is the operator\'s default.  It is BILLING-GATED before it touches the cluster: a flat per-run fee is authorized against the org\'s own prepaid balance first, so an org that cannot pay is refused without anything being created. An unreachable cluster is 503 — a run never reports a URL it did not create. Secret env is sealed into KMS and fails closed without it.  Requires a validated principal; 403 without one. The org is resolved from that validated identity and is what both pays and owns the namespace — it is never read from the body.
     * Runs a container image and gives back a URL.
     */
    async postPlatformRunRaw(requestParameters: PlatformApiPostPlatformRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RunView>> {
        if (requestParameters['runReq'] == null) {
            throw new runtime.RequiredError(
                'runReq',
                'Required parameter "runReq" was null or undefined when calling postPlatformRun().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/run`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RunReqToJSON(requestParameters['runReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RunViewFromJSON(jsonValue));
    }

    /**
     * Runs a container image and gives back a URL.  The one-call shortcut over project → app → deploy: give it a `name` and an `image` and it creates or updates an image-source application in your org\'s DEFAULT project, deploys it through the same operator Service-CR writer everything else uses, and answers its id, name, live URL, status and shape. Re-running the same name UPDATES it in place, so the call is idempotent by name.  What it produces is a first-class application, not a special object: it is listable, stoppable and redeployable through the /v1/platform routes like any other app.  `minScale` is the replica floor. `maxScale` above it declares an autoscaling ceiling; `maxScale: 0` means no autoscaler at all — a fixed run at the floor. Both are clamped to the deployment\'s limits. `runtime` and `shape` are accepted for the client contract and echoed back: the image is the runtime unit and sizing is the operator\'s default.  It is BILLING-GATED before it touches the cluster: a flat per-run fee is authorized against the org\'s own prepaid balance first, so an org that cannot pay is refused without anything being created. An unreachable cluster is 503 — a run never reports a URL it did not create. Secret env is sealed into KMS and fails closed without it.  Requires a validated principal; 403 without one. The org is resolved from that validated identity and is what both pays and owns the namespace — it is never read from the body.
     * Runs a container image and gives back a URL.
     */
    async postPlatformRun(requestParameters: PlatformApiPostPlatformRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RunView> {
        const response = await this.postPlatformRunRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Triggers a native build — an image, or the binaries a repo declares.  The fabric\'s own build trigger, and what `hanzo build` and git-push-to-deploy call. It answers 202 with the build job id: a queued build, not a pushed artifact.  Two lanes, and a build is exactly one of them. The IMAGE lane takes `repo` and the output `image` and launches a BuildKit Job that pushes it. The ARTIFACT lane takes `binaries` — the same recipe the repo\'s hanzo.yml declares — and publishes to object storage instead; it must carry no `image`, because a build produces binaries or an image, never both.  PRIVILEGED, and A BUILD BELONGS TO THE ORGANIZATION ITS CREDENTIAL NAMES. Two credentials, never a third:    - one that NAMES an organization — a person who administers it (the `hanzo     build` path, so one IAM login authorizes a build with no separate build     token), or that organization\'s own machine identity (the pipeline path). The     build is attributed to that org and confined to what it owns.   - the shared build-callback token, compared in constant time. It names NO     organization, which is both why the fabric\'s own release can publish across     brands with it and why anything that CAN name one is read first.  Both are bounded by the owned-registry allowlist. The org path is bounded again, by the org: the image\'s registry namespace must be one that organization owns, so it publishes into its own brand and can never overwrite another\'s through the shared push credential. The same confinement applies to the artifact lane\'s repo owner. There is no request field naming an organization — the attribution is read off the credential, so there is nothing for a caller to write it with.  The output image is parsed and validated as a single well-formed OCI ref before any authorization decision reads it, so a crafted ref cannot smuggle a build-exporter attribute past the check.
     * Triggers a native build — an image, or the binaries a repo declares.
     */
    async postPlatformRunnerRaw(requestParameters: PlatformApiPostPlatformRunnerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RunnerBuildResp>> {
        if (requestParameters['runnerBuildReq'] == null) {
            throw new runtime.RequiredError(
                'runnerBuildReq',
                'Required parameter "runnerBuildReq" was null or undefined when calling postPlatformRunner().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/runner`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RunnerBuildReqToJSON(requestParameters['runnerBuildReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RunnerBuildRespFromJSON(jsonValue));
    }

    /**
     * Triggers a native build — an image, or the binaries a repo declares.  The fabric\'s own build trigger, and what `hanzo build` and git-push-to-deploy call. It answers 202 with the build job id: a queued build, not a pushed artifact.  Two lanes, and a build is exactly one of them. The IMAGE lane takes `repo` and the output `image` and launches a BuildKit Job that pushes it. The ARTIFACT lane takes `binaries` — the same recipe the repo\'s hanzo.yml declares — and publishes to object storage instead; it must carry no `image`, because a build produces binaries or an image, never both.  PRIVILEGED, and A BUILD BELONGS TO THE ORGANIZATION ITS CREDENTIAL NAMES. Two credentials, never a third:    - one that NAMES an organization — a person who administers it (the `hanzo     build` path, so one IAM login authorizes a build with no separate build     token), or that organization\'s own machine identity (the pipeline path). The     build is attributed to that org and confined to what it owns.   - the shared build-callback token, compared in constant time. It names NO     organization, which is both why the fabric\'s own release can publish across     brands with it and why anything that CAN name one is read first.  Both are bounded by the owned-registry allowlist. The org path is bounded again, by the org: the image\'s registry namespace must be one that organization owns, so it publishes into its own brand and can never overwrite another\'s through the shared push credential. The same confinement applies to the artifact lane\'s repo owner. There is no request field naming an organization — the attribution is read off the credential, so there is nothing for a caller to write it with.  The output image is parsed and validated as a single well-formed OCI ref before any authorization decision reads it, so a crafted ref cannot smuggle a build-exporter attribute past the check.
     * Triggers a native build — an image, or the binaries a repo declares.
     */
    async postPlatformRunner(requestParameters: PlatformApiPostPlatformRunnerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RunnerBuildResp> {
        const response = await this.postPlatformRunnerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces an app\'s environment variables.  It writes the app\'s whole environment set and answers the updated application. This is the one post-create write path for env, and it REPLACES rather than merges: a variable absent from the body is gone, and a secret dropped from the set leaves the app\'s Secret on its next deploy.  Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`. A value marked `secret: true` is sealed into KMS and blanked in the database, so plaintext is never persisted — and the write fails 503 if KMS is unavailable rather than storing one in the clear.  The rule worth knowing: this does not restart anything. Once the app has been deployed the secret sync is re-declared immediately so the operator re-materialises the Secret, but RUNNING pods keep the environment they started with until their next deploy or restart. Requires a validated principal; 403 without one.
     * Replaces an app\'s environment variables.
     */
    async putPlatformProjectsByProjectAppsByAppEnvRaw(requestParameters: PlatformApiPutPlatformProjectsByProjectAppsByAppEnvRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AppView>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling putPlatformProjectsByProjectAppsByAppEnv().'
            );
        }

        if (requestParameters['app'] == null) {
            throw new runtime.RequiredError(
                'app',
                'Required parameter "app" was null or undefined when calling putPlatformProjectsByProjectAppsByAppEnv().'
            );
        }

        if (requestParameters['setEnvReq'] == null) {
            throw new runtime.RequiredError(
                'setEnvReq',
                'Required parameter "setEnvReq" was null or undefined when calling putPlatformProjectsByProjectAppsByAppEnv().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/platform/projects/{project}/apps/{app}/env`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"app"}}`, encodeURIComponent(String(requestParameters['app'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SetEnvReqToJSON(requestParameters['setEnvReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppViewFromJSON(jsonValue));
    }

    /**
     * Replaces an app\'s environment variables.  It writes the app\'s whole environment set and answers the updated application. This is the one post-create write path for env, and it REPLACES rather than merges: a variable absent from the body is gone, and a secret dropped from the set leaves the app\'s Secret on its next deploy.  Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`. A value marked `secret: true` is sealed into KMS and blanked in the database, so plaintext is never persisted — and the write fails 503 if KMS is unavailable rather than storing one in the clear.  The rule worth knowing: this does not restart anything. Once the app has been deployed the secret sync is re-declared immediately so the operator re-materialises the Secret, but RUNNING pods keep the environment they started with until their next deploy or restart. Requires a validated principal; 403 without one.
     * Replaces an app\'s environment variables.
     */
    async putPlatformProjectsByProjectAppsByAppEnv(requestParameters: PlatformApiPutPlatformProjectsByProjectAppsByAppEnvRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AppView> {
        const response = await this.putPlatformProjectsByProjectAppsByAppEnvRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
