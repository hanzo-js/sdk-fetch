/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ChainList,
  ChainStatus,
} from '../models/index.js';
import {
    ChainListFromJSON,
    ChainListToJSON,
    ChainStatusFromJSON,
    ChainStatusToJSON,
} from '../models/index.js';

export interface ChainsApiGetChainsByChainRequest {
    chain: string;
}

/**
 * 
 */
export class ChainsApi extends runtime.BaseAPI {

    /**
     * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
     * Reports the chains this deployment can reach.
     */
    async getChainsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChainList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/chains`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChainListFromJSON(jsonValue));
    }

    /**
     * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
     * Reports the chains this deployment can reach.
     */
    async getChains(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChainList> {
        const response = await this.getChainsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
     * Reports one chain and whether its upstream is answering.
     */
    async getChainsByChainRaw(requestParameters: ChainsApiGetChainsByChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChainStatus>> {
        if (requestParameters['chain'] == null) {
            throw new runtime.RequiredError(
                'chain',
                'Required parameter "chain" was null or undefined when calling getChainsByChain().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/chains/{chain}`;
        urlPath = urlPath.replace(`{${"chain"}}`, encodeURIComponent(String(requestParameters['chain'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChainStatusFromJSON(jsonValue));
    }

    /**
     * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
     * Reports one chain and whether its upstream is answering.
     */
    async getChainsByChain(requestParameters: ChainsApiGetChainsByChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChainStatus> {
        const response = await this.getChainsByChainRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
