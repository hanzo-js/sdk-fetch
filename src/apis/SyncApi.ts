/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  PatchSyncIn,
  SyncList,
  SyncQueued,
  SyncReq,
  SyncView,
} from '../models/index.js';
import {
    PatchSyncInFromJSON,
    PatchSyncInToJSON,
    SyncListFromJSON,
    SyncListToJSON,
    SyncQueuedFromJSON,
    SyncQueuedToJSON,
    SyncReqFromJSON,
    SyncReqToJSON,
    SyncViewFromJSON,
    SyncViewToJSON,
} from '../models/index.js';

export interface SyncApiDeleteSyncByIdRequest {
    id: string;
}

export interface SyncApiGetSyncByIdRequest {
    id: string;
}

export interface SyncApiPatchSyncByIdRequest {
    id: string;
    patchSyncIn: PatchSyncIn;
}

export interface SyncApiPostSyncRequest {
    syncReq: SyncReq;
}

export interface SyncApiPostSyncByIdRunRequest {
    id: string;
}

/**
 * 
 */
export class SyncApi extends runtime.BaseAPI {

    /**
     * Delete removes one sync and tears down the outbound mirror it derived, answering 204. The teardown is the point: without it an unsynced repository would keep force-pushing to the upstream it is no longer linked to. Org-scoped, so another tenant\'s id is the same 404 an unknown id gives.
     * Delete removes one sync and tears down the outbound mirror it derived, answering 204.
     */
    async deleteSyncByIdRaw(requestParameters: SyncApiDeleteSyncByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteSyncById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sync/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delete removes one sync and tears down the outbound mirror it derived, answering 204. The teardown is the point: without it an unsynced repository would keep force-pushing to the upstream it is no longer linked to. Org-scoped, so another tenant\'s id is the same 404 an unknown id gives.
     * Delete removes one sync and tears down the outbound mirror it derived, answering 204.
     */
    async deleteSyncById(requestParameters: SyncApiDeleteSyncByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSyncByIdRaw(requestParameters, initOverrides);
    }

    /**
     * List returns every sync link the caller\'s org has, each with its two endpoints, its direction and trigger policy, and the time it last reconciled. Scoped to the caller\'s own org — another tenant\'s links are structurally unreachable.
     * List returns every sync link the caller\'s org has, each with its two endpoints, its direction and trigger policy, and the time it last reconciled.
     */
    async getSyncRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncListFromJSON(jsonValue));
    }

    /**
     * List returns every sync link the caller\'s org has, each with its two endpoints, its direction and trigger policy, and the time it last reconciled. Scoped to the caller\'s own org — another tenant\'s links are structurally unreachable.
     * List returns every sync link the caller\'s org has, each with its two endpoints, its direction and trigger policy, and the time it last reconciled.
     */
    async getSync(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncList> {
        const response = await this.getSyncRaw(initOverrides);
        return await response.value();
    }

    /**
     * Get returns one sync by id. It is org-scoped: an id belonging to another tenant is the same 404 an unknown id gives, so a probe learns nothing about what exists.
     * Get returns one sync by id.
     */
    async getSyncByIdRaw(requestParameters: SyncApiGetSyncByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSyncById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sync/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncViewFromJSON(jsonValue));
    }

    /**
     * Get returns one sync by id. It is org-scoped: an id belonging to another tenant is the same 404 an unknown id gives, so a probe learns nothing about what exists.
     * Get returns one sync by id.
     */
    async getSyncById(requestParameters: SyncApiGetSyncByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncView> {
        const response = await this.getSyncByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Patch updates one sync\'s mutable policy — direction, trigger and actor — in place. The endpoints and the kind are immutable: re-pointing a sync is a delete and a create, so a link can never silently start syncing somewhere else. A field the request omits is left as it was. Changing the direction immediately reconciles the derived outbound mirror, so turning push off stops the upstream being written to rather than merely recording the intent.
     * Patch updates one sync\'s mutable policy — direction, trigger and actor — in place.
     */
    async patchSyncByIdRaw(requestParameters: SyncApiPatchSyncByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling patchSyncById().'
            );
        }

        if (requestParameters['patchSyncIn'] == null) {
            throw new runtime.RequiredError(
                'patchSyncIn',
                'Required parameter "patchSyncIn" was null or undefined when calling patchSyncById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sync/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchSyncInToJSON(requestParameters['patchSyncIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncViewFromJSON(jsonValue));
    }

    /**
     * Patch updates one sync\'s mutable policy — direction, trigger and actor — in place. The endpoints and the kind are immutable: re-pointing a sync is a delete and a create, so a link can never silently start syncing somewhere else. A field the request omits is left as it was. Changing the direction immediately reconciles the derived outbound mirror, so turning push off stops the upstream being written to rather than merely recording the intent.
     * Patch updates one sync\'s mutable policy — direction, trigger and actor — in place.
     */
    async patchSyncById(requestParameters: SyncApiPatchSyncByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncView> {
        const response = await this.patchSyncByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Create declares a sync between two endpoints and returns it. It is an UPSERT: re-declaring the same source and target updates that link rather than piling up duplicates, so a console that re-submits is safe. The org comes from the validated principal, never from the request, so a sync can only ever bind endpoints inside the caller\'s own org. A git source must be an https clone URL on the provider\'s own host with no embedded credentials; a target left empty is derived as a native repository named after the source. With run=true the first reconcile is queued in the background, so a large initial import never blocks this response.
     * Create declares a sync between two endpoints and returns it.
     */
    async postSyncRaw(requestParameters: SyncApiPostSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncView>> {
        if (requestParameters['syncReq'] == null) {
            throw new runtime.RequiredError(
                'syncReq',
                'Required parameter "syncReq" was null or undefined when calling postSync().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SyncReqToJSON(requestParameters['syncReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncViewFromJSON(jsonValue));
    }

    /**
     * Create declares a sync between two endpoints and returns it. It is an UPSERT: re-declaring the same source and target updates that link rather than piling up duplicates, so a console that re-submits is safe. The org comes from the validated principal, never from the request, so a sync can only ever bind endpoints inside the caller\'s own org. A git source must be an https clone URL on the provider\'s own host with no embedded credentials; a target left empty is derived as a native repository named after the source. With run=true the first reconcile is queued in the background, so a large initial import never blocks this response.
     * Create declares a sync between two endpoints and returns it.
     */
    async postSync(requestParameters: SyncApiPostSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncView> {
        const response = await this.postSyncRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Run reconciles one sync now — the manual re-sync, and the initial import for a link created without run=true. The work is handed to a bounded background worker and the call answers 202 immediately, so a large mirror-in never holds the request open; queued=true means accepted, not finished.
     * Run reconciles one sync now — the manual re-sync, and the initial import for a link created without run=true.
     */
    async postSyncByIdRunRaw(requestParameters: SyncApiPostSyncByIdRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncQueued>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSyncByIdRun().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sync/{id}/run`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncQueuedFromJSON(jsonValue));
    }

    /**
     * Run reconciles one sync now — the manual re-sync, and the initial import for a link created without run=true. The work is handed to a bounded background worker and the call answers 202 immediately, so a large mirror-in never holds the request open; queued=true means accepted, not finished.
     * Run reconciles one sync now — the manual re-sync, and the initial import for a link created without run=true.
     */
    async postSyncByIdRun(requestParameters: SyncApiPostSyncByIdRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncQueued> {
        const response = await this.postSyncByIdRunRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
