/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  IndexersOut,
  OraclesOut,
} from '../models/index.js';
import {
    IndexersOutFromJSON,
    IndexersOutToJSON,
    OraclesOutFromJSON,
    OraclesOutToJSON,
} from '../models/index.js';

/**
 * 
 */
export class ExplorerApi extends runtime.BaseAPI {

    /**
     * Reports the deployment\'s chain indexer(s) and how far each has indexed. Identity and health come from the indexer\'s /health; the latest indexed block (height + time) from its /v1/explorer/blocks. The row EXISTS if EITHER call reaches the indexer; when the indexer is entirely unreachable the answer degrades to an honest-EMPTY list at 200, not a 502. No chain HEAD is exposed by the indexer REST, so `lag` is honestly omitted rather than fabricated.
     * Reports the deployment\'s chain indexer(s) and how far each has indexed.
     */
    async getExplorerIndexersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IndexersOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/explorer/indexers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IndexersOutFromJSON(jsonValue));
    }

    /**
     * Reports the deployment\'s chain indexer(s) and how far each has indexed. Identity and health come from the indexer\'s /health; the latest indexed block (height + time) from its /v1/explorer/blocks. The row EXISTS if EITHER call reaches the indexer; when the indexer is entirely unreachable the answer degrades to an honest-EMPTY list at 200, not a 502. No chain HEAD is exposed by the indexer REST, so `lag` is honestly omitted rather than fabricated.
     * Reports the deployment\'s chain indexer(s) and how far each has indexed.
     */
    async getExplorerIndexers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IndexersOut> {
        const response = await this.getExplorerIndexersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports the on-chain price/data oracles from the graph\'s O-Chain PriceFeed registry. A reachable graph with no feeds answers an honest empty list; an unreachable or erroring graph likewise degrades to an empty list at 200 rather than a 502, so the console never error-toasts. No feed is ever fabricated.
     * Reports the on-chain price/data oracles from the graph\'s O-Chain PriceFeed registry.
     */
    async getExplorerOraclesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OraclesOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/explorer/oracles`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OraclesOutFromJSON(jsonValue));
    }

    /**
     * Reports the on-chain price/data oracles from the graph\'s O-Chain PriceFeed registry. A reachable graph with no feeds answers an honest empty list; an unreachable or erroring graph likewise degrades to an empty list at 200 rather than a 502, so the console never error-toasts. No feed is ever fabricated.
     * Reports the on-chain price/data oracles from the graph\'s O-Chain PriceFeed registry.
     */
    async getExplorerOracles(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OraclesOut> {
        const response = await this.getExplorerOraclesRaw(initOverrides);
        return await response.value();
    }

}
