/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Account,
  AskRequest,
  AskResponse,
  BalanceSheet,
  BankTally,
  BankTxnRow,
  BookRequest,
  BookResponse,
  FinancialPackage,
  GLRow,
  InboxItem,
  InboxOut,
  MetricsResponse,
  PnL,
  QuestionsResponse,
  Rule,
  RulesOut,
  ScanDraft,
  SyncTally,
  TransactionsOut,
  TrialBalance,
  UnreconciledOut,
  VendorRow,
  VendorsOut,
} from '../models/index.js';
import {
    AccountFromJSON,
    AccountToJSON,
    AskRequestFromJSON,
    AskRequestToJSON,
    AskResponseFromJSON,
    AskResponseToJSON,
    BalanceSheetFromJSON,
    BalanceSheetToJSON,
    BankTallyFromJSON,
    BankTallyToJSON,
    BankTxnRowFromJSON,
    BankTxnRowToJSON,
    BookRequestFromJSON,
    BookRequestToJSON,
    BookResponseFromJSON,
    BookResponseToJSON,
    FinancialPackageFromJSON,
    FinancialPackageToJSON,
    GLRowFromJSON,
    GLRowToJSON,
    InboxItemFromJSON,
    InboxItemToJSON,
    InboxOutFromJSON,
    InboxOutToJSON,
    MetricsResponseFromJSON,
    MetricsResponseToJSON,
    PnLFromJSON,
    PnLToJSON,
    QuestionsResponseFromJSON,
    QuestionsResponseToJSON,
    RuleFromJSON,
    RuleToJSON,
    RulesOutFromJSON,
    RulesOutToJSON,
    ScanDraftFromJSON,
    ScanDraftToJSON,
    SyncTallyFromJSON,
    SyncTallyToJSON,
    TransactionsOutFromJSON,
    TransactionsOutToJSON,
    TrialBalanceFromJSON,
    TrialBalanceToJSON,
    UnreconciledOutFromJSON,
    UnreconciledOutToJSON,
    VendorRowFromJSON,
    VendorRowToJSON,
    VendorsOutFromJSON,
    VendorsOutToJSON,
} from '../models/index.js';

export interface BooksApiGetBooksAccountsRequest {
    sandbox?: string;
}

export interface BooksApiGetBooksBankTransactionsRequest {
    sandbox?: string;
    limit?: number;
}

export interface BooksApiGetBooksBankUnreconciledRequest {
    sandbox?: string;
}

export interface BooksApiGetBooksExportRequest {
    sandbox?: string;
    from?: string;
    to?: string;
    format?: string;
    limit?: number;
}

export interface BooksApiGetBooksGlRequest {
    sandbox?: string;
    limit?: number;
}

export interface BooksApiGetBooksInboxRequest {
    sandbox?: string;
}

export interface BooksApiGetBooksMetricsRequest {
    sandbox?: string;
    from?: string;
    to?: string;
}

export interface BooksApiGetBooksPnlRequest {
    sandbox?: string;
    from?: string;
    to?: string;
}

export interface BooksApiGetBooksPositionRequest {
    sandbox?: string;
    to?: string;
}

export interface BooksApiGetBooksQuestionsRequest {
    sandbox?: string;
}

export interface BooksApiGetBooksRulesRequest {
    sandbox?: string;
}

export interface BooksApiGetBooksTransactionsRequest {
    sandbox?: string;
    from?: string;
    to?: string;
    category?: string;
    vendor?: string;
    limit?: number;
}

export interface BooksApiGetBooksTrialRequest {
    sandbox?: string;
    from?: string;
    to?: string;
}

export interface BooksApiGetBooksVendorsRequest {
    sandbox?: string;
}

export interface BooksApiPostBooksAskRequest {
    askRequest: AskRequest;
}

export interface BooksApiPostBooksBankImportRequest {
    body?: Blob;
}

export interface BooksApiPostBooksInboxRequest {
    body?: Blob;
}

export interface BooksApiPostBooksRulesRequest {
    rule: Rule;
}

export interface BooksApiPostBooksScanRequest {
    body?: Blob;
}

export interface BooksApiPostBooksScanBookRequest {
    bookRequest: BookRequest;
}

export interface BooksApiPostBooksVendorsRequest {
    vendorRow: VendorRow;
}

/**
 * 
 */
export class BooksApi extends runtime.BaseAPI {

    /**
     * Returns the org\'s chart of accounts — the seeded fixed chart every posting key in the ledger refers to.
     * Returns the org\'s chart of accounts — the seeded fixed chart every posting key in the ledger refers to.
     */
    async getBooksAccountsRaw(requestParameters: BooksApiGetBooksAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<Account>>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/accounts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(AccountFromJSON));
    }

    /**
     * Returns the org\'s chart of accounts — the seeded fixed chart every posting key in the ledger refers to.
     * Returns the org\'s chart of accounts — the seeded fixed chart every posting key in the ledger refers to.
     */
    async getBooksAccounts(requestParameters: BooksApiGetBooksAccountsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<Account>> {
        const response = await this.getBooksAccountsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s normalized bank transactions, newest first — every row the import and connector paths have ingested, with its amount in exact cents, its direction, and whether it has been matched to a voucher yet.
     * Returns the org\'s normalized bank transactions, newest first — every row the import and connector paths have ingested, with its amount in exact cents, its direction, and whether it has been matched to a voucher yet.
     */
    async getBooksBankTransactionsRaw(requestParameters: BooksApiGetBooksBankTransactionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<BankTxnRow>>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/bank/transactions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(BankTxnRowFromJSON));
    }

    /**
     * Returns the org\'s normalized bank transactions, newest first — every row the import and connector paths have ingested, with its amount in exact cents, its direction, and whether it has been matched to a voucher yet.
     * Returns the org\'s normalized bank transactions, newest first — every row the import and connector paths have ingested, with its amount in exact cents, its direction, and whether it has been matched to a voucher yet.
     */
    async getBooksBankTransactions(requestParameters: BooksApiGetBooksBankTransactionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<BankTxnRow>> {
        const response = await this.getBooksBankTransactionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s unmatched bank inflows and their open clarifying questions — the queue a human answers so an unexplained deposit is never guessed into revenue.
     * Returns the org\'s unmatched bank inflows and their open clarifying questions — the queue a human answers so an unexplained deposit is never guessed into revenue.
     */
    async getBooksBankUnreconciledRaw(requestParameters: BooksApiGetBooksBankUnreconciledRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UnreconciledOut>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/bank/unreconciled`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UnreconciledOutFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s unmatched bank inflows and their open clarifying questions — the queue a human answers so an unexplained deposit is never guessed into revenue.
     * Returns the org\'s unmatched bank inflows and their open clarifying questions — the queue a human answers so an unexplained deposit is never guessed into revenue.
     */
    async getBooksBankUnreconciled(requestParameters: BooksApiGetBooksBankUnreconciledRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UnreconciledOut> {
        const response = await this.getBooksBankUnreconciledRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the complete financial package for the caller\'s org over (from, to]: the trial balance, the P&L, the balance sheet, and the GL detail behind them — the four statements a tax preparer or an investor asks for, assembled from the one ledger in a single read so they cannot disagree with each other.
     * Returns the complete financial package for the caller\'s org over (from, to]: the trial balance, the P&L, the balance sheet, and the GL detail behind them — the four statements a tax preparer or an investor asks for, assembled from the one ledger in a single read so they cannot disagree with each other.
     */
    async getBooksExportRaw(requestParameters: BooksApiGetBooksExportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinancialPackage>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        if (requestParameters['format'] != null) {
            queryParameters['format'] = requestParameters['format'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/export`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinancialPackageFromJSON(jsonValue));
    }

    /**
     * Returns the complete financial package for the caller\'s org over (from, to]: the trial balance, the P&L, the balance sheet, and the GL detail behind them — the four statements a tax preparer or an investor asks for, assembled from the one ledger in a single read so they cannot disagree with each other.
     * Returns the complete financial package for the caller\'s org over (from, to]: the trial balance, the P&L, the balance sheet, and the GL detail behind them — the four statements a tax preparer or an investor asks for, assembled from the one ledger in a single read so they cannot disagree with each other.
     */
    async getBooksExport(requestParameters: BooksApiGetBooksExportRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinancialPackage> {
        const response = await this.getBooksExportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * ListGL returns the org\'s most recent GL Entry rows, newest first. This is the raw double-entry detail behind every statement: one row per leg, with its debit, credit, posting time and the source that booked it.
     * ListGL returns the org\'s most recent GL Entry rows, newest first.
     */
    async getBooksGlRaw(requestParameters: BooksApiGetBooksGlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<GLRow>>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/gl`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(GLRowFromJSON));
    }

    /**
     * ListGL returns the org\'s most recent GL Entry rows, newest first. This is the raw double-entry detail behind every statement: one row per leg, with its debit, credit, posting time and the source that booked it.
     * ListGL returns the org\'s most recent GL Entry rows, newest first.
     */
    async getBooksGl(requestParameters: BooksApiGetBooksGlRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<GLRow>> {
        const response = await this.getBooksGlRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s open document queue — everything uploaded but not yet booked, newest first, each with its extracted summary and the confidence the scanner resolved its category at. A booked document drops out of the queue.
     * Returns the org\'s open document queue — everything uploaded but not yet booked, newest first, each with its extracted summary and the confidence the scanner resolved its category at.
     */
    async getBooksInboxRaw(requestParameters: BooksApiGetBooksInboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InboxOut>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/inbox`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InboxOutFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s open document queue — everything uploaded but not yet booked, newest first, each with its extracted summary and the confidence the scanner resolved its category at. A booked document drops out of the queue.
     * Returns the org\'s open document queue — everything uploaded but not yet booked, newest first, each with its extracted summary and the confidence the scanner resolved its category at.
     */
    async getBooksInbox(requestParameters: BooksApiGetBooksInboxRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InboxOut> {
        const response = await this.getBooksInboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Metrics returns the org\'s deterministic SaaS-metrics snapshot over an optional (from, to] window — MRR, ARR, revenue, COGS, burn, gross margin, net income, cash, deferred revenue, monthly burn and runway — as raw int64-cent figures AND the same figures already formatted. Every number is the ledger, aggregated the one way the books define it, never a guess; it is the grounded read the unified /v1/ask advisor replays.
     * Metrics returns the org\'s deterministic SaaS-metrics snapshot over an optional (from, to] window — MRR, ARR, revenue, COGS, burn, gross margin, net income, cash, deferred revenue, monthly burn and runway — as raw int64-cent figures AND the same figures already formatted.
     */
    async getBooksMetricsRaw(requestParameters: BooksApiGetBooksMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MetricsResponse>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/metrics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MetricsResponseFromJSON(jsonValue));
    }

    /**
     * Metrics returns the org\'s deterministic SaaS-metrics snapshot over an optional (from, to] window — MRR, ARR, revenue, COGS, burn, gross margin, net income, cash, deferred revenue, monthly burn and runway — as raw int64-cent figures AND the same figures already formatted. Every number is the ledger, aggregated the one way the books define it, never a guess; it is the grounded read the unified /v1/ask advisor replays.
     * Metrics returns the org\'s deterministic SaaS-metrics snapshot over an optional (from, to] window — MRR, ARR, revenue, COGS, burn, gross margin, net income, cash, deferred revenue, monthly burn and runway — as raw int64-cent figures AND the same figures already formatted.
     */
    async getBooksMetrics(requestParameters: BooksApiGetBooksMetricsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MetricsResponse> {
        const response = await this.getBooksMetricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s accrual-basis Profit & Loss over an optional (from, to] window of RFC3339 posting times: recognized revenue, matched cost, and the net.
     * Returns the org\'s accrual-basis Profit & Loss over an optional (from, to] window of RFC3339 posting times: recognized revenue, matched cost, and the net.
     */
    async getBooksPnlRaw(requestParameters: BooksApiGetBooksPnlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PnL>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/pnl`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PnLFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s accrual-basis Profit & Loss over an optional (from, to] window of RFC3339 posting times: recognized revenue, matched cost, and the net.
     * Returns the org\'s accrual-basis Profit & Loss over an optional (from, to] window of RFC3339 posting times: recognized revenue, matched cost, and the net.
     */
    async getBooksPnl(requestParameters: BooksApiGetBooksPnlRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PnL> {
        const response = await this.getBooksPnlRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s Balance Sheet as of `to` (empty = all time), with the Assets == Liabilities + Equity equation proof.
     * Returns the org\'s Balance Sheet as of `to` (empty = all time), with the Assets == Liabilities + Equity equation proof.
     */
    async getBooksPositionRaw(requestParameters: BooksApiGetBooksPositionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BalanceSheet>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/position`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BalanceSheetFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s Balance Sheet as of `to` (empty = all time), with the Assets == Liabilities + Equity equation proof.
     * Returns the org\'s Balance Sheet as of `to` (empty = all time), with the Assets == Liabilities + Equity equation proof.
     */
    async getBooksPosition(requestParameters: BooksApiGetBooksPositionRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BalanceSheet> {
        const response = await this.getBooksPositionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the clarifying questions the caller\'s own recent GL raises — the unusual postings a founder should look at (outliers, reversals, round-offs, uncosted revenue, an overdrawn wallet), sharpest first. An empty list means the books look clean; the detector is deterministic over the ledger and invents nothing.
     * Returns the clarifying questions the caller\'s own recent GL raises — the unusual postings a founder should look at (outliers, reversals, round-offs, uncosted revenue, an overdrawn wallet), sharpest first.
     */
    async getBooksQuestionsRaw(requestParameters: BooksApiGetBooksQuestionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QuestionsResponse>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/questions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QuestionsResponseFromJSON(jsonValue));
    }

    /**
     * Returns the clarifying questions the caller\'s own recent GL raises — the unusual postings a founder should look at (outliers, reversals, round-offs, uncosted revenue, an overdrawn wallet), sharpest first. An empty list means the books look clean; the detector is deterministic over the ledger and invents nothing.
     * Returns the clarifying questions the caller\'s own recent GL raises — the unusual postings a founder should look at (outliers, reversals, round-offs, uncosted revenue, an overdrawn wallet), sharpest first.
     */
    async getBooksQuestions(requestParameters: BooksApiGetBooksQuestionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QuestionsResponse> {
        const response = await this.getBooksQuestionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s auto-categorization rules, highest priority first. A rule is a standing instruction — \"anything whose merchant contains X books to category Y\" — and it overrides a vendor\'s default category, so this is the list that decides how a future bill classifies itself.
     * Returns the org\'s auto-categorization rules, highest priority first.
     */
    async getBooksRulesRaw(requestParameters: BooksApiGetBooksRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RulesOut>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/rules`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RulesOutFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s auto-categorization rules, highest priority first. A rule is a standing instruction — \"anything whose merchant contains X books to category Y\" — and it overrides a vendor\'s default category, so this is the list that decides how a future bill classifies itself.
     * Returns the org\'s auto-categorization rules, highest priority first.
     */
    async getBooksRules(requestParameters: BooksApiGetBooksRulesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RulesOut> {
        const response = await this.getBooksRulesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s booked ledger as a single-line register, newest first: one row per voucher, with its date, description, vendor, category, source and amount in exact cents. It is the double-entry ledger projected to the register a human reads, filterable by posting-time window, category and vendor. Strictly read-only — it restates the books, it never moves them.
     * Returns the org\'s booked ledger as a single-line register, newest first: one row per voucher, with its date, description, vendor, category, source and amount in exact cents.
     */
    async getBooksTransactionsRaw(requestParameters: BooksApiGetBooksTransactionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TransactionsOut>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        if (requestParameters['category'] != null) {
            queryParameters['category'] = requestParameters['category'];
        }

        if (requestParameters['vendor'] != null) {
            queryParameters['vendor'] = requestParameters['vendor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/transactions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TransactionsOutFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s booked ledger as a single-line register, newest first: one row per voucher, with its date, description, vendor, category, source and amount in exact cents. It is the double-entry ledger projected to the register a human reads, filterable by posting-time window, category and vendor. Strictly read-only — it restates the books, it never moves them.
     * Returns the org\'s booked ledger as a single-line register, newest first: one row per voucher, with its date, description, vendor, category, source and amount in exact cents.
     */
    async getBooksTransactions(requestParameters: BooksApiGetBooksTransactionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TransactionsOut> {
        const response = await this.getBooksTransactionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s trial balance over an optional [from, to] window of RFC3339 posting times, including the opening/closing columns and the TotalDebit == TotalCredit proof that the books balance.
     * Returns the org\'s trial balance over an optional [from, to] window of RFC3339 posting times, including the opening/closing columns and the TotalDebit == TotalCredit proof that the books balance.
     */
    async getBooksTrialRaw(requestParameters: BooksApiGetBooksTrialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrialBalance>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/trial`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrialBalanceFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s trial balance over an optional [from, to] window of RFC3339 posting times, including the opening/closing columns and the TotalDebit == TotalCredit proof that the books balance.
     * Returns the org\'s trial balance over an optional [from, to] window of RFC3339 posting times, including the opening/closing columns and the TotalDebit == TotalCredit proof that the books balance.
     */
    async getBooksTrial(requestParameters: BooksApiGetBooksTrialRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrialBalance> {
        const response = await this.getBooksTrialRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s vendor book: each canonical vendor, the alias spellings a receipt may print it under, and the expense account new bills from it default to. A vendor here is what makes a scanned bill self-classify instead of asking again.
     * Returns the org\'s vendor book: each canonical vendor, the alias spellings a receipt may print it under, and the expense account new bills from it default to.
     */
    async getBooksVendorsRaw(requestParameters: BooksApiGetBooksVendorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VendorsOut>> {
        const queryParameters: any = {};

        if (requestParameters['sandbox'] != null) {
            queryParameters['sandbox'] = requestParameters['sandbox'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/vendors`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VendorsOutFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s vendor book: each canonical vendor, the alias spellings a receipt may print it under, and the expense account new bills from it default to. A vendor here is what makes a scanned bill self-classify instead of asking again.
     * Returns the org\'s vendor book: each canonical vendor, the alias spellings a receipt may print it under, and the expense account new bills from it default to.
     */
    async getBooksVendors(requestParameters: BooksApiGetBooksVendorsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VendorsOut> {
        const response = await this.getBooksVendorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers a plain-language question about the caller\'s own books — \"what is my MRR?\", \"how long is my runway?\" — with figures taken from their ledger, never a guessed number. A deterministic keyword router picks the intent and reads the real metrics, and those figures, followups and report sources are computed BEFORE any model call and are never altered by one: the optional narration seam only rephrases the sentence, and it degrades silently to the templated answer when no AI plane is wired. It is strictly read-only — it restates the books, it never posts to them.
     * Answers a plain-language question about the caller\'s own books — \"what is my MRR?\", \"how long is my runway?\" — with figures taken from their ledger, never a guessed number.
     */
    async postBooksAskRaw(requestParameters: BooksApiPostBooksAskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AskResponse>> {
        if (requestParameters['askRequest'] == null) {
            throw new runtime.RequiredError(
                'askRequest',
                'Required parameter "askRequest" was null or undefined when calling postBooksAsk().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/ask`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AskRequestToJSON(requestParameters['askRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AskResponseFromJSON(jsonValue));
    }

    /**
     * Answers a plain-language question about the caller\'s own books — \"what is my MRR?\", \"how long is my runway?\" — with figures taken from their ledger, never a guessed number. A deterministic keyword router picks the intent and reads the real metrics, and those figures, followups and report sources are computed BEFORE any model call and are never altered by one: the optional narration seam only rephrases the sentence, and it degrades silently to the templated answer when no AI plane is wired. It is strictly read-only — it restates the books, it never posts to them.
     * Answers a plain-language question about the caller\'s own books — \"what is my MRR?\", \"how long is my runway?\" — with figures taken from their ledger, never a guessed number.
     */
    async postBooksAsk(requestParameters: BooksApiPostBooksAskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AskResponse> {
        const response = await this.postBooksAskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * ANSWERS 501 UNCONDITIONALLY. It is the intended second hop of the bank-linking handshake — trade the provider\'s short-lived public token for the durable access credential and seal that credential into KMS — and nothing on the HTTP path reaches an implementation today.  The durable bank credential is the reason this hop exists: it is meant to be sealed server-side and never handed back to the caller. Since the route never succeeds, no credential is stored by it and no bank is connected through it.  Documented as refusing rather than declared with a success body, for the same reason as the first hop: it has never sent one, and stating a shape it has never produced would put a return type in every SDK for a call that always fails. A caller with no principal gets 401 before the 501.
     * Finish connecting a bank account (not yet available)
     */
    async postBooksBankExchangeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/bank/exchange`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * ANSWERS 501 UNCONDITIONALLY. It is the intended second hop of the bank-linking handshake — trade the provider\'s short-lived public token for the durable access credential and seal that credential into KMS — and nothing on the HTTP path reaches an implementation today.  The durable bank credential is the reason this hop exists: it is meant to be sealed server-side and never handed back to the caller. Since the route never succeeds, no credential is stored by it and no bank is connected through it.  Documented as refusing rather than declared with a success body, for the same reason as the first hop: it has never sent one, and stating a shape it has never produced would put a return type in every SDK for a call that always fails. A caller with no principal gets 401 before the 501.
     * Finish connecting a bank account (not yet available)
     */
    async postBooksBankExchange(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postBooksBankExchangeRaw(initOverrides);
    }

    /**
     * Takes a bank statement as RAW BYTES — the file exactly as downloaded, OFX, QFX or CSV, not wrapped in JSON — parses every row, books it against the caller org\'s own ledger, and answers the tally: how many rows were seen, how many vouchers posted, how many inflows reconciled, how many raised a question, how many were own-account transfers, and how many were skipped.  RE-IMPORTING THE SAME STATEMENT DOES NOT DOUBLE-BOOK. Every row goes through the same posting choke point every other source uses, keyed idempotently, so an overlapping statement — the usual case, since exports overlap at the month boundary — lands its new rows and counts the rest as skipped. Skipped is the number to read on a second import.  It is READ-ONLY against the bank: this ingests, it never sends money. Scoped to the caller\'s own org from the validated principal, and refused without one; `sandbox=true` writes the org\'s sandbox ledger instead of its real books. An empty body is a 400, and a file the parser cannot read is a 400 carrying the parser\'s reason rather than a partial import. On a deployment whose import parser is not built, this answers 501 rather than mishandling the file.
     * Import a bank statement file into your books
     */
    async postBooksBankImportRaw(requestParameters: BooksApiPostBooksBankImportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BankTally>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/bank/import`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BankTallyFromJSON(jsonValue));
    }

    /**
     * Takes a bank statement as RAW BYTES — the file exactly as downloaded, OFX, QFX or CSV, not wrapped in JSON — parses every row, books it against the caller org\'s own ledger, and answers the tally: how many rows were seen, how many vouchers posted, how many inflows reconciled, how many raised a question, how many were own-account transfers, and how many were skipped.  RE-IMPORTING THE SAME STATEMENT DOES NOT DOUBLE-BOOK. Every row goes through the same posting choke point every other source uses, keyed idempotently, so an overlapping statement — the usual case, since exports overlap at the month boundary — lands its new rows and counts the rest as skipped. Skipped is the number to read on a second import.  It is READ-ONLY against the bank: this ingests, it never sends money. Scoped to the caller\'s own org from the validated principal, and refused without one; `sandbox=true` writes the org\'s sandbox ledger instead of its real books. An empty body is a 400, and a file the parser cannot read is a 400 carrying the parser\'s reason rather than a partial import. On a deployment whose import parser is not built, this answers 501 rather than mishandling the file.
     * Import a bank statement file into your books
     */
    async postBooksBankImport(requestParameters: BooksApiPostBooksBankImportRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BankTally> {
        const response = await this.postBooksBankImportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Pulls every connected bank (Plaid/Teller) for the caller\'s org, maps each fetched transaction to a posting and books it idempotently, then advances that connector\'s cursor so the next sync resumes where this one stopped. One connector\'s outage is skipped rather than failing the whole sync. It reports the batch: how many transactions were seen, how many vouchers posted, how many inflows reconciled against the processor clearing account, how many raised a question, how many were own-account transfers, and how many were already-processed no-ops. It is READ-ONLY against the bank — it ingests, it never sends money.
     * Pulls every connected bank (Plaid/Teller) for the caller\'s org, maps each fetched transaction to a posting and books it idempotently, then advances that connector\'s cursor so the next sync resumes where this one stopped.
     */
    async postBooksBankSyncRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BankTally>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/bank/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BankTallyFromJSON(jsonValue));
    }

    /**
     * Pulls every connected bank (Plaid/Teller) for the caller\'s org, maps each fetched transaction to a posting and books it idempotently, then advances that connector\'s cursor so the next sync resumes where this one stopped. One connector\'s outage is skipped rather than failing the whole sync. It reports the batch: how many transactions were seen, how many vouchers posted, how many inflows reconciled against the processor clearing account, how many raised a question, how many were own-account transfers, and how many were already-processed no-ops. It is READ-ONLY against the bank — it ingests, it never sends money.
     * Pulls every connected bank (Plaid/Teller) for the caller\'s org, maps each fetched transaction to a posting and books it idempotently, then advances that connector\'s cursor so the next sync resumes where this one stopped.
     */
    async postBooksBankSync(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BankTally> {
        const response = await this.postBooksBankSyncRaw(initOverrides);
        return await response.value();
    }

    /**
     * ANSWERS 501 UNCONDITIONALLY. It is the intended first hop of the bank-linking handshake — mint the short-lived session token a browser hands to the provider\'s link widget — and nothing on the HTTP path reaches an implementation today.  The connectors behind it are written and tested; only the wiring is missing, so an org cannot connect a bank through the API at all. Until that lands, bank data reaches the books by statement import.  It is documented as refusing rather than declared with a success body precisely because it has never sent one. A response schema here would be invention: every generated SDK would carry a return type for a call that has only ever failed. A caller with no principal gets 401 before the 501.
     * Begin connecting a bank account (not yet available)
     */
    async postBooksBankTokenRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/bank/token`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * ANSWERS 501 UNCONDITIONALLY. It is the intended first hop of the bank-linking handshake — mint the short-lived session token a browser hands to the provider\'s link widget — and nothing on the HTTP path reaches an implementation today.  The connectors behind it are written and tested; only the wiring is missing, so an org cannot connect a bank through the API at all. Until that lands, bank data reaches the books by statement import.  It is documented as refusing rather than declared with a success body precisely because it has never sent one. A response schema here would be invention: every generated SDK would carry a return type for a call that has only ever failed. A caller with no principal gets 401 before the 501.
     * Begin connecting a bank account (not yet available)
     */
    async postBooksBankToken(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postBooksBankTokenRaw(initOverrides);
    }

    /**
     * Takes a document as RAW BYTES and queues it in the caller org\'s inbox as `unsorted`, answering the queued item. It is the drop box: get the paperwork in now, read it later.  It EXTRACTS NOTHING and calls no model — that is what separates it from the scan. Nothing is proposed and nothing is posted; the item simply waits to be scanned, and a booked document leaves the queue.  IDEMPOTENT BY CONTENT: the item\'s id is the file hash, so re-uploading the same bytes answers the existing item rather than adding a duplicate row — and it is the same id a scan of those bytes uses, which is how the two routes address one document. Scoped to the caller\'s own org from the validated principal and refused without one; `sandbox=true` targets the sandbox ledger, and `filename` is recorded for display. An empty or oversized upload is a 400.
     * Queue a document for later scanning
     */
    async postBooksInboxRaw(requestParameters: BooksApiPostBooksInboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InboxItem>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/inbox`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InboxItemFromJSON(jsonValue));
    }

    /**
     * Takes a document as RAW BYTES and queues it in the caller org\'s inbox as `unsorted`, answering the queued item. It is the drop box: get the paperwork in now, read it later.  It EXTRACTS NOTHING and calls no model — that is what separates it from the scan. Nothing is proposed and nothing is posted; the item simply waits to be scanned, and a booked document leaves the queue.  IDEMPOTENT BY CONTENT: the item\'s id is the file hash, so re-uploading the same bytes answers the existing item rather than adding a duplicate row — and it is the same id a scan of those bytes uses, which is how the two routes address one document. Scoped to the caller\'s own org from the validated principal and refused without one; `sandbox=true` targets the sandbox ledger, and `filename` is recorded for display. An empty or oversized upload is a 400.
     * Queue a document for later scanning
     */
    async postBooksInbox(requestParameters: BooksApiPostBooksInboxRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InboxItem> {
        const response = await this.postBooksInboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or updates one auto-categorization rule, keyed by its pattern — writing a pattern that already exists REPLACES that row\'s category and priority. The category is normalized to a real COA expense account, and anything unrecognized becomes 5900 Uncategorized rather than a guessed real account. It answers the row exactly as stored, so the caller sees the normalization. A rule overrides a vendor\'s default category, so this is the standing instruction that decides how a future bill classifies.
     * Creates or updates one auto-categorization rule, keyed by its pattern — writing a pattern that already exists REPLACES that row\'s category and priority.
     */
    async postBooksRulesRaw(requestParameters: BooksApiPostBooksRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Rule>> {
        if (requestParameters['rule'] == null) {
            throw new runtime.RequiredError(
                'rule',
                'Required parameter "rule" was null or undefined when calling postBooksRules().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/rules`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RuleToJSON(requestParameters['rule']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RuleFromJSON(jsonValue));
    }

    /**
     * Creates or updates one auto-categorization rule, keyed by its pattern — writing a pattern that already exists REPLACES that row\'s category and priority. The category is normalized to a real COA expense account, and anything unrecognized becomes 5900 Uncategorized rather than a guessed real account. It answers the row exactly as stored, so the caller sees the normalization. A rule overrides a vendor\'s default category, so this is the standing instruction that decides how a future bill classifies.
     * Creates or updates one auto-categorization rule, keyed by its pattern — writing a pattern that already exists REPLACES that row\'s category and priority.
     */
    async postBooksRules(requestParameters: BooksApiPostBooksRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Rule> {
        const response = await this.postBooksRulesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Takes a receipt or invoice as RAW BYTES — a PDF, an image or plain text, uploaded under its own content type, not wrapped in JSON — extracts what the document says, resolves the vendor\'s expense category, and answers a DRAFT carrying a balanced voucher proposed for it.  NOTHING IS POSTED. That split is the whole design: the model only ever produces a structured reading of the document, the voucher is assembled deterministically in Go from that reading, and the ledger is written only by the separate book call a human confirms. So a misread scan can propose a wrong draft; it cannot move money. Amounts are exact integer cents end to end — the extraction returns cents, never a decimal — so no rounding enters the ledger.  The draft\'s id is the FILE HASH, and that is what makes booking idempotent: re-scanning the same bytes addresses the same draft rather than queuing a second one. A row is written to the org\'s document inbox as a side effect, moving it from unsorted to draft. Scoped to the caller\'s own org from the validated principal and refused without one; `sandbox=true` targets the sandbox ledger, and `filename` is recorded for the inbox. An empty or oversized upload is a 400, and a deployment with no scanner model answers 501.
     * Scan a receipt or invoice into a proposed voucher
     */
    async postBooksScanRaw(requestParameters: BooksApiPostBooksScanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScanDraft>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/scan`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScanDraftFromJSON(jsonValue));
    }

    /**
     * Takes a receipt or invoice as RAW BYTES — a PDF, an image or plain text, uploaded under its own content type, not wrapped in JSON — extracts what the document says, resolves the vendor\'s expense category, and answers a DRAFT carrying a balanced voucher proposed for it.  NOTHING IS POSTED. That split is the whole design: the model only ever produces a structured reading of the document, the voucher is assembled deterministically in Go from that reading, and the ledger is written only by the separate book call a human confirms. So a misread scan can propose a wrong draft; it cannot move money. Amounts are exact integer cents end to end — the extraction returns cents, never a decimal — so no rounding enters the ledger.  The draft\'s id is the FILE HASH, and that is what makes booking idempotent: re-scanning the same bytes addresses the same draft rather than queuing a second one. A row is written to the org\'s document inbox as a side effect, moving it from unsorted to draft. Scoped to the caller\'s own org from the validated principal and refused without one; `sandbox=true` targets the sandbox ledger, and `filename` is recorded for the inbox. An empty or oversized upload is a 400, and a deployment with no scanner model answers 501.
     * Scan a receipt or invoice into a proposed voucher
     */
    async postBooksScan(requestParameters: BooksApiPostBooksScanRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScanDraft> {
        const response = await this.postBooksScanRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Posts a reviewed scanned bill to the ledger. It is the scanner\'s ONLY write: the voucher goes through the same post() choke point every other source uses, so it is checked to balance (Σdebit == Σcredit) and is idempotent by (scan, scanId) — re-booking the same scan answers posted=false and writes nothing. A bill whose economic identity (vendor, total, issue date) already posted under a DIFFERENT scan is refused 409 unless override is set, which is what stops the same receipt re-scanned into a new file hash from double-booking. An unbalanced voucher is refused 400.
     * Posts a reviewed scanned bill to the ledger.
     */
    async postBooksScanBookRaw(requestParameters: BooksApiPostBooksScanBookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BookResponse>> {
        if (requestParameters['bookRequest'] == null) {
            throw new runtime.RequiredError(
                'bookRequest',
                'Required parameter "bookRequest" was null or undefined when calling postBooksScanBook().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/scan/book`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BookRequestToJSON(requestParameters['bookRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BookResponseFromJSON(jsonValue));
    }

    /**
     * Posts a reviewed scanned bill to the ledger. It is the scanner\'s ONLY write: the voucher goes through the same post() choke point every other source uses, so it is checked to balance (Σdebit == Σcredit) and is idempotent by (scan, scanId) — re-booking the same scan answers posted=false and writes nothing. A bill whose economic identity (vendor, total, issue date) already posted under a DIFFERENT scan is refused 409 unless override is set, which is what stops the same receipt re-scanned into a new file hash from double-booking. An unbalanced voucher is refused 400.
     * Posts a reviewed scanned bill to the ledger.
     */
    async postBooksScanBook(requestParameters: BooksApiPostBooksScanBookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BookResponse> {
        const response = await this.postBooksScanBookRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sync ingests the caller\'s OWN org from commerce into BOTH ledgers (live and sandbox) and reports how many new vouchers posted to each. It is idempotent — money that has already been booked posts nothing on a repeat — and it is read-only against commerce: it never mints a deposit, a credit or a payout, only the accounting twin of money that already moved.
     * Sync ingests the caller\'s OWN org from commerce into BOTH ledgers (live and sandbox) and reports how many new vouchers posted to each.
     */
    async postBooksSyncRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncTally>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncTallyFromJSON(jsonValue));
    }

    /**
     * Sync ingests the caller\'s OWN org from commerce into BOTH ledgers (live and sandbox) and reports how many new vouchers posted to each. It is idempotent — money that has already been booked posts nothing on a repeat — and it is read-only against commerce: it never mints a deposit, a credit or a payout, only the accounting twin of money that already moved.
     * Sync ingests the caller\'s OWN org from commerce into BOTH ledgers (live and sandbox) and reports how many new vouchers posted to each.
     */
    async postBooksSync(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncTally> {
        const response = await this.postBooksSyncRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates or updates one vendor in the org\'s vendor book, keyed by its canonical name — writing a canonical name that already exists REPLACES that row\'s aliases and default category. A category given as a slug (\"software\") is normalized to its real COA expense account, and anything unrecognized becomes 5900 Uncategorized rather than a guessed real account. It answers the row exactly as stored, so the caller sees the normalization. Recording a vendor is what makes future bills from it self-classify instead of asking again.
     * Creates or updates one vendor in the org\'s vendor book, keyed by its canonical name — writing a canonical name that already exists REPLACES that row\'s aliases and default category.
     */
    async postBooksVendorsRaw(requestParameters: BooksApiPostBooksVendorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VendorRow>> {
        if (requestParameters['vendorRow'] == null) {
            throw new runtime.RequiredError(
                'vendorRow',
                'Required parameter "vendorRow" was null or undefined when calling postBooksVendors().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/books/vendors`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VendorRowToJSON(requestParameters['vendorRow']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VendorRowFromJSON(jsonValue));
    }

    /**
     * Creates or updates one vendor in the org\'s vendor book, keyed by its canonical name — writing a canonical name that already exists REPLACES that row\'s aliases and default category. A category given as a slug (\"software\") is normalized to its real COA expense account, and anything unrecognized becomes 5900 Uncategorized rather than a guessed real account. It answers the row exactly as stored, so the caller sees the normalization. Recording a vendor is what makes future bills from it self-classify instead of asking again.
     * Creates or updates one vendor in the org\'s vendor book, keyed by its canonical name — writing a canonical name that already exists REPLACES that row\'s aliases and default category.
     */
    async postBooksVendors(requestParameters: BooksApiPostBooksVendorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VendorRow> {
        const response = await this.postBooksVendorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
