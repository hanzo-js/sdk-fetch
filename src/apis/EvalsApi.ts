/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Board,
  DatasetList,
  DatasetReq,
  DatasetView,
  EvaluatorList,
  EvaluatorReq,
  EvaluatorView,
  ItemList,
  ItemReq,
  ItemView,
  RunRequest,
  RunSummary,
  Runs,
  ScoreConfigList,
  ScoreConfigReq,
  ScoreConfigView,
  ScoreList,
  ScoreReq,
  ScoreView,
  TraceList,
} from '../models/index.js';
import {
    BoardFromJSON,
    BoardToJSON,
    DatasetListFromJSON,
    DatasetListToJSON,
    DatasetReqFromJSON,
    DatasetReqToJSON,
    DatasetViewFromJSON,
    DatasetViewToJSON,
    EvaluatorListFromJSON,
    EvaluatorListToJSON,
    EvaluatorReqFromJSON,
    EvaluatorReqToJSON,
    EvaluatorViewFromJSON,
    EvaluatorViewToJSON,
    ItemListFromJSON,
    ItemListToJSON,
    ItemReqFromJSON,
    ItemReqToJSON,
    ItemViewFromJSON,
    ItemViewToJSON,
    RunRequestFromJSON,
    RunRequestToJSON,
    RunSummaryFromJSON,
    RunSummaryToJSON,
    RunsFromJSON,
    RunsToJSON,
    ScoreConfigListFromJSON,
    ScoreConfigListToJSON,
    ScoreConfigReqFromJSON,
    ScoreConfigReqToJSON,
    ScoreConfigViewFromJSON,
    ScoreConfigViewToJSON,
    ScoreListFromJSON,
    ScoreListToJSON,
    ScoreReqFromJSON,
    ScoreReqToJSON,
    ScoreViewFromJSON,
    ScoreViewToJSON,
    TraceListFromJSON,
    TraceListToJSON,
} from '../models/index.js';

export interface EvalsApiDeleteEvalsDatasetsByNameRequest {
    name: string;
}

export interface EvalsApiGetEvalsDatasetsRequest {
    limit?: number;
}

export interface EvalsApiGetEvalsDatasetsByNameRequest {
    name: string;
}

export interface EvalsApiGetEvalsDatasetsByNameItemsRequest {
    name: string;
    limit?: number;
}

export interface EvalsApiGetEvalsEvaluatorsRequest {
    limit?: number;
}

export interface EvalsApiGetEvalsMetricsRequest {
    range?: string;
    interval?: string;
}

export interface EvalsApiGetEvalsRubricsRequest {
    limit?: number;
}

export interface EvalsApiGetEvalsRunsRequest {
    datasetName?: string;
    limit?: number;
}

export interface EvalsApiGetEvalsScoresRequest {
    name?: string;
    runName?: string;
    traceId?: string;
    limit?: number;
}

export interface EvalsApiGetEvalsTracesRequest {
    sessionId?: string;
    runName?: string;
    datasetName?: string;
    limit?: number;
}

export interface EvalsApiPostEvalsDatasetsRequest {
    datasetReq: DatasetReq;
}

export interface EvalsApiPostEvalsDatasetsByNameItemsRequest {
    name: string;
    itemReq: ItemReq;
}

export interface EvalsApiPostEvalsEvaluatorsRequest {
    evaluatorReq: EvaluatorReq;
}

export interface EvalsApiPostEvalsRubricsRequest {
    scoreConfigReq: ScoreConfigReq;
}

export interface EvalsApiPostEvalsRunsRequest {
    runRequest: RunRequest;
    authorization?: string;
}

export interface EvalsApiPostEvalsScoresRequest {
    scoreReq: ScoreReq;
}

/**
 * 
 */
export class EvalsApi extends runtime.BaseAPI {

    /**
     * Removes the named dataset of the caller\'s org AND all of its examples, in one transaction.  This is not a detach: the examples are gone with the set, so a dataset cannot be resurrected by re-creating the name. A name this org does not have is 404 — never a silent success — and a name belonging to another tenant is the same 404, because the delete is predicated on the validated org. Requires a validated principal; 403 without one. Runs and scores already recorded against the dataset are telemetry events and are NOT deleted with it.
     * Removes the named dataset of the caller\'s org AND all of its examples, in one transaction.
     */
    async deleteEvalsDatasetsByNameRaw(requestParameters: EvalsApiDeleteEvalsDatasetsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteEvalsDatasetsByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/datasets/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Removes the named dataset of the caller\'s org AND all of its examples, in one transaction.  This is not a detach: the examples are gone with the set, so a dataset cannot be resurrected by re-creating the name. A name this org does not have is 404 — never a silent success — and a name belonging to another tenant is the same 404, because the delete is predicated on the validated org. Requires a validated principal; 403 without one. Runs and scores already recorded against the dataset are telemetry events and are NOT deleted with it.
     * Removes the named dataset of the caller\'s org AND all of its examples, in one transaction.
     */
    async deleteEvalsDatasetsByName(requestParameters: EvalsApiDeleteEvalsDatasetsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.deleteEvalsDatasetsByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the datasets your org has, each with its name, description, metadata and timestamps.  It is the only way to enumerate what an org holds. Requires a validated principal; 403 without one. Every row is filtered on the validated org, so there is no parameter that reaches another tenant\'s datasets. The item count is NOT populated here — read one dataset to get it.
     * Is the datasets your org has, each with its name, description, metadata and timestamps.
     */
    async getEvalsDatasetsRaw(requestParameters: EvalsApiGetEvalsDatasetsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DatasetList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/datasets`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DatasetListFromJSON(jsonValue));
    }

    /**
     * Is the datasets your org has, each with its name, description, metadata and timestamps.  It is the only way to enumerate what an org holds. Requires a validated principal; 403 without one. Every row is filtered on the validated org, so there is no parameter that reaches another tenant\'s datasets. The item count is NOT populated here — read one dataset to get it.
     * Is the datasets your org has, each with its name, description, metadata and timestamps.
     */
    async getEvalsDatasets(requestParameters: EvalsApiGetEvalsDatasetsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DatasetList> {
        const response = await this.getEvalsDatasetsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one dataset of the caller\'s org by name, together with its live item count — the one read that answers how big the set actually is.  A name this org does not have is 404, which is also what another tenant\'s dataset looks like from here. Requires a validated principal; 403 without one.
     * Returns one dataset of the caller\'s org by name, together with its live item count — the one read that answers how big the set actually is.
     */
    async getEvalsDatasetsByNameRaw(requestParameters: EvalsApiGetEvalsDatasetsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DatasetView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getEvalsDatasetsByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/datasets/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DatasetViewFromJSON(jsonValue));
    }

    /**
     * Returns one dataset of the caller\'s org by name, together with its live item count — the one read that answers how big the set actually is.  A name this org does not have is 404, which is also what another tenant\'s dataset looks like from here. Requires a validated principal; 403 without one.
     * Returns one dataset of the caller\'s org by name, together with its live item count — the one read that answers how big the set actually is.
     */
    async getEvalsDatasetsByName(requestParameters: EvalsApiGetEvalsDatasetsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DatasetView> {
        const response = await this.getEvalsDatasetsByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the examples in one of your datasets — the set is named in the path, because this collection only exists inside one.  Archived examples are included, so the caller sees the whole set rather than only what a run would use. Requires a validated principal; 403 without one, and the read is filtered on the validated org, so naming another tenant\'s dataset returns nothing rather than its contents.
     * Is the examples in one of your datasets — the set is named in the path, because this collection only exists inside one.
     */
    async getEvalsDatasetsByNameItemsRaw(requestParameters: EvalsApiGetEvalsDatasetsByNameItemsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ItemList>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getEvalsDatasetsByNameItems().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/datasets/{name}/items`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ItemListFromJSON(jsonValue));
    }

    /**
     * Is the examples in one of your datasets — the set is named in the path, because this collection only exists inside one.  Archived examples are included, so the caller sees the whole set rather than only what a run would use. Requires a validated principal; 403 without one, and the read is filtered on the validated org, so naming another tenant\'s dataset returns nothing rather than its contents.
     * Is the examples in one of your datasets — the set is named in the path, because this collection only exists inside one.
     */
    async getEvalsDatasetsByNameItems(requestParameters: EvalsApiGetEvalsDatasetsByNameItemsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ItemList> {
        const response = await this.getEvalsDatasetsByNameItemsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the judges your org has defined, each with its judge model, criteria and the score name it writes under.  Requires a validated principal; 403 without one, and the listing is filtered on the validated org.
     * Is the judges your org has defined, each with its judge model, criteria and the score name it writes under.
     */
    async getEvalsEvaluatorsRaw(requestParameters: EvalsApiGetEvalsEvaluatorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EvaluatorList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/evaluators`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EvaluatorListFromJSON(jsonValue));
    }

    /**
     * Is the judges your org has defined, each with its judge model, criteria and the score name it writes under.  Requires a validated principal; 403 without one, and the listing is filtered on the validated org.
     * Is the judges your org has defined, each with its judge model, criteria and the score name it writes under.
     */
    async getEvalsEvaluators(requestParameters: EvalsApiGetEvalsEvaluatorsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EvaluatorList> {
        const response = await this.getEvalsEvaluatorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is your org\'s AI overview board over a window: totals (generations, prompt and completion tokens, cost in cents, errors, success rate, distinct models and users), a gap-filled time series, a per-model breakdown with the long tail folded into \"other\", and latency percentiles read from the GenAI spans.  The window the answer was actually computed over is echoed back, so a client never has to infer it. A platform admin sees the board across ALL orgs; everyone else sees their own.  The board is HONEST-EMPTY where it cannot be computed: with no datastore wired, or under a named project scope the usage ledger does not yet carry, it answers a valid board with zero totals and a flat series rather than a fabricated number or a 500. Requires a validated principal; 403 without one.
     * Is your org\'s AI overview board over a window: totals (generations, prompt and completion tokens, cost in cents, errors, success rate, distinct models and users), a gap-filled time series, a per-model breakdown with the long tail folded into \"other\", and latency percentiles read from the GenAI spans.
     */
    async getEvalsMetricsRaw(requestParameters: EvalsApiGetEvalsMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Board>> {
        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        if (requestParameters['interval'] != null) {
            queryParameters['interval'] = requestParameters['interval'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/metrics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BoardFromJSON(jsonValue));
    }

    /**
     * Is your org\'s AI overview board over a window: totals (generations, prompt and completion tokens, cost in cents, errors, success rate, distinct models and users), a gap-filled time series, a per-model breakdown with the long tail folded into \"other\", and latency percentiles read from the GenAI spans.  The window the answer was actually computed over is echoed back, so a client never has to infer it. A platform admin sees the board across ALL orgs; everyone else sees their own.  The board is HONEST-EMPTY where it cannot be computed: with no datastore wired, or under a named project scope the usage ledger does not yet carry, it answers a valid board with zero totals and a flat series rather than a fabricated number or a 500. Requires a validated principal; 403 without one.
     * Is your org\'s AI overview board over a window: totals (generations, prompt and completion tokens, cost in cents, errors, success rate, distinct models and users), a gap-filled time series, a per-model breakdown with the long tail folded into \"other\", and latency percentiles read from the GenAI spans.
     */
    async getEvalsMetrics(requestParameters: EvalsApiGetEvalsMetricsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Board> {
        const response = await this.getEvalsMetricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the score shapes your org has declared — each name\'s data type, its numeric bounds and its allowed categories.  Requires a validated principal; 403 without one, and the listing is filtered on the validated org.
     * Is the score shapes your org has declared — each name\'s data type, its numeric bounds and its allowed categories.
     */
    async getEvalsRubricsRaw(requestParameters: EvalsApiGetEvalsRubricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScoreConfigList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/rubrics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScoreConfigListFromJSON(jsonValue));
    }

    /**
     * Is the score shapes your org has declared — each name\'s data type, its numeric bounds and its allowed categories.  Requires a validated principal; 403 without one, and the listing is filtered on the validated org.
     * Is the score shapes your org has declared — each name\'s data type, its numeric bounds and its allowed categories.
     */
    async getEvalsRubrics(requestParameters: EvalsApiGetEvalsRubricsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScoreConfigList> {
        const response = await this.getEvalsRubricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is your past runs and how they scored — the dataset and model, the judge model, how many examples were attempted and how many scored, the average score, and when it happened.  Requires a validated principal; 403 without one, and rows are filtered on the validated org. These records come from the metastore rather than the datastore, so they are readable on a deployment with no telemetry wired — but a run\'s traces and scores are not.
     * Is your past runs and how they scored — the dataset and model, the judge model, how many examples were attempted and how many scored, the average score, and when it happened.
     */
    async getEvalsRunsRaw(requestParameters: EvalsApiGetEvalsRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Runs>> {
        const queryParameters: any = {};

        if (requestParameters['datasetName'] != null) {
            queryParameters['datasetName'] = requestParameters['datasetName'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/runs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RunsFromJSON(jsonValue));
    }

    /**
     * Is your past runs and how they scored — the dataset and model, the judge model, how many examples were attempted and how many scored, the average score, and when it happened.  Requires a validated principal; 403 without one, and rows are filtered on the validated org. These records come from the metastore rather than the datastore, so they are readable on a deployment with no telemetry wired — but a run\'s traces and scores are not.
     * Is your past runs and how they scored — the dataset and model, the judge model, how many examples were attempted and how many scored, the average score, and when it happened.
     */
    async getEvalsRuns(requestParameters: EvalsApiGetEvalsRunsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Runs> {
        const response = await this.getEvalsRunsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the score events your org has recorded, narrowed by any of name, runName and traceId.  The org is bound as an authoritative predicate on the query, never taken from a header, so a filter can narrow the caller\'s own scores but can never widen past them. Requires a validated principal; 403 without one. Scores live in the datastore, so a deployment with none wired answers 503 rather than an empty page that would read as \"no scores\".
     * Is the score events your org has recorded, narrowed by any of name, runName and traceId.
     */
    async getEvalsScoresRaw(requestParameters: EvalsApiGetEvalsScoresRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScoreList>> {
        const queryParameters: any = {};

        if (requestParameters['name'] != null) {
            queryParameters['name'] = requestParameters['name'];
        }

        if (requestParameters['runName'] != null) {
            queryParameters['runName'] = requestParameters['runName'];
        }

        if (requestParameters['traceId'] != null) {
            queryParameters['traceId'] = requestParameters['traceId'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/scores`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScoreListFromJSON(jsonValue));
    }

    /**
     * Is the score events your org has recorded, narrowed by any of name, runName and traceId.  The org is bound as an authoritative predicate on the query, never taken from a header, so a filter can narrow the caller\'s own scores but can never widen past them. Requires a validated principal; 403 without one. Scores live in the datastore, so a deployment with none wired answers 503 rather than an empty page that would read as \"no scores\".
     * Is the score events your org has recorded, narrowed by any of name, runName and traceId.
     */
    async getEvalsScores(requestParameters: EvalsApiGetEvalsScoresRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScoreList> {
        const response = await this.getEvalsScoresRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the traces behind your evaluations — one per model call an evaluation made, carrying its input, output, model and timing — narrowed by any of sessionId, runName and datasetName.  Scoped by org AND by project: the project is the caller\'s server-minted scope, not a parameter, so it cannot be widened by asking. Requires a validated principal; 403 without one. Traces live in the datastore, so a deployment with none wired answers 503 rather than an empty page.
     * Is the traces behind your evaluations — one per model call an evaluation made, carrying its input, output, model and timing — narrowed by any of sessionId, runName and datasetName.
     */
    async getEvalsTracesRaw(requestParameters: EvalsApiGetEvalsTracesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TraceList>> {
        const queryParameters: any = {};

        if (requestParameters['sessionId'] != null) {
            queryParameters['sessionId'] = requestParameters['sessionId'];
        }

        if (requestParameters['runName'] != null) {
            queryParameters['runName'] = requestParameters['runName'];
        }

        if (requestParameters['datasetName'] != null) {
            queryParameters['datasetName'] = requestParameters['datasetName'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/traces`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TraceListFromJSON(jsonValue));
    }

    /**
     * Is the traces behind your evaluations — one per model call an evaluation made, carrying its input, output, model and timing — narrowed by any of sessionId, runName and datasetName.  Scoped by org AND by project: the project is the caller\'s server-minted scope, not a parameter, so it cannot be widened by asking. Requires a validated principal; 403 without one. Traces live in the datastore, so a deployment with none wired answers 503 rather than an empty page.
     * Is the traces behind your evaluations — one per model call an evaluation made, carrying its input, output, model and timing — narrowed by any of sessionId, runName and datasetName.
     */
    async getEvalsTraces(requestParameters: EvalsApiGetEvalsTracesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TraceList> {
        const response = await this.getEvalsTracesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes a dataset — the named set of graded examples a run scores a model against — under the caller\'s org and answers 201 with it.  The NAME is the key, not an id: posting a name the org already has updates that dataset\'s description and metadata and keeps its original creation time, so this is create-or-edit and never a duplicate. Its items are untouched.  Requires a validated principal; 403 without one. The org comes from the validated owner claim, never from a client X-Org-Id, so a dataset can only ever be written under the caller\'s own tenant. A description over 64 KiB is 400.
     * Writes a dataset — the named set of graded examples a run scores a model against — under the caller\'s org and answers 201 with it.
     */
    async postEvalsDatasetsRaw(requestParameters: EvalsApiPostEvalsDatasetsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DatasetView>> {
        if (requestParameters['datasetReq'] == null) {
            throw new runtime.RequiredError(
                'datasetReq',
                'Required parameter "datasetReq" was null or undefined when calling postEvalsDatasets().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/datasets`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DatasetReqToJSON(requestParameters['datasetReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DatasetViewFromJSON(jsonValue));
    }

    /**
     * Writes a dataset — the named set of graded examples a run scores a model against — under the caller\'s org and answers 201 with it.  The NAME is the key, not an id: posting a name the org already has updates that dataset\'s description and metadata and keeps its original creation time, so this is create-or-edit and never a duplicate. Its items are untouched.  Requires a validated principal; 403 without one. The org comes from the validated owner claim, never from a client X-Org-Id, so a dataset can only ever be written under the caller\'s own tenant. A description over 64 KiB is 400.
     * Writes a dataset — the named set of graded examples a run scores a model against — under the caller\'s org and answers 201 with it.
     */
    async postEvalsDatasets(requestParameters: EvalsApiPostEvalsDatasetsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DatasetView> {
        const response = await this.postEvalsDatasetsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes one graded example — its input, its expected output, free-form metadata and a status — into the dataset named in the path, and answers 201 with it.  That dataset MUST already exist for this org: an unknown one is 404, never a silent create. Requires a validated principal; 403 without one.
     * Writes one graded example — its input, its expected output, free-form metadata and a status — into the dataset named in the path, and answers 201 with it.
     */
    async postEvalsDatasetsByNameItemsRaw(requestParameters: EvalsApiPostEvalsDatasetsByNameItemsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ItemView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postEvalsDatasetsByNameItems().'
            );
        }

        if (requestParameters['itemReq'] == null) {
            throw new runtime.RequiredError(
                'itemReq',
                'Required parameter "itemReq" was null or undefined when calling postEvalsDatasetsByNameItems().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/datasets/{name}/items`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ItemReqToJSON(requestParameters['itemReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ItemViewFromJSON(jsonValue));
    }

    /**
     * Writes one graded example — its input, its expected output, free-form metadata and a status — into the dataset named in the path, and answers 201 with it.  That dataset MUST already exist for this org: an unknown one is 404, never a silent create. Requires a validated principal; 403 without one.
     * Writes one graded example — its input, its expected output, free-form metadata and a status — into the dataset named in the path, and answers 201 with it.
     */
    async postEvalsDatasetsByNameItems(requestParameters: EvalsApiPostEvalsDatasetsByNameItemsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ItemView> {
        const response = await this.postEvalsDatasetsByNameItemsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Saves a reusable judge for the caller\'s org — the judge model and the written criteria it grades against — and answers 201 with it.  Like a dataset, the NAME is the key: re-posting a name edits that judge rather than adding a second one. Requires a validated principal; 403 without one.
     * Saves a reusable judge for the caller\'s org — the judge model and the written criteria it grades against — and answers 201 with it.
     */
    async postEvalsEvaluatorsRaw(requestParameters: EvalsApiPostEvalsEvaluatorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EvaluatorView>> {
        if (requestParameters['evaluatorReq'] == null) {
            throw new runtime.RequiredError(
                'evaluatorReq',
                'Required parameter "evaluatorReq" was null or undefined when calling postEvalsEvaluators().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/evaluators`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EvaluatorReqToJSON(requestParameters['evaluatorReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EvaluatorViewFromJSON(jsonValue));
    }

    /**
     * Saves a reusable judge for the caller\'s org — the judge model and the written criteria it grades against — and answers 201 with it.  Like a dataset, the NAME is the key: re-posting a name edits that judge rather than adding a second one. Requires a validated principal; 403 without one.
     * Saves a reusable judge for the caller\'s org — the judge model and the written criteria it grades against — and answers 201 with it.
     */
    async postEvalsEvaluators(requestParameters: EvalsApiPostEvalsEvaluatorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EvaluatorView> {
        const response = await this.postEvalsEvaluatorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Defines the shape of one score name for the caller\'s org and answers 201 with it.  This is the integrity contract, not documentation: once a rubric exists for a name, every score recorded under that name is checked against it and the rubric\'s data type is AUTHORITATIVE — a caller cannot claim a different one. Out-of-range values, unlisted labels and non-finite numbers are refused at write time.  A CATEGORICAL rubric with no categories is 400, as is a non-finite bound or a minValue above maxValue. Requires a validated principal; 403 without one.
     * Defines the shape of one score name for the caller\'s org and answers 201 with it.
     */
    async postEvalsRubricsRaw(requestParameters: EvalsApiPostEvalsRubricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScoreConfigView>> {
        if (requestParameters['scoreConfigReq'] == null) {
            throw new runtime.RequiredError(
                'scoreConfigReq',
                'Required parameter "scoreConfigReq" was null or undefined when calling postEvalsRubrics().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/rubrics`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScoreConfigReqToJSON(requestParameters['scoreConfigReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScoreConfigViewFromJSON(jsonValue));
    }

    /**
     * Defines the shape of one score name for the caller\'s org and answers 201 with it.  This is the integrity contract, not documentation: once a rubric exists for a name, every score recorded under that name is checked against it and the rubric\'s data type is AUTHORITATIVE — a caller cannot claim a different one. Out-of-range values, unlisted labels and non-finite numbers are refused at write time.  A CATEGORICAL rubric with no categories is 400, as is a non-finite bound or a minValue above maxValue. Requires a validated principal; 403 without one.
     * Defines the shape of one score name for the caller\'s org and answers 201 with it.
     */
    async postEvalsRubrics(requestParameters: EvalsApiPostEvalsRubricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScoreConfigView> {
        const response = await this.postEvalsRubricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs a real evaluation and answers the summary when it is finished — this is synchronous work, not a job id.  For each ACTIVE example in the dataset it calls the model under test, records a trace, calls the LLM-as-judge, and records the judge\'s score with its reasoning. The answer carries the per-item results (item id, trace id, score, output or error) alongside items, scored and avgScore.  The dataset must belong to the caller\'s org (404 otherwise) and must have at least one ACTIVE example (422 otherwise).  It runs as YOU: the caller\'s own Authorization bearer drives the model gateway, so a request without one is 401 rather than a run made anonymously or under a service identity. Only a non-reversible hash of that credential is recorded on the traces.  Bounded and honest about it: an org may have at most 4 runs in flight and the fifth is 429 rather than queued, and the whole run is capped at 10 minutes — examples past the deadline come back with an error instead of a score, and scored counts only real successes. A run where NOTHING scored answers 502, not a 200 that looks like an evaluation. A run must be able to persist what it produces, so a deployment with no datastore wired is 503 up front. Requires a validated principal; 403 without one.
     * Runs a real evaluation and answers the summary when it is finished — this is synchronous work, not a job id.
     */
    async postEvalsRunsRaw(requestParameters: EvalsApiPostEvalsRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RunSummary>> {
        if (requestParameters['runRequest'] == null) {
            throw new runtime.RequiredError(
                'runRequest',
                'Required parameter "runRequest" was null or undefined when calling postEvalsRuns().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/runs`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RunRequestToJSON(requestParameters['runRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RunSummaryFromJSON(jsonValue));
    }

    /**
     * Runs a real evaluation and answers the summary when it is finished — this is synchronous work, not a job id.  For each ACTIVE example in the dataset it calls the model under test, records a trace, calls the LLM-as-judge, and records the judge\'s score with its reasoning. The answer carries the per-item results (item id, trace id, score, output or error) alongside items, scored and avgScore.  The dataset must belong to the caller\'s org (404 otherwise) and must have at least one ACTIVE example (422 otherwise).  It runs as YOU: the caller\'s own Authorization bearer drives the model gateway, so a request without one is 401 rather than a run made anonymously or under a service identity. Only a non-reversible hash of that credential is recorded on the traces.  Bounded and honest about it: an org may have at most 4 runs in flight and the fifth is 429 rather than queued, and the whole run is capped at 10 minutes — examples past the deadline come back with an error instead of a score, and scored counts only real successes. A run where NOTHING scored answers 502, not a 200 that looks like an evaluation. A run must be able to persist what it produces, so a deployment with no datastore wired is 503 up front. Requires a validated principal; 403 without one.
     * Runs a real evaluation and answers the summary when it is finished — this is synchronous work, not a job id.
     */
    async postEvalsRuns(requestParameters: EvalsApiPostEvalsRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RunSummary> {
        const response = await this.postEvalsRunsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Files one score event for the caller\'s org and answers 201 with it.  This is how human review and out-of-band graders land beside the automatic ones: name the score, give it a value (or a stringValue for a categorical label), and attach it to a trace, a run, a dataset example, or any combination.  Scores are validated fail-closed. A value must be FINITE — NaN and Inf are 400 — and if the org has declared a rubric for this name, that rubric decides the type and the value must satisfy it: inside the numeric bounds, or one of the allowed categories. A caller cannot override the declared type by sending a different dataType.  A score is TELEMETRY, not metadata, so it needs the datastore: a deployment with none wired answers 503 rather than accepting a score it cannot persist. Requires a validated principal; 403 without one, and the org is stamped from the validated claim rather than read off the body.
     * Files one score event for the caller\'s org and answers 201 with it.
     */
    async postEvalsScoresRaw(requestParameters: EvalsApiPostEvalsScoresRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScoreView>> {
        if (requestParameters['scoreReq'] == null) {
            throw new runtime.RequiredError(
                'scoreReq',
                'Required parameter "scoreReq" was null or undefined when calling postEvalsScores().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/evals/scores`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScoreReqToJSON(requestParameters['scoreReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScoreViewFromJSON(jsonValue));
    }

    /**
     * Files one score event for the caller\'s org and answers 201 with it.  This is how human review and out-of-band graders land beside the automatic ones: name the score, give it a value (or a stringValue for a categorical label), and attach it to a trace, a run, a dataset example, or any combination.  Scores are validated fail-closed. A value must be FINITE — NaN and Inf are 400 — and if the org has declared a rubric for this name, that rubric decides the type and the value must satisfy it: inside the numeric bounds, or one of the allowed categories. A caller cannot override the declared type by sending a different dataType.  A score is TELEMETRY, not metadata, so it needs the datastore: a deployment with none wired answers 503 rather than accepting a score it cannot persist. Requires a validated principal; 403 without one, and the org is stamped from the validated claim rather than read off the body.
     * Files one score event for the caller\'s org and answers 201 with it.
     */
    async postEvalsScores(requestParameters: EvalsApiPostEvalsScoresRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScoreView> {
        const response = await this.postEvalsScoresRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
