/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  KitList,
  PublishKitIn,
  ReplaceKitIn,
  StarterKit,
} from '../models/index.js';
import {
    KitListFromJSON,
    KitListToJSON,
    PublishKitInFromJSON,
    PublishKitInToJSON,
    ReplaceKitInFromJSON,
    ReplaceKitInToJSON,
    StarterKitFromJSON,
    StarterKitToJSON,
} from '../models/index.js';

export interface TemplateApiDeleteTemplateBySlugRequest {
    slug: string;
}

export interface TemplateApiGetTemplateBySlugRequest {
    slug: string;
}

export interface TemplateApiPostTemplateRequest {
    publishKitIn: PublishKitIn;
}

export interface TemplateApiPutTemplateBySlugRequest {
    slug: string;
    replaceKitIn: ReplaceKitIn;
}

/**
 * 
 */
export class TemplateApi extends runtime.BaseAPI {

    /**
     * Deletes the caller org\'s OWN starter kit. A slug they do not own is a 404, never a delete: the DELETE binds org.
     * Deletes the caller org\'s OWN starter kit.
     */
    async deleteTemplateBySlugRaw(requestParameters: TemplateApiDeleteTemplateBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling deleteTemplateBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/template/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the caller org\'s OWN starter kit. A slug they do not own is a 404, never a delete: the DELETE binds org.
     * Deletes the caller org\'s OWN starter kit.
     */
    async deleteTemplateBySlug(requestParameters: TemplateApiDeleteTemplateBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTemplateBySlugRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits. No request field can widen the scope: the org comes from the validated principal, so an anonymous or cross-org caller structurally sees the public catalog only.
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits.
     */
    async getTemplateRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KitList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/template`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KitListFromJSON(jsonValue));
    }

    /**
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits. No request field can widen the scope: the org comes from the validated principal, so an anonymous or cross-org caller structurally sees the public catalog only.
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits.
     */
    async getTemplate(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KitList> {
        const response = await this.getTemplateRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s. A slug another org owns reads as not found.
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s.
     */
    async getTemplateBySlugRaw(requestParameters: TemplateApiGetTemplateBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StarterKit>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling getTemplateBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/template/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StarterKitFromJSON(jsonValue));
    }

    /**
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s. A slug another org owns reads as not found.
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s.
     */
    async getTemplateBySlug(requestParameters: TemplateApiGetTemplateBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StarterKit> {
        const response = await this.getTemplateBySlugRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit. The owner is stamped by the server, so a body \"org\" is never trusted; publishing over a public-catalog slug is 409, so a slug still names exactly one kit.
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit.
     */
    async postTemplateRaw(requestParameters: TemplateApiPostTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StarterKit>> {
        if (requestParameters['publishKitIn'] == null) {
            throw new runtime.RequiredError(
                'publishKitIn',
                'Required parameter "publishKitIn" was null or undefined when calling postTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/template`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PublishKitInToJSON(requestParameters['publishKitIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StarterKitFromJSON(jsonValue));
    }

    /**
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit. The owner is stamped by the server, so a body \"org\" is never trusted; publishing over a public-catalog slug is 409, so a slug still names exactly one kit.
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit.
     */
    async postTemplate(requestParameters: TemplateApiPostTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StarterKit> {
        const response = await this.postTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit. A slug they do not own is 404, never a create: the UPDATE binds org, so a PUT can never reach another org\'s kit.
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit.
     */
    async putTemplateBySlugRaw(requestParameters: TemplateApiPutTemplateBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StarterKit>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling putTemplateBySlug().'
            );
        }

        if (requestParameters['replaceKitIn'] == null) {
            throw new runtime.RequiredError(
                'replaceKitIn',
                'Required parameter "replaceKitIn" was null or undefined when calling putTemplateBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/template/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ReplaceKitInToJSON(requestParameters['replaceKitIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StarterKitFromJSON(jsonValue));
    }

    /**
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit. A slug they do not own is 404, never a create: the UPDATE binds org, so a PUT can never reach another org\'s kit.
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit.
     */
    async putTemplateBySlug(requestParameters: TemplateApiPutTemplateBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StarterKit> {
        const response = await this.putTemplateBySlugRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
