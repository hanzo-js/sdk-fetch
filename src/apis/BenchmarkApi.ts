/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Admission,
  BenchmarkCatalog,
  Leaderboard,
  Pairing,
  Preset,
  PresetAccepted,
  PresetList,
  Suite,
} from '../models/index.js';
import {
    AdmissionFromJSON,
    AdmissionToJSON,
    BenchmarkCatalogFromJSON,
    BenchmarkCatalogToJSON,
    LeaderboardFromJSON,
    LeaderboardToJSON,
    PairingFromJSON,
    PairingToJSON,
    PresetFromJSON,
    PresetToJSON,
    PresetAcceptedFromJSON,
    PresetAcceptedToJSON,
    PresetListFromJSON,
    PresetListToJSON,
    SuiteFromJSON,
    SuiteToJSON,
} from '../models/index.js';

export interface BenchmarkApiGetBenchmarkCompareRequest {
    a: string;
    b: string;
    benchmark?: string;
}

export interface BenchmarkApiGetBenchmarkLeaderboardRequest {
    benchmark?: string;
}

export interface BenchmarkApiPostBenchmarkPresetsRequest {
    preset: Preset;
}

export interface BenchmarkApiPostBenchmarkRunsRequest {
    suite: Suite;
}

/**
 * 
 */
export class BenchmarkApi extends runtime.BaseAPI {

    /**
     * Is the canonical public benchmarks this arena runs — the id, title, axis, item count and upstream source of each, with native marking the ones the standardized harness runs today; the rest are registered and adapter-pending.  These ids are the vocabulary the rest of the surface takes: a run names them, and the leaderboard and compare read them from ?benchmark=. The catalog is deployment-wide and identical for every caller — there is no tenant in it.
     * Is the canonical public benchmarks this arena runs — the id, title, axis, item count and upstream source of each, with native marking the ones the standardized harness runs today; the rest are registered and adapter-pending.
     */
    async getBenchmarkCatalogRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BenchmarkCatalog>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/benchmark/catalog`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BenchmarkCatalogFromJSON(jsonValue));
    }

    /**
     * Is the canonical public benchmarks this arena runs — the id, title, axis, item count and upstream source of each, with native marking the ones the standardized harness runs today; the rest are registered and adapter-pending.  These ids are the vocabulary the rest of the surface takes: a run names them, and the leaderboard and compare read them from ?benchmark=. The catalog is deployment-wide and identical for every caller — there is no tenant in it.
     * Is the canonical public benchmarks this arena runs — the id, title, axis, item count and upstream source of each, with native marking the ones the standardized harness runs today; the rest are registered and adapter-pending.
     */
    async getBenchmarkCatalog(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BenchmarkCatalog> {
        const response = await this.getBenchmarkCatalogRaw(initOverrides);
        return await response.value();
    }

    /**
     * Is the ONLY valid arm-vs-arm test: it pairs the two models on the items BOTH completed, and answers rescue and damage counts with an exact-McNemar p.  Pairing is what prevents the subset artifact — comparing one model\'s easy subset against another\'s full run — so n_common, not either arm\'s own coverage, is the number to read this by.  Both a and b are required. The benchmark defaults to gpqa_diamond.
     * Is the ONLY valid arm-vs-arm test: it pairs the two models on the items BOTH completed, and answers rescue and damage counts with an exact-McNemar p.
     */
    async getBenchmarkCompareRaw(requestParameters: BenchmarkApiGetBenchmarkCompareRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Pairing>> {
        if (requestParameters['a'] == null) {
            throw new runtime.RequiredError(
                'a',
                'Required parameter "a" was null or undefined when calling getBenchmarkCompare().'
            );
        }

        if (requestParameters['b'] == null) {
            throw new runtime.RequiredError(
                'b',
                'Required parameter "b" was null or undefined when calling getBenchmarkCompare().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['benchmark'] != null) {
            queryParameters['benchmark'] = requestParameters['benchmark'];
        }

        if (requestParameters['a'] != null) {
            queryParameters['a'] = requestParameters['a'];
        }

        if (requestParameters['b'] != null) {
            queryParameters['b'] = requestParameters['b'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/benchmark/compare`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PairingFromJSON(jsonValue));
    }

    /**
     * Is the ONLY valid arm-vs-arm test: it pairs the two models on the items BOTH completed, and answers rescue and damage counts with an exact-McNemar p.  Pairing is what prevents the subset artifact — comparing one model\'s easy subset against another\'s full run — so n_common, not either arm\'s own coverage, is the number to read this by.  Both a and b are required. The benchmark defaults to gpqa_diamond.
     * Is the ONLY valid arm-vs-arm test: it pairs the two models on the items BOTH completed, and answers rescue and damage counts with an exact-McNemar p.
     */
    async getBenchmarkCompare(requestParameters: BenchmarkApiGetBenchmarkCompareRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Pairing> {
        const response = await this.getBenchmarkCompareRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers one row per model for the benchmark named — what our own harness measured, beside what the vendor claims, and the gap between them.  The gap is the point of the arena; provider-reported claims have run materially hot against one standardized harness.  The two planes are NEVER blended, and that is the rule to read the rows by: a model we have measured but no vendor has claimed for shows published null, a model with only a claim shows measured null, and gap exists only where both do.  n is coverage and is not decoration: two measured numbers taken over different item counts are not comparable, so read the row\'s n before reading its accuracy.
     * Answers one row per model for the benchmark named — what our own harness measured, beside what the vendor claims, and the gap between them.
     */
    async getBenchmarkLeaderboardRaw(requestParameters: BenchmarkApiGetBenchmarkLeaderboardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Leaderboard>> {
        const queryParameters: any = {};

        if (requestParameters['benchmark'] != null) {
            queryParameters['benchmark'] = requestParameters['benchmark'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/benchmark/leaderboard`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LeaderboardFromJSON(jsonValue));
    }

    /**
     * Answers one row per model for the benchmark named — what our own harness measured, beside what the vendor claims, and the gap between them.  The gap is the point of the arena; provider-reported claims have run materially hot against one standardized harness.  The two planes are NEVER blended, and that is the rule to read the rows by: a model we have measured but no vendor has claimed for shows published null, a model with only a claim shows measured null, and gap exists only where both do.  n is coverage and is not decoration: two measured numbers taken over different item counts are not comparable, so read the row\'s n before reading its accuracy.
     * Answers one row per model for the benchmark named — what our own harness measured, beside what the vendor claims, and the gap between them.
     */
    async getBenchmarkLeaderboard(requestParameters: BenchmarkApiGetBenchmarkLeaderboardRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Leaderboard> {
        const response = await this.getBenchmarkLeaderboardRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Are the router blends available to compose from — a named set of model arms, the rank they escalate through and the panel width that bounds fan-out — each served by the model layer as enso-<name>.  Today it answers exactly one row, the reference blend: a worked example written in models we name, published as an example of the FORM. It is deliberately not the composition of a Hanzo-served tier — the tier name exists to abstract that — so fork it and swap arms by what the leaderboard measures on your own tasks rather than reading it as a disclosure.
     * Are the router blends available to compose from — a named set of model arms, the rank they escalate through and the panel width that bounds fan-out — each served by the model layer as enso-<name>.
     */
    async getBenchmarkPresetsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PresetList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/benchmark/presets`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PresetListFromJSON(jsonValue));
    }

    /**
     * Are the router blends available to compose from — a named set of model arms, the rank they escalate through and the panel width that bounds fan-out — each served by the model layer as enso-<name>.  Today it answers exactly one row, the reference blend: a worked example written in models we name, published as an example of the FORM. It is deliberately not the composition of a Hanzo-served tier — the tier name exists to abstract that — so fork it and swap arms by what the leaderboard measures on your own tasks rather than reading it as a disclosure.
     * Are the router blends available to compose from — a named set of model arms, the rank they escalate through and the panel width that bounds fan-out — each served by the model layer as enso-<name>.
     */
    async getBenchmarkPresets(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PresetList> {
        const response = await this.getBenchmarkPresetsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Validates a router blend — its name, its arms, the rank they escalate through and the panel fan-out width — and answers 202 with the preset and the enso-<name> it would be served as.  It VALIDATES AND ECHOES: the definition is not persisted yet, so a preset accepted here is not one the model layer will resolve. Treat the response as a check on the blend, not a promise to serve it.  Defaults fill the shape rather than refusing it: an omitted rank becomes the arms in declared order and a panel below 1 becomes 1. The one real invariant is that rank may only name arms the blend declares — the same rule the model catalog enforces — and a rank naming anything else is a 422 listing exactly which entries were undeclared. A blend with no name or no arms is a 400.
     * Validates a router blend — its name, its arms, the rank they escalate through and the panel fan-out width — and answers 202 with the preset and the enso-<name> it would be served as.
     */
    async postBenchmarkPresetsRaw(requestParameters: BenchmarkApiPostBenchmarkPresetsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PresetAccepted>> {
        if (requestParameters['preset'] == null) {
            throw new runtime.RequiredError(
                'preset',
                'Required parameter "preset" was null or undefined when calling postBenchmarkPresets().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/benchmark/presets`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PresetToJSON(requestParameters['preset']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PresetAcceptedFromJSON(jsonValue));
    }

    /**
     * Validates a router blend — its name, its arms, the rank they escalate through and the panel fan-out width — and answers 202 with the preset and the enso-<name> it would be served as.  It VALIDATES AND ECHOES: the definition is not persisted yet, so a preset accepted here is not one the model layer will resolve. Treat the response as a check on the blend, not a promise to serve it.  Defaults fill the shape rather than refusing it: an omitted rank becomes the arms in declared order and a panel below 1 becomes 1. The one real invariant is that rank may only name arms the blend declares — the same rule the model catalog enforces — and a rank naming anything else is a 422 listing exactly which entries were undeclared. A blend with no name or no arms is a 400.
     * Validates a router blend — its name, its arms, the rank they escalate through and the panel fan-out width — and answers 202 with the preset and the enso-<name> it would be served as.
     */
    async postBenchmarkPresets(requestParameters: BenchmarkApiPostBenchmarkPresetsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PresetAccepted> {
        const response = await this.postBenchmarkPresetsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Admits and queues a benchmark run against a model or your own endpoint, and answers 202 with the receipt.  It is an ADMISSION, not a result: the work is done by the harness afterwards and the numbers appear on the leaderboard as it completes them.  Cost is bounded by the store rather than by a quota: attempts are append-only and keyed by (benchmark, item, model), so an (item, model) pair already attempted is skipped instead of re-spent, and re-queuing the same run is close to free.  Validation is up front and total — a request with neither model nor endpoint is a 400, one with no benchmarks is a 400, and any benchmark id outside the catalog is a 422 naming exactly which ids were unknown, so a typo never silently queues a partial run.
     * Admits and queues a benchmark run against a model or your own endpoint, and answers 202 with the receipt.
     */
    async postBenchmarkRunsRaw(requestParameters: BenchmarkApiPostBenchmarkRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Admission>> {
        if (requestParameters['suite'] == null) {
            throw new runtime.RequiredError(
                'suite',
                'Required parameter "suite" was null or undefined when calling postBenchmarkRuns().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/benchmark/runs`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SuiteToJSON(requestParameters['suite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdmissionFromJSON(jsonValue));
    }

    /**
     * Admits and queues a benchmark run against a model or your own endpoint, and answers 202 with the receipt.  It is an ADMISSION, not a result: the work is done by the harness afterwards and the numbers appear on the leaderboard as it completes them.  Cost is bounded by the store rather than by a quota: attempts are append-only and keyed by (benchmark, item, model), so an (item, model) pair already attempted is skipped instead of re-spent, and re-queuing the same run is close to free.  Validation is up front and total — a request with neither model nor endpoint is a 400, one with no benchmarks is a 400, and any benchmark id outside the catalog is a 422 naming exactly which ids were unknown, so a typo never silently queues a partial run.
     * Admits and queues a benchmark run against a model or your own endpoint, and answers 202 with the receipt.
     */
    async postBenchmarkRuns(requestParameters: BenchmarkApiPostBenchmarkRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Admission> {
        const response = await this.postBenchmarkRunsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
