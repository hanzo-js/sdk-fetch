/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Answer,
  AskAnswer,
  AskPostIn,
  ContextBundle,
  ContextIn,
  FileContent,
  IndexIn,
  IndexResult,
  Query,
  RepoTree,
  SearchResults,
} from '../models/index.js';
import {
    AnswerFromJSON,
    AnswerToJSON,
    AskAnswerFromJSON,
    AskAnswerToJSON,
    AskPostInFromJSON,
    AskPostInToJSON,
    ContextBundleFromJSON,
    ContextBundleToJSON,
    ContextInFromJSON,
    ContextInToJSON,
    FileContentFromJSON,
    FileContentToJSON,
    IndexInFromJSON,
    IndexInToJSON,
    IndexResultFromJSON,
    IndexResultToJSON,
    QueryFromJSON,
    QueryToJSON,
    RepoTreeFromJSON,
    RepoTreeToJSON,
    SearchResultsFromJSON,
    SearchResultsToJSON,
} from '../models/index.js';

export interface CodeApiGetCodeAskRequest {
    q?: string;
    repo?: string;
}

export interface CodeApiGetCodeFileRequest {
    path?: string;
    repo?: string;
}

export interface CodeApiGetCodeSearchRequest {
    q?: string;
    type?: string;
    repo?: string;
    limit?: number;
}

export interface CodeApiGetCodeTreeRequest {
    repo?: string;
}

export interface CodeApiPostCodeAskRequest {
    askPostIn: AskPostIn;
}

export interface CodeApiPostCodeContextRequest {
    contextIn: ContextIn;
}

export interface CodeApiPostCodeIndexRequest {
    indexIn: IndexIn;
}

export interface CodeApiPostCodeLspCompleteRequest {
    query: Query;
}

export interface CodeApiPostCodeLspDiagnosticsRequest {
    query: Query;
}

export interface CodeApiPostCodeLspHoverRequest {
    query: Query;
}

export interface CodeApiPostCodeLspLocateRequest {
    query: Query;
}

export interface CodeApiPostCodeLspSymbolsRequest {
    query: Query;
}

/**
 * 
 */
export class CodeApi extends runtime.BaseAPI {

    /**
     * Answers a question about the caller org\'s code with a CITED answer: retrieval packs grounding context, then the synthesizer writes the answer over exactly those spans, which come back alongside it. It never answers without grounding — with no matched code the answer is empty and says so, and with no synthesizer available the citations still come back with \"degraded\": true so the caller can reason over the spans itself.
     * Answers a question about the caller org\'s code with a CITED answer: retrieval packs grounding context, then the synthesizer writes the answer over exactly those spans, which come back alongside it.
     */
    async getCodeAskRaw(requestParameters: CodeApiGetCodeAskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AskAnswer>> {
        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/ask`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AskAnswerFromJSON(jsonValue));
    }

    /**
     * Answers a question about the caller org\'s code with a CITED answer: retrieval packs grounding context, then the synthesizer writes the answer over exactly those spans, which come back alongside it. It never answers without grounding — with no matched code the answer is empty and says so, and with no synthesizer available the citations still come back with \"degraded\": true so the caller can reason over the spans itself.
     * Answers a question about the caller org\'s code with a CITED answer: retrieval packs grounding context, then the synthesizer writes the answer over exactly those spans, which come back alongside it.
     */
    async getCodeAsk(requestParameters: CodeApiGetCodeAskRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AskAnswer> {
        const response = await this.getCodeAskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the INDEXED content of one file — read_file over the chunks the search tiers hold, for pulling up code an agent just found. It is NOT byte-verbatim: the git object plane is the source of record for exact bytes, history and blame. A file absent from the index is a 404, so an agent can tell \"not indexed\" from \"empty file\".
     * Returns the INDEXED content of one file — read_file over the chunks the search tiers hold, for pulling up code an agent just found.
     */
    async getCodeFileRaw(requestParameters: CodeApiGetCodeFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileContent>> {
        const queryParameters: any = {};

        if (requestParameters['path'] != null) {
            queryParameters['path'] = requestParameters['path'];
        }

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/file`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileContentFromJSON(jsonValue));
    }

    /**
     * Returns the INDEXED content of one file — read_file over the chunks the search tiers hold, for pulling up code an agent just found. It is NOT byte-verbatim: the git object plane is the source of record for exact bytes, history and blame. A file absent from the index is a 404, so an agent can tell \"not indexed\" from \"empty file\".
     * Returns the INDEXED content of one file — read_file over the chunks the search tiers hold, for pulling up code an agent just found.
     */
    async getCodeFile(requestParameters: CodeApiGetCodeFileRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileContent> {
        const response = await this.getCodeFileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Finds code in the caller org\'s index across three orthogonal retrieval tiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over code-tokenized text), symbolic (real definition and reference edges), and semantic (embedding cosine over AST-boundary chunks). Pick one tier with `type`, or leave it to run all three as hybrid, which is what a coding agent usually wants. It is FAIL-HONEST: a retrieval outage answers 200 with an empty result set and \"degraded\": true rather than a 5xx, so an agent degrades instead of stalling. A malformed regex is a 400.
     * Finds code in the caller org\'s index across three orthogonal retrieval tiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over code-tokenized text), symbolic (real definition and reference edges), and semantic (embedding cosine over AST-boundary chunks).
     */
    async getCodeSearchRaw(requestParameters: CodeApiGetCodeSearchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchResults>> {
        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['type'] != null) {
            queryParameters['type'] = requestParameters['type'];
        }

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/search`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchResultsFromJSON(jsonValue));
    }

    /**
     * Finds code in the caller org\'s index across three orthogonal retrieval tiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over code-tokenized text), symbolic (real definition and reference edges), and semantic (embedding cosine over AST-boundary chunks). Pick one tier with `type`, or leave it to run all three as hybrid, which is what a coding agent usually wants. It is FAIL-HONEST: a retrieval outage answers 200 with an empty result set and \"degraded\": true rather than a 5xx, so an agent degrades instead of stalling. A malformed regex is a 400.
     * Finds code in the caller org\'s index across three orthogonal retrieval tiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over code-tokenized text), symbolic (real definition and reference edges), and semantic (embedding cosine over AST-boundary chunks).
     */
    async getCodeSearch(requestParameters: CodeApiGetCodeSearchRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchResults> {
        const response = await this.getCodeSearchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one repository\'s file structure with a per-file symbol count — get_repo_structure over the org\'s own index, with no git checkout involved. A repository that has not been indexed answers an empty tree rather than an error, so an agent can tell \"nothing here\" without handling a failure.
     * Returns one repository\'s file structure with a per-file symbol count — get_repo_structure over the org\'s own index, with no git checkout involved.
     */
    async getCodeTreeRaw(requestParameters: CodeApiGetCodeTreeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RepoTree>> {
        const queryParameters: any = {};

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/tree`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RepoTreeFromJSON(jsonValue));
    }

    /**
     * Returns one repository\'s file structure with a per-file symbol count — get_repo_structure over the org\'s own index, with no git checkout involved. A repository that has not been indexed answers an empty tree rather than an error, so an agent can tell \"nothing here\" without handling a failure.
     * Returns one repository\'s file structure with a per-file symbol count — get_repo_structure over the org\'s own index, with no git checkout involved.
     */
    async getCodeTree(requestParameters: CodeApiGetCodeTreeRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RepoTree> {
        const response = await this.getCodeTreeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is askGet with the question in the request BODY, for a question too long or too awkward to put in a URL. `query` and `repo` in the body take precedence over `?q=` and `?repo=`; either source works alone.
     * Is askGet with the question in the request BODY, for a question too long or too awkward to put in a URL.
     */
    async postCodeAskRaw(requestParameters: CodeApiPostCodeAskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AskAnswer>> {
        if (requestParameters['askPostIn'] == null) {
            throw new runtime.RequiredError(
                'askPostIn',
                'Required parameter "askPostIn" was null or undefined when calling postCodeAsk().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/ask`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AskPostInToJSON(requestParameters['askPostIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AskAnswerFromJSON(jsonValue));
    }

    /**
     * Is askGet with the question in the request BODY, for a question too long or too awkward to put in a URL. `query` and `repo` in the body take precedence over `?q=` and `?repo=`; either source works alone.
     * Is askGet with the question in the request BODY, for a question too long or too awkward to put in a URL.
     */
    async postCodeAsk(requestParameters: CodeApiPostCodeAskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AskAnswer> {
        const response = await this.postCodeAskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Packs the most relevant code for a query into a token budget — THE primitive for a coding agent that has to decide what to put in a prompt. It retrieves seed spans, expands each with the definitions it calls and its key callers, then greedily fills the budget, so the answer is a coherent slice of the codebase rather than a list of disconnected matches. The top match is always included, truncated if it alone overflows, so a matched query never comes back empty. A retrieval outage answers 200 with an empty bundle rather than a 5xx.
     * Packs the most relevant code for a query into a token budget — THE primitive for a coding agent that has to decide what to put in a prompt.
     */
    async postCodeContextRaw(requestParameters: CodeApiPostCodeContextRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ContextBundle>> {
        if (requestParameters['contextIn'] == null) {
            throw new runtime.RequiredError(
                'contextIn',
                'Required parameter "contextIn" was null or undefined when calling postCodeContext().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/context`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ContextInToJSON(requestParameters['contextIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ContextBundleFromJSON(jsonValue));
    }

    /**
     * Packs the most relevant code for a query into a token budget — THE primitive for a coding agent that has to decide what to put in a prompt. It retrieves seed spans, expands each with the definitions it calls and its key callers, then greedily fills the budget, so the answer is a coherent slice of the codebase rather than a list of disconnected matches. The top match is always included, truncated if it alone overflows, so a matched query never comes back empty. A retrieval outage answers 200 with an empty bundle rather than a 5xx.
     * Packs the most relevant code for a query into a token budget — THE primitive for a coding agent that has to decide what to put in a prompt.
     */
    async postCodeContext(requestParameters: CodeApiPostCodeContextRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ContextBundle> {
        const response = await this.postCodeContextRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * (re)indexes a repository for the caller\'s org, incrementally: files whose content hash is unchanged are skipped, so re-sending a whole tree is cheap. Each file is parsed for symbols, split at AST boundaries and — when the semantic tier is available — embedded, which is what makes it searchable across all three retrieval tiers. Pass `prune` to also DELETE indexed files absent from the request, which turns the call into a full sync; without it the call is an upsert. The index is written to the caller org\'s own physically separate database.
     * (re)indexes a repository for the caller\'s org, incrementally: files whose content hash is unchanged are skipped, so re-sending a whole tree is cheap.
     */
    async postCodeIndexRaw(requestParameters: CodeApiPostCodeIndexRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IndexResult>> {
        if (requestParameters['indexIn'] == null) {
            throw new runtime.RequiredError(
                'indexIn',
                'Required parameter "indexIn" was null or undefined when calling postCodeIndex().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/index`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: IndexInToJSON(requestParameters['indexIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IndexResultFromJSON(jsonValue));
    }

    /**
     * (re)indexes a repository for the caller\'s org, incrementally: files whose content hash is unchanged are skipped, so re-sending a whole tree is cheap. Each file is parsed for symbols, split at AST boundaries and — when the semantic tier is available — embedded, which is what makes it searchable across all three retrieval tiers. Pass `prune` to also DELETE indexed files absent from the request, which turns the call into a full sync; without it the call is an upsert. The index is written to the caller org\'s own physically separate database.
     * (re)indexes a repository for the caller\'s org, incrementally: files whose content hash is unchanged are skipped, so re-sending a whole tree is cheap.
     */
    async postCodeIndex(requestParameters: CodeApiPostCodeIndexRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IndexResult> {
        const response = await this.postCodeIndexRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Offers the candidates a language server has at a position, typed and resolved through the repository\'s dependencies rather than guessed from text.
     * Offers the candidates a language server has at a position, typed and resolved through the repository\'s dependencies rather than guessed from text.
     */
    async postCodeLspCompleteRaw(requestParameters: CodeApiPostCodeLspCompleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Answer>> {
        if (requestParameters['query'] == null) {
            throw new runtime.RequiredError(
                'query',
                'Required parameter "query" was null or undefined when calling postCodeLspComplete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/lsp/complete`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QueryToJSON(requestParameters['query']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnswerFromJSON(jsonValue));
    }

    /**
     * Offers the candidates a language server has at a position, typed and resolved through the repository\'s dependencies rather than guessed from text.
     * Offers the candidates a language server has at a position, typed and resolved through the repository\'s dependencies rather than guessed from text.
     */
    async postCodeLspComplete(requestParameters: CodeApiPostCodeLspCompleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Answer> {
        const response = await this.postCodeLspCompleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports every problem the language server finds in one file — compile errors, type errors and lints, each with its span and its severity (1 error, 2 warning, 3 information, 4 hint). The position is ignored.
     * Reports every problem the language server finds in one file — compile errors, type errors and lints, each with its span and its severity (1 error, 2 warning, 3 information, 4 hint).
     */
    async postCodeLspDiagnosticsRaw(requestParameters: CodeApiPostCodeLspDiagnosticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Answer>> {
        if (requestParameters['query'] == null) {
            throw new runtime.RequiredError(
                'query',
                'Required parameter "query" was null or undefined when calling postCodeLspDiagnostics().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/lsp/diagnostics`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QueryToJSON(requestParameters['query']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnswerFromJSON(jsonValue));
    }

    /**
     * Reports every problem the language server finds in one file — compile errors, type errors and lints, each with its span and its severity (1 error, 2 warning, 3 information, 4 hint). The position is ignored.
     * Reports every problem the language server finds in one file — compile errors, type errors and lints, each with its span and its severity (1 error, 2 warning, 3 information, 4 hint).
     */
    async postCodeLspDiagnostics(requestParameters: CodeApiPostCodeLspDiagnosticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Answer> {
        const response = await this.postCodeLspDiagnosticsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Renders the type and documentation of the symbol at a position, as the language server itself renders it.  Positions are the LSP\'s: line and character are 0-BASED and character counts UTF-16 code units, so an editor\'s 1-based line must have 1 subtracted before it is sent. The repository is named by slug and is always one in the caller\'s own org; rev pins a branch, tag or commit sha, and empty means the default branch.
     * Renders the type and documentation of the symbol at a position, as the language server itself renders it.
     */
    async postCodeLspHoverRaw(requestParameters: CodeApiPostCodeLspHoverRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Answer>> {
        if (requestParameters['query'] == null) {
            throw new runtime.RequiredError(
                'query',
                'Required parameter "query" was null or undefined when calling postCodeLspHover().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/lsp/hover`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QueryToJSON(requestParameters['query']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnswerFromJSON(jsonValue));
    }

    /**
     * Renders the type and documentation of the symbol at a position, as the language server itself renders it.  Positions are the LSP\'s: line and character are 0-BASED and character counts UTF-16 code units, so an editor\'s 1-based line must have 1 subtracted before it is sent. The repository is named by slug and is always one in the caller\'s own org; rev pins a branch, tag or commit sha, and empty means the default branch.
     * Renders the type and documentation of the symbol at a position, as the language server itself renders it.
     */
    async postCodeLspHover(requestParameters: CodeApiPostCodeLspHoverRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Answer> {
        const response = await this.postCodeLspHoverRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Finds where a symbol lives: its definition, its references, its type or its implementations, chosen by relation (definition, reference, type, implementation — empty means definition).  It resolves THROUGH dependencies. An answer whose external flag is set left the repository, and its path is then the module coordinate it landed in — which is the question a static index cannot answer and this service exists for.
     * Finds where a symbol lives: its definition, its references, its type or its implementations, chosen by relation (definition, reference, type, implementation — empty means definition).
     */
    async postCodeLspLocateRaw(requestParameters: CodeApiPostCodeLspLocateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Answer>> {
        if (requestParameters['query'] == null) {
            throw new runtime.RequiredError(
                'query',
                'Required parameter "query" was null or undefined when calling postCodeLspLocate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/lsp/locate`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QueryToJSON(requestParameters['query']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnswerFromJSON(jsonValue));
    }

    /**
     * Finds where a symbol lives: its definition, its references, its type or its implementations, chosen by relation (definition, reference, type, implementation — empty means definition).  It resolves THROUGH dependencies. An answer whose external flag is set left the repository, and its path is then the module coordinate it landed in — which is the question a static index cannot answer and this service exists for.
     * Finds where a symbol lives: its definition, its references, its type or its implementations, chosen by relation (definition, reference, type, implementation — empty means definition).
     */
    async postCodeLspLocate(requestParameters: CodeApiPostCodeLspLocateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Answer> {
        const response = await this.postCodeLspLocateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Outlines one file: every declaration in it, with its kind and its span. The position is ignored — the answer is the whole file.
     * Outlines one file: every declaration in it, with its kind and its span.
     */
    async postCodeLspSymbolsRaw(requestParameters: CodeApiPostCodeLspSymbolsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Answer>> {
        if (requestParameters['query'] == null) {
            throw new runtime.RequiredError(
                'query',
                'Required parameter "query" was null or undefined when calling postCodeLspSymbols().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/code/lsp/symbols`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QueryToJSON(requestParameters['query']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnswerFromJSON(jsonValue));
    }

    /**
     * Outlines one file: every declaration in it, with its kind and its span. The position is ignored — the answer is the whole file.
     * Outlines one file: every declaration in it, with its kind and its span.
     */
    async postCodeLspSymbols(requestParameters: CodeApiPostCodeLspSymbolsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Answer> {
        const response = await this.postCodeLspSymbolsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
