/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  MlCreate,
  MlResource,
  MlResourceList,
} from '../models/index.js';
import {
    MlCreateFromJSON,
    MlCreateToJSON,
    MlResourceFromJSON,
    MlResourceToJSON,
    MlResourceListFromJSON,
    MlResourceListToJSON,
} from '../models/index.js';

export interface MlApiDeleteMlModelsByNameRequest {
    name: string;
}

export interface MlApiGetMlModelsByNameRequest {
    name: string;
}

export interface MlApiPatchMlModelsByNameRequest {
    name: string;
}

export interface MlApiPostMlModelsRequest {
    mlCreate: MlCreate;
}

export interface MlApiPostMlModelsByNamePredictRequest {
    name: string;
}

/**
 * 
 */
export class MlApi extends runtime.BaseAPI {

    /**
     * Deletes a deployed inference model. kserve owns the teardown: the InferenceService goes away and the serving deployment behind it follows, so the model stops answering predict calls. Answers 204, or 404 for a name the caller\'s org does not own.
     * Deletes a deployed inference model.
     */
    async deleteMlModelsByNameRaw(requestParameters: MlApiDeleteMlModelsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteMlModelsByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/models/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes a deployed inference model. kserve owns the teardown: the InferenceService goes away and the serving deployment behind it follows, so the model stops answering predict calls. Answers 204, or 404 for a name the caller\'s org does not own.
     * Deletes a deployed inference model.
     */
    async deleteMlModelsByName(requestParameters: MlApiDeleteMlModelsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMlModelsByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Reports whether the model-serving plane is genuinely usable: that the Kubernetes API answers, that the InferenceService CRD is actually served by this cluster, and that the cluster holds at least one serving runtime to run a model ON. It is a REAL probe, not status theatre — it makes a live call rather than reporting a flag set at boot.  200 only when everything checks out. Otherwise 503 CARRYING THE REPORT — which component failed, and the real error — and that body is the reason this is not a typed op: a typed op reaches a non-2xx by returning an error, and the envelope that produces would drop exactly the detail the probe exists to deliver.  The runtime count is reported as its own field and is a SEPARATE fact from the CRD being served: a cluster with the CRD but no runtime accepts a deploy and then never schedules it, so reporting only the CRD would answer 200 while every model hangs. A runtime list this service cannot read reports the read error instead of a count, because a missing grant is a broken probe and not an empty cluster.  It answers about the cluster, not about a tenant, so it takes no org and reveals no tenant data. A cluster with no kserve CRD reports degraded honestly rather than failing later at the first deploy.
     * Whether model serving can actually work right now
     */
    async getMlHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Reports whether the model-serving plane is genuinely usable: that the Kubernetes API answers, that the InferenceService CRD is actually served by this cluster, and that the cluster holds at least one serving runtime to run a model ON. It is a REAL probe, not status theatre — it makes a live call rather than reporting a flag set at boot.  200 only when everything checks out. Otherwise 503 CARRYING THE REPORT — which component failed, and the real error — and that body is the reason this is not a typed op: a typed op reaches a non-2xx by returning an error, and the envelope that produces would drop exactly the detail the probe exists to deliver.  The runtime count is reported as its own field and is a SEPARATE fact from the CRD being served: a cluster with the CRD but no runtime accepts a deploy and then never schedules it, so reporting only the CRD would answer 200 while every model hangs. A runtime list this service cannot read reports the read error instead of a count, because a missing grant is a broken probe and not an empty cluster.  It answers about the cluster, not about a tenant, so it takes no org and reveals no tenant data. A cluster with no kserve CRD reports degraded honestly rather than failing later at the first deploy.
     * Whether model serving can actually work right now
     */
    async getMlHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getMlHealthRaw(initOverrides);
    }

    /**
     * Lists the inference models deployed in the caller\'s org. Each entry carries the model\'s name, when Kubernetes admitted it, and kserve\'s live status — the spec is on the single-model read. An org that has deployed nothing gets an empty list.
     * Lists the inference models deployed in the caller\'s org.
     */
    async getMlModelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MlResourceList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/models`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MlResourceListFromJSON(jsonValue));
    }

    /**
     * Lists the inference models deployed in the caller\'s org. Each entry carries the model\'s name, when Kubernetes admitted it, and kserve\'s live status — the spec is on the single-model read. An org that has deployed nothing gets an empty list.
     * Lists the inference models deployed in the caller\'s org.
     */
    async getMlModels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MlResourceList> {
        const response = await this.getMlModelsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one deployed inference model. Its spec comes with it, and kserve\'s live status, which is where readiness and the serving address appear. A name the caller\'s org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s models.
     * Returns one deployed inference model.
     */
    async getMlModelsByNameRaw(requestParameters: MlApiGetMlModelsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MlResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getMlModelsByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/models/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MlResourceFromJSON(jsonValue));
    }

    /**
     * Returns one deployed inference model. Its spec comes with it, and kserve\'s live status, which is where readiness and the serving address appear. A name the caller\'s org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s models.
     * Returns one deployed inference model.
     */
    async getMlModelsByName(requestParameters: MlApiGetMlModelsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MlResource> {
        const response = await this.getMlModelsByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies a JSON merge patch to one of the caller org\'s deployed models and answers the updated resource — the way to change a model\'s image, replica count or resource requests without tearing the deployment down.  The body is relayed to Kubernetes VERBATIM. That is deliberate and it is why this route is not a typed op: re-encoding a merge patch changes what it means, because an integer that round-trips through a generic decoder comes back a float. Merge-patch semantics apply as written — a null removes a field, and a list is replaced whole rather than merged.  Scoped to the caller\'s own tenant namespace, resolved from the validated org and project; a name the caller\'s tenant does not hold is a 404, never another tenant\'s resource. An empty body is refused, and a patch Kubernetes rejects comes back 422 with its reason rather than being silently dropped.
     * Change a deployed model in place
     */
    async patchMlModelsByNameRaw(requestParameters: MlApiPatchMlModelsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling patchMlModelsByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/models/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Applies a JSON merge patch to one of the caller org\'s deployed models and answers the updated resource — the way to change a model\'s image, replica count or resource requests without tearing the deployment down.  The body is relayed to Kubernetes VERBATIM. That is deliberate and it is why this route is not a typed op: re-encoding a merge patch changes what it means, because an integer that round-trips through a generic decoder comes back a float. Merge-patch semantics apply as written — a null removes a field, and a list is replaced whole rather than merged.  Scoped to the caller\'s own tenant namespace, resolved from the validated org and project; a name the caller\'s tenant does not hold is a 404, never another tenant\'s resource. An empty body is refused, and a patch Kubernetes rejects comes back 422 with its reason rather than being silently dropped.
     * Change a deployed model in place
     */
    async patchMlModelsByName(requestParameters: MlApiPatchMlModelsByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchMlModelsByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Deploys one inference model for the caller\'s org, and answers 201 with the model as Kubernetes admitted it.  The `spec` is a kserve InferenceService spec, passed through unchanged — this plane owns the tenancy, the billing and the namespace, and kserve owns what a model IS. An unfunded org is refused BEFORE anything is created, so nobody runs free GPU compute and nobody is charged for a resource that was never made.
     * Deploys one inference model for the caller\'s org, and answers 201 with the model as Kubernetes admitted it.
     */
    async postMlModelsRaw(requestParameters: MlApiPostMlModelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MlResource>> {
        if (requestParameters['mlCreate'] == null) {
            throw new runtime.RequiredError(
                'mlCreate',
                'Required parameter "mlCreate" was null or undefined when calling postMlModels().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/models`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MlCreateToJSON(requestParameters['mlCreate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MlResourceFromJSON(jsonValue));
    }

    /**
     * Deploys one inference model for the caller\'s org, and answers 201 with the model as Kubernetes admitted it.  The `spec` is a kserve InferenceService spec, passed through unchanged — this plane owns the tenancy, the billing and the namespace, and kserve owns what a model IS. An unfunded org is refused BEFORE anything is created, so nobody runs free GPU compute and nobody is charged for a resource that was never made.
     * Deploys one inference model for the caller\'s org, and answers 201 with the model as Kubernetes admitted it.
     */
    async postMlModels(requestParameters: MlApiPostMlModelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MlResource> {
        const response = await this.postMlModelsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sends the request body to the named model\'s predictor and answers the predictor\'s reply — its status code, its body bytes and its Content-Type, all unchanged. This is the inference call itself, not a description of one.  VERBATIM IS THE CONTRACT, and it is why this route is not a typed op: a model-side error has to surface as the model\'s own error, not as this layer\'s paraphrase of it. The body shape is the kserve v2 inference protocol\'s, which means the runtime decides it, not this API. The v2 model name defaults to the resource name — kserve\'s single-model convention — and a multi-model runtime selects one with the `model` query parameter.  A model that exists but has no serving address yet answers 503 \'not ready\' rather than a confusing connection error: deployed is not the same as serving. Scoped to the caller\'s own tenant namespace from the validated org and project, so a name another tenant owns is simply a 404. The predictor\'s response body is read up to a fixed ceiling.
     * Run inference against one of your deployed models
     */
    async postMlModelsByNamePredictRaw(requestParameters: MlApiPostMlModelsByNamePredictRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postMlModelsByNamePredict().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ml/models/{name}/predict`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Sends the request body to the named model\'s predictor and answers the predictor\'s reply — its status code, its body bytes and its Content-Type, all unchanged. This is the inference call itself, not a description of one.  VERBATIM IS THE CONTRACT, and it is why this route is not a typed op: a model-side error has to surface as the model\'s own error, not as this layer\'s paraphrase of it. The body shape is the kserve v2 inference protocol\'s, which means the runtime decides it, not this API. The v2 model name defaults to the resource name — kserve\'s single-model convention — and a multi-model runtime selects one with the `model` query parameter.  A model that exists but has no serving address yet answers 503 \'not ready\' rather than a confusing connection error: deployed is not the same as serving. Scoped to the caller\'s own tenant namespace from the validated org and project, so a name another tenant owns is simply a 404. The predictor\'s response body is read up to a fixed ceiling.
     * Run inference against one of your deployed models
     */
    async postMlModelsByNamePredict(requestParameters: MlApiPostMlModelsByNamePredictRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postMlModelsByNamePredictRaw(requestParameters, initOverrides);
    }

}
