/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  SettingsReq,
  SettingsView,
} from '../models/index.js';
import {
    SettingsReqFromJSON,
    SettingsReqToJSON,
    SettingsViewFromJSON,
    SettingsViewToJSON,
} from '../models/index.js';

export interface SettingsApiGetSettingsByProductRequest {
    product: string;
}

export interface SettingsApiPutSettingsByProductRequest {
    product: string;
    settingsReq: SettingsReq;
}

/**
 * 
 */
export class SettingsApi extends runtime.BaseAPI {

    /**
     * Reads the caller org\'s configuration for one product, with every secret field MASKED — only the names of the set secrets come back, never their values, which live in KMS. A product the org has never configured is not a 404: it answers 200 with an empty config object, so the console\'s Settings tab always renders and merges its own display defaults on top.
     * Reads the caller org\'s configuration for one product, with every secret field MASKED — only the names of the set secrets come back, never their values, which live in KMS.
     */
    async getSettingsByProductRaw(requestParameters: SettingsApiGetSettingsByProductRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SettingsView>> {
        if (requestParameters['product'] == null) {
            throw new runtime.RequiredError(
                'product',
                'Required parameter "product" was null or undefined when calling getSettingsByProduct().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/settings/{product}`;
        urlPath = urlPath.replace(`{${"product"}}`, encodeURIComponent(String(requestParameters['product'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SettingsViewFromJSON(jsonValue));
    }

    /**
     * Reads the caller org\'s configuration for one product, with every secret field MASKED — only the names of the set secrets come back, never their values, which live in KMS. A product the org has never configured is not a 404: it answers 200 with an empty config object, so the console\'s Settings tab always renders and merges its own display defaults on top.
     * Reads the caller org\'s configuration for one product, with every secret field MASKED — only the names of the set secrets come back, never their values, which live in KMS.
     */
    async getSettingsByProduct(requestParameters: SettingsApiGetSettingsByProductRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SettingsView> {
        const response = await this.getSettingsByProductRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes the caller org\'s configuration for one product and answers the stored result, secrets masked. Secret VALUES are sealed into KMS under orgs/{org}/settings/{product}/{key} and never touch this deployment\'s database; with no KMS configured a write that carries any secret is refused whole (503) rather than dropping it or persisting it in the clear. A secret the body omits keeps its stored value, so a partial write never silently clears one.
     * Writes the caller org\'s configuration for one product and answers the stored result, secrets masked.
     */
    async putSettingsByProductRaw(requestParameters: SettingsApiPutSettingsByProductRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SettingsView>> {
        if (requestParameters['product'] == null) {
            throw new runtime.RequiredError(
                'product',
                'Required parameter "product" was null or undefined when calling putSettingsByProduct().'
            );
        }

        if (requestParameters['settingsReq'] == null) {
            throw new runtime.RequiredError(
                'settingsReq',
                'Required parameter "settingsReq" was null or undefined when calling putSettingsByProduct().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/settings/{product}`;
        urlPath = urlPath.replace(`{${"product"}}`, encodeURIComponent(String(requestParameters['product'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SettingsReqToJSON(requestParameters['settingsReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SettingsViewFromJSON(jsonValue));
    }

    /**
     * Writes the caller org\'s configuration for one product and answers the stored result, secrets masked. Secret VALUES are sealed into KMS under orgs/{org}/settings/{product}/{key} and never touch this deployment\'s database; with no KMS configured a write that carries any secret is refused whole (503) rather than dropping it or persisting it in the clear. A secret the body omits keeps its stored value, so a partial write never silently clears one.
     * Writes the caller org\'s configuration for one product and answers the stored result, secrets masked.
     */
    async putSettingsByProduct(requestParameters: SettingsApiPutSettingsByProductRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SettingsView> {
        const response = await this.putSettingsByProductRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
