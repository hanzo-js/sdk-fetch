/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Centre,
  ClauseCoverage,
  ControlList,
  Dropped,
  FaqList,
  FrameworkList,
  PolicyList,
  SectionWrite,
  SubprocessorList,
  TrustCoverage,
  TrustDocuments,
  UpdateList,
  Written,
} from '../models/index.js';
import {
    CentreFromJSON,
    CentreToJSON,
    ClauseCoverageFromJSON,
    ClauseCoverageToJSON,
    ControlListFromJSON,
    ControlListToJSON,
    DroppedFromJSON,
    DroppedToJSON,
    FaqListFromJSON,
    FaqListToJSON,
    FrameworkListFromJSON,
    FrameworkListToJSON,
    PolicyListFromJSON,
    PolicyListToJSON,
    SectionWriteFromJSON,
    SectionWriteToJSON,
    SubprocessorListFromJSON,
    SubprocessorListToJSON,
    TrustCoverageFromJSON,
    TrustCoverageToJSON,
    TrustDocumentsFromJSON,
    TrustDocumentsToJSON,
    UpdateListFromJSON,
    UpdateListToJSON,
    WrittenFromJSON,
    WrittenToJSON,
} from '../models/index.js';

export interface TrustApiDeleteTrustByKindByIdRequest {
    kind: string;
    id: string;
}

export interface TrustApiGetTrustControlsByIdRequest {
    id: string;
}

export interface TrustApiGetTrustCoverageByFrameworkRequest {
    framework: string;
}

export interface TrustApiGetTrustEvidenceRequest {
    control?: string;
    from?: string;
    to?: string;
    limit?: string;
}

export interface TrustApiGetTrustPublishedByOrgRequest {
    org: string;
}

export interface TrustApiPutTrustByKindByIdRequest {
    kind: string;
    id: string;
    sectionWrite: SectionWrite;
}

/**
 * 
 */
export class TrustApi extends runtime.BaseAPI {

    /**
     * Removes one record from a section of your organization\'s trust centre. A record that is not there is a 404, never a silent success. A control that belongs to the deployment\'s own inventory is removed by a commit, not by a request.
     * Removes one record from a section of your organization\'s trust centre.
     */
    async deleteTrustByKindByIdRaw(requestParameters: TrustApiDeleteTrustByKindByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Dropped>> {
        if (requestParameters['kind'] == null) {
            throw new runtime.RequiredError(
                'kind',
                'Required parameter "kind" was null or undefined when calling deleteTrustByKindById().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTrustByKindById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/{kind}/{id}`;
        urlPath = urlPath.replace(`{${"kind"}}`, encodeURIComponent(String(requestParameters['kind'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DroppedFromJSON(jsonValue));
    }

    /**
     * Removes one record from a section of your organization\'s trust centre. A record that is not there is a 404, never a silent success. A control that belongs to the deployment\'s own inventory is removed by a commit, not by a request.
     * Removes one record from a section of your organization\'s trust centre.
     */
    async deleteTrustByKindById(requestParameters: TrustApiDeleteTrustByKindByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Dropped> {
        const response = await this.deleteTrustByKindByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads YOUR organization\'s whole trust centre, including the addresses of your own gated documents. Same shape as the published door; the difference is that this one is resolved from your validated bearer and shows you your own artifacts.
     * Reads YOUR organization\'s whole trust centre, including the addresses of your own gated documents.
     */
    async getTrustRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Centre>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CentreFromJSON(jsonValue));
    }

    /**
     * Reads YOUR organization\'s whole trust centre, including the addresses of your own gated documents. Same shape as the published door; the difference is that this one is resolved from your validated bearer and shows you your own artifacts.
     * Reads YOUR organization\'s whole trust centre, including the addresses of your own gated documents.
     */
    async getTrust(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Centre> {
        const response = await this.getTrustRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists every control your organization publishes, with the counts.  A control names what it asserts, the mechanism behind it, the repository and file where that mechanism is enforced, how it is verified, and the framework clauses it maps to. Status is automated, partial or absent — and an absent one still names the clause it would satisfy, which is a roadmap, while never moving a coverage number.
     * Lists every control your organization publishes, with the counts.
     */
    async getTrustControlsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ControlList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/controls`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ControlListFromJSON(jsonValue));
    }

    /**
     * Lists every control your organization publishes, with the counts.  A control names what it asserts, the mechanism behind it, the repository and file where that mechanism is enforced, how it is verified, and the framework clauses it maps to. Status is automated, partial or absent — and an absent one still names the clause it would satisfy, which is a roadmap, while never moving a coverage number.
     * Lists every control your organization publishes, with the counts.
     */
    async getTrustControls(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ControlList> {
        const response = await this.getTrustControlsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads one control by id.
     * Reads one control by id.
     */
    async getTrustControlsByIdRaw(requestParameters: TrustApiGetTrustControlsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getTrustControlsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/controls/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads one control by id.
     * Reads one control by id.
     */
    async getTrustControlsById(requestParameters: TrustApiGetTrustControlsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getTrustControlsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads coverage: per framework, how many clauses have an automated control behind them, how many are partial, and how many have none — each carrying the unit it is counted in, because \"12 of 20\" is not a fact until you know what the 20 are.  Nothing here is a verdict. There is no boolean, and a control that only a person has read counts one rung weaker than it claims to be, because only a check that can FAIL is evidence.
     * Reads coverage: per framework, how many clauses have an automated control behind them, how many are partial, and how many have none — each carrying the unit it is counted in, because \"12 of 20\" is not a fact until you know what the 20 are.
     */
    async getTrustCoverageRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrustCoverage>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/coverage`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrustCoverageFromJSON(jsonValue));
    }

    /**
     * Reads coverage: per framework, how many clauses have an automated control behind them, how many are partial, and how many have none — each carrying the unit it is counted in, because \"12 of 20\" is not a fact until you know what the 20 are.  Nothing here is a verdict. There is no boolean, and a control that only a person has read counts one rung weaker than it claims to be, because only a check that can FAIL is evidence.
     * Reads coverage: per framework, how many clauses have an automated control behind them, how many are partial, and how many have none — each carrying the unit it is counted in, because \"12 of 20\" is not a fact until you know what the 20 are.
     */
    async getTrustCoverage(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrustCoverage> {
        const response = await this.getTrustCoverageRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads one framework clause by clause: every clause the standard publishes, what covers it, and which controls stand behind it — so a coverage number can be checked line by line rather than taken on trust.
     * Reads one framework clause by clause: every clause the standard publishes, what covers it, and which controls stand behind it — so a coverage number can be checked line by line rather than taken on trust.
     */
    async getTrustCoverageByFrameworkRaw(requestParameters: TrustApiGetTrustCoverageByFrameworkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ClauseCoverage>> {
        if (requestParameters['framework'] == null) {
            throw new runtime.RequiredError(
                'framework',
                'Required parameter "framework" was null or undefined when calling getTrustCoverageByFramework().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/coverage/{framework}`;
        urlPath = urlPath.replace(`{${"framework"}}`, encodeURIComponent(String(requestParameters['framework'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ClauseCoverageFromJSON(jsonValue));
    }

    /**
     * Reads one framework clause by clause: every clause the standard publishes, what covers it, and which controls stand behind it — so a coverage number can be checked line by line rather than taken on trust.
     * Reads one framework clause by clause: every clause the standard publishes, what covers it, and which controls stand behind it — so a coverage number can be checked line by line rather than taken on trust.
     */
    async getTrustCoverageByFramework(requestParameters: TrustApiGetTrustCoverageByFrameworkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ClauseCoverage> {
        const response = await this.getTrustCoverageByFrameworkRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists your organization\'s documents. Because this is your own centre, a gated artifact carries its address here; through the published door it does not.
     * Lists your organization\'s documents.
     */
    async getTrustDocumentsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrustDocuments>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/documents`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrustDocumentsFromJSON(jsonValue));
    }

    /**
     * Lists your organization\'s documents. Because this is your own centre, a gated artifact carries its address here; through the published door it does not.
     * Lists your organization\'s documents.
     */
    async getTrustDocuments(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrustDocuments> {
        const response = await this.getTrustDocumentsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads the audit rows that stand behind one control, over a window.  The inventory decides what evidences what: a control names the audit actions that are its trail, and this resolves the control id to those actions and reads them. So evidence cannot drift from the inventory, and it is scoped to your own organization — the query carries no organization field for a caller to fill in.  A control that nothing in the trail evidences says so plainly rather than answering an empty page, because an empty page reads like a clean quarter. A deployment with no audit store answers 501 and says the trail was not read, for the same reason.
     * Reads the audit rows that stand behind one control, over a window.
     */
    async getTrustEvidenceRaw(requestParameters: TrustApiGetTrustEvidenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['control'] != null) {
            queryParameters['control'] = requestParameters['control'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/evidence`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads the audit rows that stand behind one control, over a window.  The inventory decides what evidences what: a control names the audit actions that are its trail, and this resolves the control id to those actions and reads them. So evidence cannot drift from the inventory, and it is scoped to your own organization — the query carries no organization field for a caller to fill in.  A control that nothing in the trail evidences says so plainly rather than answering an empty page, because an empty page reads like a clean quarter. A deployment with no audit store answers 501 and says the trail was not read, for the same reason.
     * Reads the audit rows that stand behind one control, over a window.
     */
    async getTrustEvidence(requestParameters: TrustApiGetTrustEvidenceRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getTrustEvidenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists your knowledge base — the questions a reviewer asks, answered once.
     * Lists your knowledge base — the questions a reviewer asks, answered once.
     */
    async getTrustFaqRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FaqList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/faq`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FaqListFromJSON(jsonValue));
    }

    /**
     * Lists your knowledge base — the questions a reviewer asks, answered once.
     * Lists your knowledge base — the questions a reviewer asks, answered once.
     */
    async getTrustFaq(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FaqList> {
        const response = await this.getTrustFaqRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the frameworks coverage is computed against, and how many clauses each publishes. That count is the denominator of every coverage number, which is what keeps an uncovered clause visible instead of dropping out of the fraction.
     * Lists the frameworks coverage is computed against, and how many clauses each publishes.
     */
    async getTrustFrameworksRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FrameworkList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/frameworks`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FrameworkListFromJSON(jsonValue));
    }

    /**
     * Lists the frameworks coverage is computed against, and how many clauses each publishes. That count is the denominator of every coverage number, which is what keeps an uncovered clause visible instead of dropping out of the fraction.
     * Lists the frameworks coverage is computed against, and how many clauses each publishes.
     */
    async getTrustFrameworks(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FrameworkList> {
        const response = await this.getTrustFrameworksRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists your organization\'s published policies.
     * Lists your organization\'s published policies.
     */
    async getTrustPoliciesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/policies`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyListFromJSON(jsonValue));
    }

    /**
     * Lists your organization\'s published policies.
     * Lists your organization\'s published policies.
     */
    async getTrustPolicies(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyList> {
        const response = await this.getTrustPoliciesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads your organization\'s trust-centre profile — the name, tagline and summary a visitor sees, whether the centre is published, and where to send somebody who wants a gated document.
     * Reads your organization\'s trust-centre profile — the name, tagline and summary a visitor sees, whether the centre is published, and where to send somebody who wants a gated document.
     */
    async getTrustProfileRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/profile`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads your organization\'s trust-centre profile — the name, tagline and summary a visitor sees, whether the centre is published, and where to send somebody who wants a gated document.
     * Reads your organization\'s trust-centre profile — the name, tagline and summary a visitor sees, whether the centre is published, and where to send somebody who wants a gated document.
     */
    async getTrustProfile(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getTrustProfileRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads a published trust centre — the whole thing in one answer: the organization\'s profile, its control inventory, coverage computed against each framework\'s whole published clause list, its documents, subprocessors, policies, knowledge base, updates and risk profile.  This is the PUBLIC door and needs no credential, because a published trust centre is a public document. It answers only for an organization that has published one — an organization that has not is not found rather than empty, since an empty centre and a centre nobody meant to show read the same and are not the same thing.  A gated document appears here with its title, its type and its date and NO address: the listing says the artifact exists and that reading it takes a grant. Nothing an independent auditor signed is ever released through this door.
     * Reads a published trust centre — the whole thing in one answer: the organization\'s profile, its control inventory, coverage computed against each framework\'s whole published clause list, its documents, subprocessors, policies, knowledge base, updates and risk profile.
     */
    async getTrustPublishedByOrgRaw(requestParameters: TrustApiGetTrustPublishedByOrgRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Centre>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling getTrustPublishedByOrg().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/trust/published/{org}`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CentreFromJSON(jsonValue));
    }

    /**
     * Reads a published trust centre — the whole thing in one answer: the organization\'s profile, its control inventory, coverage computed against each framework\'s whole published clause list, its documents, subprocessors, policies, knowledge base, updates and risk profile.  This is the PUBLIC door and needs no credential, because a published trust centre is a public document. It answers only for an organization that has published one — an organization that has not is not found rather than empty, since an empty centre and a centre nobody meant to show read the same and are not the same thing.  A gated document appears here with its title, its type and its date and NO address: the listing says the artifact exists and that reading it takes a grant. Nothing an independent auditor signed is ever released through this door.
     * Reads a published trust centre — the whole thing in one answer: the organization\'s profile, its control inventory, coverage computed against each framework\'s whole published clause list, its documents, subprocessors, policies, knowledge base, updates and risk profile.
     */
    async getTrustPublishedByOrg(requestParameters: TrustApiGetTrustPublishedByOrgRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Centre> {
        const response = await this.getTrustPublishedByOrgRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads your risk profile — the label and value pairs describing what your organization handles and how.
     * Reads your risk profile — the label and value pairs describing what your organization handles and how.
     */
    async getTrustRiskRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/risk`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads your risk profile — the label and value pairs describing what your organization handles and how.
     * Reads your risk profile — the label and value pairs describing what your organization handles and how.
     */
    async getTrustRisk(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getTrustRiskRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the third parties your organization sends data to, each naming what it is for.
     * Lists the third parties your organization sends data to, each naming what it is for.
     */
    async getTrustSubprocessorsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubprocessorList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/subprocessors`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubprocessorListFromJSON(jsonValue));
    }

    /**
     * Lists the third parties your organization sends data to, each naming what it is for.
     * Lists the third parties your organization sends data to, each naming what it is for.
     */
    async getTrustSubprocessors(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubprocessorList> {
        const response = await this.getTrustSubprocessorsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists your trust-centre updates, newest as you ordered them.
     * Lists your trust-centre updates, newest as you ordered them.
     */
    async getTrustUpdatesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UpdateList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/updates`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UpdateListFromJSON(jsonValue));
    }

    /**
     * Lists your trust-centre updates, newest as you ordered them.
     * Lists your trust-centre updates, newest as you ordered them.
     */
    async getTrustUpdates(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UpdateList> {
        const response = await this.getTrustUpdatesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Writes one record into a section of YOUR organization\'s trust centre — profile, control, document, subprocessor, policy, faq, update or risk.  A control written here is held to exactly the rule a control committed to the deployment\'s own inventory is held to, by the same validator: its prose may not claim a certificate and may not name a framework (a framework belongs in the mappings, where it arrives attached to a number), anything short of automated must say what is missing, and a mapping to a clause no framework declares is refused rather than scored as nothing.  A document defaults to GATED. An artifact an independent auditor signed — a SOC 2 report, an ISO certificate, a penetration test, an auditor letter — cannot be made public at all; it is released through a grant. A self-assessment can, because the organization is the one attesting it.  The deployment\'s OWN control inventory is governed in git and is not writable here: naming one of its ids is a conflict, not an overwrite.
     * Writes one record into a section of YOUR organization\'s trust centre — profile, control, document, subprocessor, policy, faq, update or risk.
     */
    async putTrustByKindByIdRaw(requestParameters: TrustApiPutTrustByKindByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Written>> {
        if (requestParameters['kind'] == null) {
            throw new runtime.RequiredError(
                'kind',
                'Required parameter "kind" was null or undefined when calling putTrustByKindById().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putTrustByKindById().'
            );
        }

        if (requestParameters['sectionWrite'] == null) {
            throw new runtime.RequiredError(
                'sectionWrite',
                'Required parameter "sectionWrite" was null or undefined when calling putTrustByKindById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/trust/{kind}/{id}`;
        urlPath = urlPath.replace(`{${"kind"}}`, encodeURIComponent(String(requestParameters['kind'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SectionWriteToJSON(requestParameters['sectionWrite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WrittenFromJSON(jsonValue));
    }

    /**
     * Writes one record into a section of YOUR organization\'s trust centre — profile, control, document, subprocessor, policy, faq, update or risk.  A control written here is held to exactly the rule a control committed to the deployment\'s own inventory is held to, by the same validator: its prose may not claim a certificate and may not name a framework (a framework belongs in the mappings, where it arrives attached to a number), anything short of automated must say what is missing, and a mapping to a clause no framework declares is refused rather than scored as nothing.  A document defaults to GATED. An artifact an independent auditor signed — a SOC 2 report, an ISO certificate, a penetration test, an auditor letter — cannot be made public at all; it is released through a grant. A self-assessment can, because the organization is the one attesting it.  The deployment\'s OWN control inventory is governed in git and is not writable here: naming one of its ids is a conflict, not an overwrite.
     * Writes one record into a section of YOUR organization\'s trust centre — profile, control, document, subprocessor, policy, faq, update or risk.
     */
    async putTrustByKindById(requestParameters: TrustApiPutTrustByKindByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Written> {
        const response = await this.putTrustByKindByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
