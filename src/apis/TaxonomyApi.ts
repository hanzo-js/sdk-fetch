/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Category,
  CategoryIn,
  Deleted,
  Taxon,
  TaxonIn,
  Taxonomy,
} from '../models/index.js';
import {
    CategoryFromJSON,
    CategoryToJSON,
    CategoryInFromJSON,
    CategoryInToJSON,
    DeletedFromJSON,
    DeletedToJSON,
    TaxonFromJSON,
    TaxonToJSON,
    TaxonInFromJSON,
    TaxonInToJSON,
    TaxonomyFromJSON,
    TaxonomyToJSON,
} from '../models/index.js';

export interface TaxonomyApiDeleteTaxonomyCategoriesByIdRequest {
    id: string;
}

export interface TaxonomyApiDeleteTaxonomyTaxaByIdRequest {
    id: string;
}

export interface TaxonomyApiGetTaxonomyRequest {
    brand?: string;
}

export interface TaxonomyApiPutTaxonomyCategoriesByIdRequest {
    id: string;
    categoryIn: CategoryIn;
}

export interface TaxonomyApiPutTaxonomyTaxaByIdRequest {
    id: string;
    taxonIn: TaxonIn;
}

/**
 * 
 */
export class TaxonomyApi extends runtime.BaseAPI {

    /**
     * Removes one empty category. A category that still has taxa filed under it is refused with 409 and a count: deleting the label off a group must never silently take the products wearing it, and the alternative — orphan rows naming a category that no longer exists — is a catalogue that cannot be rendered. Move or delete its taxa first. An id no category holds is a 404.
     * Removes one empty category.
     */
    async deleteTaxonomyCategoriesByIdRaw(requestParameters: TaxonomyApiDeleteTaxonomyCategoriesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Deleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTaxonomyCategoriesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/taxonomy/categories/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeletedFromJSON(jsonValue));
    }

    /**
     * Removes one empty category. A category that still has taxa filed under it is refused with 409 and a count: deleting the label off a group must never silently take the products wearing it, and the alternative — orphan rows naming a category that no longer exists — is a catalogue that cannot be rendered. Move or delete its taxa first. An id no category holds is a 404.
     * Removes one empty category.
     */
    async deleteTaxonomyCategoriesById(requestParameters: TaxonomyApiDeleteTaxonomyCategoriesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Deleted> {
        const response = await this.deleteTaxonomyCategoriesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one product from the catalogue. An id no taxon holds is a 404. To take a product out of view without losing what was written about it, set `published` to false instead.
     * Removes one product from the catalogue.
     */
    async deleteTaxonomyTaxaByIdRaw(requestParameters: TaxonomyApiDeleteTaxonomyTaxaByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Deleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTaxonomyTaxaById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/taxonomy/taxa/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeletedFromJSON(jsonValue));
    }

    /**
     * Removes one product from the catalogue. An id no taxon holds is a 404. To take a product out of view without losing what was written about it, set `published` to false instead.
     * Removes one product from the catalogue.
     */
    async deleteTaxonomyTaxaById(requestParameters: TaxonomyApiDeleteTaxonomyTaxaByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Deleted> {
        const response = await this.deleteTaxonomyTaxaByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Read returns the product catalogue as this caller sees it: the PLATFORM catalogue — Hanzo\'s own products, the part that is true for everyone — plus the caller\'s own org\'s rows, every category in display order and each carrying the products filed under it in theirs. Another customer\'s rows are never in it. It is readable signed out, and a signed-out visitor gets the platform catalogue alone, which is what the marketing landing renders from.  Where the caller\'s org and the platform hold the same id, the caller\'s own row is the one served. That rule exists because ids are unique per ORG and not globally — two customers may each have a \"crm\", and refusing the second would tell one of them the other exists — so a collision with the platform is possible by construction and something has to win deterministically. Yours does: your own catalogue is the one you edited.  `?brand=` narrows it the way a brand\'s own console does: only the categories that brand admits, and within them only the taxa scoped to it. An unpublished row is served only to whoever may edit it — a SuperAdmin for the platform\'s, an org admin for their own — so a product can be staged before anyone sees it without becoming invisible to the person staging it.
     * Read returns the product catalogue as this caller sees it: the PLATFORM catalogue — Hanzo\'s own products, the part that is true for everyone — plus the caller\'s own org\'s rows, every category in display order and each carrying the products filed under it in theirs.
     */
    async getTaxonomyRaw(requestParameters: TaxonomyApiGetTaxonomyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Taxonomy>> {
        const queryParameters: any = {};

        if (requestParameters['brand'] != null) {
            queryParameters['brand'] = requestParameters['brand'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/taxonomy`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaxonomyFromJSON(jsonValue));
    }

    /**
     * Read returns the product catalogue as this caller sees it: the PLATFORM catalogue — Hanzo\'s own products, the part that is true for everyone — plus the caller\'s own org\'s rows, every category in display order and each carrying the products filed under it in theirs. Another customer\'s rows are never in it. It is readable signed out, and a signed-out visitor gets the platform catalogue alone, which is what the marketing landing renders from.  Where the caller\'s org and the platform hold the same id, the caller\'s own row is the one served. That rule exists because ids are unique per ORG and not globally — two customers may each have a \"crm\", and refusing the second would tell one of them the other exists — so a collision with the platform is possible by construction and something has to win deterministically. Yours does: your own catalogue is the one you edited.  `?brand=` narrows it the way a brand\'s own console does: only the categories that brand admits, and within them only the taxa scoped to it. An unpublished row is served only to whoever may edit it — a SuperAdmin for the platform\'s, an org admin for their own — so a product can be staged before anyone sees it without becoming invisible to the person staging it.
     * Read returns the product catalogue as this caller sees it: the PLATFORM catalogue — Hanzo\'s own products, the part that is true for everyone — plus the caller\'s own org\'s rows, every category in display order and each carrying the products filed under it in theirs.
     */
    async getTaxonomy(requestParameters: TaxonomyApiGetTaxonomyRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Taxonomy> {
        const response = await this.getTaxonomyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one category and returns it as stored. The id in the URL is the one it is filed under whatever the body says, so a category can never be written under a name it was not addressed by — which also makes create and replace the same act, and is why there is no POST beside this.  Platform SuperAdmin only: one catalogue serves every tenant, so an org admin who could rename a category would rename it for all of them.
     * Creates or replaces one category and returns it as stored.
     */
    async putTaxonomyCategoriesByIdRaw(requestParameters: TaxonomyApiPutTaxonomyCategoriesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Category>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putTaxonomyCategoriesById().'
            );
        }

        if (requestParameters['categoryIn'] == null) {
            throw new runtime.RequiredError(
                'categoryIn',
                'Required parameter "categoryIn" was null or undefined when calling putTaxonomyCategoriesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/taxonomy/categories/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CategoryInToJSON(requestParameters['categoryIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CategoryFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one category and returns it as stored. The id in the URL is the one it is filed under whatever the body says, so a category can never be written under a name it was not addressed by — which also makes create and replace the same act, and is why there is no POST beside this.  Platform SuperAdmin only: one catalogue serves every tenant, so an org admin who could rename a category would rename it for all of them.
     * Creates or replaces one category and returns it as stored.
     */
    async putTaxonomyCategoriesById(requestParameters: TaxonomyApiPutTaxonomyCategoriesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Category> {
        const response = await this.putTaxonomyCategoriesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one product and returns it as stored. The id in the URL is the one it is filed under whatever the body says. The category must already exist — a taxon naming a category that does not is refused with 400 rather than stored where nothing can render it.  A taxon opens exactly one way: `route` for a product the console renders itself, or `href` for one that genuinely lives at its own domain. Giving both, or neither, is refused.  Platform SuperAdmin only.
     * Creates or replaces one product and returns it as stored.
     */
    async putTaxonomyTaxaByIdRaw(requestParameters: TaxonomyApiPutTaxonomyTaxaByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Taxon>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putTaxonomyTaxaById().'
            );
        }

        if (requestParameters['taxonIn'] == null) {
            throw new runtime.RequiredError(
                'taxonIn',
                'Required parameter "taxonIn" was null or undefined when calling putTaxonomyTaxaById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/taxonomy/taxa/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: TaxonInToJSON(requestParameters['taxonIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaxonFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one product and returns it as stored. The id in the URL is the one it is filed under whatever the body says. The category must already exist — a taxon naming a category that does not is refused with 400 rather than stored where nothing can render it.  A taxon opens exactly one way: `route` for a product the console renders itself, or `href` for one that genuinely lives at its own domain. Giving both, or neither, is refused.  Platform SuperAdmin only.
     * Creates or replaces one product and returns it as stored.
     */
    async putTaxonomyTaxaById(requestParameters: TaxonomyApiPutTaxonomyTaxaByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Taxon> {
        const response = await this.putTaxonomyTaxaByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
