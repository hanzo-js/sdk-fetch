/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AccountList,
  CreateAccountIn,
  CreateWalletIn,
  SafeProposal,
  SafeTxIn,
  SignIn,
  Signature,
  Wallet,
  WalletAccount,
  WalletList,
} from '../models/index.js';
import {
    AccountListFromJSON,
    AccountListToJSON,
    CreateAccountInFromJSON,
    CreateAccountInToJSON,
    CreateWalletInFromJSON,
    CreateWalletInToJSON,
    SafeProposalFromJSON,
    SafeProposalToJSON,
    SafeTxInFromJSON,
    SafeTxInToJSON,
    SignInFromJSON,
    SignInToJSON,
    SignatureFromJSON,
    SignatureToJSON,
    WalletFromJSON,
    WalletToJSON,
    WalletAccountFromJSON,
    WalletAccountToJSON,
    WalletListFromJSON,
    WalletListToJSON,
} from '../models/index.js';

export interface WalletApiGetWalletRequest {
    project?: string;
    agent?: string;
    account?: string;
}

export interface WalletApiGetWalletByIdRequest {
    id: string;
}

export interface WalletApiPostWalletRequest {
    createWalletIn: CreateWalletIn;
}

export interface WalletApiPostWalletAccountsRequest {
    createAccountIn: CreateAccountIn;
}

export interface WalletApiPostWalletByIdKeysRequest {
    id: string;
}

export interface WalletApiPostWalletByIdSignRequest {
    id: string;
    signIn: SignIn;
}

export interface WalletApiPostWalletByIdTransactionsRequest {
    id: string;
    safeTxIn: SafeTxIn;
}

/**
 * 
 */
export class WalletApi extends runtime.BaseAPI {

    /**
     * Returns the caller org\'s wallets, newest first, optionally NARROWED within the org by project, agent or account. The org is always the bound isolation boundary — the filters only ever narrow inside it, so a caller can never widen past its own org.
     * Returns the caller org\'s wallets, newest first, optionally NARROWED within the org by project, agent or account.
     */
    async getWalletRaw(requestParameters: WalletApiGetWalletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WalletList>> {
        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['agent'] != null) {
            queryParameters['agent'] = requestParameters['agent'];
        }

        if (requestParameters['account'] != null) {
            queryParameters['account'] = requestParameters['account'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WalletListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s wallets, newest first, optionally NARROWED within the org by project, agent or account. The org is always the bound isolation boundary — the filters only ever narrow inside it, so a caller can never widen past its own org.
     * Returns the caller org\'s wallets, newest first, optionally NARROWED within the org by project, agent or account.
     */
    async getWallet(requestParameters: WalletApiGetWalletRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WalletList> {
        const response = await this.getWalletRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s wallet accounts, newest first. Accounts are physically org-scoped, so another tenant\'s are not reachable from here.
     * Returns the caller org\'s wallet accounts, newest first.
     */
    async getWalletAccountsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet/accounts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s wallet accounts, newest first. Accounts are physically org-scoped, so another tenant\'s are not reachable from here.
     * Returns the caller org\'s wallet accounts, newest first.
     */
    async getWalletAccounts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountList> {
        const response = await this.getWalletAccountsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s wallets: its scope, custody kind, tier, chain and on-chain address. The custody handle to the signing material is never part of the answer. A wallet id another org owns reads as not found, so the response cannot confirm that it exists.
     * Returns one of the caller org\'s wallets: its scope, custody kind, tier, chain and on-chain address.
     */
    async getWalletByIdRaw(requestParameters: WalletApiGetWalletByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Wallet>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getWalletById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WalletFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s wallets: its scope, custody kind, tier, chain and on-chain address. The custody handle to the signing material is never part of the answer. A wallet id another org owns reads as not found, so the response cannot confirm that it exists.
     * Returns one of the caller org\'s wallets: its scope, custody kind, tier, chain and on-chain address.
     */
    async getWalletById(requestParameters: WalletApiGetWalletByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Wallet> {
        const response = await this.getWalletByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Provisions a new signing identity under one of the caller org\'s accounts and answers the stored wallet including its on-chain address. The custody backend generates the key material — a KMS-sealed secp256k1 key, an MPC threshold key on the ring, or a Safe smart wallet owned by one — and the HANDLE to it is kept server-side and never returned. A custody kind the deployment has not wired fails CLOSED with 503: a signature is never fabricated. The wallet is scoped to the org, the caller\'s ambient project, and optionally an agent and the named account; those narrowings are what its key ref is derived from, so each must be a url-safe segment.
     * Provisions a new signing identity under one of the caller org\'s accounts and answers the stored wallet including its on-chain address.
     */
    async postWalletRaw(requestParameters: WalletApiPostWalletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Wallet>> {
        if (requestParameters['createWalletIn'] == null) {
            throw new runtime.RequiredError(
                'createWalletIn',
                'Required parameter "createWalletIn" was null or undefined when calling postWallet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateWalletInToJSON(requestParameters['createWalletIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WalletFromJSON(jsonValue));
    }

    /**
     * Provisions a new signing identity under one of the caller org\'s accounts and answers the stored wallet including its on-chain address. The custody backend generates the key material — a KMS-sealed secp256k1 key, an MPC threshold key on the ring, or a Safe smart wallet owned by one — and the HANDLE to it is kept server-side and never returned. A custody kind the deployment has not wired fails CLOSED with 503: a signature is never fabricated. The wallet is scoped to the org, the caller\'s ambient project, and optionally an agent and the named account; those narrowings are what its key ref is derived from, so each must be a url-safe segment.
     * Provisions a new signing identity under one of the caller org\'s accounts and answers the stored wallet including its on-chain address.
     */
    async postWallet(requestParameters: WalletApiPostWalletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Wallet> {
        const response = await this.postWalletRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Opens a named wallet account for the caller\'s org. An account is a GROUPING of wallets, not a key or a balance: wallets are created under one and can be listed by it. The org is stamped by the server from the validated principal, so a request can never open an account in another tenant.
     * Opens a named wallet account for the caller\'s org.
     */
    async postWalletAccountsRaw(requestParameters: WalletApiPostWalletAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WalletAccount>> {
        if (requestParameters['createAccountIn'] == null) {
            throw new runtime.RequiredError(
                'createAccountIn',
                'Required parameter "createAccountIn" was null or undefined when calling postWalletAccounts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet/accounts`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateAccountInToJSON(requestParameters['createAccountIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WalletAccountFromJSON(jsonValue));
    }

    /**
     * Opens a named wallet account for the caller\'s org. An account is a GROUPING of wallets, not a key or a balance: wallets are created under one and can be listed by it. The org is stamped by the server from the validated principal, so a request can never open an account in another tenant.
     * Opens a named wallet account for the caller\'s org.
     */
    async postWalletAccounts(requestParameters: WalletApiPostWalletAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WalletAccount> {
        const response = await this.postWalletAccountsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rolls one wallet\'s signing material through its own custody backend and answers the wallet with whatever address that produced. For KMS custody a fresh secp256k1 key is generated and sealed, which CHANGES the address — funds and approvals at the old address do not move. For a Safe the address is counterfactual and the owner shares are ring-managed, so rotation is a no-op and the address is unchanged. A backend that is not configured fails closed with 503 rather than leaving the wallet half-rotated.
     * Rolls one wallet\'s signing material through its own custody backend and answers the wallet with whatever address that produced.
     */
    async postWalletByIdKeysRaw(requestParameters: WalletApiPostWalletByIdKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Wallet>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postWalletByIdKeys().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet/{id}/keys`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WalletFromJSON(jsonValue));
    }

    /**
     * Rolls one wallet\'s signing material through its own custody backend and answers the wallet with whatever address that produced. For KMS custody a fresh secp256k1 key is generated and sealed, which CHANGES the address — funds and approvals at the old address do not move. For a Safe the address is counterfactual and the owner shares are ring-managed, so rotation is a no-op and the address is unchanged. A backend that is not configured fails closed with 503 rather than leaving the wallet half-rotated.
     * Rolls one wallet\'s signing material through its own custody backend and answers the wallet with whatever address that produced.
     */
    async postWalletByIdKeys(requestParameters: WalletApiPostWalletByIdKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Wallet> {
        const response = await this.postWalletByIdKeysRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Produces a secp256k1 signature from one of the caller org\'s wallets over a 32-byte digest, through whichever custody backend that wallet uses. Give it either a `digest` (32 bytes as hex, signed verbatim) or a `message` (hashed with Keccak256 first) — exactly one is required. The private key never leaves its backend: KMS custody opens the sealed key in-process, MPC custody produces a threshold signature on the ring. The answer carries the digest that was signed alongside the signature, so a caller can verify what it got.
     * Produces a secp256k1 signature from one of the caller org\'s wallets over a 32-byte digest, through whichever custody backend that wallet uses.
     */
    async postWalletByIdSignRaw(requestParameters: WalletApiPostWalletByIdSignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Signature>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postWalletByIdSign().'
            );
        }

        if (requestParameters['signIn'] == null) {
            throw new runtime.RequiredError(
                'signIn',
                'Required parameter "signIn" was null or undefined when calling postWalletByIdSign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet/{id}/sign`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SignInToJSON(requestParameters['signIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SignatureFromJSON(jsonValue));
    }

    /**
     * Produces a secp256k1 signature from one of the caller org\'s wallets over a 32-byte digest, through whichever custody backend that wallet uses. Give it either a `digest` (32 bytes as hex, signed verbatim) or a `message` (hashed with Keccak256 first) — exactly one is required. The private key never leaves its backend: KMS custody opens the sealed key in-process, MPC custody produces a threshold signature on the ring. The answer carries the digest that was signed alongside the signature, so a caller can verify what it got.
     * Produces a secp256k1 signature from one of the caller org\'s wallets over a 32-byte digest, through whichever custody backend that wallet uses.
     */
    async postWalletByIdSign(requestParameters: WalletApiPostWalletByIdSignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Signature> {
        const response = await this.postWalletByIdSignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Composes a Safe transaction on the MPC ring and answers its EIP-712 hash together with the owner approval the ring\'s threshold signature produced. Only a wallet whose custody is \"safe\" can do this — any other custody is a 400, because the backend itself is asked whether it can propose rather than the kind being switched on. The ring computes the Safe-tx hash bound to the Safe contract and the chain id, so the hash a caller gets back is the one the Safe will verify. This PROPOSES: it does not execute the transaction.
     * Composes a Safe transaction on the MPC ring and answers its EIP-712 hash together with the owner approval the ring\'s threshold signature produced.
     */
    async postWalletByIdTransactionsRaw(requestParameters: WalletApiPostWalletByIdTransactionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SafeProposal>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postWalletByIdTransactions().'
            );
        }

        if (requestParameters['safeTxIn'] == null) {
            throw new runtime.RequiredError(
                'safeTxIn',
                'Required parameter "safeTxIn" was null or undefined when calling postWalletByIdTransactions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/wallet/{id}/transactions`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SafeTxInToJSON(requestParameters['safeTxIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SafeProposalFromJSON(jsonValue));
    }

    /**
     * Composes a Safe transaction on the MPC ring and answers its EIP-712 hash together with the owner approval the ring\'s threshold signature produced. Only a wallet whose custody is \"safe\" can do this — any other custody is a 400, because the backend itself is asked whether it can propose rather than the kind being switched on. The ring computes the Safe-tx hash bound to the Safe contract and the chain id, so the hash a caller gets back is the one the Safe will verify. This PROPOSES: it does not execute the transaction.
     * Composes a Safe transaction on the MPC ring and answers its EIP-712 hash together with the owner approval the ring\'s threshold signature produced.
     */
    async postWalletByIdTransactions(requestParameters: WalletApiPostWalletByIdTransactionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SafeProposal> {
        const response = await this.postWalletByIdTransactionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
