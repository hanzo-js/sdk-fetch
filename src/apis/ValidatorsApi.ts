/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ChallengeView,
  SlotView,
  ValidatorClaim,
  ValidatorList,
} from '../models/index.js';
import {
    ChallengeViewFromJSON,
    ChallengeViewToJSON,
    SlotViewFromJSON,
    SlotViewToJSON,
    ValidatorClaimFromJSON,
    ValidatorClaimToJSON,
    ValidatorListFromJSON,
    ValidatorListToJSON,
} from '../models/index.js';

export interface ValidatorsApiGetValidatorsRequest {
    limit?: string;
}

export interface ValidatorsApiGetValidatorsByTokenidRequest {
    tokenId: string;
}

export interface ValidatorsApiGetValidatorsChallengeRequest {
    tokenId?: string;
}

export interface ValidatorsApiPostValidatorsRequest {
    validatorClaim: ValidatorClaim;
}

/**
 * 
 */
export class ValidatorsApi extends runtime.BaseAPI {

    /**
     * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
     * Returns the validator slots the caller\'s org has claimed.
     */
    async getValidatorsRaw(requestParameters: ValidatorsApiGetValidatorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ValidatorList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/validators`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ValidatorListFromJSON(jsonValue));
    }

    /**
     * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
     * Returns the validator slots the caller\'s org has claimed.
     */
    async getValidators(requestParameters: ValidatorsApiGetValidatorsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ValidatorList> {
        const response = await this.getValidatorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
     * Returns one claimed validator slot, scoped to the caller\'s org.
     */
    async getValidatorsByTokenidRaw(requestParameters: ValidatorsApiGetValidatorsByTokenidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SlotView>> {
        if (requestParameters['tokenId'] == null) {
            throw new runtime.RequiredError(
                'tokenId',
                'Required parameter "tokenId" was null or undefined when calling getValidatorsByTokenid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/validators/{tokenId}`;
        urlPath = urlPath.replace(`{${"tokenId"}}`, encodeURIComponent(String(requestParameters['tokenId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SlotViewFromJSON(jsonValue));
    }

    /**
     * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
     * Returns one claimed validator slot, scoped to the caller\'s org.
     */
    async getValidatorsByTokenid(requestParameters: ValidatorsApiGetValidatorsByTokenidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SlotView> {
        const response = await this.getValidatorsByTokenidRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validators verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validators before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
     * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
     */
    async getValidatorsChallengeRaw(requestParameters: ValidatorsApiGetValidatorsChallengeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChallengeView>> {
        const queryParameters: any = {};

        if (requestParameters['tokenId'] != null) {
            queryParameters['tokenId'] = requestParameters['tokenId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/validators/challenge`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChallengeViewFromJSON(jsonValue));
    }

    /**
     * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validators verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validators before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
     * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
     */
    async getValidatorsChallenge(requestParameters: ValidatorsApiGetValidatorsChallengeRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChallengeView> {
        const response = await this.getValidatorsChallengeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
     * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
     */
    async postValidatorsRaw(requestParameters: ValidatorsApiPostValidatorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SlotView>> {
        if (requestParameters['validatorClaim'] == null) {
            throw new runtime.RequiredError(
                'validatorClaim',
                'Required parameter "validatorClaim" was null or undefined when calling postValidators().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/validators`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ValidatorClaimToJSON(requestParameters['validatorClaim']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SlotViewFromJSON(jsonValue));
    }

    /**
     * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
     * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
     */
    async postValidators(requestParameters: ValidatorsApiPostValidatorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SlotView> {
        const response = await this.postValidatorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
