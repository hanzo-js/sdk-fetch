/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Audience,
  AudienceList,
  AudiencePreview,
  CalendarPost,
  Campaign,
  CampaignList,
  EnrollInput,
  EnrollResult,
  EnrollmentList,
  PostList,
  PromoList,
  Quote,
  RedeemInput,
  RedeemResult,
  Redemption,
  ScheduleInput,
  Sequence,
  SequenceList,
  SequenceStatus,
  SequenceView,
  Step,
  StepInput,
  StepList,
  Summary,
  Suppression,
  SuppressionList,
  Unsubscribed,
} from '../models/index.js';
import {
    AudienceFromJSON,
    AudienceToJSON,
    AudienceListFromJSON,
    AudienceListToJSON,
    AudiencePreviewFromJSON,
    AudiencePreviewToJSON,
    CalendarPostFromJSON,
    CalendarPostToJSON,
    CampaignFromJSON,
    CampaignToJSON,
    CampaignListFromJSON,
    CampaignListToJSON,
    EnrollInputFromJSON,
    EnrollInputToJSON,
    EnrollResultFromJSON,
    EnrollResultToJSON,
    EnrollmentListFromJSON,
    EnrollmentListToJSON,
    PostListFromJSON,
    PostListToJSON,
    PromoListFromJSON,
    PromoListToJSON,
    QuoteFromJSON,
    QuoteToJSON,
    RedeemInputFromJSON,
    RedeemInputToJSON,
    RedeemResultFromJSON,
    RedeemResultToJSON,
    RedemptionFromJSON,
    RedemptionToJSON,
    ScheduleInputFromJSON,
    ScheduleInputToJSON,
    SequenceFromJSON,
    SequenceToJSON,
    SequenceListFromJSON,
    SequenceListToJSON,
    SequenceStatusFromJSON,
    SequenceStatusToJSON,
    SequenceViewFromJSON,
    SequenceViewToJSON,
    StepFromJSON,
    StepToJSON,
    StepInputFromJSON,
    StepInputToJSON,
    StepListFromJSON,
    StepListToJSON,
    SummaryFromJSON,
    SummaryToJSON,
    SuppressionFromJSON,
    SuppressionToJSON,
    SuppressionListFromJSON,
    SuppressionListToJSON,
    UnsubscribedFromJSON,
    UnsubscribedToJSON,
} from '../models/index.js';

export interface MarketingApiDeleteMarketingAudiencesByIdRequest {
    id: string;
}

export interface MarketingApiDeleteMarketingCalendarByIdRequest {
    id: string;
}

export interface MarketingApiDeleteMarketingCampaignsByIdRequest {
    id: string;
}

export interface MarketingApiDeleteMarketingSuppressionsRequest {
    channel?: string;
    address?: string;
    reason?: string;
    createdAt?: number;
}

export interface MarketingApiGetMarketingAudiencesRequest {
    limit?: number;
}

export interface MarketingApiGetMarketingAudiencesByIdRequest {
    id: string;
}

export interface MarketingApiGetMarketingAudiencesByIdPreviewRequest {
    id: string;
}

export interface MarketingApiGetMarketingCalendarRequest {
    status?: string;
    limit?: number;
}

export interface MarketingApiGetMarketingCalendarByIdRequest {
    id: string;
}

export interface MarketingApiGetMarketingCampaignsRequest {
    status?: string;
    limit?: number;
}

export interface MarketingApiGetMarketingCampaignsByIdRequest {
    id: string;
}

export interface MarketingApiGetMarketingPromosByCodeEligibilityRequest {
    code: string;
    plan?: string;
    seats?: number;
}

export interface MarketingApiGetMarketingPromosByCodeRedemptionRequest {
    code: string;
}

export interface MarketingApiGetMarketingSequencesRequest {
    limit?: number;
}

export interface MarketingApiGetMarketingSequencesByIdRequest {
    id: string;
}

export interface MarketingApiGetMarketingSequencesByIdEnrollmentsRequest {
    id: string;
    limit?: number;
}

export interface MarketingApiGetMarketingSequencesByIdStepsRequest {
    id: string;
}

export interface MarketingApiGetMarketingSuppressionsRequest {
    limit?: number;
}

export interface MarketingApiGetMarketingUnsubscribeRequest {
    org?: string;
    channel?: string;
    address?: string;
    token?: string;
}

export interface MarketingApiPostMarketingAudiencesRequest {
    audience: Audience;
}

export interface MarketingApiPostMarketingCalendarRequest {
    calendarPost: CalendarPost;
}

export interface MarketingApiPostMarketingCalendarByIdPublishRequest {
    id: string;
}

export interface MarketingApiPostMarketingCampaignsRequest {
    campaign: Campaign;
}

export interface MarketingApiPostMarketingCampaignsByIdScheduleRequest {
    id: string;
    scheduleInput: ScheduleInput;
}

export interface MarketingApiPostMarketingPromosByCodeRedeemRequest {
    code: string;
    redeemInput: RedeemInput;
}

export interface MarketingApiPostMarketingSequencesRequest {
    sequence: Sequence;
}

export interface MarketingApiPostMarketingSequencesByIdEnrollRequest {
    id: string;
    enrollInput: EnrollInput;
}

export interface MarketingApiPostMarketingSequencesByIdEnrollmentsByEidCancelRequest {
    id: string;
    eid: string;
}

export interface MarketingApiPostMarketingSequencesByIdStatusRequest {
    id: string;
    sequenceStatus: SequenceStatus;
}

export interface MarketingApiPostMarketingSequencesByIdStepsRequest {
    id: string;
    stepInput: StepInput;
}

export interface MarketingApiPostMarketingSuppressionsRequest {
    suppression: Suppression;
}

export interface MarketingApiPutMarketingCalendarByIdRequest {
    id: string;
    calendarPost: CalendarPost;
}

export interface MarketingApiPutMarketingCampaignsByIdRequest {
    id: string;
    campaign: Campaign;
}

/**
 * 
 */
export class MarketingApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s audiences and answers 204. It deletes the saved filter only — no customer, event or enrollment is touched.
     * Removes one of the caller org\'s audiences and answers 204.
     */
    async deleteMarketingAudiencesByIdRaw(requestParameters: MarketingApiDeleteMarketingAudiencesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteMarketingAudiencesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/audiences/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s audiences and answers 204. It deletes the saved filter only — no customer, event or enrollment is touched.
     * Removes one of the caller org\'s audiences and answers 204.
     */
    async deleteMarketingAudiencesById(requestParameters: MarketingApiDeleteMarketingAudiencesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMarketingAudiencesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one of the caller org\'s posts and answers 204. A post already published is deleted from the calendar only — nothing is retracted from the network it went out on.
     * Removes one of the caller org\'s posts and answers 204.
     */
    async deleteMarketingCalendarByIdRaw(requestParameters: MarketingApiDeleteMarketingCalendarByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteMarketingCalendarById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/calendar/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s posts and answers 204. A post already published is deleted from the calendar only — nothing is retracted from the network it went out on.
     * Removes one of the caller org\'s posts and answers 204.
     */
    async deleteMarketingCalendarById(requestParameters: MarketingApiDeleteMarketingCalendarByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMarketingCalendarByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one of the caller org\'s campaigns and answers 204. A campaign belonging to another org reads as not found and is left untouched.
     * Removes one of the caller org\'s campaigns and answers 204.
     */
    async deleteMarketingCampaignsByIdRaw(requestParameters: MarketingApiDeleteMarketingCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteMarketingCampaignsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s campaigns and answers 204. A campaign belonging to another org reads as not found and is left untouched.
     * Removes one of the caller org\'s campaigns and answers 204.
     */
    async deleteMarketingCampaignsById(requestParameters: MarketingApiDeleteMarketingCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMarketingCampaignsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Re-subscribes an address on one channel and answers 204. An address that is not on the list reads as not found.
     * Re-subscribes an address on one channel and answers 204.
     */
    async deleteMarketingSuppressionsRaw(requestParameters: MarketingApiDeleteMarketingSuppressionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        if (requestParameters['address'] != null) {
            queryParameters['address'] = requestParameters['address'];
        }

        if (requestParameters['reason'] != null) {
            queryParameters['reason'] = requestParameters['reason'];
        }

        if (requestParameters['createdAt'] != null) {
            queryParameters['createdAt'] = requestParameters['createdAt'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/suppressions`;

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Re-subscribes an address on one channel and answers 204. An address that is not on the list reads as not found.
     * Re-subscribes an address on one channel and answers 204.
     */
    async deleteMarketingSuppressions(requestParameters: MarketingApiDeleteMarketingSuppressionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMarketingSuppressionsRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the org\'s saved audiences, most recently updated first.
     * Returns the org\'s saved audiences, most recently updated first.
     */
    async getMarketingAudiencesRaw(requestParameters: MarketingApiGetMarketingAudiencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AudienceList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/audiences`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AudienceListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s saved audiences, most recently updated first.
     * Returns the org\'s saved audiences, most recently updated first.
     */
    async getMarketingAudiences(requestParameters: MarketingApiGetMarketingAudiencesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AudienceList> {
        const response = await this.getMarketingAudiencesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s saved audiences. An audience belonging to another org reads as not found.
     * Returns one of the caller org\'s saved audiences.
     */
    async getMarketingAudiencesByIdRaw(requestParameters: MarketingApiGetMarketingAudiencesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Audience>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingAudiencesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/audiences/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AudienceFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s saved audiences. An audience belonging to another org reads as not found.
     * Returns one of the caller org\'s saved audiences.
     */
    async getMarketingAudiencesById(requestParameters: MarketingApiGetMarketingAudiencesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Audience> {
        const response = await this.getMarketingAudiencesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Evaluates the cohort LIVE — the same resolution an enrollment would run — and reports how big it is and how many real mailboxes it reaches. It is the honest answer to \"is this send worth making\": a cohort of 500 that mails 3 says so, in deliverable and unmatched. Nothing is sent.
     * Evaluates the cohort LIVE — the same resolution an enrollment would run — and reports how big it is and how many real mailboxes it reaches.
     */
    async getMarketingAudiencesByIdPreviewRaw(requestParameters: MarketingApiGetMarketingAudiencesByIdPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AudiencePreview>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingAudiencesByIdPreview().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/audiences/{id}/preview`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AudiencePreviewFromJSON(jsonValue));
    }

    /**
     * Evaluates the cohort LIVE — the same resolution an enrollment would run — and reports how big it is and how many real mailboxes it reaches. It is the honest answer to \"is this send worth making\": a cohort of 500 that mails 3 says so, in deliverable and unmatched. Nothing is sent.
     * Evaluates the cohort LIVE — the same resolution an enrollment would run — and reports how big it is and how many real mailboxes it reaches.
     */
    async getMarketingAudiencesByIdPreview(requestParameters: MarketingApiGetMarketingAudiencesByIdPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AudiencePreview> {
        const response = await this.getMarketingAudiencesByIdPreviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s calendar, soonest scheduled first, optionally narrowed to one status.
     * Returns the org\'s calendar, soonest scheduled first, optionally narrowed to one status.
     */
    async getMarketingCalendarRaw(requestParameters: MarketingApiGetMarketingCalendarRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostList>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/calendar`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s calendar, soonest scheduled first, optionally narrowed to one status.
     * Returns the org\'s calendar, soonest scheduled first, optionally narrowed to one status.
     */
    async getMarketingCalendar(requestParameters: MarketingApiGetMarketingCalendarRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostList> {
        const response = await this.getMarketingCalendarRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s posts, including the exact error behind a failed publish. A post belonging to another org reads as not found.
     * Returns one of the caller org\'s posts, including the exact error behind a failed publish.
     */
    async getMarketingCalendarByIdRaw(requestParameters: MarketingApiGetMarketingCalendarByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CalendarPost>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingCalendarById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/calendar/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CalendarPostFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s posts, including the exact error behind a failed publish. A post belonging to another org reads as not found.
     * Returns one of the caller org\'s posts, including the exact error behind a failed publish.
     */
    async getMarketingCalendarById(requestParameters: MarketingApiGetMarketingCalendarByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CalendarPost> {
        const response = await this.getMarketingCalendarByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s campaigns, most recently updated first, optionally narrowed to one lifecycle status.
     * Returns the org\'s campaigns, most recently updated first, optionally narrowed to one lifecycle status.
     */
    async getMarketingCampaignsRaw(requestParameters: MarketingApiGetMarketingCampaignsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignList>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/campaigns`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s campaigns, most recently updated first, optionally narrowed to one lifecycle status.
     * Returns the org\'s campaigns, most recently updated first, optionally narrowed to one lifecycle status.
     */
    async getMarketingCampaigns(requestParameters: MarketingApiGetMarketingCampaignsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignList> {
        const response = await this.getMarketingCampaignsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s campaigns. A campaign belonging to another org reads as not found.
     * Returns one of the caller org\'s campaigns.
     */
    async getMarketingCampaignsByIdRaw(requestParameters: MarketingApiGetMarketingCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Campaign>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingCampaignsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s campaigns. A campaign belonging to another org reads as not found.
     * Returns one of the caller org\'s campaigns.
     */
    async getMarketingCampaignsById(requestParameters: MarketingApiGetMarketingCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Campaign> {
        const response = await this.getMarketingCampaignsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every promo the deployment offers with its live counters: how many orgs have redeemed it and how many redemptions remain under the cap. The promos are fleet-wide, not per-org — only the counters move.
     * Returns every promo the deployment offers with its live counters: how many orgs have redeemed it and how many redemptions remain under the cap.
     */
    async getMarketingPromosRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PromoList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/promos`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PromoListFromJSON(jsonValue));
    }

    /**
     * Returns every promo the deployment offers with its live counters: how many orgs have redeemed it and how many redemptions remain under the cap. The promos are fleet-wide, not per-org — only the counters move.
     * Returns every promo the deployment offers with its live counters: how many orgs have redeemed it and how many redemptions remain under the cap.
     */
    async getMarketingPromos(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PromoList> {
        const response = await this.getMarketingPromosRaw(initOverrides);
        return await response.value();
    }

    /**
     * Prices a promo against a plan and seat count. It is PURE: nothing is redeemed, credited or counted, so it is safe to call from a pricing page on every keystroke. An inactive promo or an exhausted cap quotes ineligible with the reason rather than erroring.
     * Prices a promo against a plan and seat count.
     */
    async getMarketingPromosByCodeEligibilityRaw(requestParameters: MarketingApiGetMarketingPromosByCodeEligibilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Quote>> {
        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling getMarketingPromosByCodeEligibility().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['plan'] != null) {
            queryParameters['plan'] = requestParameters['plan'];
        }

        if (requestParameters['seats'] != null) {
            queryParameters['seats'] = requestParameters['seats'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/promos/{code}/eligibility`;
        urlPath = urlPath.replace(`{${"code"}}`, encodeURIComponent(String(requestParameters['code'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QuoteFromJSON(jsonValue));
    }

    /**
     * Prices a promo against a plan and seat count. It is PURE: nothing is redeemed, credited or counted, so it is safe to call from a pricing page on every keystroke. An inactive promo or an exhausted cap quotes ineligible with the reason rather than erroring.
     * Prices a promo against a plan and seat count.
     */
    async getMarketingPromosByCodeEligibility(requestParameters: MarketingApiGetMarketingPromosByCodeEligibilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Quote> {
        const response = await this.getMarketingPromosByCodeEligibilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s OWN redemption of a promo — an org-scoped read, so it can never surface another tenant\'s. Not found when this org has not redeemed it.
     * Returns the caller org\'s OWN redemption of a promo — an org-scoped read, so it can never surface another tenant\'s.
     */
    async getMarketingPromosByCodeRedemptionRaw(requestParameters: MarketingApiGetMarketingPromosByCodeRedemptionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Redemption>> {
        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling getMarketingPromosByCodeRedemption().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/promos/{code}/redemption`;
        urlPath = urlPath.replace(`{${"code"}}`, encodeURIComponent(String(requestParameters['code'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RedemptionFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s OWN redemption of a promo — an org-scoped read, so it can never surface another tenant\'s. Not found when this org has not redeemed it.
     * Returns the caller org\'s OWN redemption of a promo — an org-scoped read, so it can never surface another tenant\'s.
     */
    async getMarketingPromosByCodeRedemption(requestParameters: MarketingApiGetMarketingPromosByCodeRedemptionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Redemption> {
        const response = await this.getMarketingPromosByCodeRedemptionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s drip sequences, most recently updated first.
     * Returns the org\'s drip sequences, most recently updated first.
     */
    async getMarketingSequencesRaw(requestParameters: MarketingApiGetMarketingSequencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SequenceList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SequenceListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s drip sequences, most recently updated first.
     * Returns the org\'s drip sequences, most recently updated first.
     */
    async getMarketingSequences(requestParameters: MarketingApiGetMarketingSequencesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SequenceList> {
        const response = await this.getMarketingSequencesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s sequences together with its steps in send order. A sequence belonging to another org reads as not found.
     * Returns one of the caller org\'s sequences together with its steps in send order.
     */
    async getMarketingSequencesByIdRaw(requestParameters: MarketingApiGetMarketingSequencesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SequenceView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingSequencesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SequenceViewFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s sequences together with its steps in send order. A sequence belonging to another org reads as not found.
     * Returns one of the caller org\'s sequences together with its steps in send order.
     */
    async getMarketingSequencesById(requestParameters: MarketingApiGetMarketingSequencesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SequenceView> {
        const response = await this.getMarketingSequencesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns who is walking one sequence, most recently enrolled first, with each walk\'s current step and next due time.
     * Returns who is walking one sequence, most recently enrolled first, with each walk\'s current step and next due time.
     */
    async getMarketingSequencesByIdEnrollmentsRaw(requestParameters: MarketingApiGetMarketingSequencesByIdEnrollmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EnrollmentList>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingSequencesByIdEnrollments().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}/enrollments`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EnrollmentListFromJSON(jsonValue));
    }

    /**
     * Returns who is walking one sequence, most recently enrolled first, with each walk\'s current step and next due time.
     * Returns who is walking one sequence, most recently enrolled first, with each walk\'s current step and next due time.
     */
    async getMarketingSequencesByIdEnrollments(requestParameters: MarketingApiGetMarketingSequencesByIdEnrollmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EnrollmentList> {
        const response = await this.getMarketingSequencesByIdEnrollmentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one sequence\'s steps in send order.
     * Returns one sequence\'s steps in send order.
     */
    async getMarketingSequencesByIdStepsRaw(requestParameters: MarketingApiGetMarketingSequencesByIdStepsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StepList>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMarketingSequencesByIdSteps().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}/steps`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StepListFromJSON(jsonValue));
    }

    /**
     * Returns one sequence\'s steps in send order.
     * Returns one sequence\'s steps in send order.
     */
    async getMarketingSequencesByIdSteps(requestParameters: MarketingApiGetMarketingSequencesByIdStepsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StepList> {
        const response = await this.getMarketingSequencesByIdStepsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rolls up the caller org\'s campaigns: how many there are, how many are active, and the summed budget and spend in cents.
     * Rolls up the caller org\'s campaigns: how many there are, how many are active, and the summed budget and spend in cents.
     */
    async getMarketingSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Summary>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SummaryFromJSON(jsonValue));
    }

    /**
     * Rolls up the caller org\'s campaigns: how many there are, how many are active, and the summed budget and spend in cents.
     * Rolls up the caller org\'s campaigns: how many there are, how many are active, and the summed budget and spend in cents.
     */
    async getMarketingSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Summary> {
        const response = await this.getMarketingSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s opt-out list, newest first — everyone the send gate will refuse to deliver to.
     * Returns the org\'s opt-out list, newest first — everyone the send gate will refuse to deliver to.
     */
    async getMarketingSuppressionsRaw(requestParameters: MarketingApiGetMarketingSuppressionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuppressionList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/suppressions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuppressionListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s opt-out list, newest first — everyone the send gate will refuse to deliver to.
     * Returns the org\'s opt-out list, newest first — everyone the send gate will refuse to deliver to.
     */
    async getMarketingSuppressions(requestParameters: MarketingApiGetMarketingSuppressionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuppressionList> {
        const response = await this.getMarketingSuppressionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the PUBLIC one-click endpoint (no principal): a recipient clicks the signed link in an email footer. The token binds (org, channel, address), so a caller can only opt OUT exactly the tuple it was minted for — never another address and never another org. An invalid token is refused, and a deployment with no KMS-sealed key refuses rather than accepting anything.
     * Is the PUBLIC one-click endpoint (no principal): a recipient clicks the signed link in an email footer.
     */
    async getMarketingUnsubscribeRaw(requestParameters: MarketingApiGetMarketingUnsubscribeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Unsubscribed>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        if (requestParameters['address'] != null) {
            queryParameters['address'] = requestParameters['address'];
        }

        if (requestParameters['token'] != null) {
            queryParameters['token'] = requestParameters['token'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/unsubscribe`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UnsubscribedFromJSON(jsonValue));
    }

    /**
     * Is the PUBLIC one-click endpoint (no principal): a recipient clicks the signed link in an email footer. The token binds (org, channel, address), so a caller can only opt OUT exactly the tuple it was minted for — never another address and never another org. An invalid token is refused, and a deployment with no KMS-sealed key refuses rather than accepting anything.
     * Is the PUBLIC one-click endpoint (no principal): a recipient clicks the signed link in an email footer.
     */
    async getMarketingUnsubscribe(requestParameters: MarketingApiGetMarketingUnsubscribeRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Unsubscribed> {
        const response = await this.getMarketingUnsubscribeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Saves a cohort filter for the caller\'s org. Name is required. Omitting event saves the WHOLE-ORG audience — every mailable customer — which needs no analytics warehouse; naming one narrows that roster to the customers who fired it within windowDays.
     * Saves a cohort filter for the caller\'s org.
     */
    async postMarketingAudiencesRaw(requestParameters: MarketingApiPostMarketingAudiencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Audience>> {
        if (requestParameters['audience'] == null) {
            throw new runtime.RequiredError(
                'audience',
                'Required parameter "audience" was null or undefined when calling postMarketingAudiences().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/audiences`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AudienceToJSON(requestParameters['audience']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AudienceFromJSON(jsonValue));
    }

    /**
     * Saves a cohort filter for the caller\'s org. Name is required. Omitting event saves the WHOLE-ORG audience — every mailable customer — which needs no analytics warehouse; naming one narrows that roster to the customers who fired it within windowDays.
     * Saves a cohort filter for the caller\'s org.
     */
    async postMarketingAudiences(requestParameters: MarketingApiPostMarketingAudiencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Audience> {
        const response = await this.postMarketingAudiencesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a post to the content calendar. Channel and body are required. A scheduledAt in the future makes the post \"scheduled\" and the durable sweep publishes it when it comes due — claimed once, so a post publishes at most once; without one it stays a draft.
     * Adds a post to the content calendar.
     */
    async postMarketingCalendarRaw(requestParameters: MarketingApiPostMarketingCalendarRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CalendarPost>> {
        if (requestParameters['calendarPost'] == null) {
            throw new runtime.RequiredError(
                'calendarPost',
                'Required parameter "calendarPost" was null or undefined when calling postMarketingCalendar().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/calendar`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CalendarPostToJSON(requestParameters['calendarPost']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CalendarPostFromJSON(jsonValue));
    }

    /**
     * Adds a post to the content calendar. Channel and body are required. A scheduledAt in the future makes the post \"scheduled\" and the durable sweep publishes it when it comes due — claimed once, so a post publishes at most once; without one it stays a draft.
     * Adds a post to the content calendar.
     */
    async postMarketingCalendar(requestParameters: MarketingApiPostMarketingCalendarRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CalendarPost> {
        const response = await this.postMarketingCalendarRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publishes a post NOW, synchronously, whatever its schedule. No social connector is wired today, so every channel answers an honest 501 naming the seam a real one would plug into, and the post is recorded failed with that exact reason — never a faked \"published\".
     * Publishes a post NOW, synchronously, whatever its schedule.
     */
    async postMarketingCalendarByIdPublishRaw(requestParameters: MarketingApiPostMarketingCalendarByIdPublishRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CalendarPost>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postMarketingCalendarByIdPublish().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/calendar/{id}/publish`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CalendarPostFromJSON(jsonValue));
    }

    /**
     * Publishes a post NOW, synchronously, whatever its schedule. No social connector is wired today, so every channel answers an honest 501 naming the seam a real one would plug into, and the post is recorded failed with that exact reason — never a faked \"published\".
     * Publishes a post NOW, synchronously, whatever its schedule.
     */
    async postMarketingCalendarByIdPublish(requestParameters: MarketingApiPostMarketingCalendarByIdPublishRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CalendarPost> {
        const response = await this.postMarketingCalendarByIdPublishRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Registers a campaign in the caller\'s org. Name is required; channel defaults to email and status to draft, and a future scheduledAt with no explicit status makes the campaign \"scheduled\". Budget and spend are cents and are clamped to >= 0. The id, createdAt and updatedAt of the input are ignored — the server assigns them.
     * Registers a campaign in the caller\'s org.
     */
    async postMarketingCampaignsRaw(requestParameters: MarketingApiPostMarketingCampaignsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Campaign>> {
        if (requestParameters['campaign'] == null) {
            throw new runtime.RequiredError(
                'campaign',
                'Required parameter "campaign" was null or undefined when calling postMarketingCampaigns().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/campaigns`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignToJSON(requestParameters['campaign']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignFromJSON(jsonValue));
    }

    /**
     * Registers a campaign in the caller\'s org. Name is required; channel defaults to email and status to draft, and a future scheduledAt with no explicit status makes the campaign \"scheduled\". Budget and spend are cents and are clamped to >= 0. The id, createdAt and updatedAt of the input are ignored — the server assigns them.
     * Registers a campaign in the caller\'s org.
     */
    async postMarketingCampaigns(requestParameters: MarketingApiPostMarketingCampaignsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Campaign> {
        const response = await this.postMarketingCampaignsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets a campaign\'s send time and moves it to \"scheduled\". A scheduledAt of 0 clears the schedule and returns it to \"draft\".
     * Sets a campaign\'s send time and moves it to \"scheduled\".
     */
    async postMarketingCampaignsByIdScheduleRaw(requestParameters: MarketingApiPostMarketingCampaignsByIdScheduleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Campaign>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postMarketingCampaignsByIdSchedule().'
            );
        }

        if (requestParameters['scheduleInput'] == null) {
            throw new runtime.RequiredError(
                'scheduleInput',
                'Required parameter "scheduleInput" was null or undefined when calling postMarketingCampaignsByIdSchedule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/campaigns/{id}/schedule`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScheduleInputToJSON(requestParameters['scheduleInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignFromJSON(jsonValue));
    }

    /**
     * Sets a campaign\'s send time and moves it to \"scheduled\". A scheduledAt of 0 clears the schedule and returns it to \"draft\".
     * Sets a campaign\'s send time and moves it to \"scheduled\".
     */
    async postMarketingCampaignsByIdSchedule(requestParameters: MarketingApiPostMarketingCampaignsByIdScheduleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Campaign> {
        const response = await this.postMarketingCampaignsByIdScheduleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records the caller org\'s claim on a promo. NOTHING IS CREDITED: the redemption is a row, and credit into an org is an admin decision made on the admin surface against an auditable ledger.  The plan is DERIVED from the org\'s live ACTIVE/TRIALING paid subscription and can never be named by the caller — an org with no qualifying subscription is refused, and so is one whose subscription cannot be read. The seat count is the single-seat floor (claimSeats), so the recorded figure has no input that can inflate it.  Guards run under one lock so the cap cannot be raced past: the fleet-wide redemption cap, one redemption per org, one per payment instrument (REQUIRED), and the per-redemption ceiling.  It is IDEMPOTENT: an org that already redeemed gets its original redemption back with alreadyRedeemed true.
     * Records the caller org\'s claim on a promo.
     */
    async postMarketingPromosByCodeRedeemRaw(requestParameters: MarketingApiPostMarketingPromosByCodeRedeemRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RedeemResult>> {
        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling postMarketingPromosByCodeRedeem().'
            );
        }

        if (requestParameters['redeemInput'] == null) {
            throw new runtime.RequiredError(
                'redeemInput',
                'Required parameter "redeemInput" was null or undefined when calling postMarketingPromosByCodeRedeem().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/promos/{code}/redeem`;
        urlPath = urlPath.replace(`{${"code"}}`, encodeURIComponent(String(requestParameters['code'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RedeemInputToJSON(requestParameters['redeemInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RedeemResultFromJSON(jsonValue));
    }

    /**
     * Records the caller org\'s claim on a promo. NOTHING IS CREDITED: the redemption is a row, and credit into an org is an admin decision made on the admin surface against an auditable ledger.  The plan is DERIVED from the org\'s live ACTIVE/TRIALING paid subscription and can never be named by the caller — an org with no qualifying subscription is refused, and so is one whose subscription cannot be read. The seat count is the single-seat floor (claimSeats), so the recorded figure has no input that can inflate it.  Guards run under one lock so the cap cannot be raced past: the fleet-wide redemption cap, one redemption per org, one per payment instrument (REQUIRED), and the per-redemption ceiling.  It is IDEMPOTENT: an org that already redeemed gets its original redemption back with alreadyRedeemed true.
     * Records the caller org\'s claim on a promo.
     */
    async postMarketingPromosByCodeRedeem(requestParameters: MarketingApiPostMarketingPromosByCodeRedeemRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RedeemResult> {
        const response = await this.postMarketingPromosByCodeRedeemRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Registers a drip sequence in the caller\'s org. Name is required; status defaults to draft, and a sequence must be ACTIVE before it will accept enrollments. The id, createdAt and updatedAt of the input are ignored — the server assigns them.
     * Registers a drip sequence in the caller\'s org.
     */
    async postMarketingSequencesRaw(requestParameters: MarketingApiPostMarketingSequencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Sequence>> {
        if (requestParameters['sequence'] == null) {
            throw new runtime.RequiredError(
                'sequence',
                'Required parameter "sequence" was null or undefined when calling postMarketingSequences().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SequenceToJSON(requestParameters['sequence']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SequenceFromJSON(jsonValue));
    }

    /**
     * Registers a drip sequence in the caller\'s org. Name is required; status defaults to draft, and a sequence must be ACTIVE before it will accept enrollments. The id, createdAt and updatedAt of the input are ignored — the server assigns them.
     * Registers a drip sequence in the caller\'s org.
     */
    async postMarketingSequences(requestParameters: MarketingApiPostMarketingSequencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Sequence> {
        const response = await this.postMarketingSequencesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds one contact or a whole audience to a sequence and schedules the first step for each. The sequence must be ACTIVE (a draft sends nothing), and the request must name exactly one of address or audienceId.  Enrolling is ALL this does: the message itself is sent later by the drip engine, through the suppression gate, so an opted-out customer can be enrolled here and still never be mailed. Re-posting is safe — an address this sequence already took is counted in alreadyEnrolled and never double-dripped — which is what makes retrying a partially-applied announcement a resume rather than a second send.
     * Adds one contact or a whole audience to a sequence and schedules the first step for each.
     */
    async postMarketingSequencesByIdEnrollRaw(requestParameters: MarketingApiPostMarketingSequencesByIdEnrollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EnrollResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postMarketingSequencesByIdEnroll().'
            );
        }

        if (requestParameters['enrollInput'] == null) {
            throw new runtime.RequiredError(
                'enrollInput',
                'Required parameter "enrollInput" was null or undefined when calling postMarketingSequencesByIdEnroll().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}/enroll`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EnrollInputToJSON(requestParameters['enrollInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EnrollResultFromJSON(jsonValue));
    }

    /**
     * Adds one contact or a whole audience to a sequence and schedules the first step for each. The sequence must be ACTIVE (a draft sends nothing), and the request must name exactly one of address or audienceId.  Enrolling is ALL this does: the message itself is sent later by the drip engine, through the suppression gate, so an opted-out customer can be enrolled here and still never be mailed. Re-posting is safe — an address this sequence already took is counted in alreadyEnrolled and never double-dripped — which is what makes retrying a partially-applied announcement a resume rather than a second send.
     * Adds one contact or a whole audience to a sequence and schedules the first step for each.
     */
    async postMarketingSequencesByIdEnroll(requestParameters: MarketingApiPostMarketingSequencesByIdEnrollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EnrollResult> {
        const response = await this.postMarketingSequencesByIdEnrollRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stops one walk mid-sequence and answers 204: no further step is sent, and steps already delivered are not recalled. Only an ACTIVE enrollment can be canceled — one already completed or canceled reads as not found.
     * Stops one walk mid-sequence and answers 204: no further step is sent, and steps already delivered are not recalled.
     */
    async postMarketingSequencesByIdEnrollmentsByEidCancelRaw(requestParameters: MarketingApiPostMarketingSequencesByIdEnrollmentsByEidCancelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postMarketingSequencesByIdEnrollmentsByEidCancel().'
            );
        }

        if (requestParameters['eid'] == null) {
            throw new runtime.RequiredError(
                'eid',
                'Required parameter "eid" was null or undefined when calling postMarketingSequencesByIdEnrollmentsByEidCancel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}/enrollments/{eid}/cancel`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));
        urlPath = urlPath.replace(`{${"eid"}}`, encodeURIComponent(String(requestParameters['eid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Stops one walk mid-sequence and answers 204: no further step is sent, and steps already delivered are not recalled. Only an ACTIVE enrollment can be canceled — one already completed or canceled reads as not found.
     * Stops one walk mid-sequence and answers 204: no further step is sent, and steps already delivered are not recalled.
     */
    async postMarketingSequencesByIdEnrollmentsByEidCancel(requestParameters: MarketingApiPostMarketingSequencesByIdEnrollmentsByEidCancelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postMarketingSequencesByIdEnrollmentsByEidCancelRaw(requestParameters, initOverrides);
    }

    /**
     * Flips draft/active/archived — the activation gate for sending, since only an active sequence accepts enrollments. It does not touch enrollments already walking: archiving stops new ones, not in-flight ones.
     * Flips draft/active/archived — the activation gate for sending, since only an active sequence accepts enrollments.
     */
    async postMarketingSequencesByIdStatusRaw(requestParameters: MarketingApiPostMarketingSequencesByIdStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SequenceStatus>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postMarketingSequencesByIdStatus().'
            );
        }

        if (requestParameters['sequenceStatus'] == null) {
            throw new runtime.RequiredError(
                'sequenceStatus',
                'Required parameter "sequenceStatus" was null or undefined when calling postMarketingSequencesByIdStatus().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}/status`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SequenceStatusToJSON(requestParameters['sequenceStatus']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SequenceStatusFromJSON(jsonValue));
    }

    /**
     * Flips draft/active/archived — the activation gate for sending, since only an active sequence accepts enrollments. It does not touch enrollments already walking: archiving stops new ones, not in-flight ones.
     * Flips draft/active/archived — the activation gate for sending, since only an active sequence accepts enrollments.
     */
    async postMarketingSequencesByIdStatus(requestParameters: MarketingApiPostMarketingSequencesByIdStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SequenceStatus> {
        const response = await this.postMarketingSequencesByIdStatusRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Appends a message to the END of a sequence: the new step\'s idx is one past the last, so steps arrive in the order they are added. Body is required and delaySeconds must be >= 0. Adding a step does not disturb enrollments already walking — one that has passed this index simply never sees it.
     * Appends a message to the END of a sequence: the new step\'s idx is one past the last, so steps arrive in the order they are added.
     */
    async postMarketingSequencesByIdStepsRaw(requestParameters: MarketingApiPostMarketingSequencesByIdStepsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Step>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postMarketingSequencesByIdSteps().'
            );
        }

        if (requestParameters['stepInput'] == null) {
            throw new runtime.RequiredError(
                'stepInput',
                'Required parameter "stepInput" was null or undefined when calling postMarketingSequencesByIdSteps().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/sequences/{id}/steps`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StepInputToJSON(requestParameters['stepInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StepFromJSON(jsonValue));
    }

    /**
     * Appends a message to the END of a sequence: the new step\'s idx is one past the last, so steps arrive in the order they are added. Body is required and delaySeconds must be >= 0. Adding a step does not disturb enrollments already walking — one that has passed this index simply never sees it.
     * Appends a message to the END of a sequence: the new step\'s idx is one past the last, so steps arrive in the order they are added.
     */
    async postMarketingSequencesByIdSteps(requestParameters: MarketingApiPostMarketingSequencesByIdStepsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Step> {
        const response = await this.postMarketingSequencesByIdStepsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records an opt-out for the org (admin / self-service management). Address is required; channel defaults to email. It is idempotent: re-suppressing the same tuple keeps the original record rather than erroring. From here on the ONE send gate refuses that recipient on that channel.
     * Records an opt-out for the org (admin / self-service management).
     */
    async postMarketingSuppressionsRaw(requestParameters: MarketingApiPostMarketingSuppressionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Suppression>> {
        if (requestParameters['suppression'] == null) {
            throw new runtime.RequiredError(
                'suppression',
                'Required parameter "suppression" was null or undefined when calling postMarketingSuppressions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/suppressions`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SuppressionToJSON(requestParameters['suppression']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuppressionFromJSON(jsonValue));
    }

    /**
     * Records an opt-out for the org (admin / self-service management). Address is required; channel defaults to email. It is idempotent: re-suppressing the same tuple keeps the original record rather than erroring. From here on the ONE send gate refuses that recipient on that channel.
     * Records an opt-out for the org (admin / self-service management).
     */
    async postMarketingSuppressions(requestParameters: MarketingApiPostMarketingSuppressionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Suppression> {
        const response = await this.postMarketingSuppressionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces a post\'s editable fields. It is a full write, not a patch, and it RESETS the lifecycle from the schedule: a scheduledAt makes the post \"scheduled\" again and none makes it a draft — so editing a failed post requeues it rather than leaving it stuck.
     * Replaces a post\'s editable fields.
     */
    async putMarketingCalendarByIdRaw(requestParameters: MarketingApiPutMarketingCalendarByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CalendarPost>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putMarketingCalendarById().'
            );
        }

        if (requestParameters['calendarPost'] == null) {
            throw new runtime.RequiredError(
                'calendarPost',
                'Required parameter "calendarPost" was null or undefined when calling putMarketingCalendarById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/calendar/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CalendarPostToJSON(requestParameters['calendarPost']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CalendarPostFromJSON(jsonValue));
    }

    /**
     * Replaces a post\'s editable fields. It is a full write, not a patch, and it RESETS the lifecycle from the schedule: a scheduledAt makes the post \"scheduled\" again and none makes it a draft — so editing a failed post requeues it rather than leaving it stuck.
     * Replaces a post\'s editable fields.
     */
    async putMarketingCalendarById(requestParameters: MarketingApiPutMarketingCalendarByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CalendarPost> {
        const response = await this.putMarketingCalendarByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces a campaign\'s editable fields. It is a full write, not a patch: every field takes the value in the body, and an omitted one is cleared. The id comes from the path — the body cannot retarget another campaign — and createdAt is never rewritten.
     * Replaces a campaign\'s editable fields.
     */
    async putMarketingCampaignsByIdRaw(requestParameters: MarketingApiPutMarketingCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Campaign>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putMarketingCampaignsById().'
            );
        }

        if (requestParameters['campaign'] == null) {
            throw new runtime.RequiredError(
                'campaign',
                'Required parameter "campaign" was null or undefined when calling putMarketingCampaignsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/marketing/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignToJSON(requestParameters['campaign']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignFromJSON(jsonValue));
    }

    /**
     * Replaces a campaign\'s editable fields. It is a full write, not a patch: every field takes the value in the body, and an omitted one is cleared. The id comes from the path — the body cannot retarget another campaign — and createdAt is never rewritten.
     * Replaces a campaign\'s editable fields.
     */
    async putMarketingCampaignsById(requestParameters: MarketingApiPutMarketingCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Campaign> {
        const response = await this.putMarketingCampaignsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
