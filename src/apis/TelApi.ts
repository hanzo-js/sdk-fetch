/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  BuyInput,
  Call,
  CallInput,
  CallList,
  MessageInput,
  MessageList,
  Number,
  NumberList,
  SMS,
  Summary,
} from '../models/index.js';
import {
    BuyInputFromJSON,
    BuyInputToJSON,
    CallFromJSON,
    CallToJSON,
    CallInputFromJSON,
    CallInputToJSON,
    CallListFromJSON,
    CallListToJSON,
    MessageInputFromJSON,
    MessageInputToJSON,
    MessageListFromJSON,
    MessageListToJSON,
    NumberFromJSON,
    NumberToJSON,
    NumberListFromJSON,
    NumberListToJSON,
    SMSFromJSON,
    SMSToJSON,
    SummaryFromJSON,
    SummaryToJSON,
} from '../models/index.js';

export interface TelApiDeleteTelCallsByIdRequest {
    id: string;
}

export interface TelApiDeleteTelNumbersByIdRequest {
    id: string;
}

export interface TelApiGetTelNumbersAvailableRequest {
    country?: string;
    area?: string;
    type?: string;
    limit?: number;
}

export interface TelApiPostTelCallsRequest {
    callInput: CallInput;
}

export interface TelApiPostTelMessagesRequest {
    messageInput: MessageInput;
}

export interface TelApiPostTelNumbersRequest {
    buyInput: BuyInput;
}

/**
 * 
 */
export class TelApi extends runtime.BaseAPI {

    /**
     * Ends a call this org placed. The holding is read for THIS org before the carrier is asked, for the reason releaseNumber gives one surface up: an id belonging to another tenant would otherwise be hung up by whoever guessed it.
     * Ends a call this org placed.
     */
    async deleteTelCallsByIdRaw(requestParameters: TelApiDeleteTelCallsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTelCallsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/calls/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Ends a call this org placed. The holding is read for THIS org before the carrier is asked, for the reason releaseNumber gives one surface up: an id belonging to another tenant would otherwise be hung up by whoever guessed it.
     * Ends a call this org placed.
     */
    async deleteTelCallsById(requestParameters: TelApiDeleteTelCallsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.deleteTelCallsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Checks the holding is THIS org\'s before it reaches the carrier. Without that read, an id belonging to another tenant would be released by whoever guessed it.
     * Checks the holding is THIS org\'s before it reaches the carrier.
     */
    async deleteTelNumbersByIdRaw(requestParameters: TelApiDeleteTelNumbersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTelNumbersById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/numbers/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Checks the holding is THIS org\'s before it reaches the carrier. Without that read, an id belonging to another tenant would be released by whoever guessed it.
     * Checks the holding is THIS org\'s before it reaches the carrier.
     */
    async deleteTelNumbersById(requestParameters: TelApiDeleteTelNumbersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.deleteTelNumbersByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the calls this org has placed or received, newest first. Like the message list beside it, these are our own records rather than the carrier\'s.
     * Lists the calls this org has placed or received, newest first.
     */
    async getTelCallsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CallList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/calls`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CallListFromJSON(jsonValue));
    }

    /**
     * Lists the calls this org has placed or received, newest first. Like the message list beside it, these are our own records rather than the carrier\'s.
     * Lists the calls this org has placed or received, newest first.
     */
    async getTelCalls(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CallList> {
        const response = await this.getTelCallsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the messages this org has sent or received, newest first. Records from our own store, not the carrier\'s — so it is what this platform did on the org\'s behalf, which is the set an audit or a bill has to agree with.
     * Lists the messages this org has sent or received, newest first.
     */
    async getTelMessagesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessageList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/messages`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageListFromJSON(jsonValue));
    }

    /**
     * Lists the messages this org has sent or received, newest first. Records from our own store, not the carrier\'s — so it is what this platform did on the org\'s behalf, which is the set an audit or a bill has to agree with.
     * Lists the messages this org has sent or received, newest first.
     */
    async getTelMessages(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessageList> {
        const response = await this.getTelMessagesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the phone numbers this org HOLDS — the ones it has bought and not released. Distinct from the availability search one path down (`/numbers/available`), which asks the carrier what could be bought: this answers only from our own store, so it is what an org owns rather than what it could own.
     * Lists the phone numbers this org HOLDS — the ones it has bought and not released.
     */
    async getTelNumbersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NumberList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/numbers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NumberListFromJSON(jsonValue));
    }

    /**
     * Lists the phone numbers this org HOLDS — the ones it has bought and not released. Distinct from the availability search one path down (`/numbers/available`), which asks the carrier what could be bought: this answers only from our own store, so it is what an org owns rather than what it could own.
     * Lists the phone numbers this org HOLDS — the ones it has bought and not released.
     */
    async getTelNumbers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NumberList> {
        const response = await this.getTelNumbersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Asks the carrier what is available to buy. Nothing is recorded — a search is not a holding, and treating it as one is how inventory leaks.
     * Asks the carrier what is available to buy.
     */
    async getTelNumbersAvailableRaw(requestParameters: TelApiGetTelNumbersAvailableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NumberList>> {
        const queryParameters: any = {};

        if (requestParameters['country'] != null) {
            queryParameters['Country'] = requestParameters['country'];
        }

        if (requestParameters['area'] != null) {
            queryParameters['Area'] = requestParameters['area'];
        }

        if (requestParameters['type'] != null) {
            queryParameters['Type'] = requestParameters['type'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['Limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/numbers/available`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NumberListFromJSON(jsonValue));
    }

    /**
     * Asks the carrier what is available to buy. Nothing is recorded — a search is not a holding, and treating it as one is how inventory leaks.
     * Asks the carrier what is available to buy.
     */
    async getTelNumbersAvailable(requestParameters: TelApiGetTelNumbersAvailableRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NumberList> {
        const response = await this.getTelNumbersAvailableRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Counts what this org holds on the telephony plane: its numbers, its calls and its messages. The one read a dashboard makes before it asks for any list, so it answers three totals and no rows.
     * Counts what this org holds on the telephony plane: its numbers, its calls and its messages.
     */
    async getTelSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Summary>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SummaryFromJSON(jsonValue));
    }

    /**
     * Counts what this org holds on the telephony plane: its numbers, its calls and its messages. The one read a dashboard makes before it asks for any list, so it answers three totals and no rows.
     * Counts what this org holds on the telephony plane: its numbers, its calls and its messages.
     */
    async getTelSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Summary> {
        const response = await this.getTelSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Dials. An `agent` names a Hanzo assistant to answer it; the call is refused up front when no assistant plane is configured, because a call that connects to silence has already cost the person who answered it.
     * Dials.
     */
    async postTelCallsRaw(requestParameters: TelApiPostTelCallsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Call>> {
        if (requestParameters['callInput'] == null) {
            throw new runtime.RequiredError(
                'callInput',
                'Required parameter "callInput" was null or undefined when calling postTelCalls().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/calls`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CallInputToJSON(requestParameters['callInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CallFromJSON(jsonValue));
    }

    /**
     * Dials. An `agent` names a Hanzo assistant to answer it; the call is refused up front when no assistant plane is configured, because a call that connects to silence has already cost the person who answered it.
     * Dials.
     */
    async postTelCalls(requestParameters: TelApiPostTelCallsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Call> {
        const response = await this.postTelCallsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sends a message from one of this org\'s own numbers.  `from` must be a number the org HOLDS, checked against the store rather than taken on trust — a caller that could send from any number could impersonate one, and the carrier would deliver it. `to` is required, and the body needs text or media, because a message with neither is delivered as nothing and billed as something.
     * Sends a message from one of this org\'s own numbers.
     */
    async postTelMessagesRaw(requestParameters: TelApiPostTelMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SMS>> {
        if (requestParameters['messageInput'] == null) {
            throw new runtime.RequiredError(
                'messageInput',
                'Required parameter "messageInput" was null or undefined when calling postTelMessages().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/messages`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MessageInputToJSON(requestParameters['messageInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SMSFromJSON(jsonValue));
    }

    /**
     * Sends a message from one of this org\'s own numbers.  `from` must be a number the org HOLDS, checked against the store rather than taken on trust — a caller that could send from any number could impersonate one, and the carrier would deliver it. `to` is required, and the body needs text or media, because a message with neither is delivered as nothing and billed as something.
     * Sends a message from one of this org\'s own numbers.
     */
    async postTelMessages(requestParameters: TelApiPostTelMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SMS> {
        const response = await this.postTelMessagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Provisions with the carrier FIRST and records second. The other order records a holding that may not exist, and a number the platform believes it owns but cannot use is worse than one it failed to buy.
     * Provisions with the carrier FIRST and records second.
     */
    async postTelNumbersRaw(requestParameters: TelApiPostTelNumbersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Number>> {
        if (requestParameters['buyInput'] == null) {
            throw new runtime.RequiredError(
                'buyInput',
                'Required parameter "buyInput" was null or undefined when calling postTelNumbers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tel/numbers`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BuyInputToJSON(requestParameters['buyInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NumberFromJSON(jsonValue));
    }

    /**
     * Provisions with the carrier FIRST and records second. The other order records a holding that may not exist, and a number the platform believes it owns but cannot use is worse than one it failed to buy.
     * Provisions with the carrier FIRST and records second.
     */
    async postTelNumbers(requestParameters: TelApiPostTelNumbersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Number> {
        const response = await this.postTelNumbersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
