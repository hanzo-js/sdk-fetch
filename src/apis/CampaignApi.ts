/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CampaignPage,
  CampaignRecord,
  CampaignResults,
  CampaignSummary,
  CampaignUpdate,
  CampaignWrite,
  ChannelAdd,
} from '../models/index.js';
import {
    CampaignPageFromJSON,
    CampaignPageToJSON,
    CampaignRecordFromJSON,
    CampaignRecordToJSON,
    CampaignResultsFromJSON,
    CampaignResultsToJSON,
    CampaignSummaryFromJSON,
    CampaignSummaryToJSON,
    CampaignUpdateFromJSON,
    CampaignUpdateToJSON,
    CampaignWriteFromJSON,
    CampaignWriteToJSON,
    ChannelAddFromJSON,
    ChannelAddToJSON,
} from '../models/index.js';

export interface CampaignApiDeleteCampaignByIdRequest {
    id: string;
}

export interface CampaignApiDeleteCampaignByIdChannelsByKindRequest {
    id: string;
    kind: string;
}

export interface CampaignApiGetCampaignRequest {
    status?: string;
    limit?: number;
}

export interface CampaignApiGetCampaignByIdRequest {
    id: string;
}

export interface CampaignApiGetCampaignByIdMetricsRequest {
    id: string;
    range?: string;
    start?: string;
    end?: string;
}

export interface CampaignApiPostCampaignRequest {
    campaignWrite: CampaignWrite;
}

export interface CampaignApiPostCampaignByIdChannelsRequest {
    id: string;
    channelAdd: ChannelAdd;
}

export interface CampaignApiPostCampaignByIdLaunchRequest {
    id: string;
}

export interface CampaignApiPostCampaignByIdPauseRequest {
    id: string;
}

export interface CampaignApiPutCampaignByIdRequest {
    id: string;
    campaignUpdate: CampaignUpdate;
}

/**
 * 
 */
export class CampaignApi extends runtime.BaseAPI {

    /**
     * Removes one campaign of the caller\'s org and answers 204 with no body. 404 when the org has no campaign with that id.  It deletes the RECORD, not the executions: a campaign whose channels are live on a provider should be paused first, or those executions keep running with nothing here to report them.
     * Removes one campaign of the caller\'s org and answers 204 with no body.
     */
    async deleteCampaignByIdRaw(requestParameters: CampaignApiDeleteCampaignByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCampaignById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one campaign of the caller\'s org and answers 204 with no body. 404 when the org has no campaign with that id.  It deletes the RECORD, not the executions: a campaign whose channels are live on a provider should be paused first, or those executions keep running with nothing here to report them.
     * Removes one campaign of the caller\'s org and answers 204 with no body.
     */
    async deleteCampaignById(requestParameters: CampaignApiDeleteCampaignByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteCampaignByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Drops one channel from a campaign and returns the updated campaign. 404 when the campaign carries no channel of that kind.  It removes the channel from the PLAN. A channel that is live at its provider should be paused first — dropping the row here leaves nothing to pause it with afterwards.
     * Drops one channel from a campaign and returns the updated campaign.
     */
    async deleteCampaignByIdChannelsByKindRaw(requestParameters: CampaignApiDeleteCampaignByIdChannelsByKindRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignRecord>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCampaignByIdChannelsByKind().'
            );
        }

        if (requestParameters['kind'] == null) {
            throw new runtime.RequiredError(
                'kind',
                'Required parameter "kind" was null or undefined when calling deleteCampaignByIdChannelsByKind().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}/channels/{kind}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));
        urlPath = urlPath.replace(`{${"kind"}}`, encodeURIComponent(String(requestParameters['kind'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignRecordFromJSON(jsonValue));
    }

    /**
     * Drops one channel from a campaign and returns the updated campaign. 404 when the campaign carries no channel of that kind.  It removes the channel from the PLAN. A channel that is live at its provider should be paused first — dropping the row here leaves nothing to pause it with afterwards.
     * Drops one channel from a campaign and returns the updated campaign.
     */
    async deleteCampaignByIdChannelsByKind(requestParameters: CampaignApiDeleteCampaignByIdChannelsByKindRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignRecord> {
        const response = await this.deleteCampaignByIdChannelsByKindRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s campaigns, newest first, optionally narrowed to one status.  A campaign is the top-level go-to-market object: a value that SPANS channels (paid, organic, email) and fans out to the executor for each. The listing is org-scoped server-side, so one org can never see another\'s campaigns.
     * Returns the org\'s campaigns, newest first, optionally narrowed to one status.
     */
    async getCampaignRaw(requestParameters: CampaignApiGetCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignPage>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignPageFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s campaigns, newest first, optionally narrowed to one status.  A campaign is the top-level go-to-market object: a value that SPANS channels (paid, organic, email) and fans out to the executor for each. The listing is org-scoped server-side, so one org can never see another\'s campaigns.
     * Returns the org\'s campaigns, newest first, optionally narrowed to one status.
     */
    async getCampaign(requestParameters: CampaignApiGetCampaignRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignPage> {
        const response = await this.getCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one campaign of the caller\'s org — its name, audience, creatives, channels with their per-channel launch state, schedule, budget and status. 404 when the org has no campaign with that id.
     * Returns one campaign of the caller\'s org — its name, audience, creatives, channels with their per-channel launch state, schedule, budget and status.
     */
    async getCampaignByIdRaw(requestParameters: CampaignApiGetCampaignByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignRecord>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCampaignById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignRecordFromJSON(jsonValue));
    }

    /**
     * Returns one campaign of the caller\'s org — its name, audience, creatives, channels with their per-channel launch state, schedule, budget and status. 404 when the org has no campaign with that id.
     * Returns one campaign of the caller\'s org — its name, audience, creatives, channels with their per-channel launch state, schedule, budget and status.
     */
    async getCampaignById(requestParameters: CampaignApiGetCampaignByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignRecord> {
        const response = await this.getCampaignByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a campaign\'s results over a window: the analytics funnel (impressions, clicks, conversions, revenue, visitors), the spend each channel\'s connector reports, and the derived growth KPIs — CTR, CVR, CAC and ROAS.  There is exactly ONE metrics plane and nothing is stored here: the funnel is an analytics query over the campaign\'s utm_campaign-tagged events, and the spend is each provider\'s own number read through the org\'s connector. A warehouse that is not emitting yet degrades to available:false with zeroes — honest-empty, never a 500 and never a fabricated number. When the campaign runs more than one creative and an experiment is wired, abTest carries the A/B analysis.
     * Returns a campaign\'s results over a window: the analytics funnel (impressions, clicks, conversions, revenue, visitors), the spend each channel\'s connector reports, and the derived growth KPIs — CTR, CVR, CAC and ROAS.
     */
    async getCampaignByIdMetricsRaw(requestParameters: CampaignApiGetCampaignByIdMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignResults>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCampaignByIdMetrics().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}/metrics`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignResultsFromJSON(jsonValue));
    }

    /**
     * Returns a campaign\'s results over a window: the analytics funnel (impressions, clicks, conversions, revenue, visitors), the spend each channel\'s connector reports, and the derived growth KPIs — CTR, CVR, CAC and ROAS.  There is exactly ONE metrics plane and nothing is stored here: the funnel is an analytics query over the campaign\'s utm_campaign-tagged events, and the spend is each provider\'s own number read through the org\'s connector. A warehouse that is not emitting yet degrades to available:false with zeroes — honest-empty, never a 500 and never a fabricated number. When the campaign runs more than one creative and an experiment is wired, abTest carries the A/B analysis.
     * Returns a campaign\'s results over a window: the analytics funnel (impressions, clicks, conversions, revenue, visitors), the spend each channel\'s connector reports, and the derived growth KPIs — CTR, CVR, CAC and ROAS.
     */
    async getCampaignByIdMetrics(requestParameters: CampaignApiGetCampaignByIdMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignResults> {
        const response = await this.getCampaignByIdMetricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s go-to-market roll-up: how many campaigns exist, how many are live, their total budget in cents, and which channel executors this deployment can actually reach.  The channel list is the deployment\'s honest capability, not a wish: a kind missing from it is one a launch will record as \"unavailable\" rather than fail on.
     * Returns the org\'s go-to-market roll-up: how many campaigns exist, how many are live, their total budget in cents, and which channel executors this deployment can actually reach.
     */
    async getCampaignSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignSummary>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignSummaryFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s go-to-market roll-up: how many campaigns exist, how many are live, their total budget in cents, and which channel executors this deployment can actually reach.  The channel list is the deployment\'s honest capability, not a wish: a kind missing from it is one a launch will record as \"unavailable\" rather than fail on.
     * Returns the org\'s go-to-market roll-up: how many campaigns exist, how many are live, their total budget in cents, and which channel executors this deployment can actually reach.
     */
    async getCampaignSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignSummary> {
        const response = await this.getCampaignSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates a campaign as a DRAFT and returns it.  A draft is inert: nothing is sent, no connector is touched and no budget is committed until the campaign is launched. The channels named here are validated and de-duplicated by kind (one executor per kind), and every channel starts \"pending\" whatever the caller claims — a client can never assert a launched state.
     * Creates a campaign as a DRAFT and returns it.
     */
    async postCampaignRaw(requestParameters: CampaignApiPostCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignRecord>> {
        if (requestParameters['campaignWrite'] == null) {
            throw new runtime.RequiredError(
                'campaignWrite',
                'Required parameter "campaignWrite" was null or undefined when calling postCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignWriteToJSON(requestParameters['campaignWrite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignRecordFromJSON(jsonValue));
    }

    /**
     * Creates a campaign as a DRAFT and returns it.  A draft is inert: nothing is sent, no connector is touched and no budget is committed until the campaign is launched. The channels named here are validated and de-duplicated by kind (one executor per kind), and every channel starts \"pending\" whatever the caller claims — a client can never assert a launched state.
     * Creates a campaign as a DRAFT and returns it.
     */
    async postCampaign(requestParameters: CampaignApiPostCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignRecord> {
        const response = await this.postCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a channel to a campaign, or REPLACES the one it already has of that kind, and returns the updated campaign.  A campaign carries at most one channel per kind, because the kind IS the executor: adding a second \"paid\" channel would mean two ad accounts running one campaign with no way to tell their results apart. The new channel starts \"pending\" — adding it does not launch it.
     * Adds a channel to a campaign, or REPLACES the one it already has of that kind, and returns the updated campaign.
     */
    async postCampaignByIdChannelsRaw(requestParameters: CampaignApiPostCampaignByIdChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignRecord>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postCampaignByIdChannels().'
            );
        }

        if (requestParameters['channelAdd'] == null) {
            throw new runtime.RequiredError(
                'channelAdd',
                'Required parameter "channelAdd" was null or undefined when calling postCampaignByIdChannels().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}/channels`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ChannelAddToJSON(requestParameters['channelAdd']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignRecordFromJSON(jsonValue));
    }

    /**
     * Adds a channel to a campaign, or REPLACES the one it already has of that kind, and returns the updated campaign.  A campaign carries at most one channel per kind, because the kind IS the executor: adding a second \"paid\" channel would mean two ad accounts running one campaign with no way to tell their results apart. The new channel starts \"pending\" — adding it does not launch it.
     * Adds a channel to a campaign, or REPLACES the one it already has of that kind, and returns the updated campaign.
     */
    async postCampaignByIdChannels(requestParameters: CampaignApiPostCampaignByIdChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignRecord> {
        const response = await this.postCampaignByIdChannelsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Pushes the campaign live on each of its channels through that channel\'s executor and answers the whole campaign with the per-channel outcome written back onto it.  The fan-out is BEST-EFFORT PER CHANNEL, and the honest reading of the result is the rule most callers get wrong: one channel failing never aborts the others, so each channel row carries its own `live`, `failed` or `unavailable` status and detail, and a paid launch can be live while an email launch failed. The campaign itself is `live` when AT LEAST ONE channel launched and `failed` only when none did — `live` is not a claim that every channel launched. Repeating the call is safe: a channel already live is skipped, never re-launched. A campaign carrying more than one creative has its variant assigned here by the experiment seam and tagged as `utm_content`.  Org-scoped and fails closed: a valid bearer is required (403 without one), the campaign is read under the caller\'s OWN org so another tenant\'s id is a 404, and a campaign with no channels is a 400 — there is nothing to launch. Each executor resolves its own org\'s connector token from the org passed to it, so a launch can never spend through another tenant\'s connector.
     * Launch a campaign across every channel it declares
     */
    async postCampaignByIdLaunchRaw(requestParameters: CampaignApiPostCampaignByIdLaunchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postCampaignByIdLaunch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}/launch`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Pushes the campaign live on each of its channels through that channel\'s executor and answers the whole campaign with the per-channel outcome written back onto it.  The fan-out is BEST-EFFORT PER CHANNEL, and the honest reading of the result is the rule most callers get wrong: one channel failing never aborts the others, so each channel row carries its own `live`, `failed` or `unavailable` status and detail, and a paid launch can be live while an email launch failed. The campaign itself is `live` when AT LEAST ONE channel launched and `failed` only when none did — `live` is not a claim that every channel launched. Repeating the call is safe: a channel already live is skipped, never re-launched. A campaign carrying more than one creative has its variant assigned here by the experiment seam and tagged as `utm_content`.  Org-scoped and fails closed: a valid bearer is required (403 without one), the campaign is read under the caller\'s OWN org so another tenant\'s id is a 404, and a campaign with no channels is a 400 — there is nothing to launch. Each executor resolves its own org\'s connector token from the org passed to it, so a launch can never spend through another tenant\'s connector.
     * Launch a campaign across every channel it declares
     */
    async postCampaignByIdLaunch(requestParameters: CampaignApiPostCampaignByIdLaunchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCampaignByIdLaunchRaw(requestParameters, initOverrides);
    }

    /**
     * Pauses each live channel on its provider and answers the whole campaign, moved to `paused`, with the per-channel outcome written back onto it.  Only channels that are live and carry a provider reference are touched; a channel whose executor is no longer wired is marked `unavailable` and one whose pause errored is marked `failed`, with the reason on the row. The campaign still reports `paused` in both cases, and that is deliberate rather than sloppy: no live channel remains that this process will meter, and the rows say exactly which provider was not reached so it can be settled by hand.  Org-scoped and fails closed: a valid bearer is required (403 without one) and the campaign is read under the caller\'s OWN org, so another tenant\'s id is a 404.
     * Pause every live channel on a campaign at its provider
     */
    async postCampaignByIdPauseRaw(requestParameters: CampaignApiPostCampaignByIdPauseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postCampaignByIdPause().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}/pause`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Pauses each live channel on its provider and answers the whole campaign, moved to `paused`, with the per-channel outcome written back onto it.  Only channels that are live and carry a provider reference are touched; a channel whose executor is no longer wired is marked `unavailable` and one whose pause errored is marked `failed`, with the reason on the row. The campaign still reports `paused` in both cases, and that is deliberate rather than sloppy: no live channel remains that this process will meter, and the rows say exactly which provider was not reached so it can be settled by hand.  Org-scoped and fails closed: a valid bearer is required (403 without one) and the campaign is read under the caller\'s OWN org, so another tenant\'s id is a 404.
     * Pause every live channel on a campaign at its provider
     */
    async postCampaignByIdPause(requestParameters: CampaignApiPostCampaignByIdPauseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCampaignByIdPauseRaw(requestParameters, initOverrides);
    }

    /**
     * Rewrites a campaign\'s core fields — name, audience, creatives, schedule and budget — and returns the updated campaign.  Channels are replaced ONLY while the campaign is still a draft. Once it is launched its channels carry provider state (an external id, a live status), so they are added and removed explicitly through the channels sub-resource instead; a whole-object write would silently orphan a running execution.
     * Rewrites a campaign\'s core fields — name, audience, creatives, schedule and budget — and returns the updated campaign.
     */
    async putCampaignByIdRaw(requestParameters: CampaignApiPutCampaignByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignRecord>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putCampaignById().'
            );
        }

        if (requestParameters['campaignUpdate'] == null) {
            throw new runtime.RequiredError(
                'campaignUpdate',
                'Required parameter "campaignUpdate" was null or undefined when calling putCampaignById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/campaign/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignUpdateToJSON(requestParameters['campaignUpdate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignRecordFromJSON(jsonValue));
    }

    /**
     * Rewrites a campaign\'s core fields — name, audience, creatives, schedule and budget — and returns the updated campaign.  Channels are replaced ONLY while the campaign is still a draft. Once it is launched its channels carry provider state (an external id, a live status), so they are added and removed explicitly through the channels sub-resource instead; a whole-object write would silently orphan a running execution.
     * Rewrites a campaign\'s core fields — name, audience, creatives, schedule and budget — and returns the updated campaign.
     */
    async putCampaignById(requestParameters: CampaignApiPutCampaignByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignRecord> {
        const response = await this.putCampaignByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
