/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  DriveIn,
  DriveItem,
  DriveList,
  FileList,
  SpaceHealth,
  SpaceIn,
  SpaceItem,
  SpaceList,
} from '../models/index.js';
import {
    DriveInFromJSON,
    DriveInToJSON,
    DriveItemFromJSON,
    DriveItemToJSON,
    DriveListFromJSON,
    DriveListToJSON,
    FileListFromJSON,
    FileListToJSON,
    SpaceHealthFromJSON,
    SpaceHealthToJSON,
    SpaceInFromJSON,
    SpaceInToJSON,
    SpaceItemFromJSON,
    SpaceItemToJSON,
    SpaceListFromJSON,
    SpaceListToJSON,
} from '../models/index.js';

export interface SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest {
    space: string;
    drive: string;
}

export interface SpaceApiGetSpaceBySpaceDrivesRequest {
    space: string;
}

export interface SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest {
    space: string;
    drive: string;
    folder?: string;
    recursive?: string;
}

export interface SpaceApiPostSpaceBySpaceDrivesRequest {
    space: string;
    driveIn: DriveIn;
}

export interface SpaceApiPostSpaceSpacesRequest {
    spaceIn: SpaceIn;
}

/**
 * 
 */
export class SpaceApi extends runtime.BaseAPI {

    /**
     * Removes an EMPTY drive and answers 204.  A drive holding files is 409 rather than a cascade: deleting an org\'s files behind a single drive call is not a thing this surface will do silently. A drive or space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the drive is gone.
     * Removes an EMPTY drive and answers 204.
     */
    async deleteSpaceBySpaceDrivesByDriveRaw(requestParameters: SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['space'] == null) {
            throw new runtime.RequiredError(
                'space',
                'Required parameter "space" was null or undefined when calling deleteSpaceBySpaceDrivesByDrive().'
            );
        }

        if (requestParameters['drive'] == null) {
            throw new runtime.RequiredError(
                'drive',
                'Required parameter "drive" was null or undefined when calling deleteSpaceBySpaceDrivesByDrive().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/{space}/drives/{drive}`;
        urlPath = urlPath.replace(`{${"space"}}`, encodeURIComponent(String(requestParameters['space'])));
        urlPath = urlPath.replace(`{${"drive"}}`, encodeURIComponent(String(requestParameters['drive'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes an EMPTY drive and answers 204.  A drive holding files is 409 rather than a cascade: deleting an org\'s files behind a single drive call is not a thing this surface will do silently. A drive or space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the drive is gone.
     * Removes an EMPTY drive and answers 204.
     */
    async deleteSpaceBySpaceDrivesByDrive(requestParameters: SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSpaceBySpaceDrivesByDriveRaw(requestParameters, initOverrides);
    }

    /**
     * Lists a space\'s drives.  Listing the drives IS listing the space\'s root folder, because a drive is the first segment of a key and nothing else — so the two can never disagree the way a drives table and the keys under it would. A space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * Lists a space\'s drives.
     */
    async getSpaceBySpaceDrivesRaw(requestParameters: SpaceApiGetSpaceBySpaceDrivesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveList>> {
        if (requestParameters['space'] == null) {
            throw new runtime.RequiredError(
                'space',
                'Required parameter "space" was null or undefined when calling getSpaceBySpaceDrives().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/{space}/drives`;
        urlPath = urlPath.replace(`{${"space"}}`, encodeURIComponent(String(requestParameters['space'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveListFromJSON(jsonValue));
    }

    /**
     * Lists a space\'s drives.  Listing the drives IS listing the space\'s root folder, because a drive is the first segment of a key and nothing else — so the two can never disagree the way a drives table and the keys under it would. A space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * Lists a space\'s drives.
     */
    async getSpaceBySpaceDrives(requestParameters: SpaceApiGetSpaceBySpaceDrivesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveList> {
        const response = await this.getSpaceBySpaceDrivesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists one folder level of a drive.  Folder-style by default: sub-folders come back as folder entries, which is the file-manager view. `?recursive=true` lists every file flat under the folder instead. Names are RELATIVE to `?folder=`, and the listing is bounded so a huge drive cannot exhaust memory — Total is what came back, not what the drive holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * Lists one folder level of a drive.
     */
    async getSpaceBySpaceDrivesByDriveFilesRaw(requestParameters: SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileList>> {
        if (requestParameters['space'] == null) {
            throw new runtime.RequiredError(
                'space',
                'Required parameter "space" was null or undefined when calling getSpaceBySpaceDrivesByDriveFiles().'
            );
        }

        if (requestParameters['drive'] == null) {
            throw new runtime.RequiredError(
                'drive',
                'Required parameter "drive" was null or undefined when calling getSpaceBySpaceDrivesByDriveFiles().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['folder'] != null) {
            queryParameters['folder'] = requestParameters['folder'];
        }

        if (requestParameters['recursive'] != null) {
            queryParameters['recursive'] = requestParameters['recursive'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/{space}/drives/{drive}/files`;
        urlPath = urlPath.replace(`{${"space"}}`, encodeURIComponent(String(requestParameters['space'])));
        urlPath = urlPath.replace(`{${"drive"}}`, encodeURIComponent(String(requestParameters['drive'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileListFromJSON(jsonValue));
    }

    /**
     * Lists one folder level of a drive.  Folder-style by default: sub-folders come back as folder entries, which is the file-manager view. `?recursive=true` lists every file flat under the folder instead. Names are RELATIVE to `?folder=`, and the listing is bounded so a huge drive cannot exhaust memory — Total is what came back, not what the drive holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * Lists one folder level of a drive.
     */
    async getSpaceBySpaceDrivesByDriveFiles(requestParameters: SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileList> {
        const response = await this.getSpaceBySpaceDrivesByDriveFilesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Health reports whether this deployment can serve spaces, drives and files.  It is a REAL probe rather than a constant: 200 when object-store credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no space and bills nothing.
     * Health reports whether this deployment can serve spaces, drives and files.
     */
    async getSpaceHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SpaceHealth>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SpaceHealthFromJSON(jsonValue));
    }

    /**
     * Health reports whether this deployment can serve spaces, drives and files.  It is a REAL probe rather than a constant: 200 when object-store credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no space and bills nothing.
     * Health reports whether this deployment can serve spaces, drives and files.
     */
    async getSpaceHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SpaceHealth> {
        const response = await this.getSpaceHealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller org\'s own spaces.  Only the caller\'s: every space is physically named under a per-org prefix and the listing strips that prefix, so another org\'s spaces are not in the answer at all. Another org\'s space is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
     * Lists the caller org\'s own spaces.
     */
    async getSpaceSpacesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SpaceList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/spaces`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SpaceListFromJSON(jsonValue));
    }

    /**
     * Lists the caller org\'s own spaces.  Only the caller\'s: every space is physically named under a per-org prefix and the listing strips that prefix, so another org\'s spaces are not in the answer at all. Another org\'s space is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
     * Lists the caller org\'s own spaces.
     */
    async getSpaceSpaces(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SpaceList> {
        const response = await this.getSpaceSpacesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Makes a new drive in a space and answers 201 with it.  A drive is a PREFIX and not a bucket, so making one writes a zero-byte marker at \"<name>/\" — which is what makes an empty drive visible to a listing that has no other key to find. A name already taken in the space is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the drive exists.
     * Makes a new drive in a space and answers 201 with it.
     */
    async postSpaceBySpaceDrivesRaw(requestParameters: SpaceApiPostSpaceBySpaceDrivesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveItem>> {
        if (requestParameters['space'] == null) {
            throw new runtime.RequiredError(
                'space',
                'Required parameter "space" was null or undefined when calling postSpaceBySpaceDrives().'
            );
        }

        if (requestParameters['driveIn'] == null) {
            throw new runtime.RequiredError(
                'driveIn',
                'Required parameter "driveIn" was null or undefined when calling postSpaceBySpaceDrives().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/{space}/drives`;
        urlPath = urlPath.replace(`{${"space"}}`, encodeURIComponent(String(requestParameters['space'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DriveInToJSON(requestParameters['driveIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveItemFromJSON(jsonValue));
    }

    /**
     * Makes a new drive in a space and answers 201 with it.  A drive is a PREFIX and not a bucket, so making one writes a zero-byte marker at \"<name>/\" — which is what makes an empty drive visible to a listing that has no other key to find. A name already taken in the space is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the drive exists.
     * Makes a new drive in a space and answers 201 with it.
     */
    async postSpaceBySpaceDrives(requestParameters: SpaceApiPostSpaceBySpaceDrivesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveItem> {
        const response = await this.postSpaceBySpaceDrivesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Makes a new space for the caller\'s org and answers 201 with it.  The one bucket a space\'s files live in is derived from the caller\'s VALIDATED org, so an org can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the space exists.
     * Makes a new space for the caller\'s org and answers 201 with it.
     */
    async postSpaceSpacesRaw(requestParameters: SpaceApiPostSpaceSpacesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SpaceItem>> {
        if (requestParameters['spaceIn'] == null) {
            throw new runtime.RequiredError(
                'spaceIn',
                'Required parameter "spaceIn" was null or undefined when calling postSpaceSpaces().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/space/spaces`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SpaceInToJSON(requestParameters['spaceIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SpaceItemFromJSON(jsonValue));
    }

    /**
     * Makes a new space for the caller\'s org and answers 201 with it.  The one bucket a space\'s files live in is derived from the caller\'s VALIDATED org, so an org can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the space exists.
     * Makes a new space for the caller\'s org and answers 201 with it.
     */
    async postSpaceSpaces(requestParameters: SpaceApiPostSpaceSpacesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SpaceItem> {
        const response = await this.postSpaceSpacesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
