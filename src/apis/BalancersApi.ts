/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CreateLBReq,
  LbList,
  LbView,
} from '../models/index.js';
import {
    CreateLBReqFromJSON,
    CreateLBReqToJSON,
    LbListFromJSON,
    LbListToJSON,
    LbViewFromJSON,
    LbViewToJSON,
} from '../models/index.js';

export interface BalancersApiDeleteBalancersByIdRequest {
    id: string;
}

export interface BalancersApiGetBalancersByIdRequest {
    id: string;
}

export interface BalancersApiPostBalancersRequest {
    createLBReq: CreateLBReq;
}

/**
 * 
 */
export class BalancersApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s load balancers and answers 204. Ownership is confirmed by re-fetching the resource before anything is deleted, so a cross-tenant id is a 404 rather than a delete of another org\'s load balancer.
     * Removes one of the caller org\'s load balancers and answers 204.
     */
    async deleteBalancersByIdRaw(requestParameters: BalancersApiDeleteBalancersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteBalancersById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/balancers/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s load balancers and answers 204. Ownership is confirmed by re-fetching the resource before anything is deleted, so a cross-tenant id is a 404 rather than a delete of another org\'s load balancer.
     * Removes one of the caller org\'s load balancers and answers 204.
     */
    async deleteBalancersById(requestParameters: BalancersApiDeleteBalancersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteBalancersByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Returns every load balancer the caller\'s org owns, under the friendly names the org created them with. Same account-wide filter as the VPC listing: a load balancer outside the caller\'s \"o\"<orgHash>- namespace is never in the answer.
     * Returns every load balancer the caller\'s org owns, under the friendly names the org created them with.
     */
    async getBalancersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LbList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/balancers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LbListFromJSON(jsonValue));
    }

    /**
     * Returns every load balancer the caller\'s org owns, under the friendly names the org created them with. Same account-wide filter as the VPC listing: a load balancer outside the caller\'s \"o\"<orgHash>- namespace is never in the answer.
     * Returns every load balancer the caller\'s org owns, under the friendly names the org created them with.
     */
    async getBalancers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LbList> {
        const response = await this.getBalancersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s load balancers by id. One that exists in another org\'s namespace is reported 404, never 403 — the same existence-oracle guard the VPC read applies.
     * Returns one of the caller org\'s load balancers by id.
     */
    async getBalancersByIdRaw(requestParameters: BalancersApiGetBalancersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LbView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getBalancersById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/balancers/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LbViewFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s load balancers by id. One that exists in another org\'s namespace is reported 404, never 403 — the same existence-oracle guard the VPC read applies.
     * Returns one of the caller org\'s load balancers by id.
     */
    async getBalancersById(requestParameters: BalancersApiGetBalancersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LbView> {
        const response = await this.getBalancersByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a load balancer in the caller\'s org namespace and answers 201 with it. The physical DigitalOcean name is derived server-side from the validated org; a name that already exists there is a 409. Omitting forwarding rules yields a usable HTTP 80→80 load balancer rather than a 422.
     * Creates a load balancer in the caller\'s org namespace and answers 201 with it.
     */
    async postBalancersRaw(requestParameters: BalancersApiPostBalancersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LbView>> {
        if (requestParameters['createLBReq'] == null) {
            throw new runtime.RequiredError(
                'createLBReq',
                'Required parameter "createLBReq" was null or undefined when calling postBalancers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/balancers`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateLBReqToJSON(requestParameters['createLBReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LbViewFromJSON(jsonValue));
    }

    /**
     * Creates a load balancer in the caller\'s org namespace and answers 201 with it. The physical DigitalOcean name is derived server-side from the validated org; a name that already exists there is a 409. Omitting forwarding rules yields a usable HTTP 80→80 load balancer rather than a 422.
     * Creates a load balancer in the caller\'s org namespace and answers 201 with it.
     */
    async postBalancers(requestParameters: BalancersApiPostBalancersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LbView> {
        const response = await this.postBalancersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
