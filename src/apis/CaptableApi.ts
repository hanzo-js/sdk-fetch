/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CaptableCompany,
  CaptableCompanyUpdate,
  CaptableDeleted,
  CaptableEquityPlans,
  CaptableInvestments,
  CaptableNotes,
  CaptableOptions,
  CaptableRoundCloseRequest,
  CaptableRoundDetail,
  CaptableRounds,
  CaptableSafes,
  CaptableShareClass,
  CaptableShares,
  CaptableStakeholder,
  CaptableStakeholderPatch,
  CaptableSummary,
  CaptableUpdated,
} from '../models/index.js';
import {
    CaptableCompanyFromJSON,
    CaptableCompanyToJSON,
    CaptableCompanyUpdateFromJSON,
    CaptableCompanyUpdateToJSON,
    CaptableDeletedFromJSON,
    CaptableDeletedToJSON,
    CaptableEquityPlansFromJSON,
    CaptableEquityPlansToJSON,
    CaptableInvestmentsFromJSON,
    CaptableInvestmentsToJSON,
    CaptableNotesFromJSON,
    CaptableNotesToJSON,
    CaptableOptionsFromJSON,
    CaptableOptionsToJSON,
    CaptableRoundCloseRequestFromJSON,
    CaptableRoundCloseRequestToJSON,
    CaptableRoundDetailFromJSON,
    CaptableRoundDetailToJSON,
    CaptableRoundsFromJSON,
    CaptableRoundsToJSON,
    CaptableSafesFromJSON,
    CaptableSafesToJSON,
    CaptableShareClassFromJSON,
    CaptableShareClassToJSON,
    CaptableSharesFromJSON,
    CaptableSharesToJSON,
    CaptableStakeholderFromJSON,
    CaptableStakeholderToJSON,
    CaptableStakeholderPatchFromJSON,
    CaptableStakeholderPatchToJSON,
    CaptableSummaryFromJSON,
    CaptableSummaryToJSON,
    CaptableUpdatedFromJSON,
    CaptableUpdatedToJSON,
} from '../models/index.js';

export interface CaptableApiDeleteCaptableConvertiblesByIdRequest {
    id: string;
}

export interface CaptableApiDeleteCaptableOptionsByIdRequest {
    id: string;
}

export interface CaptableApiDeleteCaptableSafesByIdRequest {
    id: string;
}

export interface CaptableApiDeleteCaptableSharesByIdRequest {
    id: string;
}

export interface CaptableApiDeleteCaptableStakeholdersByIdRequest {
    id: string;
}

export interface CaptableApiGetCaptableRoundsByIdRequest {
    id: string;
}

export interface CaptableApiPatchCaptableClassesByIdRequest {
    id: string;
}

export interface CaptableApiPatchCaptableStakeholdersByIdRequest {
    id: string;
    captableStakeholderPatch: CaptableStakeholderPatch;
}

export interface CaptableApiPostCaptableRoundsByIdCloseRequest {
    id: string;
    captableRoundCloseRequest: CaptableRoundCloseRequest;
}

export interface CaptableApiPostCaptableRoundsByIdInvestmentsRequest {
    id: string;
}

export interface CaptableApiPutCaptableCompanyRequest {
    captableCompanyUpdate: CaptableCompanyUpdate;
}

/**
 * 
 */
export class CaptableApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s convertible notes, taking its principal out of the cap table\'s unconverted-instrument totals. An id this org does not hold is not found.
     * Removes one of the caller org\'s convertible notes, taking its principal out of the cap table\'s unconverted-instrument totals.
     */
    async deleteCaptableConvertiblesByIdRaw(requestParameters: CaptableApiDeleteCaptableConvertiblesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableDeleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCaptableConvertiblesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/convertibles/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableDeletedFromJSON(jsonValue));
    }

    /**
     * Removes one of the caller org\'s convertible notes, taking its principal out of the cap table\'s unconverted-instrument totals. An id this org does not hold is not found.
     * Removes one of the caller org\'s convertible notes, taking its principal out of the cap table\'s unconverted-instrument totals.
     */
    async deleteCaptableConvertiblesById(requestParameters: CaptableApiDeleteCaptableConvertiblesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableDeleted> {
        const response = await this.deleteCaptableConvertiblesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one of the caller org\'s option grants, taking its shares out of the cap table\'s granted-options and fully-diluted counts. An id this org does not hold is not found.
     * Removes one of the caller org\'s option grants, taking its shares out of the cap table\'s granted-options and fully-diluted counts.
     */
    async deleteCaptableOptionsByIdRaw(requestParameters: CaptableApiDeleteCaptableOptionsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableDeleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCaptableOptionsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/options/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableDeletedFromJSON(jsonValue));
    }

    /**
     * Removes one of the caller org\'s option grants, taking its shares out of the cap table\'s granted-options and fully-diluted counts. An id this org does not hold is not found.
     * Removes one of the caller org\'s option grants, taking its shares out of the cap table\'s granted-options and fully-diluted counts.
     */
    async deleteCaptableOptionsById(requestParameters: CaptableApiDeleteCaptableOptionsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableDeleted> {
        const response = await this.deleteCaptableOptionsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one of the caller org\'s SAFEs, taking its capital out of the cap table\'s unconverted-instrument totals. An id this org does not hold is not found.
     * Removes one of the caller org\'s SAFEs, taking its capital out of the cap table\'s unconverted-instrument totals.
     */
    async deleteCaptableSafesByIdRaw(requestParameters: CaptableApiDeleteCaptableSafesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableDeleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCaptableSafesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/safes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableDeletedFromJSON(jsonValue));
    }

    /**
     * Removes one of the caller org\'s SAFEs, taking its capital out of the cap table\'s unconverted-instrument totals. An id this org does not hold is not found.
     * Removes one of the caller org\'s SAFEs, taking its capital out of the cap table\'s unconverted-instrument totals.
     */
    async deleteCaptableSafesById(requestParameters: CaptableApiDeleteCaptableSafesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableDeleted> {
        const response = await this.deleteCaptableSafesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one of the caller org\'s share certificates, taking its shares out of the cap table\'s outstanding and fully-diluted counts. An id this org does not hold is not found.
     * Removes one of the caller org\'s share certificates, taking its shares out of the cap table\'s outstanding and fully-diluted counts.
     */
    async deleteCaptableSharesByIdRaw(requestParameters: CaptableApiDeleteCaptableSharesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableDeleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCaptableSharesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/shares/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableDeletedFromJSON(jsonValue));
    }

    /**
     * Removes one of the caller org\'s share certificates, taking its shares out of the cap table\'s outstanding and fully-diluted counts. An id this org does not hold is not found.
     * Removes one of the caller org\'s share certificates, taking its shares out of the cap table\'s outstanding and fully-diluted counts.
     */
    async deleteCaptableSharesById(requestParameters: CaptableApiDeleteCaptableSharesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableDeleted> {
        const response = await this.deleteCaptableSharesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one of the caller org\'s stakeholders. It REFUSES to orphan issued equity: a holder that still holds share certificates or option grants cannot be deleted, and answers 400 saying so — release or transfer the holdings first. An id this org does not hold is not found.
     * Removes one of the caller org\'s stakeholders.
     */
    async deleteCaptableStakeholdersByIdRaw(requestParameters: CaptableApiDeleteCaptableStakeholdersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableDeleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCaptableStakeholdersById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/stakeholders/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableDeletedFromJSON(jsonValue));
    }

    /**
     * Removes one of the caller org\'s stakeholders. It REFUSES to orphan issued equity: a holder that still holds share certificates or option grants cannot be deleted, and answers 400 saying so — release or transfer the holdings first. An id this org does not hold is not found.
     * Removes one of the caller org\'s stakeholders.
     */
    async deleteCaptableStakeholdersById(requestParameters: CaptableApiDeleteCaptableStakeholdersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableDeleted> {
        const response = await this.deleteCaptableStakeholdersByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s share classes, in creation order. A share class is what a certificate is issued in, and every class the company has authorized appears. The response is a bare JSON array, not an envelope.
     * Returns the caller org\'s share classes, in creation order.
     */
    async getCaptableClassesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CaptableShareClass>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/classes`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CaptableShareClassFromJSON));
    }

    /**
     * Returns the caller org\'s share classes, in creation order. A share class is what a certificate is issued in, and every class the company has authorized appears. The response is a bare JSON array, not an envelope.
     * Returns the caller org\'s share classes, in creation order.
     */
    async getCaptableClasses(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CaptableShareClass>> {
        const response = await this.getCaptableClassesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s cap-table company record. The row is seeded when the tenant\'s store first opens, so it always exists; its name and incorporation details are set with PUT /v1/captable/company.
     * Returns the caller org\'s cap-table company record.
     */
    async getCaptableCompanyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableCompany>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/company`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableCompanyFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s cap-table company record. The row is seeded when the tenant\'s store first opens, so it always exists; its name and incorporation details are set with PUT /v1/captable/company.
     * Returns the caller org\'s cap-table company record.
     */
    async getCaptableCompany(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableCompany> {
        const response = await this.getCaptableCompanyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s convertible notes, newest first. A note\'s principal sits OUTSIDE issued equity until it converts, so it is not part of the share counts.
     * Returns the caller org\'s convertible notes, newest first.
     */
    async getCaptableConvertiblesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableNotes>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/convertibles`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableNotesFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s convertible notes, newest first. A note\'s principal sits OUTSIDE issued equity until it converts, so it is not part of the share counts.
     * Returns the caller org\'s convertible notes, newest first.
     */
    async getCaptableConvertibles(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableNotes> {
        const response = await this.getCaptableConvertiblesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s investments, newest first. It spans every round, so it is the flat ledger of cheques written into the company, each naming its investor and the round it went into.
     * Returns the caller org\'s investments, newest first.
     */
    async getCaptableInvestmentsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableInvestments>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/investments`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableInvestmentsFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s investments, newest first. It spans every round, so it is the flat ledger of cheques written into the company, each naming its investor and the round it went into.
     * Returns the caller org\'s investments, newest first.
     */
    async getCaptableInvestments(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableInvestments> {
        const response = await this.getCaptableInvestmentsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s option grants, newest first. Each row is joined to its grantee and its equity plan. Grants that are EXERCISED, EXPIRED or CANCELLED are listed here but do not dilute the cap table.
     * Returns the caller org\'s option grants, newest first.
     */
    async getCaptableOptionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableOptions>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/options`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableOptionsFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s option grants, newest first. Each row is joined to its grantee and its equity plan. Grants that are EXERCISED, EXPIRED or CANCELLED are listed here but do not dilute the cap table.
     * Returns the caller org\'s option grants, newest first.
     */
    async getCaptableOptions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableOptions> {
        const response = await this.getCaptableOptionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s equity plans, newest first. An equity plan is an option pool: a reserve of shares, drawn from one share class, that option grants are written against.
     * Returns the caller org\'s equity plans, newest first.
     */
    async getCaptablePlansRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableEquityPlans>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/plans`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableEquityPlansFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s equity plans, newest first. An equity plan is an option pool: a reserve of shares, drawn from one share class, that option grants are written against.
     * Returns the caller org\'s equity plans, newest first.
     */
    async getCaptablePlans(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableEquityPlans> {
        const response = await this.getCaptablePlansRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s fundraising rounds, newest first. A round groups a fundraising event; a PRICED round also carries the share class and price per share it issues at.
     * Returns the caller org\'s fundraising rounds, newest first.
     */
    async getCaptableRoundsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableRounds>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/rounds`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableRoundsFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s fundraising rounds, newest first. A round groups a fundraising event; a PRICED round also carries the share class and price per share it issues at.
     * Returns the caller org\'s fundraising rounds, newest first.
     */
    async getCaptableRounds(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableRounds> {
        const response = await this.getCaptableRoundsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s fundraising rounds together with every investment written into it, oldest first. A round id that does not exist in the caller\'s org is not found — including one that exists in another tenant, since the org comes from the caller\'s principal and is part of the lookup.
     * Returns one of the caller org\'s fundraising rounds together with every investment written into it, oldest first.
     */
    async getCaptableRoundsByIdRaw(requestParameters: CaptableApiGetCaptableRoundsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableRoundDetail>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCaptableRoundsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/rounds/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableRoundDetailFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s fundraising rounds together with every investment written into it, oldest first. A round id that does not exist in the caller\'s org is not found — including one that exists in another tenant, since the org comes from the caller\'s principal and is part of the lookup.
     * Returns one of the caller org\'s fundraising rounds together with every investment written into it, oldest first.
     */
    async getCaptableRoundsById(requestParameters: CaptableApiGetCaptableRoundsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableRoundDetail> {
        const response = await this.getCaptableRoundsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s SAFEs, newest first. A SAFE is a simple agreement for future equity: its capital sits OUTSIDE issued equity until it converts, so it is not part of the share counts.
     * Returns the caller org\'s SAFEs, newest first.
     */
    async getCaptableSafesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableSafes>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/safes`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableSafesFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s SAFEs, newest first. A SAFE is a simple agreement for future equity: its capital sits OUTSIDE issued equity until it converts, so it is not part of the share counts.
     * Returns the caller org\'s SAFEs, newest first.
     */
    async getCaptableSafes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableSafes> {
        const response = await this.getCaptableSafesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s share certificates, newest first. Each row is joined to its holder and its share class, so a certificate names who holds it and what class it is in without a second call.
     * Returns the caller org\'s share certificates, newest first.
     */
    async getCaptableSharesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableShares>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/shares`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableSharesFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s share certificates, newest first. Each row is joined to its holder and its share class, so a certificate names who holds it and what class it is in without a second call.
     * Returns the caller org\'s share certificates, newest first.
     */
    async getCaptableShares(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableShares> {
        const response = await this.getCaptableSharesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s stakeholders, newest first. The response is a bare JSON array, not an envelope. Each row carries the holder\'s contact and address fields alongside the company\'s name.
     * Returns the caller org\'s stakeholders, newest first.
     */
    async getCaptableStakeholdersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CaptableStakeholder>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/stakeholders`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CaptableStakeholderFromJSON));
    }

    /**
     * Returns the caller org\'s stakeholders, newest first. The response is a bare JSON array, not an envelope. Each row carries the holder\'s contact and address fields alongside the company\'s name.
     * Returns the caller org\'s stakeholders, newest first.
     */
    async getCaptableStakeholders(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CaptableStakeholder>> {
        const response = await this.getCaptableStakeholdersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Computes the caller org\'s cap table. It answers who owns what on a fully-diluted basis: outstanding shares, granted options, per-stakeholder ownership percentages, each share class\'s authorized versus issued position, and the capital sitting on SAFEs and convertible notes that have not yet converted. Only non-terminal option grants dilute — EXERCISED, EXPIRED and CANCELLED grants are excluded, so equity issued through an exercised option is never counted twice.
     * Computes the caller org\'s cap table.
     */
    async getCaptableSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableSummary>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableSummaryFromJSON(jsonValue));
    }

    /**
     * Computes the caller org\'s cap table. It answers who owns what on a fully-diluted basis: outstanding shares, granted options, per-stakeholder ownership percentages, each share class\'s authorized versus issued position, and the capital sitting on SAFEs and convertible notes that have not yet converted. Only non-terminal option grants dilute — EXERCISED, EXPIRED and CANCELLED grants are excluded, so equity issued through an exercised option is never counted twice.
     * Computes the caller org\'s cap table.
     */
    async getCaptableSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableSummary> {
        const response = await this.getCaptableSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Rewrites one share class — the amendment path for a class whose authorized count, price, seniority or preference terms have changed.  It REPLACES the class rather than merging into it: every field is taken from this body, so an omitted field resets to the create-time default instead of keeping its current value. Send the full class. The index and the derived prefix are unchanged by an amendment. An id that is not this company\'s is not found.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Amend a share class
     */
    async patchCaptableClassesByIdRaw(requestParameters: CaptableApiPatchCaptableClassesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling patchCaptableClassesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/classes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Rewrites one share class — the amendment path for a class whose authorized count, price, seniority or preference terms have changed.  It REPLACES the class rather than merging into it: every field is taken from this body, so an omitted field resets to the create-time default instead of keeping its current value. Send the full class. The index and the derived prefix are unchanged by an amendment. An id that is not this company\'s is not found.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Amend a share class
     */
    async patchCaptableClassesById(requestParameters: CaptableApiPatchCaptableClassesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchCaptableClassesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Changes one of the caller org\'s stakeholders. It is a PARTIAL update: only the fields the request names are written, and a field sent as null clears that column. A request that names no updatable field is refused, and an id this org does not hold is not found.  The values are stored as sent. Unlike adding a stakeholder, this route does not check the email\'s shape or the type and relationship vocabularies, so it can record a value that adding one would have rejected.
     * Changes one of the caller org\'s stakeholders.
     */
    async patchCaptableStakeholdersByIdRaw(requestParameters: CaptableApiPatchCaptableStakeholdersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableUpdated>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling patchCaptableStakeholdersById().'
            );
        }

        if (requestParameters['captableStakeholderPatch'] == null) {
            throw new runtime.RequiredError(
                'captableStakeholderPatch',
                'Required parameter "captableStakeholderPatch" was null or undefined when calling patchCaptableStakeholdersById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/stakeholders/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: CaptableStakeholderPatchToJSON(requestParameters['captableStakeholderPatch']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableUpdatedFromJSON(jsonValue));
    }

    /**
     * Changes one of the caller org\'s stakeholders. It is a PARTIAL update: only the fields the request names are written, and a field sent as null clears that column. A request that names no updatable field is refused, and an id this org does not hold is not found.  The values are stored as sent. Unlike adding a stakeholder, this route does not check the email\'s shape or the type and relationship vocabularies, so it can record a value that adding one would have rejected.
     * Changes one of the caller org\'s stakeholders.
     */
    async patchCaptableStakeholdersById(requestParameters: CaptableApiPatchCaptableStakeholdersByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableUpdated> {
        const response = await this.patchCaptableStakeholdersByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a class of stock — its authorized share count, votes per share, par and issue price, seniority, conversion rights and liquidation/participation multiples — which is what shares, priced rounds and equity plans are then issued against.  Two fields are the company\'s to assign, not the caller\'s: the class index auto-increments per company, and the certificate prefix is DERIVED from the class type (CS for COMMON, PS for anything else), so a prefix in the body is ignored.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Define a share class
     */
    async postCaptableClassesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/classes`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates a class of stock — its authorized share count, votes per share, par and issue price, seniority, conversion rights and liquidation/participation multiples — which is what shares, priced rounds and equity plans are then issued against.  Two fields are the company\'s to assign, not the caller\'s: the class index auto-increments per company, and the certificate prefix is DERIVED from the class type (CS for COMMON, PS for anything else), so a prefix in the body is ignored.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Define a share class
     */
    async postCaptableClasses(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableClassesRaw(initOverrides);
    }

    /**
     * Records a convertible note held by a stakeholder: the principal, the conversion cap, discount and interest rate, MFN, and the issue and board-approval dates.  The stakeholder must already exist in this company, and the note\'s public id must be unused there — a reused id is a conflict rather than an overwrite. Like a SAFE, this records the instrument only; conversion is not performed here.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Record a convertible note
     */
    async postCaptableConvertiblesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/convertibles`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Records a convertible note held by a stakeholder: the principal, the conversion cap, discount and interest rate, MFN, and the issue and board-approval dates.  The stakeholder must already exist in this company, and the note\'s public id must be unused there — a reused id is a conflict rather than an overwrite. Like a SAFE, this records the instrument only; conversion is not performed here.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Record a convertible note
     */
    async postCaptableConvertibles(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableConvertiblesRaw(initOverrides);
    }

    /**
     * Records an option grant to a stakeholder under an equity plan — quantity, exercise price, ISO/NSO type, cliff and vesting years, and the issue, expiration, vesting-start, board-approval and Rule 144 dates.  The stakeholder and the equity plan must both already exist in this company, and the grant id must be unused there — a reused grant id is a conflict, so a grant can never be overwritten by a later one carrying the same number.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Grant options from an equity plan
     */
    async postCaptableOptionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/options`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Records an option grant to a stakeholder under an equity plan — quantity, exercise price, ISO/NSO type, cliff and vesting years, and the issue, expiration, vesting-start, board-approval and Rule 144 dates.  The stakeholder and the equity plan must both already exist in this company, and the grant id must be unused there — a reused grant id is a conflict, so a grant can never be overwritten by a later one carrying the same number.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Grant options from an equity plan
     */
    async postCaptableOptions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableOptionsRaw(initOverrides);
    }

    /**
     * Reserves a pool of shares out of a share class for option grants, with the board approval and effective dates and what happens to cancelled options.  The share class must already exist in this company — a plan cannot reserve out of nothing. Note the field name the bundle reads for the cancellation behaviour is `defaultCancellatonBehavior`; that spelling is the wire, and a correctly spelled key is simply not seen.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Open an equity incentive plan
     */
    async postCaptablePlansRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/plans`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Reserves a pool of shares out of a share class for option grants, with the board approval and effective dates and what happens to cancelled options.  The share class must already exist in this company — a plan cannot reserve out of nothing. Note the field name the bundle reads for the cancellation behaviour is `defaultCancellatonBehavior`; that spelling is the wire, and a correctly spelled key is simply not seen.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Open an equity incentive plan
     */
    async postCaptablePlans(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptablePlansRaw(initOverrides);
    }

    /**
     * Opens a round with its name, type and target amount. It starts OPEN with nothing raised; investments are then added to it, and closing it is its own call.  A PRICED round is the constrained case: it requires a share class that exists in this company and a price per share above zero, because that price is what converts each investment into issued shares. Its pre-money valuation is optional. A non-priced round carries none of the three.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Open a funding round
     */
    async postCaptableRoundsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/rounds`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Opens a round with its name, type and target amount. It starts OPEN with nothing raised; investments are then added to it, and closing it is its own call.  A PRICED round is the constrained case: it requires a share class that exists in this company and a price per share above zero, because that price is what converts each investment into issued shares. Its pre-money valuation is optional. A non-priced round carries none of the three.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Open a funding round
     */
    async postCaptableRounds(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableRoundsRaw(initOverrides);
    }

    /**
     * Closes one of the caller org\'s fundraising rounds, recording the close date and moving its status to CLOSED. Only an OPEN round can be closed: a round that is already closed — like an id this org does not hold — is not found. Closing a round does not change what was invested in it.
     * Closes one of the caller org\'s fundraising rounds, recording the close date and moving its status to CLOSED.
     */
    async postCaptableRoundsByIdCloseRaw(requestParameters: CaptableApiPostCaptableRoundsByIdCloseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableUpdated>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postCaptableRoundsByIdClose().'
            );
        }

        if (requestParameters['captableRoundCloseRequest'] == null) {
            throw new runtime.RequiredError(
                'captableRoundCloseRequest',
                'Required parameter "captableRoundCloseRequest" was null or undefined when calling postCaptableRoundsByIdClose().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/rounds/{id}/close`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CaptableRoundCloseRequestToJSON(requestParameters['captableRoundCloseRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableUpdatedFromJSON(jsonValue));
    }

    /**
     * Closes one of the caller org\'s fundraising rounds, recording the close date and moving its status to CLOSED. Only an OPEN round can be closed: a round that is already closed — like an id this org does not hold — is not found. Closing a round does not change what was invested in it.
     * Closes one of the caller org\'s fundraising rounds, recording the close date and moving its status to CLOSED.
     */
    async postCaptableRoundsByIdClose(requestParameters: CaptableApiPostCaptableRoundsByIdCloseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableUpdated> {
        const response = await this.postCaptableRoundsByIdCloseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records what a stakeholder put into a round and adds it to the round\'s raised total.  On a PRICED round this ISSUES SHARES as well as recording the money: the amount is divided by the round\'s price per share, rounded DOWN to whole shares, and a new certificate for them is issued to the investor in the round\'s share class — so an amount too small to buy one whole share is refused rather than recorded as a zero-share investment. On a non-priced round the money is recorded and no shares are issued.  The round must exist in this company and still be OPEN — a closed round refuses further investment — and the investor must already be a stakeholder here. The date defaults to today when omitted.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Record an investment into a round
     */
    async postCaptableRoundsByIdInvestmentsRaw(requestParameters: CaptableApiPostCaptableRoundsByIdInvestmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postCaptableRoundsByIdInvestments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/rounds/{id}/investments`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Records what a stakeholder put into a round and adds it to the round\'s raised total.  On a PRICED round this ISSUES SHARES as well as recording the money: the amount is divided by the round\'s price per share, rounded DOWN to whole shares, and a new certificate for them is issued to the investor in the round\'s share class — so an amount too small to buy one whole share is refused rather than recorded as a zero-share investment. On a non-priced round the money is recorded and no shares are issued.  The round must exist in this company and still be OPEN — a closed round refuses further investment — and the investor must already be a stakeholder here. The date defaults to today when omitted.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Record an investment into a round
     */
    async postCaptableRoundsByIdInvestments(requestParameters: CaptableApiPostCaptableRoundsByIdInvestmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableRoundsByIdInvestmentsRaw(requestParameters, initOverrides);
    }

    /**
     * Records a Simple Agreement for Future Equity held by a stakeholder: the capital in, the valuation cap and discount, MFN and pro-rata rights, pre- or post-money type, and the issue and board-approval dates.  The stakeholder must already exist in this company, and the SAFE\'s public id must be unused there — a reused id is a conflict rather than an overwrite. This records the instrument; it does not convert it, so nothing is issued against a share class until a round does that.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Record a SAFE
     */
    async postCaptableSafesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/safes`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Records a Simple Agreement for Future Equity held by a stakeholder: the capital in, the valuation cap and discount, MFN and pro-rata rights, pre- or post-money type, and the issue and board-approval dates.  The stakeholder must already exist in this company, and the SAFE\'s public id must be unused there — a reused id is a conflict rather than an overwrite. This records the instrument; it does not convert it, so nothing is issued against a share class until a round does that.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Record a SAFE
     */
    async postCaptableSafes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableSafesRaw(initOverrides);
    }

    /**
     * Issues shares of a class to a stakeholder as a certificate: quantity, price and capital contributed, the vesting cliff and term, the legends on the certificate, and the issue, Rule 144, vesting-start and board-approval dates.  Both the stakeholder and the share class must already exist in this company, and the certificate id must be unused there — a reused id is a conflict, never a silent overwrite of an existing certificate.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Issue a share certificate
     */
    async postCaptableSharesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/shares`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Issues shares of a class to a stakeholder as a certificate: quantity, price and capital contributed, the vesting cliff and term, the legends on the certificate, and the issue, Rule 144, vesting-start and board-approval dates.  Both the stakeholder and the share class must already exist in this company, and the certificate id must be unused there — a reused id is a conflict, never a silent overwrite of an existing certificate.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Issue a share certificate
     */
    async postCaptableShares(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableSharesRaw(initOverrides);
    }

    /**
     * Moves shares from one certificate to another stakeholder, in one atomic step.  OMITTING `quantity` transfers the WHOLE certificate, which simply reassigns it and answers newShareId null — that is the difference between a full and a partial transfer, and it is why quantity is absent rather than zero. A partial transfer shrinks the source certificate and issues a NEW one to the recipient, so it requires a `certificateId` for that new certificate and refuses a reused one. The quantity must be between 1 and what the source certificate actually holds; the recipient must be a stakeholder of this same company.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Transfer shares to another stakeholder
     */
    async postCaptableSharesTransferRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/shares/transfer`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Moves shares from one certificate to another stakeholder, in one atomic step.  OMITTING `quantity` transfers the WHOLE certificate, which simply reassigns it and answers newShareId null — that is the difference between a full and a partial transfer, and it is why quantity is absent rather than zero. A partial transfer shrinks the source certificate and issues a NEW one to the recipient, so it requires a `certificateId` for that new certificate and refuses a reused one. The quantity must be between 1 and what the source certificate actually holds; the recipient must be a stakeholder of this same company.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Transfer shares to another stakeholder
     */
    async postCaptableSharesTransfer(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableSharesTransferRaw(initOverrides);
    }

    /**
     * Records the people and institutions that can hold equity — the rows every share, option, SAFE, note and investment is issued to.  The body is ONE stakeholder object or an ARRAY of them, and the array is the point: a whole roster loads in a single call. Email is the identity within the company, so a stakeholder whose email is already on the table is SKIPPED rather than duplicated or rejected — the 201 reports how many rows were actually inserted, which is what makes re-running an import safe. Validation is all-or-nothing across the batch: one bad entry refuses the whole array.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Add stakeholders to the cap table
     */
    async postCaptableStakeholdersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/stakeholders`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Records the people and institutions that can hold equity — the rows every share, option, SAFE, note and investment is issued to.  The body is ONE stakeholder object or an ARRAY of them, and the array is the point: a whole roster loads in a single call. Email is the identity within the company, so a stakeholder whose email is already on the table is SKIPPED rather than duplicated or rejected — the 201 reports how many rows were actually inserted, which is what makes re-running an import safe. Validation is all-or-nothing across the batch: one bad entry refuses the whole array.  Writes the caller\'s OWN cap table: the org resolved from the validated principal selects the tenant\'s store and scopes every row, so there is no field by which a caller can write into another company\'s table; a request with no validated org is refused. The whole write runs in one transaction, so a refusal leaves nothing behind. Validation is the cap-table bundle\'s and so is its refusal: a bad body comes back as {success:false, message, errors:[…]} with the failing fields listed, and numeric fields accept a number OR a numeric string. Bodies are capped at 1 MiB.
     * Add stakeholders to the cap table
     */
    async postCaptableStakeholders(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCaptableStakeholdersRaw(initOverrides);
    }

    /**
     * Sets the caller org\'s company name and incorporation details. The name is required; the three incorporation fields are optional and each is stored as empty when omitted, so a call that sends only a name CLEARS them. The company row itself is seeded when the tenant\'s store first opens, so this never creates one.
     * Sets the caller org\'s company name and incorporation details.
     */
    async putCaptableCompanyRaw(requestParameters: CaptableApiPutCaptableCompanyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CaptableUpdated>> {
        if (requestParameters['captableCompanyUpdate'] == null) {
            throw new runtime.RequiredError(
                'captableCompanyUpdate',
                'Required parameter "captableCompanyUpdate" was null or undefined when calling putCaptableCompany().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/captable/company`;

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CaptableCompanyUpdateToJSON(requestParameters['captableCompanyUpdate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CaptableUpdatedFromJSON(jsonValue));
    }

    /**
     * Sets the caller org\'s company name and incorporation details. The name is required; the three incorporation fields are optional and each is stored as empty when omitted, so a call that sends only a name CLEARS them. The company row itself is seeded when the tenant\'s store first opens, so this never creates one.
     * Sets the caller org\'s company name and incorporation details.
     */
    async putCaptableCompany(requestParameters: CaptableApiPutCaptableCompanyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CaptableUpdated> {
        const response = await this.putCaptableCompanyRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
