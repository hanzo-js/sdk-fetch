/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CatalogList,
  MetricList,
  PromptDetail,
  PromptList,
  PromptReq,
} from '../models/index.js';
import {
    CatalogListFromJSON,
    CatalogListToJSON,
    MetricListFromJSON,
    MetricListToJSON,
    PromptDetailFromJSON,
    PromptDetailToJSON,
    PromptListFromJSON,
    PromptListToJSON,
    PromptReqFromJSON,
    PromptReqToJSON,
} from '../models/index.js';

export interface PromptApiDeletePromptByNameRequest {
    name: string;
}

export interface PromptApiGetPromptByNameRequest {
    name: string;
}

export interface PromptApiPostPromptRequest {
    promptReq: PromptReq;
}

/**
 * 
 */
export class PromptApi extends runtime.BaseAPI {

    /**
     * Delete removes one of the caller org\'s prompts and every version of it, answering 204. It is scoped to the caller\'s org, so a name another tenant owns is the same 404 an unknown name gives. There is no undo: the version history goes with it.
     * Delete removes one of the caller org\'s prompts and every version of it, answering 204.
     */
    async deletePromptByNameRaw(requestParameters: PromptApiDeletePromptByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deletePromptByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prompt/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delete removes one of the caller org\'s prompts and every version of it, answering 204. It is scoped to the caller\'s org, so a name another tenant owns is the same 404 an unknown name gives. There is no undo: the version history goes with it.
     * Delete removes one of the caller org\'s prompts and every version of it, answering 204.
     */
    async deletePromptByName(requestParameters: PromptApiDeletePromptByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePromptByNameRaw(requestParameters, initOverrides);
    }

    /**
     * List returns the caller org\'s prompt library as one row per prompt: its name, type, every version number it has, its taxonomy and when it last changed. The template bodies are deliberately absent — fetch one prompt to read its text.
     * List returns the caller org\'s prompt library as one row per prompt: its name, type, every version number it has, its taxonomy and when it last changed.
     */
    async getPromptRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PromptList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prompt`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PromptListFromJSON(jsonValue));
    }

    /**
     * List returns the caller org\'s prompt library as one row per prompt: its name, type, every version number it has, its taxonomy and when it last changed. The template bodies are deliberately absent — fetch one prompt to read its text.
     * List returns the caller org\'s prompt library as one row per prompt: its name, type, every version number it has, its taxonomy and when it last changed.
     */
    async getPrompt(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PromptList> {
        const response = await this.getPromptRaw(initOverrides);
        return await response.value();
    }

    /**
     * Get returns one of the caller org\'s prompts: its CURRENT template text plus the metadata of every version it has had. The history carries version numbers, types and timestamps only — not each version\'s body — so a long history cannot inflate this response. A name the caller\'s org does not own is 404, whoever owns it.
     * Get returns one of the caller org\'s prompts: its CURRENT template text plus the metadata of every version it has had.
     */
    async getPromptByNameRaw(requestParameters: PromptApiGetPromptByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PromptDetail>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getPromptByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prompt/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PromptDetailFromJSON(jsonValue));
    }

    /**
     * Get returns one of the caller org\'s prompts: its CURRENT template text plus the metadata of every version it has had. The history carries version numbers, types and timestamps only — not each version\'s body — so a long history cannot inflate this response. A name the caller\'s org does not own is 404, whoever owns it.
     * Get returns one of the caller org\'s prompts: its CURRENT template text plus the metadata of every version it has had.
     */
    async getPromptByName(requestParameters: PromptApiGetPromptByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PromptDetail> {
        const response = await this.getPromptByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Catalog returns the read-only starter prompt library shipped with the binary — reference content every tenant sees the same, NOT the caller\'s own prompts and never mixed into them. An org\'s library stays honestly empty until someone explicitly imports a starter, which is an ordinary POST /v1/prompt. Entries that would fail the create guards are dropped, so everything offered here can actually be imported.
     * Catalog returns the read-only starter prompt library shipped with the binary — reference content every tenant sees the same, NOT the caller\'s own prompts and never mixed into them.
     */
    async getPromptCatalogRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CatalogList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prompt/catalog`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CatalogListFromJSON(jsonValue));
    }

    /**
     * Catalog returns the read-only starter prompt library shipped with the binary — reference content every tenant sees the same, NOT the caller\'s own prompts and never mixed into them. An org\'s library stays honestly empty until someone explicitly imports a starter, which is an ordinary POST /v1/prompt. Entries that would fail the create guards are dropped, so everything offered here can actually be imported.
     * Catalog returns the read-only starter prompt library shipped with the binary — reference content every tenant sees the same, NOT the caller\'s own prompts and never mixed into them.
     */
    async getPromptCatalog(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CatalogList> {
        const response = await this.getPromptCatalogRaw(initOverrides);
        return await response.value();
    }

    /**
     * Metrics returns real per-prompt statistics for the caller\'s org: how many versions each prompt has, which one is current, and when it was created and last changed. Every number is counted from the store — nothing here is estimated or fabricated.
     * Metrics returns real per-prompt statistics for the caller\'s org: how many versions each prompt has, which one is current, and when it was created and last changed.
     */
    async getPromptMetricsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MetricList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prompt/metrics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MetricListFromJSON(jsonValue));
    }

    /**
     * Metrics returns real per-prompt statistics for the caller\'s org: how many versions each prompt has, which one is current, and when it was created and last changed. Every number is counted from the store — nothing here is estimated or fabricated.
     * Metrics returns real per-prompt statistics for the caller\'s org: how many versions each prompt has, which one is current, and when it was created and last changed.
     */
    async getPromptMetrics(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MetricList> {
        const response = await this.getPromptMetricsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Create records a prompt for the caller\'s org and answers 201 with it. A name the org already uses is NOT an error and NOT an overwrite: it appends a new version, so the library keeps real, inspectable history and the response carries the whole version list. The name is also the URL segment the prompt is fetched by, which is why its shape is constrained and a handful of names are reserved.
     * Create records a prompt for the caller\'s org and answers 201 with it.
     */
    async postPromptRaw(requestParameters: PromptApiPostPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PromptDetail>> {
        if (requestParameters['promptReq'] == null) {
            throw new runtime.RequiredError(
                'promptReq',
                'Required parameter "promptReq" was null or undefined when calling postPrompt().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prompt`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PromptReqToJSON(requestParameters['promptReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PromptDetailFromJSON(jsonValue));
    }

    /**
     * Create records a prompt for the caller\'s org and answers 201 with it. A name the org already uses is NOT an error and NOT an overwrite: it appends a new version, so the library keeps real, inspectable history and the response carries the whole version list. The name is also the URL segment the prompt is fetched by, which is why its shape is constrained and a handful of names are reserved.
     * Create records a prompt for the caller\'s org and answers 201 with it.
     */
    async postPrompt(requestParameters: PromptApiPostPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PromptDetail> {
        const response = await this.postPromptRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
