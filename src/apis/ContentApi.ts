/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  BoardPage,
  ChannelList,
  GenerateInput,
  GenerateResult,
  PublishInput,
  PublishResult,
  StateGraph,
  TransitionIn,
  TransitionResult,
} from '../models/index.js';
import {
    BoardPageFromJSON,
    BoardPageToJSON,
    ChannelListFromJSON,
    ChannelListToJSON,
    GenerateInputFromJSON,
    GenerateInputToJSON,
    GenerateResultFromJSON,
    GenerateResultToJSON,
    PublishInputFromJSON,
    PublishInputToJSON,
    PublishResultFromJSON,
    PublishResultToJSON,
    StateGraphFromJSON,
    StateGraphToJSON,
    TransitionInFromJSON,
    TransitionInToJSON,
    TransitionResultFromJSON,
    TransitionResultToJSON,
} from '../models/index.js';

export interface ContentApiGetContentBoardRequest {
    status?: string;
    project?: string;
    doctype?: string;
    limit?: number;
}

export interface ContentApiPostContentByDoctypeByNameTransitionRequest {
    doctype: string;
    name: string;
    transitionIn: TransitionIn;
}

export interface ContentApiPostContentGenerateRequest {
    generateInput: GenerateInput;
}

export interface ContentApiPostContentPublishRequest {
    publishInput: PublishInput;
}

/**
 * 
 */
export class ContentApi extends runtime.BaseAPI {

    /**
     * Aggregates the caller org\'s marketing content across every publishable content type into ONE queue board — the cross-type read the framework\'s per-DocType list cannot give. It never fails on a partial outage: a content type the org has not installed, or one whose search errors, is skipped and logged rather than failing the whole board.
     * Aggregates the caller org\'s marketing content across every publishable content type into ONE queue board — the cross-type read the framework\'s per-DocType list cannot give.
     */
    async getContentBoardRaw(requestParameters: ContentApiGetContentBoardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BoardPage>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['doctype'] != null) {
            queryParameters['doctype'] = requestParameters['doctype'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/content/board`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BoardPageFromJSON(jsonValue));
    }

    /**
     * Aggregates the caller org\'s marketing content across every publishable content type into ONE queue board — the cross-type read the framework\'s per-DocType list cannot give. It never fails on a partial outage: a content type the org has not installed, or one whose search errors, is skipped and logged rather than failing the whole board.
     * Aggregates the caller org\'s marketing content across every publishable content type into ONE queue board — the cross-type read the framework\'s per-DocType list cannot give.
     */
    async getContentBoard(requestParameters: ContentApiGetContentBoardRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BoardPage> {
        const response = await this.getContentBoardRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the distribution channels the caller\'s org has connected — the social integrations a publish can target. A deployment with no distribution edge wired answers 503 rather than an empty list that would read as \"no channels\".
     * Lists the distribution channels the caller\'s org has connected — the social integrations a publish can target.
     */
    async getContentChannelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChannelList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/content/channels`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChannelListFromJSON(jsonValue));
    }

    /**
     * Lists the distribution channels the caller\'s org has connected — the social integrations a publish can target. A deployment with no distribution edge wired answers 503 rather than an empty list that would read as \"no channels\".
     * Lists the distribution channels the caller\'s org has connected — the social integrations a publish can target.
     */
    async getContentChannels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChannelList> {
        const response = await this.getContentChannelsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the ONE marketing-content state machine: the ordered lifecycle states, which state a fresh document starts in, which one is publicly live, and the legal successors of every state. The console builds its board columns and its per-item action buttons from this single answer, so the UI and the write-time enforcement hook can never disagree about what is legal.
     * Returns the ONE marketing-content state machine: the ordered lifecycle states, which state a fresh document starts in, which one is publicly live, and the legal successors of every state.
     */
    async getContentLifecycleRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StateGraph>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/content/lifecycle`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StateGraphFromJSON(jsonValue));
    }

    /**
     * Returns the ONE marketing-content state machine: the ordered lifecycle states, which state a fresh document starts in, which one is publicly live, and the legal successors of every state. The console builds its board columns and its per-item action buttons from this single answer, so the UI and the write-time enforcement hook can never disagree about what is legal.
     * Returns the ONE marketing-content state machine: the ordered lifecycle states, which state a fresh document starts in, which one is publicly live, and the legal successors of every state.
     */
    async getContentLifecycle(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StateGraph> {
        const response = await this.getContentLifecycleRaw(initOverrides);
        return await response.value();
    }

    /**
     * Moves one content item to a new lifecycle state and, on the move to published, fans it out to the item\'s channels. The edge must be legal for the item\'s current state — an illegal move is refused with 409 — and the status write re-validates it at the storage boundary. Distribution is best effort: its honest state is reported on the result and a distribution failure never rolls the status change back.
     * Moves one content item to a new lifecycle state and, on the move to published, fans it out to the item\'s channels.
     */
    async postContentByDoctypeByNameTransitionRaw(requestParameters: ContentApiPostContentByDoctypeByNameTransitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TransitionResult>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling postContentByDoctypeByNameTransition().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postContentByDoctypeByNameTransition().'
            );
        }

        if (requestParameters['transitionIn'] == null) {
            throw new runtime.RequiredError(
                'transitionIn',
                'Required parameter "transitionIn" was null or undefined when calling postContentByDoctypeByNameTransition().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/content/{doctype}/{name}/transition`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TransitionInToJSON(requestParameters['transitionIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TransitionResultFromJSON(jsonValue));
    }

    /**
     * Moves one content item to a new lifecycle state and, on the move to published, fans it out to the item\'s channels. The edge must be legal for the item\'s current state — an illegal move is refused with 409 — and the status write re-validates it at the storage boundary. Distribution is best effort: its honest state is reported on the result and a distribution failure never rolls the status change back.
     * Moves one content item to a new lifecycle state and, on the move to published, fans it out to the item\'s channels.
     */
    async postContentByDoctypeByNameTransition(requestParameters: ContentApiPostContentByDoctypeByNameTransitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TransitionResult> {
        const response = await this.postContentByDoctypeByNameTransitionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Draft a piece of marketing content and file it in the CMS as a draft.  Answers 201 with the created draft\'s identity — {doctype, name, status} — and the document itself lands in the CMS through the SAME validate and lifecycle-hook pipeline an ordinary create runs. This is a WRITE, not a preview: there is no dry-run, and every call that succeeds leaves a document behind.  `doctype` picks which of two generation planes runs, and they are the only two. Campaign and SocialPost are drafted as brand COPY on the platform AI plane (zen5 by default, overridable per request with `model` or per deployment); Asset is a studio image render the AI plane never sees. Everything else about the call is identical.  MONEY, metered in exactly one place per mode and never both. Copy rides the platform\'s own inference meter — the org\'s balance is authorised before the model call and debited at the exact token cost after — so content never re-bills it. A studio render is invisible to that meter, so content is the sole meter for it: the org is gated BEFORE the GPU compute and refused 402 when out of funds or over its spend cap, and the debit is recorded only once the render actually returns, because the billable event is the consumed compute and not the CMS row. `project` rides the BODY rather than a server-minted identity claim, so it attributes spend but a project-scoped cap stays soft on it — the org is the value that is enforced.  The org is the caller\'s own, resolved once from the validated principal and never read from the body; a caller without one is refused 403. Status is not the generator\'s to choose: a generated item is ALWAYS a draft, and the storage-boundary hook enforces that a second time.  It fails closed rather than inventing anything. An unknown content type is 404 and a deployment whose marketing module is not installed is 409 naming the install call. An AI plane or studio that is unconfigured or unreachable, a graph the studio rejects, and a render that does not return in time all degrade to 503 — never fabricated copy, never a fake render. A `source_media` that fails the SSRF and traversal validator is 400 raised before the billing gate and before the studio is contacted, so a hostile source never costs the caller anything.
     * Draft a piece of marketing content and file it in the CMS as a draft.
     */
    async postContentGenerateRaw(requestParameters: ContentApiPostContentGenerateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GenerateResult>> {
        if (requestParameters['generateInput'] == null) {
            throw new runtime.RequiredError(
                'generateInput',
                'Required parameter "generateInput" was null or undefined when calling postContentGenerate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/content/generate`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GenerateInputToJSON(requestParameters['generateInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GenerateResultFromJSON(jsonValue));
    }

    /**
     * Draft a piece of marketing content and file it in the CMS as a draft.  Answers 201 with the created draft\'s identity — {doctype, name, status} — and the document itself lands in the CMS through the SAME validate and lifecycle-hook pipeline an ordinary create runs. This is a WRITE, not a preview: there is no dry-run, and every call that succeeds leaves a document behind.  `doctype` picks which of two generation planes runs, and they are the only two. Campaign and SocialPost are drafted as brand COPY on the platform AI plane (zen5 by default, overridable per request with `model` or per deployment); Asset is a studio image render the AI plane never sees. Everything else about the call is identical.  MONEY, metered in exactly one place per mode and never both. Copy rides the platform\'s own inference meter — the org\'s balance is authorised before the model call and debited at the exact token cost after — so content never re-bills it. A studio render is invisible to that meter, so content is the sole meter for it: the org is gated BEFORE the GPU compute and refused 402 when out of funds or over its spend cap, and the debit is recorded only once the render actually returns, because the billable event is the consumed compute and not the CMS row. `project` rides the BODY rather than a server-minted identity claim, so it attributes spend but a project-scoped cap stays soft on it — the org is the value that is enforced.  The org is the caller\'s own, resolved once from the validated principal and never read from the body; a caller without one is refused 403. Status is not the generator\'s to choose: a generated item is ALWAYS a draft, and the storage-boundary hook enforces that a second time.  It fails closed rather than inventing anything. An unknown content type is 404 and a deployment whose marketing module is not installed is 409 naming the install call. An AI plane or studio that is unconfigured or unreachable, a graph the studio rejects, and a render that does not return in time all degrade to 503 — never fabricated copy, never a fake render. A `source_media` that fails the SSRF and traversal validator is 400 raised before the billing gate and before the studio is contacted, so a hostile source never costs the caller anything.
     * Draft a piece of marketing content and file it in the CMS as a draft.
     */
    async postContentGenerate(requestParameters: ContentApiPostContentGenerateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GenerateResult> {
        const response = await this.postContentGenerateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publish distributes one CMS content item to the channels recorded on it and returns the honest per-channel outcome. The item names itself — its caption, media and channel list are read from the stored document, not from this request. It is idempotent per channel (a channel already posted for this item is skipped), and a publish that loses the per-item lease to a live publisher answers status \"in_progress\" having posted nothing.
     * Publish distributes one CMS content item to the channels recorded on it and returns the honest per-channel outcome.
     */
    async postContentPublishRaw(requestParameters: ContentApiPostContentPublishRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublishResult>> {
        if (requestParameters['publishInput'] == null) {
            throw new runtime.RequiredError(
                'publishInput',
                'Required parameter "publishInput" was null or undefined when calling postContentPublish().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/content/publish`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PublishInputToJSON(requestParameters['publishInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PublishResultFromJSON(jsonValue));
    }

    /**
     * Publish distributes one CMS content item to the channels recorded on it and returns the honest per-channel outcome. The item names itself — its caption, media and channel list are read from the stored document, not from this request. It is idempotent per channel (a channel already posted for this item is skipped), and a publish that loses the per-item lease to a live publisher answers status \"in_progress\" having posted nothing.
     * Publish distributes one CMS content item to the channels recorded on it and returns the honest per-channel outcome.
     */
    async postContentPublish(requestParameters: ContentApiPostContentPublishRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublishResult> {
        const response = await this.postContentPublishRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
