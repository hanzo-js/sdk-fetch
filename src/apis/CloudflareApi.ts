/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  BucketCreateIn,
  D1Query,
  DatabaseCreateIn,
  DomainAddIn,
  NamespaceCreateIn,
  PagesDeploy,
  PagesProjectCreate,
  PurgeIn,
  RouteCreateIn,
  SubdomainSetIn,
  WorkerScriptPut,
} from '../models/index.js';
import {
    BucketCreateInFromJSON,
    BucketCreateInToJSON,
    D1QueryFromJSON,
    D1QueryToJSON,
    DatabaseCreateInFromJSON,
    DatabaseCreateInToJSON,
    DomainAddInFromJSON,
    DomainAddInToJSON,
    NamespaceCreateInFromJSON,
    NamespaceCreateInToJSON,
    PagesDeployFromJSON,
    PagesDeployToJSON,
    PagesProjectCreateFromJSON,
    PagesProjectCreateToJSON,
    PurgeInFromJSON,
    PurgeInToJSON,
    RouteCreateInFromJSON,
    RouteCreateInToJSON,
    SubdomainSetInFromJSON,
    SubdomainSetInToJSON,
    WorkerScriptPutFromJSON,
    WorkerScriptPutToJSON,
} from '../models/index.js';

export interface CloudflareApiDeleteCloudflareD1DatabasesByDatabaseRequest {
    database: string;
}

export interface CloudflareApiDeleteCloudflareKvNamespacesByNamespaceRequest {
    namespace: string;
}

export interface CloudflareApiDeleteCloudflareKvNamespacesByNamespaceValuesByKeyRequest {
    namespace: string;
    key: string;
}

export interface CloudflareApiDeleteCloudflarePagesProjectsByProjectRequest {
    project: string;
}

export interface CloudflareApiDeleteCloudflarePagesProjectsByProjectDomainsByDomainRequest {
    project: string;
    domain: string;
}

export interface CloudflareApiDeleteCloudflareR2BucketsByBucketRequest {
    bucket: string;
}

export interface CloudflareApiDeleteCloudflareWorkersScriptsByScriptRequest {
    script: string;
}

export interface CloudflareApiDeleteCloudflareWorkersZonesByZoneRoutesByRouteRequest {
    zone: string;
    route: string;
}

export interface CloudflareApiGetCloudflareD1DatabasesRequest {
    page?: string;
    perPage?: string;
    name?: string;
}

export interface CloudflareApiGetCloudflareKvNamespacesRequest {
    page?: string;
    perPage?: string;
    order?: string;
    direction?: string;
}

export interface CloudflareApiGetCloudflareKvNamespacesByNamespaceValuesByKeyRequest {
    namespace: string;
    key: string;
}

export interface CloudflareApiGetCloudflarePagesProjectsByProjectRequest {
    project: string;
}

export interface CloudflareApiGetCloudflareR2BucketsRequest {
    perPage?: string;
    cursor?: string;
    nameContains?: string;
    order?: string;
    direction?: string;
}

export interface CloudflareApiGetCloudflareWorkersZonesByZoneRoutesRequest {
    zone: string;
}

export interface CloudflareApiGetCloudflareZonesRequest {
    page?: string;
    perPage?: string;
    name?: string;
    status?: string;
    order?: string;
    direction?: string;
}

export interface CloudflareApiGetCloudflareZonesByZoneRequest {
    zone: string;
}

export interface CloudflareApiGetCloudflareZonesByZoneAnalyticsRequest {
    zone: string;
    since?: string;
    until?: string;
    continuous?: string;
}

export interface CloudflareApiPostCloudflareAiRunByWildcard1Request {
    wildcard1: string;
}

export interface CloudflareApiPostCloudflareD1DatabasesRequest {
    databaseCreateIn: DatabaseCreateIn;
}

export interface CloudflareApiPostCloudflareD1DatabasesByDatabaseQueryRequest {
    database: string;
    d1Query?: D1Query;
}

export interface CloudflareApiPostCloudflareKvNamespacesRequest {
    namespaceCreateIn: NamespaceCreateIn;
}

export interface CloudflareApiPostCloudflarePagesProjectsRequest {
    pagesProjectCreate: PagesProjectCreate;
}

export interface CloudflareApiPostCloudflarePagesProjectsByProjectDeploymentsRequest {
    project: string;
    pagesDeploy?: PagesDeploy;
}

export interface CloudflareApiPostCloudflarePagesProjectsByProjectDomainsRequest {
    project: string;
    domainAddIn: DomainAddIn;
}

export interface CloudflareApiPostCloudflareR2BucketsRequest {
    bucketCreateIn: BucketCreateIn;
}

export interface CloudflareApiPostCloudflareWorkersScriptsByScriptSubdomainRequest {
    script: string;
    subdomainSetIn: SubdomainSetIn;
}

export interface CloudflareApiPostCloudflareWorkersZonesByZoneRoutesRequest {
    zone: string;
    routeCreateIn: RouteCreateIn;
}

export interface CloudflareApiPostCloudflareZonesByZonePurgeRequest {
    zone: string;
    purgeIn: PurgeIn;
}

export interface CloudflareApiPutCloudflareKvNamespacesByNamespaceValuesByKeyRequest {
    namespace: string;
    key: string;
}

export interface CloudflareApiPutCloudflareWorkersScriptsByScriptRequest {
    script: string;
    workerScriptPut?: WorkerScriptPut;
}

/**
 * 
 */
export class CloudflareApi extends runtime.BaseAPI {

    /**
     * Deletes a D1 database and everything stored in it. Requires org admin.
     * Deletes a D1 database and everything stored in it.
     */
    async deleteCloudflareD1DatabasesByDatabaseRaw(requestParameters: CloudflareApiDeleteCloudflareD1DatabasesByDatabaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['database'] == null) {
            throw new runtime.RequiredError(
                'database',
                'Required parameter "database" was null or undefined when calling deleteCloudflareD1DatabasesByDatabase().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/d1/databases/{database}`;
        urlPath = urlPath.replace(`{${"database"}}`, encodeURIComponent(String(requestParameters['database'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Deletes a D1 database and everything stored in it. Requires org admin.
     * Deletes a D1 database and everything stored in it.
     */
    async deleteCloudflareD1DatabasesByDatabase(requestParameters: CloudflareApiDeleteCloudflareD1DatabasesByDatabaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflareD1DatabasesByDatabaseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * KVNamespaceDelete deletes a Workers KV namespace and every key in it. Requires org admin.
     * KVNamespaceDelete deletes a Workers KV namespace and every key in it.
     */
    async deleteCloudflareKvNamespacesByNamespaceRaw(requestParameters: CloudflareApiDeleteCloudflareKvNamespacesByNamespaceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['namespace'] == null) {
            throw new runtime.RequiredError(
                'namespace',
                'Required parameter "namespace" was null or undefined when calling deleteCloudflareKvNamespacesByNamespace().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/kv/namespaces/{namespace}`;
        urlPath = urlPath.replace(`{${"namespace"}}`, encodeURIComponent(String(requestParameters['namespace'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * KVNamespaceDelete deletes a Workers KV namespace and every key in it. Requires org admin.
     * KVNamespaceDelete deletes a Workers KV namespace and every key in it.
     */
    async deleteCloudflareKvNamespacesByNamespace(requestParameters: CloudflareApiDeleteCloudflareKvNamespacesByNamespaceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflareKvNamespacesByNamespaceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * KVValueDelete removes one key from a Workers KV namespace. Requires org admin.
     * KVValueDelete removes one key from a Workers KV namespace.
     */
    async deleteCloudflareKvNamespacesByNamespaceValuesByKeyRaw(requestParameters: CloudflareApiDeleteCloudflareKvNamespacesByNamespaceValuesByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['namespace'] == null) {
            throw new runtime.RequiredError(
                'namespace',
                'Required parameter "namespace" was null or undefined when calling deleteCloudflareKvNamespacesByNamespaceValuesByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling deleteCloudflareKvNamespacesByNamespaceValuesByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/kv/namespaces/{namespace}/values/{key}`;
        urlPath = urlPath.replace(`{${"namespace"}}`, encodeURIComponent(String(requestParameters['namespace'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * KVValueDelete removes one key from a Workers KV namespace. Requires org admin.
     * KVValueDelete removes one key from a Workers KV namespace.
     */
    async deleteCloudflareKvNamespacesByNamespaceValuesByKey(requestParameters: CloudflareApiDeleteCloudflareKvNamespacesByNamespaceValuesByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflareKvNamespacesByNamespaceValuesByKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes a Cloudflare Pages project, and with it every deployment it has ever made. Requires org admin.
     * Deletes a Cloudflare Pages project, and with it every deployment it has ever made.
     */
    async deleteCloudflarePagesProjectsByProjectRaw(requestParameters: CloudflareApiDeleteCloudflarePagesProjectsByProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling deleteCloudflarePagesProjectsByProject().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects/{project}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Deletes a Cloudflare Pages project, and with it every deployment it has ever made. Requires org admin.
     * Deletes a Cloudflare Pages project, and with it every deployment it has ever made.
     */
    async deleteCloudflarePagesProjectsByProject(requestParameters: CloudflareApiDeleteCloudflarePagesProjectsByProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflarePagesProjectsByProjectRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Detaches a custom domain from a Cloudflare Pages project. Requires org admin.
     * Detaches a custom domain from a Cloudflare Pages project.
     */
    async deleteCloudflarePagesProjectsByProjectDomainsByDomainRaw(requestParameters: CloudflareApiDeleteCloudflarePagesProjectsByProjectDomainsByDomainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling deleteCloudflarePagesProjectsByProjectDomainsByDomain().'
            );
        }

        if (requestParameters['domain'] == null) {
            throw new runtime.RequiredError(
                'domain',
                'Required parameter "domain" was null or undefined when calling deleteCloudflarePagesProjectsByProjectDomainsByDomain().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects/{project}/domains/{domain}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"domain"}}`, encodeURIComponent(String(requestParameters['domain'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Detaches a custom domain from a Cloudflare Pages project. Requires org admin.
     * Detaches a custom domain from a Cloudflare Pages project.
     */
    async deleteCloudflarePagesProjectsByProjectDomainsByDomain(requestParameters: CloudflareApiDeleteCloudflarePagesProjectsByProjectDomainsByDomainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflarePagesProjectsByProjectDomainsByDomainRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes an R2 bucket. Requires org admin. Cloudflare refuses a bucket that still holds objects, and that refusal is relayed.
     * Deletes an R2 bucket.
     */
    async deleteCloudflareR2BucketsByBucketRaw(requestParameters: CloudflareApiDeleteCloudflareR2BucketsByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling deleteCloudflareR2BucketsByBucket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/r2/buckets/{bucket}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Deletes an R2 bucket. Requires org admin. Cloudflare refuses a bucket that still holds objects, and that refusal is relayed.
     * Deletes an R2 bucket.
     */
    async deleteCloudflareR2BucketsByBucket(requestParameters: CloudflareApiDeleteCloudflareR2BucketsByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflareR2BucketsByBucketRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes a Worker script from the org\'s Cloudflare account. Requires org admin. Routes bound to the script stop serving it.
     * Removes a Worker script from the org\'s Cloudflare account.
     */
    async deleteCloudflareWorkersScriptsByScriptRaw(requestParameters: CloudflareApiDeleteCloudflareWorkersScriptsByScriptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['script'] == null) {
            throw new runtime.RequiredError(
                'script',
                'Required parameter "script" was null or undefined when calling deleteCloudflareWorkersScriptsByScript().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/scripts/{script}`;
        urlPath = urlPath.replace(`{${"script"}}`, encodeURIComponent(String(requestParameters['script'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Removes a Worker script from the org\'s Cloudflare account. Requires org admin. Routes bound to the script stop serving it.
     * Removes a Worker script from the org\'s Cloudflare account.
     */
    async deleteCloudflareWorkersScriptsByScript(requestParameters: CloudflareApiDeleteCloudflareWorkersScriptsByScriptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflareWorkersScriptsByScriptRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Unbinds a Worker route, so its pattern stops dispatching to a script. Requires org admin.
     * Unbinds a Worker route, so its pattern stops dispatching to a script.
     */
    async deleteCloudflareWorkersZonesByZoneRoutesByRouteRaw(requestParameters: CloudflareApiDeleteCloudflareWorkersZonesByZoneRoutesByRouteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['zone'] == null) {
            throw new runtime.RequiredError(
                'zone',
                'Required parameter "zone" was null or undefined when calling deleteCloudflareWorkersZonesByZoneRoutesByRoute().'
            );
        }

        if (requestParameters['route'] == null) {
            throw new runtime.RequiredError(
                'route',
                'Required parameter "route" was null or undefined when calling deleteCloudflareWorkersZonesByZoneRoutesByRoute().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/zones/{zone}/routes/{route}`;
        urlPath = urlPath.replace(`{${"zone"}}`, encodeURIComponent(String(requestParameters['zone'])));
        urlPath = urlPath.replace(`{${"route"}}`, encodeURIComponent(String(requestParameters['route'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Unbinds a Worker route, so its pattern stops dispatching to a script. Requires org admin.
     * Unbinds a Worker route, so its pattern stops dispatching to a script.
     */
    async deleteCloudflareWorkersZonesByZoneRoutesByRoute(requestParameters: CloudflareApiDeleteCloudflareWorkersZonesByZoneRoutesByRouteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteCloudflareWorkersZonesByZoneRoutesByRouteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the D1 databases on the org\'s Cloudflare account. Any org member may read.
     * Lists the D1 databases on the org\'s Cloudflare account.
     */
    async getCloudflareD1DatabasesRaw(requestParameters: CloudflareApiGetCloudflareD1DatabasesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['perPage'] != null) {
            queryParameters['per_page'] = requestParameters['perPage'];
        }

        if (requestParameters['name'] != null) {
            queryParameters['name'] = requestParameters['name'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/d1/databases`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Lists the D1 databases on the org\'s Cloudflare account. Any org member may read.
     * Lists the D1 databases on the org\'s Cloudflare account.
     */
    async getCloudflareD1Databases(requestParameters: CloudflareApiGetCloudflareD1DatabasesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareD1DatabasesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * KVNamespaceList lists the Workers KV namespaces on the org\'s Cloudflare account. Any org member may read.
     * KVNamespaceList lists the Workers KV namespaces on the org\'s Cloudflare account.
     */
    async getCloudflareKvNamespacesRaw(requestParameters: CloudflareApiGetCloudflareKvNamespacesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['perPage'] != null) {
            queryParameters['per_page'] = requestParameters['perPage'];
        }

        if (requestParameters['order'] != null) {
            queryParameters['order'] = requestParameters['order'];
        }

        if (requestParameters['direction'] != null) {
            queryParameters['direction'] = requestParameters['direction'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/kv/namespaces`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * KVNamespaceList lists the Workers KV namespaces on the org\'s Cloudflare account. Any org member may read.
     * KVNamespaceList lists the Workers KV namespaces on the org\'s Cloudflare account.
     */
    async getCloudflareKvNamespaces(requestParameters: CloudflareApiGetCloudflareKvNamespacesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareKvNamespacesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers one KV key\'s value from the org\'s OWN Cloudflare account as RAW BYTES under the content type it was written with — not wrapped in a JSON envelope, which is why this is not a typed op. Any org member may read. A key that does not exist is Cloudflare\'s own 404; an invalid namespace, or a key that is empty, over 512 bytes, not valid UTF-8, or carries a control character, is 400; 503 if the org has never connected a Cloudflare token.
     * Read a Workers KV value as its stored bytes
     */
    async getCloudflareKvNamespacesByNamespaceValuesByKeyRaw(requestParameters: CloudflareApiGetCloudflareKvNamespacesByNamespaceValuesByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['namespace'] == null) {
            throw new runtime.RequiredError(
                'namespace',
                'Required parameter "namespace" was null or undefined when calling getCloudflareKvNamespacesByNamespaceValuesByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getCloudflareKvNamespacesByNamespaceValuesByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/kv/namespaces/{namespace}/values/{key}`;
        urlPath = urlPath.replace(`{${"namespace"}}`, encodeURIComponent(String(requestParameters['namespace'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers one KV key\'s value from the org\'s OWN Cloudflare account as RAW BYTES under the content type it was written with — not wrapped in a JSON envelope, which is why this is not a typed op. Any org member may read. A key that does not exist is Cloudflare\'s own 404; an invalid namespace, or a key that is empty, over 512 bytes, not valid UTF-8, or carries a control character, is 400; 503 if the org has never connected a Cloudflare token.
     * Read a Workers KV value as its stored bytes
     */
    async getCloudflareKvNamespacesByNamespaceValuesByKey(requestParameters: CloudflareApiGetCloudflareKvNamespacesByNamespaceValuesByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getCloudflareKvNamespacesByNamespaceValuesByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the org\'s Cloudflare Pages projects. Any org member may read.
     * Lists the org\'s Cloudflare Pages projects.
     */
    async getCloudflarePagesProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Lists the org\'s Cloudflare Pages projects. Any org member may read.
     * Lists the org\'s Cloudflare Pages projects.
     */
    async getCloudflarePagesProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflarePagesProjectsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads one Cloudflare Pages project — its build config, deployment configs and latest deployment. Any org member may read.
     * Reads one Cloudflare Pages project — its build config, deployment configs and latest deployment.
     */
    async getCloudflarePagesProjectsByProjectRaw(requestParameters: CloudflareApiGetCloudflarePagesProjectsByProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getCloudflarePagesProjectsByProject().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects/{project}`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads one Cloudflare Pages project — its build config, deployment configs and latest deployment. Any org member may read.
     * Reads one Cloudflare Pages project — its build config, deployment configs and latest deployment.
     */
    async getCloudflarePagesProjectsByProject(requestParameters: CloudflareApiGetCloudflarePagesProjectsByProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflarePagesProjectsByProjectRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the R2 buckets on the org\'s Cloudflare account. Any org member may read.
     * Lists the R2 buckets on the org\'s Cloudflare account.
     */
    async getCloudflareR2BucketsRaw(requestParameters: CloudflareApiGetCloudflareR2BucketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['perPage'] != null) {
            queryParameters['per_page'] = requestParameters['perPage'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['nameContains'] != null) {
            queryParameters['name_contains'] = requestParameters['nameContains'];
        }

        if (requestParameters['order'] != null) {
            queryParameters['order'] = requestParameters['order'];
        }

        if (requestParameters['direction'] != null) {
            queryParameters['direction'] = requestParameters['direction'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/r2/buckets`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Lists the R2 buckets on the org\'s Cloudflare account. Any org member may read.
     * Lists the R2 buckets on the org\'s Cloudflare account.
     */
    async getCloudflareR2Buckets(requestParameters: CloudflareApiGetCloudflareR2BucketsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareR2BucketsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the Worker scripts on the org\'s Cloudflare account. Any org member may read.
     * Lists the Worker scripts on the org\'s Cloudflare account.
     */
    async getCloudflareWorkersScriptsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/scripts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Lists the Worker scripts on the org\'s Cloudflare account. Any org member may read.
     * Lists the Worker scripts on the org\'s Cloudflare account.
     */
    async getCloudflareWorkersScripts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareWorkersScriptsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads the org account\'s workers.dev subdomain — the name under which every subdomain-enabled script is served. Any org member may read.
     * Reads the org account\'s workers.dev subdomain — the name under which every subdomain-enabled script is served.
     */
    async getCloudflareWorkersSubdomainRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/subdomain`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads the org account\'s workers.dev subdomain — the name under which every subdomain-enabled script is served. Any org member may read.
     * Reads the org account\'s workers.dev subdomain — the name under which every subdomain-enabled script is served.
     */
    async getCloudflareWorkersSubdomain(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareWorkersSubdomainRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the Worker routes bound within one zone — the URL patterns that dispatch to a script. Any org member may read. Routes are zone-scoped, so no account is resolved.
     * Lists the Worker routes bound within one zone — the URL patterns that dispatch to a script.
     */
    async getCloudflareWorkersZonesByZoneRoutesRaw(requestParameters: CloudflareApiGetCloudflareWorkersZonesByZoneRoutesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['zone'] == null) {
            throw new runtime.RequiredError(
                'zone',
                'Required parameter "zone" was null or undefined when calling getCloudflareWorkersZonesByZoneRoutes().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/zones/{zone}/routes`;
        urlPath = urlPath.replace(`{${"zone"}}`, encodeURIComponent(String(requestParameters['zone'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Lists the Worker routes bound within one zone — the URL patterns that dispatch to a script. Any org member may read. Routes are zone-scoped, so no account is resolved.
     * Lists the Worker routes bound within one zone — the URL patterns that dispatch to a script.
     */
    async getCloudflareWorkersZonesByZoneRoutes(requestParameters: CloudflareApiGetCloudflareWorkersZonesByZoneRoutesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareWorkersZonesByZoneRoutesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the Cloudflare zones the org\'s connected API token can see, paged and filtered by the query parameters Cloudflare itself accepts. Zones are token-scoped by Cloudflare, so no account is resolved. Any org member may read.  Zone and DNS-record MANAGEMENT is not here: it stays on the Hanzo DNS plane (/v1/dns). This only surfaces the Cloudflare zone objects the asset plane needs — a zone id is what addresses a Worker route or an analytics read.
     * Lists the Cloudflare zones the org\'s connected API token can see, paged and filtered by the query parameters Cloudflare itself accepts.
     */
    async getCloudflareZonesRaw(requestParameters: CloudflareApiGetCloudflareZonesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['perPage'] != null) {
            queryParameters['per_page'] = requestParameters['perPage'];
        }

        if (requestParameters['name'] != null) {
            queryParameters['name'] = requestParameters['name'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['order'] != null) {
            queryParameters['order'] = requestParameters['order'];
        }

        if (requestParameters['direction'] != null) {
            queryParameters['direction'] = requestParameters['direction'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/zones`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Lists the Cloudflare zones the org\'s connected API token can see, paged and filtered by the query parameters Cloudflare itself accepts. Zones are token-scoped by Cloudflare, so no account is resolved. Any org member may read.  Zone and DNS-record MANAGEMENT is not here: it stays on the Hanzo DNS plane (/v1/dns). This only surfaces the Cloudflare zone objects the asset plane needs — a zone id is what addresses a Worker route or an analytics read.
     * Lists the Cloudflare zones the org\'s connected API token can see, paged and filtered by the query parameters Cloudflare itself accepts.
     */
    async getCloudflareZones(requestParameters: CloudflareApiGetCloudflareZonesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareZonesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads one Cloudflare zone the org\'s token can see. Any org member may read. A zone id the token cannot see is Cloudflare\'s own not-found, relayed.
     * Reads one Cloudflare zone the org\'s token can see.
     */
    async getCloudflareZonesByZoneRaw(requestParameters: CloudflareApiGetCloudflareZonesByZoneRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['zone'] == null) {
            throw new runtime.RequiredError(
                'zone',
                'Required parameter "zone" was null or undefined when calling getCloudflareZonesByZone().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/zones/{zone}`;
        urlPath = urlPath.replace(`{${"zone"}}`, encodeURIComponent(String(requestParameters['zone'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads one Cloudflare zone the org\'s token can see. Any org member may read. A zone id the token cannot see is Cloudflare\'s own not-found, relayed.
     * Reads one Cloudflare zone the org\'s token can see.
     */
    async getCloudflareZonesByZone(requestParameters: CloudflareApiGetCloudflareZonesByZoneRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareZonesByZoneRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads a zone\'s Cloudflare traffic dashboard — requests, bandwidth, threats and pageviews over the since/until window. Any org member may read.  A zone whose Cloudflare plan does not serve this endpoint yields Cloudflare\'s OWN error, never a fabricated success.
     * Reads a zone\'s Cloudflare traffic dashboard — requests, bandwidth, threats and pageviews over the since/until window.
     */
    async getCloudflareZonesByZoneAnalyticsRaw(requestParameters: CloudflareApiGetCloudflareZonesByZoneAnalyticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['zone'] == null) {
            throw new runtime.RequiredError(
                'zone',
                'Required parameter "zone" was null or undefined when calling getCloudflareZonesByZoneAnalytics().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['until'] != null) {
            queryParameters['until'] = requestParameters['until'];
        }

        if (requestParameters['continuous'] != null) {
            queryParameters['continuous'] = requestParameters['continuous'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/zones/{zone}/analytics`;
        urlPath = urlPath.replace(`{${"zone"}}`, encodeURIComponent(String(requestParameters['zone'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reads a zone\'s Cloudflare traffic dashboard — requests, bandwidth, threats and pageviews over the since/until window. Any org member may read.  A zone whose Cloudflare plan does not serve this endpoint yields Cloudflare\'s OWN error, never a fabricated success.
     * Reads a zone\'s Cloudflare traffic dashboard — requests, bandwidth, threats and pageviews over the since/until window.
     */
    async getCloudflareZonesByZoneAnalytics(requestParameters: CloudflareApiGetCloudflareZonesByZoneAnalyticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getCloudflareZonesByZoneAnalyticsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs a Workers AI model — the model id is the rest of the path, e.g. `@cf/meta/llama-3.1-8b-instruct` — on the org\'s OWN Cloudflare account and relays the model\'s output. The request body is whatever the chosen model takes (a prompt, chat messages, a base64 audio clip) and is forwarded unchanged; the response is the model\'s own, which for an image or audio model is BYTES under Cloudflare\'s content type rather than JSON. Both halves are why this is not a typed op.  It is the ONE PRICED route on this plane, because a run is inference rather than passthrough. The org\'s own token already paid Cloudflare for the compute, so Hanzo debits only the thin BYO routing fee — never the full inference cost — and meters it on the SAME `ai` product axis and per-project caps as every other model call, so Workers AI spend sums with LLM spend. The fee has a floor, so every run leaves a usage row even for a modality that reports no tokens, and it emits one gen_ai span with `gen_ai.system = cloudflare`.  Gated by BALANCE, not by the admin bit that guards the destructive verbs here: a validated org is enough, and a frozen, broke or over-cap org is refused with the fleet-wide 402/503 billing contract BEFORE any byte reaches Cloudflare — no run, and no account discovery either. An empty or oversized body is 400, as is a model id that is not a plain Cloudflare model path; 503 if the org has never connected a Cloudflare token.
     * Run a Cloudflare Workers AI model and get its output back
     */
    async postCloudflareAiRunByWildcard1Raw(requestParameters: CloudflareApiPostCloudflareAiRunByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling postCloudflareAiRunByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/ai/run/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Runs a Workers AI model — the model id is the rest of the path, e.g. `@cf/meta/llama-3.1-8b-instruct` — on the org\'s OWN Cloudflare account and relays the model\'s output. The request body is whatever the chosen model takes (a prompt, chat messages, a base64 audio clip) and is forwarded unchanged; the response is the model\'s own, which for an image or audio model is BYTES under Cloudflare\'s content type rather than JSON. Both halves are why this is not a typed op.  It is the ONE PRICED route on this plane, because a run is inference rather than passthrough. The org\'s own token already paid Cloudflare for the compute, so Hanzo debits only the thin BYO routing fee — never the full inference cost — and meters it on the SAME `ai` product axis and per-project caps as every other model call, so Workers AI spend sums with LLM spend. The fee has a floor, so every run leaves a usage row even for a modality that reports no tokens, and it emits one gen_ai span with `gen_ai.system = cloudflare`.  Gated by BALANCE, not by the admin bit that guards the destructive verbs here: a validated org is enough, and a frozen, broke or over-cap org is refused with the fleet-wide 402/503 billing contract BEFORE any byte reaches Cloudflare — no run, and no account discovery either. An empty or oversized body is 400, as is a model id that is not a plain Cloudflare model path; 503 if the org has never connected a Cloudflare token.
     * Run a Cloudflare Workers AI model and get its output back
     */
    async postCloudflareAiRunByWildcard1(requestParameters: CloudflareApiPostCloudflareAiRunByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCloudflareAiRunByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * Creates a D1 database on the org\'s Cloudflare account. Requires org admin.
     * Creates a D1 database on the org\'s Cloudflare account.
     */
    async postCloudflareD1DatabasesRaw(requestParameters: CloudflareApiPostCloudflareD1DatabasesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['databaseCreateIn'] == null) {
            throw new runtime.RequiredError(
                'databaseCreateIn',
                'Required parameter "databaseCreateIn" was null or undefined when calling postCloudflareD1Databases().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/d1/databases`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DatabaseCreateInToJSON(requestParameters['databaseCreateIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Creates a D1 database on the org\'s Cloudflare account. Requires org admin.
     * Creates a D1 database on the org\'s Cloudflare account.
     */
    async postCloudflareD1Databases(requestParameters: CloudflareApiPostCloudflareD1DatabasesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareD1DatabasesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Executes a statement on one D1 database on the org\'s OWN Cloudflare account and relays D1\'s result set. `sql` is required and `params` carries the bound values in placeholder order — use them rather than interpolating values into the statement.  The body is checked for a non-empty `sql` and then forwarded VERBATIM, so every field D1 accepts reaches D1 even though only two are named here; the declared schema is open for that reason. That verbatim forward is why this is not a typed op — decoding and re-encoding the body would drop `params`, where the query\'s bound values live. Requires ORG ADMIN (403 otherwise); a malformed body or missing `sql` is 400; 503 if the org has never connected a Cloudflare token.
     * Run a SQL statement against a D1 database
     */
    async postCloudflareD1DatabasesByDatabaseQueryRaw(requestParameters: CloudflareApiPostCloudflareD1DatabasesByDatabaseQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['database'] == null) {
            throw new runtime.RequiredError(
                'database',
                'Required parameter "database" was null or undefined when calling postCloudflareD1DatabasesByDatabaseQuery().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/d1/databases/{database}/query`;
        urlPath = urlPath.replace(`{${"database"}}`, encodeURIComponent(String(requestParameters['database'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: D1QueryToJSON(requestParameters['d1Query']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Executes a statement on one D1 database on the org\'s OWN Cloudflare account and relays D1\'s result set. `sql` is required and `params` carries the bound values in placeholder order — use them rather than interpolating values into the statement.  The body is checked for a non-empty `sql` and then forwarded VERBATIM, so every field D1 accepts reaches D1 even though only two are named here; the declared schema is open for that reason. That verbatim forward is why this is not a typed op — decoding and re-encoding the body would drop `params`, where the query\'s bound values live. Requires ORG ADMIN (403 otherwise); a malformed body or missing `sql` is 400; 503 if the org has never connected a Cloudflare token.
     * Run a SQL statement against a D1 database
     */
    async postCloudflareD1DatabasesByDatabaseQuery(requestParameters: CloudflareApiPostCloudflareD1DatabasesByDatabaseQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareD1DatabasesByDatabaseQueryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * KVNamespaceCreate creates a Workers KV namespace on the org\'s Cloudflare account. Requires org admin. Cloudflare mints the namespace id the value routes address.
     * KVNamespaceCreate creates a Workers KV namespace on the org\'s Cloudflare account.
     */
    async postCloudflareKvNamespacesRaw(requestParameters: CloudflareApiPostCloudflareKvNamespacesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['namespaceCreateIn'] == null) {
            throw new runtime.RequiredError(
                'namespaceCreateIn',
                'Required parameter "namespaceCreateIn" was null or undefined when calling postCloudflareKvNamespaces().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/kv/namespaces`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NamespaceCreateInToJSON(requestParameters['namespaceCreateIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * KVNamespaceCreate creates a Workers KV namespace on the org\'s Cloudflare account. Requires org admin. Cloudflare mints the namespace id the value routes address.
     * KVNamespaceCreate creates a Workers KV namespace on the org\'s Cloudflare account.
     */
    async postCloudflareKvNamespaces(requestParameters: CloudflareApiPostCloudflareKvNamespacesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareKvNamespacesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a Cloudflare Pages project on the org\'s account. Requires org admin. Only the modeled fields reach Cloudflare, so an unmodeled key in the request is dropped rather than forwarded.
     * Creates a Cloudflare Pages project on the org\'s account.
     */
    async postCloudflarePagesProjectsRaw(requestParameters: CloudflareApiPostCloudflarePagesProjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['pagesProjectCreate'] == null) {
            throw new runtime.RequiredError(
                'pagesProjectCreate',
                'Required parameter "pagesProjectCreate" was null or undefined when calling postCloudflarePagesProjects().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PagesProjectCreateToJSON(requestParameters['pagesProjectCreate']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Creates a Cloudflare Pages project on the org\'s account. Requires org admin. Only the modeled fields reach Cloudflare, so an unmodeled key in the request is dropped rather than forwarded.
     * Creates a Cloudflare Pages project on the org\'s account.
     */
    async postCloudflarePagesProjects(requestParameters: CloudflareApiPostCloudflarePagesProjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflarePagesProjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Starts a build and deployment of one Cloudflare Pages project on the org\'s OWN Cloudflare account, and relays Cloudflare\'s deployment record back. `branch` picks what to build; OMITTING it builds the project\'s production branch.  A body it cannot parse is IGNORED rather than refused — the deployment falls back to the production branch — which is the one rule to get right here and the reason this is not a typed op: a typed request would answer 400 where this deploys. Requires ORG ADMIN (403 otherwise), and 503 if the org has never connected a Cloudflare token.
     * Trigger a new Pages deployment for a project
     */
    async postCloudflarePagesProjectsByProjectDeploymentsRaw(requestParameters: CloudflareApiPostCloudflarePagesProjectsByProjectDeploymentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postCloudflarePagesProjectsByProjectDeployments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects/{project}/deployments`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PagesDeployToJSON(requestParameters['pagesDeploy']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Starts a build and deployment of one Cloudflare Pages project on the org\'s OWN Cloudflare account, and relays Cloudflare\'s deployment record back. `branch` picks what to build; OMITTING it builds the project\'s production branch.  A body it cannot parse is IGNORED rather than refused — the deployment falls back to the production branch — which is the one rule to get right here and the reason this is not a typed op: a typed request would answer 400 where this deploys. Requires ORG ADMIN (403 otherwise), and 503 if the org has never connected a Cloudflare token.
     * Trigger a new Pages deployment for a project
     */
    async postCloudflarePagesProjectsByProjectDeployments(requestParameters: CloudflareApiPostCloudflarePagesProjectsByProjectDeploymentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflarePagesProjectsByProjectDeploymentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Attaches a custom domain to a Cloudflare Pages project. Requires org admin. Cloudflare owns validation and certificate issuance from here on.
     * Attaches a custom domain to a Cloudflare Pages project.
     */
    async postCloudflarePagesProjectsByProjectDomainsRaw(requestParameters: CloudflareApiPostCloudflarePagesProjectsByProjectDomainsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postCloudflarePagesProjectsByProjectDomains().'
            );
        }

        if (requestParameters['domainAddIn'] == null) {
            throw new runtime.RequiredError(
                'domainAddIn',
                'Required parameter "domainAddIn" was null or undefined when calling postCloudflarePagesProjectsByProjectDomains().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/pages/projects/{project}/domains`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DomainAddInToJSON(requestParameters['domainAddIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Attaches a custom domain to a Cloudflare Pages project. Requires org admin. Cloudflare owns validation and certificate issuance from here on.
     * Attaches a custom domain to a Cloudflare Pages project.
     */
    async postCloudflarePagesProjectsByProjectDomains(requestParameters: CloudflareApiPostCloudflarePagesProjectsByProjectDomainsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflarePagesProjectsByProjectDomainsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an R2 bucket on the org\'s Cloudflare account. Requires org admin.
     * Creates an R2 bucket on the org\'s Cloudflare account.
     */
    async postCloudflareR2BucketsRaw(requestParameters: CloudflareApiPostCloudflareR2BucketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['bucketCreateIn'] == null) {
            throw new runtime.RequiredError(
                'bucketCreateIn',
                'Required parameter "bucketCreateIn" was null or undefined when calling postCloudflareR2Buckets().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/r2/buckets`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BucketCreateInToJSON(requestParameters['bucketCreateIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Creates an R2 bucket on the org\'s Cloudflare account. Requires org admin.
     * Creates an R2 bucket on the org\'s Cloudflare account.
     */
    async postCloudflareR2Buckets(requestParameters: CloudflareApiPostCloudflareR2BucketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareR2BucketsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publishes or withdraws one Worker script on the account\'s workers.dev subdomain. Requires org admin.
     * Publishes or withdraws one Worker script on the account\'s workers.dev subdomain.
     */
    async postCloudflareWorkersScriptsByScriptSubdomainRaw(requestParameters: CloudflareApiPostCloudflareWorkersScriptsByScriptSubdomainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['script'] == null) {
            throw new runtime.RequiredError(
                'script',
                'Required parameter "script" was null or undefined when calling postCloudflareWorkersScriptsByScriptSubdomain().'
            );
        }

        if (requestParameters['subdomainSetIn'] == null) {
            throw new runtime.RequiredError(
                'subdomainSetIn',
                'Required parameter "subdomainSetIn" was null or undefined when calling postCloudflareWorkersScriptsByScriptSubdomain().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/scripts/{script}/subdomain`;
        urlPath = urlPath.replace(`{${"script"}}`, encodeURIComponent(String(requestParameters['script'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubdomainSetInToJSON(requestParameters['subdomainSetIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Publishes or withdraws one Worker script on the account\'s workers.dev subdomain. Requires org admin.
     * Publishes or withdraws one Worker script on the account\'s workers.dev subdomain.
     */
    async postCloudflareWorkersScriptsByScriptSubdomain(requestParameters: CloudflareApiPostCloudflareWorkersScriptsByScriptSubdomainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareWorkersScriptsByScriptSubdomainRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Binds a URL pattern in a zone to a Worker script. Requires org admin — a route is what puts a script in front of live traffic.
     * Binds a URL pattern in a zone to a Worker script.
     */
    async postCloudflareWorkersZonesByZoneRoutesRaw(requestParameters: CloudflareApiPostCloudflareWorkersZonesByZoneRoutesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['zone'] == null) {
            throw new runtime.RequiredError(
                'zone',
                'Required parameter "zone" was null or undefined when calling postCloudflareWorkersZonesByZoneRoutes().'
            );
        }

        if (requestParameters['routeCreateIn'] == null) {
            throw new runtime.RequiredError(
                'routeCreateIn',
                'Required parameter "routeCreateIn" was null or undefined when calling postCloudflareWorkersZonesByZoneRoutes().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/zones/{zone}/routes`;
        urlPath = urlPath.replace(`{${"zone"}}`, encodeURIComponent(String(requestParameters['zone'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RouteCreateInToJSON(requestParameters['routeCreateIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Binds a URL pattern in a zone to a Worker script. Requires org admin — a route is what puts a script in front of live traffic.
     * Binds a URL pattern in a zone to a Worker script.
     */
    async postCloudflareWorkersZonesByZoneRoutes(requestParameters: CloudflareApiPostCloudflareWorkersZonesByZoneRoutesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareWorkersZonesByZoneRoutesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Drops a zone\'s Cloudflare edge cache — either the whole zone (purge_everything) or exactly the listed file URLs. Requires org admin.  Purging is the one zone-scoped WRITE this plane owns. It is not DNS — no record changes — so it does not belong on /v1/dns, and it is not a connection, so it does not belong on the integrations plane. It is a cache operation on a zone, which is what this asset plane is for. It takes the admin gate because dropping a zone\'s cache sends every subsequent request to the origin: on a site fronting a small origin that is a self-inflicted load spike, so it is a change, not a look.  Exactly one selector is required. Cloudflare treats a body with neither as a no-op and answers 200, which reads as \"purged\" to a caller that never purged anything — the failure we refuse to pass through.
     * Drops a zone\'s Cloudflare edge cache — either the whole zone (purge_everything) or exactly the listed file URLs.
     */
    async postCloudflareZonesByZonePurgeRaw(requestParameters: CloudflareApiPostCloudflareZonesByZonePurgeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['zone'] == null) {
            throw new runtime.RequiredError(
                'zone',
                'Required parameter "zone" was null or undefined when calling postCloudflareZonesByZonePurge().'
            );
        }

        if (requestParameters['purgeIn'] == null) {
            throw new runtime.RequiredError(
                'purgeIn',
                'Required parameter "purgeIn" was null or undefined when calling postCloudflareZonesByZonePurge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/zones/{zone}/purge`;
        urlPath = urlPath.replace(`{${"zone"}}`, encodeURIComponent(String(requestParameters['zone'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PurgeInToJSON(requestParameters['purgeIn']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Drops a zone\'s Cloudflare edge cache — either the whole zone (purge_everything) or exactly the listed file URLs. Requires org admin.  Purging is the one zone-scoped WRITE this plane owns. It is not DNS — no record changes — so it does not belong on /v1/dns, and it is not a connection, so it does not belong on the integrations plane. It is a cache operation on a zone, which is what this asset plane is for. It takes the admin gate because dropping a zone\'s cache sends every subsequent request to the origin: on a site fronting a small origin that is a self-inflicted load spike, so it is a change, not a look.  Exactly one selector is required. Cloudflare treats a body with neither as a no-op and answers 200, which reads as \"purged\" to a caller that never purged anything — the failure we refuse to pass through.
     * Drops a zone\'s Cloudflare edge cache — either the whole zone (purge_everything) or exactly the listed file URLs.
     */
    async postCloudflareZonesByZonePurge(requestParameters: CloudflareApiPostCloudflareZonesByZonePurgeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postCloudflareZonesByZonePurgeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stores one KV key on the org\'s OWN Cloudflare account. The REQUEST BODY IS THE VALUE, forwarded verbatim under the caller\'s own Content-Type (`text/plain` when none is sent), so a value is never re-encoded on the way in — which is why this is not a typed op. `expiration` and `expiration_ttl` may ride the query string and are passed through to Cloudflare. Requires ORG ADMIN (403 otherwise); the same namespace and key validation as the read answers 400; 503 if the org has never connected a Cloudflare token.
     * Write a Workers KV value from the request body
     */
    async putCloudflareKvNamespacesByNamespaceValuesByKeyRaw(requestParameters: CloudflareApiPutCloudflareKvNamespacesByNamespaceValuesByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['namespace'] == null) {
            throw new runtime.RequiredError(
                'namespace',
                'Required parameter "namespace" was null or undefined when calling putCloudflareKvNamespacesByNamespaceValuesByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling putCloudflareKvNamespacesByNamespaceValuesByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/kv/namespaces/{namespace}/values/{key}`;
        urlPath = urlPath.replace(`{${"namespace"}}`, encodeURIComponent(String(requestParameters['namespace'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Stores one KV key on the org\'s OWN Cloudflare account. The REQUEST BODY IS THE VALUE, forwarded verbatim under the caller\'s own Content-Type (`text/plain` when none is sent), so a value is never re-encoded on the way in — which is why this is not a typed op. `expiration` and `expiration_ttl` may ride the query string and are passed through to Cloudflare. Requires ORG ADMIN (403 otherwise); the same namespace and key validation as the read answers 400; 503 if the org has never connected a Cloudflare token.
     * Write a Workers KV value from the request body
     */
    async putCloudflareKvNamespacesByNamespaceValuesByKey(requestParameters: CloudflareApiPutCloudflareKvNamespacesByNamespaceValuesByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.putCloudflareKvNamespacesByNamespaceValuesByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Publishes a module Worker to the org\'s OWN Cloudflare account under the name in the path, replacing whatever was there, and relays Cloudflare\'s result. `script` carries the module SOURCE; the optional compatibility date, compatibility flags and bindings are packed into the multipart upload Cloudflare expects.  The path names the script and the body field named `script` is its source — two different things that share a name, which is exactly why this cannot be a typed op: a binder that gives the URL the last word would overwrite the source with the script\'s name. Requires ORG ADMIN (403 otherwise); an unparseable body or empty source is 400; 503 if the org has never connected a Cloudflare token.
     * Upload or replace a module Worker script
     */
    async putCloudflareWorkersScriptsByScriptRaw(requestParameters: CloudflareApiPutCloudflareWorkersScriptsByScriptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['script'] == null) {
            throw new runtime.RequiredError(
                'script',
                'Required parameter "script" was null or undefined when calling putCloudflareWorkersScriptsByScript().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloudflare/workers/scripts/{script}`;
        urlPath = urlPath.replace(`{${"script"}}`, encodeURIComponent(String(requestParameters['script'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: WorkerScriptPutToJSON(requestParameters['workerScriptPut']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Publishes a module Worker to the org\'s OWN Cloudflare account under the name in the path, replacing whatever was there, and relays Cloudflare\'s result. `script` carries the module SOURCE; the optional compatibility date, compatibility flags and bindings are packed into the multipart upload Cloudflare expects.  The path names the script and the body field named `script` is its source — two different things that share a name, which is exactly why this cannot be a typed op: a binder that gives the URL the last word would overwrite the source with the script\'s name. Requires ORG ADMIN (403 otherwise); an unparseable body or empty source is 400; 503 if the org has never connected a Cloudflare token.
     * Upload or replace a module Worker script
     */
    async putCloudflareWorkersScriptsByScript(requestParameters: CloudflareApiPutCloudflareWorkersScriptsByScriptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.putCloudflareWorkersScriptsByScriptRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
