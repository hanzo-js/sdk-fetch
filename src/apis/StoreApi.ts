/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';

export interface StoreApiDeleteStoreByStoreidRequest {
    storeid: string;
}

export interface StoreApiDeleteStoreByStoreidListingByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiGetStoreByStoreidRequest {
    storeid: string;
}

export interface StoreApiGetStoreByStoreidBundleByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiGetStoreByStoreidListingRequest {
    storeid: string;
}

export interface StoreApiGetStoreByStoreidListingByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiGetStoreByStoreidProductByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiGetStoreByStoreidVariantByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiPatchStoreByStoreidRequest {
    storeid: string;
}

export interface StoreApiPatchStoreByStoreidListingByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiPostStoreByStoreidRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidAuthorizeRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidAuthorizeByOrderidRequest {
    storeid: string;
    orderid: string;
}

export interface StoreApiPostStoreByStoreidCaptureByOrderidRequest {
    storeid: string;
    orderid: string;
}

export interface StoreApiPostStoreByStoreidChargeRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidCheckoutAuthorizeRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidCheckoutAuthorizeByOrderidRequest {
    storeid: string;
    orderid: string;
}

export interface StoreApiPostStoreByStoreidCheckoutCaptureByOrderidRequest {
    storeid: string;
    orderid: string;
}

export interface StoreApiPostStoreByStoreidCheckoutChargeRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidCheckoutPaypalCancelByPaykeyRequest {
    storeid: string;
    payKey: string;
}

export interface StoreApiPostStoreByStoreidCheckoutPaypalConfirmByPaykeyRequest {
    storeid: string;
    payKey: string;
}

export interface StoreApiPostStoreByStoreidCheckoutPaypalPayRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidListingByKeyRequest {
    storeid: string;
    key: string;
}

export interface StoreApiPostStoreByStoreidPaypalCancelByPaykeyRequest {
    storeid: string;
    payKey: string;
}

export interface StoreApiPostStoreByStoreidPaypalConfirmByPaykeyRequest {
    storeid: string;
    payKey: string;
}

export interface StoreApiPostStoreByStoreidPaypalPayRequest {
    storeid: string;
}

export interface StoreApiPostStoreByStoreidTrialRequest {
    storeid: string;
}

export interface StoreApiPutStoreByStoreidRequest {
    storeid: string;
}

export interface StoreApiPutStoreByStoreidListingByKeyRequest {
    storeid: string;
    key: string;
}

/**
 * 
 */
export class StoreApi extends runtime.BaseAPI {

    /**
     * Removes the addressed store and answers 204 with no body. Before the live row goes, the entity is written once more under a tombstone kind, so the deletion leaves a recoverable copy rather than destroying the record outright; the store\'s listing overrides live inside that row and go with it. The id is resolved inside the caller org\'s own namespace, so an unknown or foreign id is 404. Requires an admin or store-write token.
     * Delete a storefront, keeping a recoverable copy
     */
    async deleteStoreByStoreidRaw(requestParameters: StoreApiDeleteStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling deleteStoreByStoreid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the addressed store and answers 204 with no body. Before the live row goes, the entity is written once more under a tombstone kind, so the deletion leaves a recoverable copy rather than destroying the record outright; the store\'s listing overrides live inside that row and go with it. The id is resolved inside the caller org\'s own namespace, so an unknown or foreign id is 404. Requires an admin or store-write token.
     * Delete a storefront, keeping a recoverable copy
     */
    async deleteStoreByStoreid(requestParameters: StoreApiDeleteStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteStoreByStoreidRaw(requestParameters, initOverrides);
    }

    /**
     * Drops the key from the store\'s listing map and re-saves the store, answering 204 with no body. It UN-OVERRIDES rather than deletes: the product, variant or bundle itself is untouched and simply reverts to its catalog values on this storefront. A key that is not present is 404, and so is a store id outside the caller org\'s namespace. Admin-gated.
     * Remove a listing override
     */
    async deleteStoreByStoreidListingByKeyRaw(requestParameters: StoreApiDeleteStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling deleteStoreByStoreidListingByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling deleteStoreByStoreidListingByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/listing/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Drops the key from the store\'s listing map and re-saves the store, answering 204 with no body. It UN-OVERRIDES rather than deletes: the product, variant or bundle itself is untouched and simply reverts to its catalog values on this storefront. A key that is not present is 404, and so is a store id outside the caller org\'s namespace. Admin-gated.
     * Remove a listing override
     */
    async deleteStoreByStoreidListingByKey(requestParameters: StoreApiDeleteStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteStoreByStoreidListingByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Answers a pagination envelope — page, display, the rows, and a total count — read from the caller org\'s OWN namespaced database, so one tenant can never list another\'s stores. Sorting defaults to the store slug and is overridable with sort; display is the page size and page applies only alongside it, and either one that is not a positive integer is refused rather than silently ignored. The limit query overrides the reported COUNT only and never the rows returned. A request that resolves no org namespace is served an empty page, never an unscoped scan. Readable with an admin token, a store-scoped token, or the anonymous published storefront key.
     * List your org\'s storefronts as a page
     */
    async getStoreRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers a pagination envelope — page, display, the rows, and a total count — read from the caller org\'s OWN namespaced database, so one tenant can never list another\'s stores. Sorting defaults to the store slug and is overridable with sort; display is the page size and page applies only alongside it, and either one that is not a positive integer is refused rather than silently ignored. The limit query overrides the reported COUNT only and never the rows returned. A request that resolves no org namespace is served an empty page, never an unscoped scan. Readable with an admin token, a store-scoped token, or the anonymous published storefront key.
     * List your org\'s storefronts as a page
     */
    async getStore(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreRaw(initOverrides);
    }

    /**
     * Answers allowed, the store id, and a status of trial, active, payment_required, store_required or unavailable — the entitlement check a merchant surface gates on. The rule that surprises people is that entitlement is PER STORE, not per org: the store needs its own current subscription on the entry plan, either trialing with a trial end still ahead or active with a period end still ahead, so an org-wide balance or a sibling store\'s plan unlocks nothing here. The store comes from the X-Store-Id header and otherwise falls back to the org\'s first store; neither resolving is store_required with allowed false, and a backing-store failure is 503 with status unavailable — a retry signal, not a denial.
     * Whether a store is entitled to trade, and why
     */
    async getStoreAccessRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/access`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers allowed, the store id, and a status of trial, active, payment_required, store_required or unavailable — the entitlement check a merchant surface gates on. The rule that surprises people is that entitlement is PER STORE, not per org: the store needs its own current subscription on the entry plan, either trialing with a trial end still ahead or active with a period end still ahead, so an org-wide balance or a sibling store\'s plan unlocks nothing here. The store comes from the X-Store-Id header and otherwise falls back to the org\'s first store; neither resolving is store_required with allowed false, and a backing-store failure is 503 with status unavailable — a retry signal, not a denial.
     * Whether a store is entitled to trade, and why
     */
    async getStoreAccess(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreAccessRaw(initOverrides);
    }

    /**
     * Reads the addressed store from the caller org\'s own namespaced database, so an id belonging to another tenant is simply absent there and answers 404 rather than leaking its existence. The body is the stored entity including its embedded listing override map. Readable with an admin or store-read token and also with the anonymous published storefront key, which is what lets a logged-out storefront resolve the store it is rendering.
     * Fetch one storefront
     */
    async getStoreByStoreidRaw(requestParameters: StoreApiGetStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling getStoreByStoreid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Reads the addressed store from the caller org\'s own namespaced database, so an id belonging to another tenant is simply absent there and answers 404 rather than leaking its existence. The body is the stored entity including its embedded listing override map. Readable with an admin or store-read token and also with the anonymous published storefront key, which is what lets a logged-out storefront resolve the store it is rendering.
     * Fetch one storefront
     */
    async getStoreByStoreid(requestParameters: StoreApiGetStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreByStoreidRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the stored bundle with the store\'s listing for it laid over the top — every non-empty listing field wins, and the currency is forced to the store\'s own — so the caller reads what this storefront actually sells rather than the catalog-wide record. The overlay is keyed by the item\'s ID: a listing filed only under a slug or SKU does not reach it, unlike the listing reads, which do fall back to those. An unknown store or key is 404. Readable with an admin token or the anonymous published storefront key.
     * Fetch a bundle as this storefront sells it
     */
    async getStoreByStoreidBundleByKeyRaw(requestParameters: StoreApiGetStoreByStoreidBundleByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling getStoreByStoreidBundleByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getStoreByStoreidBundleByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/bundle/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns the stored bundle with the store\'s listing for it laid over the top — every non-empty listing field wins, and the currency is forced to the store\'s own — so the caller reads what this storefront actually sells rather than the catalog-wide record. The overlay is keyed by the item\'s ID: a listing filed only under a slug or SKU does not reach it, unlike the listing reads, which do fall back to those. An unknown store or key is 404. Readable with an admin token or the anonymous published storefront key.
     * Fetch a bundle as this storefront sells it
     */
    async getStoreByStoreidBundleByKey(requestParameters: StoreApiGetStoreByStoreidBundleByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreByStoreidBundleByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Returns every override this store applies to catalog items — name, price, list price, media, availability and the hidden flag — keyed by product or variant id, in one read. A listing is an OVERRIDE, not a product: the catalog item exists independently and this map only says how this storefront presents it. Read from the caller org\'s own namespaced database, so a store id belonging to another tenant is 404. Readable with an admin token or the anonymous published storefront key.
     * The storefront\'s whole listing override map
     */
    async getStoreByStoreidListingRaw(requestParameters: StoreApiGetStoreByStoreidListingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling getStoreByStoreidListing().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/listing`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns every override this store applies to catalog items — name, price, list price, media, availability and the hidden flag — keyed by product or variant id, in one read. A listing is an OVERRIDE, not a product: the catalog item exists independently and this map only says how this storefront presents it. Read from the caller org\'s own namespaced database, so a store id belonging to another tenant is 404. Readable with an admin token or the anonymous published storefront key.
     * The storefront\'s whole listing override map
     */
    async getStoreByStoreidListing(requestParameters: StoreApiGetStoreByStoreidListingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreByStoreidListingRaw(requestParameters, initOverrides);
    }

    /**
     * Looks the key up in the store\'s listing map first and, failing that, matches it against each listing\'s slug and then its SKU — so a storefront holding only a product\'s URL slug can still resolve the override. That fallback is unique to the listing reads; the item overlay routes match by id alone. A key matching none of the three is 404, as is a store id outside the caller org\'s namespace. Readable with an admin token or the anonymous published storefront key.
     * Fetch one listing override, by item id or by its slug or SKU
     */
    async getStoreByStoreidListingByKeyRaw(requestParameters: StoreApiGetStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling getStoreByStoreidListingByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getStoreByStoreidListingByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/listing/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Looks the key up in the store\'s listing map first and, failing that, matches it against each listing\'s slug and then its SKU — so a storefront holding only a product\'s URL slug can still resolve the override. That fallback is unique to the listing reads; the item overlay routes match by id alone. A key matching none of the three is 404, as is a store id outside the caller org\'s namespace. Readable with an admin token or the anonymous published storefront key.
     * Fetch one listing override, by item id or by its slug or SKU
     */
    async getStoreByStoreidListingByKey(requestParameters: StoreApiGetStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreByStoreidListingByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the stored product with the store\'s listing for it laid over the top — non-empty listing fields replace the catalog values and the currency is forced to the store\'s own — which is what lets two storefronts sell the same catalog product at their own price, name and media. The overlay is keyed by the product\'s ID, so a listing filed only under a slug or SKU does not apply here. An unknown store or key is 404. Readable with an admin token or the anonymous published storefront key.
     * Fetch a product as this storefront sells it
     */
    async getStoreByStoreidProductByKeyRaw(requestParameters: StoreApiGetStoreByStoreidProductByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling getStoreByStoreidProductByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getStoreByStoreidProductByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/product/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns the stored product with the store\'s listing for it laid over the top — non-empty listing fields replace the catalog values and the currency is forced to the store\'s own — which is what lets two storefronts sell the same catalog product at their own price, name and media. The overlay is keyed by the product\'s ID, so a listing filed only under a slug or SKU does not apply here. An unknown store or key is 404. Readable with an admin token or the anonymous published storefront key.
     * Fetch a product as this storefront sells it
     */
    async getStoreByStoreidProductByKey(requestParameters: StoreApiGetStoreByStoreidProductByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreByStoreidProductByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the stored variant with the store\'s listing for it overlaid — non-empty listing fields replace the catalog values and the currency is forced to the store\'s own — which is what makes per-storefront pricing of a shared variant possible. The overlay is keyed by the variant\'s ID, never by its slug or SKU. An unknown store or key is 404. Readable with an admin token or the anonymous published storefront key.
     * Fetch a variant as this storefront sells it
     */
    async getStoreByStoreidVariantByKeyRaw(requestParameters: StoreApiGetStoreByStoreidVariantByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling getStoreByStoreidVariantByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getStoreByStoreidVariantByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/variant/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns the stored variant with the store\'s listing for it overlaid — non-empty listing fields replace the catalog values and the currency is forced to the store\'s own — which is what makes per-storefront pricing of a shared variant possible. The overlay is keyed by the variant\'s ID, never by its slug or SKU. An unknown store or key is 404. Readable with an admin token or the anonymous published storefront key.
     * Fetch a variant as this storefront sells it
     */
    async getStoreByStoreidVariantByKey(requestParameters: StoreApiGetStoreByStoreidVariantByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreByStoreidVariantByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the caller org\'s store resolved FROM THE AUTHENTICATED ORG rather than from a path id — which is how an admin dashboard or a storefront edge learns the store id it should then read and write against. An X-Store-Id header selects a specific store, resolved only inside the caller\'s own namespace, so a foreign id cannot cross the tenant boundary and answers 404 instead. With no header the org\'s first store is returned, and an org that has none yet has its canonical default provisioned lazily and idempotently, carrying no payment credentials. Only when there is no org in context, or provisioning fails, does it fall back to a placeholder store literally named default, which a storefront edge should treat as unconfigured.
     * Resolve your org\'s active storefront without naming an id
     */
    async getStoreCurrentRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/current`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns the caller org\'s store resolved FROM THE AUTHENTICATED ORG rather than from a path id — which is how an admin dashboard or a storefront edge learns the store id it should then read and write against. An X-Store-Id header selects a specific store, resolved only inside the caller\'s own namespace, so a foreign id cannot cross the tenant boundary and answers 404 instead. With no header the org\'s first store is returned, and an org that has none yet has its canonical default provisioned lazily and idempotently, carrying no payment credentials. Only when there is no org in context, or provisioning fails, does it fall back to a placeholder store literally named default, which a storefront edge should treat as unconfigured.
     * Resolve your org\'s active storefront without naming an id
     */
    async getStoreCurrent(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getStoreCurrentRaw(initOverrides);
    }

    /**
     * Loads the stored store and decodes the body over it, so only the fields the body names change and everything else keeps its stored value — the difference from the full replace, which clears what it is not told. Answers the merged entity. The id is resolved inside the caller org\'s own namespace, so an unknown or foreign id is 404. Requires an admin token, or one holding both store read and store write.
     * Change part of a storefront
     */
    async patchStoreByStoreidRaw(requestParameters: StoreApiPatchStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling patchStoreByStoreid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Loads the stored store and decodes the body over it, so only the fields the body names change and everything else keeps its stored value — the difference from the full replace, which clears what it is not told. Answers the merged entity. The id is resolved inside the caller org\'s own namespace, so an unknown or foreign id is 404. Requires an admin token, or one holding both store read and store write.
     * Change part of a storefront
     */
    async patchStoreByStoreid(requestParameters: StoreApiPatchStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchStoreByStoreidRaw(requestParameters, initOverrides);
    }

    /**
     * Requires the key to already be present — an absent one is 404 — and answers the store\'s listing map at 200. Read the behaviour before relying on it: the decoded body is applied to a COPY taken out of the map and is never assigned back, so the stored listing is unchanged and the map returned is exactly the map that was already there. An actual edit to an existing listing has to go through the upsert, which does write its result back into the store. A body that fails to decode is still 400. Admin-gated and namespaced to the caller\'s org.
     * Confirm a listing override exists and re-save the store
     */
    async patchStoreByStoreidListingByKeyRaw(requestParameters: StoreApiPatchStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling patchStoreByStoreidListingByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling patchStoreByStoreidListingByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/listing/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Requires the key to already be present — an absent one is 404 — and answers the store\'s listing map at 200. Read the behaviour before relying on it: the decoded body is applied to a COPY taken out of the map and is never assigned back, so the stored listing is unchanged and the map returned is exactly the map that was already there. An actual edit to an existing listing has to go through the upsert, which does write its result back into the store. A body that fails to decode is still 400. Admin-gated and namespaced to the caller\'s org.
     * Confirm a listing override exists and re-save the store
     */
    async patchStoreByStoreidListingByKey(requestParameters: StoreApiPatchStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchStoreByStoreidListingByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Creates a store from the body inside the caller org\'s own namespaced database, so the row is physically isolated to that tenant from its first write, and answers it at 201 with a Location header naming its id. Requires an admin or store-write token: the anonymous published storefront key may READ stores but never create one. A body that fails to decode is 400.
     * Create a storefront
     */
    async postStoreRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates a store from the body inside the caller org\'s own namespaced database, so the row is physically isolated to that tenant from its first write, and answers it at 201 with a Location header naming its id. Requires an admin or store-write token: the anonymous published storefront key may READ stores but never create one. A body that fails to decode is 400.
     * Create a storefront
     */
    async postStore(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreRaw(initOverrides);
    }

    /**
     * Re-dispatches the request into the handler the intended verb would have reached, taking that verb from a _method form value or query parameter and then from the X-HTTP-Method-Override header, the header winning when both are present. Only PUT, PATCH and DELETE are accepted; anything else resolves to 405. The trap is the default: naming NO override at all is treated as a partial update, never as a create. Authorization is whatever the underlying operation requires, since the real handler runs.
     * Method-override tunnel for clients that cannot send PUT, PATCH or DELETE
     */
    async postStoreByStoreidRaw(requestParameters: StoreApiPostStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Re-dispatches the request into the handler the intended verb would have reached, taking that verb from a _method form value or query parameter and then from the X-HTTP-Method-Override header, the header winning when both are present. Only PUT, PATCH and DELETE are accepted; anything else resolves to 405. The trap is the default: naming NO override at all is treated as a partial update, never as a create. Authorization is whatever the underlying operation requires, since the real handler runs.
     * Method-override tunnel for clients that cannot send PUT, PATCH or DELETE
     */
    async postStoreByStoreid(requestParameters: StoreApiPostStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidRaw(requestParameters, initOverrides);
    }

    /**
     * Tallies a new order for the addressed store from the user, payment and order body, reserves its items, runs the processor authorization and answers the saved order with a Location header pointing at it. The gate is a token carrying admin or published scope, so a published storefront key is enough; no token is 401 and a token with neither bit is 403. The store is loaded BEFORE any payment work and its currency OVERRIDES whatever the body asked for, so a store that will not load ends the call with 500 and nothing is charged. On any authorization failure the reservations are released and the order and payment are persisted as cancelled, so a failed attempt still leaves a durable record. Capture is a separate call.
     * Authorize a new order against a storefront, holding the funds without settling them
     */
    async postStoreByStoreidAuthorizeRaw(requestParameters: StoreApiPostStoreByStoreidAuthorizeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidAuthorize().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/authorize`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Tallies a new order for the addressed store from the user, payment and order body, reserves its items, runs the processor authorization and answers the saved order with a Location header pointing at it. The gate is a token carrying admin or published scope, so a published storefront key is enough; no token is 401 and a token with neither bit is 403. The store is loaded BEFORE any payment work and its currency OVERRIDES whatever the body asked for, so a store that will not load ends the call with 500 and nothing is charged. On any authorization failure the reservations are released and the order and payment are persisted as cancelled, so a failed attempt still leaves a durable record. Capture is a separate call.
     * Authorize a new order against a storefront, holding the funds without settling them
     */
    async postStoreByStoreidAuthorize(requestParameters: StoreApiPostStoreByStoreidAuthorizeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidAuthorizeRaw(requestParameters, initOverrides);
    }

    /**
     * Continues the order named in the path rather than minting a new one, holding funds for it. The order is loaded from the caller org\'s own store, so an id belonging to another tenant is a 404. The rule most callers get wrong is that the body\'s order object is MERGED onto the loaded order before the tally — this is not a read-only reference, and a field sent here overwrites what is stored. The gate, the store resolution and the currency override behave exactly as on the bodiless-id sibling, and settling is still the capture call\'s job.
     * Authorize an order that already exists, holding the funds without settling them
     */
    async postStoreByStoreidAuthorizeByOrderidRaw(requestParameters: StoreApiPostStoreByStoreidAuthorizeByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidAuthorizeByOrderid().'
            );
        }

        if (requestParameters['orderid'] == null) {
            throw new runtime.RequiredError(
                'orderid',
                'Required parameter "orderid" was null or undefined when calling postStoreByStoreidAuthorizeByOrderid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/authorize/{orderid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"orderid"}}`, encodeURIComponent(String(requestParameters['orderid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Continues the order named in the path rather than minting a new one, holding funds for it. The order is loaded from the caller org\'s own store, so an id belonging to another tenant is a 404. The rule most callers get wrong is that the body\'s order object is MERGED onto the loaded order before the tally — this is not a read-only reference, and a field sent here overwrites what is stored. The gate, the store resolution and the currency override behave exactly as on the bodiless-id sibling, and settling is still the capture call\'s job.
     * Authorize an order that already exists, holding the funds without settling them
     */
    async postStoreByStoreidAuthorizeByOrderid(requestParameters: StoreApiPostStoreByStoreidAuthorizeByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidAuthorizeByOrderidRaw(requestParameters, initOverrides);
    }

    /**
     * Settles the order named in the path — the second half of the two-step flow — and answers the updated order with a Location header. Dispatch follows the order\'s STORED payment type, and a successful capture is the moment the rest of the system learns about the sale: order and payment rows are updated, coupon redemptions, referral, cart and stats are written, the confirmation email goes out, and the paid and completed events are emitted. A capture failure releases the order\'s inventory reservations and answers 400, so a failed settlement never leaves items held.
     * Capture a previously authorized order and settle the payment
     */
    async postStoreByStoreidCaptureByOrderidRaw(requestParameters: StoreApiPostStoreByStoreidCaptureByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCaptureByOrderid().'
            );
        }

        if (requestParameters['orderid'] == null) {
            throw new runtime.RequiredError(
                'orderid',
                'Required parameter "orderid" was null or undefined when calling postStoreByStoreidCaptureByOrderid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/capture/{orderid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"orderid"}}`, encodeURIComponent(String(requestParameters['orderid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Settles the order named in the path — the second half of the two-step flow — and answers the updated order with a Location header. Dispatch follows the order\'s STORED payment type, and a successful capture is the moment the rest of the system learns about the sale: order and payment rows are updated, coupon redemptions, referral, cart and stats are written, the confirmation email goes out, and the paid and completed events are emitted. A capture failure releases the order\'s inventory reservations and answers 400, so a failed settlement never leaves items held.
     * Capture a previously authorized order and settle the payment
     */
    async postStoreByStoreidCaptureByOrderid(requestParameters: StoreApiPostStoreByStoreidCaptureByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCaptureByOrderidRaw(requestParameters, initOverrides);
    }

    /**
     * Runs authorization and capture back to back against a freshly created order — the one-step flow for callers with no reason to hold funds. It takes the authorize body and inherits every authorize rule: the store\'s currency wins over the body, the items are reserved before the processor is called, and the amount bounds the processor enforces still apply. There is no order id on this address, so it can never continue an existing order. Either half failing answers 400, and the capture side effects — confirmation email, redemptions, stats, the paid and completed events — run only when both halves succeed.
     * Authorize and capture a new order in one call
     */
    async postStoreByStoreidChargeRaw(requestParameters: StoreApiPostStoreByStoreidChargeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCharge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/charge`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Runs authorization and capture back to back against a freshly created order — the one-step flow for callers with no reason to hold funds. It takes the authorize body and inherits every authorize rule: the store\'s currency wins over the body, the items are reserved before the processor is called, and the amount bounds the processor enforces still apply. There is no order id on this address, so it can never continue an existing order. Either half failing answers 400, and the capture side effects — confirmation email, redemptions, stats, the paid and completed events — run only when both halves succeed.
     * Authorize and capture a new order in one call
     */
    async postStoreByStoreidCharge(requestParameters: StoreApiPostStoreByStoreidChargeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidChargeRaw(requestParameters, initOverrides);
    }

    /**
     * Authorizes a new order for the addressed store and holds the funds, answering the saved order with a Location header. It binds the identical handler as the shorter authorize address, so the two are ONE operation at two spellings and not two behaviours; the checkout prefix is the newer one. Every rule carries over: admin or published scope on the token, the store loaded first with its currency overriding the body, items reserved before the processor call, and reservations released with the order persisted cancelled on failure. Nothing is settled here.
     * Authorize a new order against a storefront, holding the funds — the checkout spelling
     */
    async postStoreByStoreidCheckoutAuthorizeRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutAuthorizeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutAuthorize().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/authorize`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Authorizes a new order for the addressed store and holds the funds, answering the saved order with a Location header. It binds the identical handler as the shorter authorize address, so the two are ONE operation at two spellings and not two behaviours; the checkout prefix is the newer one. Every rule carries over: admin or published scope on the token, the store loaded first with its currency overriding the body, items reserved before the processor call, and reservations released with the order persisted cancelled on failure. Nothing is settled here.
     * Authorize a new order against a storefront, holding the funds — the checkout spelling
     */
    async postStoreByStoreidCheckoutAuthorize(requestParameters: StoreApiPostStoreByStoreidCheckoutAuthorizeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutAuthorizeRaw(requestParameters, initOverrides);
    }

    /**
     * Continues the order named in the path rather than minting one, and shares its handler byte for byte with the unprefixed authorize-by-id address. The order is loaded from the caller org\'s own store, so another tenant\'s id is a 404, and the body\'s order object is merged onto the loaded row before the tally — a field sent here overwrites what is stored. Store resolution, the token gate and the currency override behave as on every other authorize address; settle with the capture address and the same order id.
     * Authorize an existing order, holding the funds — the checkout spelling
     */
    async postStoreByStoreidCheckoutAuthorizeByOrderidRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutAuthorizeByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutAuthorizeByOrderid().'
            );
        }

        if (requestParameters['orderid'] == null) {
            throw new runtime.RequiredError(
                'orderid',
                'Required parameter "orderid" was null or undefined when calling postStoreByStoreidCheckoutAuthorizeByOrderid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/authorize/{orderid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"orderid"}}`, encodeURIComponent(String(requestParameters['orderid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Continues the order named in the path rather than minting one, and shares its handler byte for byte with the unprefixed authorize-by-id address. The order is loaded from the caller org\'s own store, so another tenant\'s id is a 404, and the body\'s order object is merged onto the loaded row before the tally — a field sent here overwrites what is stored. Store resolution, the token gate and the currency override behave as on every other authorize address; settle with the capture address and the same order id.
     * Authorize an existing order, holding the funds — the checkout spelling
     */
    async postStoreByStoreidCheckoutAuthorizeByOrderid(requestParameters: StoreApiPostStoreByStoreidCheckoutAuthorizeByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutAuthorizeByOrderidRaw(requestParameters, initOverrides);
    }

    /**
     * Settles the authorized order named in the path and answers the updated order with a Location header, running the same handler as the unprefixed capture address. Dispatch follows the order\'s stored payment type. Success is what triggers the downstream work — order and payment updates, redemptions, referral, cart and stats, the confirmation email, and the paid and completed events — while a failure releases the order\'s inventory reservations and answers 400.
     * Capture a previously authorized order and settle it — the checkout spelling
     */
    async postStoreByStoreidCheckoutCaptureByOrderidRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutCaptureByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutCaptureByOrderid().'
            );
        }

        if (requestParameters['orderid'] == null) {
            throw new runtime.RequiredError(
                'orderid',
                'Required parameter "orderid" was null or undefined when calling postStoreByStoreidCheckoutCaptureByOrderid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/capture/{orderid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"orderid"}}`, encodeURIComponent(String(requestParameters['orderid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Settles the authorized order named in the path and answers the updated order with a Location header, running the same handler as the unprefixed capture address. Dispatch follows the order\'s stored payment type. Success is what triggers the downstream work — order and payment updates, redemptions, referral, cart and stats, the confirmation email, and the paid and completed events — while a failure releases the order\'s inventory reservations and answers 400.
     * Capture a previously authorized order and settle it — the checkout spelling
     */
    async postStoreByStoreidCheckoutCaptureByOrderid(requestParameters: StoreApiPostStoreByStoreidCheckoutCaptureByOrderidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutCaptureByOrderidRaw(requestParameters, initOverrides);
    }

    /**
     * Performs authorization and capture back to back against a newly created order for the addressed store, on the same handler as the unprefixed charge address. It takes the authorize body and inherits every authorize rule, including the store\'s currency winning over the body and the items being reserved before the processor is called. There is no order id on this address, so it can never continue an existing order. Either half failing answers 400, and the capture side effects run only when both succeed.
     * Authorize and capture a new order in one call — the checkout spelling
     */
    async postStoreByStoreidCheckoutChargeRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutChargeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutCharge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/charge`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Performs authorization and capture back to back against a newly created order for the addressed store, on the same handler as the unprefixed charge address. It takes the authorize body and inherits every authorize rule, including the store\'s currency winning over the body and the items being reserved before the processor is called. There is no order id on this address, so it can never continue an existing order. Either half failing answers 400, and the capture side effects run only when both succeed.
     * Authorize and capture a new order in one call — the checkout spelling
     */
    async postStoreByStoreidCheckoutCharge(requestParameters: StoreApiPostStoreByStoreidCheckoutChargeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutChargeRaw(requestParameters, initOverrides);
    }

    /**
     * Meant to void the payments carrying the given pay key, stamp them cancelled and cancel the order, but the shared checkout handler resolves its order from an ORDER ID path parameter this route does not carry. The result is an untyped order and a cancel dispatch that refuses with 400 before the pay key lookup ever runs. Token gate, namespacing and store resolution happen first, so a missing token is still 401 and an unloadable store still 500. It is the same handler as the unprefixed cancel address, with the same outcome.
     * PayPal cancel by pay key — refuses, exactly as the unprefixed address does
     */
    async postStoreByStoreidCheckoutPaypalCancelByPaykeyRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutPaypalCancelByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutPaypalCancelByPaykey().'
            );
        }

        if (requestParameters['payKey'] == null) {
            throw new runtime.RequiredError(
                'payKey',
                'Required parameter "payKey" was null or undefined when calling postStoreByStoreidCheckoutPaypalCancelByPaykey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/paypal/cancel/{payKey}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"payKey"}}`, encodeURIComponent(String(requestParameters['payKey'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Meant to void the payments carrying the given pay key, stamp them cancelled and cancel the order, but the shared checkout handler resolves its order from an ORDER ID path parameter this route does not carry. The result is an untyped order and a cancel dispatch that refuses with 400 before the pay key lookup ever runs. Token gate, namespacing and store resolution happen first, so a missing token is still 401 and an unloadable store still 500. It is the same handler as the unprefixed cancel address, with the same outcome.
     * PayPal cancel by pay key — refuses, exactly as the unprefixed address does
     */
    async postStoreByStoreidCheckoutPaypalCancelByPaykey(requestParameters: StoreApiPostStoreByStoreidCheckoutPaypalCancelByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutPaypalCancelByPaykeyRaw(requestParameters, initOverrides);
    }

    /**
     * Meant to mark the payments carrying the given pay key as paid and set the order to paid, it cannot reach that work from this address: the shared checkout handler takes its order from an ORDER ID path parameter this route does not carry, so the order is always fresh and untyped and the confirm dispatch refuses with 400 before the pay key is queried. The token gate, the namespace middleware and the store lookup all run ahead of that, so authentication and store failures surface first. Behaviour is identical to the unprefixed confirm address; the checkout prefix changes nothing here.
     * PayPal confirm by pay key — refuses, exactly as the unprefixed address does
     */
    async postStoreByStoreidCheckoutPaypalConfirmByPaykeyRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutPaypalConfirmByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutPaypalConfirmByPaykey().'
            );
        }

        if (requestParameters['payKey'] == null) {
            throw new runtime.RequiredError(
                'payKey',
                'Required parameter "payKey" was null or undefined when calling postStoreByStoreidCheckoutPaypalConfirmByPaykey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/paypal/confirm/{payKey}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"payKey"}}`, encodeURIComponent(String(requestParameters['payKey'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Meant to mark the payments carrying the given pay key as paid and set the order to paid, it cannot reach that work from this address: the shared checkout handler takes its order from an ORDER ID path parameter this route does not carry, so the order is always fresh and untyped and the confirm dispatch refuses with 400 before the pay key is queried. The token gate, the namespace middleware and the store lookup all run ahead of that, so authentication and store failures surface first. Behaviour is identical to the unprefixed confirm address; the checkout prefix changes nothing here.
     * PayPal confirm by pay key — refuses, exactly as the unprefixed address does
     */
    async postStoreByStoreidCheckoutPaypalConfirmByPaykey(requestParameters: StoreApiPostStoreByStoreidCheckoutPaypalConfirmByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutPaypalConfirmByPaykeyRaw(requestParameters, initOverrides);
    }

    /**
     * Begins a PayPal authorization by running the ordinary store authorize flow, since the route binds that exact handler — body, store resolution, tally, reservations and failure behaviour are the authorize address\'s, unchanged. The processor is chosen from the body\'s payment type, so this path reaches PayPal only when that type says so. A successful PayPal authorization stamps a pay key onto the payment, which is the key the confirm and cancel addresses filter on. Build against the plain authorize address instead.
     * Start a PayPal authorization for a new order — the checkout spelling
     */
    async postStoreByStoreidCheckoutPaypalPayRaw(requestParameters: StoreApiPostStoreByStoreidCheckoutPaypalPayRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidCheckoutPaypalPay().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/checkout/paypal/pay`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Begins a PayPal authorization by running the ordinary store authorize flow, since the route binds that exact handler — body, store resolution, tally, reservations and failure behaviour are the authorize address\'s, unchanged. The processor is chosen from the body\'s payment type, so this path reaches PayPal only when that type says so. A successful PayPal authorization stamps a pay key onto the payment, which is the key the confirm and cancel addresses filter on. Build against the plain authorize address instead.
     * Start a PayPal authorization for a new order — the checkout spelling
     */
    async postStoreByStoreidCheckoutPaypalPay(requestParameters: StoreApiPostStoreByStoreidCheckoutPaypalPayRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidCheckoutPaypalPayRaw(requestParameters, initOverrides);
    }

    /**
     * Creates the override and answers the store\'s ENTIRE listing map at 201 with a Location header — not just the entry that was added. A key already present is refused 400: creation never silently overwrites, so changing an existing listing has to be an explicit replace. The stored listing has its currency stamped from the store\'s own, which the replace path does not do. The key is matched exactly here, with none of the slug or SKU fallback the read allows. Admin-gated and resolved inside the caller org\'s namespace.
     * Add a listing override under a new key
     */
    async postStoreByStoreidListingByKeyRaw(requestParameters: StoreApiPostStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidListingByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling postStoreByStoreidListingByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/listing/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates the override and answers the store\'s ENTIRE listing map at 201 with a Location header — not just the entry that was added. A key already present is refused 400: creation never silently overwrites, so changing an existing listing has to be an explicit replace. The stored listing has its currency stamped from the store\'s own, which the replace path does not do. The key is matched exactly here, with none of the slug or SKU fallback the read allows. Admin-gated and resolved inside the caller org\'s namespace.
     * Add a listing override under a new key
     */
    async postStoreByStoreidListingByKey(requestParameters: StoreApiPostStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidListingByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Intended to void the payments carrying the given pay key, stamp them cancelled and cancel the order, it never reaches that work: the shared checkout handler reads its order from an ORDER ID path parameter this route does not carry, leaving an untyped order that the cancel dispatch refuses with 400 before the pay key lookup runs. Authentication, namespacing and store resolution happen ahead of the refusal, so a missing token is 401 and an unloadable store 500. Cancelling a real PayPal authorization needs an address that carries the order id.
     * PayPal cancel by pay key — refuses, because a pay key alone does not identify the order
     */
    async postStoreByStoreidPaypalCancelByPaykeyRaw(requestParameters: StoreApiPostStoreByStoreidPaypalCancelByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidPaypalCancelByPaykey().'
            );
        }

        if (requestParameters['payKey'] == null) {
            throw new runtime.RequiredError(
                'payKey',
                'Required parameter "payKey" was null or undefined when calling postStoreByStoreidPaypalCancelByPaykey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/paypal/cancel/{payKey}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"payKey"}}`, encodeURIComponent(String(requestParameters['payKey'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Intended to void the payments carrying the given pay key, stamp them cancelled and cancel the order, it never reaches that work: the shared checkout handler reads its order from an ORDER ID path parameter this route does not carry, leaving an untyped order that the cancel dispatch refuses with 400 before the pay key lookup runs. Authentication, namespacing and store resolution happen ahead of the refusal, so a missing token is 401 and an unloadable store 500. Cancelling a real PayPal authorization needs an address that carries the order id.
     * PayPal cancel by pay key — refuses, because a pay key alone does not identify the order
     */
    async postStoreByStoreidPaypalCancelByPaykey(requestParameters: StoreApiPostStoreByStoreidPaypalCancelByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidPaypalCancelByPaykeyRaw(requestParameters, initOverrides);
    }

    /**
     * Intended to mark every payment carrying the given pay key as paid and flip the order to paid, it cannot do that from this address and does not pretend to: the shared checkout handler resolves its order from an ORDER ID path parameter that this route does not carry, so it always works against a fresh untyped order and the confirm dispatch refuses it with 400 before the pay key is ever queried. The token gate, the namespace and the store lookup all run ahead of that, so a missing token is still 401 and an unloadable store still 500. Drive a PayPal return through an address that carries the order id.
     * PayPal confirm by pay key — refuses, because a pay key alone does not identify the order
     */
    async postStoreByStoreidPaypalConfirmByPaykeyRaw(requestParameters: StoreApiPostStoreByStoreidPaypalConfirmByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidPaypalConfirmByPaykey().'
            );
        }

        if (requestParameters['payKey'] == null) {
            throw new runtime.RequiredError(
                'payKey',
                'Required parameter "payKey" was null or undefined when calling postStoreByStoreidPaypalConfirmByPaykey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/paypal/confirm/{payKey}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"payKey"}}`, encodeURIComponent(String(requestParameters['payKey'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Intended to mark every payment carrying the given pay key as paid and flip the order to paid, it cannot do that from this address and does not pretend to: the shared checkout handler resolves its order from an ORDER ID path parameter that this route does not carry, so it always works against a fresh untyped order and the confirm dispatch refuses it with 400 before the pay key is ever queried. The token gate, the namespace and the store lookup all run ahead of that, so a missing token is still 401 and an unloadable store still 500. Drive a PayPal return through an address that carries the order id.
     * PayPal confirm by pay key — refuses, because a pay key alone does not identify the order
     */
    async postStoreByStoreidPaypalConfirmByPaykey(requestParameters: StoreApiPostStoreByStoreidPaypalConfirmByPaykeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidPaypalConfirmByPaykeyRaw(requestParameters, initOverrides);
    }

    /**
     * Runs the ordinary store authorize flow — the route binds that very handler, so the body, the store resolution, the tally, the reservations and the failure behaviour are the authorize address\'s, unchanged. It reaches PayPal only when the body\'s payment type says so; nothing about this path forces the processor, so a card-typed payment posted here authorizes on the card processor instead. A successful PayPal authorization stamps a pay key onto the payment, which is the key the confirm and cancel addresses filter on. It is the older entry point; the plain authorize address is the one to build against.
     * Start a PayPal authorization for a new order
     */
    async postStoreByStoreidPaypalPayRaw(requestParameters: StoreApiPostStoreByStoreidPaypalPayRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidPaypalPay().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/paypal/pay`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Runs the ordinary store authorize flow — the route binds that very handler, so the body, the store resolution, the tally, the reservations and the failure behaviour are the authorize address\'s, unchanged. It reaches PayPal only when the body\'s payment type says so; nothing about this path forces the processor, so a card-typed payment posted here authorizes on the card processor instead. A successful PayPal authorization stamps a pay key onto the payment, which is the key the confirm and cancel addresses filter on. It is the older entry point; the plain authorize address is the one to build against.
     * Start a PayPal authorization for a new order
     */
    async postStoreByStoreidPaypalPay(requestParameters: StoreApiPostStoreByStoreidPaypalPayRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidPaypalPayRaw(requestParameters, initOverrides);
    }

    /**
     * Creates a trialing subscription for the addressed store on the entry plan and grants that plan\'s trial credit, answering 201 when this call actually started one and 200 with a reason otherwise — not_new when the store already has billing history, trial_not_configured when no entry plan is wired. The window is always the SEVEN-DAY no-card trial, because this address never presents a card; the longer card-present window is reached only by adding a card afterwards. Entitlement is per store while the billing subject is the org, so every store an org owns takes its own trial. Admin-gated and namespaced to the caller\'s org: no resolvable store is 404 with store_required, and a backing-store failure is 503.
     * Start this store\'s no-card trial on the entry plan
     */
    async postStoreByStoreidTrialRaw(requestParameters: StoreApiPostStoreByStoreidTrialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling postStoreByStoreidTrial().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/trial`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates a trialing subscription for the addressed store on the entry plan and grants that plan\'s trial credit, answering 201 when this call actually started one and 200 with a reason otherwise — not_new when the store already has billing history, trial_not_configured when no entry plan is wired. The window is always the SEVEN-DAY no-card trial, because this address never presents a card; the longer card-present window is reached only by adding a card afterwards. Entitlement is per store while the billing subject is the org, so every store an org owns takes its own trial. Admin-gated and namespaced to the caller\'s org: no resolvable store is 404 with store_required, and a backing-store failure is 503.
     * Start this store\'s no-card trial on the entry plan
     */
    async postStoreByStoreidTrial(requestParameters: StoreApiPostStoreByStoreidTrialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreByStoreidTrialRaw(requestParameters, initOverrides);
    }

    /**
     * Answers a freshly minted token carrying ONLY the published-read permission — enough for a logged-out shopper\'s storefront to read your published catalog and nothing more, with no write and no admin scope. It is org-bound, signed with the org\'s own secret and subject to the org id, so unlike a shared service token it can never act on another tenant. Minting ROTATES rather than accumulates: the previous storefront token is dropped first and is invalid immediately, so re-minting is how you revoke. Admin is enforced by the handler as well as the route, because the route\'s token gate does not apply on the identity path and a plain member must not be able to mint their org\'s key.
     * Mint your org\'s least-privilege storefront read key
     */
    async postStoreTokenRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/token`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers a freshly minted token carrying ONLY the published-read permission — enough for a logged-out shopper\'s storefront to read your published catalog and nothing more, with no write and no admin scope. It is org-bound, signed with the org\'s own secret and subject to the org id, so unlike a shared service token it can never act on another tenant. Minting ROTATES rather than accumulates: the previous storefront token is dropped first and is invalid immediately, so re-minting is how you revoke. Admin is enforced by the handler as well as the route, because the route\'s token gate does not apply on the identity path and a plain member must not be able to mint their org\'s key.
     * Mint your org\'s least-privilege storefront read key
     */
    async postStoreToken(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postStoreTokenRaw(initOverrides);
    }

    /**
     * This is a true REPLACEMENT, not a merge: the stored key is preserved but the body is decoded onto a fresh entity, so every field the body omits is written back as its zero value. Use the partial update when you mean to change part of a store. The id is resolved inside the caller org\'s own namespace, so an unknown or foreign id is a 404 before anything is written. Requires an admin token, or one holding both store read and store write.
     * Replace a storefront outright
     */
    async putStoreByStoreidRaw(requestParameters: StoreApiPutStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling putStoreByStoreid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * This is a true REPLACEMENT, not a merge: the stored key is preserved but the body is decoded onto a fresh entity, so every field the body omits is written back as its zero value. Use the partial update when you mean to change part of a store. The id is resolved inside the caller org\'s own namespace, so an unknown or foreign id is a 404 before anything is written. Requires an admin token, or one holding both store read and store write.
     * Replace a storefront outright
     */
    async putStoreByStoreid(requestParameters: StoreApiPutStoreByStoreidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.putStoreByStoreidRaw(requestParameters, initOverrides);
    }

    /**
     * Decodes the body over the existing listing when the key is present, so fields it omits keep their stored values, and builds the listing from the body alone when the key is new. Answers 200 when it replaced something and 201 with a Location header when it created it; either way the body is the store\'s entire listing map, not the single entry. Unlike creation, this path does NOT restamp the listing\'s currency from the store. Admin-gated, with the store resolved inside the caller org\'s namespace.
     * Upsert a listing override
     */
    async putStoreByStoreidListingByKeyRaw(requestParameters: StoreApiPutStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['storeid'] == null) {
            throw new runtime.RequiredError(
                'storeid',
                'Required parameter "storeid" was null or undefined when calling putStoreByStoreidListingByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling putStoreByStoreidListingByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/store/{storeid}/listing/{key}`;
        urlPath = urlPath.replace(`{${"storeid"}}`, encodeURIComponent(String(requestParameters['storeid'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Decodes the body over the existing listing when the key is present, so fields it omits keep their stored values, and builds the listing from the body alone when the key is new. Answers 200 when it replaced something and 201 with a Location header when it created it; either way the body is the store\'s entire listing map, not the single entry. Unlike creation, this path does NOT restamp the listing\'s currency from the store. Admin-gated, with the store resolved inside the caller org\'s namespace.
     * Upsert a listing override
     */
    async putStoreByStoreidListingByKey(requestParameters: StoreApiPutStoreByStoreidListingByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.putStoreByStoreidListingByKeyRaw(requestParameters, initOverrides);
    }

}
