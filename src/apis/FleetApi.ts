/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  FleetBoard,
  JobCancel,
  JobCanceled,
  JobList,
  SampleAccepted,
  SampleIngest,
  SampleList,
  WorkerList,
} from '../models/index.js';
import {
    FleetBoardFromJSON,
    FleetBoardToJSON,
    JobCancelFromJSON,
    JobCancelToJSON,
    JobCanceledFromJSON,
    JobCanceledToJSON,
    JobListFromJSON,
    JobListToJSON,
    SampleAcceptedFromJSON,
    SampleAcceptedToJSON,
    SampleIngestFromJSON,
    SampleIngestToJSON,
    SampleListFromJSON,
    SampleListToJSON,
    WorkerListFromJSON,
    WorkerListToJSON,
} from '../models/index.js';

export interface FleetApiCancelFleetJobRequest {
    id: string;
    jobCancel: JobCancel;
}

export interface FleetApiListFleetJobsRequest {
    gpu?: string;
    status?: string;
}

export interface FleetApiListFleetSamplesRequest {
    unit?: string;
    source?: string;
    range?: string;
}

export interface FleetApiRecordFleetSampleRequest {
    sampleIngest: SampleIngest;
}

/**
 * 
 */
export class FleetApi extends runtime.BaseAPI {

    /**
     * Cancels a queued or running render in the caller\'s org. The engine cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in another tenant\'s shard is 404, exactly like one that never existed. An already-finished job is 409.
     * Cancels a queued or running render in the caller\'s org.
     */
    async cancelFleetJobRaw(requestParameters: FleetApiCancelFleetJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<JobCanceled>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling cancelFleetJob().'
            );
        }

        if (requestParameters['jobCancel'] == null) {
            throw new runtime.RequiredError(
                'jobCancel',
                'Required parameter "jobCancel" was null or undefined when calling cancelFleetJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/fleet/jobs/{id}/cancel`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: JobCancelToJSON(requestParameters['jobCancel']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => JobCanceledFromJSON(jsonValue));
    }

    /**
     * Cancels a queued or running render in the caller\'s org. The engine cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in another tenant\'s shard is 404, exactly like one that never existed. An already-finished job is 409.
     * Cancels a queued or running render in the caller\'s org.
     */
    async cancelFleetJob(requestParameters: FleetApiCancelFleetJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<JobCanceled> {
        const response = await this.cancelFleetJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.  A unit with a live snapshot of its own keeps it; the rest are overlaid from the utilization series, and only when the sample agrees about the SOURCE — two planes could mint the same unit id, and a board must never show one machine\'s load on another\'s row. BYO GPU units also carry their gpu-jobs queue depth. Every source is folded in independently: a broken one costs its own rows and nothing else.
     * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.
     */
    async listFleetRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/fleet`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetBoardFromJSON(jsonValue));
    }

    /**
     * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.  A unit with a live snapshot of its own keeps it; the rest are overlaid from the utilization series, and only when the sample agrees about the SOURCE — two planes could mint the same unit id, and a board must never show one machine\'s load on another\'s row. BYO GPU units also carry their gpu-jobs queue depth. Every source is folded in independently: a broken one costs its own rows and nothing else.
     * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.
     */
    async listFleet(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetBoard> {
        const response = await this.listFleetRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.  A job whose worker died — STARTED with an elapsed lease and not yet reclaimed — reads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an empty queue rather than an error.
     * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.
     */
    async listFleetJobsRaw(requestParameters: FleetApiListFleetJobsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<JobList>> {
        const queryParameters: any = {};

        if (requestParameters['gpu'] != null) {
            queryParameters['gpu'] = requestParameters['gpu'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/fleet/jobs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => JobListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.  A job whose worker died — STARTED with an elapsed lease and not yet reclaimed — reads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an empty queue rather than an error.
     * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.
     */
    async listFleetJobs(requestParameters: FleetApiListFleetJobsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<JobList> {
        const response = await this.listFleetJobsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s utilization series, oldest first.  A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and safe to echo); a warehouse failure is logged and answered 503 \"unavailable\", because a chart that silently reads \"no load\" when the truth is \"we cannot tell\" is worse than one that says so. An ABSENT warehouse is different again: it returns an empty series, which renders honestly as \"no samples yet\".
     * Returns the caller org\'s utilization series, oldest first.
     */
    async listFleetSamplesRaw(requestParameters: FleetApiListFleetSamplesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SampleList>> {
        const queryParameters: any = {};

        if (requestParameters['unit'] != null) {
            queryParameters['unit'] = requestParameters['unit'];
        }

        if (requestParameters['source'] != null) {
            queryParameters['source'] = requestParameters['source'];
        }

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/fleet/samples`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SampleListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s utilization series, oldest first.  A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and safe to echo); a warehouse failure is logged and answered 503 \"unavailable\", because a chart that silently reads \"no load\" when the truth is \"we cannot tell\" is worse than one that says so. An ABSENT warehouse is different again: it returns an empty series, which renders honestly as \"no samples yet\".
     * Returns the caller org\'s utilization series, oldest first.
     */
    async listFleetSamples(requestParameters: FleetApiListFleetSamplesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SampleList> {
        const response = await this.listFleetSamplesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself. The Machines and GPUs pages fold the same data into their normalized shapes; this is the canonical raw list a fleet view (or the CLI\'s `status`) reads.
     * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself.
     */
    async listFleetWorkersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkerList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/fleet/workers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkerListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself. The Machines and GPUs pages fold the same data into their normalized shapes; this is the canonical raw list a fleet view (or the CLI\'s `status`) reads.
     * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself.
     */
    async listFleetWorkers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkerList> {
        const response = await this.listFleetWorkersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays. The org is the validated principal and source/kind are fixed server-side, so a worker names only its own metrics — never another tenant or another source. Answers 202: the warehouse write is DETACHED (its own bounded context, never in the response path), so a slow or absent warehouse cannot stall a heartbeat.
     * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays.
     */
    async recordFleetSampleRaw(requestParameters: FleetApiRecordFleetSampleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SampleAccepted>> {
        if (requestParameters['sampleIngest'] == null) {
            throw new runtime.RequiredError(
                'sampleIngest',
                'Required parameter "sampleIngest" was null or undefined when calling recordFleetSample().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/fleet/samples`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SampleIngestToJSON(requestParameters['sampleIngest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SampleAcceptedFromJSON(jsonValue));
    }

    /**
     * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays. The org is the validated principal and source/kind are fixed server-side, so a worker names only its own metrics — never another tenant or another source. Answers 202: the warehouse write is DETACHED (its own bounded context, never in the response path), so a slow or absent warehouse cannot stall a heartbeat.
     * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays.
     */
    async recordFleetSample(requestParameters: FleetApiRecordFleetSampleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SampleAccepted> {
        const response = await this.recordFleetSampleRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
