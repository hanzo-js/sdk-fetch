/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AccountFoldView,
  CloudAccountsView,
  ProvidersView,
  UnlinkedView,
  VenueLinkRequest,
} from '../models/index.js';
import {
    AccountFoldViewFromJSON,
    AccountFoldViewToJSON,
    CloudAccountsViewFromJSON,
    CloudAccountsViewToJSON,
    ProvidersViewFromJSON,
    ProvidersViewToJSON,
    UnlinkedViewFromJSON,
    UnlinkedViewToJSON,
    VenueLinkRequestFromJSON,
    VenueLinkRequestToJSON,
} from '../models/index.js';

export interface CloudApiDeleteCloudByProviderAccountsByLabelRequest {
    provider: string;
    label: string;
}

export interface CloudApiPostCloudByProviderAccountsRequest {
    provider: string;
    venueLinkRequest: VenueLinkRequest;
}

export interface CloudApiPostCloudByProviderAccountsByLabelSyncRequest {
    provider: string;
    label: string;
}

/**
 * 
 */
export class CloudApi extends runtime.BaseAPI {

    /**
     * Forgets one linked cloud account: it detaches every fleet cluster THIS account folded (its own names, in its own shard — a neighbour\'s cluster of the same name is untouched), deletes the sealed credential, and drops the index row.  It is idempotent and deliberately not an existence oracle: an account this org does not hold answers exactly the same as one it just removed. A cluster that fails to detach is logged and the unlink continues, so a dead provider cannot strand a credential. Requires org admin.
     * Forgets one linked cloud account: it detaches every fleet cluster THIS account folded (its own names, in its own shard — a neighbour\'s cluster of the same name is untouched), deletes the sealed credential, and drops the index row.
     */
    async deleteCloudByProviderAccountsByLabelRaw(requestParameters: CloudApiDeleteCloudByProviderAccountsByLabelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UnlinkedView>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling deleteCloudByProviderAccountsByLabel().'
            );
        }

        if (requestParameters['label'] == null) {
            throw new runtime.RequiredError(
                'label',
                'Required parameter "label" was null or undefined when calling deleteCloudByProviderAccountsByLabel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloud/{provider}/accounts/{label}`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));
        urlPath = urlPath.replace(`{${"label"}}`, encodeURIComponent(String(requestParameters['label'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UnlinkedViewFromJSON(jsonValue));
    }

    /**
     * Forgets one linked cloud account: it detaches every fleet cluster THIS account folded (its own names, in its own shard — a neighbour\'s cluster of the same name is untouched), deletes the sealed credential, and drops the index row.  It is idempotent and deliberately not an existence oracle: an account this org does not hold answers exactly the same as one it just removed. A cluster that fails to detach is logged and the unlink continues, so a dead provider cannot strand a credential. Requires org admin.
     * Forgets one linked cloud account: it detaches every fleet cluster THIS account folded (its own names, in its own shard — a neighbour\'s cluster of the same name is untouched), deletes the sealed credential, and drops the index row.
     */
    async deleteCloudByProviderAccountsByLabel(requestParameters: CloudApiDeleteCloudByProviderAccountsByLabelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UnlinkedView> {
        const response = await this.deleteCloudByProviderAccountsByLabelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the clouds this deployment can link and what linking each one needs — the DigitalOcean token, the AWS role and external id, the GCP credential JSON, the Azure app — plus whether the provider can be linked without storing any long-lived secret. It is the catalog a \"connect a cloud\" screen renders; it reports no account and no credential.
     * Returns the clouds this deployment can link and what linking each one needs — the DigitalOcean token, the AWS role and external id, the GCP credential JSON, the Azure app — plus whether the provider can be linked without storing any long-lived secret.
     */
    async getCloudRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvidersView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloud`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvidersViewFromJSON(jsonValue));
    }

    /**
     * Returns the clouds this deployment can link and what linking each one needs — the DigitalOcean token, the AWS role and external id, the GCP credential JSON, the Azure app — plus whether the provider can be linked without storing any long-lived secret. It is the catalog a \"connect a cloud\" screen renders; it reports no account and no credential.
     * Returns the clouds this deployment can link and what linking each one needs — the DigitalOcean token, the AWS role and external id, the GCP credential JSON, the Azure app — plus whether the provider can be linked without storing any long-lived secret.
     */
    async getCloud(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvidersView> {
        const response = await this.getCloudRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller org\'s linked cloud accounts across every provider: which account each one is at the provider, which fleet clusters it folded, and when it was last discovered. Metadata only — a sealed credential never appears in a response. Another org\'s accounts are not visible and not countable.
     * Lists the caller org\'s linked cloud accounts across every provider: which account each one is at the provider, which fleet clusters it folded, and when it was last discovered.
     */
    async getCloudAccountsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CloudAccountsView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloud/accounts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CloudAccountsViewFromJSON(jsonValue));
    }

    /**
     * Lists the caller org\'s linked cloud accounts across every provider: which account each one is at the provider, which fleet clusters it folded, and when it was last discovered. Metadata only — a sealed credential never appears in a response. Another org\'s accounts are not visible and not countable.
     * Lists the caller org\'s linked cloud accounts across every provider: which account each one is at the provider, which fleet clusters it folded, and when it was last discovered.
     */
    async getCloudAccounts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CloudAccountsView> {
        const response = await this.getCloudAccountsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Links one of the caller org\'s cloud accounts and folds the Kubernetes clusters it finds there into the ONE Hanzo fleet, so they appear at /v1/clusters and can run work like any managed or bring-your-own cluster. Answers 201.  The credential is verified LIVE against the provider BEFORE anything is stored, so a bad one is refused and nothing is written; it is then sealed in the org\'s own KMS namespace and never appears in a response, the account index, or a log line. Discovery follows, and a cluster that fails to fold is reported as DATA in the clusters list rather than failing the link.  Re-linking a label that already exists re-seals its credential and re-folds it, so this is how a rotated token is replaced. Requires org admin.
     * Links one of the caller org\'s cloud accounts and folds the Kubernetes clusters it finds there into the ONE Hanzo fleet, so they appear at /v1/clusters and can run work like any managed or bring-your-own cluster.
     */
    async postCloudByProviderAccountsRaw(requestParameters: CloudApiPostCloudByProviderAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountFoldView>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling postCloudByProviderAccounts().'
            );
        }

        if (requestParameters['venueLinkRequest'] == null) {
            throw new runtime.RequiredError(
                'venueLinkRequest',
                'Required parameter "venueLinkRequest" was null or undefined when calling postCloudByProviderAccounts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloud/{provider}/accounts`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VenueLinkRequestToJSON(requestParameters['venueLinkRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountFoldViewFromJSON(jsonValue));
    }

    /**
     * Links one of the caller org\'s cloud accounts and folds the Kubernetes clusters it finds there into the ONE Hanzo fleet, so they appear at /v1/clusters and can run work like any managed or bring-your-own cluster. Answers 201.  The credential is verified LIVE against the provider BEFORE anything is stored, so a bad one is refused and nothing is written; it is then sealed in the org\'s own KMS namespace and never appears in a response, the account index, or a log line. Discovery follows, and a cluster that fails to fold is reported as DATA in the clusters list rather than failing the link.  Re-linking a label that already exists re-seals its credential and re-folds it, so this is how a rotated token is replaced. Requires org admin.
     * Links one of the caller org\'s cloud accounts and folds the Kubernetes clusters it finds there into the ONE Hanzo fleet, so they appear at /v1/clusters and can run work like any managed or bring-your-own cluster.
     */
    async postCloudByProviderAccounts(requestParameters: CloudApiPostCloudByProviderAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountFoldView> {
        const response = await this.postCloudByProviderAccountsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Re-discovers one already-linked cloud account and reconciles what it folded: kubeconfigs are refreshed, clusters that appeared since the last sync are folded, and clusters this account folded that the provider no longer returns are detached — only this account\'s own, in the fleet shard it was linked into.  It is idempotent, it reads the credential already sealed at link time, and a discovery failure leaves the existing fold set alone rather than mass-detaching it. An account this org has not linked is not found. Requires org admin.
     * Re-discovers one already-linked cloud account and reconciles what it folded: kubeconfigs are refreshed, clusters that appeared since the last sync are folded, and clusters this account folded that the provider no longer returns are detached — only this account\'s own, in the fleet shard it was linked into.
     */
    async postCloudByProviderAccountsByLabelSyncRaw(requestParameters: CloudApiPostCloudByProviderAccountsByLabelSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountFoldView>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling postCloudByProviderAccountsByLabelSync().'
            );
        }

        if (requestParameters['label'] == null) {
            throw new runtime.RequiredError(
                'label',
                'Required parameter "label" was null or undefined when calling postCloudByProviderAccountsByLabelSync().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/cloud/{provider}/accounts/{label}/sync`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));
        urlPath = urlPath.replace(`{${"label"}}`, encodeURIComponent(String(requestParameters['label'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountFoldViewFromJSON(jsonValue));
    }

    /**
     * Re-discovers one already-linked cloud account and reconciles what it folded: kubeconfigs are refreshed, clusters that appeared since the last sync are folded, and clusters this account folded that the provider no longer returns are detached — only this account\'s own, in the fleet shard it was linked into.  It is idempotent, it reads the credential already sealed at link time, and a discovery failure leaves the existing fold set alone rather than mass-detaching it. An account this org has not linked is not found. Requires org admin.
     * Re-discovers one already-linked cloud account and reconciles what it folded: kubeconfigs are refreshed, clusters that appeared since the last sync are folded, and clusters this account folded that the provider no longer returns are detached — only this account\'s own, in the fleet shard it was linked into.
     */
    async postCloudByProviderAccountsByLabelSync(requestParameters: CloudApiPostCloudByProviderAccountsByLabelSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountFoldView> {
        const response = await this.postCloudByProviderAccountsByLabelSyncRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
