/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EnablementBoard,
  EnablementOptRef,
  PricingHealth,
  PricingModelList,
  PricingPlanList,
  PricingPresetList,
  PricingProviderList,
  PricingRegionList,
  PricingSyncOut,
  PricingTierList,
  PricingToolList,
  UserEnablementItem,
} from '../models/index.js';
import {
    EnablementBoardFromJSON,
    EnablementBoardToJSON,
    EnablementOptRefFromJSON,
    EnablementOptRefToJSON,
    PricingHealthFromJSON,
    PricingHealthToJSON,
    PricingModelListFromJSON,
    PricingModelListToJSON,
    PricingPlanListFromJSON,
    PricingPlanListToJSON,
    PricingPresetListFromJSON,
    PricingPresetListToJSON,
    PricingProviderListFromJSON,
    PricingProviderListToJSON,
    PricingRegionListFromJSON,
    PricingRegionListToJSON,
    PricingSyncOutFromJSON,
    PricingSyncOutToJSON,
    PricingTierListFromJSON,
    PricingTierListToJSON,
    PricingToolListFromJSON,
    PricingToolListToJSON,
    UserEnablementItemFromJSON,
    UserEnablementItemToJSON,
} from '../models/index.js';

export interface PricingApiGetPricingModelByNameRequest {
    name: string;
}

export interface PricingApiPostPricingEnablementOptinRequest {
    enablementOptRef: EnablementOptRef;
}

export interface PricingApiPostPricingEnablementOptoutRequest {
    enablementOptRef: EnablementOptRef;
}

/**
 * 
 */
export class PricingApi extends runtime.BaseAPI {

    /**
     * Returns the whole pricing catalog in one document: Zen and third-party models, providers, model families, the free-model list, plan and infrastructure pricing. Every model and provider it names is filtered to what the caller\'s org may see — the same gate the leaf routes apply, so this can never be an un-gated second source for what they hide.
     * Returns the whole pricing catalog in one document: Zen and third-party models, providers, model families, the free-model list, plan and infrastructure pricing.
     */
    async getPricingRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the whole pricing catalog in one document: Zen and third-party models, providers, model families, the free-model list, plan and infrastructure pricing. Every model and provider it names is filtered to what the caller\'s org may see — the same gate the leaf routes apply, so this can never be an un-gated second source for what they hide.
     * Returns the whole pricing catalog in one document: Zen and third-party models, providers, model families, the free-model list, plan and infrastructure pricing.
     */
    async getPricing(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the Hanzo Base plans — the managed-instance tiers, each with its monthly and annual price, storage and request allowances and feature list.
     * Returns the Hanzo Base plans — the managed-instance tiers, each with its monthly and annual price, storage and request allowances and feature list.
     */
    async getPricingBaseRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPlanList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/base`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPlanListFromJSON(jsonValue));
    }

    /**
     * Returns the Hanzo Base plans — the managed-instance tiers, each with its monthly and annual price, storage and request allowances and feature list.
     * Returns the Hanzo Base plans — the managed-instance tiers, each with its monthly and annual price, storage and request allowances and feature list.
     */
    async getPricingBase(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPlanList> {
        const response = await this.getPricingBaseRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the blockchain access plans — the RPC and node tiers, each with its monthly price, compute-unit allowance and feature list.
     * Returns the blockchain access plans — the RPC and node tiers, each with its monthly price, compute-unit allowance and feature list.
     */
    async getPricingBlockchainRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPlanList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/blockchain`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPlanListFromJSON(jsonValue));
    }

    /**
     * Returns the blockchain access plans — the RPC and node tiers, each with its monthly price, compute-unit allowance and feature list.
     * Returns the blockchain access plans — the RPC and node tiers, each with its monthly price, compute-unit allowance and feature list.
     */
    async getPricingBlockchain(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPlanList> {
        const response = await this.getPricingBlockchainRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the public cloud section of the catalog in one document: its instance plans, its regions and its block-storage prices. The section\'s internal half — the provider costs Hanzo pays and the plan-to- provider routing table — is stripped before it is served, so this is what a customer may see and nothing more.
     * Returns the public cloud section of the catalog in one document: its instance plans, its regions and its block-storage prices.
     */
    async getPricingCloudRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/cloud`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the public cloud section of the catalog in one document: its instance plans, its regions and its block-storage prices. The section\'s internal half — the provider costs Hanzo pays and the plan-to- provider routing table — is stripped before it is served, so this is what a customer may see and nothing more.
     * Returns the public cloud section of the catalog in one document: its instance plans, its regions and its block-storage prices.
     */
    async getPricingCloud(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingCloudRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns just the cloud instance plans — each with its vCPU, memory, disk, CPU type, VM allowance, feature list and monthly and hourly price. It is the plans of the cloud section on their own.
     * Returns just the cloud instance plans — each with its vCPU, memory, disk, CPU type, VM allowance, feature list and monthly and hourly price.
     */
    async getPricingCloudPlansRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPlanList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/cloud/plans`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPlanListFromJSON(jsonValue));
    }

    /**
     * Returns just the cloud instance plans — each with its vCPU, memory, disk, CPU type, VM allowance, feature list and monthly and hourly price. It is the plans of the cloud section on their own.
     * Returns just the cloud instance plans — each with its vCPU, memory, disk, CPU type, VM allowance, feature list and monthly and hourly price.
     */
    async getPricingCloudPlans(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPlanList> {
        const response = await this.getPricingCloudPlansRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the regions a cloud instance can be placed in, each with its id, display name and physical location. It is the regions of the cloud section on their own.
     * Returns the regions a cloud instance can be placed in, each with its id, display name and physical location.
     */
    async getPricingCloudRegionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingRegionList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/cloud/regions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingRegionListFromJSON(jsonValue));
    }

    /**
     * Returns the regions a cloud instance can be placed in, each with its id, display name and physical location. It is the regions of the cloud section on their own.
     * Returns the regions a cloud instance can be placed in, each with its id, display name and physical location.
     */
    async getPricingCloudRegions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingRegionList> {
        const response = await this.getPricingCloudRegionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the block-storage prices of the cloud section: the per-GB monthly rate and the volume size bounds a caller may ask for.
     * Returns the block-storage prices of the cloud section: the per-GB monthly rate and the volume size bounds a caller may ask for.
     */
    async getPricingCloudStorageRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/cloud/storage`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the block-storage prices of the cloud section: the per-GB monthly rate and the volume size bounds a caller may ask for.
     * Returns the block-storage prices of the cloud section: the per-GB monthly rate and the volume size bounds a caller may ask for.
     */
    async getPricingCloudStorage(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingCloudStorageRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the compute section of the catalog: the cloud provider and region the prices are quoted for, the monthly markup applied to them, the full instance-size tier list and the named presets. It is the whole section as the pricing source records it, un-gated — no model or provider identity appears in it.
     * Returns the compute section of the catalog: the cloud provider and region the prices are quoted for, the monthly markup applied to them, the full instance-size tier list and the named presets.
     */
    async getPricingComputeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/compute`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the compute section of the catalog: the cloud provider and region the prices are quoted for, the monthly markup applied to them, the full instance-size tier list and the named presets. It is the whole section as the pricing source records it, un-gated — no model or provider identity appears in it.
     * Returns the compute section of the catalog: the cloud provider and region the prices are quoted for, the monthly markup applied to them, the full instance-size tier list and the named presets.
     */
    async getPricingCompute(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingComputeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns just the named compute sizes — the short, human-labelled list (\"Starter\", \"Pro\") a size picker renders, each carrying its provider slug, vCPU, memory, disk and price. It is the presets of the compute section on their own, for a caller that does not need the full tier table.
     * Returns just the named compute sizes — the short, human-labelled list (\"Starter\", \"Pro\") a size picker renders, each carrying its provider slug, vCPU, memory, disk and price.
     */
    async getPricingComputePresetsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPresetList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/compute/presets`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPresetListFromJSON(jsonValue));
    }

    /**
     * Returns just the named compute sizes — the short, human-labelled list (\"Starter\", \"Pro\") a size picker renders, each carrying its provider slug, vCPU, memory, disk and price. It is the presets of the compute section on their own, for a caller that does not need the full tier table.
     * Returns just the named compute sizes — the short, human-labelled list (\"Starter\", \"Pro\") a size picker renders, each carrying its provider slug, vCPU, memory, disk and price.
     */
    async getPricingComputePresets(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPresetList> {
        const response = await this.getPricingComputePresetsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the Hanzo Datastore rate card: the tier list, the per-GB storage and egress usage rates, the annual discount and the trial. It is the section as authored, un-gated — no provider identity appears in it.  The route was missing while the data existed, so this 404d and every visitor to hanzo.ai\'s Infrastructure tab was told pricing was \"temporarily unavailable\".
     * Returns the Hanzo Datastore rate card: the tier list, the per-GB storage and egress usage rates, the annual discount and the trial.
     */
    async getPricingDatastoreRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/datastore`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the Hanzo Datastore rate card: the tier list, the per-GB storage and egress usage rates, the annual discount and the trial. It is the section as authored, un-gated — no provider identity appears in it.  The route was missing while the data existed, so this 404d and every visitor to hanzo.ai\'s Infrastructure tab was told pricing was \"temporarily unavailable\".
     * Returns the Hanzo Datastore rate card: the tier list, the per-GB storage and egress usage rates, the annual discount and the trial.
     */
    async getPricingDatastore(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingDatastoreRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in. Read-only and safe for any caller — one without a validated principal simply sees the generally-available items and no opt-in affordance, never another org\'s state.
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in.
     */
    async getPricingEnablementRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EnablementBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/enablement`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EnablementBoardFromJSON(jsonValue));
    }

    /**
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in. Read-only and safe for any caller — one without a validated principal simply sees the generally-available items and no opt-in affordance, never another org\'s state.
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in.
     */
    async getPricingEnablement(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EnablementBoard> {
        const response = await this.getPricingEnablementRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the models the catalog highlights, filtered to what the caller\'s org may see. It is the same catalog as ListModels narrowed to entries the pricing source marks featured.
     * Returns the models the catalog highlights, filtered to what the caller\'s org may see.
     */
    async getPricingFeaturedRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingModelList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/featured`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingModelListFromJSON(jsonValue));
    }

    /**
     * Returns the models the catalog highlights, filtered to what the caller\'s org may see. It is the same catalog as ListModels narrowed to entries the pricing source marks featured.
     * Returns the models the catalog highlights, filtered to what the caller\'s org may see.
     */
    async getPricingFeatured(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingModelList> {
        const response = await this.getPricingFeaturedRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the models that cost nothing to call, filtered to what the caller\'s org may see. It is the same catalog as ListModels narrowed to entries the pricing source marks free.
     * Returns the models that cost nothing to call, filtered to what the caller\'s org may see.
     */
    async getPricingFreeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingModelList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/free`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingModelListFromJSON(jsonValue));
    }

    /**
     * Returns the models that cost nothing to call, filtered to what the caller\'s org may see. It is the same catalog as ListModels narrowed to entries the pricing source marks free.
     * Returns the models that cost nothing to call, filtered to what the caller\'s org may see.
     */
    async getPricingFree(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingModelList> {
        const response = await this.getPricingFreeRaw(initOverrides);
        return await response.value();
    }

    /**
     * ListGPUTiers returns the rentable GPU configurations, each with its accelerator count and model, VRAM, vCPU, host memory and hourly price.
     * ListGPUTiers returns the rentable GPU configurations, each with its accelerator count and model, VRAM, vCPU, host memory and hourly price.
     */
    async getPricingGpuRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingTierList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/gpu`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingTierListFromJSON(jsonValue));
    }

    /**
     * ListGPUTiers returns the rentable GPU configurations, each with its accelerator count and model, VRAM, vCPU, host memory and hourly price.
     * ListGPUTiers returns the rentable GPU configurations, each with its accelerator count and model, VRAM, vCPU, host memory and hourly price.
     */
    async getPricingGpu(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingTierList> {
        const response = await this.getPricingGpuRaw(initOverrides);
        return await response.value();
    }

    /**
     * Health reports that the pricing subsystem is mounted and serving. It answers from the process itself and consults neither the catalog bundle nor the enablement store, so it stays \"ok\" while either is degraded.
     * Health reports that the pricing subsystem is mounted and serving.
     */
    async getPricingHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingHealth>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingHealthFromJSON(jsonValue));
    }

    /**
     * Health reports that the pricing subsystem is mounted and serving. It answers from the process itself and consults neither the catalog bundle nor the enablement store, so it stays \"ok\" while either is degraded.
     * Health reports that the pricing subsystem is mounted and serving.
     */
    async getPricingHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingHealth> {
        const response = await this.getPricingHealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * ListIAMPlans returns the identity plans — the Hanzo IAM tiers, each with its monthly and annual price, monthly-active-user allowance and feature list.
     * ListIAMPlans returns the identity plans — the Hanzo IAM tiers, each with its monthly and annual price, monthly-active-user allowance and feature list.
     */
    async getPricingIamRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPlanList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/iam`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPlanListFromJSON(jsonValue));
    }

    /**
     * ListIAMPlans returns the identity plans — the Hanzo IAM tiers, each with its monthly and annual price, monthly-active-user allowance and feature list.
     * ListIAMPlans returns the identity plans — the Hanzo IAM tiers, each with its monthly and annual price, monthly-active-user allowance and feature list.
     */
    async getPricingIam(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPlanList> {
        const response = await this.getPricingIamRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one model\'s catalog entry — its pricing, context window and capabilities as the pricing source records them. A model hidden for the caller\'s org answers the same 404 an unknown name does, so a disabled model gets no existence oracle.
     * Returns one model\'s catalog entry — its pricing, context window and capabilities as the pricing source records them.
     */
    async getPricingModelByNameRaw(requestParameters: PricingApiGetPricingModelByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getPricingModelByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/model/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns one model\'s catalog entry — its pricing, context window and capabilities as the pricing source records them. A model hidden for the caller\'s org answers the same 404 an unknown name does, so a disabled model gets no existence oracle.
     * Returns one model\'s catalog entry — its pricing, context window and capabilities as the pricing source records them.
     */
    async getPricingModelByName(requestParameters: PricingApiGetPricingModelByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingModelByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the whole model catalog — every model the gateway serves, Zen and third-party alike — filtered to what the caller\'s org may see. A model an admin has disabled is absent; one in beta appears only for an org granted it. A SuperAdmin sees every model, each annotated with its enablement state.
     * Returns the whole model catalog — every model the gateway serves, Zen and third-party alike — filtered to what the caller\'s org may see.
     */
    async getPricingModelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingModelList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/models`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingModelListFromJSON(jsonValue));
    }

    /**
     * Returns the whole model catalog — every model the gateway serves, Zen and third-party alike — filtered to what the caller\'s org may see. A model an admin has disabled is absent; one in beta appears only for an org granted it. A SuperAdmin sees every model, each annotated with its enablement state.
     * Returns the whole model catalog — every model the gateway serves, Zen and third-party alike — filtered to what the caller\'s org may see.
     */
    async getPricingModels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingModelList> {
        const response = await this.getPricingModelsRaw(initOverrides);
        return await response.value();
    }

    /**
     * ListPaaSPlans returns the application-hosting plans — the deploy-and-host tiers, each with its monthly and annual price, app and memory allowances and feature list.
     * ListPaaSPlans returns the application-hosting plans — the deploy-and-host tiers, each with its monthly and annual price, app and memory allowances and feature list.
     */
    async getPricingPaasRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPlanList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/paas`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPlanListFromJSON(jsonValue));
    }

    /**
     * ListPaaSPlans returns the application-hosting plans — the deploy-and-host tiers, each with its monthly and annual price, app and memory allowances and feature list.
     * ListPaaSPlans returns the application-hosting plans — the deploy-and-host tiers, each with its monthly and annual price, app and memory allowances and feature list.
     */
    async getPricingPaas(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPlanList> {
        const response = await this.getPricingPaasRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the pricing policy document: the revenue-sharing terms (the idle-resale share and the open-source share, each with its percentage and who is eligible) and the commitments Hanzo makes about how it bills — no hidden fees, no egress charges, no surprise bills.
     * Returns the pricing policy document: the revenue-sharing terms (the idle-resale share and the open-source share, each with its percentage and who is eligible) and the commitments Hanzo makes about how it bills — no hidden fees, no egress charges, no surprise bills.
     */
    async getPricingPolicyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/policy`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the pricing policy document: the revenue-sharing terms (the idle-resale share and the open-source share, each with its percentage and who is eligible) and the commitments Hanzo makes about how it bills — no hidden fees, no egress charges, no surprise bills.
     * Returns the pricing policy document: the revenue-sharing terms (the idle-resale share and the open-source share, each with its percentage and who is eligible) and the commitments Hanzo makes about how it bills — no hidden fees, no egress charges, no surprise bills.
     */
    async getPricingPolicy(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingPolicyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the model providers the catalog knows, each with its info object, filtered to what the caller\'s org may see. A provider an admin has disabled is absent — and so are its models everywhere else on this surface, because a provider\'s state cascades to what it serves.
     * Returns the model providers the catalog knows, each with its info object, filtered to what the caller\'s org may see.
     */
    async getPricingProvidersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingProviderList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/providers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingProviderListFromJSON(jsonValue));
    }

    /**
     * Returns the model providers the catalog knows, each with its info object, filtered to what the caller\'s org may see. A provider an admin has disabled is absent — and so are its models everywhere else on this surface, because a provider\'s state cascades to what it serves.
     * Returns the model providers the catalog knows, each with its info object, filtered to what the caller\'s org may see.
     */
    async getPricingProviders(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingProviderList> {
        const response = await this.getPricingProvidersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the managed-service rate cards — Search, Crawl, Vector, Console and Managed Services — each with its own tiers, and some with usage rates or a comparison table. It is the section as authored, un-gated.  These are DISPLAY rate cards: what a product costs, not what a plan grants. No entitlement or limit fields ride here, so nothing can bill off them.
     * Returns the managed-service rate cards — Search, Crawl, Vector, Console and Managed Services — each with its own tiers, and some with usage rates or a comparison table.
     */
    async getPricingServicesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/services`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the managed-service rate cards — Search, Crawl, Vector, Console and Managed Services — each with its own tiers, and some with usage rates or a comparison table. It is the section as authored, un-gated.  These are DISPLAY rate cards: what a product costs, not what a plan grants. No entitlement or limit fields ride here, so nothing can bill off them.
     * Returns the managed-service rate cards — Search, Crawl, Vector, Console and Managed Services — each with its own tiers, and some with usage rates or a comparison table.
     */
    async getPricingServices(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingServicesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the API subscription plans — the account-level tiers a customer subscribes to, each with its monthly and annual price, included credit, rate limits and feature list.
     * Returns the API subscription plans — the account-level tiers a customer subscribes to, each with its monthly and annual price, included credit, rate limits and feature list.
     */
    async getPricingSubscriptionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingPlanList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/subscriptions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingPlanListFromJSON(jsonValue));
    }

    /**
     * Returns the API subscription plans — the account-level tiers a customer subscribes to, each with its monthly and annual price, included credit, rate limits and feature list.
     * Returns the API subscription plans — the account-level tiers a customer subscribes to, each with its monthly and annual price, included credit, rate limits and feature list.
     */
    async getPricingSubscriptions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingPlanList> {
        const response = await this.getPricingSubscriptionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the catalog\'s headline statistics — model counts by family and the provider directory. The provider sub-object is filtered to what the caller\'s org may see, so a disabled provider\'s name never leaks; the aggregate counts are the catalog\'s own, over everything it holds.
     * Returns the catalog\'s headline statistics — model counts by family and the provider directory.
     */
    async getPricingSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the catalog\'s headline statistics — model counts by family and the provider directory. The provider sub-object is filtered to what the caller\'s org may see, so a disabled provider\'s name never leaks; the aggregate counts are the catalog\'s own, over everything it holds.
     * Returns the catalog\'s headline statistics — model counts by family and the provider directory.
     */
    async getPricingSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getPricingSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the per-use tool prices — web search, code interpreter, file storage, image generation, speech-to-text and text-to-speech — each with the unit it is billed by and its price in that unit.
     * Returns the per-use tool prices — web search, code interpreter, file storage, image generation, speech-to-text and text-to-speech — each with the unit it is billed by and its price in that unit.
     */
    async getPricingToolsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingToolList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/tools`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingToolListFromJSON(jsonValue));
    }

    /**
     * Returns the per-use tool prices — web search, code interpreter, file storage, image generation, speech-to-text and text-to-speech — each with the unit it is billed by and its price in that unit.
     * Returns the per-use tool prices — web search, code interpreter, file storage, image generation, speech-to-text and text-to-speech — each with the unit it is billed by and its price in that unit.
     */
    async getPricingTools(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingToolList> {
        const response = await this.getPricingToolsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Opts the caller\'s OWN org into a beta item. The org is the caller\'s validated one, so this can never target another org, and the registry refuses anything not in beta — so it can neither re-open an item an operator turned off nor touch one that is already generally available. Requires a signed-in caller with an org.
     * Opts the caller\'s OWN org into a beta item.
     */
    async postPricingEnablementOptinRaw(requestParameters: PricingApiPostPricingEnablementOptinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserEnablementItem>> {
        if (requestParameters['enablementOptRef'] == null) {
            throw new runtime.RequiredError(
                'enablementOptRef',
                'Required parameter "enablementOptRef" was null or undefined when calling postPricingEnablementOptin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/enablement/optin`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EnablementOptRefToJSON(requestParameters['enablementOptRef']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserEnablementItemFromJSON(jsonValue));
    }

    /**
     * Opts the caller\'s OWN org into a beta item. The org is the caller\'s validated one, so this can never target another org, and the registry refuses anything not in beta — so it can neither re-open an item an operator turned off nor touch one that is already generally available. Requires a signed-in caller with an org.
     * Opts the caller\'s OWN org into a beta item.
     */
    async postPricingEnablementOptin(requestParameters: PricingApiPostPricingEnablementOptinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserEnablementItem> {
        const response = await this.postPricingEnablementOptinRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent. The org is the caller\'s validated one, so this can never revoke another org\'s grant. Requires a signed-in caller with an org.
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent.
     */
    async postPricingEnablementOptoutRaw(requestParameters: PricingApiPostPricingEnablementOptoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserEnablementItem>> {
        if (requestParameters['enablementOptRef'] == null) {
            throw new runtime.RequiredError(
                'enablementOptRef',
                'Required parameter "enablementOptRef" was null or undefined when calling postPricingEnablementOptout().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/enablement/optout`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EnablementOptRefToJSON(requestParameters['enablementOptRef']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserEnablementItemFromJSON(jsonValue));
    }

    /**
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent. The org is the caller\'s validated one, so this can never revoke another org\'s grant. Requires a signed-in caller with an org.
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent.
     */
    async postPricingEnablementOptout(requestParameters: PricingApiPostPricingEnablementOptoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserEnablementItem> {
        const response = await this.postPricingEnablementOptoutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Refreshes the third-party section of the catalog from its upstream listings and returns the time the refreshed catalog was stamped with. The fetch runs in Go and the markup transform in the pricing bundle. SuperAdmin only; every other caller is refused.
     * Refreshes the third-party section of the catalog from its upstream listings and returns the time the refreshed catalog was stamped with.
     */
    async postPricingSyncRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricingSyncOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pricing/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricingSyncOutFromJSON(jsonValue));
    }

    /**
     * Refreshes the third-party section of the catalog from its upstream listings and returns the time the refreshed catalog was stamped with. The fetch runs in Go and the markup transform in the pricing bundle. SuperAdmin only; every other caller is refused.
     * Refreshes the third-party section of the catalog from its upstream listings and returns the time the refreshed catalog was stamped with.
     */
    async postPricingSync(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricingSyncOut> {
        const response = await this.postPricingSyncRaw(initOverrides);
        return await response.value();
    }

}
