/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  FlowCreate,
  FlowRun,
  FlowStatus,
  FlowUpdate,
} from '../models/index.js';
import {
    FlowCreateFromJSON,
    FlowCreateToJSON,
    FlowRunFromJSON,
    FlowRunToJSON,
    FlowStatusFromJSON,
    FlowStatusToJSON,
    FlowUpdateFromJSON,
    FlowUpdateToJSON,
} from '../models/index.js';

export interface FlowApiDeleteFlowWorkflowsByWorkflowRequest {
    workflow: string;
}

export interface FlowApiGetFlowRunsRequest {
    workflow?: string;
}

export interface FlowApiGetFlowWorkflowsRequest {
    page?: string;
    size?: string;
}

export interface FlowApiGetFlowWorkflowsByWorkflowRequest {
    workflow: string;
}

export interface FlowApiPatchFlowWorkflowsByWorkflowRequest {
    workflow: string;
    flowUpdate: FlowUpdate;
}

export interface FlowApiPostFlowRunsRequest {
    flowRun: FlowRun;
}

export interface FlowApiPostFlowWorkflowsRequest {
    flowCreate: FlowCreate;
}

/**
 * 
 */
export class FlowApi extends runtime.BaseAPI {

    /**
     * Deletes one of the caller\'s workflows and its runs. Ownership is verified first; a foreign id answers 404 and deletes nothing.
     * Deletes one of the caller\'s workflows and its runs.
     */
    async deleteFlowWorkflowsByWorkflowRaw(requestParameters: FlowApiDeleteFlowWorkflowsByWorkflowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['workflow'] == null) {
            throw new runtime.RequiredError(
                'workflow',
                'Required parameter "workflow" was null or undefined when calling deleteFlowWorkflowsByWorkflow().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/workflows/{workflow}`;
        urlPath = urlPath.replace(`{${"workflow"}}`, encodeURIComponent(String(requestParameters['workflow'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Deletes one of the caller\'s workflows and its runs. Ownership is verified first; a foreign id answers 404 and deletes nothing.
     * Deletes one of the caller\'s workflows and its runs.
     */
    async deleteFlowWorkflowsByWorkflow(requestParameters: FlowApiDeleteFlowWorkflowsByWorkflowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.deleteFlowWorkflowsByWorkflowRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs reads one workflow\'s recorded runs: every component build with its result, keyed by component. Ownership is verified first — run records never cross the org boundary.
     * Runs reads one workflow\'s recorded runs: every component build with its result, keyed by component.
     */
    async getFlowRunsRaw(requestParameters: FlowApiGetFlowRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['workflow'] != null) {
            queryParameters['workflow'] = requestParameters['workflow'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/runs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Runs reads one workflow\'s recorded runs: every component build with its result, keyed by component. Ownership is verified first — run records never cross the org boundary.
     * Runs reads one workflow\'s recorded runs: every component build with its result, keyed by component.
     */
    async getFlowRuns(requestParameters: FlowApiGetFlowRunsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getFlowRunsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Status reports whether the flow service is reachable and which version it runs. It is the product\'s own /health and /v1/version composed — an honest lens for \"is the workflow plane up\", never a fabricated ok.
     * Status reports whether the flow service is reachable and which version it runs.
     */
    async getFlowStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlowStatus>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/status`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowStatusFromJSON(jsonValue));
    }

    /**
     * Status reports whether the flow service is reachable and which version it runs. It is the product\'s own /health and /v1/version composed — an honest lens for \"is the workflow plane up\", never a fabricated ok.
     * Status reports whether the flow service is reachable and which version it runs.
     */
    async getFlowStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlowStatus> {
        const response = await this.getFlowStatusRaw(initOverrides);
        return await response.value();
    }

    /**
     * Workflows lists the caller\'s workflows, paged. The list is scoped server-side to the org\'s project — the page can only ever hold the caller\'s own workflows.
     * Workflows lists the caller\'s workflows, paged.
     */
    async getFlowWorkflowsRaw(requestParameters: FlowApiGetFlowWorkflowsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/workflows`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Workflows lists the caller\'s workflows, paged. The list is scoped server-side to the org\'s project — the page can only ever hold the caller\'s own workflows.
     * Workflows lists the caller\'s workflows, paged.
     */
    async getFlowWorkflows(requestParameters: FlowApiGetFlowWorkflowsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getFlowWorkflowsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Workflow reads one of the caller\'s workflows — the full record, graph included. A workflow outside the caller\'s org answers 404, indistinguishable from one that does not exist.
     * Workflow reads one of the caller\'s workflows — the full record, graph included.
     */
    async getFlowWorkflowsByWorkflowRaw(requestParameters: FlowApiGetFlowWorkflowsByWorkflowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['workflow'] == null) {
            throw new runtime.RequiredError(
                'workflow',
                'Required parameter "workflow" was null or undefined when calling getFlowWorkflowsByWorkflow().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/workflows/{workflow}`;
        urlPath = urlPath.replace(`{${"workflow"}}`, encodeURIComponent(String(requestParameters['workflow'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Workflow reads one of the caller\'s workflows — the full record, graph included. A workflow outside the caller\'s org answers 404, indistinguishable from one that does not exist.
     * Workflow reads one of the caller\'s workflows — the full record, graph included.
     */
    async getFlowWorkflowsByWorkflow(requestParameters: FlowApiGetFlowWorkflowsByWorkflowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.getFlowWorkflowsByWorkflowRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Patches one of the caller\'s workflows: name, description, graph, or the locked flag — only the stated fields move. Ownership is verified before the patch reaches the product.
     * Patches one of the caller\'s workflows: name, description, graph, or the locked flag — only the stated fields move.
     */
    async patchFlowWorkflowsByWorkflowRaw(requestParameters: FlowApiPatchFlowWorkflowsByWorkflowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['workflow'] == null) {
            throw new runtime.RequiredError(
                'workflow',
                'Required parameter "workflow" was null or undefined when calling patchFlowWorkflowsByWorkflow().'
            );
        }

        if (requestParameters['flowUpdate'] == null) {
            throw new runtime.RequiredError(
                'flowUpdate',
                'Required parameter "flowUpdate" was null or undefined when calling patchFlowWorkflowsByWorkflow().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/workflows/{workflow}`;
        urlPath = urlPath.replace(`{${"workflow"}}`, encodeURIComponent(String(requestParameters['workflow'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: FlowUpdateToJSON(requestParameters['flowUpdate']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Patches one of the caller\'s workflows: name, description, graph, or the locked flag — only the stated fields move. Ownership is verified before the patch reaches the product.
     * Patches one of the caller\'s workflows: name, description, graph, or the locked flag — only the stated fields move.
     */
    async patchFlowWorkflowsByWorkflow(requestParameters: FlowApiPatchFlowWorkflowsByWorkflowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.patchFlowWorkflowsByWorkflowRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Run executes one of the caller\'s workflows synchronously: the graph runs in the flow service and the response carries the run\'s session and outputs. A graph whose components fail reports the product\'s own error. Runs are bounded by the product\'s five-minute sync ceiling.
     * Run executes one of the caller\'s workflows synchronously: the graph runs in the flow service and the response carries the run\'s session and outputs.
     */
    async postFlowRunsRaw(requestParameters: FlowApiPostFlowRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['flowRun'] == null) {
            throw new runtime.RequiredError(
                'flowRun',
                'Required parameter "flowRun" was null or undefined when calling postFlowRuns().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/runs`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FlowRunToJSON(requestParameters['flowRun']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Run executes one of the caller\'s workflows synchronously: the graph runs in the flow service and the response carries the run\'s session and outputs. A graph whose components fail reports the product\'s own error. Runs are bounded by the product\'s five-minute sync ceiling.
     * Run executes one of the caller\'s workflows synchronously: the graph runs in the flow service and the response carries the run\'s session and outputs.
     */
    async postFlowRuns(requestParameters: FlowApiPostFlowRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postFlowRunsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a workflow in the caller\'s org. The org\'s project id is pinned server-side from the validated principal — there is no field by which a caller could place a workflow in another org.
     * Creates a workflow in the caller\'s org.
     */
    async postFlowWorkflowsRaw(requestParameters: FlowApiPostFlowWorkflowsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['flowCreate'] == null) {
            throw new runtime.RequiredError(
                'flowCreate',
                'Required parameter "flowCreate" was null or undefined when calling postFlowWorkflows().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/flow/workflows`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FlowCreateToJSON(requestParameters['flowCreate']),
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Creates a workflow in the caller\'s org. The org\'s project id is pinned server-side from the validated principal — there is no field by which a caller could place a workflow in another org.
     * Creates a workflow in the caller\'s org.
     */
    async postFlowWorkflows(requestParameters: FlowApiPostFlowWorkflowsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.postFlowWorkflowsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
