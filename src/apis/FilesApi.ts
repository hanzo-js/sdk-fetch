/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';

export interface FilesApiGetFilesBySidRequest {
    sid: string;
}

/**
 * 
 */
export class FilesApi extends runtime.BaseAPI {

    /**
     * Lists what a session\'s sandbox holds — the uploads a run can read and the artifacts it produced — each then fetched from /v1/download.  It answers a BARE JSON ARRAY of {name, lastModified}, where `name` is the same {session_id}/{fileId} identifier download takes, because that is what the client matches on. An object wrapper would be a wire change, which is why this is not a typed operation.
     * List the files in an execution session
     */
    async getFilesBySidRaw(requestParameters: FilesApiGetFilesBySidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['sid'] == null) {
            throw new runtime.RequiredError(
                'sid',
                'Required parameter "sid" was null or undefined when calling getFilesBySid().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/files/{sid}`;
        urlPath = urlPath.replace(`{${"sid"}}`, encodeURIComponent(String(requestParameters['sid'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Lists what a session\'s sandbox holds — the uploads a run can read and the artifacts it produced — each then fetched from /v1/download.  It answers a BARE JSON ARRAY of {name, lastModified}, where `name` is the same {session_id}/{fileId} identifier download takes, because that is what the client matches on. An object wrapper would be a wire change, which is why this is not a typed operation.
     * List the files in an execution session
     */
    async getFilesBySid(requestParameters: FilesApiGetFilesBySidRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getFilesBySidRaw(requestParameters, initOverrides);
    }

}
