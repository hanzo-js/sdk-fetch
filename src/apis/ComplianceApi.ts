/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AccList,
  AccView,
  AccreditationDecision,
  AccreditationReq,
  AuditList,
  CheckList,
  CheckView,
  HealthView,
  RecordList,
  StatusView,
  Subject,
  SubjectList,
  SubjectReq,
  VerificationDecision,
  VerificationReq,
} from '../models/index.js';
import {
    AccListFromJSON,
    AccListToJSON,
    AccViewFromJSON,
    AccViewToJSON,
    AccreditationDecisionFromJSON,
    AccreditationDecisionToJSON,
    AccreditationReqFromJSON,
    AccreditationReqToJSON,
    AuditListFromJSON,
    AuditListToJSON,
    CheckListFromJSON,
    CheckListToJSON,
    CheckViewFromJSON,
    CheckViewToJSON,
    HealthViewFromJSON,
    HealthViewToJSON,
    RecordListFromJSON,
    RecordListToJSON,
    StatusViewFromJSON,
    StatusViewToJSON,
    SubjectFromJSON,
    SubjectToJSON,
    SubjectListFromJSON,
    SubjectListToJSON,
    SubjectReqFromJSON,
    SubjectReqToJSON,
    VerificationDecisionFromJSON,
    VerificationDecisionToJSON,
    VerificationReqFromJSON,
    VerificationReqToJSON,
} from '../models/index.js';

export interface ComplianceApiGetComplianceAccreditationRequest {
    limit?: number;
}

export interface ComplianceApiGetComplianceAccreditationByIdRequest {
    id: string;
}

export interface ComplianceApiGetComplianceAuditRequest {
    result?: string;
}

export interface ComplianceApiGetComplianceRecordsRequest {
    limit?: number;
}

export interface ComplianceApiGetComplianceSubjectsRequest {
    limit?: number;
}

export interface ComplianceApiGetComplianceSubjectsByIdRequest {
    id: string;
}

export interface ComplianceApiGetComplianceVerificationsRequest {
    limit?: number;
}

export interface ComplianceApiGetComplianceVerificationsByIdRequest {
    id: string;
}

export interface ComplianceApiPostComplianceAccreditationRequest {
    accreditationReq: AccreditationReq;
}

export interface ComplianceApiPostComplianceAccreditationByIdDecisionRequest {
    id: string;
    accreditationDecision: AccreditationDecision;
}

export interface ComplianceApiPostComplianceSubjectsRequest {
    subjectReq: SubjectReq;
}

export interface ComplianceApiPostComplianceVerificationsRequest {
    verificationReq: VerificationReq;
}

export interface ComplianceApiPostComplianceVerificationsByIdDecisionRequest {
    id: string;
    verificationDecision: VerificationDecision;
}

export interface ComplianceApiPostComplianceVerificationsByIdRefreshRequest {
    id: string;
}

/**
 * 
 */
export class ComplianceApi extends runtime.BaseAPI {

    /**
     * Returns the org\'s tracked accreditation-state records, newest first — evidence entries the org keeps, never a platform certification.
     * Returns the org\'s tracked accreditation-state records, newest first — evidence entries the org keeps, never a platform certification.
     */
    async getComplianceAccreditationRaw(requestParameters: ComplianceApiGetComplianceAccreditationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/accreditation`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s tracked accreditation-state records, newest first — evidence entries the org keeps, never a platform certification.
     * Returns the org\'s tracked accreditation-state records, newest first — evidence entries the org keeps, never a platform certification.
     */
    async getComplianceAccreditation(requestParameters: ComplianceApiGetComplianceAccreditationRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccList> {
        const response = await this.getComplianceAccreditationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one tracked accreditation record.
     * Returns one tracked accreditation record.
     */
    async getComplianceAccreditationByIdRaw(requestParameters: ComplianceApiGetComplianceAccreditationByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getComplianceAccreditationById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/accreditation/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccViewFromJSON(jsonValue));
    }

    /**
     * Returns one tracked accreditation record.
     * Returns one tracked accreditation record.
     */
    async getComplianceAccreditationById(requestParameters: ComplianceApiGetComplianceAccreditationByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccView> {
        const response = await this.getComplianceAccreditationByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * AuditRead is the compliance read of the SHARED tamper-evident audit plane — the SOC 2 posture surface (privileged actions: who started/decided what, when). The org is PINNED to the caller\'s validated org and the rows are narrowed to compliance.* actions. Fail-closed: no principal is a 403, no configured audit store a 501.
     * AuditRead is the compliance read of the SHARED tamper-evident audit plane — the SOC 2 posture surface (privileged actions: who started/decided what, when).
     */
    async getComplianceAuditRaw(requestParameters: ComplianceApiGetComplianceAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuditList>> {
        const queryParameters: any = {};

        if (requestParameters['result'] != null) {
            queryParameters['result'] = requestParameters['result'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/audit`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuditListFromJSON(jsonValue));
    }

    /**
     * AuditRead is the compliance read of the SHARED tamper-evident audit plane — the SOC 2 posture surface (privileged actions: who started/decided what, when). The org is PINNED to the caller\'s validated org and the rows are narrowed to compliance.* actions. Fail-closed: no principal is a 403, no configured audit store a 501.
     * AuditRead is the compliance read of the SHARED tamper-evident audit plane — the SOC 2 posture surface (privileged actions: who started/decided what, when).
     */
    async getComplianceAudit(requestParameters: ComplianceApiGetComplianceAuditRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuditList> {
        const response = await this.getComplianceAuditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Health reports subsystem liveness and the wired verification provider. Fail-open on purpose: it never probes the external provider, so a provider outage cannot fail liveness.
     * Health reports subsystem liveness and the wired verification provider.
     */
    async getComplianceHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HealthView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HealthViewFromJSON(jsonValue));
    }

    /**
     * Health reports subsystem liveness and the wired verification provider. Fail-open on purpose: it never probes the external provider, so a provider outage cannot fail liveness.
     * Health reports subsystem liveness and the wired verification provider.
     */
    async getComplianceHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HealthView> {
        const response = await this.getComplianceHealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * ListRecords is the unified compliance-record view for the org: its verifications and accreditation records together, each provider-reported or tracked, never platform-asserted. PII stays in the subject store; records carry only opaque ids and statuses.
     * ListRecords is the unified compliance-record view for the org: its verifications and accreditation records together, each provider-reported or tracked, never platform-asserted.
     */
    async getComplianceRecordsRaw(requestParameters: ComplianceApiGetComplianceRecordsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RecordList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/records`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RecordListFromJSON(jsonValue));
    }

    /**
     * ListRecords is the unified compliance-record view for the org: its verifications and accreditation records together, each provider-reported or tracked, never platform-asserted. PII stays in the subject store; records carry only opaque ids and statuses.
     * ListRecords is the unified compliance-record view for the org: its verifications and accreditation records together, each provider-reported or tracked, never platform-asserted.
     */
    async getComplianceRecords(requestParameters: ComplianceApiGetComplianceRecordsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RecordList> {
        const response = await this.getComplianceRecordsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Status is the org\'s honest posture read: the wired provider and the per-status tally of its verifications. It is deliberately NOT a boolean \"compliant\" — it reports counts of provider-reported states and carries the boundary disclaimer.
     * Status is the org\'s honest posture read: the wired provider and the per-status tally of its verifications.
     */
    async getComplianceStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StatusView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/status`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StatusViewFromJSON(jsonValue));
    }

    /**
     * Status is the org\'s honest posture read: the wired provider and the per-status tally of its verifications. It is deliberately NOT a boolean \"compliant\" — it reports counts of provider-reported states and carries the boundary disclaimer.
     * Status is the org\'s honest posture read: the wired provider and the per-status tally of its verifications.
     */
    async getComplianceStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StatusView> {
        const response = await this.getComplianceStatusRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s subjects as PII-MINIMIZED summaries — no name or email, only whether an email is on file. The full record is returned only by the explicit single-subject read.
     * Returns the org\'s subjects as PII-MINIMIZED summaries — no name or email, only whether an email is on file.
     */
    async getComplianceSubjectsRaw(requestParameters: ComplianceApiGetComplianceSubjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubjectList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/subjects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubjectListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s subjects as PII-MINIMIZED summaries — no name or email, only whether an email is on file. The full record is returned only by the explicit single-subject read.
     * Returns the org\'s subjects as PII-MINIMIZED summaries — no name or email, only whether an email is on file.
     */
    async getComplianceSubjects(requestParameters: ComplianceApiGetComplianceSubjectsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubjectList> {
        const response = await this.getComplianceSubjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one subject WITH its contact PII — the only surface that returns it, and only to the owning org. The response is never cached by any intermediary.
     * Returns one subject WITH its contact PII — the only surface that returns it, and only to the owning org.
     */
    async getComplianceSubjectsByIdRaw(requestParameters: ComplianceApiGetComplianceSubjectsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Subject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getComplianceSubjectsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/subjects/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubjectFromJSON(jsonValue));
    }

    /**
     * Returns one subject WITH its contact PII — the only surface that returns it, and only to the owning org. The response is never cached by any intermediary.
     * Returns one subject WITH its contact PII — the only surface that returns it, and only to the owning org.
     */
    async getComplianceSubjectsById(requestParameters: ComplianceApiGetComplianceSubjectsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Subject> {
        const response = await this.getComplianceSubjectsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s KYC/KYB verifications, newest first — opaque subject references and provider-reported statuses only, no subject PII.
     * Returns the org\'s KYC/KYB verifications, newest first — opaque subject references and provider-reported statuses only, no subject PII.
     */
    async getComplianceVerificationsRaw(requestParameters: ComplianceApiGetComplianceVerificationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/verifications`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CheckListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s KYC/KYB verifications, newest first — opaque subject references and provider-reported statuses only, no subject PII.
     * Returns the org\'s KYC/KYB verifications, newest first — opaque subject references and provider-reported statuses only, no subject PII.
     */
    async getComplianceVerifications(requestParameters: ComplianceApiGetComplianceVerificationsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckList> {
        const response = await this.getComplianceVerificationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one verification — its opaque subject reference and provider-reported status, no subject PII.
     * Returns one verification — its opaque subject reference and provider-reported status, no subject PII.
     */
    async getComplianceVerificationsByIdRaw(requestParameters: ComplianceApiGetComplianceVerificationsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getComplianceVerificationsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/verifications/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CheckViewFromJSON(jsonValue));
    }

    /**
     * Returns one verification — its opaque subject reference and provider-reported status, no subject PII.
     * Returns one verification — its opaque subject reference and provider-reported status, no subject PII.
     */
    async getComplianceVerificationsById(requestParameters: ComplianceApiGetComplianceVerificationsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckView> {
        const response = await this.getComplianceVerificationsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records an ASSERTED accreditation state for a subject — the subject\'s own assertion, with no verifier. Every CONFIRMED state (provider_verified, reviewer_confirmed) and every rejected/expired state is a DECISION recorded via the decision endpoint, attributed to the reviewer — a create can never stamp a confirmation. The underlying figures (income, net worth) are never stored; only the method, category, and state.
     * Records an ASSERTED accreditation state for a subject — the subject\'s own assertion, with no verifier.
     */
    async postComplianceAccreditationRaw(requestParameters: ComplianceApiPostComplianceAccreditationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccView>> {
        if (requestParameters['accreditationReq'] == null) {
            throw new runtime.RequiredError(
                'accreditationReq',
                'Required parameter "accreditationReq" was null or undefined when calling postComplianceAccreditation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/accreditation`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AccreditationReqToJSON(requestParameters['accreditationReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccViewFromJSON(jsonValue));
    }

    /**
     * Records an ASSERTED accreditation state for a subject — the subject\'s own assertion, with no verifier. Every CONFIRMED state (provider_verified, reviewer_confirmed) and every rejected/expired state is a DECISION recorded via the decision endpoint, attributed to the reviewer — a create can never stamp a confirmation. The underlying figures (income, net worth) are never stored; only the method, category, and state.
     * Records an ASSERTED accreditation state for a subject — the subject\'s own assertion, with no verifier.
     */
    async postComplianceAccreditation(requestParameters: ComplianceApiPostComplianceAccreditationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccView> {
        const response = await this.postComplianceAccreditationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records an org reviewer\'s decision on an accreditation record — a reviewer confirmation, a provider verification the reviewer has evidence of (a CPA/attorney letter, a verifier report), a rejection, or an expiry. ROLE-GATED (an org admin or platform reviewer) and ATTRIBUTED: the reviewer\'s identity is recorded as ReviewerSub and audited. Human-in-the-loop: the platform never confirms on its own, and even a provider_verified state carries the reviewer who recorded it.
     * Records an org reviewer\'s decision on an accreditation record — a reviewer confirmation, a provider verification the reviewer has evidence of (a CPA/attorney letter, a verifier report), a rejection, or an expiry.
     */
    async postComplianceAccreditationByIdDecisionRaw(requestParameters: ComplianceApiPostComplianceAccreditationByIdDecisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postComplianceAccreditationByIdDecision().'
            );
        }

        if (requestParameters['accreditationDecision'] == null) {
            throw new runtime.RequiredError(
                'accreditationDecision',
                'Required parameter "accreditationDecision" was null or undefined when calling postComplianceAccreditationByIdDecision().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/accreditation/{id}/decision`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AccreditationDecisionToJSON(requestParameters['accreditationDecision']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccViewFromJSON(jsonValue));
    }

    /**
     * Records an org reviewer\'s decision on an accreditation record — a reviewer confirmation, a provider verification the reviewer has evidence of (a CPA/attorney letter, a verifier report), a rejection, or an expiry. ROLE-GATED (an org admin or platform reviewer) and ATTRIBUTED: the reviewer\'s identity is recorded as ReviewerSub and audited. Human-in-the-loop: the platform never confirms on its own, and even a provider_verified state carries the reviewer who recorded it.
     * Records an org reviewer\'s decision on an accreditation record — a reviewer confirmation, a provider verification the reviewer has evidence of (a CPA/attorney letter, a verifier report), a rejection, or an expiry.
     */
    async postComplianceAccreditationByIdDecision(requestParameters: ComplianceApiPostComplianceAccreditationByIdDecisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccView> {
        const response = await this.postComplianceAccreditationByIdDecisionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a party the org is verifying as part of its own onboarding/compliance — a team member, vendor, customer, or counterparty. The subject\'s contact PII (name/email) is sealed at rest and returned only to the owning org; downstream records reference the subject by opaque id.
     * Records a party the org is verifying as part of its own onboarding/compliance — a team member, vendor, customer, or counterparty.
     */
    async postComplianceSubjectsRaw(requestParameters: ComplianceApiPostComplianceSubjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Subject>> {
        if (requestParameters['subjectReq'] == null) {
            throw new runtime.RequiredError(
                'subjectReq',
                'Required parameter "subjectReq" was null or undefined when calling postComplianceSubjects().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/subjects`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubjectReqToJSON(requestParameters['subjectReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubjectFromJSON(jsonValue));
    }

    /**
     * Records a party the org is verifying as part of its own onboarding/compliance — a team member, vendor, customer, or counterparty. The subject\'s contact PII (name/email) is sealed at rest and returned only to the owning org; downstream records reference the subject by opaque id.
     * Records a party the org is verifying as part of its own onboarding/compliance — a team member, vendor, customer, or counterparty.
     */
    async postComplianceSubjects(requestParameters: ComplianceApiPostComplianceSubjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Subject> {
        const response = await this.postComplianceSubjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Begins a KYC/KYB verification of a subject through the wired provider — an existing subject by id, or one created inline from the request. The returned status is provider-reported and never terminal on a fresh start: starting a verification can never yield a verified record, and a provider error is a 502, never a verification.
     * Begins a KYC/KYB verification of a subject through the wired provider — an existing subject by id, or one created inline from the request.
     */
    async postComplianceVerificationsRaw(requestParameters: ComplianceApiPostComplianceVerificationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckView>> {
        if (requestParameters['verificationReq'] == null) {
            throw new runtime.RequiredError(
                'verificationReq',
                'Required parameter "verificationReq" was null or undefined when calling postComplianceVerifications().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/verifications`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VerificationReqToJSON(requestParameters['verificationReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CheckViewFromJSON(jsonValue));
    }

    /**
     * Begins a KYC/KYB verification of a subject through the wired provider — an existing subject by id, or one created inline from the request. The returned status is provider-reported and never terminal on a fresh start: starting a verification can never yield a verified record, and a provider error is a 502, never a verification.
     * Begins a KYC/KYB verification of a subject through the wired provider — an existing subject by id, or one created inline from the request.
     */
    async postComplianceVerifications(requestParameters: ComplianceApiPostComplianceVerificationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckView> {
        const response = await this.postComplianceVerificationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a privileged reviewer\'s MANUAL decision on a verification — the human-in-the-loop path, and the ONLY route to a passing status when no real provider is wired. It produces a DISTINCT reviewer_confirmed, never a provider_verified (a provider decision is the provider\'s to report, via the webhook or a reconcile), and it is ROLE-GATED (an org admin or platform reviewer) AND ATTRIBUTED (the reviewer\'s user id is DecidedBy), so a manual pass is always accountable.
     * Records a privileged reviewer\'s MANUAL decision on a verification — the human-in-the-loop path, and the ONLY route to a passing status when no real provider is wired.
     */
    async postComplianceVerificationsByIdDecisionRaw(requestParameters: ComplianceApiPostComplianceVerificationsByIdDecisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postComplianceVerificationsByIdDecision().'
            );
        }

        if (requestParameters['verificationDecision'] == null) {
            throw new runtime.RequiredError(
                'verificationDecision',
                'Required parameter "verificationDecision" was null or undefined when calling postComplianceVerificationsByIdDecision().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/verifications/{id}/decision`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VerificationDecisionToJSON(requestParameters['verificationDecision']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CheckViewFromJSON(jsonValue));
    }

    /**
     * Records a privileged reviewer\'s MANUAL decision on a verification — the human-in-the-loop path, and the ONLY route to a passing status when no real provider is wired. It produces a DISTINCT reviewer_confirmed, never a provider_verified (a provider decision is the provider\'s to report, via the webhook or a reconcile), and it is ROLE-GATED (an org admin or platform reviewer) AND ATTRIBUTED (the reviewer\'s user id is DecidedBy), so a manual pass is always accountable.
     * Records a privileged reviewer\'s MANUAL decision on a verification — the human-in-the-loop path, and the ONLY route to a passing status when no real provider is wired.
     */
    async postComplianceVerificationsByIdDecision(requestParameters: ComplianceApiPostComplianceVerificationsByIdDecisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckView> {
        const response = await this.postComplianceVerificationsByIdDecisionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Polls the wired provider for its current decision and records it, ATTRIBUTED to the provider — the internal PULL reconcile. For the Manual provider the check stays pending; for a hosted provider it reflects the provider\'s settled status. A poll error is a 502, never a verification.
     * Polls the wired provider for its current decision and records it, ATTRIBUTED to the provider — the internal PULL reconcile.
     */
    async postComplianceVerificationsByIdRefreshRaw(requestParameters: ComplianceApiPostComplianceVerificationsByIdRefreshRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postComplianceVerificationsByIdRefresh().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/verifications/{id}/refresh`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CheckViewFromJSON(jsonValue));
    }

    /**
     * Polls the wired provider for its current decision and records it, ATTRIBUTED to the provider — the internal PULL reconcile. For the Manual provider the check stays pending; for a hosted provider it reflects the provider\'s settled status. A poll error is a 502, never a verification.
     * Polls the wired provider for its current decision and records it, ATTRIBUTED to the provider — the internal PULL reconcile.
     */
    async postComplianceVerificationsByIdRefresh(requestParameters: ComplianceApiPostComplianceVerificationsByIdRefreshRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckView> {
        const response = await this.postComplianceVerificationsByIdRefreshRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The external PUSH reconcile: a verification provider (or a Hanzo relay) signals that a check settled, and the reconciled check comes back. It authenticates by an HMAC SIGNATURE over the RAW body bytes rather than by a principal — an external caller has no validated org — and the org is then resolved FROM the record the signed provider reference matches, so a call can only ever touch the one tenant that owns that reference.  The body carries NO trusted decision. A valid signature cannot force a status: the reference only says WHICH check to re-read, and the status is then pulled from the wired provider, which stays the source of truth. With no real provider configured a check stays pending, and the only route to a passing status is the role-gated, attributed reviewer decision.  An unknown reference is a benign 200 `{\"ignored\": ...}` no-op, not an error, so a provider replaying stale events neither retry-storms nor learns whether a reference exists in some other tenant. Fails closed otherwise: 501 unless a webhook secret is configured, 401 on a signature that does not verify, 400 with no provider reference, 413 over 1 MiB, and 502 if the secret or the provider is unreachable.
     * Provider push that settles a verification, authenticated by HMAC signature
     */
    async postComplianceVerificationsWebhookRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/compliance/verifications/webhook`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The external PUSH reconcile: a verification provider (or a Hanzo relay) signals that a check settled, and the reconciled check comes back. It authenticates by an HMAC SIGNATURE over the RAW body bytes rather than by a principal — an external caller has no validated org — and the org is then resolved FROM the record the signed provider reference matches, so a call can only ever touch the one tenant that owns that reference.  The body carries NO trusted decision. A valid signature cannot force a status: the reference only says WHICH check to re-read, and the status is then pulled from the wired provider, which stays the source of truth. With no real provider configured a check stays pending, and the only route to a passing status is the role-gated, attributed reviewer decision.  An unknown reference is a benign 200 `{\"ignored\": ...}` no-op, not an error, so a provider replaying stale events neither retry-storms nor learns whether a reference exists in some other tenant. Fails closed otherwise: 501 unless a webhook secret is configured, 401 on a signature that does not verify, 400 with no provider reference, 413 over 1 MiB, and 502 if the secret or the provider is unreachable.
     * Provider push that settles a verification, authenticated by HMAC signature
     */
    async postComplianceVerificationsWebhook(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postComplianceVerificationsWebhookRaw(initOverrides);
    }

}
