/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  IngressMiddlewares,
  IngressRoutes,
  IngressServices,
  IngressStatus,
  IngressTLS,
  ModelMiddleware,
  Route,
  TLSConfig,
  Upstream,
} from '../models/index.js';
import {
    IngressMiddlewaresFromJSON,
    IngressMiddlewaresToJSON,
    IngressRoutesFromJSON,
    IngressRoutesToJSON,
    IngressServicesFromJSON,
    IngressServicesToJSON,
    IngressStatusFromJSON,
    IngressStatusToJSON,
    IngressTLSFromJSON,
    IngressTLSToJSON,
    ModelMiddlewareFromJSON,
    ModelMiddlewareToJSON,
    RouteFromJSON,
    RouteToJSON,
    TLSConfigFromJSON,
    TLSConfigToJSON,
    UpstreamFromJSON,
    UpstreamToJSON,
} from '../models/index.js';

export interface IngressApiDeleteIngressMiddlewaresByIdRequest {
    id: string;
}

export interface IngressApiDeleteIngressRoutesByIdRequest {
    id: string;
}

export interface IngressApiDeleteIngressServicesByIdRequest {
    id: string;
}

export interface IngressApiGetIngressMiddlewaresByIdRequest {
    id: string;
}

export interface IngressApiGetIngressRoutesByIdRequest {
    id: string;
}

export interface IngressApiGetIngressServicesByIdRequest {
    id: string;
}

export interface IngressApiPostIngressMiddlewaresRequest {
    modelMiddleware: ModelMiddleware;
}

export interface IngressApiPostIngressRoutesRequest {
    route: Route;
}

export interface IngressApiPostIngressServicesRequest {
    upstream: Upstream;
}

export interface IngressApiPutIngressMiddlewaresByIdRequest {
    id: string;
    modelMiddleware: ModelMiddleware;
}

export interface IngressApiPutIngressRoutesByIdRequest {
    id: string;
    route: Route;
}

export interface IngressApiPutIngressServicesByIdRequest {
    id: string;
    upstream: Upstream;
}

export interface IngressApiPutIngressTlsRequest {
    tLSConfig: TLSConfig;
}

/**
 * 
 */
export class IngressApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s edge transforms and hot-applies the change. Routes still naming it stop being served (they compile as skipped) until they name a transform that exists. Answers 204; an id this org does not hold is 404.
     * Removes one of the caller org\'s edge transforms and hot-applies the change.
     */
    async deleteIngressMiddlewaresByIdRaw(requestParameters: IngressApiDeleteIngressMiddlewaresByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteIngressMiddlewaresById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/middlewares/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s edge transforms and hot-applies the change. Routes still naming it stop being served (they compile as skipped) until they name a transform that exists. Answers 204; an id this org does not hold is 404.
     * Removes one of the caller org\'s edge transforms and hot-applies the change.
     */
    async deleteIngressMiddlewaresById(requestParameters: IngressApiDeleteIngressMiddlewaresByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteIngressMiddlewaresByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one of the caller org\'s routing rules and hot-applies the shrunken table, freeing its host for another claim. Answers 204; an id this org does not hold is 404.
     * Removes one of the caller org\'s routing rules and hot-applies the shrunken table, freeing its host for another claim.
     */
    async deleteIngressRoutesByIdRaw(requestParameters: IngressApiDeleteIngressRoutesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteIngressRoutesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/routes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s routing rules and hot-applies the shrunken table, freeing its host for another claim. Answers 204; an id this org does not hold is 404.
     * Removes one of the caller org\'s routing rules and hot-applies the shrunken table, freeing its host for another claim.
     */
    async deleteIngressRoutesById(requestParameters: IngressApiDeleteIngressRoutesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteIngressRoutesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one of the caller org\'s backend pools and hot-applies the change. Routes still pointing at it stop being served (they compile as skipped) until they name a pool that exists. Answers 204; an id this org does not hold is 404.
     * Removes one of the caller org\'s backend pools and hot-applies the change.
     */
    async deleteIngressServicesByIdRaw(requestParameters: IngressApiDeleteIngressServicesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteIngressServicesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/services/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s backend pools and hot-applies the change. Routes still pointing at it stop being served (they compile as skipped) until they name a pool that exists. Answers 204; an id this org does not hold is 404.
     * Removes one of the caller org\'s backend pools and hot-applies the change.
     */
    async deleteIngressServicesById(requestParameters: IngressApiDeleteIngressServicesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteIngressServicesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Returns every edge transform the caller\'s org has configured, ordered by id. A route names the ones it wants, in order.
     * Returns every edge transform the caller\'s org has configured, ordered by id.
     */
    async getIngressMiddlewaresRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IngressMiddlewares>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/middlewares`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IngressMiddlewaresFromJSON(jsonValue));
    }

    /**
     * Returns every edge transform the caller\'s org has configured, ordered by id. A route names the ones it wants, in order.
     * Returns every edge transform the caller\'s org has configured, ordered by id.
     */
    async getIngressMiddlewares(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IngressMiddlewares> {
        const response = await this.getIngressMiddlewaresRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s edge transforms by id.
     * Returns one of the caller org\'s edge transforms by id.
     */
    async getIngressMiddlewaresByIdRaw(requestParameters: IngressApiGetIngressMiddlewaresByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelMiddleware>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getIngressMiddlewaresById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/middlewares/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelMiddlewareFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s edge transforms by id.
     * Returns one of the caller org\'s edge transforms by id.
     */
    async getIngressMiddlewaresById(requestParameters: IngressApiGetIngressMiddlewaresByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelMiddleware> {
        const response = await this.getIngressMiddlewaresByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every routing rule the caller\'s org has configured, ordered by id. A route maps an exact Host (and optional path prefix) to a service.
     * Returns every routing rule the caller\'s org has configured, ordered by id.
     */
    async getIngressRoutesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IngressRoutes>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/routes`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IngressRoutesFromJSON(jsonValue));
    }

    /**
     * Returns every routing rule the caller\'s org has configured, ordered by id. A route maps an exact Host (and optional path prefix) to a service.
     * Returns every routing rule the caller\'s org has configured, ordered by id.
     */
    async getIngressRoutes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IngressRoutes> {
        const response = await this.getIngressRoutesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s routing rules by id.
     * Returns one of the caller org\'s routing rules by id.
     */
    async getIngressRoutesByIdRaw(requestParameters: IngressApiGetIngressRoutesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Route>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getIngressRoutesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/routes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RouteFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s routing rules by id.
     * Returns one of the caller org\'s routing rules by id.
     */
    async getIngressRoutesById(requestParameters: IngressApiGetIngressRoutesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Route> {
        const response = await this.getIngressRoutesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every backend pool the caller\'s org has configured, ordered by id. A service is the weighted round-robin target a route dispatches to.
     * Returns every backend pool the caller\'s org has configured, ordered by id.
     */
    async getIngressServicesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IngressServices>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/services`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IngressServicesFromJSON(jsonValue));
    }

    /**
     * Returns every backend pool the caller\'s org has configured, ordered by id. A service is the weighted round-robin target a route dispatches to.
     * Returns every backend pool the caller\'s org has configured, ordered by id.
     */
    async getIngressServices(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IngressServices> {
        const response = await this.getIngressServicesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s backend pools by id.
     * Returns one of the caller org\'s backend pools by id.
     */
    async getIngressServicesByIdRaw(requestParameters: IngressApiGetIngressServicesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Upstream>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getIngressServicesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/services/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UpstreamFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s backend pools by id.
     * Returns one of the caller org\'s backend pools by id.
     */
    async getIngressServicesById(requestParameters: IngressApiGetIngressServicesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Upstream> {
        const response = await this.getIngressServicesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Status reports the ingress edge\'s live posture: the role this instance runs in (app or edge), whether its listeners are bound and on which addresses, the ACME posture (staging flag and certificate cache directory), how many hosts the compiled route table currently serves, and how many the ACME HostPolicy will issue a certificate for.
     * Status reports the ingress edge\'s live posture: the role this instance runs in (app or edge), whether its listeners are bound and on which addresses, the ACME posture (staging flag and certificate cache directory), how many hosts the compiled route table currently serves, and how many the ACME HostPolicy will issue a certificate for.
     */
    async getIngressStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IngressStatus>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/status`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IngressStatusFromJSON(jsonValue));
    }

    /**
     * Status reports the ingress edge\'s live posture: the role this instance runs in (app or edge), whether its listeners are bound and on which addresses, the ACME posture (staging flag and certificate cache directory), how many hosts the compiled route table currently serves, and how many the ACME HostPolicy will issue a certificate for.
     * Status reports the ingress edge\'s live posture: the role this instance runs in (app or edge), whether its listeners are bound and on which addresses, the ACME posture (staging flag and certificate cache directory), how many hosts the compiled route table currently serves, and how many the ACME HostPolicy will issue a certificate for.
     */
    async getIngressStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IngressStatus> {
        const response = await this.getIngressStatusRaw(initOverrides);
        return await response.value();
    }

    /**
     * GetTLS returns the caller org\'s ACME intent together with the edge-wide TLS facts it lands in: which role this instance runs in, whether its listeners are bound, every host the ACME HostPolicy will issue a certificate for (the union across ALL orgs of TLS-marked routes and configured extraHosts, because one process holds one certificate cache), and the ACME directory and account email the process was started with.
     * GetTLS returns the caller org\'s ACME intent together with the edge-wide TLS facts it lands in: which role this instance runs in, whether its listeners are bound, every host the ACME HostPolicy will issue a certificate for (the union across ALL orgs of TLS-marked routes and configured extraHosts, because one process holds one certificate cache), and the ACME directory and account email the process was started with.
     */
    async getIngressTlsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IngressTLS>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/tls`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IngressTLSFromJSON(jsonValue));
    }

    /**
     * GetTLS returns the caller org\'s ACME intent together with the edge-wide TLS facts it lands in: which role this instance runs in, whether its listeners are bound, every host the ACME HostPolicy will issue a certificate for (the union across ALL orgs of TLS-marked routes and configured extraHosts, because one process holds one certificate cache), and the ACME directory and account email the process was started with.
     * GetTLS returns the caller org\'s ACME intent together with the edge-wide TLS facts it lands in: which role this instance runs in, whether its listeners are bound, every host the ACME HostPolicy will issue a certificate for (the union across ALL orgs of TLS-marked routes and configured extraHosts, because one process holds one certificate cache), and the ACME directory and account email the process was started with.
     */
    async getIngressTls(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IngressTLS> {
        const response = await this.getIngressTlsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one edge transform and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. type must be one of redirectScheme, stripPrefix, addPrefix or headers, and stripPrefix/addPrefix each require their config key.
     * Creates or replaces one edge transform and hot-applies it.
     */
    async postIngressMiddlewaresRaw(requestParameters: IngressApiPostIngressMiddlewaresRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelMiddleware>> {
        if (requestParameters['modelMiddleware'] == null) {
            throw new runtime.RequiredError(
                'modelMiddleware',
                'Required parameter "modelMiddleware" was null or undefined when calling postIngressMiddlewares().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/middlewares`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ModelMiddlewareToJSON(requestParameters['modelMiddleware']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelMiddlewareFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one edge transform and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. type must be one of redirectScheme, stripPrefix, addPrefix or headers, and stripPrefix/addPrefix each require their config key.
     * Creates or replaces one edge transform and hot-applies it.
     */
    async postIngressMiddlewares(requestParameters: IngressApiPostIngressMiddlewaresRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelMiddleware> {
        const response = await this.postIngressMiddlewaresRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A route\'s host is a GLOBALLY unique DNS claim: a host another org\'s route already holds is refused 409, so no tenant can hijack another\'s hostname.
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart.
     */
    async postIngressRoutesRaw(requestParameters: IngressApiPostIngressRoutesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Route>> {
        if (requestParameters['route'] == null) {
            throw new runtime.RequiredError(
                'route',
                'Required parameter "route" was null or undefined when calling postIngressRoutes().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/routes`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RouteToJSON(requestParameters['route']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RouteFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A route\'s host is a GLOBALLY unique DNS claim: a host another org\'s route already holds is refused 409, so no tenant can hijack another\'s hostname.
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart.
     */
    async postIngressRoutes(requestParameters: IngressApiPostIngressRoutesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Route> {
        const response = await this.postIngressRoutesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one backend pool and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A pool needs at least one backend and every backend URL must be http(s)://host[:port].
     * Creates or replaces one backend pool and hot-applies it.
     */
    async postIngressServicesRaw(requestParameters: IngressApiPostIngressServicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Upstream>> {
        if (requestParameters['upstream'] == null) {
            throw new runtime.RequiredError(
                'upstream',
                'Required parameter "upstream" was null or undefined when calling postIngressServices().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/services`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: UpstreamToJSON(requestParameters['upstream']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UpstreamFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one backend pool and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A pool needs at least one backend and every backend URL must be http(s)://host[:port].
     * Creates or replaces one backend pool and hot-applies it.
     */
    async postIngressServices(requestParameters: IngressApiPostIngressServicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Upstream> {
        const response = await this.postIngressServicesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one edge transform and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. type must be one of redirectScheme, stripPrefix, addPrefix or headers, and stripPrefix/addPrefix each require their config key.
     * Creates or replaces one edge transform and hot-applies it.
     */
    async putIngressMiddlewaresByIdRaw(requestParameters: IngressApiPutIngressMiddlewaresByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelMiddleware>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putIngressMiddlewaresById().'
            );
        }

        if (requestParameters['modelMiddleware'] == null) {
            throw new runtime.RequiredError(
                'modelMiddleware',
                'Required parameter "modelMiddleware" was null or undefined when calling putIngressMiddlewaresById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/middlewares/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ModelMiddlewareToJSON(requestParameters['modelMiddleware']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelMiddlewareFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one edge transform and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. type must be one of redirectScheme, stripPrefix, addPrefix or headers, and stripPrefix/addPrefix each require their config key.
     * Creates or replaces one edge transform and hot-applies it.
     */
    async putIngressMiddlewaresById(requestParameters: IngressApiPutIngressMiddlewaresByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelMiddleware> {
        const response = await this.putIngressMiddlewaresByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A route\'s host is a GLOBALLY unique DNS claim: a host another org\'s route already holds is refused 409, so no tenant can hijack another\'s hostname.
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart.
     */
    async putIngressRoutesByIdRaw(requestParameters: IngressApiPutIngressRoutesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Route>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putIngressRoutesById().'
            );
        }

        if (requestParameters['route'] == null) {
            throw new runtime.RequiredError(
                'route',
                'Required parameter "route" was null or undefined when calling putIngressRoutesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/routes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: RouteToJSON(requestParameters['route']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RouteFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A route\'s host is a GLOBALLY unique DNS claim: a host another org\'s route already holds is refused 409, so no tenant can hijack another\'s hostname.
     * Creates or replaces one routing rule and hot-applies the new table — there is no config file and no restart.
     */
    async putIngressRoutesById(requestParameters: IngressApiPutIngressRoutesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Route> {
        const response = await this.putIngressRoutesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces one backend pool and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A pool needs at least one backend and every backend URL must be http(s)://host[:port].
     * Creates or replaces one backend pool and hot-applies it.
     */
    async putIngressServicesByIdRaw(requestParameters: IngressApiPutIngressServicesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Upstream>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putIngressServicesById().'
            );
        }

        if (requestParameters['upstream'] == null) {
            throw new runtime.RequiredError(
                'upstream',
                'Required parameter "upstream" was null or undefined when calling putIngressServicesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/services/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpstreamToJSON(requestParameters['upstream']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UpstreamFromJSON(jsonValue));
    }

    /**
     * Creates or replaces one backend pool and hot-applies it. POST mints an id when the body omits one; PUT takes the id from the URL, which wins over any id in the body. A pool needs at least one backend and every backend URL must be http(s)://host[:port].
     * Creates or replaces one backend pool and hot-applies it.
     */
    async putIngressServicesById(requestParameters: IngressApiPutIngressServicesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Upstream> {
        const response = await this.putIngressServicesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * PutTLS replaces the caller org\'s ACME intent and hot-applies what can be hot-applied. extraHosts are normalized and validated, then feed the ACME HostPolicy on the reload this op performs, alongside the per-route tls flags. acmeEmail and staging bind an ACME account for the lifetime of an edge process, so they only take effect when the edge (re)starts — the returned note says so.
     * PutTLS replaces the caller org\'s ACME intent and hot-applies what can be hot-applied.
     */
    async putIngressTlsRaw(requestParameters: IngressApiPutIngressTlsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TLSConfig>> {
        if (requestParameters['tLSConfig'] == null) {
            throw new runtime.RequiredError(
                'tLSConfig',
                'Required parameter "tLSConfig" was null or undefined when calling putIngressTls().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ingress/tls`;

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: TLSConfigToJSON(requestParameters['tLSConfig']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TLSConfigFromJSON(jsonValue));
    }

    /**
     * PutTLS replaces the caller org\'s ACME intent and hot-applies what can be hot-applied. extraHosts are normalized and validated, then feed the ACME HostPolicy on the reload this op performs, alongside the per-route tls flags. acmeEmail and staging bind an ACME account for the lifetime of an edge process, so they only take effect when the edge (re)starts — the returned note says so.
     * PutTLS replaces the caller org\'s ACME intent and hot-applies what can be hot-applied.
     */
    async putIngressTls(requestParameters: IngressApiPutIngressTlsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TLSConfig> {
        const response = await this.putIngressTlsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
