/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  TrailPage,
} from '../models/index.js';
import {
    TrailPageFromJSON,
    TrailPageToJSON,
} from '../models/index.js';

export interface AuditApiGetAuditRequest {
    sub?: string;
    action?: string;
    resource?: string;
    resourceId?: string;
    result?: string;
    since?: string;
    until?: string;
    pageSize?: string;
    p?: string;
}

/**
 * 
 */
export class AuditApi extends runtime.BaseAPI {

    /**
     * List reads the caller\'s OWN org audit trail, newest first, with the total the filter matched so a console can page it.  Every filter is optional and applies WITHIN the caller\'s org — the org itself is the validated principal\'s and can never be widened by a request. Fails closed: an absent principal is a true \"not signed in\" (401), and a deployment with no local tamper-evident store answers an honest 501 rather than silently serving somebody else\'s trail.
     * List reads the caller\'s OWN org audit trail, newest first, with the total the filter matched so a console can page it.
     */
    async getAuditRaw(requestParameters: AuditApiGetAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrailPage>> {
        const queryParameters: any = {};

        if (requestParameters['sub'] != null) {
            queryParameters['sub'] = requestParameters['sub'];
        }

        if (requestParameters['action'] != null) {
            queryParameters['action'] = requestParameters['action'];
        }

        if (requestParameters['resource'] != null) {
            queryParameters['resource'] = requestParameters['resource'];
        }

        if (requestParameters['resourceId'] != null) {
            queryParameters['resourceId'] = requestParameters['resourceId'];
        }

        if (requestParameters['result'] != null) {
            queryParameters['result'] = requestParameters['result'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['until'] != null) {
            queryParameters['until'] = requestParameters['until'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        if (requestParameters['p'] != null) {
            queryParameters['p'] = requestParameters['p'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/audit`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrailPageFromJSON(jsonValue));
    }

    /**
     * List reads the caller\'s OWN org audit trail, newest first, with the total the filter matched so a console can page it.  Every filter is optional and applies WITHIN the caller\'s org — the org itself is the validated principal\'s and can never be widened by a request. Fails closed: an absent principal is a true \"not signed in\" (401), and a deployment with no local tamper-evident store answers an honest 501 rather than silently serving somebody else\'s trail.
     * List reads the caller\'s OWN org audit trail, newest first, with the total the filter matched so a console can page it.
     */
    async getAudit(requestParameters: AuditApiGetAuditRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrailPage> {
        const response = await this.getAuditRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
