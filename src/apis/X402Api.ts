/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Receipt,
} from '../models/index.js';
import {
    ReceiptFromJSON,
    ReceiptToJSON,
} from '../models/index.js';

export interface X402ApiGetX402SettlementsByIdRequest {
    id: string;
}

/**
 * 
 */
export class X402Api extends runtime.BaseAPI {

    /**
     * Settlement reads one x402 payment receipt by id.  It is scoped to the caller\'s PAYER org — the ledger that was debited — so one tenant can never read another\'s settlement, and an id that exists but belongs to somebody else is a 404 exactly like one that does not exist. A caller with no billable identity is refused outright.
     * Settlement reads one x402 payment receipt by id.
     */
    async getX402SettlementsByIdRaw(requestParameters: X402ApiGetX402SettlementsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Receipt>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getX402SettlementsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/x402/settlements/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReceiptFromJSON(jsonValue));
    }

    /**
     * Settlement reads one x402 payment receipt by id.  It is scoped to the caller\'s PAYER org — the ledger that was debited — so one tenant can never read another\'s settlement, and an id that exists but belongs to somebody else is a 404 exactly like one that does not exist. A caller with no billable identity is refused outright.
     * Settlement reads one x402 payment receipt by id.
     */
    async getX402SettlementsById(requestParameters: X402ApiGetX402SettlementsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Receipt> {
        const response = await this.getX402SettlementsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
