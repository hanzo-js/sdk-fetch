/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ArtifactOut,
  ArtifactsOut,
  ExperimentsOut,
  GrantOut,
  GrantRequest,
  IngestOut,
  IngestRequest,
  ProjectsOut,
  ResearchArtifact,
  ResearchTotals,
} from '../models/index.js';
import {
    ArtifactOutFromJSON,
    ArtifactOutToJSON,
    ArtifactsOutFromJSON,
    ArtifactsOutToJSON,
    ExperimentsOutFromJSON,
    ExperimentsOutToJSON,
    GrantOutFromJSON,
    GrantOutToJSON,
    GrantRequestFromJSON,
    GrantRequestToJSON,
    IngestOutFromJSON,
    IngestOutToJSON,
    IngestRequestFromJSON,
    IngestRequestToJSON,
    ProjectsOutFromJSON,
    ProjectsOutToJSON,
    ResearchArtifactFromJSON,
    ResearchArtifactToJSON,
    ResearchTotalsFromJSON,
    ResearchTotalsToJSON,
} from '../models/index.js';

export interface ResearchApiGetResearchArtifactsRequest {
    project?: string;
    run?: string;
    since?: number;
}

export interface ResearchApiGetResearchArtifactsBySha256Request {
    sha256: string;
}

export interface ResearchApiGetResearchExperimentsRequest {
    project?: string;
    kind?: string;
}

export interface ResearchApiGetResearchTotalsRequest {
    project?: string;
}

export interface ResearchApiPostResearchArtifactsRequest {
    researchArtifact: ResearchArtifact;
}

export interface ResearchApiPostResearchExperimentsRequest {
    ingestRequest: IngestRequest;
}

export interface ResearchApiPostResearchGrantsRequest {
    grantRequest: GrantRequest;
}

/**
 * 
 */
export class ResearchApi extends runtime.BaseAPI {

    /**
     * Returns the caller org\'s research-diary feed newest-first — the snapshots and reports tied to its runs, as metadata and content addresses; the bytes themselves are fetched by hash. ?run= narrows to one run, ?project= to one project (default the caller\'s project scope), and ?since= to a unix second.
     * Returns the caller org\'s research-diary feed newest-first — the snapshots and reports tied to its runs, as metadata and content addresses; the bytes themselves are fetched by hash.
     */
    async getResearchArtifactsRaw(requestParameters: ResearchApiGetResearchArtifactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactsOut>> {
        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['run'] != null) {
            queryParameters['run'] = requestParameters['run'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/artifacts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactsOutFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s research-diary feed newest-first — the snapshots and reports tied to its runs, as metadata and content addresses; the bytes themselves are fetched by hash. ?run= narrows to one run, ?project= to one project (default the caller\'s project scope), and ?since= to a unix second.
     * Returns the caller org\'s research-diary feed newest-first — the snapshots and reports tied to its runs, as metadata and content addresses; the bytes themselves are fetched by hash.
     */
    async getResearchArtifacts(requestParameters: ResearchApiGetResearchArtifactsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactsOut> {
        const response = await this.getResearchArtifactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Streams the artifact\'s stored bytes — the retrieval half of hash-addressing, where the diary feed hands out hashes and this hands back what they name. The Content-Type is image/png when the artifact was recorded as a snapshot and application/octet-stream otherwise; it comes from the recorded KIND, not from sniffing the bytes, so an artifact filed as a report always arrives as opaque bytes.  The hash is an address, and the read is NOT global. The store file IS the org, so the same bytes recorded by two tenants are two artifacts, and a hash that exists but belongs to somebody else is a 404 exactly like one that was never recorded — knowing a content hash is never enough to read it. A caller with no validated org is refused 403 outright.  Project narrows further INSIDE that org: the artifact\'s project must equal the caller\'s, which is `?project=` when given and otherwise the caller\'s own project scope, defaulting to the default project. So an artifact filed under a named project is not found until the caller names that project — a mismatch is the same 404 an unknown hash gets, never a distinguishable refusal.  The address can be trusted because the WRITE derived it: the server hashes the bytes it stores, inside the trust boundary, and refuses a client-supplied sha256 that disagrees with them, so poisoning a first write would take a preimage. This read does not re-hash — it looks the hash up as a key.  One shape to expect: this route writes its errors IN-BAND as {\"error\": …} at the real status code, not the {status, error} envelope the typed ops beside it return. It is mounted under an error-flattening filter that would otherwise rewrite its 4xx, so the body is written before that filter runs. A store that cannot be opened is a 500.
     * Fetch one recorded artifact\'s bytes by its content hash.
     */
    async getResearchArtifactsBySha256Raw(requestParameters: ResearchApiGetResearchArtifactsBySha256Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['sha256'] == null) {
            throw new runtime.RequiredError(
                'sha256',
                'Required parameter "sha256" was null or undefined when calling getResearchArtifactsBySha256().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/artifacts/{sha256}`;
        urlPath = urlPath.replace(`{${"sha256"}}`, encodeURIComponent(String(requestParameters['sha256'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Streams the artifact\'s stored bytes — the retrieval half of hash-addressing, where the diary feed hands out hashes and this hands back what they name. The Content-Type is image/png when the artifact was recorded as a snapshot and application/octet-stream otherwise; it comes from the recorded KIND, not from sniffing the bytes, so an artifact filed as a report always arrives as opaque bytes.  The hash is an address, and the read is NOT global. The store file IS the org, so the same bytes recorded by two tenants are two artifacts, and a hash that exists but belongs to somebody else is a 404 exactly like one that was never recorded — knowing a content hash is never enough to read it. A caller with no validated org is refused 403 outright.  Project narrows further INSIDE that org: the artifact\'s project must equal the caller\'s, which is `?project=` when given and otherwise the caller\'s own project scope, defaulting to the default project. So an artifact filed under a named project is not found until the caller names that project — a mismatch is the same 404 an unknown hash gets, never a distinguishable refusal.  The address can be trusted because the WRITE derived it: the server hashes the bytes it stores, inside the trust boundary, and refuses a client-supplied sha256 that disagrees with them, so poisoning a first write would take a preimage. This read does not re-hash — it looks the hash up as a key.  One shape to expect: this route writes its errors IN-BAND as {\"error\": …} at the real status code, not the {status, error} envelope the typed ops beside it return. It is mounted under an error-flattening filter that would otherwise rewrite its 4xx, so the body is written before that filter runs. A store that cannot be opened is a 500.
     * Fetch one recorded artifact\'s bytes by its content hash.
     */
    async getResearchArtifactsBySha256(requestParameters: ResearchApiGetResearchArtifactsBySha256Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getResearchArtifactsBySha256Raw(requestParameters, initOverrides);
    }

    /**
     * Returns the caller org\'s CANONICAL experiments — the deterministic deduped view over the versioned history. With no ?project= it reads the org\'s whole set across projects (the ops board\'s cross-project view, since a project is a sub-scope of the one tenant); ?project= narrows to one and ?kind= to one discriminator.
     * Returns the caller org\'s CANONICAL experiments — the deterministic deduped view over the versioned history.
     */
    async getResearchExperimentsRaw(requestParameters: ResearchApiGetResearchExperimentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExperimentsOut>> {
        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/experiments`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExperimentsOutFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s CANONICAL experiments — the deterministic deduped view over the versioned history. With no ?project= it reads the org\'s whole set across projects (the ops board\'s cross-project view, since a project is a sub-scope of the one tenant); ?project= narrows to one and ?kind= to one discriminator.
     * Returns the caller org\'s CANONICAL experiments — the deterministic deduped view over the versioned history.
     */
    async getResearchExperiments(requestParameters: ResearchApiGetResearchExperimentsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExperimentsOut> {
        const response = await this.getResearchExperimentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every research project in the caller\'s org with its real totals — canonical and retained side by side — which is the ops board\'s \"every project + real totals\" view.
     * Returns every research project in the caller\'s org with its real totals — canonical and retained side by side — which is the ops board\'s \"every project + real totals\" view.
     */
    async getResearchProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectsOutFromJSON(jsonValue));
    }

    /**
     * Returns every research project in the caller\'s org with its real totals — canonical and retained side by side — which is the ops board\'s \"every project + real totals\" view.
     * Returns every research project in the caller\'s org with its real totals — canonical and retained side by side — which is the ops board\'s \"every project + real totals\" view.
     */
    async getResearchProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectsOut> {
        const response = await this.getResearchProjectsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s headline aggregate plus a per-kind breakdown — the observatory\'s poll target. Canonical and retained counts travel together, so a deduped view never reads as loss. ?project= narrows to one project.
     * Returns the caller org\'s headline aggregate plus a per-kind breakdown — the observatory\'s poll target.
     */
    async getResearchTotalsRaw(requestParameters: ResearchApiGetResearchTotalsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResearchTotals>> {
        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/totals`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResearchTotalsFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s headline aggregate plus a per-kind breakdown — the observatory\'s poll target. Canonical and retained counts travel together, so a deduped view never reads as loss. ?project= narrows to one project.
     * Returns the caller org\'s headline aggregate plus a per-kind breakdown — the observatory\'s poll target.
     */
    async getResearchTotals(requestParameters: ResearchApiGetResearchTotalsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResearchTotals> {
        const response = await this.getResearchTotalsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records one research-diary artifact — a board snapshot or a generated report — CONTENT-ADDRESSED inside the trust boundary. The caller submits the bytes as base64 `content`; the SERVER hashes them and THAT hash is the identity and the ref, so the address can never be poisoned by a client-asserted one. A client-supplied sha256, if present, must match the bytes. The project is the SERVER\'s value and visibility is forced private. Re-posting the same bytes is a no-op that reports created=false.
     * Records one research-diary artifact — a board snapshot or a generated report — CONTENT-ADDRESSED inside the trust boundary.
     */
    async postResearchArtifactsRaw(requestParameters: ResearchApiPostResearchArtifactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        if (requestParameters['researchArtifact'] == null) {
            throw new runtime.RequiredError(
                'researchArtifact',
                'Required parameter "researchArtifact" was null or undefined when calling postResearchArtifacts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/artifacts`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResearchArtifactToJSON(requestParameters['researchArtifact']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Records one research-diary artifact — a board snapshot or a generated report — CONTENT-ADDRESSED inside the trust boundary. The caller submits the bytes as base64 `content`; the SERVER hashes them and THAT hash is the identity and the ref, so the address can never be poisoned by a client-asserted one. A client-supplied sha256, if present, must match the bytes. The project is the SERVER\'s value and visibility is forced private. Re-posting the same bytes is a no-op that reports created=false.
     * Records one research-diary artifact — a board snapshot or a generated report — CONTENT-ADDRESSED inside the trust boundary.
     */
    async postResearchArtifacts(requestParameters: ResearchApiPostResearchArtifactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.postResearchArtifactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Appends one batch of experiment and attempt versions to the caller org\'s evidence store, idempotently by content, then rolls it up to the analytics plane best-effort. The project is the SERVER\'s value and visibility is forced private — an upload grants no training or publication right, which is a separate call. A run carrying a BYO endpoint is SSRF-checked before the store is touched. The answer carries BOTH the canonical (deduped) and retained (full history) counts, so a caller sees the versioned truth rather than a dedup that reads as loss.
     * Appends one batch of experiment and attempt versions to the caller org\'s evidence store, idempotently by content, then rolls it up to the analytics plane best-effort.
     */
    async postResearchExperimentsRaw(requestParameters: ResearchApiPostResearchExperimentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IngestOut>> {
        if (requestParameters['ingestRequest'] == null) {
            throw new runtime.RequiredError(
                'ingestRequest',
                'Required parameter "ingestRequest" was null or undefined when calling postResearchExperiments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/experiments`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: IngestRequestToJSON(requestParameters['ingestRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IngestOutFromJSON(jsonValue));
    }

    /**
     * Appends one batch of experiment and attempt versions to the caller org\'s evidence store, idempotently by content, then rolls it up to the analytics plane best-effort. The project is the SERVER\'s value and visibility is forced private — an upload grants no training or publication right, which is a separate call. A run carrying a BYO endpoint is SSRF-checked before the store is touched. The answer carries BOTH the canonical (deduped) and retained (full history) counts, so a caller sees the versioned truth rather than a dedup that reads as loss.
     * Appends one batch of experiment and attempt versions to the caller org\'s evidence store, idempotently by content, then rolls it up to the analytics plane best-effort.
     */
    async postResearchExperiments(requestParameters: ResearchApiPostResearchExperimentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IngestOut> {
        const response = await this.postResearchExperimentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records the SEPARATE authorization an upload never implies: a record\'s visibility (private, org or public) and, for a run, its training and commons-publication consent. Address a run by its stable id or an artifact by its sha256; an artifact grant sets visibility only. The ORG is the tenant boundary and comes from the validated principal, so a caller can only ever grant within its own org; `project` locates WHICH record inside it and defaults to the caller\'s project scope.
     * Records the SEPARATE authorization an upload never implies: a record\'s visibility (private, org or public) and, for a run, its training and commons-publication consent.
     */
    async postResearchGrantsRaw(requestParameters: ResearchApiPostResearchGrantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantOut>> {
        if (requestParameters['grantRequest'] == null) {
            throw new runtime.RequiredError(
                'grantRequest',
                'Required parameter "grantRequest" was null or undefined when calling postResearchGrants().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/research/grants`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GrantRequestToJSON(requestParameters['grantRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantOutFromJSON(jsonValue));
    }

    /**
     * Records the SEPARATE authorization an upload never implies: a record\'s visibility (private, org or public) and, for a run, its training and commons-publication consent. Address a run by its stable id or an artifact by its sha256; an artifact grant sets visibility only. The ORG is the tenant boundary and comes from the validated principal, so a caller can only ever grant within its own org; `project` locates WHICH record inside it and defaults to the caller\'s project scope.
     * Records the SEPARATE authorization an upload never implies: a record\'s visibility (private, org or public) and, for a run, its training and commons-publication consent.
     */
    async postResearchGrants(requestParameters: ResearchApiPostResearchGrantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantOut> {
        const response = await this.postResearchGrantsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
