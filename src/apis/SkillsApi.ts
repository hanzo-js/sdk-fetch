/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AuthoredSkillList,
  SkillDeleted,
  SkillIn,
  SkillWritten,
  SourceToolList,
} from '../models/index.js';
import {
    AuthoredSkillListFromJSON,
    AuthoredSkillListToJSON,
    SkillDeletedFromJSON,
    SkillDeletedToJSON,
    SkillInFromJSON,
    SkillInToJSON,
    SkillWrittenFromJSON,
    SkillWrittenToJSON,
    SourceToolListFromJSON,
    SourceToolListToJSON,
} from '../models/index.js';

export interface SkillsApiDeleteSkillsByIdRequest {
    id: string;
}

export interface SkillsApiGetSkillsRequest {
    activated?: string;
}

export interface SkillsApiPostSkillsRequest {
    skillIn: SkillIn;
}

/**
 * 
 */
export class SkillsApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s authored skills. Scoped to the caller\'s org, so an id belonging to another tenant is never reached. Removing what is not there is not an error — the caller\'s intent is \"gone\", and it is.
     * Removes one of the caller org\'s authored skills.
     */
    async deleteSkillsByIdRaw(requestParameters: SkillsApiDeleteSkillsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SkillDeleted>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteSkillsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/skills/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SkillDeletedFromJSON(jsonValue));
    }

    /**
     * Removes one of the caller org\'s authored skills. Scoped to the caller\'s org, so an id belonging to another tenant is never reached. Removing what is not there is not an error — the caller\'s intent is \"gone\", and it is.
     * Removes one of the caller org\'s authored skills.
     */
    async deleteSkillsById(requestParameters: SkillsApiDeleteSkillsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SkillDeleted> {
        const response = await this.deleteSkillsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag. A skill is discovery and activation metadata attached to an agent, never called directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed to one source, not a second store: a name a caller sees here is the same entry, with the same activation state, that discovery reports.
     * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag.
     */
    async getSkillsRaw(requestParameters: SkillsApiGetSkillsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SourceToolList>> {
        const queryParameters: any = {};

        if (requestParameters['activated'] != null) {
            queryParameters['activated'] = requestParameters['activated'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/skills`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SourceToolListFromJSON(jsonValue));
    }

    /**
     * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag. A skill is discovery and activation metadata attached to an agent, never called directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed to one source, not a second store: a name a caller sees here is the same entry, with the same activation state, that discovery reports.
     * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag.
     */
    async getSkills(requestParameters: SkillsApiGetSkillsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SourceToolList> {
        const response = await this.getSkillsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller org\'s OWN skills with their SKILL.md bodies. GET /v1/skills is the registry view — the brand\'s catalogue plus this org\'s, with activation flags and no bodies; this is the EDITABLE set, so it carries the content that view omits and nothing the org did not write.
     * Lists the caller org\'s OWN skills with their SKILL.md bodies.
     */
    async getSkillsAuthoredRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthoredSkillList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/skills/authored`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthoredSkillListFromJSON(jsonValue));
    }

    /**
     * Lists the caller org\'s OWN skills with their SKILL.md bodies. GET /v1/skills is the registry view — the brand\'s catalogue plus this org\'s, with activation flags and no bodies; this is the EDITABLE set, so it carries the content that view omits and nothing the org did not write.
     * Lists the caller org\'s OWN skills with their SKILL.md bodies.
     */
    async getSkillsAuthored(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthoredSkillList> {
        const response = await this.getSkillsAuthoredRaw(initOverrides);
        return await response.value();
    }

    /**
     * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record. The id is derived from the name, so writing the same name again REVISES that skill rather than accumulating near-duplicates that would then collide in the registry. An org\'s skills are private to it by construction — they live in a different store from the brand\'s embedded catalogue and have no path into the public gallery — and a brand skill always wins a name collision against an org\'s.
     * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record.
     */
    async postSkillsRaw(requestParameters: SkillsApiPostSkillsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SkillWritten>> {
        if (requestParameters['skillIn'] == null) {
            throw new runtime.RequiredError(
                'skillIn',
                'Required parameter "skillIn" was null or undefined when calling postSkills().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/skills`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SkillInToJSON(requestParameters['skillIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SkillWrittenFromJSON(jsonValue));
    }

    /**
     * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record. The id is derived from the name, so writing the same name again REVISES that skill rather than accumulating near-duplicates that would then collide in the registry. An org\'s skills are private to it by construction — they live in a different store from the brand\'s embedded catalogue and have no path into the public gallery — and a brand skill always wins a name collision against an org\'s.
     * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record.
     */
    async postSkills(requestParameters: SkillsApiPostSkillsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SkillWritten> {
        const response = await this.postSkillsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
