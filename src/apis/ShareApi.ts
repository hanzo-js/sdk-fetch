/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EnableResp,
  SharesOut,
} from '../models/index.js';
import {
    EnableRespFromJSON,
    EnableRespToJSON,
    SharesOutFromJSON,
    SharesOutToJSON,
} from '../models/index.js';

/**
 * 
 */
export class ShareApi extends runtime.BaseAPI {

    /**
     * Returns the tunnel shares the caller\'s org currently has open, across every environment that org has enabled. It is a READ and it degrades honestly: an unconfigured deployment, an org that has not provisioned yet, and an unreachable controller all answer an EMPTY list at 200 rather than an error, so the console never error-toasts on load.
     * Returns the tunnel shares the caller\'s org currently has open, across every environment that org has enabled.
     */
    async getShareRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SharesOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/share`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SharesOutFromJSON(jsonValue));
    }

    /**
     * Returns the tunnel shares the caller\'s org currently has open, across every environment that org has enabled. It is a READ and it degrades honestly: an unconfigured deployment, an org that has not provisioned yet, and an unreachable controller all answer an EMPTY list at 200 rather than an error, so the console never error-toasts on load.
     * Returns the tunnel shares the caller\'s org currently has open, across every environment that org has enabled.
     */
    async getShare(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SharesOut> {
        const response = await this.getShareRaw(initOverrides);
        return await response.value();
    }

    /**
     * Enable provisions the caller org\'s tunnel account and returns the credential the `hanzo share` CLI needs to run a tunnel. It is idempotent: the account is keyed deterministically off the VALIDATED org, so a repeat call hands back the same account rather than creating a second one, and a caller can only ever provision their OWN org\'s account. 503 when the deployment has no share controller configured; 502 when that controller is unreachable.
     * Enable provisions the caller org\'s tunnel account and returns the credential the `hanzo share` CLI needs to run a tunnel.
     */
    async postShareEnableRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EnableResp>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/share/enable`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EnableRespFromJSON(jsonValue));
    }

    /**
     * Enable provisions the caller org\'s tunnel account and returns the credential the `hanzo share` CLI needs to run a tunnel. It is idempotent: the account is keyed deterministically off the VALIDATED org, so a repeat call hands back the same account rather than creating a second one, and a caller can only ever provision their OWN org\'s account. 503 when the deployment has no share controller configured; 502 when that controller is unreachable.
     * Enable provisions the caller org\'s tunnel account and returns the credential the `hanzo share` CLI needs to run a tunnel.
     */
    async postShareEnable(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EnableResp> {
        const response = await this.postShareEnableRaw(initOverrides);
        return await response.value();
    }

}
