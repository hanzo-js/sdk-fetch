/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EngineStatus,
} from '../models/index.js';
import {
    EngineStatusFromJSON,
    EngineStatusToJSON,
} from '../models/index.js';

export interface EngineApiEngineModelRequest {
    model?: string;
}

/**
 * 
 */
export class EngineApi extends runtime.BaseAPI {

    /**
     * Model reads one model\'s load state — loaded, unloading, or not_found, as the engine itself reports it.
     * Read one model\'s load state on the serving runtime
     */
    async engineModelRaw(requestParameters: EngineApiEngineModelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        if (requestParameters['model'] != null) {
            queryParameters['model'] = requestParameters['model'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/engine/model`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Model reads one model\'s load state — loaded, unloading, or not_found, as the engine itself reports it.
     * Read one model\'s load state on the serving runtime
     */
    async engineModel(requestParameters: EngineApiEngineModelRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.engineModelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Models lists the models the engine serves, each with its load state — the server\'s own model table (its standard list envelope, load status included), relayed verbatim.
     * List the models the serving runtime holds, with each one\'s load state
     */
    async engineModelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/engine/models`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Models lists the models the engine serves, each with its load state — the server\'s own model table (its standard list envelope, load status included), relayed verbatim.
     * List the models the serving runtime holds, with each one\'s load state
     */
    async engineModels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.engineModelsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Status reports whether the engine deployment is reachable and which build revision it runs — an honest lens for \"is the serving runtime up\", never a fabricated ok.
     * Whether the serving runtime is reachable, and which build it runs
     */
    async engineStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EngineStatus>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/engine/status`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EngineStatusFromJSON(jsonValue));
    }

    /**
     * Status reports whether the engine deployment is reachable and which build revision it runs — an honest lens for \"is the serving runtime up\", never a fabricated ok.
     * Whether the serving runtime is reachable, and which build it runs
     */
    async engineStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EngineStatus> {
        const response = await this.engineStatusRaw(initOverrides);
        return await response.value();
    }

    /**
     * System reads the engine host\'s inventory: OS, CPU, memory, every accelerator device with its VRAM and compute capability, and the build\'s capabilities (CUDA/Metal/flash-attention) — the real hardware under the serving runtime, relayed verbatim.
     * The serving host\'s own inventory: devices, memory and build capabilities
     */
    async engineSystemRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/engine/system`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * System reads the engine host\'s inventory: OS, CPU, memory, every accelerator device with its VRAM and compute capability, and the build\'s capabilities (CUDA/Metal/flash-attention) — the real hardware under the serving runtime, relayed verbatim.
     * The serving host\'s own inventory: devices, memory and build capabilities
     */
    async engineSystem(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.engineSystemRaw(initOverrides);
        return await response.value();
    }

}
