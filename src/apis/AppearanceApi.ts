/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Appearance,
} from '../models/index.js';
import {
    AppearanceFromJSON,
    AppearanceToJSON,
} from '../models/index.js';

export interface AppearanceApiPostAppearanceRequest {
    appearance: Appearance;
}

/**
 * 
 */
export class AppearanceApi extends runtime.BaseAPI {

    /**
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
     */
    async getAppearanceRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Appearance>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/appearance`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppearanceFromJSON(jsonValue));
    }

    /**
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
     */
    async getAppearance(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Appearance> {
        const response = await this.getAppearanceRaw(initOverrides);
        return await response.value();
    }

    /**
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
     */
    async postAppearanceRaw(requestParameters: AppearanceApiPostAppearanceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Appearance>> {
        if (requestParameters['appearance'] == null) {
            throw new runtime.RequiredError(
                'appearance',
                'Required parameter "appearance" was null or undefined when calling postAppearance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/appearance`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AppearanceToJSON(requestParameters['appearance']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppearanceFromJSON(jsonValue));
    }

    /**
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
     */
    async postAppearance(requestParameters: AppearanceApiPostAppearanceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Appearance> {
        const response = await this.postAppearanceRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
