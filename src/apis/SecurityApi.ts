/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  FindingList,
  FindingView,
  RuleList,
  Ruleset,
  ScanDetail,
  ScanList,
  ScanView,
  SubmitReq,
} from '../models/index.js';
import {
    FindingListFromJSON,
    FindingListToJSON,
    FindingViewFromJSON,
    FindingViewToJSON,
    RuleListFromJSON,
    RuleListToJSON,
    RulesetFromJSON,
    RulesetToJSON,
    ScanDetailFromJSON,
    ScanDetailToJSON,
    ScanListFromJSON,
    ScanListToJSON,
    ScanViewFromJSON,
    ScanViewToJSON,
    SubmitReqFromJSON,
    SubmitReqToJSON,
} from '../models/index.js';

export interface SecurityApiGetSecurityFindingsRequest {
    scanId?: string;
    minSeverity?: string;
    limit?: number;
}

export interface SecurityApiGetSecurityFindingsByIdRequest {
    id: string;
}

export interface SecurityApiGetSecurityScansRequest {
    limit?: number;
}

export interface SecurityApiGetSecurityScansByIdRequest {
    id: string;
}

export interface SecurityApiPostSecurityScansRequest {
    submitReq: SubmitReq;
}

/**
 * 
 */
export class SecurityApi extends runtime.BaseAPI {

    /**
     * Is the org\'s findings — rule, severity, path, line, masked preview and fingerprint — newest first, across scans or within one.  A minSeverity outside critical|high|medium|low is refused rather than quietly ignored, so a filter typo cannot read as \"no findings\". Strictly org-scoped, and a caller with no validated org is refused.
     * Is the org\'s findings — rule, severity, path, line, masked preview and fingerprint — newest first, across scans or within one.
     */
    async getSecurityFindingsRaw(requestParameters: SecurityApiGetSecurityFindingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FindingList>> {
        const queryParameters: any = {};

        if (requestParameters['scanId'] != null) {
            queryParameters['scanId'] = requestParameters['scanId'];
        }

        if (requestParameters['minSeverity'] != null) {
            queryParameters['minSeverity'] = requestParameters['minSeverity'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/findings`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FindingListFromJSON(jsonValue));
    }

    /**
     * Is the org\'s findings — rule, severity, path, line, masked preview and fingerprint — newest first, across scans or within one.  A minSeverity outside critical|high|medium|low is refused rather than quietly ignored, so a filter typo cannot read as \"no findings\". Strictly org-scoped, and a caller with no validated org is refused.
     * Is the org\'s findings — rule, severity, path, line, masked preview and fingerprint — newest first, across scans or within one.
     */
    async getSecurityFindings(requestParameters: SecurityApiGetSecurityFindingsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FindingList> {
        const response = await this.getSecurityFindingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a single finding: which rule fired, where (path and line), the masked preview and the SHA-256 fingerprint of the secret — the raw secret is not stored and cannot be read back.  Scoped to the caller\'s org, and a finding belonging to another org is the same 404 as one that never existed.
     * Returns a single finding: which rule fired, where (path and line), the masked preview and the SHA-256 fingerprint of the secret — the raw secret is not stored and cannot be read back.
     */
    async getSecurityFindingsByIdRaw(requestParameters: SecurityApiGetSecurityFindingsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FindingView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSecurityFindingsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/findings/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FindingViewFromJSON(jsonValue));
    }

    /**
     * Returns a single finding: which rule fired, where (path and line), the masked preview and the SHA-256 fingerprint of the secret — the raw secret is not stored and cannot be read back.  Scoped to the caller\'s org, and a finding belonging to another org is the same 404 as one that never existed.
     * Returns a single finding: which rule fired, where (path and line), the masked preview and the SHA-256 fingerprint of the secret — the raw secret is not stored and cannot be read back.
     */
    async getSecurityFindingsById(requestParameters: SecurityApiGetSecurityFindingsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FindingView> {
        const response = await this.getSecurityFindingsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports that the scanning subsystem is serving and how many secret-detection rules the engine holds.  It has no external dependency — the answer is ok whenever the findings store opened — so it measures this process rather than anything downstream. It reads no tenant: a prober that sends no principal is answered, not refused.
     * Reports that the scanning subsystem is serving and how many secret-detection rules the engine holds.
     */
    async getSecurityHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Ruleset>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RulesetFromJSON(jsonValue));
    }

    /**
     * Reports that the scanning subsystem is serving and how many secret-detection rules the engine holds.  It has no external dependency — the answer is ok whenever the findings store opened — so it measures this process rather than anything downstream. It reads no tenant: a prober that sends no principal is answered, not refused.
     * Reports that the scanning subsystem is serving and how many secret-detection rules the engine holds.
     */
    async getSecurityHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Ruleset> {
        const response = await this.getSecurityHealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * Is the secret-detection catalog the engine scans with.  It returns every rule a scan can fire — the id, name and severity a finding cites — so a caller can render or triage results without hard-coding the catalog. It is the same for everyone and discloses nothing tenant-specific, so it carries no org scope.
     * Is the secret-detection catalog the engine scans with.
     */
    async getSecurityRulesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RuleList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/rules`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RuleListFromJSON(jsonValue));
    }

    /**
     * Is the secret-detection catalog the engine scans with.  It returns every rule a scan can fire — the id, name and severity a finding cites — so a caller can render or triage results without hard-coding the catalog. It is the same for everyone and discloses nothing tenant-specific, so it carries no org scope.
     * Is the secret-detection catalog the engine scans with.
     */
    async getSecurityRules(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RuleList> {
        const response = await this.getSecurityRulesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Is the org\'s scan history, newest first, each as the same summary the submission answered — files read, findings fired, tally by severity.  Strictly org-scoped: a caller only ever sees its own scans, and one with no validated org is refused.
     * Is the org\'s scan history, newest first, each as the same summary the submission answered — files read, findings fired, tally by severity.
     */
    async getSecurityScansRaw(requestParameters: SecurityApiGetSecurityScansRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScanList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/scans`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScanListFromJSON(jsonValue));
    }

    /**
     * Is the org\'s scan history, newest first, each as the same summary the submission answered — files read, findings fired, tally by severity.  Strictly org-scoped: a caller only ever sees its own scans, and one with no validated org is refused.
     * Is the org\'s scan history, newest first, each as the same summary the submission answered — files read, findings fired, tally by severity.
     */
    async getSecurityScans(requestParameters: SecurityApiGetSecurityScansRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScanList> {
        const response = await this.getSecurityScansRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one scan together with every finding on it, so the detail view is one round-trip rather than a list call per scan. The findings carry masked previews and fingerprints, never secrets.  Scoped to the caller\'s org: a scan id belonging to another org is the same 404 as an id that never existed, so a ruleset learns nothing about what exists elsewhere. No validated org is refused.
     * Returns one scan together with every finding on it, so the detail view is one round-trip rather than a list call per scan.
     */
    async getSecurityScansByIdRaw(requestParameters: SecurityApiGetSecurityScansByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScanDetail>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSecurityScansById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/scans/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScanDetailFromJSON(jsonValue));
    }

    /**
     * Returns one scan together with every finding on it, so the detail view is one round-trip rather than a list call per scan. The findings carry masked previews and fingerprints, never secrets.  Scoped to the caller\'s org: a scan id belonging to another org is the same 404 as an id that never existed, so a ruleset learns nothing about what exists elsewhere. No validated org is refused.
     * Returns one scan together with every finding on it, so the detail view is one round-trip rather than a list call per scan.
     */
    async getSecurityScansById(requestParameters: SecurityApiGetSecurityScansByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScanDetail> {
        const response = await this.getSecurityScansByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs the detection engine over a batch of files and answers 201 with the scan summary: how many files were read, how many findings fired, and the tally by severity.  THE SUBMITTED CONTENT IS NEVER STORED. It is scanned in memory; what persists is the finding — its rule, its path and line, a MASKED preview (first and last characters kept, the middle starred) and the SHA-256 fingerprint of the raw secret. The fingerprint is what makes the same secret recognisable across scans and after rotation without the secret ever being written down.  It requires a validated org, which scopes the stored scan and every finding on it; a caller with no org is refused. Bounded at 500 files and 8 MiB of total content per submission — split a larger tree across scans. One scan is one metered unit, and the scan is recorded in the audit log with its tally, never with its findings.
     * Runs the detection engine over a batch of files and answers 201 with the scan summary: how many files were read, how many findings fired, and the tally by severity.
     */
    async postSecurityScansRaw(requestParameters: SecurityApiPostSecurityScansRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScanView>> {
        if (requestParameters['submitReq'] == null) {
            throw new runtime.RequiredError(
                'submitReq',
                'Required parameter "submitReq" was null or undefined when calling postSecurityScans().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/security/scans`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubmitReqToJSON(requestParameters['submitReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScanViewFromJSON(jsonValue));
    }

    /**
     * Runs the detection engine over a batch of files and answers 201 with the scan summary: how many files were read, how many findings fired, and the tally by severity.  THE SUBMITTED CONTENT IS NEVER STORED. It is scanned in memory; what persists is the finding — its rule, its path and line, a MASKED preview (first and last characters kept, the middle starred) and the SHA-256 fingerprint of the raw secret. The fingerprint is what makes the same secret recognisable across scans and after rotation without the secret ever being written down.  It requires a validated org, which scopes the stored scan and every finding on it; a caller with no org is refused. Bounded at 500 files and 8 MiB of total content per submission — split a larger tree across scans. One scan is one metered unit, and the scan is recorded in the audit log with its tally, never with its findings.
     * Runs the detection engine over a batch of files and answers 201 with the scan summary: how many files were read, how many findings fired, and the tally by severity.
     */
    async postSecurityScans(requestParameters: SecurityApiPostSecurityScansRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScanView> {
        const response = await this.postSecurityScansRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
