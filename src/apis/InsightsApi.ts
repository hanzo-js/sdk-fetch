/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EventList,
  InsightsStatus,
} from '../models/index.js';
import {
    EventListFromJSON,
    EventListToJSON,
    InsightsStatusFromJSON,
    InsightsStatusToJSON,
} from '../models/index.js';

export interface InsightsApiGetInsightsEventsRequest {
    limit?: number;
}

/**
 * 
 */
export class InsightsApi extends runtime.BaseAPI {

    /**
     * Returns the caller org\'s most recent product events, newest first. The console\'s raw-event view over event.event — the same table the capture doors fill — one row per stored event, with the row\'s attributes returned as the properties object.  The org is the validated principal\'s — never a parameter — and a read requires a real bearer, never the write-only publishable key. 403 without a validated bearer, 503 when the warehouse is unreachable.
     * Returns the caller org\'s most recent product events, newest first.
     */
    async getInsightsEventsRaw(requestParameters: InsightsApiGetInsightsEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EventList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/insights/events`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EventListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s most recent product events, newest first. The console\'s raw-event view over event.event — the same table the capture doors fill — one row per stored event, with the row\'s attributes returned as the properties object.  The org is the validated principal\'s — never a parameter — and a read requires a real bearer, never the write-only publishable key. 403 without a validated bearer, 503 when the warehouse is unreachable.
     * Returns the caller org\'s most recent product events, newest first.
     */
    async getInsightsEvents(requestParameters: InsightsApiGetInsightsEventsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EventList> {
        const response = await this.getInsightsEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports that the unified insights surface is serving. It reads no tenant data and consults no dependency, so it answers 200 unconditionally and needs no principal — liveness must be probe-able. The warehouse-connectivity probe is a different question and lives at GET /v1/analytics/health.
     * Reports that the unified insights surface is serving.
     */
    async getInsightsHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InsightsStatus>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/insights/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InsightsStatusFromJSON(jsonValue));
    }

    /**
     * Reports that the unified insights surface is serving. It reads no tenant data and consults no dependency, so it answers 200 unconditionally and needs no principal — liveness must be probe-able. The warehouse-connectivity probe is a different question and lives at GET /v1/analytics/health.
     * Reports that the unified insights surface is serving.
     */
    async getInsightsHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InsightsStatus> {
        const response = await this.getInsightsHealthRaw(initOverrides);
        return await response.value();
    }

}
