/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Blob,
  EndIn,
  LeaseIn,
  Leased,
  PathIn,
  Ran,
  RunIn,
  StopIn,
  Stopped,
  WriteIn,
  Wrote,
} from '../models/index.js';
import {
    BlobFromJSON,
    BlobToJSON,
    EndInFromJSON,
    EndInToJSON,
    LeaseInFromJSON,
    LeaseInToJSON,
    LeasedFromJSON,
    LeasedToJSON,
    PathInFromJSON,
    PathInToJSON,
    RanFromJSON,
    RanToJSON,
    RunInFromJSON,
    RunInToJSON,
    StopInFromJSON,
    StopInToJSON,
    StoppedFromJSON,
    StoppedToJSON,
    WriteInFromJSON,
    WriteInToJSON,
    WroteFromJSON,
    WroteToJSON,
} from '../models/index.js';

export interface SandboxesApiDeleteSandboxesByIdRequest {
    id: string;
}

export interface SandboxesApiEndSandboxRequest {
    endIn: EndIn;
}

export interface SandboxesApiGetSandboxesByIdRequest {
    id: string;
}

export interface SandboxesApiGetSandboxesByIdFsRequest {
    id: string;
}

export interface SandboxesApiGetSandboxesByIdScreenRequest {
    id: string;
}

export interface SandboxesApiGetSandboxesByIdScreenWsRequest {
    id: string;
}

export interface SandboxesApiGetSandboxesByIdTerminalRequest {
    id: string;
}

export interface SandboxesApiGetSandboxesByIdTerminalWsRequest {
    id: string;
}

export interface SandboxesApiLeaseSandboxRequest {
    leaseIn: LeaseIn;
}

export interface SandboxesApiPostSandboxesByIdExecRequest {
    id: string;
}

export interface SandboxesApiPostSandboxesByIdFsRequest {
    id: string;
}

export interface SandboxesApiPostSandboxesByIdScreenTicketRequest {
    id: string;
}

export interface SandboxesApiPostSandboxesByIdTerminalTicketRequest {
    id: string;
}

export interface SandboxesApiReadSandboxFileRequest {
    pathIn: PathIn;
}

export interface SandboxesApiRunInSandboxRequest {
    runIn: RunIn;
}

export interface SandboxesApiStopRunRequest {
    stopIn: StopIn;
}

export interface SandboxesApiWriteSandboxFileRequest {
    writeIn: WriteIn;
}

/**
 * 
 */
export class SandboxesApi extends runtime.BaseAPI {

    /**
     * Stops the sandbox\'s pod and drops the lease. The VOLUME survives by default, so a `dev` or `desktop` sandbox can be leased again over the same project and find its checkout where it left it.  `purge=1` deletes the volume too. It is opt-in because it is the one part of this that cannot be undone.
     * End a sandbox
     */
    async deleteSandboxesByIdRaw(requestParameters: SandboxesApiDeleteSandboxesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteSandboxesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Stops the sandbox\'s pod and drops the lease. The VOLUME survives by default, so a `dev` or `desktop` sandbox can be leased again over the same project and find its checkout where it left it.  `purge=1` deletes the volume too. It is opt-in because it is the one part of this that cannot be undone.
     * End a sandbox
     */
    async deleteSandboxesById(requestParameters: SandboxesApiDeleteSandboxesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSandboxesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
     * End a sandbox and release it
     */
    async endSandboxRaw(requestParameters: SandboxesApiEndSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['endIn'] == null) {
            throw new runtime.RequiredError(
                'endIn',
                'Required parameter "endIn" was null or undefined when calling endSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/end`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EndInToJSON(requestParameters['endIn']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
     * End a sandbox and release it
     */
    async endSandbox(requestParameters: SandboxesApiEndSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.endSandboxRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the caller org\'s sandboxes, newest first. `project` and `status` narrow it, and both are read from the QUERY STRING.  It answers from the org\'s own store rather than from the cluster, so a sandbox whose pod has since died still appears, carrying the status it was last known to have. That is deliberate: a lease you are being charged for should not vanish from the list because the thing behind it fell over.
     * The sandboxes this org holds
     */
    async getSandboxesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Lists the caller org\'s sandboxes, newest first. `project` and `status` narrow it, and both are read from the QUERY STRING.  It answers from the org\'s own store rather than from the cluster, so a sandbox whose pod has since died still appears, carrying the status it was last known to have. That is deliberate: a lease you are being charged for should not vanish from the list because the thing behind it fell over.
     * The sandboxes this org holds
     */
    async getSandboxes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesRaw(initOverrides);
    }

    /**
     * Returns one of the caller org\'s sandboxes. An id belonging to another org answers 404 and not 403 — a 403 would confirm the id exists, and whether a given sandbox exists is itself a cross-tenant fact.
     * One sandbox
     */
    async getSandboxesByIdRaw(requestParameters: SandboxesApiGetSandboxesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns one of the caller org\'s sandboxes. An id belonging to another org answers 404 and not 403 — a 403 would confirm the id exists, and whether a given sandbox exists is itself a cross-tenant fact.
     * One sandbox
     */
    async getSandboxesById(requestParameters: SandboxesApiGetSandboxesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
     * Read a file, or list a directory
     */
    async getSandboxesByIdFsRaw(requestParameters: SandboxesApiGetSandboxesByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxesByIdFs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/fs`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
     * Read a file, or list a directory
     */
    async getSandboxesByIdFs(requestParameters: SandboxesApiGetSandboxesByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesByIdFsRaw(requestParameters, initOverrides);
    }

    /**
     * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
     * The screen, as a page
     */
    async getSandboxesByIdScreenRaw(requestParameters: SandboxesApiGetSandboxesByIdScreenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxesByIdScreen().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/screen`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
     * The screen, as a page
     */
    async getSandboxesByIdScreen(requestParameters: SandboxesApiGetSandboxesByIdScreenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesByIdScreenRaw(requestParameters, initOverrides);
    }

    /**
     * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
     * The screen, as a socket
     */
    async getSandboxesByIdScreenWsRaw(requestParameters: SandboxesApiGetSandboxesByIdScreenWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxesByIdScreenWs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/screen/ws`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
     * The screen, as a socket
     */
    async getSandboxesByIdScreenWs(requestParameters: SandboxesApiGetSandboxesByIdScreenWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesByIdScreenWsRaw(requestParameters, initOverrides);
    }

    /**
     * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
     * The terminal, as a page
     */
    async getSandboxesByIdTerminalRaw(requestParameters: SandboxesApiGetSandboxesByIdTerminalRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxesByIdTerminal().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/terminal`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
     * The terminal, as a page
     */
    async getSandboxesByIdTerminal(requestParameters: SandboxesApiGetSandboxesByIdTerminalRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesByIdTerminalRaw(requestParameters, initOverrides);
    }

    /**
     * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
     * The terminal, as a socket
     */
    async getSandboxesByIdTerminalWsRaw(requestParameters: SandboxesApiGetSandboxesByIdTerminalWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxesByIdTerminalWs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/terminal/ws`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
     * The terminal, as a socket
     */
    async getSandboxesByIdTerminalWs(requestParameters: SandboxesApiGetSandboxesByIdTerminalWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxesByIdTerminalWsRaw(requestParameters, initOverrides);
    }

    /**
     * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
     * Lease a sandbox — a real computer — or resume one you hold
     */
    async leaseSandboxRaw(requestParameters: SandboxesApiLeaseSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Leased>> {
        if (requestParameters['leaseIn'] == null) {
            throw new runtime.RequiredError(
                'leaseIn',
                'Required parameter "leaseIn" was null or undefined when calling leaseSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/lease`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LeaseInToJSON(requestParameters['leaseIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LeasedFromJSON(jsonValue));
    }

    /**
     * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
     * Lease a sandbox — a real computer — or resume one you hold
     */
    async leaseSandbox(requestParameters: SandboxesApiLeaseSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Leased> {
        const response = await this.leaseSandboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a sandbox and returns it. `class` is one of `exec`, `dev`, `desktop` or `android`; `dev` and `desktop` are attached to a `project`, which is required for them and names the volume the work persists on. `ttlSec` bounds the lease, and `image` overrides the class default.  This is the ONLY path that creates cluster objects. The isolation boundary is the pod\'s runtime class, one field, so what a sandbox is confined by is a deployment decision rather than anything this operation negotiates.
     * Lease a sandbox
     */
    async postSandboxesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates a sandbox and returns it. `class` is one of `exec`, `dev`, `desktop` or `android`; `dev` and `desktop` are attached to a `project`, which is required for them and names the volume the work persists on. `ttlSec` bounds the lease, and `image` overrides the class default.  This is the ONLY path that creates cluster objects. The isolation boundary is the pod\'s runtime class, one field, so what a sandbox is confined by is a deployment decision rather than anything this operation negotiates.
     * Lease a sandbox
     */
    async postSandboxes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSandboxesRaw(initOverrides);
    }

    /**
     * Runs a command inside the sandbox and returns its exit code, stdout and stderr. A non-zero exit is a SUCCESSFUL call carrying a failed program — the HTTP status stays 200, because \"the tests failed\" and \"the sandbox is broken\" are different facts.  NOTHING RUNS IN cloud. The command is streamed to the Kubernetes exec subresource of the sandbox\'s pod, which runs under the gVisor runtime class. The sandbox is addressed by pod NAME through the apiserver, never by address.
     * Run a command in a sandbox
     */
    async postSandboxesByIdExecRaw(requestParameters: SandboxesApiPostSandboxesByIdExecRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxesByIdExec().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/exec`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Runs a command inside the sandbox and returns its exit code, stdout and stderr. A non-zero exit is a SUCCESSFUL call carrying a failed program — the HTTP status stays 200, because \"the tests failed\" and \"the sandbox is broken\" are different facts.  NOTHING RUNS IN cloud. The command is streamed to the Kubernetes exec subresource of the sandbox\'s pod, which runs under the gVisor runtime class. The sandbox is addressed by pod NAME through the apiserver, never by address.
     * Run a command in a sandbox
     */
    async postSandboxesByIdExec(requestParameters: SandboxesApiPostSandboxesByIdExecRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSandboxesByIdExecRaw(requestParameters, initOverrides);
    }

    /**
     * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
     * Write a file
     */
    async postSandboxesByIdFsRaw(requestParameters: SandboxesApiPostSandboxesByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxesByIdFs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/fs`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
     * Write a file
     */
    async postSandboxesByIdFs(requestParameters: SandboxesApiPostSandboxesByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSandboxesByIdFsRaw(requestParameters, initOverrides);
    }

    /**
     * Mints a SINGLE-USE ticket for this sandbox\'s DISPLAY and returns `{ticket, expiresIn, url}`, where url is the desktop PAGE with the ticket already on it. The same ticket as the terminal\'s, minted for a different door.  A ticket says which org and which sandbox, and the terminal and the screen are two views of one machine: a caller who may type in a sandbox may look at it. What the door decides is the URL handed back, which is the only part that differs.  Mint one per screen, and mint a fresh one to reconnect.
     * Open a screen
     */
    async postSandboxesByIdScreenTicketRaw(requestParameters: SandboxesApiPostSandboxesByIdScreenTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxesByIdScreenTicket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/screen/ticket`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Mints a SINGLE-USE ticket for this sandbox\'s DISPLAY and returns `{ticket, expiresIn, url}`, where url is the desktop PAGE with the ticket already on it. The same ticket as the terminal\'s, minted for a different door.  A ticket says which org and which sandbox, and the terminal and the screen are two views of one machine: a caller who may type in a sandbox may look at it. What the door decides is the URL handed back, which is the only part that differs.  Mint one per screen, and mint a fresh one to reconnect.
     * Open a screen
     */
    async postSandboxesByIdScreenTicket(requestParameters: SandboxesApiPostSandboxesByIdScreenTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSandboxesByIdScreenTicketRaw(requestParameters, initOverrides);
    }

    /**
     * Mints a SINGLE-USE ticket for one interactive terminal in this sandbox and returns `{ticket, expiresIn, url}`, where url is the terminal PAGE with the ticket already on it.  It exists because a browser carries no Authorization header into a WebSocket or an iframe, so a terminal cannot be authenticated the way every other route here is. The ticket is a credential MINTED for that one terminal: bound to this org and this sandbox, valid for thirty seconds, and gone the first time it is presented. A long-lived bearer in a query string would instead be written into every access log on the path.  Mint one per terminal, and mint a fresh one to reconnect.
     * Open a terminal
     */
    async postSandboxesByIdTerminalTicketRaw(requestParameters: SandboxesApiPostSandboxesByIdTerminalTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxesByIdTerminalTicket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/{id}/terminal/ticket`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Mints a SINGLE-USE ticket for one interactive terminal in this sandbox and returns `{ticket, expiresIn, url}`, where url is the terminal PAGE with the ticket already on it.  It exists because a browser carries no Authorization header into a WebSocket or an iframe, so a terminal cannot be authenticated the way every other route here is. The ticket is a credential MINTED for that one terminal: bound to this org and this sandbox, valid for thirty seconds, and gone the first time it is presented. A long-lived bearer in a query string would instead be written into every access log on the path.  Mint one per terminal, and mint a fresh one to reconnect.
     * Open a terminal
     */
    async postSandboxesByIdTerminalTicket(requestParameters: SandboxesApiPostSandboxesByIdTerminalTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSandboxesByIdTerminalTicketRaw(requestParameters, initOverrides);
    }

    /**
     * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
     * Read a file from a sandbox you hold
     */
    async readSandboxFileRaw(requestParameters: SandboxesApiReadSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        if (requestParameters['pathIn'] == null) {
            throw new runtime.RequiredError(
                'pathIn',
                'Required parameter "pathIn" was null or undefined when calling readSandboxFile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/read`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PathInToJSON(requestParameters['pathIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BlobFromJSON(jsonValue));
    }

    /**
     * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
     * Read a file from a sandbox you hold
     */
    async readSandboxFile(requestParameters: SandboxesApiReadSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.readSandboxFileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
     * Run a command in a sandbox you hold and read its output
     */
    async runInSandboxRaw(requestParameters: SandboxesApiRunInSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Ran>> {
        if (requestParameters['runIn'] == null) {
            throw new runtime.RequiredError(
                'runIn',
                'Required parameter "runIn" was null or undefined when calling runInSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/run`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RunInToJSON(requestParameters['runIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RanFromJSON(jsonValue));
    }

    /**
     * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
     * Run a command in a sandbox you hold and read its output
     */
    async runInSandbox(requestParameters: SandboxesApiRunInSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Ran> {
        const response = await this.runInSandboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
     * Stop what a sandbox is running, and keep the sandbox
     */
    async stopRunRaw(requestParameters: SandboxesApiStopRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Stopped>> {
        if (requestParameters['stopIn'] == null) {
            throw new runtime.RequiredError(
                'stopIn',
                'Required parameter "stopIn" was null or undefined when calling stopRun().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/stop`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StopInToJSON(requestParameters['stopIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StoppedFromJSON(jsonValue));
    }

    /**
     * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
     * Stop what a sandbox is running, and keep the sandbox
     */
    async stopRun(requestParameters: SandboxesApiStopRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Stopped> {
        const response = await this.stopRunRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
     * Write a file into a sandbox you hold
     */
    async writeSandboxFileRaw(requestParameters: SandboxesApiWriteSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Wrote>> {
        if (requestParameters['writeIn'] == null) {
            throw new runtime.RequiredError(
                'writeIn',
                'Required parameter "writeIn" was null or undefined when calling writeSandboxFile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandboxes/write`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WriteInToJSON(requestParameters['writeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WroteFromJSON(jsonValue));
    }

    /**
     * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
     * Write a file into a sandbox you hold
     */
    async writeSandboxFile(requestParameters: SandboxesApiWriteSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Wrote> {
        const response = await this.writeSandboxFileRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
