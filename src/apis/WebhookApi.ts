/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CreateEndpointIn,
  DeliveryList,
  Endpoint,
  EndpointList,
  TestResult,
  UpdateEndpointIn,
} from '../models/index.js';
import {
    CreateEndpointInFromJSON,
    CreateEndpointInToJSON,
    DeliveryListFromJSON,
    DeliveryListToJSON,
    EndpointFromJSON,
    EndpointToJSON,
    EndpointListFromJSON,
    EndpointListToJSON,
    TestResultFromJSON,
    TestResultToJSON,
    UpdateEndpointInFromJSON,
    UpdateEndpointInToJSON,
} from '../models/index.js';

export interface WebhookApiDeleteWebhookByIdRequest {
    id: string;
}

export interface WebhookApiGetWebhookByIdRequest {
    id: string;
}

export interface WebhookApiGetWebhookByIdDeliveriesRequest {
    id: string;
    limit?: number;
    status?: string;
}

export interface WebhookApiPostWebhookRequest {
    createEndpointIn: CreateEndpointIn;
}

export interface WebhookApiPostWebhookByIdSecretRequest {
    id: string;
}

export interface WebhookApiPostWebhookByIdTestRequest {
    id: string;
}

export interface WebhookApiPutWebhookByIdRequest {
    id: string;
    updateEndpointIn: UpdateEndpointIn;
}

/**
 * 
 */
export class WebhookApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s webhook endpoints and answers 204 with no body. Delivery stops immediately and the endpoint\'s signing secret is gone with it; its recorded delivery history goes too. An id another org owns reads as not found.
     * Removes one of the caller org\'s webhook endpoints and answers 204 with no body.
     */
    async deleteWebhookByIdRaw(requestParameters: WebhookApiDeleteWebhookByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteWebhookById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s webhook endpoints and answers 204 with no body. Delivery stops immediately and the endpoint\'s signing secret is gone with it; its recorded delivery history goes too. An id another org owns reads as not found.
     * Removes one of the caller org\'s webhook endpoints and answers 204 with no body.
     */
    async deleteWebhookById(requestParameters: WebhookApiDeleteWebhookByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteWebhookByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Returns every webhook endpoint the caller\'s org has registered, newest first, each with its 7-day delivery and failure counts. Signing secrets are redacted here — a secret leaves the server only on create and on rotate. The listing is physically org-scoped, so another tenant\'s endpoints are not reachable from this route at all.
     * Returns every webhook endpoint the caller\'s org has registered, newest first, each with its 7-day delivery and failure counts.
     */
    async getWebhookRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EndpointList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointListFromJSON(jsonValue));
    }

    /**
     * Returns every webhook endpoint the caller\'s org has registered, newest first, each with its 7-day delivery and failure counts. Signing secrets are redacted here — a secret leaves the server only on create and on rotate. The listing is physically org-scoped, so another tenant\'s endpoints are not reachable from this route at all.
     * Returns every webhook endpoint the caller\'s org has registered, newest first, each with its 7-day delivery and failure counts.
     */
    async getWebhook(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EndpointList> {
        const response = await this.getWebhookRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s webhook endpoints with its 7-day delivery and failure counts, signing secret redacted. An id another org owns reads as not found, so the response cannot confirm that it exists.
     * Returns one of the caller org\'s webhook endpoints with its 7-day delivery and failure counts, signing secret redacted.
     */
    async getWebhookByIdRaw(requestParameters: WebhookApiGetWebhookByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Endpoint>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getWebhookById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s webhook endpoints with its 7-day delivery and failure counts, signing secret redacted. An id another org owns reads as not found, so the response cannot confirm that it exists.
     * Returns one of the caller org\'s webhook endpoints with its 7-day delivery and failure counts, signing secret redacted.
     */
    async getWebhookById(requestParameters: WebhookApiGetWebhookByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Endpoint> {
        const response = await this.getWebhookByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one endpoint\'s per-attempt delivery log, newest first — the record of what was sent, what the subscriber answered, and how long it took. One event that retried three times appears as three rows sharing a delivery id. It is org-scoped exactly like every other route here: the endpoint lookup only ever finds THIS org\'s endpoint, so another org\'s id is a 404 and never a window onto its logs.
     * Returns one endpoint\'s per-attempt delivery log, newest first — the record of what was sent, what the subscriber answered, and how long it took.
     */
    async getWebhookByIdDeliveriesRaw(requestParameters: WebhookApiGetWebhookByIdDeliveriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeliveryList>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getWebhookByIdDeliveries().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook/{id}/deliveries`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeliveryListFromJSON(jsonValue));
    }

    /**
     * Returns one endpoint\'s per-attempt delivery log, newest first — the record of what was sent, what the subscriber answered, and how long it took. One event that retried three times appears as three rows sharing a delivery id. It is org-scoped exactly like every other route here: the endpoint lookup only ever finds THIS org\'s endpoint, so another org\'s id is a 404 and never a window onto its logs.
     * Returns one endpoint\'s per-attempt delivery log, newest first — the record of what was sent, what the subscriber answered, and how long it took.
     */
    async getWebhookByIdDeliveries(requestParameters: WebhookApiGetWebhookByIdDeliveriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeliveryList> {
        const response = await this.getWebhookByIdDeliveriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Registers a new webhook subscription for the caller\'s org and answers 201 with the endpoint INCLUDING its freshly minted signing secret. This is one of only two responses that ever carry that secret (the other is rotate) — store it now, because no later read returns it. The org is stamped by the server from the validated principal, so a body can never register an endpoint in another tenant.
     * Registers a new webhook subscription for the caller\'s org and answers 201 with the endpoint INCLUDING its freshly minted signing secret.
     */
    async postWebhookRaw(requestParameters: WebhookApiPostWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Endpoint>> {
        if (requestParameters['createEndpointIn'] == null) {
            throw new runtime.RequiredError(
                'createEndpointIn',
                'Required parameter "createEndpointIn" was null or undefined when calling postWebhook().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateEndpointInToJSON(requestParameters['createEndpointIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointFromJSON(jsonValue));
    }

    /**
     * Registers a new webhook subscription for the caller\'s org and answers 201 with the endpoint INCLUDING its freshly minted signing secret. This is one of only two responses that ever carry that secret (the other is rotate) — store it now, because no later read returns it. The org is stamped by the server from the validated principal, so a body can never register an endpoint in another tenant.
     * Registers a new webhook subscription for the caller\'s org and answers 201 with the endpoint INCLUDING its freshly minted signing secret.
     */
    async postWebhook(requestParameters: WebhookApiPostWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Endpoint> {
        const response = await this.postWebhookRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Mints a NEW HMAC signing secret for the endpoint and answers the endpoint WITH it — the only other response besides create that ever carries a secret. The old secret stops working the instant this returns: every subsequent delivery signs with the new one, with no overlap window. Call it when the subscriber is ready to swap the value on its side, not before.
     * Mints a NEW HMAC signing secret for the endpoint and answers the endpoint WITH it — the only other response besides create that ever carries a secret.
     */
    async postWebhookByIdSecretRaw(requestParameters: WebhookApiPostWebhookByIdSecretRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Endpoint>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postWebhookByIdSecret().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook/{id}/secret`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointFromJSON(jsonValue));
    }

    /**
     * Mints a NEW HMAC signing secret for the endpoint and answers the endpoint WITH it — the only other response besides create that ever carries a secret. The old secret stops working the instant this returns: every subsequent delivery signs with the new one, with no overlap window. Call it when the subscriber is ready to swap the value on its side, not before.
     * Mints a NEW HMAC signing secret for the endpoint and answers the endpoint WITH it — the only other response besides create that ever carries a secret.
     */
    async postWebhookByIdSecret(requestParameters: WebhookApiPostWebhookByIdSecretRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Endpoint> {
        const response = await this.postWebhookByIdSecretRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sends ONE signed test event to the endpoint right now and answers the outcome inline, so the console can show whether the subscriber is reachable without waiting for real traffic. It takes the same attempt path the bus dispatcher takes — one attempt, 10s timeout, no retry ladder — and records the result in the endpoint\'s delivery log. It works on a DISABLED endpoint too: validating one you have paused is the whole point.
     * Sends ONE signed test event to the endpoint right now and answers the outcome inline, so the console can show whether the subscriber is reachable without waiting for real traffic.
     */
    async postWebhookByIdTestRaw(requestParameters: WebhookApiPostWebhookByIdTestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TestResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postWebhookByIdTest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook/{id}/test`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TestResultFromJSON(jsonValue));
    }

    /**
     * Sends ONE signed test event to the endpoint right now and answers the outcome inline, so the console can show whether the subscriber is reachable without waiting for real traffic. It takes the same attempt path the bus dispatcher takes — one attempt, 10s timeout, no retry ladder — and records the result in the endpoint\'s delivery log. It works on a DISABLED endpoint too: validating one you have paused is the whole point.
     * Sends ONE signed test event to the endpoint right now and answers the outcome inline, so the console can show whether the subscriber is reachable without waiting for real traffic.
     */
    async postWebhookByIdTest(requestParameters: WebhookApiPostWebhookByIdTestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TestResult> {
        const response = await this.postWebhookByIdTestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the editable fields of one of the caller org\'s endpoints — url, events, status and description — and answers the stored row with its secret redacted. It is a full replace, not a patch: an omitted field is written as its empty value, and an omitted or empty events list resubscribes the endpoint to EVERY event. The signing secret and the creation time are immutable here; rotate the secret with POST /v1/webhook/{id}/secret.
     * Replaces the editable fields of one of the caller org\'s endpoints — url, events, status and description — and answers the stored row with its secret redacted.
     */
    async putWebhookByIdRaw(requestParameters: WebhookApiPutWebhookByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Endpoint>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putWebhookById().'
            );
        }

        if (requestParameters['updateEndpointIn'] == null) {
            throw new runtime.RequiredError(
                'updateEndpointIn',
                'Required parameter "updateEndpointIn" was null or undefined when calling putWebhookById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/webhook/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateEndpointInToJSON(requestParameters['updateEndpointIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointFromJSON(jsonValue));
    }

    /**
     * Replaces the editable fields of one of the caller org\'s endpoints — url, events, status and description — and answers the stored row with its secret redacted. It is a full replace, not a patch: an omitted field is written as its empty value, and an omitted or empty events list resubscribes the endpoint to EVERY event. The signing secret and the creation time are immutable here; rotate the secret with POST /v1/webhook/{id}/secret.
     * Replaces the editable fields of one of the caller org\'s endpoints — url, events, status and description — and answers the stored row with its secret redacted.
     */
    async putWebhookById(requestParameters: WebhookApiPutWebhookByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Endpoint> {
        const response = await this.putWebhookByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
