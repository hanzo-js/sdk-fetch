/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  SeoAuditIn,
  SeoAuditOut,
  SeoBacklinkIn,
  SeoBacklinkOut,
  SeoCompetitorIn,
  SeoCompetitorOut,
  SeoIdeaIn,
  SeoIdeaOut,
  SeoKeywordIn,
  SeoKeywordOut,
  SeoRankIn,
  SeoRankOut,
  SeoRateOut,
} from '../models/index.js';
import {
    SeoAuditInFromJSON,
    SeoAuditInToJSON,
    SeoAuditOutFromJSON,
    SeoAuditOutToJSON,
    SeoBacklinkInFromJSON,
    SeoBacklinkInToJSON,
    SeoBacklinkOutFromJSON,
    SeoBacklinkOutToJSON,
    SeoCompetitorInFromJSON,
    SeoCompetitorInToJSON,
    SeoCompetitorOutFromJSON,
    SeoCompetitorOutToJSON,
    SeoIdeaInFromJSON,
    SeoIdeaInToJSON,
    SeoIdeaOutFromJSON,
    SeoIdeaOutToJSON,
    SeoKeywordInFromJSON,
    SeoKeywordInToJSON,
    SeoKeywordOutFromJSON,
    SeoKeywordOutToJSON,
    SeoRankInFromJSON,
    SeoRankInToJSON,
    SeoRankOutFromJSON,
    SeoRankOutToJSON,
    SeoRateOutFromJSON,
    SeoRateOutToJSON,
} from '../models/index.js';

export interface SeoApiSeoAuditRequest {
    seoAuditIn: SeoAuditIn;
}

export interface SeoApiSeoBacklinkRequest {
    seoBacklinkIn: SeoBacklinkIn;
}

export interface SeoApiSeoCompetitorRequest {
    seoCompetitorIn: SeoCompetitorIn;
}

export interface SeoApiSeoIdeaRequest {
    seoIdeaIn: SeoIdeaIn;
}

export interface SeoApiSeoKeywordRequest {
    seoKeywordIn: SeoKeywordIn;
}

export interface SeoApiSeoRankRequest {
    seoRankIn: SeoRankIn;
}

/**
 * 
 */
export class SeoApi extends runtime.BaseAPI {

    /**
     * Fetches one page and reports what it gets wrong.  It returns the page\'s on-page score, its title and description, how much readable text it carries, and the full set of named checks — is it https, does it have one h1, is the title duplicated, is it slow, is it a redirect, is anything on it broken. It is the technical half of search visibility, and it is the half a developer can act on this afternoon.  ONE PAGE, LIVE, IN THIS REQUEST. It is deliberately not a site crawl: a crawl is a job with a lifecycle, and this answers the same questions about the page somebody is actually looking at, now, with no task id to poll. Point it at the pages that matter one at a time.  It is priced per page fetched, which is one.
     * Fetch one page and report what it gets wrong
     */
    async seoAuditRaw(requestParameters: SeoApiSeoAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoAuditOut>> {
        if (requestParameters['seoAuditIn'] == null) {
            throw new runtime.RequiredError(
                'seoAuditIn',
                'Required parameter "seoAuditIn" was null or undefined when calling seoAudit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/audit`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeoAuditInToJSON(requestParameters['seoAuditIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoAuditOutFromJSON(jsonValue));
    }

    /**
     * Fetches one page and reports what it gets wrong.  It returns the page\'s on-page score, its title and description, how much readable text it carries, and the full set of named checks — is it https, does it have one h1, is the title duplicated, is it slow, is it a redirect, is anything on it broken. It is the technical half of search visibility, and it is the half a developer can act on this afternoon.  ONE PAGE, LIVE, IN THIS REQUEST. It is deliberately not a site crawl: a crawl is a job with a lifecycle, and this answers the same questions about the page somebody is actually looking at, now, with no task id to poll. Point it at the pages that matter one at a time.  It is priced per page fetched, which is one.
     * Fetch one page and report what it gets wrong
     */
    async seoAudit(requestParameters: SeoApiSeoAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoAuditOut> {
        const response = await this.seoAuditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Summarises who links to a target.  It returns the authority score, how many links point at it and from how many distinct sites, how many of those are broken, and how much of the profile reads as spam. Distinct sites is the number to read: a thousand links from one domain is one endorsement, and a profile that grew fast in links and not in domains is usually a profile somebody bought.  The target can be a whole domain, a subdomain, or one page URL — the summary is scoped to whatever is named. It is priced per request, so a domain with ten million links costs the same as one with ten.
     * Who links to a target, and how much of it is broken or spam
     */
    async seoBacklinkRaw(requestParameters: SeoApiSeoBacklinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoBacklinkOut>> {
        if (requestParameters['seoBacklinkIn'] == null) {
            throw new runtime.RequiredError(
                'seoBacklinkIn',
                'Required parameter "seoBacklinkIn" was null or undefined when calling seoBacklink().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/backlinks`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeoBacklinkInToJSON(requestParameters['seoBacklinkIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoBacklinkOutFromJSON(jsonValue));
    }

    /**
     * Summarises who links to a target.  It returns the authority score, how many links point at it and from how many distinct sites, how many of those are broken, and how much of the profile reads as spam. Distinct sites is the number to read: a thousand links from one domain is one endorsement, and a profile that grew fast in links and not in domains is usually a profile somebody bought.  The target can be a whole domain, a subdomain, or one page URL — the summary is scoped to whatever is named. It is priced per request, so a domain with ten million links costs the same as one with ten.
     * Who links to a target, and how much of it is broken or spam
     */
    async seoBacklink(requestParameters: SeoApiSeoBacklinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoBacklinkOut> {
        const response = await this.seoBacklinkRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Names the domains that place for the same phrases.  Given a set of phrases it returns the sites that appear across them, with each one\'s average position, how many of the phrases it places for, its share of the available attention and the visits that earns. It answers \"who am I actually up against here\", which is a different question from \"who do I think my competitors are\" and frequently a different answer.  Pair it with seoRank: this says who is in the race, seoRank says where any one of them finishes. It is priced per row, so Limit decides the cost.
     * The domains that place for the same phrases
     */
    async seoCompetitorRaw(requestParameters: SeoApiSeoCompetitorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoCompetitorOut>> {
        if (requestParameters['seoCompetitorIn'] == null) {
            throw new runtime.RequiredError(
                'seoCompetitorIn',
                'Required parameter "seoCompetitorIn" was null or undefined when calling seoCompetitor().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/competitors`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeoCompetitorInToJSON(requestParameters['seoCompetitorIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoCompetitorOutFromJSON(jsonValue));
    }

    /**
     * Names the domains that place for the same phrases.  Given a set of phrases it returns the sites that appear across them, with each one\'s average position, how many of the phrases it places for, its share of the available attention and the visits that earns. It answers \"who am I actually up against here\", which is a different question from \"who do I think my competitors are\" and frequently a different answer.  Pair it with seoRank: this says who is in the race, seoRank says where any one of them finishes. It is priced per row, so Limit decides the cost.
     * The domains that place for the same phrases
     */
    async seoCompetitor(requestParameters: SeoApiSeoCompetitorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoCompetitorOut> {
        const response = await this.seoCompetitorRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Grows a seed phrase into the phrases nobody named yet.  It takes phrases you have and returns phrases in the same category that you do not — relevant rather than merely containing the seed — each with its search volume, click cost, competition and how hard its first page is to reach. This is where a keyword list comes FROM; seoKeyword is where a list you already have gets measured.  It is priced per row, so Limit is the knob that decides what the call costs. Total says how many more there were.
     * Grow a seed phrase into the phrases nobody named yet
     */
    async seoIdeaRaw(requestParameters: SeoApiSeoIdeaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoIdeaOut>> {
        if (requestParameters['seoIdeaIn'] == null) {
            throw new runtime.RequiredError(
                'seoIdeaIn',
                'Required parameter "seoIdeaIn" was null or undefined when calling seoIdea().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/ideas`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeoIdeaInToJSON(requestParameters['seoIdeaIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoIdeaOutFromJSON(jsonValue));
    }

    /**
     * Grows a seed phrase into the phrases nobody named yet.  It takes phrases you have and returns phrases in the same category that you do not — relevant rather than merely containing the seed — each with its search volume, click cost, competition and how hard its first page is to reach. This is where a keyword list comes FROM; seoKeyword is where a list you already have gets measured.  It is priced per row, so Limit is the knob that decides what the call costs. Total says how many more there were.
     * Grow a seed phrase into the phrases nobody named yet
     */
    async seoIdea(requestParameters: SeoApiSeoIdeaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoIdeaOut> {
        const response = await this.seoIdeaRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Measures phrases the caller already has.  It answers, for each phrase named, how many people search it in a month, what an advertising click on it costs, and how contested that advertising is. This is the ground fact of search: everything else on this surface is a question about phrases, and this is the one that says whether a phrase is worth having.  Give it phrases you already suspect. To find phrases you have not thought of, use seoIdea; to find the ones a site already places for, use seoRank.  The market defaults to the United States in English. It is priced per request rather than per phrase, so asking about fifty phrases costs what asking about one does.
     * How often named phrases are searched, and what a click costs
     */
    async seoKeywordRaw(requestParameters: SeoApiSeoKeywordRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoKeywordOut>> {
        if (requestParameters['seoKeywordIn'] == null) {
            throw new runtime.RequiredError(
                'seoKeywordIn',
                'Required parameter "seoKeywordIn" was null or undefined when calling seoKeyword().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/keywords`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeoKeywordInToJSON(requestParameters['seoKeywordIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoKeywordOutFromJSON(jsonValue));
    }

    /**
     * Measures phrases the caller already has.  It answers, for each phrase named, how many people search it in a month, what an advertising click on it costs, and how contested that advertising is. This is the ground fact of search: everything else on this surface is a question about phrases, and this is the one that says whether a phrase is worth having.  Give it phrases you already suspect. To find phrases you have not thought of, use seoIdea; to find the ones a site already places for, use seoRank.  The market defaults to the United States in English. It is priced per request rather than per phrase, so asking about fifty phrases costs what asking about one does.
     * How often named phrases are searched, and what a click costs
     */
    async seoKeyword(requestParameters: SeoApiSeoKeywordRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoKeywordOut> {
        const response = await this.seoKeywordRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports every phrase a domain already places for.  For each one it gives the phrase, the position on the results page, the page of the site that placed, that result\'s headline, the phrase\'s monthly searches and the visits the placement is estimated to earn. It is the single most direct question about a site\'s search visibility — yours or a competitor\'s, since it takes any domain.  Position is the ABSOLUTE rank, counting every element on the page — the ads, the answer boxes, the map — because that is what a person scrolling actually passes. An organic-only rank flatters a result that sits below half a screen of other things.  It is priced per row, so Limit decides what the call costs, and Total says how many more there were.
     * Every phrase a domain already places for, with its position
     */
    async seoRankRaw(requestParameters: SeoApiSeoRankRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoRankOut>> {
        if (requestParameters['seoRankIn'] == null) {
            throw new runtime.RequiredError(
                'seoRankIn',
                'Required parameter "seoRankIn" was null or undefined when calling seoRank().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/rankings`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeoRankInToJSON(requestParameters['seoRankIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoRankOutFromJSON(jsonValue));
    }

    /**
     * Reports every phrase a domain already places for.  For each one it gives the phrase, the position on the results page, the page of the site that placed, that result\'s headline, the phrase\'s monthly searches and the visits the placement is estimated to earn. It is the single most direct question about a site\'s search visibility — yours or a competitor\'s, since it takes any domain.  Position is the ABSOLUTE rank, counting every element on the page — the ads, the answer boxes, the map — because that is what a person scrolling actually passes. An organic-only rank flatters a result that sits below half a screen of other things.  It is priced per row, so Limit decides what the call costs, and Total says how many more there were.
     * Every phrase a domain already places for, with its position
     */
    async seoRank(requestParameters: SeoApiSeoRankRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoRankOut> {
        const response = await this.seoRankRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publishes what every call on this surface costs.  The numbers are read from the upstream\'s own published price list, not from a table kept here, so a price change on their side moves this card within the hour and moves what is debited with it. That is the whole of the pricing model: this surface resells at cost, and the cost is theirs to state.  A row has two numbers because a call has two costs: a flat charge for asking, and a charge per row returned. An op priced per request reports zero for the second, and for one priced per row the total is `request + result x limit` — which is the amount your balance is authorized against before the call, and roughly what you will be debited after it.  It is a read and it is free: asking what something costs must not require the balance that would pay for it. If the upstream cannot be reached the card comes back empty rather than stale — a price nobody can confirm is not a price.
     * What each call on this surface costs, from the vendor\'s own list
     */
    async seoRateRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeoRateOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/seo/rates`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeoRateOutFromJSON(jsonValue));
    }

    /**
     * Publishes what every call on this surface costs.  The numbers are read from the upstream\'s own published price list, not from a table kept here, so a price change on their side moves this card within the hour and moves what is debited with it. That is the whole of the pricing model: this surface resells at cost, and the cost is theirs to state.  A row has two numbers because a call has two costs: a flat charge for asking, and a charge per row returned. An op priced per request reports zero for the second, and for one priced per row the total is `request + result x limit` — which is the amount your balance is authorized against before the call, and roughly what you will be debited after it.  It is a read and it is free: asking what something costs must not require the balance that would pay for it. If the upstream cannot be reached the card comes back empty rather than stale — a price nobody can confirm is not a price.
     * What each call on this surface costs, from the vendor\'s own list
     */
    async seoRate(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeoRateOut> {
        const response = await this.seoRateRaw(initOverrides);
        return await response.value();
    }

}
