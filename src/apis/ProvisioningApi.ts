/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ProvisionRequest,
  ProvisionResult,
  ProvisionedResource,
  ProvisionedSummary,
} from '../models/index.js';
import {
    ProvisionRequestFromJSON,
    ProvisionRequestToJSON,
    ProvisionResultFromJSON,
    ProvisionResultToJSON,
    ProvisionedResourceFromJSON,
    ProvisionedResourceToJSON,
    ProvisionedSummaryFromJSON,
    ProvisionedSummaryToJSON,
} from '../models/index.js';

export interface ProvisioningApiDeleteProvisioningDatastoreByNameRequest {
    name: string;
}

export interface ProvisioningApiDeleteProvisioningDocdbByNameRequest {
    name: string;
}

export interface ProvisioningApiDeleteProvisioningKvByNameRequest {
    name: string;
}

export interface ProvisioningApiDeleteProvisioningS3ByNameRequest {
    name: string;
}

export interface ProvisioningApiDeleteProvisioningSearchByNameRequest {
    name: string;
}

export interface ProvisioningApiDeleteProvisioningSqlByNameRequest {
    name: string;
}

export interface ProvisioningApiDeleteProvisioningVectorByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningDatastoreByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningDocdbByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningKvByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningS3ByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningSearchByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningSqlByNameRequest {
    name: string;
}

export interface ProvisioningApiGetProvisioningVectorByNameRequest {
    name: string;
}

export interface ProvisioningApiPostProvisioningDatastoreRequest {
    provisionRequest: ProvisionRequest;
}

export interface ProvisioningApiPostProvisioningDocdbRequest {
    provisionRequest: ProvisionRequest;
}

export interface ProvisioningApiPostProvisioningKvRequest {
    provisionRequest: ProvisionRequest;
}

export interface ProvisioningApiPostProvisioningS3Request {
    provisionRequest: ProvisionRequest;
}

export interface ProvisioningApiPostProvisioningSearchRequest {
    provisionRequest: ProvisionRequest;
}

export interface ProvisioningApiPostProvisioningSqlRequest {
    provisionRequest: ProvisionRequest;
}

export interface ProvisioningApiPostProvisioningVectorRequest {
    provisionRequest: ProvisionRequest;
}

/**
 * 
 */
export class ProvisioningApi extends runtime.BaseAPI {

    /**
     * Deprovisions one Hanzo Datastore warehouse. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated instance, then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404.
     * Deprovisions one Hanzo Datastore warehouse.
     */
    async deleteProvisioningDatastoreByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningDatastoreByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningDatastoreByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/datastore/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deprovisions one Hanzo Datastore warehouse. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated instance, then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404.
     * Deprovisions one Hanzo Datastore warehouse.
     */
    async deleteProvisioningDatastoreByName(requestParameters: ProvisioningApiDeleteProvisioningDatastoreByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningDatastoreByNameRaw(requestParameters, initOverrides);
    }

    /**
     * DropDocDB deprovisions one Hanzo DocDB database. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated FerretDB instance, then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404.
     * DropDocDB deprovisions one Hanzo DocDB database.
     */
    async deleteProvisioningDocdbByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningDocdbByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningDocdbByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/docdb/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * DropDocDB deprovisions one Hanzo DocDB database. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated FerretDB instance, then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404.
     * DropDocDB deprovisions one Hanzo DocDB database.
     */
    async deleteProvisioningDocdbByName(requestParameters: ProvisioningApiDeleteProvisioningDocdbByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningDocdbByNameRaw(requestParameters, initOverrides);
    }

    /**
     * DropKV deprovisions one Hanzo KV store. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated Valkey instance, then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404.
     * DropKV deprovisions one Hanzo KV store.
     */
    async deleteProvisioningKvByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningKvByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningKvByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/kv/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * DropKV deprovisions one Hanzo KV store. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated Valkey instance, then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404.
     * DropKV deprovisions one Hanzo KV store.
     */
    async deleteProvisioningKvByName(requestParameters: ProvisioningApiDeleteProvisioningKvByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningKvByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Deletes one bucket from the shared object store and removes its metadata row. Answers 204 with no body; a second call is a 404.
     * Deletes one bucket from the shared object store and removes its metadata row.
     */
    async deleteProvisioningS3ByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningS3ByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningS3ByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/s3/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one bucket from the shared object store and removes its metadata row. Answers 204 with no body; a second call is a 404.
     * Deletes one bucket from the shared object store and removes its metadata row.
     */
    async deleteProvisioningS3ByName(requestParameters: ProvisioningApiDeleteProvisioningS3ByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningS3ByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Deletes one search index from the shared backend and removes its metadata row. Answers 204 with no body; a second call is a 404.
     * Deletes one search index from the shared backend and removes its metadata row.
     */
    async deleteProvisioningSearchByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningSearchByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningSearchByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/search/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one search index from the shared backend and removes its metadata row. Answers 204 with no body; a second call is a 404.
     * Deletes one search index from the shared backend and removes its metadata row.
     */
    async deleteProvisioningSearchByName(requestParameters: ProvisioningApiDeleteProvisioningSearchByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningSearchByNameRaw(requestParameters, initOverrides);
    }

    /**
     * DropSQL deprovisions one Hanzo SQL database. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated Postgres instance — never a live app pointed at a deleted backend — then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404, not a second delete.
     * DropSQL deprovisions one Hanzo SQL database.
     */
    async deleteProvisioningSqlByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningSqlByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningSqlByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/sql/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * DropSQL deprovisions one Hanzo SQL database. It reverts any app instance bound to it back to Base BEFORE tearing down the org\'s dedicated Postgres instance — never a live app pointed at a deleted backend — then deletes the sealed credential and removes the metadata row. Answers 204 with no body; a second call is a 404, not a second delete.
     * DropSQL deprovisions one Hanzo SQL database.
     */
    async deleteProvisioningSqlByName(requestParameters: ProvisioningApiDeleteProvisioningSqlByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningSqlByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Deletes one vector collection from the shared backend and removes its metadata row. Answers 204 with no body; a second call is a 404.
     * Deletes one vector collection from the shared backend and removes its metadata row.
     */
    async deleteProvisioningVectorByNameRaw(requestParameters: ProvisioningApiDeleteProvisioningVectorByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteProvisioningVectorByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/vector/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one vector collection from the shared backend and removes its metadata row. Answers 204 with no body; a second call is a 404.
     * Deletes one vector collection from the shared backend and removes its metadata row.
     */
    async deleteProvisioningVectorByName(requestParameters: ProvisioningApiDeleteProvisioningVectorByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProvisioningVectorByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the caller org\'s Hanzo Datastore warehouses. Each one is a DEDICATED analytical instance the org alone runs, so the host is that instance\'s own in-cluster Service and the port is its HTTP port, 8123.
     * Lists the caller org\'s Hanzo Datastore warehouses.
     */
    async getProvisioningDatastoreRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/datastore`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * Lists the caller org\'s Hanzo Datastore warehouses. Each one is a DEDICATED analytical instance the org alone runs, so the host is that instance\'s own in-cluster Service and the port is its HTTP port, 8123.
     * Lists the caller org\'s Hanzo Datastore warehouses.
     */
    async getProvisioningDatastore(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningDatastoreRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one Hanzo Datastore warehouse\'s metadata. It carries the warehouse\'s status, its instance address and the admin user the instance booted with — never the password. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view rather than the row.
     * Returns one Hanzo Datastore warehouse\'s metadata.
     */
    async getProvisioningDatastoreByNameRaw(requestParameters: ProvisioningApiGetProvisioningDatastoreByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningDatastoreByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/datastore/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * Returns one Hanzo Datastore warehouse\'s metadata. It carries the warehouse\'s status, its instance address and the admin user the instance booted with — never the password. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view rather than the row.
     * Returns one Hanzo Datastore warehouse\'s metadata.
     */
    async getProvisioningDatastoreByName(requestParameters: ProvisioningApiGetProvisioningDatastoreByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningDatastoreByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * ListDocDB lists the caller org\'s Hanzo DocDB document databases. Each one is a DEDICATED FerretDB instance the org alone runs, speaking the MongoDB wire protocol, so the host is that instance\'s own in-cluster Service and the port is 27017.
     * ListDocDB lists the caller org\'s Hanzo DocDB document databases.
     */
    async getProvisioningDocdbRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/docdb`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * ListDocDB lists the caller org\'s Hanzo DocDB document databases. Each one is a DEDICATED FerretDB instance the org alone runs, speaking the MongoDB wire protocol, so the host is that instance\'s own in-cluster Service and the port is 27017.
     * ListDocDB lists the caller org\'s Hanzo DocDB document databases.
     */
    async getProvisioningDocdb(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningDocdbRaw(initOverrides);
        return await response.value();
    }

    /**
     * GetDocDB returns one Hanzo DocDB database\'s metadata. It carries the database\'s status, its instance address and the SCRAM user the instance was set up with — never the password. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view.
     * GetDocDB returns one Hanzo DocDB database\'s metadata.
     */
    async getProvisioningDocdbByNameRaw(requestParameters: ProvisioningApiGetProvisioningDocdbByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningDocdbByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/docdb/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * GetDocDB returns one Hanzo DocDB database\'s metadata. It carries the database\'s status, its instance address and the SCRAM user the instance was set up with — never the password. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view.
     * GetDocDB returns one Hanzo DocDB database\'s metadata.
     */
    async getProvisioningDocdbByName(requestParameters: ProvisioningApiGetProvisioningDocdbByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningDocdbByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * ListKV lists the caller org\'s Hanzo KV stores. Each one is a DEDICATED Valkey instance the org alone runs, so the host is that instance\'s own in-cluster Service and the port is 6379.
     * ListKV lists the caller org\'s Hanzo KV stores.
     */
    async getProvisioningKvRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/kv`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * ListKV lists the caller org\'s Hanzo KV stores. Each one is a DEDICATED Valkey instance the org alone runs, so the host is that instance\'s own in-cluster Service and the port is 6379.
     * ListKV lists the caller org\'s Hanzo KV stores.
     */
    async getProvisioningKv(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningKvRaw(initOverrides);
        return await response.value();
    }

    /**
     * GetKV returns one Hanzo KV store\'s metadata. It carries the store\'s status, its instance address and the Valkey user it authenticates as (\"default\", the only user a requirepass instance has) — never the password. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view.
     * GetKV returns one Hanzo KV store\'s metadata.
     */
    async getProvisioningKvByNameRaw(requestParameters: ProvisioningApiGetProvisioningKvByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningKvByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/kv/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * GetKV returns one Hanzo KV store\'s metadata. It carries the store\'s status, its instance address and the Valkey user it authenticates as (\"default\", the only user a requirepass instance has) — never the password. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view.
     * GetKV returns one Hanzo KV store\'s metadata.
     */
    async getProvisioningKvByName(requestParameters: ProvisioningApiGetProvisioningKvByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningKvByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller org\'s object-storage buckets. A bucket lives in an already-live shared object store and is reached through the public gateway. The names here are the friendly ones the org provisioned; the physical bucket is org-namespaced underneath, which is what keeps two tenants\' buckets distinct.
     * Lists the caller org\'s object-storage buckets.
     */
    async getProvisioningS3Raw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/s3`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * Lists the caller org\'s object-storage buckets. A bucket lives in an already-live shared object store and is reached through the public gateway. The names here are the friendly ones the org provisioned; the physical bucket is org-namespaced underneath, which is what keeps two tenants\' buckets distinct.
     * Lists the caller org\'s object-storage buckets.
     */
    async getProvisioningS3(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningS3Raw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one bucket\'s metadata. It carries the bucket\'s status and the gateway address it is reached at, and no username: the object store authenticates with a shared, out-of-band key rather than a per-bucket credential.
     * Returns one bucket\'s metadata.
     */
    async getProvisioningS3ByNameRaw(requestParameters: ProvisioningApiGetProvisioningS3ByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningS3ByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/s3/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * Returns one bucket\'s metadata. It carries the bucket\'s status and the gateway address it is reached at, and no username: the object store authenticates with a shared, out-of-band key rather than a per-bucket credential.
     * Returns one bucket\'s metadata.
     */
    async getProvisioningS3ByName(requestParameters: ProvisioningApiGetProvisioningS3ByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningS3ByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller org\'s search indexes. An index is a logical resource inside an already-live shared backend, so every one of them is reached through the public gateway rather than at an instance of its own.
     * Lists the caller org\'s search indexes.
     */
    async getProvisioningSearchRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/search`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * Lists the caller org\'s search indexes. An index is a logical resource inside an already-live shared backend, so every one of them is reached through the public gateway rather than at an instance of its own.
     * Lists the caller org\'s search indexes.
     */
    async getProvisioningSearch(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningSearchRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one search index\'s metadata. It carries the index\'s status and the gateway address it is reached at, and no username: the backend authenticates with a shared, out-of-band key rather than a per-index credential.
     * Returns one search index\'s metadata.
     */
    async getProvisioningSearchByNameRaw(requestParameters: ProvisioningApiGetProvisioningSearchByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningSearchByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/search/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * Returns one search index\'s metadata. It carries the index\'s status and the gateway address it is reached at, and no username: the backend authenticates with a shared, out-of-band key rather than a per-index credential.
     * Returns one search index\'s metadata.
     */
    async getProvisioningSearchByName(requestParameters: ProvisioningApiGetProvisioningSearchByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningSearchByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * ListSQL lists the caller org\'s Hanzo SQL databases. Each one is a DEDICATED PostgreSQL instance the org alone runs, so the host is that instance\'s own in-cluster Service and the port is 5432.
     * ListSQL lists the caller org\'s Hanzo SQL databases.
     */
    async getProvisioningSqlRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/sql`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * ListSQL lists the caller org\'s Hanzo SQL databases. Each one is a DEDICATED PostgreSQL instance the org alone runs, so the host is that instance\'s own in-cluster Service and the port is 5432.
     * ListSQL lists the caller org\'s Hanzo SQL databases.
     */
    async getProvisioningSql(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningSqlRaw(initOverrides);
        return await response.value();
    }

    /**
     * GetSQL returns one Hanzo SQL database\'s metadata. It carries the database\'s status, its instance address and the admin user Postgres booted with — never the password, which is returned once at create and otherwise lives only in Hanzo KMS. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view rather than from the row.
     * GetSQL returns one Hanzo SQL database\'s metadata.
     */
    async getProvisioningSqlByNameRaw(requestParameters: ProvisioningApiGetProvisioningSqlByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningSqlByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/sql/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * GetSQL returns one Hanzo SQL database\'s metadata. It carries the database\'s status, its instance address and the admin user Postgres booted with — never the password, which is returned once at create and otherwise lives only in Hanzo KMS. A still-booting instance reads \"provisioning\", reconciled from the operator\'s live view rather than from the row.
     * GetSQL returns one Hanzo SQL database\'s metadata.
     */
    async getProvisioningSqlByName(requestParameters: ProvisioningApiGetProvisioningSqlByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningSqlByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller org\'s vector collections. A collection is a logical resource inside an already-live shared backend, so every one of them is reached through the public gateway rather than at an instance of its own.
     * Lists the caller org\'s vector collections.
     */
    async getProvisioningVectorRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProvisionedSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/vector`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProvisionedSummaryFromJSON));
    }

    /**
     * Lists the caller org\'s vector collections. A collection is a logical resource inside an already-live shared backend, so every one of them is reached through the public gateway rather than at an instance of its own.
     * Lists the caller org\'s vector collections.
     */
    async getProvisioningVector(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProvisionedSummary>> {
        const response = await this.getProvisioningVectorRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one vector collection\'s metadata. It carries the collection\'s status and the gateway address it is reached at, and no username: the backend authenticates with a shared, out-of-band key rather than a per-collection credential, so there is no per-resource user to report.
     * Returns one vector collection\'s metadata.
     */
    async getProvisioningVectorByNameRaw(requestParameters: ProvisioningApiGetProvisioningVectorByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionedResource>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getProvisioningVectorByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/vector/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionedResourceFromJSON(jsonValue));
    }

    /**
     * Returns one vector collection\'s metadata. It carries the collection\'s status and the gateway address it is reached at, and no username: the backend authenticates with a shared, out-of-band key rather than a per-collection credential, so there is no per-resource user to report.
     * Returns one vector collection\'s metadata.
     */
    async getProvisioningVectorByName(requestParameters: ProvisioningApiGetProvisioningVectorByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionedResource> {
        const response = await this.getProvisioningVectorByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Launches your org\'s OWN Hanzo Datastore instance and answers with its `datastore://` connection string.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster this fails closed with 503 rather than handing back a shared one.
     * Launches your org\'s OWN Hanzo Datastore instance and answers with its `datastore://` connection string.
     */
    async postProvisioningDatastoreRaw(requestParameters: ProvisioningApiPostProvisioningDatastoreRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningDatastore().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/datastore`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * Launches your org\'s OWN Hanzo Datastore instance and answers with its `datastore://` connection string.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster this fails closed with 503 rather than handing back a shared one.
     * Launches your org\'s OWN Hanzo Datastore instance and answers with its `datastore://` connection string.
     */
    async postProvisioningDatastore(requestParameters: ProvisioningApiPostProvisioningDatastoreRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningDatastoreRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * CreateDocDB launches your org\'s OWN document-database instance and answers with its `mongodb://` connection string. It speaks the MongoDB wire protocol, so existing MongoDB drivers connect unchanged.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster this fails closed with 503 rather than handing back a shared one.
     * CreateDocDB launches your org\'s OWN document-database instance and answers with its `mongodb://` connection string.
     */
    async postProvisioningDocdbRaw(requestParameters: ProvisioningApiPostProvisioningDocdbRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningDocdb().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/docdb`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * CreateDocDB launches your org\'s OWN document-database instance and answers with its `mongodb://` connection string. It speaks the MongoDB wire protocol, so existing MongoDB drivers connect unchanged.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster this fails closed with 503 rather than handing back a shared one.
     * CreateDocDB launches your org\'s OWN document-database instance and answers with its `mongodb://` connection string.
     */
    async postProvisioningDocdb(requestParameters: ProvisioningApiPostProvisioningDocdbRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningDocdbRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * CreateKV launches your org\'s OWN key-value instance and answers with its `kv://` connection string.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster this fails closed with 503 rather than handing back a shared one.
     * CreateKV launches your org\'s OWN key-value instance and answers with its `kv://` connection string.
     */
    async postProvisioningKvRaw(requestParameters: ProvisioningApiPostProvisioningKvRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningKv().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/kv`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * CreateKV launches your org\'s OWN key-value instance and answers with its `kv://` connection string.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster this fails closed with 503 rather than handing back a shared one.
     * CreateKV launches your org\'s OWN key-value instance and answers with its `kv://` connection string.
     */
    async postProvisioningKv(requestParameters: ProvisioningApiPostProvisioningKvRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningKvRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an S3-compatible bucket inside the already-running shared object store and answers with the endpoint that reaches it.
     * Creates an S3-compatible bucket inside the already-running shared object store and answers with the endpoint that reaches it.
     */
    async postProvisioningS3Raw(requestParameters: ProvisioningApiPostProvisioningS3Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningS3().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/s3`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * Creates an S3-compatible bucket inside the already-running shared object store and answers with the endpoint that reaches it.
     * Creates an S3-compatible bucket inside the already-running shared object store and answers with the endpoint that reaches it.
     */
    async postProvisioningS3(requestParameters: ProvisioningApiPostProvisioningS3Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningS3Raw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a search index inside the already-running shared search backend and answers with the endpoint that reaches it.
     * Creates a search index inside the already-running shared search backend and answers with the endpoint that reaches it.
     */
    async postProvisioningSearchRaw(requestParameters: ProvisioningApiPostProvisioningSearchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningSearch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/search`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * Creates a search index inside the already-running shared search backend and answers with the endpoint that reaches it.
     * Creates a search index inside the already-running shared search backend and answers with the endpoint that reaches it.
     */
    async postProvisioningSearch(requestParameters: ProvisioningApiPostProvisioningSearchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningSearchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * CreateSQL launches your org\'s OWN PostgreSQL instance and answers with its `postgres://` connection string.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster, where there is no orchestrator to launch one, this fails closed with 503 rather than handing back a shared one.
     * CreateSQL launches your org\'s OWN PostgreSQL instance and answers with its `postgres://` connection string.
     */
    async postProvisioningSqlRaw(requestParameters: ProvisioningApiPostProvisioningSqlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningSql().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/sql`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * CreateSQL launches your org\'s OWN PostgreSQL instance and answers with its `postgres://` connection string.  The instance is yours alone — a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster, where there is no orchestrator to launch one, this fails closed with 503 rather than handing back a shared one.
     * CreateSQL launches your org\'s OWN PostgreSQL instance and answers with its `postgres://` connection string.
     */
    async postProvisioningSql(requestParameters: ProvisioningApiPostProvisioningSqlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningSqlRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a vector collection inside the already-running shared vector backend and answers with the endpoint that reaches it.
     * Creates a vector collection inside the already-running shared vector backend and answers with the endpoint that reaches it.
     */
    async postProvisioningVectorRaw(requestParameters: ProvisioningApiPostProvisioningVectorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvisionResult>> {
        if (requestParameters['provisionRequest'] == null) {
            throw new runtime.RequiredError(
                'provisionRequest',
                'Required parameter "provisionRequest" was null or undefined when calling postProvisioningVector().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/provisioning/vector`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProvisionRequestToJSON(requestParameters['provisionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvisionResultFromJSON(jsonValue));
    }

    /**
     * Creates a vector collection inside the already-running shared vector backend and answers with the endpoint that reaches it.
     * Creates a vector collection inside the already-running shared vector backend and answers with the endpoint that reaches it.
     */
    async postProvisioningVector(requestParameters: ProvisioningApiPostProvisioningVectorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvisionResult> {
        const response = await this.postProvisioningVectorRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
