/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  RegistryImageList,
  RegistryMint,
  RegistryPackageList,
  RegistryProjectList,
  RegistryStatus,
  RegistryTagList,
  RegistryToken,
} from '../models/index.js';
import {
    RegistryImageListFromJSON,
    RegistryImageListToJSON,
    RegistryMintFromJSON,
    RegistryMintToJSON,
    RegistryPackageListFromJSON,
    RegistryPackageListToJSON,
    RegistryProjectListFromJSON,
    RegistryProjectListToJSON,
    RegistryStatusFromJSON,
    RegistryStatusToJSON,
    RegistryTagListFromJSON,
    RegistryTagListToJSON,
    RegistryTokenFromJSON,
    RegistryTokenToJSON,
} from '../models/index.js';

export interface RegistryApiGetRegistryPackagesRequest {
    query?: string;
}

export interface RegistryApiGetRegistryTagsRequest {
    image?: string;
}

export interface RegistryApiPostRegistryTokenRequest {
    registryMint: RegistryMint;
}

/**
 * 
 */
export class RegistryApi extends runtime.BaseAPI {

    /**
     * Images lists the org\'s container repositories, read live from the OCI catalog and filtered server-side to the org\'s namespace — the page can only ever hold the caller\'s own images.
     * Images lists the org\'s container repositories, read live from the OCI catalog and filtered server-side to the org\'s namespace — the page can only ever hold the caller\'s own images.
     */
    async getRegistryImagesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistryImageList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/registry/images`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistryImageListFromJSON(jsonValue));
    }

    /**
     * Images lists the org\'s container repositories, read live from the OCI catalog and filtered server-side to the org\'s namespace — the page can only ever hold the caller\'s own images.
     * Images lists the org\'s container repositories, read live from the OCI catalog and filtered server-side to the org\'s namespace — the page can only ever hold the caller\'s own images.
     */
    async getRegistryImages(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistryImageList> {
        const response = await this.getRegistryImagesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Packages lists the org\'s npm packages — `<org>` and `@<org>/…` — from the npm registry\'s search index, optionally narrowed by a query within that scope. The org boundary is applied server-side after the search, so a query can never widen it.
     * Packages lists the org\'s npm packages — `<org>` and `@<org>/…` — from the npm registry\'s search index, optionally narrowed by a query within that scope.
     */
    async getRegistryPackagesRaw(requestParameters: RegistryApiGetRegistryPackagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistryPackageList>> {
        const queryParameters: any = {};

        if (requestParameters['query'] != null) {
            queryParameters['query'] = requestParameters['query'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/registry/packages`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistryPackageListFromJSON(jsonValue));
    }

    /**
     * Packages lists the org\'s npm packages — `<org>` and `@<org>/…` — from the npm registry\'s search index, optionally narrowed by a query within that scope. The org boundary is applied server-side after the search, so a query can never widen it.
     * Packages lists the org\'s npm packages — `<org>` and `@<org>/…` — from the npm registry\'s search index, optionally narrowed by a query within that scope.
     */
    async getRegistryPackages(requestParameters: RegistryApiGetRegistryPackagesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistryPackageList> {
        const response = await this.getRegistryPackagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Projects lists the namespaces the caller can see with what each holds: the org\'s slug, its repository count on the OCI catalog, and its package count on the npm registry. Today that is exactly one row — the caller\'s org.
     * Projects lists the namespaces the caller can see with what each holds: the org\'s slug, its repository count on the OCI catalog, and its package count on the npm registry.
     */
    async getRegistryProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistryProjectList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/registry/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistryProjectListFromJSON(jsonValue));
    }

    /**
     * Projects lists the namespaces the caller can see with what each holds: the org\'s slug, its repository count on the OCI catalog, and its package count on the npm registry. Today that is exactly one row — the caller\'s org.
     * Projects lists the namespaces the caller can see with what each holds: the org\'s slug, its repository count on the OCI catalog, and its package count on the npm registry.
     */
    async getRegistryProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistryProjectList> {
        const response = await this.getRegistryProjectsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Status reports whether the OCI and npm registries are reachable and, when the OCI half is auth-gated, which token realm its challenge advertises — an honest lens for \"is the registry plane up\", never a fabricated ok.
     * Status reports whether the OCI and npm registries are reachable and, when the OCI half is auth-gated, which token realm its challenge advertises — an honest lens for \"is the registry plane up\", never a fabricated ok.
     */
    async getRegistryStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistryStatus>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/registry/status`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistryStatusFromJSON(jsonValue));
    }

    /**
     * Status reports whether the OCI and npm registries are reachable and, when the OCI half is auth-gated, which token realm its challenge advertises — an honest lens for \"is the registry plane up\", never a fabricated ok.
     * Status reports whether the OCI and npm registries are reachable and, when the OCI half is auth-gated, which token realm its challenge advertises — an honest lens for \"is the registry plane up\", never a fabricated ok.
     */
    async getRegistryStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistryStatus> {
        const response = await this.getRegistryStatusRaw(initOverrides);
        return await response.value();
    }

    /**
     * Tags lists one org-owned repository\'s tags, read live from the OCI registry. The repository is addressed inside the org\'s namespace — a name outside it cannot be expressed, and an unknown one answers 404.
     * Tags lists one org-owned repository\'s tags, read live from the OCI registry.
     */
    async getRegistryTagsRaw(requestParameters: RegistryApiGetRegistryTagsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistryTagList>> {
        const queryParameters: any = {};

        if (requestParameters['image'] != null) {
            queryParameters['image'] = requestParameters['image'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/registry/tags`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistryTagListFromJSON(jsonValue));
    }

    /**
     * Tags lists one org-owned repository\'s tags, read live from the OCI registry. The repository is addressed inside the org\'s namespace — a name outside it cannot be expressed, and an unknown one answers 404.
     * Tags lists one org-owned repository\'s tags, read live from the OCI registry.
     */
    async getRegistryTags(requestParameters: RegistryApiGetRegistryTagsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistryTagList> {
        const response = await this.getRegistryTagsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Token mints a short-lived, pull-only registry token for exactly one of the org\'s images, through the same IAM realm the docker CLI authenticates against. The scope is pinned server-side to `<org>/<image>` with the `pull` action — no field exists to name another org\'s image or ask for push. Use it as `Authorization: Bearer …` on the OCI wire; it expires in minutes.
     * Token mints a short-lived, pull-only registry token for exactly one of the org\'s images, through the same IAM realm the docker CLI authenticates against.
     */
    async postRegistryTokenRaw(requestParameters: RegistryApiPostRegistryTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistryToken>> {
        if (requestParameters['registryMint'] == null) {
            throw new runtime.RequiredError(
                'registryMint',
                'Required parameter "registryMint" was null or undefined when calling postRegistryToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/registry/token`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RegistryMintToJSON(requestParameters['registryMint']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistryTokenFromJSON(jsonValue));
    }

    /**
     * Token mints a short-lived, pull-only registry token for exactly one of the org\'s images, through the same IAM realm the docker CLI authenticates against. The scope is pinned server-side to `<org>/<image>` with the `pull` action — no field exists to name another org\'s image or ask for push. Use it as `Authorization: Bearer …` on the OCI wire; it expires in minutes.
     * Token mints a short-lived, pull-only registry token for exactly one of the org\'s images, through the same IAM realm the docker CLI authenticates against.
     */
    async postRegistryToken(requestParameters: RegistryApiPostRegistryTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistryToken> {
        const response = await this.postRegistryTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
