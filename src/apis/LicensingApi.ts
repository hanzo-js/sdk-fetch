/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  LicensingFingerprintRequest,
  LicensingFingerprintResponse,
  LicensingHealthView,
  LicensingIssueRequest,
  LicensingIssueResponse,
  LicensingPubkeyView,
  LicensingRelease,
  LicensingReleaseAsset,
  LicensingReleaseList,
  LicensingRevokeRequest,
  LicensingRevokeResponse,
  LicensingVerifyRequest,
  LicensingVerifyResponse,
} from '../models/index.js';
import {
    LicensingFingerprintRequestFromJSON,
    LicensingFingerprintRequestToJSON,
    LicensingFingerprintResponseFromJSON,
    LicensingFingerprintResponseToJSON,
    LicensingHealthViewFromJSON,
    LicensingHealthViewToJSON,
    LicensingIssueRequestFromJSON,
    LicensingIssueRequestToJSON,
    LicensingIssueResponseFromJSON,
    LicensingIssueResponseToJSON,
    LicensingPubkeyViewFromJSON,
    LicensingPubkeyViewToJSON,
    LicensingReleaseFromJSON,
    LicensingReleaseToJSON,
    LicensingReleaseAssetFromJSON,
    LicensingReleaseAssetToJSON,
    LicensingReleaseListFromJSON,
    LicensingReleaseListToJSON,
    LicensingRevokeRequestFromJSON,
    LicensingRevokeRequestToJSON,
    LicensingRevokeResponseFromJSON,
    LicensingRevokeResponseToJSON,
    LicensingVerifyRequestFromJSON,
    LicensingVerifyRequestToJSON,
    LicensingVerifyResponseFromJSON,
    LicensingVerifyResponseToJSON,
} from '../models/index.js';

export interface LicensingApiGetLicensingDownloadByReleaseRequest {
    release: string;
    xLicenseToken?: string;
    token?: string;
}

export interface LicensingApiGetLicensingReleasesByReleaseRequest {
    release: string;
}

export interface LicensingApiPostLicensingFingerprintRequest {
    licensingFingerprintRequest: LicensingFingerprintRequest;
}

export interface LicensingApiPostLicensingIssueRequest {
    licensingIssueRequest: LicensingIssueRequest;
}

export interface LicensingApiPostLicensingReleasesRequest {
    licensingRelease: LicensingRelease;
}

export interface LicensingApiPostLicensingRevokeRequest {
    licensingRevokeRequest: LicensingRevokeRequest;
}

export interface LicensingApiPostLicensingVerifyRequest {
    licensingVerifyRequest: LicensingVerifyRequest;
}

/**
 * 
 */
export class LicensingApi extends runtime.BaseAPI {

    /**
     * Download resolves a release to its artifact, gated on a valid license.  The gate is the LICENSE token, not the IAM bearer: being signed in is not permission to download a paid binary — holding a good license for it is. The token must verify against this deployment\'s public key, be unrevoked, be scoped to the release\'s app, and carry every feature the release requires. Present it as the `X-License-Token` header (preferred, since a header does not land in proxy logs) or as `?token=`.  The response pairs the artifact URL with its cosign signature so the client verifies the binary BEFORE trusting it: a signed URL alone proves where the bytes came from, not what they are. A yanked release is 410 Gone.
     * Download resolves a release to its artifact, gated on a valid license.
     */
    async getLicensingDownloadByReleaseRaw(requestParameters: LicensingApiGetLicensingDownloadByReleaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingReleaseAsset>> {
        if (requestParameters['release'] == null) {
            throw new runtime.RequiredError(
                'release',
                'Required parameter "release" was null or undefined when calling getLicensingDownloadByRelease().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['token'] != null) {
            queryParameters['token'] = requestParameters['token'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xLicenseToken'] != null) {
            headerParameters['X-License-Token'] = String(requestParameters['xLicenseToken']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/download/{release}`;
        urlPath = urlPath.replace(`{${"release"}}`, encodeURIComponent(String(requestParameters['release'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingReleaseAssetFromJSON(jsonValue));
    }

    /**
     * Download resolves a release to its artifact, gated on a valid license.  The gate is the LICENSE token, not the IAM bearer: being signed in is not permission to download a paid binary — holding a good license for it is. The token must verify against this deployment\'s public key, be unrevoked, be scoped to the release\'s app, and carry every feature the release requires. Present it as the `X-License-Token` header (preferred, since a header does not land in proxy logs) or as `?token=`.  The response pairs the artifact URL with its cosign signature so the client verifies the binary BEFORE trusting it: a signed URL alone proves where the bytes came from, not what they are. A yanked release is 410 Gone.
     * Download resolves a release to its artifact, gated on a valid license.
     */
    async getLicensingDownloadByRelease(requestParameters: LicensingApiGetLicensingDownloadByReleaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingReleaseAsset> {
        const response = await this.getLicensingDownloadByReleaseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Health reports which signer this deployment mints with, and in which env.  It answers 200 whenever the process is up: there is nothing downstream to probe, since the KMS is reached only when a token is actually minted. Its value is the `signer` field — `\"signer\":\"local\"` on a production host says that deployment is signing licenses with a development key, which is a misconfiguration worth paging on rather than a healthy 200.
     * Health reports which signer this deployment mints with, and in which env.
     */
    async getLicensingHealthzRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingHealthView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/healthz`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingHealthViewFromJSON(jsonValue));
    }

    /**
     * Health reports which signer this deployment mints with, and in which env.  It answers 200 whenever the process is up: there is nothing downstream to probe, since the KMS is reached only when a token is actually minted. Its value is the `signer` field — `\"signer\":\"local\"` on a production host says that deployment is signing licenses with a development key, which is a misconfiguration worth paging on rather than a healthy 200.
     * Health reports which signer this deployment mints with, and in which env.
     */
    async getLicensingHealthz(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingHealthView> {
        const response = await this.getLicensingHealthzRaw(initOverrides);
        return await response.value();
    }

    /**
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.  This is the only public-safe surface here and the reason the whole scheme works offline: the engine embeds or fetches this key once and then verifies every license itself, with no call home per launch. The private half never enters this process — it lives in the KMS — so nothing served here is a secret. `provider` names the KMS holding that half; `\"local\"` means a development key, and a token signed by one is not a production credential.
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.
     */
    async getLicensingJwksRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingPubkeyView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/jwks`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingPubkeyViewFromJSON(jsonValue));
    }

    /**
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.  This is the only public-safe surface here and the reason the whole scheme works offline: the engine embeds or fetches this key once and then verifies every license itself, with no call home per launch. The private half never enters this process — it lives in the KMS — so nothing served here is a secret. `provider` names the KMS holding that half; `\"local\"` means a development key, and a token signed by one is not a production credential.
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.
     */
    async getLicensingJwks(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingPubkeyView> {
        const response = await this.getLicensingJwksRaw(initOverrides);
        return await response.value();
    }

    /**
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.  This is the only public-safe surface here and the reason the whole scheme works offline: the engine embeds or fetches this key once and then verifies every license itself, with no call home per launch. The private half never enters this process — it lives in the KMS — so nothing served here is a secret. `provider` names the KMS holding that half; `\"local\"` means a development key, and a token signed by one is not a production credential.
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.
     */
    async getLicensingPubkeyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingPubkeyView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/pubkey`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingPubkeyViewFromJSON(jsonValue));
    }

    /**
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.  This is the only public-safe surface here and the reason the whole scheme works offline: the engine embeds or fetches this key once and then verifies every license itself, with no call home per launch. The private half never enters this process — it lives in the KMS — so nothing served here is a secret. `provider` names the KMS holding that half; `\"local\"` means a development key, and a token signed by one is not a production credential.
     * Pubkey publishes the Ed25519 PUBLIC verification key, at both /pubkey and /jwks.
     */
    async getLicensingPubkey(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingPubkeyView> {
        const response = await this.getLicensingPubkeyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the signed binary releases this deployment can serve.  Metadata only, and no download URL: the artifact is behind GET /v1/licensing/download/{release}, which is gated on a valid license token. Knowing that a release exists is not permission to run it, which is why this list needs no license of its own.
     * Lists the signed binary releases this deployment can serve.
     */
    async getLicensingReleasesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingReleaseList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/releases`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingReleaseListFromJSON(jsonValue));
    }

    /**
     * Lists the signed binary releases this deployment can serve.  Metadata only, and no download URL: the artifact is behind GET /v1/licensing/download/{release}, which is gated on a valid license token. Knowing that a release exists is not permission to run it, which is why this list needs no license of its own.
     * Lists the signed binary releases this deployment can serve.
     */
    async getLicensingReleases(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingReleaseList> {
        const response = await this.getLicensingReleasesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads one release\'s metadata: its product, version, platform and the cosign material a client verifies the binary against.  An unknown id is 404. Like the list, this is metadata only — the bytes are behind the license-gated download.
     * Reads one release\'s metadata: its product, version, platform and the cosign material a client verifies the binary against.
     */
    async getLicensingReleasesByReleaseRaw(requestParameters: LicensingApiGetLicensingReleasesByReleaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingRelease>> {
        if (requestParameters['release'] == null) {
            throw new runtime.RequiredError(
                'release',
                'Required parameter "release" was null or undefined when calling getLicensingReleasesByRelease().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/releases/{release}`;
        urlPath = urlPath.replace(`{${"release"}}`, encodeURIComponent(String(requestParameters['release'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingReleaseFromJSON(jsonValue));
    }

    /**
     * Reads one release\'s metadata: its product, version, platform and the cosign material a client verifies the binary against.  An unknown id is 404. Like the list, this is metadata only — the bytes are behind the license-gated download.
     * Reads one release\'s metadata: its product, version, platform and the cosign material a client verifies the binary against.
     */
    async getLicensingReleasesByRelease(requestParameters: LicensingApiGetLicensingReleasesByReleaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingRelease> {
        const response = await this.getLicensingReleasesByReleaseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Fingerprint turns raw device signals into the opaque value that binds a license to one machine.  This is the anti-copy step: the value returned here is folded into the signed token, so a token minted with it runs only on the device it was bound to. The derivation is one-way and salted — the signals are never stored and never echoed back — so the response is safe to persist client-side and pass to issue. Signals too weak to identify a machine (a hostname alone) are refused rather than turned into a binding that would collide with other machines.
     * Fingerprint turns raw device signals into the opaque value that binds a license to one machine.
     */
    async postLicensingFingerprintRaw(requestParameters: LicensingApiPostLicensingFingerprintRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingFingerprintResponse>> {
        if (requestParameters['licensingFingerprintRequest'] == null) {
            throw new runtime.RequiredError(
                'licensingFingerprintRequest',
                'Required parameter "licensingFingerprintRequest" was null or undefined when calling postLicensingFingerprint().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/fingerprint`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LicensingFingerprintRequestToJSON(requestParameters['licensingFingerprintRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingFingerprintResponseFromJSON(jsonValue));
    }

    /**
     * Fingerprint turns raw device signals into the opaque value that binds a license to one machine.  This is the anti-copy step: the value returned here is folded into the signed token, so a token minted with it runs only on the device it was bound to. The derivation is one-way and salted — the signals are never stored and never echoed back — so the response is safe to persist client-side and pass to issue. Signals too weak to identify a machine (a hostname alone) are refused rather than turned into a binding that would collide with other machines.
     * Fingerprint turns raw device signals into the opaque value that binds a license to one machine.
     */
    async postLicensingFingerprint(requestParameters: LicensingApiPostLicensingFingerprintRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingFingerprintResponse> {
        const response = await this.postLicensingFingerprintRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Issue mints a signed license token for a product the caller\'s org already pays for.  The order is the whole security argument: the caller is an IAM-validated principal, commerce is then asked whether that principal\'s ORG holds an ACTIVE entitlement for the product, and only then is a token signed — by the KMS, never by key material in this process. A product the org does not own answers 403 and no token. The signed features are the plan\'s features verbatim, so the engine enforces exactly what was bought, and the expiry is clamped to the entitlement\'s so a token cannot outlive the subscription that paid for it.  The token is the credential the engine runs on. Treat it as a secret.
     * Issue mints a signed license token for a product the caller\'s org already pays for.
     */
    async postLicensingIssueRaw(requestParameters: LicensingApiPostLicensingIssueRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingIssueResponse>> {
        if (requestParameters['licensingIssueRequest'] == null) {
            throw new runtime.RequiredError(
                'licensingIssueRequest',
                'Required parameter "licensingIssueRequest" was null or undefined when calling postLicensingIssue().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/issue`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LicensingIssueRequestToJSON(requestParameters['licensingIssueRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingIssueResponseFromJSON(jsonValue));
    }

    /**
     * Issue mints a signed license token for a product the caller\'s org already pays for.  The order is the whole security argument: the caller is an IAM-validated principal, commerce is then asked whether that principal\'s ORG holds an ACTIVE entitlement for the product, and only then is a token signed — by the KMS, never by key material in this process. A product the org does not own answers 403 and no token. The signed features are the plan\'s features verbatim, so the engine enforces exactly what was bought, and the expiry is clamped to the entitlement\'s so a token cannot outlive the subscription that paid for it.  The token is the credential the engine runs on. Treat it as a secret.
     * Issue mints a signed license token for a product the caller\'s org already pays for.
     */
    async postLicensingIssue(requestParameters: LicensingApiPostLicensingIssueRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingIssueResponse> {
        const response = await this.postLicensingIssueRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publishes a signed binary release, answering 201 Created.  Outside dev a release MUST carry its cosign signature: this is how a binary becomes downloadable, so accepting an unsigned one would let an unverifiable artifact into the distribution path. Org-admin only — publishing is an operator action, not something a licensee does.
     * Publishes a signed binary release, answering 201 Created.
     */
    async postLicensingReleasesRaw(requestParameters: LicensingApiPostLicensingReleasesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingRelease>> {
        if (requestParameters['licensingRelease'] == null) {
            throw new runtime.RequiredError(
                'licensingRelease',
                'Required parameter "licensingRelease" was null or undefined when calling postLicensingReleases().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/releases`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LicensingReleaseToJSON(requestParameters['licensingRelease']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingReleaseFromJSON(jsonValue));
    }

    /**
     * Publishes a signed binary release, answering 201 Created.  Outside dev a release MUST carry its cosign signature: this is how a binary becomes downloadable, so accepting an unsigned one would let an unverifiable artifact into the distribution path. Org-admin only — publishing is an operator action, not something a licensee does.
     * Publishes a signed binary release, answering 201 Created.
     */
    async postLicensingReleases(requestParameters: LicensingApiPostLicensingReleasesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingRelease> {
        const response = await this.postLicensingReleasesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Revoke turns off tokens that have already been issued.  A signed token cannot be un-signed, so revocation is the only way to withdraw one: this appends an entry that verify and the license-gated download both consult. It is a POST rather than a DELETE because it APPENDS a durable, attributed record — the entry names the admin who recorded it and when — rather than removing one.  Org-admin only. Scope it as narrowly as the incident allows: \"nonce\" for one leaked token, \"holder\" for one compromised account, \"fingerprint\" for one stolen machine, \"release\" when a whole build is bad.
     * Revoke turns off tokens that have already been issued.
     */
    async postLicensingRevokeRaw(requestParameters: LicensingApiPostLicensingRevokeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingRevokeResponse>> {
        if (requestParameters['licensingRevokeRequest'] == null) {
            throw new runtime.RequiredError(
                'licensingRevokeRequest',
                'Required parameter "licensingRevokeRequest" was null or undefined when calling postLicensingRevoke().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/revoke`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LicensingRevokeRequestToJSON(requestParameters['licensingRevokeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingRevokeResponseFromJSON(jsonValue));
    }

    /**
     * Revoke turns off tokens that have already been issued.  A signed token cannot be un-signed, so revocation is the only way to withdraw one: this appends an entry that verify and the license-gated download both consult. It is a POST rather than a DELETE because it APPENDS a durable, attributed record — the entry names the admin who recorded it and when — rather than removing one.  Org-admin only. Scope it as narrowly as the incident allows: \"nonce\" for one leaked token, \"holder\" for one compromised account, \"fingerprint\" for one stolen machine, \"release\" when a whole build is bad.
     * Revoke turns off tokens that have already been issued.
     */
    async postLicensingRevoke(requestParameters: LicensingApiPostLicensingRevokeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingRevokeResponse> {
        const response = await this.postLicensingRevokeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Verify checks a license token online: signature, schema, expiry, app_id and the revocation list.  It is UNAUTHENTICATED and always answers 200 — a bad token is `valid:false` with a reason rather than an error status, because \"is this token good\" is a question anyone may ask about a credential they already hold and the answer is the same either way. It is also OPTIONAL: the engine verifies OFFLINE against the published public key (GET /v1/licensing/pubkey) and needs this endpoint only to learn about revocation, so an outage here never stops a paid customer working.
     * Verify checks a license token online: signature, schema, expiry, app_id and the revocation list.
     */
    async postLicensingVerifyRaw(requestParameters: LicensingApiPostLicensingVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LicensingVerifyResponse>> {
        if (requestParameters['licensingVerifyRequest'] == null) {
            throw new runtime.RequiredError(
                'licensingVerifyRequest',
                'Required parameter "licensingVerifyRequest" was null or undefined when calling postLicensingVerify().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/licensing/verify`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LicensingVerifyRequestToJSON(requestParameters['licensingVerifyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LicensingVerifyResponseFromJSON(jsonValue));
    }

    /**
     * Verify checks a license token online: signature, schema, expiry, app_id and the revocation list.  It is UNAUTHENTICATED and always answers 200 — a bad token is `valid:false` with a reason rather than an error status, because \"is this token good\" is a question anyone may ask about a credential they already hold and the answer is the same either way. It is also OPTIONAL: the engine verifies OFFLINE against the published public key (GET /v1/licensing/pubkey) and needs this endpoint only to learn about revocation, so an outage here never stops a paid customer working.
     * Verify checks a license token online: signature, schema, expiry, app_id and the revocation list.
     */
    async postLicensingVerify(requestParameters: LicensingApiPostLicensingVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LicensingVerifyResponse> {
        const response = await this.postLicensingVerifyRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
