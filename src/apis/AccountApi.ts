/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ApiKeyList,
  Appearance,
  CsrfResp,
  EmbedStatusResp,
  KeyTypeIn,
  MintedKey,
  OnboardReq,
  OnboardResp,
  RevokedKey,
} from '../models/index.js';
import {
    ApiKeyListFromJSON,
    ApiKeyListToJSON,
    AppearanceFromJSON,
    AppearanceToJSON,
    CsrfRespFromJSON,
    CsrfRespToJSON,
    EmbedStatusRespFromJSON,
    EmbedStatusRespToJSON,
    KeyTypeInFromJSON,
    KeyTypeInToJSON,
    MintedKeyFromJSON,
    MintedKeyToJSON,
    OnboardReqFromJSON,
    OnboardReqToJSON,
    OnboardRespFromJSON,
    OnboardRespToJSON,
    RevokedKeyFromJSON,
    RevokedKeyToJSON,
} from '../models/index.js';

export interface AccountApiDeleteAccountKeysRequest {
    type?: string;
}

export interface AccountApiGetAccountAvatarByOrgByUserByDigestRequest {
    org: string;
    user: string;
    digest: string;
}

export interface AccountApiGetAccountEmbedRequest {
    app?: string;
}

export interface AccountApiPostAccountAppearanceRequest {
    appearance: Appearance;
}

export interface AccountApiPostAccountKeysRequest {
    keyTypeIn: KeyTypeIn;
}

export interface AccountApiPostAccountOrgsRequest {
    onboardReq: OnboardReq;
}

/**
 * 
 */
export class AccountApi extends runtime.BaseAPI {

    /**
     * Revokes the caller\'s own API key of the requested class. The class is the same field mint takes — `?type=publishable`, defaulting to secret — so revoking the key that ships in a browser bundle does not sign its holder out of their own API: the other key keeps working.  Revoking is how a key is replaced when it does not need replacing; minting the same class again rotates it in one step. IAM drops the credential immediately, but the gateway caches keys for a few minutes, so a request that beat the cache expiry may still be served.  For callers written against the older shape, the class is also accepted in a JSON request body, read only when `?type=` is absent.
     * Revokes the caller\'s own API key of the requested class.
     */
    async deleteAccountKeysRaw(requestParameters: AccountApiDeleteAccountKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevokedKey>> {
        const queryParameters: any = {};

        if (requestParameters['type'] != null) {
            queryParameters['type'] = requestParameters['type'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/keys`;

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevokedKeyFromJSON(jsonValue));
    }

    /**
     * Revokes the caller\'s own API key of the requested class. The class is the same field mint takes — `?type=publishable`, defaulting to secret — so revoking the key that ships in a browser bundle does not sign its holder out of their own API: the other key keeps working.  Revoking is how a key is replaced when it does not need replacing; minting the same class again rotates it in one step. IAM drops the credential immediately, but the gateway caches keys for a few minutes, so a request that beat the cache expiry may still be served.  For callers written against the older shape, the class is also accepted in a JSON request body, read only when `?type=` is absent.
     * Revokes the caller\'s own API key of the requested class.
     */
    async deleteAccountKeys(requestParameters: AccountApiDeleteAccountKeysRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevokedKey> {
        const response = await this.deleteAccountKeysRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
     */
    async getAccountAppearanceRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Appearance>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/appearance`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppearanceFromJSON(jsonValue));
    }

    /**
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
     */
    async getAccountAppearance(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Appearance> {
        const response = await this.getAccountAppearanceRaw(initOverrides);
        return await response.value();
    }

    /**
     * Streams a profile photo\'s raw BYTES. This is the address stored on the user\'s IAM record and rendered directly by an `<img>`, so it takes no credentials — the 64-hex content digest in the path is the capability, and it can only be produced by someone who already has the image.  The Content-Type is derived from the stored bytes and the response carries nosniff, so only a real raster image is ever served and only under its true type. Anything else — a miss, a malformed path, an object that is not an image — is one 404, and a hit caches for a year because the address is the content.
     * Fetch a profile photo
     */
    async getAccountAvatarByOrgByUserByDigestRaw(requestParameters: AccountApiGetAccountAvatarByOrgByUserByDigestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling getAccountAvatarByOrgByUserByDigest().'
            );
        }

        if (requestParameters['user'] == null) {
            throw new runtime.RequiredError(
                'user',
                'Required parameter "user" was null or undefined when calling getAccountAvatarByOrgByUserByDigest().'
            );
        }

        if (requestParameters['digest'] == null) {
            throw new runtime.RequiredError(
                'digest',
                'Required parameter "digest" was null or undefined when calling getAccountAvatarByOrgByUserByDigest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/avatar/{org}/{user}/{digest}`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"user"}}`, encodeURIComponent(String(requestParameters['user'])));
        urlPath = urlPath.replace(`{${"digest"}}`, encodeURIComponent(String(requestParameters['digest'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Streams a profile photo\'s raw BYTES. This is the address stored on the user\'s IAM record and rendered directly by an `<img>`, so it takes no credentials — the 64-hex content digest in the path is the capability, and it can only be produced by someone who already has the image.  The Content-Type is derived from the stored bytes and the response carries nosniff, so only a real raster image is ever served and only under its true type. Anything else — a miss, a malformed path, an object that is not an image — is one 404, and a hit caches for a year because the address is the content.
     * Fetch a profile photo
     */
    async getAccountAvatarByOrgByUserByDigest(requestParameters: AccountApiGetAccountAvatarByOrgByUserByDigestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getAccountAvatarByOrgByUserByDigestRaw(requestParameters, initOverrides);
    }

    /**
     * IssueCSRFToken mints the anti-CSRF token a browser echoes as X-CSRF-Token on every money write (mint/revoke a key, top up, onboard, and the billing/commerce write verbs). The token is bound to the caller\'s validated identity and expires, so one minted for one identity cannot authorize a write as another.  It is answered no-store, so it is never cached by a shared proxy. This is the same-origin endpoint the embedded console reads — the Same-Origin Policy is what stops a cross-site page from reading the response and forging a write.
     * IssueCSRFToken mints the anti-CSRF token a browser echoes as X-CSRF-Token on every money write (mint/revoke a key, top up, onboard, and the billing/commerce write verbs).
     */
    async getAccountCsrfRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CsrfResp>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/csrf`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CsrfRespFromJSON(jsonValue));
    }

    /**
     * IssueCSRFToken mints the anti-CSRF token a browser echoes as X-CSRF-Token on every money write (mint/revoke a key, top up, onboard, and the billing/commerce write verbs). The token is bound to the caller\'s validated identity and expires, so one minted for one identity cannot authorize a write as another.  It is answered no-store, so it is never cached by a shared proxy. This is the same-origin endpoint the embedded console reads — the Same-Origin Policy is what stops a cross-site page from reading the response and forging a write.
     * IssueCSRFToken mints the anti-CSRF token a browser echoes as X-CSRF-Token on every money write (mint/revoke a key, top up, onboard, and the billing/commerce write verbs).
     */
    async getAccountCsrf(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CsrfResp> {
        const response = await this.getAccountCsrfRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports whether one of this brand\'s shared embedded apps (cms, erp, help) may be framed by the caller and is actually running, so a console module can choose between the embed and the provision panel.  It answers two questions the browser cannot answer for itself. ENTITLEMENT is server-authoritative: each app is a single shared per-BRAND instance, so only a member of the owning brand org — or a SuperAdmin — is given the embed URL; every other caller gets phase \"not-entitled\" and no URL. REACHABILITY is a probe of that origin, which a cross-origin page cannot read for itself.  The probed host is always <app>.<this deployment\'s own brand domain>: no part of it comes from the request, so this can never be steered into probing an arbitrary origin.
     * Reports whether one of this brand\'s shared embedded apps (cms, erp, help) may be framed by the caller and is actually running, so a console module can choose between the embed and the provision panel.
     */
    async getAccountEmbedRaw(requestParameters: AccountApiGetAccountEmbedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EmbedStatusResp>> {
        const queryParameters: any = {};

        if (requestParameters['app'] != null) {
            queryParameters['app'] = requestParameters['app'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/embed`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EmbedStatusRespFromJSON(jsonValue));
    }

    /**
     * Reports whether one of this brand\'s shared embedded apps (cms, erp, help) may be framed by the caller and is actually running, so a console module can choose between the embed and the provision panel.  It answers two questions the browser cannot answer for itself. ENTITLEMENT is server-authoritative: each app is a single shared per-BRAND instance, so only a member of the owning brand org — or a SuperAdmin — is given the embed URL; every other caller gets phase \"not-entitled\" and no URL. REACHABILITY is a probe of that origin, which a cross-origin page cannot read for itself.  The probed host is always <app>.<this deployment\'s own brand domain>: no part of it comes from the request, so this can never be steered into probing an arbitrary origin.
     * Reports whether one of this brand\'s shared embedded apps (cms, erp, help) may be framed by the caller and is actually running, so a console module can choose between the embed and the provision panel.
     */
    async getAccountEmbed(requestParameters: AccountApiGetAccountEmbedRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EmbedStatusResp> {
        const response = await this.getAccountEmbedRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago. No secret material comes back: a secret key is represented by its prefix, and only a publishable key (public by construction) carries its full value.  A transient IAM read failure reports an empty set rather than a 5xx, so the page shows the honest empty state and never a fabricated key.
     * Returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago.
     */
    async getAccountKeysRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ApiKeyList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/keys`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ApiKeyListFromJSON(jsonValue));
    }

    /**
     * Returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago. No secret material comes back: a secret key is represented by its prefix, and only a publishable key (public by construction) carries its full value.  A transient IAM read failure reports an empty set rather than a 5xx, so the page shows the honest empty state and never a fabricated key.
     * Returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago.
     */
    async getAccountKeys(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ApiKeyList> {
        const response = await this.getAccountKeysRaw(initOverrides);
        return await response.value();
    }

    /**
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
     */
    async postAccountAppearanceRaw(requestParameters: AccountApiPostAccountAppearanceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Appearance>> {
        if (requestParameters['appearance'] == null) {
            throw new runtime.RequiredError(
                'appearance',
                'Required parameter "appearance" was null or undefined when calling postAccountAppearance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/appearance`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AppearanceToJSON(requestParameters['appearance']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AppearanceFromJSON(jsonValue));
    }

    /**
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
     */
    async postAccountAppearance(requestParameters: AccountApiPostAccountAppearanceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Appearance> {
        const response = await this.postAccountAppearanceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stores one image as the signed-in user\'s profile photo and answers the URL it is served from, which is also written to the user\'s IAM record — so every surface that already renders `avatar` picks it up with no further call.  The body is a multipart form with a `file` part. The format is decided by the BYTES, never the filename or the part\'s Content-Type: png, jpeg, gif and webp are accepted and everything else is refused with 415, so an SVG cannot be stored as a picture and later served as a program. Over 8 MiB is 413; empty is 400.  The photo is addressed by the sha256 of its bytes, so setting a new one yields a new URL rather than a stale cache of the old face. The caller is taken from the validated identity ONLY — there is no way to name a different subject — so this always sets your own photo, and a caller with no organization yet is refused.
     * Set your profile photo
     */
    async postAccountAvatarRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/avatar`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Stores one image as the signed-in user\'s profile photo and answers the URL it is served from, which is also written to the user\'s IAM record — so every surface that already renders `avatar` picks it up with no further call.  The body is a multipart form with a `file` part. The format is decided by the BYTES, never the filename or the part\'s Content-Type: png, jpeg, gif and webp are accepted and everything else is refused with 415, so an SVG cannot be stored as a picture and later served as a program. Over 8 MiB is 413; empty is 400.  The photo is addressed by the sha256 of its bytes, so setting a new one yields a new URL rather than a stale cache of the old face. The caller is taken from the validated identity ONLY — there is no way to name a different subject — so this always sets your own photo, and a caller with no organization yet is refused.
     * Set your profile photo
     */
    async postAccountAvatar(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postAccountAvatarRaw(initOverrides);
    }

    /**
     * Creates — or rotates — the caller\'s API key of the requested type and returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.  Rotating is what creating means here: a user holds one key per type, so the endpoint is idempotent by (caller, type) and the superseded credential stops working. Two live secrets for one user would make \"revoke my key\" a lie.
     * Creates — or rotates — the caller\'s API key of the requested type and returns it ONCE.
     */
    async postAccountKeysRaw(requestParameters: AccountApiPostAccountKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MintedKey>> {
        if (requestParameters['keyTypeIn'] == null) {
            throw new runtime.RequiredError(
                'keyTypeIn',
                'Required parameter "keyTypeIn" was null or undefined when calling postAccountKeys().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/keys`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: KeyTypeInToJSON(requestParameters['keyTypeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MintedKeyFromJSON(jsonValue));
    }

    /**
     * Creates — or rotates — the caller\'s API key of the requested type and returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.  Rotating is what creating means here: a user holds one key per type, so the endpoint is idempotent by (caller, type) and the superseded credential stops working. Two live secrets for one user would make \"revoke my key\" a lie.
     * Creates — or rotates — the caller\'s API key of the requested type and returns it ONCE.
     */
    async postAccountKeys(requestParameters: AccountApiPostAccountKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MintedKey> {
        const response = await this.postAccountKeysRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no home org): create + MOVE the user in as admin, so their next     JWT carries the new owner and the cloud scopes everything to it. This is the     path a fresh OAuth sign-up takes, from the sign-up application\'s org.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
     * Onboard creates the caller\'s organization.
     */
    async postAccountOrgsRaw(requestParameters: AccountApiPostAccountOrgsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OnboardResp>> {
        if (requestParameters['onboardReq'] == null) {
            throw new runtime.RequiredError(
                'onboardReq',
                'Required parameter "onboardReq" was null or undefined when calling postAccountOrgs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/orgs`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OnboardReqToJSON(requestParameters['onboardReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OnboardRespFromJSON(jsonValue));
    }

    /**
     * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no home org): create + MOVE the user in as admin, so their next     JWT carries the new owner and the cloud scopes everything to it. This is the     path a fresh OAuth sign-up takes, from the sign-up application\'s org.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
     * Onboard creates the caller\'s organization.
     */
    async postAccountOrgs(requestParameters: AccountApiPostAccountOrgsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OnboardResp> {
        const response = await this.postAccountOrgsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
