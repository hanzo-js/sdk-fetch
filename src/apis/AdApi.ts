/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AdCampaign,
  AdSummary,
  CampaignInput,
  CampaignList,
  UpdateCampaignIn,
} from '../models/index.js';
import {
    AdCampaignFromJSON,
    AdCampaignToJSON,
    AdSummaryFromJSON,
    AdSummaryToJSON,
    CampaignInputFromJSON,
    CampaignInputToJSON,
    CampaignListFromJSON,
    CampaignListToJSON,
    UpdateCampaignInFromJSON,
    UpdateCampaignInToJSON,
} from '../models/index.js';

export interface AdApiDeleteAdCampaignsByIdRequest {
    id: string;
}

export interface AdApiGetAdCampaignsRequest {
    status?: string;
    limit?: number;
}

export interface AdApiGetAdCampaignsByIdRequest {
    id: string;
}

export interface AdApiPostAdCampaignsRequest {
    campaignInput: CampaignInput;
}

export interface AdApiPostAdCampaignsByIdLaunchRequest {
    id: string;
}

export interface AdApiPutAdCampaignsByIdRequest {
    id: string;
    updateCampaignIn: UpdateCampaignIn;
}

/**
 * 
 */
export class AdApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s campaigns and answers 204 with no body. It deletes the stored record only: a campaign already launched keeps running on the ad network, which must be stopped there. An id another org owns reads as not found.
     * Removes one of the caller org\'s campaigns and answers 204 with no body.
     */
    async deleteAdCampaignsByIdRaw(requestParameters: AdApiDeleteAdCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteAdCampaignsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s campaigns and answers 204 with no body. It deletes the stored record only: a campaign already launched keeps running on the ad network, which must be stopped there. An id another org owns reads as not found.
     * Removes one of the caller org\'s campaigns and answers 204 with no body.
     */
    async deleteAdCampaignsById(requestParameters: AdApiDeleteAdCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteAdCampaignsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the caller org\'s ad campaigns, most recently updated first, optionally narrowed to one lifecycle status. The listing is bounded by the org: another tenant\'s campaigns are not reachable from here at all.
     * Returns the caller org\'s ad campaigns, most recently updated first, optionally narrowed to one lifecycle status.
     */
    async getAdCampaignsRaw(requestParameters: AdApiGetAdCampaignsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignList>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/campaigns`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s ad campaigns, most recently updated first, optionally narrowed to one lifecycle status. The listing is bounded by the org: another tenant\'s campaigns are not reachable from here at all.
     * Returns the caller org\'s ad campaigns, most recently updated first, optionally narrowed to one lifecycle status.
     */
    async getAdCampaigns(requestParameters: AdApiGetAdCampaignsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignList> {
        const response = await this.getAdCampaignsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s campaigns. An id another org owns reads as not found, so the response cannot confirm that it exists.
     * Returns one of the caller org\'s campaigns.
     */
    async getAdCampaignsByIdRaw(requestParameters: AdApiGetAdCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdCampaign>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAdCampaignsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdCampaignFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s campaigns. An id another org owns reads as not found, so the response cannot confirm that it exists.
     * Returns one of the caller org\'s campaigns.
     */
    async getAdCampaignsById(requestParameters: AdApiGetAdCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdCampaign> {
        const response = await this.getAdCampaignsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rolls the caller org\'s ad campaigns up into four numbers: how many campaigns exist, how many are active, and the summed budget and spend across all of them. Budget and spend are MINOR units (cents), the same units the campaign rows carry. It counts only this org\'s campaigns.
     * Rolls the caller org\'s ad campaigns up into four numbers: how many campaigns exist, how many are active, and the summed budget and spend across all of them.
     */
    async getAdSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdSummary>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdSummaryFromJSON(jsonValue));
    }

    /**
     * Rolls the caller org\'s ad campaigns up into four numbers: how many campaigns exist, how many are active, and the summed budget and spend across all of them. Budget and spend are MINOR units (cents), the same units the campaign rows carry. It counts only this org\'s campaigns.
     * Rolls the caller org\'s ad campaigns up into four numbers: how many campaigns exist, how many are active, and the summed budget and spend across all of them.
     */
    async getAdSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdSummary> {
        const response = await this.getAdSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Registers a new ad campaign for the caller\'s org and answers 201 with the stored row. It only records the campaign — nothing is sent to the ad network until POST /v1/ad/campaigns/{id}/launch runs it. The org is stamped by the server from the validated principal, so a body can never place a campaign in another tenant.
     * Registers a new ad campaign for the caller\'s org and answers 201 with the stored row.
     */
    async postAdCampaignsRaw(requestParameters: AdApiPostAdCampaignsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdCampaign>> {
        if (requestParameters['campaignInput'] == null) {
            throw new runtime.RequiredError(
                'campaignInput',
                'Required parameter "campaignInput" was null or undefined when calling postAdCampaigns().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/campaigns`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignInputToJSON(requestParameters['campaignInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdCampaignFromJSON(jsonValue));
    }

    /**
     * Registers a new ad campaign for the caller\'s org and answers 201 with the stored row. It only records the campaign — nothing is sent to the ad network until POST /v1/ad/campaigns/{id}/launch runs it. The org is stamped by the server from the validated principal, so a body can never place a campaign in another tenant.
     * Registers a new ad campaign for the caller\'s org and answers 201 with the stored row.
     */
    async postAdCampaigns(requestParameters: AdApiPostAdCampaignsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdCampaign> {
        const response = await this.postAdCampaignsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates the campaign on its platform under the CALLER ORG\'S own connected ad account, records the provider campaign id, flips the stored campaign to active and answers the updated record. No ad-network token is held here: it is resolved from KMS through the org\'s connector at launch time, BEFORE any provider call, so an org that has not connected that platform gets 424 and no spend can ever start on a connection the org did not make. Meta is executed for real; a campaign on a platform whose provider is not wired yet answers 501 even when the connector is connected, and an edge failure at the platform is 502. The optional {account} body overrides the target ad account for this launch and is TOLERANT — a malformed or non-JSON body is ignored and the campaign launches on its stored account rather than being refused. A campaign id another org owns reads as not found.
     * Run one of your stored campaigns on its ad network
     */
    async postAdCampaignsByIdLaunchRaw(requestParameters: AdApiPostAdCampaignsByIdLaunchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdCampaignsByIdLaunch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/campaigns/{id}/launch`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates the campaign on its platform under the CALLER ORG\'S own connected ad account, records the provider campaign id, flips the stored campaign to active and answers the updated record. No ad-network token is held here: it is resolved from KMS through the org\'s connector at launch time, BEFORE any provider call, so an org that has not connected that platform gets 424 and no spend can ever start on a connection the org did not make. Meta is executed for real; a campaign on a platform whose provider is not wired yet answers 501 even when the connector is connected, and an edge failure at the platform is 502. The optional {account} body overrides the target ad account for this launch and is TOLERANT — a malformed or non-JSON body is ignored and the campaign launches on its stored account rather than being refused. A campaign id another org owns reads as not found.
     * Run one of your stored campaigns on its ad network
     */
    async postAdCampaignsByIdLaunch(requestParameters: AdApiPostAdCampaignsByIdLaunchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postAdCampaignsByIdLaunchRaw(requestParameters, initOverrides);
    }

    /**
     * Replaces the user-owned fields of one of the caller org\'s campaigns and answers the stored row. It is a full replace, not a patch: every field is written from the request, so an omitted one is cleared. externalId is launch-owned and is never touched here, so editing a campaign cannot break its link to a live provider execution.
     * Replaces the user-owned fields of one of the caller org\'s campaigns and answers the stored row.
     */
    async putAdCampaignsByIdRaw(requestParameters: AdApiPutAdCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdCampaign>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putAdCampaignsById().'
            );
        }

        if (requestParameters['updateCampaignIn'] == null) {
            throw new runtime.RequiredError(
                'updateCampaignIn',
                'Required parameter "updateCampaignIn" was null or undefined when calling putAdCampaignsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ad/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateCampaignInToJSON(requestParameters['updateCampaignIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdCampaignFromJSON(jsonValue));
    }

    /**
     * Replaces the user-owned fields of one of the caller org\'s campaigns and answers the stored row. It is a full replace, not a patch: every field is written from the request, so an omitted one is cleared. externalId is launch-owned and is never touched here, so editing a campaign cannot break its link to a live provider execution.
     * Replaces the user-owned fields of one of the caller org\'s campaigns and answers the stored row.
     */
    async putAdCampaignsById(requestParameters: AdApiPutAdCampaignsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdCampaign> {
        const response = await this.putAdCampaignsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
