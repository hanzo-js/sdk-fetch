/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ApplicationList,
  Company,
  CompanyList,
  CompanyReq,
  Contact,
  ContactList,
  ContactReq,
  CrmSummary,
  OppList,
  OppReq,
  Opportunity,
  PatchApplicationIn,
  ProgramApplication,
} from '../models/index.js';
import {
    ApplicationListFromJSON,
    ApplicationListToJSON,
    CompanyFromJSON,
    CompanyToJSON,
    CompanyListFromJSON,
    CompanyListToJSON,
    CompanyReqFromJSON,
    CompanyReqToJSON,
    ContactFromJSON,
    ContactToJSON,
    ContactListFromJSON,
    ContactListToJSON,
    ContactReqFromJSON,
    ContactReqToJSON,
    CrmSummaryFromJSON,
    CrmSummaryToJSON,
    OppListFromJSON,
    OppListToJSON,
    OppReqFromJSON,
    OppReqToJSON,
    OpportunityFromJSON,
    OpportunityToJSON,
    PatchApplicationInFromJSON,
    PatchApplicationInToJSON,
    ProgramApplicationFromJSON,
    ProgramApplicationToJSON,
} from '../models/index.js';

export interface CrmApiDeleteCrmCompaniesByIdRequest {
    id: string;
}

export interface CrmApiDeleteCrmContactsByIdRequest {
    id: string;
}

export interface CrmApiDeleteCrmOpportunitiesByIdRequest {
    id: string;
}

export interface CrmApiGetCrmApplicationsRequest {
    stage?: string;
    limit?: number;
}

export interface CrmApiGetCrmApplicationsByIdRequest {
    id: string;
}

export interface CrmApiGetCrmCompaniesRequest {
    limit?: number;
}

export interface CrmApiGetCrmCompaniesByIdRequest {
    id: string;
}

export interface CrmApiGetCrmContactsRequest {
    companyId?: string;
    limit?: number;
}

export interface CrmApiGetCrmContactsByIdRequest {
    id: string;
}

export interface CrmApiGetCrmOpportunitiesRequest {
    stage?: string;
    limit?: number;
}

export interface CrmApiGetCrmOpportunitiesByIdRequest {
    id: string;
}

export interface CrmApiPatchCrmApplicationsByIdRequest {
    id: string;
    patchApplicationIn: PatchApplicationIn;
}

export interface CrmApiPostCrmCompaniesRequest {
    companyReq: CompanyReq;
}

export interface CrmApiPostCrmContactsRequest {
    contactReq: ContactReq;
}

export interface CrmApiPostCrmOpportunitiesRequest {
    oppReq: OppReq;
}

export interface CrmApiPutCrmCompaniesByIdRequest {
    id: string;
    companyReq: CompanyReq;
}

export interface CrmApiPutCrmContactsByIdRequest {
    id: string;
    contactReq: ContactReq;
}

export interface CrmApiPutCrmOpportunitiesByIdRequest {
    id: string;
    oppReq: OppReq;
}

/**
 * 
 */
export class CrmApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s companies and answers 204. Any contact or opportunity in the org that referenced it keeps existing with the reference cleared, so nothing is left pointing at a company that is gone.
     * Removes one of the caller org\'s companies and answers 204.
     */
    async deleteCrmCompaniesByIdRaw(requestParameters: CrmApiDeleteCrmCompaniesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCrmCompaniesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/companies/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s companies and answers 204. Any contact or opportunity in the org that referenced it keeps existing with the reference cleared, so nothing is left pointing at a company that is gone.
     * Removes one of the caller org\'s companies and answers 204.
     */
    async deleteCrmCompaniesById(requestParameters: CrmApiDeleteCrmCompaniesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteCrmCompaniesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one of the caller org\'s contacts and answers 204. Any opportunity in the org that named it point of contact keeps existing with that reference cleared.
     * Removes one of the caller org\'s contacts and answers 204.
     */
    async deleteCrmContactsByIdRaw(requestParameters: CrmApiDeleteCrmContactsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCrmContactsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/contacts/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s contacts and answers 204. Any opportunity in the org that named it point of contact keeps existing with that reference cleared.
     * Removes one of the caller org\'s contacts and answers 204.
     */
    async deleteCrmContactsById(requestParameters: CrmApiDeleteCrmContactsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteCrmContactsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one of the caller org\'s deals and answers 204.
     * Removes one of the caller org\'s deals and answers 204.
     */
    async deleteCrmOpportunitiesByIdRaw(requestParameters: CrmApiDeleteCrmOpportunitiesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteCrmOpportunitiesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/opportunities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s deals and answers 204.
     * Removes one of the caller org\'s deals and answers 204.
     */
    async deleteCrmOpportunitiesById(requestParameters: CrmApiDeleteCrmOpportunitiesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteCrmOpportunitiesByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the org\'s Startup Program applications, newest first. Each carries its AI screen and its stage history; a stage narrows the page to one pipeline stage.
     * Returns the org\'s Startup Program applications, newest first.
     */
    async getCrmApplicationsRaw(requestParameters: CrmApiGetCrmApplicationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ApplicationList>> {
        const queryParameters: any = {};

        if (requestParameters['stage'] != null) {
            queryParameters['stage'] = requestParameters['stage'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/applications`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ApplicationListFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s Startup Program applications, newest first. Each carries its AI screen and its stage history; a stage narrows the page to one pipeline stage.
     * Returns the org\'s Startup Program applications, newest first.
     */
    async getCrmApplications(requestParameters: CrmApiGetCrmApplicationsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ApplicationList> {
        const response = await this.getCrmApplicationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one Startup Program application with its AI screen and stage history. An id belonging to another org reads as not found.
     * Returns one Startup Program application with its AI screen and stage history.
     */
    async getCrmApplicationsByIdRaw(requestParameters: CrmApiGetCrmApplicationsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProgramApplication>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCrmApplicationsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/applications/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProgramApplicationFromJSON(jsonValue));
    }

    /**
     * Returns one Startup Program application with its AI screen and stage history. An id belonging to another org reads as not found.
     * Returns one Startup Program application with its AI screen and stage history.
     */
    async getCrmApplicationsById(requestParameters: CrmApiGetCrmApplicationsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProgramApplication> {
        const response = await this.getCrmApplicationsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s companies, most recently updated first.
     * Returns the caller org\'s companies, most recently updated first.
     */
    async getCrmCompaniesRaw(requestParameters: CrmApiGetCrmCompaniesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompanyList>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/companies`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s companies, most recently updated first.
     * Returns the caller org\'s companies, most recently updated first.
     */
    async getCrmCompanies(requestParameters: CrmApiGetCrmCompaniesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompanyList> {
        const response = await this.getCrmCompaniesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s companies. An id belonging to another org reads as not found.
     * Returns one of the caller org\'s companies.
     */
    async getCrmCompaniesByIdRaw(requestParameters: CrmApiGetCrmCompaniesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Company>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCrmCompaniesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/companies/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s companies. An id belonging to another org reads as not found.
     * Returns one of the caller org\'s companies.
     */
    async getCrmCompaniesById(requestParameters: CrmApiGetCrmCompaniesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Company> {
        const response = await this.getCrmCompaniesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s contacts, most recently updated first. A companyId narrows the page to the people at that company.
     * Returns the caller org\'s contacts, most recently updated first.
     */
    async getCrmContactsRaw(requestParameters: CrmApiGetCrmContactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ContactList>> {
        const queryParameters: any = {};

        if (requestParameters['companyId'] != null) {
            queryParameters['companyId'] = requestParameters['companyId'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/contacts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ContactListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s contacts, most recently updated first. A companyId narrows the page to the people at that company.
     * Returns the caller org\'s contacts, most recently updated first.
     */
    async getCrmContacts(requestParameters: CrmApiGetCrmContactsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ContactList> {
        const response = await this.getCrmContactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s contacts. An id belonging to another org reads as not found.
     * Returns one of the caller org\'s contacts.
     */
    async getCrmContactsByIdRaw(requestParameters: CrmApiGetCrmContactsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Contact>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCrmContactsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/contacts/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ContactFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s contacts. An id belonging to another org reads as not found.
     * Returns one of the caller org\'s contacts.
     */
    async getCrmContactsById(requestParameters: CrmApiGetCrmContactsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Contact> {
        const response = await this.getCrmContactsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s deals, most recently updated first. A stage narrows the page to one pipeline stage.
     * Returns the caller org\'s deals, most recently updated first.
     */
    async getCrmOpportunitiesRaw(requestParameters: CrmApiGetCrmOpportunitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OppList>> {
        const queryParameters: any = {};

        if (requestParameters['stage'] != null) {
            queryParameters['stage'] = requestParameters['stage'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/opportunities`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OppListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s deals, most recently updated first. A stage narrows the page to one pipeline stage.
     * Returns the caller org\'s deals, most recently updated first.
     */
    async getCrmOpportunities(requestParameters: CrmApiGetCrmOpportunitiesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OppList> {
        const response = await this.getCrmOpportunitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s deals. An id belonging to another org reads as not found.
     * Returns one of the caller org\'s deals.
     */
    async getCrmOpportunitiesByIdRaw(requestParameters: CrmApiGetCrmOpportunitiesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Opportunity>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCrmOpportunitiesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/opportunities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OpportunityFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s deals. An id belonging to another org reads as not found.
     * Returns one of the caller org\'s deals.
     */
    async getCrmOpportunitiesById(requestParameters: CrmApiGetCrmOpportunitiesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Opportunity> {
        const response = await this.getCrmOpportunitiesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Summary counts the caller org\'s CRM records: companies, contacts, opportunities.
     * Summary counts the caller org\'s CRM records: companies, contacts, opportunities.
     */
    async getCrmSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CrmSummary>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CrmSummaryFromJSON(jsonValue));
    }

    /**
     * Summary counts the caller org\'s CRM records: companies, contacts, opportunities.
     * Summary counts the caller org\'s CRM records: companies, contacts, opportunities.
     */
    async getCrmSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CrmSummary> {
        const response = await this.getCrmSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Moves one Startup Program application through the pipeline. The move is recorded on the application\'s timeline, attributed to the calling staff user: it may advance exactly one stage, go back to any earlier stage, reject from any non-rejected stage, or reopen a rejected application to `applied`; anything else is refused. Rejecting requires a reason. A note with no stage change is still recorded.
     * Moves one Startup Program application through the pipeline.
     */
    async patchCrmApplicationsByIdRaw(requestParameters: CrmApiPatchCrmApplicationsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProgramApplication>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling patchCrmApplicationsById().'
            );
        }

        if (requestParameters['patchApplicationIn'] == null) {
            throw new runtime.RequiredError(
                'patchApplicationIn',
                'Required parameter "patchApplicationIn" was null or undefined when calling patchCrmApplicationsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/applications/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchApplicationInToJSON(requestParameters['patchApplicationIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProgramApplicationFromJSON(jsonValue));
    }

    /**
     * Moves one Startup Program application through the pipeline. The move is recorded on the application\'s timeline, attributed to the calling staff user: it may advance exactly one stage, go back to any earlier stage, reject from any non-rejected stage, or reopen a rejected application to `applied`; anything else is refused. Rejecting requires a reason. A note with no stage change is still recorded.
     * Moves one Startup Program application through the pipeline.
     */
    async patchCrmApplicationsById(requestParameters: CrmApiPatchCrmApplicationsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProgramApplication> {
        const response = await this.patchCrmApplicationsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Files an application to the Startup Program and answers the id and pipeline stage it landed at.  This is the ONE unauthenticated route in crm. It takes no principal and never reads a caller org: the application is filed against the DEPLOYMENT\'s own program org — the brand, hanzo unless white-labelled — so there is no tenant to name and none to leak. Reading the application back is staff-only and lives elsewhere.  company, contactName and a parseable email are required; everything else is optional context. Re-submitting the same (email, company) REFRESHES the existing application instead of filing a second one, so an impatient applicant cannot duplicate their own lead — that is a 200 where a first submission is a 201. A filled `hp` honeypot field is answered exactly like a success and stored nowhere, so a bot cannot tell a drop from an accept.  Filing is not screening: the application lands at stage `applied` with its AI screen still pending, and the screen runs afterwards on its own clock. A company and contact are also projected into the program org\'s ordinary CRM lists, best-effort — that projection failing does not fail the application. Bodies over 64 KiB are refused, and submissions are rate-limited.
     * Apply to the Startup Program from the public form
     */
    async postCrmApplicationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/applications`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Files an application to the Startup Program and answers the id and pipeline stage it landed at.  This is the ONE unauthenticated route in crm. It takes no principal and never reads a caller org: the application is filed against the DEPLOYMENT\'s own program org — the brand, hanzo unless white-labelled — so there is no tenant to name and none to leak. Reading the application back is staff-only and lives elsewhere.  company, contactName and a parseable email are required; everything else is optional context. Re-submitting the same (email, company) REFRESHES the existing application instead of filing a second one, so an impatient applicant cannot duplicate their own lead — that is a 200 where a first submission is a 201. A filled `hp` honeypot field is answered exactly like a success and stored nowhere, so a bot cannot tell a drop from an accept.  Filing is not screening: the application lands at stage `applied` with its AI screen still pending, and the screen runs afterwards on its own clock. A company and contact are also projected into the program org\'s ordinary CRM lists, best-effort — that projection failing does not fail the application. Bodies over 64 KiB are refused, and submissions are rate-limited.
     * Apply to the Startup Program from the public form
     */
    async postCrmApplications(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postCrmApplicationsRaw(initOverrides);
    }

    /**
     * Adds a company to the caller\'s org and answers 201 with the stored record. A name is required; an empty currency defaults to USD.
     * Adds a company to the caller\'s org and answers 201 with the stored record.
     */
    async postCrmCompaniesRaw(requestParameters: CrmApiPostCrmCompaniesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Company>> {
        if (requestParameters['companyReq'] == null) {
            throw new runtime.RequiredError(
                'companyReq',
                'Required parameter "companyReq" was null or undefined when calling postCrmCompanies().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/companies`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CompanyReqToJSON(requestParameters['companyReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * Adds a company to the caller\'s org and answers 201 with the stored record. A name is required; an empty currency defaults to USD.
     * Adds a company to the caller\'s org and answers 201 with the stored record.
     */
    async postCrmCompanies(requestParameters: CrmApiPostCrmCompaniesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Company> {
        const response = await this.postCrmCompaniesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a person to the caller\'s org and answers 201 with the stored record. One of firstName, lastName or email is required, and a companyId must name a company in the same org.
     * Adds a person to the caller\'s org and answers 201 with the stored record.
     */
    async postCrmContactsRaw(requestParameters: CrmApiPostCrmContactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Contact>> {
        if (requestParameters['contactReq'] == null) {
            throw new runtime.RequiredError(
                'contactReq',
                'Required parameter "contactReq" was null or undefined when calling postCrmContacts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/contacts`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ContactReqToJSON(requestParameters['contactReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ContactFromJSON(jsonValue));
    }

    /**
     * Adds a person to the caller\'s org and answers 201 with the stored record. One of firstName, lastName or email is required, and a companyId must name a company in the same org.
     * Adds a person to the caller\'s org and answers 201 with the stored record.
     */
    async postCrmContacts(requestParameters: CrmApiPostCrmContactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Contact> {
        const response = await this.postCrmContactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a deal to the caller\'s org and answers 201 with the stored record. A name is required; the stage defaults to NEW; companyId and pointOfContactId must name records in the same org.
     * Adds a deal to the caller\'s org and answers 201 with the stored record.
     */
    async postCrmOpportunitiesRaw(requestParameters: CrmApiPostCrmOpportunitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Opportunity>> {
        if (requestParameters['oppReq'] == null) {
            throw new runtime.RequiredError(
                'oppReq',
                'Required parameter "oppReq" was null or undefined when calling postCrmOpportunities().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/opportunities`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OppReqToJSON(requestParameters['oppReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OpportunityFromJSON(jsonValue));
    }

    /**
     * Adds a deal to the caller\'s org and answers 201 with the stored record. A name is required; the stage defaults to NEW; companyId and pointOfContactId must name records in the same org.
     * Adds a deal to the caller\'s org and answers 201 with the stored record.
     */
    async postCrmOpportunities(requestParameters: CrmApiPostCrmOpportunitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Opportunity> {
        const response = await this.postCrmOpportunitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces one of the caller org\'s companies. Every writable field is taken from the request, so a field the request omits is CLEARED — send the whole record. A name is required.
     * Replaces one of the caller org\'s companies.
     */
    async putCrmCompaniesByIdRaw(requestParameters: CrmApiPutCrmCompaniesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Company>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putCrmCompaniesById().'
            );
        }

        if (requestParameters['companyReq'] == null) {
            throw new runtime.RequiredError(
                'companyReq',
                'Required parameter "companyReq" was null or undefined when calling putCrmCompaniesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/companies/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CompanyReqToJSON(requestParameters['companyReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * Replaces one of the caller org\'s companies. Every writable field is taken from the request, so a field the request omits is CLEARED — send the whole record. A name is required.
     * Replaces one of the caller org\'s companies.
     */
    async putCrmCompaniesById(requestParameters: CrmApiPutCrmCompaniesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Company> {
        const response = await this.putCrmCompaniesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces one of the caller org\'s contacts. Every writable field is taken from the request, so a field the request omits is CLEARED — send the whole record. One of firstName, lastName or email is required.
     * Replaces one of the caller org\'s contacts.
     */
    async putCrmContactsByIdRaw(requestParameters: CrmApiPutCrmContactsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Contact>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putCrmContactsById().'
            );
        }

        if (requestParameters['contactReq'] == null) {
            throw new runtime.RequiredError(
                'contactReq',
                'Required parameter "contactReq" was null or undefined when calling putCrmContactsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/contacts/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ContactReqToJSON(requestParameters['contactReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ContactFromJSON(jsonValue));
    }

    /**
     * Replaces one of the caller org\'s contacts. Every writable field is taken from the request, so a field the request omits is CLEARED — send the whole record. One of firstName, lastName or email is required.
     * Replaces one of the caller org\'s contacts.
     */
    async putCrmContactsById(requestParameters: CrmApiPutCrmContactsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Contact> {
        const response = await this.putCrmContactsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces one of the caller org\'s deals. Every writable field is taken from the request, so a field the request omits is CLEARED — send the whole record. A name is required and the stage must be a pipeline stage.
     * Replaces one of the caller org\'s deals.
     */
    async putCrmOpportunitiesByIdRaw(requestParameters: CrmApiPutCrmOpportunitiesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Opportunity>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putCrmOpportunitiesById().'
            );
        }

        if (requestParameters['oppReq'] == null) {
            throw new runtime.RequiredError(
                'oppReq',
                'Required parameter "oppReq" was null or undefined when calling putCrmOpportunitiesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/crm/opportunities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: OppReqToJSON(requestParameters['oppReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OpportunityFromJSON(jsonValue));
    }

    /**
     * Replaces one of the caller org\'s deals. Every writable field is taken from the request, so a field the request omits is CLEARED — send the whole record. A name is required and the stage must be a pipeline stage.
     * Replaces one of the caller org\'s deals.
     */
    async putCrmOpportunitiesById(requestParameters: CrmApiPutCrmOpportunitiesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Opportunity> {
        const response = await this.putCrmOpportunitiesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
