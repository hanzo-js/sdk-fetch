/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  BucketIn,
  BucketItem,
  BucketList,
  ObjectList,
  PresignResponse,
  S3Health,
  UploadIn,
} from '../models/index.js';
import {
    BucketInFromJSON,
    BucketInToJSON,
    BucketItemFromJSON,
    BucketItemToJSON,
    BucketListFromJSON,
    BucketListToJSON,
    ObjectListFromJSON,
    ObjectListToJSON,
    PresignResponseFromJSON,
    PresignResponseToJSON,
    S3HealthFromJSON,
    S3HealthToJSON,
    UploadInFromJSON,
    UploadInToJSON,
} from '../models/index.js';

export interface S3ApiDeleteS3BucketsByBucketRequest {
    bucket: string;
}

export interface S3ApiGetS3BucketsByBucketObjectsRequest {
    bucket: string;
    prefix?: string;
    recursive?: string;
}

export interface S3ApiPostS3BucketsRequest {
    bucketIn: BucketIn;
}

export interface S3ApiPostS3BucketsByBucketObjectsRequest {
    bucket: string;
    uploadIn: UploadIn;
}

/**
 * 
 */
export class S3Api extends runtime.BaseAPI {

    /**
     * Removes an EMPTY bucket and answers 204.  A non-empty bucket is 409 rather than a cascade: deleting a tenant\'s objects behind a single bucket call is not a thing this surface will do silently. A bucket the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the bucket is gone.
     * Removes an EMPTY bucket and answers 204.
     */
    async deleteS3BucketsByBucketRaw(requestParameters: S3ApiDeleteS3BucketsByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling deleteS3BucketsByBucket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/s3/buckets/{bucket}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes an EMPTY bucket and answers 204.  A non-empty bucket is 409 rather than a cascade: deleting a tenant\'s objects behind a single bucket call is not a thing this surface will do silently. A bucket the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the bucket is gone.
     * Removes an EMPTY bucket and answers 204.
     */
    async deleteS3BucketsByBucket(requestParameters: S3ApiDeleteS3BucketsByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteS3BucketsByBucketRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the caller org\'s own buckets.  Only the caller\'s: every bucket is physically named under a per-org prefix and the listing strips that prefix, so a tenant sees friendly names and another tenant\'s buckets are not in the answer at all. Another org\'s bucket is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
     * Lists the caller org\'s own buckets.
     */
    async getS3BucketsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BucketList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/s3/buckets`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BucketListFromJSON(jsonValue));
    }

    /**
     * Lists the caller org\'s own buckets.  Only the caller\'s: every bucket is physically named under a per-org prefix and the listing strips that prefix, so a tenant sees friendly names and another tenant\'s buckets are not in the answer at all. Another org\'s bucket is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
     * Lists the caller org\'s own buckets.
     */
    async getS3Buckets(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BucketList> {
        const response = await this.getS3BucketsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists one folder level of a bucket.  Folder-style by default: sub-prefixes come back as directory entries, which is the file-manager view. `?recursive=true` lists every key flat under the prefix instead. Keys are RELATIVE to `?prefix=`, and the listing is bounded so a huge bucket cannot exhaust memory — Total is what came back, not what the bucket holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * Lists one folder level of a bucket.
     */
    async getS3BucketsByBucketObjectsRaw(requestParameters: S3ApiGetS3BucketsByBucketObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ObjectList>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling getS3BucketsByBucketObjects().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['prefix'] != null) {
            queryParameters['prefix'] = requestParameters['prefix'];
        }

        if (requestParameters['recursive'] != null) {
            queryParameters['recursive'] = requestParameters['recursive'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/s3/buckets/{bucket}/objects`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ObjectListFromJSON(jsonValue));
    }

    /**
     * Lists one folder level of a bucket.  Folder-style by default: sub-prefixes come back as directory entries, which is the file-manager view. `?recursive=true` lists every key flat under the prefix instead. Keys are RELATIVE to `?prefix=`, and the listing is bounded so a huge bucket cannot exhaust memory — Total is what came back, not what the bucket holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * Lists one folder level of a bucket.
     */
    async getS3BucketsByBucketObjects(requestParameters: S3ApiGetS3BucketsByBucketObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ObjectList> {
        const response = await this.getS3BucketsByBucketObjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Health reports whether this deployment can serve object storage.  It is a REAL probe rather than a constant: 200 when admin credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no bucket and bills nothing.
     * Health reports whether this deployment can serve object storage.
     */
    async getS3HealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<S3Health>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/s3/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => S3HealthFromJSON(jsonValue));
    }

    /**
     * Health reports whether this deployment can serve object storage.  It is a REAL probe rather than a constant: 200 when admin credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no bucket and bills nothing.
     * Health reports whether this deployment can serve object storage.
     */
    async getS3Health(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<S3Health> {
        const response = await this.getS3HealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * Makes a new bucket for the caller\'s org and answers 201 with it.  The physical name is derived from the caller\'s validated org, so a tenant can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the bucket exists.
     * Makes a new bucket for the caller\'s org and answers 201 with it.
     */
    async postS3BucketsRaw(requestParameters: S3ApiPostS3BucketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BucketItem>> {
        if (requestParameters['bucketIn'] == null) {
            throw new runtime.RequiredError(
                'bucketIn',
                'Required parameter "bucketIn" was null or undefined when calling postS3Buckets().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/s3/buckets`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BucketInToJSON(requestParameters['bucketIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BucketItemFromJSON(jsonValue));
    }

    /**
     * Makes a new bucket for the caller\'s org and answers 201 with it.  The physical name is derived from the caller\'s validated org, so a tenant can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the bucket exists.
     * Makes a new bucket for the caller\'s org and answers 201 with it.
     */
    async postS3Buckets(requestParameters: S3ApiPostS3BucketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BucketItem> {
        const response = await this.postS3BucketsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Mints a presigned PUT URL the caller uploads to DIRECTLY.  The bytes never pass through this binary and the admin credential never leaves the server: the URL is signed against the PUBLIC host, scoped to exactly this bucket and key, and expires. A deployment with no public endpoint configured cannot mint one and answers 503 rather than a URL that will not work.  Billed per call — for MINTING the URL, which is the work this operation does; the upload that follows it goes straight to the store and is not seen here. The balance is checked BEFORE anything is touched, so an unfunded org is refused with no URL issued.
     * Mints a presigned PUT URL the caller uploads to DIRECTLY.
     */
    async postS3BucketsByBucketObjectsRaw(requestParameters: S3ApiPostS3BucketsByBucketObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PresignResponse>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling postS3BucketsByBucketObjects().'
            );
        }

        if (requestParameters['uploadIn'] == null) {
            throw new runtime.RequiredError(
                'uploadIn',
                'Required parameter "uploadIn" was null or undefined when calling postS3BucketsByBucketObjects().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/s3/buckets/{bucket}/objects`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: UploadInToJSON(requestParameters['uploadIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PresignResponseFromJSON(jsonValue));
    }

    /**
     * Mints a presigned PUT URL the caller uploads to DIRECTLY.  The bytes never pass through this binary and the admin credential never leaves the server: the URL is signed against the PUBLIC host, scoped to exactly this bucket and key, and expires. A deployment with no public endpoint configured cannot mint one and answers 503 rather than a URL that will not work.  Billed per call — for MINTING the URL, which is the work this operation does; the upload that follows it goes straight to the store and is not seen here. The balance is checked BEFORE anything is touched, so an unfunded org is refused with no URL issued.
     * Mints a presigned PUT URL the caller uploads to DIRECTLY.
     */
    async postS3BucketsByBucketObjects(requestParameters: S3ApiPostS3BucketsByBucketObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PresignResponse> {
        const response = await this.postS3BucketsByBucketObjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
