/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EnablementBoard,
  EnablementOptRef,
  UserEnablementItem,
} from '../models/index.js';
import {
    EnablementBoardFromJSON,
    EnablementBoardToJSON,
    EnablementOptRefFromJSON,
    EnablementOptRefToJSON,
    UserEnablementItemFromJSON,
    UserEnablementItemToJSON,
} from '../models/index.js';

export interface EnablementApiPostEnablementOptinRequest {
    enablementOptRef: EnablementOptRef;
}

export interface EnablementApiPostEnablementOptoutRequest {
    enablementOptRef: EnablementOptRef;
}

/**
 * 
 */
export class EnablementApi extends runtime.BaseAPI {

    /**
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in. Read-only and safe for any caller — one without a validated principal simply sees the generally-available items and no opt-in affordance, never another org\'s state.
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in.
     */
    async getEnablementRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EnablementBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/enablement`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EnablementBoardFromJSON(jsonValue));
    }

    /**
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in. Read-only and safe for any caller — one without a validated principal simply sees the generally-available items and no opt-in affordance, never another org\'s state.
     * Returns what the caller\'s org can actually use: every managed item with its global state, whether it is effective here, whether this org is already opted into its beta, and whether it may still opt in.
     */
    async getEnablement(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EnablementBoard> {
        const response = await this.getEnablementRaw(initOverrides);
        return await response.value();
    }

    /**
     * Opts the caller\'s OWN org into a beta item. The org is the caller\'s validated one, so this can never target another org, and the registry refuses anything not in beta — so it can neither re-open an item an operator turned off nor touch one that is already generally available. Requires a signed-in caller with an org.
     * Opts the caller\'s OWN org into a beta item.
     */
    async postEnablementOptinRaw(requestParameters: EnablementApiPostEnablementOptinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserEnablementItem>> {
        if (requestParameters['enablementOptRef'] == null) {
            throw new runtime.RequiredError(
                'enablementOptRef',
                'Required parameter "enablementOptRef" was null or undefined when calling postEnablementOptin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/enablement/optin`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EnablementOptRefToJSON(requestParameters['enablementOptRef']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserEnablementItemFromJSON(jsonValue));
    }

    /**
     * Opts the caller\'s OWN org into a beta item. The org is the caller\'s validated one, so this can never target another org, and the registry refuses anything not in beta — so it can neither re-open an item an operator turned off nor touch one that is already generally available. Requires a signed-in caller with an org.
     * Opts the caller\'s OWN org into a beta item.
     */
    async postEnablementOptin(requestParameters: EnablementApiPostEnablementOptinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserEnablementItem> {
        const response = await this.postEnablementOptinRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent. The org is the caller\'s validated one, so this can never revoke another org\'s grant. Requires a signed-in caller with an org.
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent.
     */
    async postEnablementOptoutRaw(requestParameters: EnablementApiPostEnablementOptoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserEnablementItem>> {
        if (requestParameters['enablementOptRef'] == null) {
            throw new runtime.RequiredError(
                'enablementOptRef',
                'Required parameter "enablementOptRef" was null or undefined when calling postEnablementOptout().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/enablement/optout`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EnablementOptRefToJSON(requestParameters['enablementOptRef']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserEnablementItemFromJSON(jsonValue));
    }

    /**
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent. The org is the caller\'s validated one, so this can never revoke another org\'s grant. Requires a signed-in caller with an org.
     * Removes the caller\'s OWN org from a beta item\'s grant list, the reverse of OptIntoBeta and idempotent.
     */
    async postEnablementOptout(requestParameters: EnablementApiPostEnablementOptoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserEnablementItem> {
        const response = await this.postEnablementOptoutRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
