/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AccessOut,
  AccrualsOut,
  ActionOut,
  AdminBonusesEnvelope,
  AdminBook,
  AdminCatalogOut,
  AdminEnablementBoard,
  AdminEnablementItem,
  AdminReportOut,
  AffiliateOut,
  AimetricsOut,
  AnalyticsOut,
  AnchorOut,
  Approval,
  ApproveRequest,
  AuthorResult,
  AuthorSweepResult,
  BackfillIn,
  BackfillOut,
  BasesOut,
  BasisResult,
  CapIn,
  ComputeOut,
  CordonIn,
  CustomerDetailOut,
  CustomersOut,
  DirectoryOut,
  Disbursal,
  DropletIn,
  FinanceOut,
  FlagsOut,
  GrantIn,
  GrantOut,
  GrantsOut,
  IamRowsOut,
  InvoicesOut,
  ListOut,
  MeOut,
  MetricsOut,
  MoneyOut,
  MutationOut,
  NameIn,
  O11yOut,
  OrgsOut,
  Overlay,
  OverviewOut,
  PayoutOut,
  PayoutRequest,
  PayoutResult,
  PolicyOut,
  PolicyRequest,
  ProductsOut,
  PromoIn,
  ProviderPatchIn,
  ProvidersCreditOut,
  RateSet,
  RawOut,
  ReadOut,
  RecordsOut,
  ReferralsOut,
  ReloadIn,
  RevenueOut,
  ScaleIn,
  SeedOut,
  SeedRequest,
  ServiceInput,
  ServiceModeIn,
  ServiceOut,
  ServicesOut,
  SetEnablementBody,
  SetFlagIn,
  SignerOut,
  SubscriptionsOut,
  SubsystemsOut,
  SweepEnvelope,
  SweepOut,
  SweepRequest,
  SyncOut,
  UsageFundingOut,
  UsageOut,
  UsersOut,
  VerifyOut,
  VolumeIn,
  VolumeSnapshotOut,
  VolumesOut,
  WaitlistBoostRequest,
} from '../models/index.js';
import {
    AccessOutFromJSON,
    AccessOutToJSON,
    AccrualsOutFromJSON,
    AccrualsOutToJSON,
    ActionOutFromJSON,
    ActionOutToJSON,
    AdminBonusesEnvelopeFromJSON,
    AdminBonusesEnvelopeToJSON,
    AdminBookFromJSON,
    AdminBookToJSON,
    AdminCatalogOutFromJSON,
    AdminCatalogOutToJSON,
    AdminEnablementBoardFromJSON,
    AdminEnablementBoardToJSON,
    AdminEnablementItemFromJSON,
    AdminEnablementItemToJSON,
    AdminReportOutFromJSON,
    AdminReportOutToJSON,
    AffiliateOutFromJSON,
    AffiliateOutToJSON,
    AimetricsOutFromJSON,
    AimetricsOutToJSON,
    AnalyticsOutFromJSON,
    AnalyticsOutToJSON,
    AnchorOutFromJSON,
    AnchorOutToJSON,
    ApprovalFromJSON,
    ApprovalToJSON,
    ApproveRequestFromJSON,
    ApproveRequestToJSON,
    AuthorResultFromJSON,
    AuthorResultToJSON,
    AuthorSweepResultFromJSON,
    AuthorSweepResultToJSON,
    BackfillInFromJSON,
    BackfillInToJSON,
    BackfillOutFromJSON,
    BackfillOutToJSON,
    BasesOutFromJSON,
    BasesOutToJSON,
    BasisResultFromJSON,
    BasisResultToJSON,
    CapInFromJSON,
    CapInToJSON,
    ComputeOutFromJSON,
    ComputeOutToJSON,
    CordonInFromJSON,
    CordonInToJSON,
    CustomerDetailOutFromJSON,
    CustomerDetailOutToJSON,
    CustomersOutFromJSON,
    CustomersOutToJSON,
    DirectoryOutFromJSON,
    DirectoryOutToJSON,
    DisbursalFromJSON,
    DisbursalToJSON,
    DropletInFromJSON,
    DropletInToJSON,
    FinanceOutFromJSON,
    FinanceOutToJSON,
    FlagsOutFromJSON,
    FlagsOutToJSON,
    GrantInFromJSON,
    GrantInToJSON,
    GrantOutFromJSON,
    GrantOutToJSON,
    GrantsOutFromJSON,
    GrantsOutToJSON,
    IamRowsOutFromJSON,
    IamRowsOutToJSON,
    InvoicesOutFromJSON,
    InvoicesOutToJSON,
    ListOutFromJSON,
    ListOutToJSON,
    MeOutFromJSON,
    MeOutToJSON,
    MetricsOutFromJSON,
    MetricsOutToJSON,
    MoneyOutFromJSON,
    MoneyOutToJSON,
    MutationOutFromJSON,
    MutationOutToJSON,
    NameInFromJSON,
    NameInToJSON,
    O11yOutFromJSON,
    O11yOutToJSON,
    OrgsOutFromJSON,
    OrgsOutToJSON,
    OverlayFromJSON,
    OverlayToJSON,
    OverviewOutFromJSON,
    OverviewOutToJSON,
    PayoutOutFromJSON,
    PayoutOutToJSON,
    PayoutRequestFromJSON,
    PayoutRequestToJSON,
    PayoutResultFromJSON,
    PayoutResultToJSON,
    PolicyOutFromJSON,
    PolicyOutToJSON,
    PolicyRequestFromJSON,
    PolicyRequestToJSON,
    ProductsOutFromJSON,
    ProductsOutToJSON,
    PromoInFromJSON,
    PromoInToJSON,
    ProviderPatchInFromJSON,
    ProviderPatchInToJSON,
    ProvidersCreditOutFromJSON,
    ProvidersCreditOutToJSON,
    RateSetFromJSON,
    RateSetToJSON,
    RawOutFromJSON,
    RawOutToJSON,
    ReadOutFromJSON,
    ReadOutToJSON,
    RecordsOutFromJSON,
    RecordsOutToJSON,
    ReferralsOutFromJSON,
    ReferralsOutToJSON,
    ReloadInFromJSON,
    ReloadInToJSON,
    RevenueOutFromJSON,
    RevenueOutToJSON,
    ScaleInFromJSON,
    ScaleInToJSON,
    SeedOutFromJSON,
    SeedOutToJSON,
    SeedRequestFromJSON,
    SeedRequestToJSON,
    ServiceInputFromJSON,
    ServiceInputToJSON,
    ServiceModeInFromJSON,
    ServiceModeInToJSON,
    ServiceOutFromJSON,
    ServiceOutToJSON,
    ServicesOutFromJSON,
    ServicesOutToJSON,
    SetEnablementBodyFromJSON,
    SetEnablementBodyToJSON,
    SetFlagInFromJSON,
    SetFlagInToJSON,
    SignerOutFromJSON,
    SignerOutToJSON,
    SubscriptionsOutFromJSON,
    SubscriptionsOutToJSON,
    SubsystemsOutFromJSON,
    SubsystemsOutToJSON,
    SweepEnvelopeFromJSON,
    SweepEnvelopeToJSON,
    SweepOutFromJSON,
    SweepOutToJSON,
    SweepRequestFromJSON,
    SweepRequestToJSON,
    SyncOutFromJSON,
    SyncOutToJSON,
    UsageFundingOutFromJSON,
    UsageFundingOutToJSON,
    UsageOutFromJSON,
    UsageOutToJSON,
    UsersOutFromJSON,
    UsersOutToJSON,
    VerifyOutFromJSON,
    VerifyOutToJSON,
    VolumeInFromJSON,
    VolumeInToJSON,
    VolumeSnapshotOutFromJSON,
    VolumeSnapshotOutToJSON,
    VolumesOutFromJSON,
    VolumesOutToJSON,
    WaitlistBoostRequestFromJSON,
    WaitlistBoostRequestToJSON,
} from '../models/index.js';

export interface AdminApiAdminAIMetricsRequest {
    range?: string;
}

export interface AdminApiAdminAnalyticsRequest {
    range?: string;
}

export interface AdminApiAdminApplicationsRequest {
    owner?: string;
    p?: string;
    pageSize?: string;
}

export interface AdminApiAdminAuditRequest {
    org?: string;
    sub?: string;
    action?: string;
    resource?: string;
    resourceId?: string;
    result?: string;
    since?: string;
    until?: string;
    pageSize?: string;
    p?: string;
}

export interface AdminApiAdminCapsRequest {
    org?: string;
    id?: string;
}

export interface AdminApiAdminComputeRequest {
    kind?: string;
    org?: string;
    range?: string;
}

export interface AdminApiAdminCordonNodeRequest {
    id: string;
    cordonIn: CordonIn;
}

export interface AdminApiAdminCreateCapRequest {
    capIn: CapIn;
}

export interface AdminApiAdminCustomerRequest {
    org: string;
}

export interface AdminApiAdminDeleteCapRequest {
    id: string;
    org?: string;
}

export interface AdminApiAdminDeleteDropletRequest {
    id: string;
    size?: string;
    disk?: boolean;
}

export interface AdminApiAdminDeleteLoadBalancerRequest {
    id: string;
}

export interface AdminApiAdminDeleteVolumeRequest {
    id: string;
    snapshot?: string;
    name?: string;
    sizeGiB?: number;
}

export interface AdminApiAdminDisablePluginRequest {
    name: string;
    nameIn: NameIn;
}

export interface AdminApiAdminEnablePluginRequest {
    name: string;
    nameIn: NameIn;
}

export interface AdminApiAdminFinanceBackfillRequest {
    backfillIn: BackfillIn;
}

export interface AdminApiAdminGrantCreditRequest {
    org: string;
    grantIn: GrantIn;
}

export interface AdminApiAdminGrantsRequest {
    org?: string;
    result?: string;
    limit?: string;
}

export interface AdminApiAdminInfraRequest {
    refresh?: string;
}

export interface AdminApiAdminInvoicesRequest {
    status?: string;
    org?: string;
    limit?: string;
}

export interface AdminApiAdminIssueGrantRequest {
    grantIn: GrantIn;
}

export interface AdminApiAdminMetricsRequest {
    window?: string;
    limit?: string;
}

export interface AdminApiAdminO11yRequest {
    range?: string;
}

export interface AdminApiAdminPluginsRequest {
    scope?: string;
}

export interface AdminApiAdminProductsRequest {
    kind?: string;
    tier?: string;
    env?: string;
}

export interface AdminApiAdminReactivateCustomerRequest {
    org: string;
}

export interface AdminApiAdminReloadPluginRequest {
    name: string;
    reloadIn: ReloadIn;
}

export interface AdminApiAdminResizeDropletRequest {
    id: string;
    dropletIn: DropletIn;
}

export interface AdminApiAdminResizeVolumeRequest {
    id: string;
    volumeIn: VolumeIn;
}

export interface AdminApiAdminRolesRequest {
    owner?: string;
    p?: string;
    pageSize?: string;
}

export interface AdminApiAdminScaleNodePoolRequest {
    id: string;
    pool: string;
    scaleIn: ScaleIn;
}

export interface AdminApiAdminSetFlagRequest {
    key: string;
    setFlagIn: SetFlagIn;
}

export interface AdminApiAdminSetPromoRequest {
    promoIn: PromoIn;
}

export interface AdminApiAdminSetServiceModeRequest {
    service: string;
    serviceModeIn: ServiceModeIn;
}

export interface AdminApiAdminSnapshotVolumeRequest {
    id: string;
    volumeIn: VolumeIn;
}

export interface AdminApiAdminSubscriptionsRequest {
    status?: string;
    org?: string;
    limit?: string;
}

export interface AdminApiAdminSubsystemsRequest {
    range?: string;
}

export interface AdminApiAdminSuspendCustomerRequest {
    org: string;
}

export interface AdminApiAdminUpdateCapRequest {
    id: string;
    capIn: CapIn;
}

export interface AdminApiAdminUpsertServiceRequest {
    serviceInput: ServiceInput;
}

export interface AdminApiAdminUsageRequest {
    org?: string;
}

export interface AdminApiAdminUsageFundingRequest {
    from?: string;
    to?: string;
}

export interface AdminApiAdminUsersRequest {
    org?: string;
    q?: string;
    p?: string;
    pageSize?: string;
}

export interface AdminApiAdminWaitlistRequest {
    waitlist?: string;
    page?: string;
    pageSize?: string;
}

export interface AdminApiAdminWaitlistBoostRequest {
    waitlistBoostRequest: WaitlistBoostRequest;
}

export interface AdminApiGetAdminAffiliatesRequest {
    limit?: number;
}

export interface AdminApiGetAdminAuthorsRequest {
    limit?: number;
}

export interface AdminApiGetAdminAuthorsByIdBasisRequest {
    id: string;
    period?: string;
}

export interface AdminApiGetAdminReferralsBonusesRequest {
    limit?: string;
}

export interface AdminApiGetAdminTreasuryRequest {
    limit?: number;
}

export interface AdminApiPatchAdminCatalogModelsByWildcard1Request {
    wildcard1: string;
}

export interface AdminApiPatchAdminCatalogProvidersByNameRequest {
    name: string;
    providerPatchIn: ProviderPatchIn;
}

export interface AdminApiPostAdminAffiliatesByIdApproveRequest {
    id: string;
    approval: Approval;
}

export interface AdminApiPostAdminAffiliatesByIdPayoutRequest {
    id: string;
    disbursal: Disbursal;
}

export interface AdminApiPostAdminAffiliatesByIdRateRequest {
    id: string;
    rateSet: RateSet;
}

export interface AdminApiPostAdminAffiliatesByIdSuspendRequest {
    id: string;
}

export interface AdminApiPostAdminAuthorsByIdApproveRequest {
    id: string;
    approveRequest: ApproveRequest;
}

export interface AdminApiPostAdminAuthorsByIdPayoutRequest {
    id: string;
    payoutRequest: PayoutRequest;
}

export interface AdminApiPostAdminAuthorsByIdSuspendRequest {
    id: string;
}

export interface AdminApiPostAdminTreasuryPolicyRequest {
    policyRequest: PolicyRequest;
}

export interface AdminApiPostAdminTreasurySeedRequest {
    seedRequest: SeedRequest;
}

export interface AdminApiPostAdminTreasurySweepRequest {
    sweepRequest: SweepRequest;
}

export interface AdminApiPutAdminEnablementRequest {
    setEnablementBody: SetEnablementBody;
}

/**
 * 
 */
export class AdminApi extends runtime.BaseAPI {

    /**
     * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).  Every signal degrades INDEPENDENTLY — a table that is absent or errors contributes its zero value and the read still succeeds. Generation latency is a SEPARATE query from generations and cost on purpose: a duration/attribute mismatch there must not zero the two numbers that did read.
     * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).
     */
    async adminAIMetricsRaw(requestParameters: AdminApiAdminAIMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AimetricsOut>> {
        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/aimetrics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AimetricsOutFromJSON(jsonValue));
    }

    /**
     * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).  Every signal degrades INDEPENDENTLY — a table that is absent or errors contributes its zero value and the read still succeeds. Generation latency is a SEPARATE query from generations and cost on purpose: a duration/attribute mismatch there must not zero the two numbers that did read.
     * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).
     */
    async adminAIMetrics(requestParameters: AdminApiAdminAIMetricsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AimetricsOut> {
        const response = await this.adminAIMetricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.  The window is the caller\'s, not the fleet\'s: a SuperAdmin gets every org, a white-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).  sources[] carries each upstream\'s freshness so a partial read is VISIBLE rather than silently low: a ledger that answered for only some orgs marks commerce-ledger degraded instead of publishing an undercount as healthy.
     * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.
     */
    async adminAnalyticsRaw(requestParameters: AdminApiAdminAnalyticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AnalyticsOut>> {
        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/analytics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnalyticsOutFromJSON(jsonValue));
    }

    /**
     * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.  The window is the caller\'s, not the fleet\'s: a SuperAdmin gets every org, a white-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).  sources[] carries each upstream\'s freshness so a partial read is VISIBLE rather than silently low: a ledger that answered for only some orgs marks commerce-ledger degraded instead of publishing an undercount as healthy.
     * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.
     */
    async adminAnalytics(requestParameters: AdminApiAdminAnalyticsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AnalyticsOut> {
        const response = await this.adminAnalyticsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications. These are the platform\'s OIDC clients — the console reads clientId off each row.
     * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications.
     */
    async adminApplicationsRaw(requestParameters: AdminApiAdminApplicationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IamRowsOut>> {
        const queryParameters: any = {};

        if (requestParameters['owner'] != null) {
            queryParameters['owner'] = requestParameters['owner'];
        }

        if (requestParameters['p'] != null) {
            queryParameters['p'] = requestParameters['p'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/applications`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IamRowsOutFromJSON(jsonValue));
    }

    /**
     * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications. These are the platform\'s OIDC clients — the console reads clientId off each row.
     * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications.
     */
    async adminApplications(requestParameters: AdminApiAdminApplicationsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IamRowsOut> {
        const response = await this.adminApplicationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads cloud\'s tamper-evident audit trail, newest first, with the chain\'s live integrity attached so a listing can be badged as verified.  When cloud has no local store configured it falls back to forwarding IAM\'s own get-records trail verbatim — a DIFFERENT trail, federated so the endpoint never regresses to an empty list. Those rows carry no integrity of ours, so the field is null there.
     * Reads cloud\'s tamper-evident audit trail, newest first, with the chain\'s live integrity attached so a listing can be badged as verified.
     */
    async adminAuditRaw(requestParameters: AdminApiAdminAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RecordsOut>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['sub'] != null) {
            queryParameters['sub'] = requestParameters['sub'];
        }

        if (requestParameters['action'] != null) {
            queryParameters['action'] = requestParameters['action'];
        }

        if (requestParameters['resource'] != null) {
            queryParameters['resource'] = requestParameters['resource'];
        }

        if (requestParameters['resourceId'] != null) {
            queryParameters['resourceId'] = requestParameters['resourceId'];
        }

        if (requestParameters['result'] != null) {
            queryParameters['result'] = requestParameters['result'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['until'] != null) {
            queryParameters['until'] = requestParameters['until'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        if (requestParameters['p'] != null) {
            queryParameters['p'] = requestParameters['p'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/audit`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RecordsOutFromJSON(jsonValue));
    }

    /**
     * Reads cloud\'s tamper-evident audit trail, newest first, with the chain\'s live integrity attached so a listing can be badged as verified.  When cloud has no local store configured it falls back to forwarding IAM\'s own get-records trail verbatim — a DIFFERENT trail, federated so the endpoint never regresses to an empty list. Those rows carry no integrity of ours, so the field is null there.
     * Reads cloud\'s tamper-evident audit trail, newest first, with the chain\'s live integrity attached so a listing can be badged as verified.
     */
    async adminAudit(requestParameters: AdminApiAdminAuditRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RecordsOut> {
        const response = await this.adminAuditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Walks the WHOLE hash chain and reports whether it is intact: how many records were checked, the head hash to pin externally against tail-truncation, and — when the chain is broken — the seq of the first bad record and why.  brokenAt is -1 exactly when ok is true. An unconfigured store is an honest failure here rather than a fabricated pass.
     * Walks the WHOLE hash chain and reports whether it is intact: how many records were checked, the head hash to pin externally against tail-truncation, and — when the chain is broken — the seq of the first bad record and why.
     */
    async adminAuditVerifyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VerifyOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/audit/verify`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VerifyOutFromJSON(jsonValue));
    }

    /**
     * Walks the WHOLE hash chain and reports whether it is intact: how many records were checked, the head hash to pin externally against tail-truncation, and — when the chain is broken — the seq of the first bad record and why.  brokenAt is -1 exactly when ok is true. An unconfigured store is an honest failure here rather than a fabricated pass.
     * Walks the WHOLE hash chain and reports whether it is intact: how many records were checked, the head hash to pin externally against tail-truncation, and — when the chain is broken — the seq of the first bad record and why.
     */
    async adminAuditVerify(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VerifyOut> {
        const response = await this.adminAuditVerifyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.  The scope is enforced TWICE: the upstream is asked for the caller\'s org, AND every row it returns is re-checked against the resolved scope. An upstream that ignored the filter therefore degrades to empty, never to a cross-tenant leak.  The Base engine is being embedded into cloud; until it lands this proxies BASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying so — the honest not-yet state, never fabricated instances.
     * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.
     */
    async adminBasesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BasesOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/bases`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BasesOutFromJSON(jsonValue));
    }

    /**
     * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.  The scope is enforced TWICE: the upstream is asked for the caller\'s org, AND every row it returns is re-checked against the resolved scope. An upstream that ignored the filter therefore degrades to empty, never to a cross-tenant leak.  The Base engine is being embedded into cloud; until it lands this proxies BASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying so — the honest not-yet state, never fabricated instances.
     * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.
     */
    async adminBases(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BasesOut> {
        const response = await this.adminBasesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.  These are the SAME rows the customer edits in their own console — a platform override and a customer budget are one model, not two.
     * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.
     */
    async adminCapsRaw(requestParameters: AdminApiAdminCapsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['id'] != null) {
            queryParameters['id'] = requestParameters['id'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/caps`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.  These are the SAME rows the customer edits in their own console — a platform override and a customer budget are one model, not two.
     * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.
     */
    async adminCaps(requestParameters: AdminApiAdminCapsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminCapsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event. The console folds these into its org → app → project tree.  A machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one (stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same fold the console applies, done in the warehouse so the count is over every machine and not just the page.  Honest-empty when the warehouse is not connected or hanzo.compute_usage is not provisioned yet: an empty list, never a fabricated fleet.
     * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event.
     */
    async adminComputeRaw(requestParameters: AdminApiAdminComputeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ComputeOut>> {
        const queryParameters: any = {};

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/compute`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ComputeOutFromJSON(jsonValue));
    }

    /**
     * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event. The console folds these into its org → app → project tree.  A machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one (stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same fold the console applies, done in the warehouse so the count is over every machine and not just the page.  Honest-empty when the warehouse is not connected or hanzo.compute_usage is not provisioned yet: an empty list, never a fabricated fleet.
     * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event.
     */
    async adminCompute(requestParameters: AdminApiAdminComputeRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ComputeOut> {
        const response = await this.adminComputeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.  It is the ONE infra change that does not go through the run discipline, because there is no destructive verdict to check: cordoning is reversible and evicting respects the cluster\'s own PodDisruptionBudgets. It reads the cached board for the same reason. The outcome is audited either way, and the result reports how many pods were evicted.
     * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.
     */
    async adminCordonNodeRaw(requestParameters: AdminApiAdminCordonNodeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminCordonNode().'
            );
        }

        if (requestParameters['cordonIn'] == null) {
            throw new runtime.RequiredError(
                'cordonIn',
                'Required parameter "cordonIn" was null or undefined when calling adminCordonNode().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/nodes/{id}/cordon`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CordonInToJSON(requestParameters['cordonIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.  It is the ONE infra change that does not go through the run discipline, because there is no destructive verdict to check: cordoning is reversible and evicting respects the cluster\'s own PodDisruptionBudgets. It reads the cached board for the same reason. The outcome is audited either way, and the result reports how many pods were evicted.
     * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.
     */
    async adminCordonNode(requestParameters: AdminApiAdminCordonNodeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminCordonNodeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows. The body is commerce\'s spend-alert contract, forwarded byte-for-byte.
     * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows.
     */
    async adminCreateCapRaw(requestParameters: AdminApiAdminCreateCapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        if (requestParameters['capIn'] == null) {
            throw new runtime.RequiredError(
                'capIn',
                'Required parameter "capIn" was null or undefined when calling adminCreateCap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/caps`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CapInToJSON(requestParameters['capIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows. The body is commerce\'s spend-alert contract, forwarded byte-for-byte.
     * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows.
     */
    async adminCreateCap(requestParameters: AdminApiAdminCreateCapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminCreateCapRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers GET /v1/admin/customers/:org.
     * Answers GET /v1/admin/customers/:org.
     */
    async adminCustomerRaw(requestParameters: AdminApiAdminCustomerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerDetailOut>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling adminCustomer().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/customers/{org}`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CustomerDetailOutFromJSON(jsonValue));
    }

    /**
     * Answers GET /v1/admin/customers/:org.
     * Answers GET /v1/admin/customers/:org.
     */
    async adminCustomer(requestParameters: AdminApiAdminCustomerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerDetailOut> {
        const response = await this.adminCustomerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.  Each row costs one IAM read plus the org\'s money reads, fanned out under a fixed concurrency ceiling so a large fleet cannot stampede the upstreams. Every read is best-effort per row: an upstream miss degrades THAT field to its honest zero rather than failing the fleet.
     * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.
     */
    async adminCustomersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomersOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/customers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CustomersOutFromJSON(jsonValue));
    }

    /**
     * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.  Each row costs one IAM read plus the org\'s money reads, fanned out under a fixed concurrency ceiling so a large fleet cannot stampede the upstreams. Every read is best-effort per row: an upstream miss degrades THAT field to its honest zero rather than failing the fleet.
     * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.
     */
    async adminCustomers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomersOut> {
        const response = await this.adminCustomersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Removes one cap by id, lifting the ceiling entirely.
     * Removes one cap by id, lifting the ceiling entirely.
     */
    async adminDeleteCapRaw(requestParameters: AdminApiAdminDeleteCapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminDeleteCap().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/caps/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Removes one cap by id, lifting the ceiling entirely.
     * Removes one cap by id, lifting the ceiling entirely.
     */
    async adminDeleteCap(requestParameters: AdminApiAdminDeleteCapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminDeleteCapRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Destroys a droplet the board has just proven is NOT a DOKS node. There is no snapshot-first undo for a droplet the way there is for a volume: the local disk goes with it.
     * Destroys a droplet the board has just proven is NOT a DOKS node.
     */
    async adminDeleteDropletRaw(requestParameters: AdminApiAdminDeleteDropletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminDeleteDroplet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        if (requestParameters['disk'] != null) {
            queryParameters['disk'] = requestParameters['disk'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/droplets/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Destroys a droplet the board has just proven is NOT a DOKS node. There is no snapshot-first undo for a droplet the way there is for a volume: the local disk goes with it.
     * Destroys a droplet the board has just proven is NOT a DOKS node.
     */
    async adminDeleteDroplet(requestParameters: AdminApiAdminDeleteDropletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminDeleteDropletRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
     * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
     */
    async adminDeleteLoadBalancerRaw(requestParameters: AdminApiAdminDeleteLoadBalancerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminDeleteLoadBalancer().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/loadbalancers/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
     * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
     */
    async adminDeleteLoadBalancer(requestParameters: AdminApiAdminDeleteLoadBalancerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminDeleteLoadBalancerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Destroys a volume the board has just proven no PersistentVolume in any cluster references. Irreversible, so it snapshots first unless explicitly waived — the snapshot IS the undo.
     * Destroys a volume the board has just proven no PersistentVolume in any cluster references.
     */
    async adminDeleteVolumeRaw(requestParameters: AdminApiAdminDeleteVolumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminDeleteVolume().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['snapshot'] != null) {
            queryParameters['snapshot'] = requestParameters['snapshot'];
        }

        if (requestParameters['name'] != null) {
            queryParameters['name'] = requestParameters['name'];
        }

        if (requestParameters['sizeGiB'] != null) {
            queryParameters['sizeGiB'] = requestParameters['sizeGiB'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/volumes/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Destroys a volume the board has just proven no PersistentVolume in any cluster references. Irreversible, so it snapshots first unless explicitly waived — the snapshot IS the undo.
     * Destroys a volume the board has just proven no PersistentVolume in any cluster references.
     */
    async adminDeleteVolume(requestParameters: AdminApiAdminDeleteVolumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminDeleteVolumeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stops the plugin. Its routes STAY REGISTERED and answer 503 — not 404.  That is zip\'s choice and this keeps it. Removing the routes would mutate the route table, and re-adding them on enable would grow it without bound across repeated cycles, which is the invariant that makes reloads flat in memory. It is also the better answer: 404 says \"no such API\" and a client may cache it and stop retrying, while 503 says \"this API exists and is down right now\", which is true and retryable. Which of the two 503s this is — deliberate stop or crash — is what the status\'s disabled flag reports.
     * Stops the plugin.
     */
    async adminDisablePluginRaw(requestParameters: AdminApiAdminDisablePluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ActionOut>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling adminDisablePlugin().'
            );
        }

        if (requestParameters['nameIn'] == null) {
            throw new runtime.RequiredError(
                'nameIn',
                'Required parameter "nameIn" was null or undefined when calling adminDisablePlugin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/plugins/{name}/disable`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NameInToJSON(requestParameters['nameIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ActionOutFromJSON(jsonValue));
    }

    /**
     * Stops the plugin. Its routes STAY REGISTERED and answer 503 — not 404.  That is zip\'s choice and this keeps it. Removing the routes would mutate the route table, and re-adding them on enable would grow it without bound across repeated cycles, which is the invariant that makes reloads flat in memory. It is also the better answer: 404 says \"no such API\" and a client may cache it and stop retrying, while 503 says \"this API exists and is down right now\", which is true and retryable. Which of the two 503s this is — deliberate stop or crash — is what the status\'s disabled flag reports.
     * Stops the plugin.
     */
    async adminDisablePlugin(requestParameters: AdminApiAdminDisablePluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ActionOut> {
        const response = await this.adminDisablePluginRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag. Named for what an operator means by it.
     * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag.
     */
    async adminEnablePluginRaw(requestParameters: AdminApiAdminEnablePluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ActionOut>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling adminEnablePlugin().'
            );
        }

        if (requestParameters['nameIn'] == null) {
            throw new runtime.RequiredError(
                'nameIn',
                'Required parameter "nameIn" was null or undefined when calling adminEnablePlugin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/plugins/{name}/enable`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NameInToJSON(requestParameters['nameIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ActionOutFromJSON(jsonValue));
    }

    /**
     * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag. Named for what an operator means by it.
     * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag.
     */
    async adminEnablePlugin(requestParameters: AdminApiAdminEnablePluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ActionOut> {
        const response = await this.adminEnablePluginRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers GET /v1/admin/finance. It reads the multi-vendor COGS from commerce /v1/costs, the DO promo-credit/burn-down treasury view, and the fleet commerce revenue, then hands them to ComputeFinance. SuperAdmin only.
     * Answers GET /v1/admin/finance.
     */
    async adminFinanceRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinanceOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/finance`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinanceOutFromJSON(jsonValue));
    }

    /**
     * Answers GET /v1/admin/finance. It reads the multi-vendor COGS from commerce /v1/costs, the DO promo-credit/burn-down treasury view, and the fleet commerce revenue, then hands them to ComputeFinance. SuperAdmin only.
     * Answers GET /v1/admin/finance.
     */
    async adminFinance(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinanceOut> {
        const response = await this.adminFinanceRaw(initOverrides);
        return await response.value();
    }

    /**
     * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.  It is IDEMPOTENT: the deposit uses the fixed ref \"backfill:<org>\", so re-running it credits the wallet at most once. Safe to retry.  The pre-migration balance is read from the CO-RESIDENT commerce ledger, not over HTTP: the admin HTTP client dials an unroutable in-process address and would read $0, and a phantom zero would silently carry nothing while reporting success. When commerce is not co-resident this fails rather than migrating nothing.
     * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.
     */
    async adminFinanceBackfillRaw(requestParameters: AdminApiAdminFinanceBackfillRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BackfillOut>> {
        if (requestParameters['backfillIn'] == null) {
            throw new runtime.RequiredError(
                'backfillIn',
                'Required parameter "backfillIn" was null or undefined when calling adminFinanceBackfill().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/finance/backfill`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BackfillInToJSON(requestParameters['backfillIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BackfillOutFromJSON(jsonValue));
    }

    /**
     * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.  It is IDEMPOTENT: the deposit uses the fixed ref \"backfill:<org>\", so re-running it credits the wallet at most once. Safe to retry.  The pre-migration balance is read from the CO-RESIDENT commerce ledger, not over HTTP: the admin HTTP client dials an unroutable in-process address and would read $0, and a phantom zero would silently carry nothing while reporting success. When commerce is not co-resident this fails rather than migrating nothing.
     * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.
     */
    async adminFinanceBackfill(requestParameters: AdminApiAdminFinanceBackfillRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BackfillOut> {
        const response = await this.adminFinanceBackfillRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
     * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
     */
    async adminFlagsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlagsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/flags`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlagsOutFromJSON(jsonValue));
    }

    /**
     * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
     * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
     */
    async adminFlags(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlagsOut> {
        const response = await this.adminFlagsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.  The credit lands on the account account.Payer resolves, NOT necessarily the org: name a member of a pooled org and the pool is credited. The receipt echoes the subject so the caller can see which.
     * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.
     */
    async adminGrantCreditRaw(requestParameters: AdminApiAdminGrantCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantOut>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling adminGrantCredit().'
            );
        }

        if (requestParameters['grantIn'] == null) {
            throw new runtime.RequiredError(
                'grantIn',
                'Required parameter "grantIn" was null or undefined when calling adminGrantCredit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/customers/{org}/credit`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GrantInToJSON(requestParameters['grantIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantOutFromJSON(jsonValue));
    }

    /**
     * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.  The credit lands on the account account.Payer resolves, NOT necessarily the org: name a member of a pooled org and the pool is credited. The receipt echoes the subject so the caller can see which.
     * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.
     */
    async adminGrantCredit(requestParameters: AdminApiAdminGrantCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantOut> {
        const response = await this.adminGrantCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.  It is a PROJECTION of the tamper-evident audit trail, not a second store: every grant is written there as action \"admin.customer.credit\", so this view cannot drift from what actually happened, and FAILED grants appear too.  A deployment with no local audit store has no history to project, and says so with an empty list and a msg rather than an error.
     * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.
     */
    async adminGrantsRaw(requestParameters: AdminApiAdminGrantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantsOut>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['result'] != null) {
            queryParameters['result'] = requestParameters['result'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/grants`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantsOutFromJSON(jsonValue));
    }

    /**
     * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.  It is a PROJECTION of the tamper-evident audit trail, not a second store: every grant is written there as action \"admin.customer.credit\", so this view cannot drift from what actually happened, and FAILED grants appear too.  A deployment with no local audit store has no history to project, and says so with an empty list and a msg rather than an error.
     * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.
     */
    async adminGrants(requestParameters: AdminApiAdminGrantsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantsOut> {
        const response = await this.adminGrantsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.  It is cached for up to a minute because one read is a fan-out over the DO API plus a full pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION re-scans from scratch and ignores this cache.  Only an unusable DO account is a hard failure. A partial read still produces a board, with the failing source named in sources[] — except for clusters and volumes, which the safety verdict depends on; without those the analysis degrades rather than classifying anything it cannot prove.
     * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.
     */
    async adminInfraRaw(requestParameters: AdminApiAdminInfraRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReadOut>> {
        const queryParameters: any = {};

        if (requestParameters['refresh'] != null) {
            queryParameters['refresh'] = requestParameters['refresh'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReadOutFromJSON(jsonValue));
    }

    /**
     * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.  It is cached for up to a minute because one read is a fan-out over the DO API plus a full pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION re-scans from scratch and ignores this cache.  Only an unusable DO account is a hard failure. A partial read still produces a board, with the failing source named in sources[] — except for clusters and volumes, which the safety verdict depends on; without those the analysis degrades rather than classifying anything it cannot prove.
     * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.
     */
    async adminInfra(requestParameters: AdminApiAdminInfraRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReadOut> {
        const response = await this.adminInfraRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers GET /v1/admin/invoices.   GET /v1/admin/invoices?org=&status=&limit=
     * Answers GET /v1/admin/invoices.
     */
    async adminInvoicesRaw(requestParameters: AdminApiAdminInvoicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InvoicesOut>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/invoices`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InvoicesOutFromJSON(jsonValue));
    }

    /**
     * Answers GET /v1/admin/invoices.   GET /v1/admin/invoices?org=&status=&limit=
     * Answers GET /v1/admin/invoices.
     */
    async adminInvoices(requestParameters: AdminApiAdminInvoicesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InvoicesOut> {
        const response = await this.adminInvoicesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Issues a credit grant to any org from the operator Grants view, with the target named in the body. It funnels through the SAME core.ApplyGrant that POST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path and one audit trail behind both.
     * Issues a credit grant to any org from the operator Grants view, with the target named in the body.
     */
    async adminIssueGrantRaw(requestParameters: AdminApiAdminIssueGrantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantOut>> {
        if (requestParameters['grantIn'] == null) {
            throw new runtime.RequiredError(
                'grantIn',
                'Required parameter "grantIn" was null or undefined when calling adminIssueGrant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/grants`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GrantInToJSON(requestParameters['grantIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantOutFromJSON(jsonValue));
    }

    /**
     * Issues a credit grant to any org from the operator Grants view, with the target named in the body. It funnels through the SAME core.ApplyGrant that POST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path and one audit trail behind both.
     * Issues a credit grant to any org from the operator Grants view, with the target named in the body.
     */
    async adminIssueGrant(requestParameters: AdminApiAdminIssueGrantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantOut> {
        const response = await this.adminIssueGrantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is. The fields come from the sanitized identity headers the gate just read, so they are authoritative and never client-forgeable; nothing is looked up.
     * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is.
     */
    async adminMeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MeOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/me`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MeOutFromJSON(jsonValue));
    }

    /**
     * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is. The fields come from the sanitized identity headers the gate just read, so they are authoritative and never client-forgeable; nothing is looked up.
     * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is.
     */
    async adminMe(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MeOut> {
        const response = await this.adminMeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out). SuperAdmin only.   GET /v1/admin/metrics?window=30d&limit=20
     * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out).
     */
    async adminMetricsRaw(requestParameters: AdminApiAdminMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MetricsOut>> {
        const queryParameters: any = {};

        if (requestParameters['window'] != null) {
            queryParameters['window'] = requestParameters['window'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/metrics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MetricsOutFromJSON(jsonValue));
    }

    /**
     * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out). SuperAdmin only.   GET /v1/admin/metrics?window=30d&limit=20
     * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out).
     */
    async adminMetrics(requestParameters: AdminApiAdminMetricsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MetricsOut> {
        const response = await this.adminMetricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * moneyBoardHandler answers GET /v1/admin/money.
     * moneyBoardHandler answers GET /v1/admin/money.
     */
    async adminMoneyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MoneyOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/money`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MoneyOutFromJSON(jsonValue));
    }

    /**
     * moneyBoardHandler answers GET /v1/admin/money.
     * moneyBoardHandler answers GET /v1/admin/money.
     */
    async adminMoney(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MoneyOut> {
        const response = await this.adminMoneyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.  Every signal degrades INDEPENDENTLY. A table that is absent or errors contributes its zero value and the read still succeeds, so the board renders exactly what the warehouse holds rather than failing whole because one of four sources is missing. Same when the warehouse is not connected at all: the zero board, never a fabricated fleet.
     * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.
     */
    async adminO11yRaw(requestParameters: AdminApiAdminO11yRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yOut>> {
        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/o11y`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yOutFromJSON(jsonValue));
    }

    /**
     * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.  Every signal degrades INDEPENDENTLY. A table that is absent or errors contributes its zero value and the read still succeeds, so the board renders exactly what the warehouse holds rather than failing whole because one of four sources is missing. Same when the warehouse is not connected at all: the zero board, never a fabricated fleet.
     * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.
     */
    async adminO11y(requestParameters: AdminApiAdminO11yRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yOut> {
        const response = await this.adminO11yRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.  The rows are the caller\'s tenant window, not the fleet: a SuperAdmin gets every org, a white-label admin only their own subtree. A per-org read that fails degrades THAT row to an honest zero — this panel carries no sources[] channel to report freshness on, so the alternative would be a fleet total that silently reads healthy.
     * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.
     */
    async adminOrgsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrgsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/orgs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrgsOutFromJSON(jsonValue));
    }

    /**
     * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.  The rows are the caller\'s tenant window, not the fleet: a SuperAdmin gets every org, a white-label admin only their own subtree. A per-org read that fails degrades THAT row to an honest zero — this panel carries no sources[] channel to report freshness on, so the alternative would be a fleet total that silently reads healthy.
     * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.
     */
    async adminOrgs(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrgsOut> {
        const response = await this.adminOrgsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.  It ALWAYS answers 200 — a tile board that fails as a whole because one upstream is down is useless. Instead every upstream reports itself in sources[]: ok, degraded, or not-configured. A commerce read that failed for ANY org marks that source degraded, because the spend/credits totals are then an undercount and must not read healthy.  The AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the plane that owns \"what was served\". They used to come from the money plane with the token counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in which the fleet served fifteen thousand requests. Credits still come from commerce, which owns the wallet.
     * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.
     */
    async adminOverviewRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OverviewOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/overview`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OverviewOutFromJSON(jsonValue));
    }

    /**
     * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.  It ALWAYS answers 200 — a tile board that fails as a whole because one upstream is down is useless. Instead every upstream reports itself in sources[]: ok, degraded, or not-configured. A commerce read that failed for ANY org marks that source degraded, because the spend/credits totals are then an undercount and must not read healthy.  The AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the plane that owns \"what was served\". They used to come from the money plane with the token counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in which the fleet served fifteen thousand requests. Credits still come from commerce, which owns the wallet.
     * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.
     */
    async adminOverview(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OverviewOut> {
        const response = await this.adminOverviewRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.  Reading this from deployment config would answer what was INTENDED. Only the process knows what is TRUE, and during a rolling upgrade the two disagree on purpose.
     * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.
     */
    async adminPluginsRaw(requestParameters: AdminApiAdminPluginsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListOut>> {
        const queryParameters: any = {};

        if (requestParameters['scope'] != null) {
            queryParameters['scope'] = requestParameters['scope'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/plugins`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListOutFromJSON(jsonValue));
    }

    /**
     * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.  Reading this from deployment config would answer what was INTENDED. Only the process knows what is TRUE, and during a rolling upgrade the two disagree on purpose.
     * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.
     */
    async adminPlugins(requestParameters: AdminApiAdminPluginsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListOut> {
        const response = await this.adminPluginsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict. Optionally narrowed by kind, tier or env, each an exact match.  The rows are the SAME observation /v1/platform/fleet renders — read through the in-process platform seam, not a second k8s client — so the two boards can never disagree about what the fleet is. A PaaS plane that is not co-resident yields an honestly empty registry, never a fabricated row.
     * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict.
     */
    async adminProductsRaw(requestParameters: AdminApiAdminProductsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProductsOut>> {
        const queryParameters: any = {};

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        if (requestParameters['tier'] != null) {
            queryParameters['tier'] = requestParameters['tier'];
        }

        if (requestParameters['env'] != null) {
            queryParameters['env'] = requestParameters['env'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/products`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProductsOutFromJSON(jsonValue));
    }

    /**
     * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict. Optionally narrowed by kind, tier or env, each an exact match.  The rows are the SAME observation /v1/platform/fleet renders — read through the in-process platform seam, not a second k8s client — so the two boards can never disagree about what the fleet is. A PaaS plane that is not co-resident yields an honestly empty registry, never a fabricated row.
     * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict.
     */
    async adminProducts(requestParameters: AdminApiAdminProductsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProductsOut> {
        const response = await this.adminProductsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads the current platform plan promo — the singleton discount offer, e.g. the 50%-off launch promo. Commerce stores it in the reserved platform namespace, so the org sent with the read is the admin org and the service token is what passes commerce\'s own platform-admin gate.
     * Reads the current platform plan promo — the singleton discount offer, e.g.
     */
    async adminPromoRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/promos`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Reads the current platform plan promo — the singleton discount offer, e.g. the 50%-off launch promo. Commerce stores it in the reserved platform namespace, so the org sent with the read is the admin org and the service token is what passes commerce\'s own platform-admin gate.
     * Reads the current platform plan promo — the singleton discount offer, e.g.
     */
    async adminPromo(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminPromoRaw(initOverrides);
        return await response.value();
    }

    /**
     * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger. SuperAdmin-guarded (see Routes).
     * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger.
     */
    async adminProvidersCreditRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProvidersCreditOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/providers/credit`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProvidersCreditOutFromJSON(jsonValue));
    }

    /**
     * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger. SuperAdmin-guarded (see Routes).
     * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger.
     */
    async adminProvidersCredit(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProvidersCreditOut> {
        const response = await this.adminProvidersCreditRaw(initOverrides);
        return await response.value();
    }

    /**
     * Restores access for every member of the org, undoing a suspend. It reports the same per-user breakdown.
     * Restores access for every member of the org, undoing a suspend.
     */
    async adminReactivateCustomerRaw(requestParameters: AdminApiAdminReactivateCustomerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccessOut>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling adminReactivateCustomer().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/customers/{org}/reactivate`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccessOutFromJSON(jsonValue));
    }

    /**
     * Restores access for every member of the org, undoing a suspend. It reports the same per-user breakdown.
     * Restores access for every member of the org, undoing a suspend.
     */
    async adminReactivateCustomer(requestParameters: AdminApiAdminReactivateCustomerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccessOut> {
        const response = await this.adminReactivateCustomerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Swaps a plugin for another build without dropping a request. The replacement is started and proven to be LISTENING before any traffic moves to it, so a bad build leaves the old one serving and returns an error rather than a hole; the old process then drains before it is killed.  With a version or url+sum it pins; naming a digest this host has run before is the rollback, and costs no network because the digest IS the cache key. With neither it restarts what is already loaded.  Fleet scope applies it to one host at a time and STOPS at the first failure, so a build that cannot come up reaches exactly one host.
     * Swaps a plugin for another build without dropping a request.
     */
    async adminReloadPluginRaw(requestParameters: AdminApiAdminReloadPluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ActionOut>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling adminReloadPlugin().'
            );
        }

        if (requestParameters['reloadIn'] == null) {
            throw new runtime.RequiredError(
                'reloadIn',
                'Required parameter "reloadIn" was null or undefined when calling adminReloadPlugin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/plugins/{name}/reload`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ReloadInToJSON(requestParameters['reloadIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ActionOutFromJSON(jsonValue));
    }

    /**
     * Swaps a plugin for another build without dropping a request. The replacement is started and proven to be LISTENING before any traffic moves to it, so a bad build leaves the old one serving and returns an error rather than a hole; the old process then drains before it is killed.  With a version or url+sum it pins; naming a digest this host has run before is the rollback, and costs no network because the digest IS the cache key. With neither it restarts what is already loaded.  Fleet scope applies it to one host at a time and STOPS at the first failure, so a build that cannot come up reaches exactly one host.
     * Swaps a plugin for another build without dropping a request.
     */
    async adminReloadPlugin(requestParameters: AdminApiAdminReloadPluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ActionOut> {
        const response = await this.adminReloadPluginRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Changes a droplet\'s plan. Same refusal as delete and for the same reason: a DOKS node\'s size is the node pool\'s to declare.  disk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet DOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO requires the droplet to be powered off and applies the change asynchronously, so the response carries the action to poll, not a completed change.
     * Changes a droplet\'s plan.
     */
    async adminResizeDropletRaw(requestParameters: AdminApiAdminResizeDropletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminResizeDroplet().'
            );
        }

        if (requestParameters['dropletIn'] == null) {
            throw new runtime.RequiredError(
                'dropletIn',
                'Required parameter "dropletIn" was null or undefined when calling adminResizeDroplet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/droplets/{id}/resize`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DropletInToJSON(requestParameters['dropletIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Changes a droplet\'s plan. Same refusal as delete and for the same reason: a DOKS node\'s size is the node pool\'s to declare.  disk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet DOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO requires the droplet to be powered off and applies the change asynchronously, so the response carries the action to poll, not a completed change.
     * Changes a droplet\'s plan.
     */
    async adminResizeDroplet(requestParameters: AdminApiAdminResizeDropletRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminResizeDropletRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other direction is a data migration this board deliberately refuses to run.  The MECHANISM follows the volume\'s owner, because there is exactly one way to grow each kind completely. A volume a PVC claims is grown by patching the claim: the CSI driver then resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV, device and filesystem all agreeing. Calling DigitalOcean directly for that volume would grow the device while the PV kept declaring the old capacity and the filesystem never grew at all. One operation, one correct mechanism per owner — not two ways to do it.
     * Grows a volume.
     */
    async adminResizeVolumeRaw(requestParameters: AdminApiAdminResizeVolumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminResizeVolume().'
            );
        }

        if (requestParameters['volumeIn'] == null) {
            throw new runtime.RequiredError(
                'volumeIn',
                'Required parameter "volumeIn" was null or undefined when calling adminResizeVolume().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/volumes/{id}/resize`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VolumeInToJSON(requestParameters['volumeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other direction is a data migration this board deliberately refuses to run.  The MECHANISM follows the volume\'s owner, because there is exactly one way to grow each kind completely. A volume a PVC claims is grown by patching the claim: the CSI driver then resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV, device and filesystem all agreeing. Calling DigitalOcean directly for that volume would grow the device while the PV kept declaring the old capacity and the filesystem never grew at all. One operation, one correct mechanism per owner — not two ways to do it.
     * Grows a volume.
     */
    async adminResizeVolume(requestParameters: AdminApiAdminResizeVolumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminResizeVolumeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.  ORTHOGONAL to /v1/admin/finance, which is the COGS/margin view of what WE pay vendors. This is the customer side: what each customer holds, spends and subscribes to.  arpu divides realized spend by PAYING customers, not by all of them — a fleet of free signups must not deflate the number. A customer counts as paying when it has spend or MRR.  An org whose money did not read degrades to honest zeros and marks the commerce source degraded in sources[], so a partial fleet read is visible instead of quietly low.
     * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.
     */
    async adminRevenueRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevenueOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/revenue`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevenueOutFromJSON(jsonValue));
    }

    /**
     * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.  ORTHOGONAL to /v1/admin/finance, which is the COGS/margin view of what WE pay vendors. This is the customer side: what each customer holds, spends and subscribes to.  arpu divides realized spend by PAYING customers, not by all of them — a fleet of free signups must not deflate the number. A customer counts as paying when it has spend or MRR.  An org whose money did not read degrades to honest zeros and marks the commerce source degraded in sources[], so a partial fleet read is visible instead of quietly low.
     * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.
     */
    async adminRevenue(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevenueOut> {
        const response = await this.adminRevenueRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
     * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
     */
    async adminRolesRaw(requestParameters: AdminApiAdminRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IamRowsOut>> {
        const queryParameters: any = {};

        if (requestParameters['owner'] != null) {
            queryParameters['owner'] = requestParameters['owner'];
        }

        if (requestParameters['p'] != null) {
            queryParameters['p'] = requestParameters['p'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/roles`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IamRowsOutFromJSON(jsonValue));
    }

    /**
     * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
     * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
     */
    async adminRoles(requestParameters: AdminApiAdminRolesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IamRowsOut> {
        const response = await this.adminRolesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.  The response states what the board could NOT prove: DOKS picks which nodes a shrink removes, so no particular pod is shown to survive one. See NodePool.ScaleTo.
     * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.
     */
    async adminScaleNodePoolRaw(requestParameters: AdminApiAdminScaleNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MutationOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminScaleNodePool().'
            );
        }

        if (requestParameters['pool'] == null) {
            throw new runtime.RequiredError(
                'pool',
                'Required parameter "pool" was null or undefined when calling adminScaleNodePool().'
            );
        }

        if (requestParameters['scaleIn'] == null) {
            throw new runtime.RequiredError(
                'scaleIn',
                'Required parameter "scaleIn" was null or undefined when calling adminScaleNodePool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/clusters/{id}/nodepools/{pool}/scale`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));
        urlPath = urlPath.replace(`{${"pool"}}`, encodeURIComponent(String(requestParameters['pool'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScaleInToJSON(requestParameters['scaleIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MutationOutFromJSON(jsonValue));
    }

    /**
     * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.  The response states what the board could NOT prove: DOKS picks which nodes a shrink removes, so no particular pod is shown to survive one. See NodePool.ScaleTo.
     * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.
     */
    async adminScaleNodePool(requestParameters: AdminApiAdminScaleNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MutationOut> {
        const response = await this.adminScaleNodePoolRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one service at a time\" view.
     * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine.
     */
    async adminServicesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServicesOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/services`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServicesOutFromJSON(jsonValue));
    }

    /**
     * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one service at a time\" view.
     * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine.
     */
    async adminServices(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServicesOut> {
        const response = await this.adminServicesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands. The flip is hot: this pod applies it immediately and peers converge within one evaluation TTL (15s by default), with no redeploy.  The body reaches the flag engine BYTE-FOR-BYTE — it is the engine\'s definition format, not this layer\'s, so a field the engine understands and admin does not must still arrive intact. setFlagIn names the two fields that matter for documentation; it is not a filter.  The write is recorded in the store\'s activity log against the caller\'s email.
     * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands.
     */
    async adminSetFlagRaw(requestParameters: AdminApiAdminSetFlagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlagsOut>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling adminSetFlag().'
            );
        }

        if (requestParameters['setFlagIn'] == null) {
            throw new runtime.RequiredError(
                'setFlagIn',
                'Required parameter "setFlagIn" was null or undefined when calling adminSetFlag().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/flags/{key}`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SetFlagInToJSON(requestParameters['setFlagIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlagsOutFromJSON(jsonValue));
    }

    /**
     * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands. The flip is hot: this pod applies it immediately and peers converge within one evaluation TTL (15s by default), with no redeploy.  The body reaches the flag engine BYTE-FOR-BYTE — it is the engine\'s definition format, not this layer\'s, so a field the engine understands and admin does not must still arrive intact. setFlagIn names the two fields that matter for documentation; it is not a filter.  The write is recorded in the store\'s activity log against the caller\'s email.
     * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands.
     */
    async adminSetFlag(requestParameters: AdminApiAdminSetFlagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlagsOut> {
        const response = await this.adminSetFlagRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Upserts the platform plan promo — the ONE place the offer is configured.  The body is commerce\'s own promo contract and is forwarded BYTE-FOR-BYTE, so no field commerce accepts is dropped in transit. promoIn names its documented fields.
     * Upserts the platform plan promo — the ONE place the offer is configured.
     */
    async adminSetPromoRaw(requestParameters: AdminApiAdminSetPromoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        if (requestParameters['promoIn'] == null) {
            throw new runtime.RequiredError(
                'promoIn',
                'Required parameter "promoIn" was null or undefined when calling adminSetPromo().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/promos`;

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: PromoInToJSON(requestParameters['promoIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Upserts the platform plan promo — the ONE place the offer is configured.  The body is commerce\'s own promo contract and is forwarded BYTE-FOR-BYTE, so no field commerce accepts is dropped in transit. promoIn names its documented fields.
     * Upserts the platform plan promo — the ONE place the offer is configured.
     */
    async adminSetPromo(requestParameters: AdminApiAdminSetPromoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminSetPromoRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Flips ONE service\'s waitlist switch — the launch lever. Hot: it takes effect on this pod immediately and on peers within one evaluation TTL, with no redeploy. An unknown service is a 404, not a silent create; onboarding goes through upsertService.
     * Flips ONE service\'s waitlist switch — the launch lever.
     */
    async adminSetServiceModeRaw(requestParameters: AdminApiAdminSetServiceModeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServiceOut>> {
        if (requestParameters['service'] == null) {
            throw new runtime.RequiredError(
                'service',
                'Required parameter "service" was null or undefined when calling adminSetServiceMode().'
            );
        }

        if (requestParameters['serviceModeIn'] == null) {
            throw new runtime.RequiredError(
                'serviceModeIn',
                'Required parameter "serviceModeIn" was null or undefined when calling adminSetServiceMode().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/services/{service}/mode`;
        urlPath = urlPath.replace(`{${"service"}}`, encodeURIComponent(String(requestParameters['service'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServiceModeInToJSON(requestParameters['serviceModeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServiceOutFromJSON(jsonValue));
    }

    /**
     * Flips ONE service\'s waitlist switch — the launch lever. Hot: it takes effect on this pod immediately and on peers within one evaluation TTL, with no redeploy. An unknown service is a 404, not a silent create; onboarding goes through upsertService.
     * Flips ONE service\'s waitlist switch — the launch lever.
     */
    async adminSetServiceMode(requestParameters: AdminApiAdminSetServiceModeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServiceOut> {
        const response = await this.adminSetServiceModeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.  It re-scans the board first (never the cache) so the volume it snapshots is one that exists right now, and audits the outcome either way.
     * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.
     */
    async adminSnapshotVolumeRaw(requestParameters: AdminApiAdminSnapshotVolumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VolumeSnapshotOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminSnapshotVolume().'
            );
        }

        if (requestParameters['volumeIn'] == null) {
            throw new runtime.RequiredError(
                'volumeIn',
                'Required parameter "volumeIn" was null or undefined when calling adminSnapshotVolume().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/infra/volumes/{id}/snapshot`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VolumeInToJSON(requestParameters['volumeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VolumeSnapshotOutFromJSON(jsonValue));
    }

    /**
     * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.  It re-scans the board first (never the cache) so the volume it snapshots is one that exists right now, and audits the outcome either way.
     * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.
     */
    async adminSnapshotVolume(requestParameters: AdminApiAdminSnapshotVolumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VolumeSnapshotOut> {
        const response = await this.adminSnapshotVolumeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers GET /v1/admin/subscriptions.   GET /v1/admin/subscriptions?org=&status=&limit=
     * Answers GET /v1/admin/subscriptions.
     */
    async adminSubscriptionsRaw(requestParameters: AdminApiAdminSubscriptionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubscriptionsOut>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/subscriptions`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubscriptionsOutFromJSON(jsonValue));
    }

    /**
     * Answers GET /v1/admin/subscriptions.   GET /v1/admin/subscriptions?org=&status=&limit=
     * Answers GET /v1/admin/subscriptions.
     */
    async adminSubscriptions(requestParameters: AdminApiAdminSubscriptionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubscriptionsOut> {
        const response = await this.adminSubscriptionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry window (default 30d) — the same enum, and the same helpers, as the o11y board.
     * subsystems answers GET /v1/admin/subsystems.
     */
    async adminSubsystemsRaw(requestParameters: AdminApiAdminSubsystemsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubsystemsOut>> {
        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/subsystems`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubsystemsOutFromJSON(jsonValue));
    }

    /**
     * subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry window (default 30d) — the same enum, and the same helpers, as the o11y board.
     * subsystems answers GET /v1/admin/subsystems.
     */
    async adminSubsystems(requestParameters: AdminApiAdminSubsystemsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubsystemsOut> {
        const response = await this.adminSubsystemsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token. Fully reversible with ReactivateCustomer.  The result names every user updated and every user that was NOT — a partial failure leaves the org in a mixed state and says so instead of reporting a clean success.
     * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token.
     */
    async adminSuspendCustomerRaw(requestParameters: AdminApiAdminSuspendCustomerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccessOut>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling adminSuspendCustomer().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/customers/{org}/suspend`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccessOutFromJSON(jsonValue));
    }

    /**
     * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token. Fully reversible with ReactivateCustomer.  The result names every user updated and every user that was NOT — a partial failure leaves the org in a mixed state and says so instead of reporting a clean success.
     * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token.
     */
    async adminSuspendCustomer(requestParameters: AdminApiAdminSuspendCustomerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccessOut> {
        const response = await this.adminSuspendCustomerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers the operator\'s \"Sync now\" button. There is nothing to kick: admin aggregates LIVE on every read, so the button is just a re-read. It acknowledges honestly with started:true rather than pretending a batch job was queued.
     * Answers the operator\'s \"Sync now\" button.
     */
    async adminSyncRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncOutFromJSON(jsonValue));
    }

    /**
     * Answers the operator\'s \"Sync now\" button. There is nothing to kick: admin aggregates LIVE on every read, so the button is just a re-read. It acknowledges honestly with started:true rather than pretending a batch job was queued.
     * Answers the operator\'s \"Sync now\" button.
     */
    async adminSync(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncOut> {
        const response = await this.adminSyncRaw(initOverrides);
        return await response.value();
    }

    /**
     * Edits one cap by id — raise or lower the ceiling, flip enforcement. The body is commerce\'s spend-alert patch contract, forwarded byte-for-byte.
     * Edits one cap by id — raise or lower the ceiling, flip enforcement.
     */
    async adminUpdateCapRaw(requestParameters: AdminApiAdminUpdateCapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling adminUpdateCap().'
            );
        }

        if (requestParameters['capIn'] == null) {
            throw new runtime.RequiredError(
                'capIn',
                'Required parameter "capIn" was null or undefined when calling adminUpdateCap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/caps/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: CapInToJSON(requestParameters['capIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Edits one cap by id — raise or lower the ceiling, flip enforcement. The body is commerce\'s spend-alert patch contract, forwarded byte-for-byte.
     * Edits one cap by id — raise or lower the ceiling, flip enforcement.
     */
    async adminUpdateCap(requestParameters: AdminApiAdminUpdateCapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminUpdateCapRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live switch — editing the hosts of a service that is already open must not silently close it again.
     * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy.
     */
    async adminUpsertServiceRaw(requestParameters: AdminApiAdminUpsertServiceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServiceOut>> {
        if (requestParameters['serviceInput'] == null) {
            throw new runtime.RequiredError(
                'serviceInput',
                'Required parameter "serviceInput" was null or undefined when calling adminUpsertService().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/services`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServiceInputToJSON(requestParameters['serviceInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServiceOutFromJSON(jsonValue));
    }

    /**
     * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live switch — editing the hosts of a service that is already open must not silently close it again.
     * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy.
     */
    async adminUpsertService(requestParameters: AdminApiAdminUpsertServiceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServiceOut> {
        const response = await this.adminUpsertServiceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.  It reads the AI ledger (ledger.go), which is the plane that owns this question. It used to ask the commerce billing API instead, once per org, and answer with a hardcoded empty series, zero tokens and zero requests, on the reasoning that a trend and a split were \"not derivable from the commerce billing API\". They are not — but the question was never commerce\'s. hanzo.cloud_usage carries a row per served request, so all three fall out of the same window the totals do.
     * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.
     */
    async adminUsageRaw(requestParameters: AdminApiAdminUsageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageOut>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/usage`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageOutFromJSON(jsonValue));
    }

    /**
     * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.  It reads the AI ledger (ledger.go), which is the plane that owns this question. It used to ask the commerce billing API instead, once per org, and answer with a hardcoded empty series, zero tokens and zero requests, on the reasoning that a trend and a split were \"not derivable from the commerce billing API\". They are not — but the question was never commerce\'s. hanzo.cloud_usage carries a row per served request, so all three fall out of the same window the totals do.
     * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.
     */
    async adminUsage(requestParameters: AdminApiAdminUsageRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageOut> {
        const response = await this.adminUsageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).  The class is resolved at the PROVIDER level from the credit ledger, not per call — the per-call split, and the `byo` class, arrive when the metering write stamps a funding column on cloud_usage and this can GROUP BY it directly. Until then a provider with remaining grant reports all of its usage as credit, which is right in aggregate and approximate at the boundary where a grant runs out mid-window.  An unparseable window falls back to the last 30 days rather than refusing: this is a dashboard read, and a typo in a date must not blank the board.
     * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).
     */
    async adminUsageFundingRaw(requestParameters: AdminApiAdminUsageFundingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageFundingOut>> {
        const queryParameters: any = {};

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/usage/funding`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageFundingOutFromJSON(jsonValue));
    }

    /**
     * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).  The class is resolved at the PROVIDER level from the credit ledger, not per call — the per-call split, and the `byo` class, arrive when the metering write stamps a funding column on cloud_usage and this can GROUP BY it directly. Until then a provider with remaining grant reports all of its usage as credit, which is right in aggregate and approximate at the boundary where a grant runs out mid-window.  An unparseable window falls back to the last 30 days rather than refusing: this is a dashboard read, and a typo in a date must not blank the board.
     * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).
     */
    async adminUsageFunding(requestParameters: AdminApiAdminUsageFundingRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageFundingOut> {
        const response = await this.adminUsageFundingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the user directory across the caller\'s tenant window, one page at a time. total is IAM\'s REAL total, so the console can page through it.  A SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for them the owner is hard-pinned to their own org and org is ignored, which is what keeps the directory from becoming a cross-tenant read.
     * Lists the user directory across the caller\'s tenant window, one page at a time.
     */
    async adminUsersRaw(requestParameters: AdminApiAdminUsersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsersOut>> {
        const queryParameters: any = {};

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['p'] != null) {
            queryParameters['p'] = requestParameters['p'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/users`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsersOutFromJSON(jsonValue));
    }

    /**
     * Lists the user directory across the caller\'s tenant window, one page at a time. total is IAM\'s REAL total, so the console can page through it.  A SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for them the owner is hard-pinned to their own org and org is ignored, which is what keeps the directory from becoming a cross-tenant read.
     * Lists the user directory across the caller\'s tenant window, one page at a time.
     */
    async adminUsers(requestParameters: AdminApiAdminUsersRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsersOut> {
        const response = await this.adminUsersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.  A volume\'s usedGiB and pct are null, always: DO exposes capacity and attachment but no fill, so the console renders \"—\" rather than a number nobody measured. The datastore card is the one real fill here, and it is the number to scale on.  The two sources degrade independently — a DO outage still returns the datastore fill, and a disconnected datastore still returns the DO fleet.
     * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.
     */
    async adminVolumesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VolumesOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/volumes`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VolumesOutFromJSON(jsonValue));
    }

    /**
     * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.  A volume\'s usedGiB and pct are null, always: DO exposes capacity and attachment but no fill, so the console renders \"—\" rather than a number nobody measured. The datastore card is the one real fill here, and it is the number to scale on.  The two sources degrade independently — a DO outage still returns the datastore fill, and a disconnected datastore still returns the DO fleet.
     * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.
     */
    async adminVolumes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VolumesOut> {
        const response = await this.adminVolumesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.  The engine\'s payload is forwarded VERBATIM as data; the console normalizes it. When the engine is not configured on this deployment the read still succeeds, with an empty object and a msg saying so, so the panel shows an honest not-wired state instead of an error the operator would chase.
     * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.
     */
    async adminWaitlistRaw(requestParameters: AdminApiAdminWaitlistRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        const queryParameters: any = {};

        if (requestParameters['waitlist'] != null) {
            queryParameters['waitlist'] = requestParameters['waitlist'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/waitlist`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.  The engine\'s payload is forwarded VERBATIM as data; the console normalizes it. When the engine is not configured on this deployment the read still succeeds, with an empty object and a msg saying so, so the panel shows an honest not-wired state instead of an error the operator would chase.
     * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.
     */
    async adminWaitlist(requestParameters: AdminApiAdminWaitlistRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminWaitlistRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Grants a user waitlist points, moving them up toward the access cutoff. This is the access lever: the cutoff itself does not move, the person does.  It funnels through the engine\'s verified grant seam (POST /v1/waitlist/award with source=\"grant\" — the ONE path that honours an explicit points amount) and writes a tamper-evident audit row either way, so a FAILED grant is recorded too. The reason field goes only to that row.
     * Grants a user waitlist points, moving them up toward the access cutoff.
     */
    async adminWaitlistBoostRaw(requestParameters: AdminApiAdminWaitlistBoostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RawOut>> {
        if (requestParameters['waitlistBoostRequest'] == null) {
            throw new runtime.RequiredError(
                'waitlistBoostRequest',
                'Required parameter "waitlistBoostRequest" was null or undefined when calling adminWaitlistBoost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/waitlist/boost`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WaitlistBoostRequestToJSON(requestParameters['waitlistBoostRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RawOutFromJSON(jsonValue));
    }

    /**
     * Grants a user waitlist points, moving them up toward the access cutoff. This is the access lever: the cutoff itself does not move, the person does.  It funnels through the engine\'s verified grant seam (POST /v1/waitlist/award with source=\"grant\" — the ONE path that honours an explicit points amount) and writes a tamper-evident audit row either way, so a FAILED grant is recorded too. The reason field goes only to that row.
     * Grants a user waitlist points, moving them up toward the access cutoff.
     */
    async adminWaitlistBoost(requestParameters: AdminApiAdminWaitlistBoostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RawOut> {
        const response = await this.adminWaitlistBoostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.  PLATFORM SUDO ONLY, and a non-admin is refused outright. This is the cross-tenant view and it names orgs — exactly what the partner-facing leaderboard refuses to do. There is deliberately no org-scoped variant of this read; a partner sees its own standing through its own dashboard. Bounded per request.
     * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.
     */
    async getAdminAffiliatesRaw(requestParameters: AdminApiGetAdminAffiliatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DirectoryOut>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/affiliates`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DirectoryOutFromJSON(jsonValue));
    }

    /**
     * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.  PLATFORM SUDO ONLY, and a non-admin is refused outright. This is the cross-tenant view and it names orgs — exactly what the partner-facing leaderboard refuses to do. There is deliberately no org-scoped variant of this read; a partner sees its own standing through its own dashboard. Bounded per request.
     * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.
     */
    async getAdminAffiliates(requestParameters: AdminApiGetAdminAffiliatesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DirectoryOut> {
        const response = await this.getAdminAffiliatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.  It is a Hanzo platform operation: a caller who is not a SuperAdmin gets 403. It exposes the owning org of each author, which no tenant-facing read ever does.
     * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.
     */
    async getAdminAuthorsRaw(requestParameters: AdminApiGetAdminAuthorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminBook>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/authors`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminBookFromJSON(jsonValue));
    }

    /**
     * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.  It is a Hanzo platform operation: a caller who is not a SuperAdmin gets 403. It exposes the owning org of each author, which no tenant-facing read ever does.
     * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.
     */
    async getAdminAuthors(requestParameters: AdminApiGetAdminAuthorsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminBook> {
        const response = await this.getAdminAuthorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.  The data object carries: id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger (every row with its spend, the share applied then, the platform\'s matching half, whether it satisfies the formula and the attribution edges that explain it), reconciliation (does the ledger foot to the balance) and window (what slice was actually returned) — plus period when one was requested.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.
     */
    async getAdminAuthorsByIdBasisRaw(requestParameters: AdminApiGetAdminAuthorsByIdBasisRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BasisResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAdminAuthorsByIdBasis().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['period'] != null) {
            queryParameters['period'] = requestParameters['period'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/authors/{id}/basis`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BasisResultFromJSON(jsonValue));
    }

    /**
     * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.  The data object carries: id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger (every row with its spend, the share applied then, the platform\'s matching half, whether it satisfies the formula and the attribution edges that explain it), reconciliation (does the ledger foot to the balance) and window (what slice was actually returned) — plus period when one was requested.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.
     */
    async getAdminAuthorsByIdBasis(requestParameters: AdminApiGetAdminAuthorsByIdBasisRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BasisResult> {
        const response = await this.getAdminAuthorsByIdBasisRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console. Nothing is hidden: this is the admin\'s view of what exists and what is currently off, in beta or generally available. SuperAdmin only; every other caller is refused.
     * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console.
     */
    async getAdminCatalogRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminCatalogOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/catalog`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminCatalogOutFromJSON(jsonValue));
    }

    /**
     * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console. Nothing is hidden: this is the admin\'s view of what exists and what is currently off, in beta or generally available. SuperAdmin only; every other caller is refused.
     * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console.
     */
    async getAdminCatalog(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminCatalogOut> {
        const response = await this.getAdminCatalogRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta. An item nobody has touched is absent, because an untouched item is generally available; the console composes the candidate list from the live catalog. SuperAdmin only; every other caller is refused.
     * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta.
     */
    async getAdminEnablementRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminEnablementBoard>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/enablement`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminEnablementBoardFromJSON(jsonValue));
    }

    /**
     * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta. An item nobody has touched is absent, because an untouched item is generally available; the console composes the candidate list from the live catalog. SuperAdmin only; every other caller is refused.
     * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta.
     */
    async getAdminEnablement(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminEnablementBoard> {
        const response = await this.getAdminEnablementRaw(initOverrides);
        return await response.value();
    }

    /**
     * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.  Read the liability figure carefully — it is commission accrued and NOT yet paid, so it is money owed, not money spent, and the per-level split says how much of it comes from direct referrals versus the second and third levels.  PLATFORM SUDO ONLY, cross-tenant, and it names orgs. It reads the SAME single attribution spine the accrual itself walks, so the board and the ledger cannot disagree. Amounts are integer cents.
     * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.
     */
    async getAdminReferralsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReferralsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/referrals`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReferralsOutFromJSON(jsonValue));
    }

    /**
     * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.  Read the liability figure carefully — it is commission accrued and NOT yet paid, so it is money owed, not money spent, and the per-level split says how much of it comes from direct referrals versus the second and third levels.  PLATFORM SUDO ONLY, cross-tenant, and it names orgs. It reads the SAME single attribution spine the accrual itself walks, so the board and the ledger cannot disagree. Amounts are integer cents.
     * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.
     */
    async getAdminReferrals(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReferralsOut> {
        const response = await this.getAdminReferralsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns every referral edge in the directory with a fleet summary.  SuperAdmin only, fail-closed. This is the ATTRIBUTION directory — who referred whom and whether that referee became a customer. It carries no amounts because this package issues none. The cross-tenant referral ANALYTICS board (top referrers, conversion) is a different surface, GET /v1/admin/referrals, owned by the affiliates subsystem over the shared attribution spine.
     * Returns every referral edge in the directory with a fleet summary.
     */
    async getAdminReferralsBonusesRaw(requestParameters: AdminApiGetAdminReferralsBonusesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminBonusesEnvelope>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/referrals/bonuses`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminBonusesEnvelopeFromJSON(jsonValue));
    }

    /**
     * Returns every referral edge in the directory with a fleet summary.  SuperAdmin only, fail-closed. This is the ATTRIBUTION directory — who referred whom and whether that referee became a customer. It carries no amounts because this package issues none. The cross-tenant referral ANALYTICS board (top referrers, conversion) is a different surface, GET /v1/admin/referrals, owned by the affiliates subsystem over the shared attribution spine.
     * Returns every referral edge in the directory with a fleet summary.
     */
    async getAdminReferralsBonuses(requestParameters: AdminApiGetAdminReferralsBonusesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminBonusesEnvelope> {
        const response = await this.getAdminReferralsBonusesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root. ?limit= bounds the journal page.
     * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root.
     */
    async getAdminTreasuryRaw(requestParameters: AdminApiGetAdminTreasuryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminReportOut>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/treasury`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminReportOutFromJSON(jsonValue));
    }

    /**
     * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root. ?limit= bounds the journal page.
     * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root.
     */
    async getAdminTreasury(requestParameters: AdminApiGetAdminTreasuryRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminReportOut> {
        const response = await this.getAdminTreasuryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets one model\'s availability overlay — and the price overrides applied on top of the catalog — then answers the new effective overlay, so a console needs no second read. The model id is the whole remaining path, so a slashed id like `acme/some-model-1` addresses intact.  SuperAdmin only; every other caller is 403, decided before the body is read. The overlay is PLATFORM-WIDE — this is the catalog every org prices against, not a per-org setting — and `betaOrgs` is what narrows a beta to named orgs.  Only the fields the patch names change; an entry with no overlay yet starts from the catalog default, which is enabled. `state` is the coherent tri-state setter (`off`|`beta`|`ga`) and the low-level `enabled`/`beta` flags are applied AFTER it, so they win where both are sent; anything else in `state` is 400. A field sent as an explicit `null` arrives indistinguishable from an absent one, so null does not clear anything.  The rule worth reading twice: a disabled entry that still carries beta orgs IS a beta — `{\"enabled\":false,\"betaOrgs\":[\"acme\"]}` leaves acme seeing the model. Only an explicit `off` (or `beta:false`) with an empty list is the absolute kill switch that a user\'s own beta opt-in can never re-open.  `overrides` is an RFC 7386 merge patch, stored and echoed back verbatim; it must be a JSON object or null — an array or a scalar is refused — and is bounded in size and nesting depth. An uninitialised overlay store answers 503.
     * Turn one model off, into beta for named orgs, or generally available
     */
    async patchAdminCatalogModelsByWildcard1Raw(requestParameters: AdminApiPatchAdminCatalogModelsByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling patchAdminCatalogModelsByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/catalog/models/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Sets one model\'s availability overlay — and the price overrides applied on top of the catalog — then answers the new effective overlay, so a console needs no second read. The model id is the whole remaining path, so a slashed id like `acme/some-model-1` addresses intact.  SuperAdmin only; every other caller is 403, decided before the body is read. The overlay is PLATFORM-WIDE — this is the catalog every org prices against, not a per-org setting — and `betaOrgs` is what narrows a beta to named orgs.  Only the fields the patch names change; an entry with no overlay yet starts from the catalog default, which is enabled. `state` is the coherent tri-state setter (`off`|`beta`|`ga`) and the low-level `enabled`/`beta` flags are applied AFTER it, so they win where both are sent; anything else in `state` is 400. A field sent as an explicit `null` arrives indistinguishable from an absent one, so null does not clear anything.  The rule worth reading twice: a disabled entry that still carries beta orgs IS a beta — `{\"enabled\":false,\"betaOrgs\":[\"acme\"]}` leaves acme seeing the model. Only an explicit `off` (or `beta:false`) with an empty list is the absolute kill switch that a user\'s own beta opt-in can never re-open.  `overrides` is an RFC 7386 merge patch, stored and echoed back verbatim; it must be a JSON object or null — an array or a scalar is refused — and is bounded in size and nesting depth. An uninitialised overlay store answers 503.
     * Turn one model off, into beta for named orgs, or generally available
     */
    async patchAdminCatalogModelsByWildcard1(requestParameters: AdminApiPatchAdminCatalogModelsByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchAdminCatalogModelsByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * Sets one provider\'s availability overlay.  The overlay decides whether a provider is off, in beta for named orgs, or generally available, and carries the price overrides applied on top of the catalog. Only the fields the patch names change; every other field keeps the value it had, and an absent overlay starts from the catalog default (enabled). Answers the new effective overlay, so a console needs no second read.  SuperAdmin only.
     * Sets one provider\'s availability overlay.
     */
    async patchAdminCatalogProvidersByNameRaw(requestParameters: AdminApiPatchAdminCatalogProvidersByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Overlay>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling patchAdminCatalogProvidersByName().'
            );
        }

        if (requestParameters['providerPatchIn'] == null) {
            throw new runtime.RequiredError(
                'providerPatchIn',
                'Required parameter "providerPatchIn" was null or undefined when calling patchAdminCatalogProvidersByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/catalog/providers/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: ProviderPatchInToJSON(requestParameters['providerPatchIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OverlayFromJSON(jsonValue));
    }

    /**
     * Sets one provider\'s availability overlay.  The overlay decides whether a provider is off, in beta for named orgs, or generally available, and carries the price overrides applied on top of the catalog. Only the fields the patch names change; every other field keeps the value it had, and an absent overlay starts from the catalog default (enabled). Answers the new effective overlay, so a console needs no second read.  SuperAdmin only.
     * Sets one provider\'s availability overlay.
     */
    async patchAdminCatalogProvidersByName(requestParameters: AdminApiPatchAdminCatalogProvidersByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Overlay> {
        const response = await this.patchAdminCatalogProvidersByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.  The code is taken from the body if one is given, else the vanity code the applicant requested, else a slug derived for them. Codes are ONE global namespace, so a taken code is a 409 and nothing is approved. The minted code is also mirrored as a link row so click tracking is uniform across every code the affiliate holds; that mirror is best-effort and its failure never fails the approval.  Approval is what makes an affiliate eligible: before it, attribution against its code does not resolve and no sweep accrues to it. PLATFORM SUDO ONLY. Audited.
     * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.
     */
    async postAdminAffiliatesByIdApproveRaw(requestParameters: AdminApiPostAdminAffiliatesByIdApproveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AffiliateOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAffiliatesByIdApprove().'
            );
        }

        if (requestParameters['approval'] == null) {
            throw new runtime.RequiredError(
                'approval',
                'Required parameter "approval" was null or undefined when calling postAdminAffiliatesByIdApprove().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/affiliates/{id}/approve`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ApprovalToJSON(requestParameters['approval']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AffiliateOutFromJSON(jsonValue));
    }

    /**
     * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.  The code is taken from the body if one is given, else the vanity code the applicant requested, else a slug derived for them. Codes are ONE global namespace, so a taken code is a 409 and nothing is approved. The minted code is also mirrored as a link row so click tracking is uniform across every code the affiliate holds; that mirror is best-effort and its failure never fails the approval.  Approval is what makes an affiliate eligible: before it, attribution against its code does not resolve and no sweep accrues to it. PLATFORM SUDO ONLY. Audited.
     * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.
     */
    async postAdminAffiliatesByIdApprove(requestParameters: AdminApiPostAdminAffiliatesByIdApproveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AffiliateOut> {
        const response = await this.postAdminAffiliatesByIdApproveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.  The amount is reserved atomically against the affiliate\'s PENDING commission — accrued minus paid — so a payout can never exceed what is owed. The METHOD decides whether money actually moves: `credits` issues a commerce grant into the affiliate ORG\'s own wallet, tagged so the ledger can tell an affiliate payout apart from an admin or referral grant; every other method — wire, paypal and the rest — is RECORD-ONLY: the payout row and the balances move, the cash is disbursed out of band.  The amount is integer cents and must be positive. PLATFORM SUDO ONLY. Audited.
     * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.
     */
    async postAdminAffiliatesByIdPayoutRaw(requestParameters: AdminApiPostAdminAffiliatesByIdPayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PayoutOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAffiliatesByIdPayout().'
            );
        }

        if (requestParameters['disbursal'] == null) {
            throw new runtime.RequiredError(
                'disbursal',
                'Required parameter "disbursal" was null or undefined when calling postAdminAffiliatesByIdPayout().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/affiliates/{id}/payout`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DisbursalToJSON(requestParameters['disbursal']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PayoutOutFromJSON(jsonValue));
    }

    /**
     * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.  The amount is reserved atomically against the affiliate\'s PENDING commission — accrued minus paid — so a payout can never exceed what is owed. The METHOD decides whether money actually moves: `credits` issues a commerce grant into the affiliate ORG\'s own wallet, tagged so the ledger can tell an affiliate payout apart from an admin or referral grant; every other method — wire, paypal and the rest — is RECORD-ONLY: the payout row and the balances move, the cash is disbursed out of band.  The amount is integer cents and must be positive. PLATFORM SUDO ONLY. Audited.
     * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.
     */
    async postAdminAffiliatesByIdPayout(requestParameters: AdminApiPostAdminAffiliatesByIdPayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PayoutOut> {
        const response = await this.postAdminAffiliatesByIdPayoutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.  The rate is CAPPED so that the direct rate plus the platform-wide second- and third-level rates can never exceed the whole margin — the structural guarantee that everything paid on one source event stays inside the margin actually earned. The cap is resolved from the rates in force at the moment of the call and quoted in the refusal, because those switches move; a hardcoded bound would start lying the moment somebody edits the schedule.  Only the direct level is per-affiliate. The second and third levels are platform switches and are not settable here. The change applies to FUTURE accruals — commission already latched for a period is not recomputed. PLATFORM SUDO ONLY. Audited.
     * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.
     */
    async postAdminAffiliatesByIdRateRaw(requestParameters: AdminApiPostAdminAffiliatesByIdRateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AffiliateOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAffiliatesByIdRate().'
            );
        }

        if (requestParameters['rateSet'] == null) {
            throw new runtime.RequiredError(
                'rateSet',
                'Required parameter "rateSet" was null or undefined when calling postAdminAffiliatesByIdRate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/affiliates/{id}/rate`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RateSetToJSON(requestParameters['rateSet']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AffiliateOutFromJSON(jsonValue));
    }

    /**
     * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.  The rate is CAPPED so that the direct rate plus the platform-wide second- and third-level rates can never exceed the whole margin — the structural guarantee that everything paid on one source event stays inside the margin actually earned. The cap is resolved from the rates in force at the moment of the call and quoted in the refusal, because those switches move; a hardcoded bound would start lying the moment somebody edits the schedule.  Only the direct level is per-affiliate. The second and third levels are platform switches and are not settable here. The change applies to FUTURE accruals — commission already latched for a period is not recomputed. PLATFORM SUDO ONLY. Audited.
     * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.
     */
    async postAdminAffiliatesByIdRate(requestParameters: AdminApiPostAdminAffiliatesByIdRateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AffiliateOut> {
        const response = await this.postAdminAffiliatesByIdRateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.  It CLAWS NOTHING BACK. Commission already accrued stays accrued and stays payable, and existing attribution edges are left standing — suspension ends earning, it does not unwind history. PLATFORM SUDO ONLY. Audited.
     * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.
     */
    async postAdminAffiliatesByIdSuspendRaw(requestParameters: AdminApiPostAdminAffiliatesByIdSuspendRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AffiliateOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAffiliatesByIdSuspend().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/affiliates/{id}/suspend`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AffiliateOutFromJSON(jsonValue));
    }

    /**
     * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.  It CLAWS NOTHING BACK. Commission already accrued stays accrued and stays payable, and existing attribution edges are left standing — suspension ends earning, it does not unwind history. PLATFORM SUDO ONLY. Audited.
     * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.
     */
    async postAdminAffiliatesByIdSuspend(requestParameters: AdminApiPostAdminAffiliatesByIdSuspendRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AffiliateOut> {
        const response = await this.postAdminAffiliatesByIdSuspendRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.  This is the cron path, and it is LATCHED at most once per affiliate, source org and period — so re-running it inside the same period accrues nothing further. Safe to retry, and safe to run by hand beside the schedule.  Commission is a rate of Hanzo\'s MARGIN on that spend, never of the customer\'s gross bill, so every level\'s share summed over one source event stays within the margin actually earned and the customer\'s charge is untouched. Nothing accrues past the third upline level, and only an APPROVED affiliate accrues at all.  The same spend read drives the OSS author royalty — one read, both programs — so the answer reports royalties accrued alongside. PLATFORM SUDO ONLY. Bounded per run; a source whose spend cannot be read is skipped and picked up next time, never half-accrued.
     * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.
     */
    async postAdminAffiliatesSweepRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccrualsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/affiliates/sweep`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccrualsOutFromJSON(jsonValue));
    }

    /**
     * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.  This is the cron path, and it is LATCHED at most once per affiliate, source org and period — so re-running it inside the same period accrues nothing further. Safe to retry, and safe to run by hand beside the schedule.  Commission is a rate of Hanzo\'s MARGIN on that spend, never of the customer\'s gross bill, so every level\'s share summed over one source event stays within the margin actually earned and the customer\'s charge is untouched. Nothing accrues past the third upline level, and only an APPROVED affiliate accrues at all.  The same spend read drives the OSS author royalty — one read, both programs — so the answer reports royalties accrued alongside. PLATFORM SUDO ONLY. Bounded per run; a source whose spend cannot be read is skipped and picked up next time, never half-accrued.
     * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.
     */
    async postAdminAffiliatesSweep(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccrualsOut> {
        const response = await this.postAdminAffiliatesSweepRaw(initOverrides);
        return await response.value();
    }

    /**
     * Admits one author to EARNING, optionally on a negotiated royalty share. Until this runs, a connected author accrues nothing however many verified repositories they have.  A share override applies from here forward only — existing ledger rows keep the share that was applied when they were written, because a rate change must never rewrite what was already owed.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Admits one author to EARNING, optionally on a negotiated royalty share.
     */
    async postAdminAuthorsByIdApproveRaw(requestParameters: AdminApiPostAdminAuthorsByIdApproveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthorResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAuthorsByIdApprove().'
            );
        }

        if (requestParameters['approveRequest'] == null) {
            throw new runtime.RequiredError(
                'approveRequest',
                'Required parameter "approveRequest" was null or undefined when calling postAdminAuthorsByIdApprove().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/authors/{id}/approve`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ApproveRequestToJSON(requestParameters['approveRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthorResultFromJSON(jsonValue));
    }

    /**
     * Admits one author to EARNING, optionally on a negotiated royalty share. Until this runs, a connected author accrues nothing however many verified repositories they have.  A share override applies from here forward only — existing ledger rows keep the share that was applied when they were written, because a rate change must never rewrite what was already owed.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Admits one author to EARNING, optionally on a negotiated royalty share.
     */
    async postAdminAuthorsByIdApprove(requestParameters: AdminApiPostAdminAuthorsByIdApproveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthorResult> {
        const response = await this.postAdminAuthorsByIdApproveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a payout of accrued royalty and settles it.  The amount is RESERVED against the author\'s pending royalty atomically before anything is paid, so a payout can never exceed what is owed even under concurrent calls. An external author\'s payout is then BACKED against the platform reserve fund — a second, independent guard — and refused with 402 if the reserve cannot cover it, with the reservation voided. A \"credits\" method issues the actual wallet grant after both guards; a cash method is record-only. A first-party (treasury) author\'s royalty is realized into Hanzo\'s own reserve instead of an external wallet, and every payout row discloses which of the three it was.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Records a payout of accrued royalty and settles it.
     */
    async postAdminAuthorsByIdPayoutRaw(requestParameters: AdminApiPostAdminAuthorsByIdPayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PayoutResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAuthorsByIdPayout().'
            );
        }

        if (requestParameters['payoutRequest'] == null) {
            throw new runtime.RequiredError(
                'payoutRequest',
                'Required parameter "payoutRequest" was null or undefined when calling postAdminAuthorsByIdPayout().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/authors/{id}/payout`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PayoutRequestToJSON(requestParameters['payoutRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PayoutResultFromJSON(jsonValue));
    }

    /**
     * Records a payout of accrued royalty and settles it.  The amount is RESERVED against the author\'s pending royalty atomically before anything is paid, so a payout can never exceed what is owed even under concurrent calls. An external author\'s payout is then BACKED against the platform reserve fund — a second, independent guard — and refused with 402 if the reserve cannot cover it, with the reservation voided. A \"credits\" method issues the actual wallet grant after both guards; a cash method is record-only. A first-party (treasury) author\'s royalty is realized into Hanzo\'s own reserve instead of an external wallet, and every payout row discloses which of the three it was.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Records a payout of accrued royalty and settles it.
     */
    async postAdminAuthorsByIdPayout(requestParameters: AdminApiPostAdminAuthorsByIdPayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PayoutResult> {
        const response = await this.postAdminAuthorsByIdPayoutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stops one author earning. Their record, verified claims and ledger are untouched — suspension halts future accrual, it does not erase what was already owed, and it does not delete the evidence behind it.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Stops one author earning.
     */
    async postAdminAuthorsByIdSuspendRaw(requestParameters: AdminApiPostAdminAuthorsByIdSuspendRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthorResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAdminAuthorsByIdSuspend().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/authors/{id}/suspend`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthorResultFromJSON(jsonValue));
    }

    /**
     * Stops one author earning. Their record, verified claims and ledger are untouched — suspension halts future accrual, it does not erase what was already owed, and it does not delete the evidence behind it.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Stops one author earning.
     */
    async postAdminAuthorsByIdSuspend(requestParameters: AdminApiPostAdminAuthorsByIdSuspendRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthorResult> {
        const response = await this.postAdminAuthorsByIdSuspendRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.  It is an OVERRIDE, not the mechanism: a background scheduler runs the same sweep on its own, and every author\'s dashboard read sweeps their own accruals lazily. This is the manual trigger for an operator who needs the numbers now. It is idempotent — the per-period latch means running it twice accrues nothing the second time.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.
     */
    async postAdminAuthorsSweepRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthorSweepResult>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/authors/sweep`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthorSweepResultFromJSON(jsonValue));
    }

    /**
     * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.  It is an OVERRIDE, not the mechanism: a background scheduler runs the same sweep on its own, and every author\'s dashboard read sweeps their own accruals lazily. This is the manual trigger for an operator who needs the numbers now. It is idempotent — the per-period latch means running it twice accrues nothing the second time.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.
     */
    async postAdminAuthorsSweep(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthorSweepResult> {
        const response = await this.postAdminAuthorsSweepRaw(initOverrides);
        return await response.value();
    }

    /**
     * Qualify-checks every pending referral and advances the ones that now qualify.  SuperAdmin only, fail-closed. This is the cron path, and the ONLY path that advances a referral: a referee QUALIFIES once they have made metered spend — the honest signal that they actually used the product rather than merely signing up.  Qualifying moves NO money. It records that an attribution became a real customer; what is owed for that is an affiliate payable in commerce, settled by wire or to a connected wallet. One pass is bounded, so a large backlog drains over several runs instead of wedging one request, and the latch makes the transition at-most-once under a concurrent sweep.  It reads nothing from the caller — the counters it returns are the whole result.
     * Qualify-checks every pending referral and advances the ones that now qualify.
     */
    async postAdminReferralsSweepRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SweepEnvelope>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/referrals/sweep`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SweepEnvelopeFromJSON(jsonValue));
    }

    /**
     * Qualify-checks every pending referral and advances the ones that now qualify.  SuperAdmin only, fail-closed. This is the cron path, and the ONLY path that advances a referral: a referee QUALIFIES once they have made metered spend — the honest signal that they actually used the product rather than merely signing up.  Qualifying moves NO money. It records that an attribution became a real customer; what is owed for that is an affiliate payable in commerce, settled by wire or to a connected wallet. One pass is bounded, so a large backlog drains over several runs instead of wedging one request, and the latch makes the transition at-most-once under a concurrent sweep.  It reads nothing from the caller — the counters it returns are the whole result.
     * Qualify-checks every pending referral and advances the ones that now qualify.
     */
    async postAdminReferralsSweep(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SweepEnvelope> {
        const response = await this.postAdminReferralsSweepRaw(initOverrides);
        return await response.value();
    }

    /**
     * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status. When the chain path is wired it signs and submits the anchor transaction and records it; when it is not, it returns the root that WOULD be committed plus the exact remaining wiring step and records nothing false. A submit that fails still answers 200 with the anchor\'s own status set to \"error\" — the attempt is the product. SuperAdmin only.
     * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status.
     */
    async postAdminTreasuryAnchorRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AnchorOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/treasury/anchor`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnchorOutFromJSON(jsonValue));
    }

    /**
     * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status. When the chain path is wired it signs and submits the anchor transaction and records it; when it is not, it returns the root that WOULD be committed plus the exact remaining wiring step and records nothing false. A submit that fails still answers 200 with the anchor\'s own status set to \"error\" — the attempt is the product. SuperAdmin only.
     * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status.
     */
    async postAdminTreasuryAnchor(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AnchorOut> {
        const response = await this.postAdminTreasuryAnchorRaw(initOverrides);
        return await response.value();
    }

    /**
     * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy. 0–10000; the change is audited. SuperAdmin only.
     * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy.
     */
    async postAdminTreasuryPolicyRaw(requestParameters: AdminApiPostAdminTreasuryPolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyOut>> {
        if (requestParameters['policyRequest'] == null) {
            throw new runtime.RequiredError(
                'policyRequest',
                'Required parameter "policyRequest" was null or undefined when calling postAdminTreasuryPolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/treasury/policy`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PolicyRequestToJSON(requestParameters['policyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyOutFromJSON(jsonValue));
    }

    /**
     * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy. 0–10000; the change is audited. SuperAdmin only.
     * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy.
     */
    async postAdminTreasuryPolicy(requestParameters: AdminApiPostAdminTreasuryPolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyOut> {
        const response = await this.postAdminTreasuryPolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote. A repeat of the same ref is at-most-once and reports created=false. SuperAdmin only.
     * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote.
     */
    async postAdminTreasurySeedRaw(requestParameters: AdminApiPostAdminTreasurySeedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SeedOut>> {
        if (requestParameters['seedRequest'] == null) {
            throw new runtime.RequiredError(
                'seedRequest',
                'Required parameter "seedRequest" was null or undefined when calling postAdminTreasurySeed().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/treasury/seed`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SeedRequestToJSON(requestParameters['seedRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SeedOutFromJSON(jsonValue));
    }

    /**
     * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote. A repeat of the same ref is at-most-once and reports created=false. SuperAdmin only.
     * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote.
     */
    async postAdminTreasurySeed(requestParameters: AdminApiPostAdminTreasurySeedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SeedOut> {
        const response = await this.postAdminTreasurySeedRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved. It is idempotent per period: a re-run of a period already swept accrues nothing and reports created=false. SuperAdmin only.
     * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved.
     */
    async postAdminTreasurySweepRaw(requestParameters: AdminApiPostAdminTreasurySweepRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SweepOut>> {
        if (requestParameters['sweepRequest'] == null) {
            throw new runtime.RequiredError(
                'sweepRequest',
                'Required parameter "sweepRequest" was null or undefined when calling postAdminTreasurySweep().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/treasury/sweep`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SweepRequestToJSON(requestParameters['sweepRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SweepOutFromJSON(jsonValue));
    }

    /**
     * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved. It is idempotent per period: a re-run of a period already swept accrues nothing and reports created=false. SuperAdmin only.
     * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved.
     */
    async postAdminTreasurySweep(requestParameters: AdminApiPostAdminTreasurySweepRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SweepOut> {
        const response = await this.postAdminTreasurySweepRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta. It is generic over kind, so the same call manages models, providers and product features through the one registry. `off` is an absolute kill switch: a self-service opt-in can never re-open it. SuperAdmin only; every other caller is refused.
     * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta.
     */
    async putAdminEnablementRaw(requestParameters: AdminApiPutAdminEnablementRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminEnablementItem>> {
        if (requestParameters['setEnablementBody'] == null) {
            throw new runtime.RequiredError(
                'setEnablementBody',
                'Required parameter "setEnablementBody" was null or undefined when calling putAdminEnablement().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/enablement`;

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SetEnablementBodyToJSON(requestParameters['setEnablementBody']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminEnablementItemFromJSON(jsonValue));
    }

    /**
     * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta. It is generic over kind, so the same call manages models, providers and product features through the one registry. `off` is an absolute kill switch: a self-service opt-in can never re-open it. SuperAdmin only; every other caller is refused.
     * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta.
     */
    async putAdminEnablement(requestParameters: AdminApiPutAdminEnablementRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminEnablementItem> {
        const response = await this.putAdminEnablementRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas. It provisions-or-resolves the caller org\'s treasury wallet on the deployed MPC ring and installs it, so every later anchor commits the ledger root SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent — a repeat resolves the same wallet, which is why the address is a PUT. SuperAdmin only.
     * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas.
     */
    async putAdminTreasuryAnchorSignerRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SignerOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/treasury/anchor/signer`;

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SignerOutFromJSON(jsonValue));
    }

    /**
     * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas. It provisions-or-resolves the caller org\'s treasury wallet on the deployed MPC ring and installs it, so every later anchor commits the ledger root SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent — a repeat resolves the same wallet, which is why the address is a PUT. SuperAdmin only.
     * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas.
     */
    async putAdminTreasuryAnchorSigner(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SignerOut> {
        const response = await this.putAdminTreasuryAnchorSignerRaw(initOverrides);
        return await response.value();
    }

}
