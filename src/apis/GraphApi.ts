/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  GraphAssertIn,
  GraphAssertOut,
  GraphNeighborsIn,
  GraphNeighborsOut,
  GraphQLIn,
  GraphQLOut,
  GraphReadOut,
  GraphResolveIn,
  GraphResolveOut,
  GraphVocabularyOut,
} from '../models/index.js';
import {
    GraphAssertInFromJSON,
    GraphAssertInToJSON,
    GraphAssertOutFromJSON,
    GraphAssertOutToJSON,
    GraphNeighborsInFromJSON,
    GraphNeighborsInToJSON,
    GraphNeighborsOutFromJSON,
    GraphNeighborsOutToJSON,
    GraphQLInFromJSON,
    GraphQLInToJSON,
    GraphQLOutFromJSON,
    GraphQLOutToJSON,
    GraphReadOutFromJSON,
    GraphReadOutToJSON,
    GraphResolveInFromJSON,
    GraphResolveInToJSON,
    GraphResolveOutFromJSON,
    GraphResolveOutToJSON,
    GraphVocabularyOutFromJSON,
    GraphVocabularyOutToJSON,
} from '../models/index.js';

export interface GraphApiGraphAssertRequest {
    graphAssertIn: GraphAssertIn;
}

export interface GraphApiGraphNeighborsRequest {
    graphNeighborsIn: GraphNeighborsIn;
}

export interface GraphApiGraphReadRequest {
    entity?: string;
    relation?: string;
    value?: string;
    asOf?: string;
    limit?: number;
}

export interface GraphApiGraphResolveRequest {
    graphResolveIn: GraphResolveIn;
}

export interface GraphApiGraphSearchRequest {
    q?: string;
    relation?: string;
    asOf?: string;
    limit?: number;
}

export interface GraphApiPostGraphGraphqlRequest {
    graphQLIn?: GraphQLIn;
}

/**
 * 
 */
export class GraphApi extends runtime.BaseAPI {

    /**
     * Assert what is true of an entity
     */
    async graphAssertRaw(requestParameters: GraphApiGraphAssertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphAssertOut>> {
        if (requestParameters['graphAssertIn'] == null) {
            throw new runtime.RequiredError(
                'graphAssertIn',
                'Required parameter "graphAssertIn" was null or undefined when calling graphAssert().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GraphAssertInToJSON(requestParameters['graphAssertIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphAssertOutFromJSON(jsonValue));
    }

    /**
     * Assert what is true of an entity
     */
    async graphAssert(requestParameters: GraphApiGraphAssertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphAssertOut> {
        const response = await this.graphAssertRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Walk the edges from a seed set, bounded
     */
    async graphNeighborsRaw(requestParameters: GraphApiGraphNeighborsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphNeighborsOut>> {
        if (requestParameters['graphNeighborsIn'] == null) {
            throw new runtime.RequiredError(
                'graphNeighborsIn',
                'Required parameter "graphNeighborsIn" was null or undefined when calling graphNeighbors().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph/neighbors`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GraphNeighborsInToJSON(requestParameters['graphNeighborsIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphNeighborsOutFromJSON(jsonValue));
    }

    /**
     * Walk the edges from a seed set, bounded
     */
    async graphNeighbors(requestParameters: GraphApiGraphNeighborsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphNeighborsOut> {
        const response = await this.graphNeighborsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Read the assertions this organization has recorded
     */
    async graphReadRaw(requestParameters: GraphApiGraphReadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphReadOut>> {
        const queryParameters: any = {};

        if (requestParameters['entity'] != null) {
            queryParameters['entity'] = requestParameters['entity'];
        }

        if (requestParameters['relation'] != null) {
            queryParameters['relation'] = requestParameters['relation'];
        }

        if (requestParameters['value'] != null) {
            queryParameters['value'] = requestParameters['value'];
        }

        if (requestParameters['asOf'] != null) {
            queryParameters['as_of'] = requestParameters['asOf'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphReadOutFromJSON(jsonValue));
    }

    /**
     * Read the assertions this organization has recorded
     */
    async graphRead(requestParameters: GraphApiGraphReadRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphReadOut> {
        const response = await this.graphReadRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * What is in force about an entity as of an instant, and what disagreed
     */
    async graphResolveRaw(requestParameters: GraphApiGraphResolveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphResolveOut>> {
        if (requestParameters['graphResolveIn'] == null) {
            throw new runtime.RequiredError(
                'graphResolveIn',
                'Required parameter "graphResolveIn" was null or undefined when calling graphResolve().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph/resolve`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GraphResolveInToJSON(requestParameters['graphResolveIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphResolveOutFromJSON(jsonValue));
    }

    /**
     * What is in force about an entity as of an instant, and what disagreed
     */
    async graphResolve(requestParameters: GraphApiGraphResolveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphResolveOut> {
        const response = await this.graphResolveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Finds assertions by their text where read finds them by their keys.  It is the READ with one more term, not a second way to leave the store: same order, same ceiling, same tenancy, and searching composes with narrowing by relation and by instant because all of them are terms of one filter.  It resolves nothing. What matches is what was asserted, including claims that were later corrected — which is the honest answer to \"where is this mentioned\" and the reason the caller then asks resolve about what it found.
     * Find assertions by their text rather than by an entity key
     */
    async graphSearchRaw(requestParameters: GraphApiGraphSearchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphReadOut>> {
        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['relation'] != null) {
            queryParameters['relation'] = requestParameters['relation'];
        }

        if (requestParameters['asOf'] != null) {
            queryParameters['as_of'] = requestParameters['asOf'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph/search`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphReadOutFromJSON(jsonValue));
    }

    /**
     * Finds assertions by their text where read finds them by their keys.  It is the READ with one more term, not a second way to leave the store: same order, same ceiling, same tenancy, and searching composes with narrowing by relation and by instant because all of them are terms of one filter.  It resolves nothing. What matches is what was asserted, including claims that were later corrected — which is the honest answer to \"where is this mentioned\" and the reason the caller then asks resolve about what it found.
     * Find assertions by their text rather than by an entity key
     */
    async graphSearch(requestParameters: GraphApiGraphSearchRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphReadOut> {
        const response = await this.graphSearchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The relations in use, and the rule that resolves a conflict
     */
    async graphVocabularyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphVocabularyOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph/vocabulary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphVocabularyOutFromJSON(jsonValue));
    }

    /**
     * The relations in use, and the rule that resolves a conflict
     */
    async graphVocabulary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphVocabularyOut> {
        const response = await this.graphVocabularyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Runs a GraphQL query against this organization\'s assertions.  It is the one endpoint here a caller can TRAVERSE: the REST ops each answer a single question, so composing them — the entities this one points at, and what each of those resolves to — costs a request per hop with the intermediate keys held by the caller. Here that is one query and the nesting is the answer\'s shape.  It adds no way to ask anything new. Every field runs the SAME operation the matching REST route runs, so the tenancy, the as-of bound, the traversal bounds and the conflict rule are the ones already in force; the schema is served by introspection.  A query that cannot run answers 200 with an `errors` list, which is the wire every GraphQL client parses.
     * Ask the graph in one request, traversing.
     */
    async postGraphGraphqlRaw(requestParameters: GraphApiPostGraphGraphqlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphQLOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/graph/graphql`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GraphQLInToJSON(requestParameters['graphQLIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphQLOutFromJSON(jsonValue));
    }

    /**
     * Runs a GraphQL query against this organization\'s assertions.  It is the one endpoint here a caller can TRAVERSE: the REST ops each answer a single question, so composing them — the entities this one points at, and what each of those resolves to — costs a request per hop with the intermediate keys held by the caller. Here that is one query and the nesting is the answer\'s shape.  It adds no way to ask anything new. Every field runs the SAME operation the matching REST route runs, so the tenancy, the as-of bound, the traversal bounds and the conflict rule are the ones already in force; the schema is served by introspection.  A query that cannot run answers 200 with an `errors` list, which is the wire every GraphQL client parses.
     * Ask the graph in one request, traversing.
     */
    async postGraphGraphql(requestParameters: GraphApiPostGraphGraphqlRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphQLOut> {
        const response = await this.postGraphGraphqlRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
