/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CreateVPCReq,
  VpcList,
  VpcView,
} from '../models/index.js';
import {
    CreateVPCReqFromJSON,
    CreateVPCReqToJSON,
    VpcListFromJSON,
    VpcListToJSON,
    VpcViewFromJSON,
    VpcViewToJSON,
} from '../models/index.js';

export interface VpcsApiDeleteVpcsByIdRequest {
    id: string;
}

export interface VpcsApiGetVpcsByIdRequest {
    id: string;
}

export interface VpcsApiPostVpcsRequest {
    createVPCReq: CreateVPCReq;
}

/**
 * 
 */
export class VpcsApi extends runtime.BaseAPI {

    /**
     * Removes one of the caller org\'s VPCs and answers 204. Ownership is confirmed by re-fetching the resource and checking its physical name carries the caller\'s org prefix BEFORE anything is deleted, so a cross-tenant id is a 404 rather than a delete of another org\'s VPC.
     * Removes one of the caller org\'s VPCs and answers 204.
     */
    async deleteVpcsByIdRaw(requestParameters: VpcsApiDeleteVpcsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteVpcsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/vpcs/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one of the caller org\'s VPCs and answers 204. Ownership is confirmed by re-fetching the resource and checking its physical name carries the caller\'s org prefix BEFORE anything is deleted, so a cross-tenant id is a 404 rather than a delete of another org\'s VPC.
     * Removes one of the caller org\'s VPCs and answers 204.
     */
    async deleteVpcsById(requestParameters: VpcsApiDeleteVpcsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteVpcsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Returns every VPC the caller\'s org owns, under the friendly names the org created them with. DigitalOcean is one account for the whole deployment, so the account-wide inventory is filtered to the caller\'s own \"o\"<orgHash>- name prefix and the prefix is stripped — another org\'s VPC is not merely hidden, it is never in the answer.
     * Returns every VPC the caller\'s org owns, under the friendly names the org created them with.
     */
    async getVpcsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VpcList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/vpcs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VpcListFromJSON(jsonValue));
    }

    /**
     * Returns every VPC the caller\'s org owns, under the friendly names the org created them with. DigitalOcean is one account for the whole deployment, so the account-wide inventory is filtered to the caller\'s own \"o\"<orgHash>- name prefix and the prefix is stripped — another org\'s VPC is not merely hidden, it is never in the answer.
     * Returns every VPC the caller\'s org owns, under the friendly names the org created them with.
     */
    async getVpcs(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VpcList> {
        const response = await this.getVpcsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the caller org\'s VPCs by id. A VPC that exists but sits in another org\'s namespace is reported 404, never 403 — the answer must not tell one tenant that another tenant\'s resource exists.
     * Returns one of the caller org\'s VPCs by id.
     */
    async getVpcsByIdRaw(requestParameters: VpcsApiGetVpcsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VpcView>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getVpcsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/vpcs/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VpcViewFromJSON(jsonValue));
    }

    /**
     * Returns one of the caller org\'s VPCs by id. A VPC that exists but sits in another org\'s namespace is reported 404, never 403 — the answer must not tell one tenant that another tenant\'s resource exists.
     * Returns one of the caller org\'s VPCs by id.
     */
    async getVpcsById(requestParameters: VpcsApiGetVpcsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VpcView> {
        const response = await this.getVpcsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a VPC in the caller\'s org namespace and answers 201 with it. The physical DigitalOcean name is derived server-side from the validated org, so a tenant can only ever create inside its own namespace; a name that already exists there is a 409.
     * Creates a VPC in the caller\'s org namespace and answers 201 with it.
     */
    async postVpcsRaw(requestParameters: VpcsApiPostVpcsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VpcView>> {
        if (requestParameters['createVPCReq'] == null) {
            throw new runtime.RequiredError(
                'createVPCReq',
                'Required parameter "createVPCReq" was null or undefined when calling postVpcs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/vpcs`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateVPCReqToJSON(requestParameters['createVPCReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VpcViewFromJSON(jsonValue));
    }

    /**
     * Creates a VPC in the caller\'s org namespace and answers 201 with it. The physical DigitalOcean name is derived server-side from the validated org, so a tenant can only ever create inside its own namespace; a name that already exists there is a 409.
     * Creates a VPC in the caller\'s org namespace and answers 201 with it.
     */
    async postVpcs(requestParameters: VpcsApiPostVpcsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VpcView> {
        const response = await this.postVpcsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
