/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  BucketRecord,
  BucketWrite,
  BusAck,
  BusMessage,
  BusPublish,
  BusRequest,
  ConsumerPage,
  ConsumerRecord,
  ConsumerWrite,
  FetchQuery,
  KvAck,
  KvEntry,
  KvPage,
  KvWrite,
  MessagePage,
  StreamPage,
  StreamRecord,
  StreamUpdate,
  StreamWrite,
} from '../models/index.js';
import {
    BucketRecordFromJSON,
    BucketRecordToJSON,
    BucketWriteFromJSON,
    BucketWriteToJSON,
    BusAckFromJSON,
    BusAckToJSON,
    BusMessageFromJSON,
    BusMessageToJSON,
    BusPublishFromJSON,
    BusPublishToJSON,
    BusRequestFromJSON,
    BusRequestToJSON,
    ConsumerPageFromJSON,
    ConsumerPageToJSON,
    ConsumerRecordFromJSON,
    ConsumerRecordToJSON,
    ConsumerWriteFromJSON,
    ConsumerWriteToJSON,
    FetchQueryFromJSON,
    FetchQueryToJSON,
    KvAckFromJSON,
    KvAckToJSON,
    KvEntryFromJSON,
    KvEntryToJSON,
    KvPageFromJSON,
    KvPageToJSON,
    KvWriteFromJSON,
    KvWriteToJSON,
    MessagePageFromJSON,
    MessagePageToJSON,
    StreamPageFromJSON,
    StreamPageToJSON,
    StreamRecordFromJSON,
    StreamRecordToJSON,
    StreamUpdateFromJSON,
    StreamUpdateToJSON,
    StreamWriteFromJSON,
    StreamWriteToJSON,
} from '../models/index.js';

export interface PubsubApiDeletePubsubJetstreamStreamsByStreamRequest {
    stream: string;
}

export interface PubsubApiDeletePubsubJetstreamStreamsByStreamConsumersByNameRequest {
    stream: string;
    name: string;
}

export interface PubsubApiDeletePubsubKvByBucketRequest {
    bucket: string;
}

export interface PubsubApiDeletePubsubKvByBucketByKeyRequest {
    bucket: string;
    key: string;
}

export interface PubsubApiGetPubsubJetstreamStreamsByStreamRequest {
    stream: string;
}

export interface PubsubApiGetPubsubJetstreamStreamsByStreamConsumersRequest {
    stream: string;
}

export interface PubsubApiGetPubsubJetstreamStreamsByStreamConsumersByNameRequest {
    stream: string;
    name: string;
}

export interface PubsubApiGetPubsubKvByBucketByKeyRequest {
    bucket: string;
    key: string;
}

export interface PubsubApiGetPubsubKvByBucketByKeyHistoryRequest {
    bucket: string;
    key: string;
}

export interface PubsubApiPostPubsubJetstreamStreamsRequest {
    streamWrite: StreamWrite;
}

export interface PubsubApiPostPubsubJetstreamStreamsByStreamConsumersRequest {
    stream: string;
    consumerWrite: ConsumerWrite;
}

export interface PubsubApiPostPubsubJetstreamStreamsByStreamConsumersByNameNextRequest {
    stream: string;
    name: string;
    fetchQuery: FetchQuery;
}

export interface PubsubApiPostPubsubKvByBucketRequest {
    bucket: string;
    bucketWrite: BucketWrite;
}

export interface PubsubApiPostPubsubPublishRequest {
    busPublish: BusPublish;
}

export interface PubsubApiPostPubsubRequestRequest {
    busRequest: BusRequest;
}

export interface PubsubApiPutPubsubJetstreamStreamsByStreamRequest {
    stream: string;
    streamUpdate: StreamUpdate;
}

export interface PubsubApiPutPubsubKvByBucketByKeyRequest {
    bucket: string;
    key: string;
    kvWrite: KvWrite;
}

/**
 * 
 */
export class PubsubApi extends runtime.BaseAPI {

    /**
     * Removes one stream of the caller\'s org — its retained messages and its consumers with it — and answers 204 with no body. 404 when the org has no stream of that name.
     * Removes one stream of the caller\'s org — its retained messages and its consumers with it — and answers 204 with no body.
     */
    async deletePubsubJetstreamStreamsByStreamRaw(requestParameters: PubsubApiDeletePubsubJetstreamStreamsByStreamRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling deletePubsubJetstreamStreamsByStream().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one stream of the caller\'s org — its retained messages and its consumers with it — and answers 204 with no body. 404 when the org has no stream of that name.
     * Removes one stream of the caller\'s org — its retained messages and its consumers with it — and answers 204 with no body.
     */
    async deletePubsubJetstreamStreamsByStream(requestParameters: PubsubApiDeletePubsubJetstreamStreamsByStreamRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePubsubJetstreamStreamsByStreamRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one consumer — its cursor, not the stream\'s messages — and answers 204 with no body. 404 when the stream or the consumer does not exist.
     * Removes one consumer — its cursor, not the stream\'s messages — and answers 204 with no body.
     */
    async deletePubsubJetstreamStreamsByStreamConsumersByNameRaw(requestParameters: PubsubApiDeletePubsubJetstreamStreamsByStreamConsumersByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling deletePubsubJetstreamStreamsByStreamConsumersByName().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deletePubsubJetstreamStreamsByStreamConsumersByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}/consumers/{name}`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one consumer — its cursor, not the stream\'s messages — and answers 204 with no body. 404 when the stream or the consumer does not exist.
     * Removes one consumer — its cursor, not the stream\'s messages — and answers 204 with no body.
     */
    async deletePubsubJetstreamStreamsByStreamConsumersByName(requestParameters: PubsubApiDeletePubsubJetstreamStreamsByStreamConsumersByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePubsubJetstreamStreamsByStreamConsumersByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one bucket of the caller\'s org — every key and every revision with it — and answers 204 with no body. 404 when the org has no bucket of that name.
     * Removes one bucket of the caller\'s org — every key and every revision with it — and answers 204 with no body.
     */
    async deletePubsubKvByBucketRaw(requestParameters: PubsubApiDeletePubsubKvByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling deletePubsubKvByBucket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/kv/{bucket}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one bucket of the caller\'s org — every key and every revision with it — and answers 204 with no body. 404 when the org has no bucket of that name.
     * Removes one bucket of the caller\'s org — every key and every revision with it — and answers 204 with no body.
     */
    async deletePubsubKvByBucket(requestParameters: PubsubApiDeletePubsubKvByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePubsubKvByBucketRaw(requestParameters, initOverrides);
    }

    /**
     * Delete removes one key — a delete marker in the key\'s history, so watchers see it and Get answers 404 — and answers 204 with no body. 404 when the bucket does not exist.
     * Delete removes one key — a delete marker in the key\'s history, so watchers see it and Get answers 404 — and answers 204 with no body.
     */
    async deletePubsubKvByBucketByKeyRaw(requestParameters: PubsubApiDeletePubsubKvByBucketByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling deletePubsubKvByBucketByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling deletePubsubKvByBucketByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/kv/{bucket}/{key}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delete removes one key — a delete marker in the key\'s history, so watchers see it and Get answers 404 — and answers 204 with no body. 404 when the bucket does not exist.
     * Delete removes one key — a delete marker in the key\'s history, so watchers see it and Get answers 404 — and answers 204 with no body.
     */
    async deletePubsubKvByBucketByKey(requestParameters: PubsubApiDeletePubsubKvByBucketByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePubsubKvByBucketByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the org\'s streams, sorted by name.  A stream is the durable log: it captures every message published to its subjects and retains them by its own limits, independent of any consumer. The listing is org-scoped server-side — one org can never see another\'s streams, and the platform\'s own planes never appear.
     * Returns the org\'s streams, sorted by name.
     */
    async getPubsubJetstreamStreamsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StreamPage>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StreamPageFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s streams, sorted by name.  A stream is the durable log: it captures every message published to its subjects and retains them by its own limits, independent of any consumer. The listing is org-scoped server-side — one org can never see another\'s streams, and the platform\'s own planes never appear.
     * Returns the org\'s streams, sorted by name.
     */
    async getPubsubJetstreamStreams(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StreamPage> {
        const response = await this.getPubsubJetstreamStreamsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one stream of the caller\'s org — its configuration and its live state (messages, bytes, sequence range, consumer count). 404 when the org has no stream of that name.
     * Returns one stream of the caller\'s org — its configuration and its live state (messages, bytes, sequence range, consumer count).
     */
    async getPubsubJetstreamStreamsByStreamRaw(requestParameters: PubsubApiGetPubsubJetstreamStreamsByStreamRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StreamRecord>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling getPubsubJetstreamStreamsByStream().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StreamRecordFromJSON(jsonValue));
    }

    /**
     * Returns one stream of the caller\'s org — its configuration and its live state (messages, bytes, sequence range, consumer count). 404 when the org has no stream of that name.
     * Returns one stream of the caller\'s org — its configuration and its live state (messages, bytes, sequence range, consumer count).
     */
    async getPubsubJetstreamStreamsByStream(requestParameters: PubsubApiGetPubsubJetstreamStreamsByStreamRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StreamRecord> {
        const response = await this.getPubsubJetstreamStreamsByStreamRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one stream\'s consumers, sorted by name. 404 when the org has no stream of that name.
     * Returns one stream\'s consumers, sorted by name.
     */
    async getPubsubJetstreamStreamsByStreamConsumersRaw(requestParameters: PubsubApiGetPubsubJetstreamStreamsByStreamConsumersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConsumerPage>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling getPubsubJetstreamStreamsByStreamConsumers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}/consumers`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConsumerPageFromJSON(jsonValue));
    }

    /**
     * Returns one stream\'s consumers, sorted by name. 404 when the org has no stream of that name.
     * Returns one stream\'s consumers, sorted by name.
     */
    async getPubsubJetstreamStreamsByStreamConsumers(requestParameters: PubsubApiGetPubsubJetstreamStreamsByStreamConsumersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConsumerPage> {
        const response = await this.getPubsubJetstreamStreamsByStreamConsumersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts. 404 when the stream or the consumer does not exist.
     * Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts.
     */
    async getPubsubJetstreamStreamsByStreamConsumersByNameRaw(requestParameters: PubsubApiGetPubsubJetstreamStreamsByStreamConsumersByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConsumerRecord>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling getPubsubJetstreamStreamsByStreamConsumersByName().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getPubsubJetstreamStreamsByStreamConsumersByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}/consumers/{name}`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConsumerRecordFromJSON(jsonValue));
    }

    /**
     * Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts. 404 when the stream or the consumer does not exist.
     * Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts.
     */
    async getPubsubJetstreamStreamsByStreamConsumersByName(requestParameters: PubsubApiGetPubsubJetstreamStreamsByStreamConsumersByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConsumerRecord> {
        const response = await this.getPubsubJetstreamStreamsByStreamConsumersByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Get returns one key\'s current value and revision. 404 when the bucket does not exist, the key was never written, or its latest revision is a delete.
     * Get returns one key\'s current value and revision.
     */
    async getPubsubKvByBucketByKeyRaw(requestParameters: PubsubApiGetPubsubKvByBucketByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KvEntry>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling getPubsubKvByBucketByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getPubsubKvByBucketByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/kv/{bucket}/{key}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KvEntryFromJSON(jsonValue));
    }

    /**
     * Get returns one key\'s current value and revision. 404 when the bucket does not exist, the key was never written, or its latest revision is a delete.
     * Get returns one key\'s current value and revision.
     */
    async getPubsubKvByBucketByKey(requestParameters: PubsubApiGetPubsubKvByBucketByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KvEntry> {
        const response = await this.getPubsubKvByBucketByKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * History returns one key\'s retained revisions, oldest first — every put and every delete marker up to the bucket\'s History depth. 404 when the bucket does not exist or the key was never written.
     * History returns one key\'s retained revisions, oldest first — every put and every delete marker up to the bucket\'s History depth.
     */
    async getPubsubKvByBucketByKeyHistoryRaw(requestParameters: PubsubApiGetPubsubKvByBucketByKeyHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KvPage>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling getPubsubKvByBucketByKeyHistory().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getPubsubKvByBucketByKeyHistory().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/kv/{bucket}/{key}/history`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KvPageFromJSON(jsonValue));
    }

    /**
     * History returns one key\'s retained revisions, oldest first — every put and every delete marker up to the bucket\'s History depth. 404 when the bucket does not exist or the key was never written.
     * History returns one key\'s retained revisions, oldest first — every put and every delete marker up to the bucket\'s History depth.
     */
    async getPubsubKvByBucketByKeyHistory(requestParameters: PubsubApiGetPubsubKvByBucketByKeyHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KvPage> {
        const response = await this.getPubsubKvByBucketByKeyHistoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a durable stream capturing the given subjects and returns it. 409 when the org already has a stream of that name; the subjects are the org\'s own and cannot collide with another org\'s.
     * Creates a durable stream capturing the given subjects and returns it.
     */
    async postPubsubJetstreamStreamsRaw(requestParameters: PubsubApiPostPubsubJetstreamStreamsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StreamRecord>> {
        if (requestParameters['streamWrite'] == null) {
            throw new runtime.RequiredError(
                'streamWrite',
                'Required parameter "streamWrite" was null or undefined when calling postPubsubJetstreamStreams().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StreamWriteToJSON(requestParameters['streamWrite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StreamRecordFromJSON(jsonValue));
    }

    /**
     * Creates a durable stream capturing the given subjects and returns it. 409 when the org already has a stream of that name; the subjects are the org\'s own and cannot collide with another org\'s.
     * Creates a durable stream capturing the given subjects and returns it.
     */
    async postPubsubJetstreamStreams(requestParameters: PubsubApiPostPubsubJetstreamStreamsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StreamRecord> {
        const response = await this.postPubsubJetstreamStreamsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a durable consumer on one stream and returns it. A consumer is a named cursor: it tracks what has been delivered and what is acknowledged, so many workers can share it and none sees a message twice outside redelivery. 409 when the stream already has a consumer of that name with a different configuration.
     * Creates a durable consumer on one stream and returns it.
     */
    async postPubsubJetstreamStreamsByStreamConsumersRaw(requestParameters: PubsubApiPostPubsubJetstreamStreamsByStreamConsumersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConsumerRecord>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling postPubsubJetstreamStreamsByStreamConsumers().'
            );
        }

        if (requestParameters['consumerWrite'] == null) {
            throw new runtime.RequiredError(
                'consumerWrite',
                'Required parameter "consumerWrite" was null or undefined when calling postPubsubJetstreamStreamsByStreamConsumers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}/consumers`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ConsumerWriteToJSON(requestParameters['consumerWrite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConsumerRecordFromJSON(jsonValue));
    }

    /**
     * Creates a durable consumer on one stream and returns it. A consumer is a named cursor: it tracks what has been delivered and what is acknowledged, so many workers can share it and none sees a message twice outside redelivery. 409 when the stream already has a consumer of that name with a different configuration.
     * Creates a durable consumer on one stream and returns it.
     */
    async postPubsubJetstreamStreamsByStreamConsumers(requestParameters: PubsubApiPostPubsubJetstreamStreamsByStreamConsumersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConsumerRecord> {
        const response = await this.postPubsubJetstreamStreamsByStreamConsumersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream. The hand-off is at-most-once: a message returned here is acked here, so a caller that loses the response does not see it again. Workers needing at-least-once delivery consume the same consumer over the NATS port, where acks are theirs to send. An empty batch after the wait is an empty page, not an error.
     * Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream.
     */
    async postPubsubJetstreamStreamsByStreamConsumersByNameNextRaw(requestParameters: PubsubApiPostPubsubJetstreamStreamsByStreamConsumersByNameNextRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessagePage>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling postPubsubJetstreamStreamsByStreamConsumersByNameNext().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postPubsubJetstreamStreamsByStreamConsumersByNameNext().'
            );
        }

        if (requestParameters['fetchQuery'] == null) {
            throw new runtime.RequiredError(
                'fetchQuery',
                'Required parameter "fetchQuery" was null or undefined when calling postPubsubJetstreamStreamsByStreamConsumersByNameNext().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}/consumers/{name}/next`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FetchQueryToJSON(requestParameters['fetchQuery']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessagePageFromJSON(jsonValue));
    }

    /**
     * Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream. The hand-off is at-most-once: a message returned here is acked here, so a caller that loses the response does not see it again. Workers needing at-least-once delivery consume the same consumer over the NATS port, where acks are theirs to send. An empty batch after the wait is an empty page, not an error.
     * Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream.
     */
    async postPubsubJetstreamStreamsByStreamConsumersByNameNext(requestParameters: PubsubApiPostPubsubJetstreamStreamsByStreamConsumersByNameNextRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessagePage> {
        const response = await this.postPubsubJetstreamStreamsByStreamConsumersByNameNextRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a KV bucket and returns it. A bucket is keyed state on the same durable plane as the streams: each key holds up to History revisions, entries can expire by TTL, and watchers on the NATS port see every write. 409 when the org already has a bucket of that name.
     * Creates a KV bucket and returns it.
     */
    async postPubsubKvByBucketRaw(requestParameters: PubsubApiPostPubsubKvByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BucketRecord>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling postPubsubKvByBucket().'
            );
        }

        if (requestParameters['bucketWrite'] == null) {
            throw new runtime.RequiredError(
                'bucketWrite',
                'Required parameter "bucketWrite" was null or undefined when calling postPubsubKvByBucket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/kv/{bucket}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BucketWriteToJSON(requestParameters['bucketWrite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BucketRecordFromJSON(jsonValue));
    }

    /**
     * Creates a KV bucket and returns it. A bucket is keyed state on the same durable plane as the streams: each key holds up to History revisions, entries can expire by TTL, and watchers on the NATS port see every write. 409 when the org already has a bucket of that name.
     * Creates a KV bucket and returns it.
     */
    async postPubsubKvByBucket(requestParameters: PubsubApiPostPubsubKvByBucketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BucketRecord> {
        const response = await this.postPubsubKvByBucketRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publish puts one message on the org\'s bus. When a stream captures the subject the write is DURABLE — the receipt names the stream and sequence only after JetStream has it on storage, and a repeated Nats-Msg-Id header within the dedup window answers duplicate instead of storing twice. When nothing captures it, the message goes out core NATS: delivered to current subscribers, receipt {ok}, nothing retained.
     * Publish puts one message on the org\'s bus.
     */
    async postPubsubPublishRaw(requestParameters: PubsubApiPostPubsubPublishRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BusAck>> {
        if (requestParameters['busPublish'] == null) {
            throw new runtime.RequiredError(
                'busPublish',
                'Required parameter "busPublish" was null or undefined when calling postPubsubPublish().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/publish`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BusPublishToJSON(requestParameters['busPublish']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BusAckFromJSON(jsonValue));
    }

    /**
     * Publish puts one message on the org\'s bus. When a stream captures the subject the write is DURABLE — the receipt names the stream and sequence only after JetStream has it on storage, and a repeated Nats-Msg-Id header within the dedup window answers duplicate instead of storing twice. When nothing captures it, the message goes out core NATS: delivered to current subscribers, receipt {ok}, nothing retained.
     * Publish puts one message on the org\'s bus.
     */
    async postPubsubPublish(requestParameters: PubsubApiPostPubsubPublishRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BusAck> {
        const response = await this.postPubsubPublishRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Request sends one request on the org\'s bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port. 404 when nobody is listening on the subject; 408 when a responder exists but no reply arrived within the timeout.
     * Request sends one request on the org\'s bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port.
     */
    async postPubsubRequestRaw(requestParameters: PubsubApiPostPubsubRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BusMessage>> {
        if (requestParameters['busRequest'] == null) {
            throw new runtime.RequiredError(
                'busRequest',
                'Required parameter "busRequest" was null or undefined when calling postPubsubRequest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/request`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BusRequestToJSON(requestParameters['busRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BusMessageFromJSON(jsonValue));
    }

    /**
     * Request sends one request on the org\'s bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port. 404 when nobody is listening on the subject; 408 when a responder exists but no reply arrived within the timeout.
     * Request sends one request on the org\'s bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port.
     */
    async postPubsubRequest(requestParameters: PubsubApiPostPubsubRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BusMessage> {
        const response = await this.postPubsubRequestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rewrites a stream\'s configuration — subjects, limits, discard — and returns the updated stream. It is a PUT: the spec sent replaces the spec held, with one reading for the enums a caller omits — an empty storage, retention or discard keeps the stream\'s current one, because JetStream holds storage and retention immutable and refuses a change with a 400 rather than this door pretending it took.
     * Rewrites a stream\'s configuration — subjects, limits, discard — and returns the updated stream.
     */
    async putPubsubJetstreamStreamsByStreamRaw(requestParameters: PubsubApiPutPubsubJetstreamStreamsByStreamRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StreamRecord>> {
        if (requestParameters['stream'] == null) {
            throw new runtime.RequiredError(
                'stream',
                'Required parameter "stream" was null or undefined when calling putPubsubJetstreamStreamsByStream().'
            );
        }

        if (requestParameters['streamUpdate'] == null) {
            throw new runtime.RequiredError(
                'streamUpdate',
                'Required parameter "streamUpdate" was null or undefined when calling putPubsubJetstreamStreamsByStream().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/jetstream/streams/{stream}`;
        urlPath = urlPath.replace(`{${"stream"}}`, encodeURIComponent(String(requestParameters['stream'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: StreamUpdateToJSON(requestParameters['streamUpdate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StreamRecordFromJSON(jsonValue));
    }

    /**
     * Rewrites a stream\'s configuration — subjects, limits, discard — and returns the updated stream. It is a PUT: the spec sent replaces the spec held, with one reading for the enums a caller omits — an empty storage, retention or discard keeps the stream\'s current one, because JetStream holds storage and retention immutable and refuses a change with a 400 rather than this door pretending it took.
     * Rewrites a stream\'s configuration — subjects, limits, discard — and returns the updated stream.
     */
    async putPubsubJetstreamStreamsByStream(requestParameters: PubsubApiPutPubsubJetstreamStreamsByStreamRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StreamRecord> {
        const response = await this.putPubsubJetstreamStreamsByStreamRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Put sets one key to one value and returns the revision the write created. Writes are versioned: each put is a new revision and the bucket retains up to its History of them per key.
     * Put sets one key to one value and returns the revision the write created.
     */
    async putPubsubKvByBucketByKeyRaw(requestParameters: PubsubApiPutPubsubKvByBucketByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KvAck>> {
        if (requestParameters['bucket'] == null) {
            throw new runtime.RequiredError(
                'bucket',
                'Required parameter "bucket" was null or undefined when calling putPubsubKvByBucketByKey().'
            );
        }

        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling putPubsubKvByBucketByKey().'
            );
        }

        if (requestParameters['kvWrite'] == null) {
            throw new runtime.RequiredError(
                'kvWrite',
                'Required parameter "kvWrite" was null or undefined when calling putPubsubKvByBucketByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/pubsub/kv/{bucket}/{key}`;
        urlPath = urlPath.replace(`{${"bucket"}}`, encodeURIComponent(String(requestParameters['bucket'])));
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: KvWriteToJSON(requestParameters['kvWrite']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KvAckFromJSON(jsonValue));
    }

    /**
     * Put sets one key to one value and returns the revision the write created. Writes are versioned: each put is a new revision and the bucket retains up to its History of them per key.
     * Put sets one key to one value and returns the revision the write created.
     */
    async putPubsubKvByBucketByKey(requestParameters: PubsubApiPutPubsubKvByBucketByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KvAck> {
        const response = await this.putPubsubKvByBucketByKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
