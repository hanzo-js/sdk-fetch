/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  KitList,
  PublishKitIn,
  ReplaceKitIn,
  StarterKit,
} from '../models/index.js';
import {
    KitListFromJSON,
    KitListToJSON,
    PublishKitInFromJSON,
    PublishKitInToJSON,
    ReplaceKitInFromJSON,
    ReplaceKitInToJSON,
    StarterKitFromJSON,
    StarterKitToJSON,
} from '../models/index.js';

export interface TemplatesApiDeleteTemplatesBySlugRequest {
    slug: string;
}

export interface TemplatesApiGetTemplatesBySlugRequest {
    slug: string;
}

export interface TemplatesApiPostTemplatesRequest {
    publishKitIn: PublishKitIn;
}

export interface TemplatesApiPutTemplatesBySlugRequest {
    slug: string;
    replaceKitIn: ReplaceKitIn;
}

/**
 * 
 */
export class TemplatesApi extends runtime.BaseAPI {

    /**
     * Deletes the caller org\'s OWN starter kit. A slug they do not own is a 404, never a delete: the DELETE binds org.
     * Deletes the caller org\'s OWN starter kit.
     */
    async deleteTemplatesBySlugRaw(requestParameters: TemplatesApiDeleteTemplatesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling deleteTemplatesBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/templates/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the caller org\'s OWN starter kit. A slug they do not own is a 404, never a delete: the DELETE binds org.
     * Deletes the caller org\'s OWN starter kit.
     */
    async deleteTemplatesBySlug(requestParameters: TemplatesApiDeleteTemplatesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTemplatesBySlugRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits. No request field can widen the scope: the org comes from the validated principal, so an anonymous or cross-org caller structurally sees the public catalog only.
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits.
     */
    async getTemplatesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KitList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/templates`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KitListFromJSON(jsonValue));
    }

    /**
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits. No request field can widen the scope: the org comes from the validated principal, so an anonymous or cross-org caller structurally sees the public catalog only.
     * Lists the public starter-kit catalog plus, for a validated caller, that org\'s own private kits.
     */
    async getTemplates(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KitList> {
        const response = await this.getTemplatesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s. A slug another org owns reads as not found.
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s.
     */
    async getTemplatesBySlugRaw(requestParameters: TemplatesApiGetTemplatesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StarterKit>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling getTemplatesBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/templates/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StarterKitFromJSON(jsonValue));
    }

    /**
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s. A slug another org owns reads as not found.
     * Returns one starter kit: the caller org\'s own by that slug, else the public catalog\'s.
     */
    async getTemplatesBySlug(requestParameters: TemplatesApiGetTemplatesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StarterKit> {
        const response = await this.getTemplatesBySlugRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit. The owner is stamped by the server, so a body \"org\" is never trusted; publishing over a public-catalog slug is 409, so a slug still names exactly one kit.
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit.
     */
    async postTemplatesRaw(requestParameters: TemplatesApiPostTemplatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StarterKit>> {
        if (requestParameters['publishKitIn'] == null) {
            throw new runtime.RequiredError(
                'publishKitIn',
                'Required parameter "publishKitIn" was null or undefined when calling postTemplates().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/templates`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PublishKitInToJSON(requestParameters['publishKitIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StarterKitFromJSON(jsonValue));
    }

    /**
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit. The owner is stamped by the server, so a body \"org\" is never trusted; publishing over a public-catalog slug is 409, so a slug still names exactly one kit.
     * Creates a starter kit PRIVATE to the caller\'s org and answers 201 with the stored kit.
     */
    async postTemplates(requestParameters: TemplatesApiPostTemplatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StarterKit> {
        const response = await this.postTemplatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit. A slug they do not own is 404, never a create: the UPDATE binds org, so a PUT can never reach another org\'s kit.
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit.
     */
    async putTemplatesBySlugRaw(requestParameters: TemplatesApiPutTemplatesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StarterKit>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling putTemplatesBySlug().'
            );
        }

        if (requestParameters['replaceKitIn'] == null) {
            throw new runtime.RequiredError(
                'replaceKitIn',
                'Required parameter "replaceKitIn" was null or undefined when calling putTemplatesBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/templates/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ReplaceKitInToJSON(requestParameters['replaceKitIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StarterKitFromJSON(jsonValue));
    }

    /**
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit. A slug they do not own is 404, never a create: the UPDATE binds org, so a PUT can never reach another org\'s kit.
     * Overwrites the caller org\'s OWN starter kit at the path slug, answering the stored kit.
     */
    async putTemplatesBySlug(requestParameters: TemplatesApiPutTemplatesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StarterKit> {
        const response = await this.putTemplatesBySlugRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
