/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Catalog,
  CreateFlowReq,
  CreateVersionIn,
  Flow,
  FlowPage,
  FlowRun,
  FlowVersion,
  PatchFlowIn,
  PopulatedFlow,
  RunIn,
  RunPage,
  RunResp,
  VersionPage,
} from '../models/index.js';
import {
    CatalogFromJSON,
    CatalogToJSON,
    CreateFlowReqFromJSON,
    CreateFlowReqToJSON,
    CreateVersionInFromJSON,
    CreateVersionInToJSON,
    FlowFromJSON,
    FlowToJSON,
    FlowPageFromJSON,
    FlowPageToJSON,
    FlowRunFromJSON,
    FlowRunToJSON,
    FlowVersionFromJSON,
    FlowVersionToJSON,
    PatchFlowInFromJSON,
    PatchFlowInToJSON,
    PopulatedFlowFromJSON,
    PopulatedFlowToJSON,
    RunInFromJSON,
    RunInToJSON,
    RunPageFromJSON,
    RunPageToJSON,
    RunRespFromJSON,
    RunRespToJSON,
    VersionPageFromJSON,
    VersionPageToJSON,
} from '../models/index.js';

export interface AutomationsApiDeleteAutomationsFlowsByIdRequest {
    id: string;
}

export interface AutomationsApiGetAutomationsFlowsRequest {
    limit?: number;
}

export interface AutomationsApiGetAutomationsFlowsByIdRequest {
    id: string;
}

export interface AutomationsApiGetAutomationsFlowsByIdVersionsRequest {
    id: string;
    limit?: number;
}

export interface AutomationsApiGetAutomationsRunsRequest {
    flowId?: string;
    limit?: number;
}

export interface AutomationsApiGetAutomationsRunsByIdRequest {
    id: string;
}

export interface AutomationsApiPatchAutomationsFlowsByIdRequest {
    id: string;
    patchFlowIn: PatchFlowIn;
}

export interface AutomationsApiPostAutomationsConnectorsByIdRunRequest {
    id: string;
    runIn: RunIn;
}

export interface AutomationsApiPostAutomationsFlowsRequest {
    createFlowReq: CreateFlowReq;
}

export interface AutomationsApiPostAutomationsFlowsByIdDisableRequest {
    id: string;
}

export interface AutomationsApiPostAutomationsFlowsByIdEnableRequest {
    id: string;
}

export interface AutomationsApiPostAutomationsFlowsByIdOperationsRequest {
    id: string;
}

export interface AutomationsApiPostAutomationsFlowsByIdRunRequest {
    id: string;
}

export interface AutomationsApiPostAutomationsFlowsByIdVersionsRequest {
    id: string;
    createVersionIn: CreateVersionIn;
}

export interface AutomationsApiPostAutomationsHooksBySourceByEventRequest {
    source: string;
    event: string;
}

export interface AutomationsApiPostAutomationsRunsByIdResumeRequest {
    id: string;
}

/**
 * 
 */
export class AutomationsApi extends runtime.BaseAPI {

    /**
     * Deletes one automation, its versions and its run history. It answers no content, and a flow of another org answers not-found.
     * Deletes one automation, its versions and its run history.
     */
    async deleteAutomationsFlowsByIdRaw(requestParameters: AutomationsApiDeleteAutomationsFlowsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteAutomationsFlowsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one automation, its versions and its run history. It answers no content, and a flow of another org answers not-found.
     * Deletes one automation, its versions and its run history.
     */
    async deleteAutomationsFlowsById(requestParameters: AutomationsApiDeleteAutomationsFlowsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteAutomationsFlowsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Connectors returns the connector catalogue. Each entry is an external service a flow step can invoke, carrying its auth descriptor and the input properties of its actions and triggers. The catalogue is the same for every tenant, so the gate is a validated principal rather than a per-org view.
     * Connectors returns the connector catalogue.
     */
    async getAutomationsConnectorsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Catalog>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/connectors`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CatalogFromJSON(jsonValue));
    }

    /**
     * Connectors returns the connector catalogue. Each entry is an external service a flow step can invoke, carrying its auth descriptor and the input properties of its actions and triggers. The catalogue is the same for every tenant, so the gate is a validated principal rather than a per-org view.
     * Connectors returns the connector catalogue.
     */
    async getAutomationsConnectors(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Catalog> {
        const response = await this.getAutomationsConnectorsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s automations, most-recently-updated first. The optional `limit` query bounds the page.
     * Returns the caller org\'s automations, most-recently-updated first.
     */
    async getAutomationsFlowsRaw(requestParameters: AutomationsApiGetAutomationsFlowsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlowPage>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowPageFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s automations, most-recently-updated first. The optional `limit` query bounds the page.
     * Returns the caller org\'s automations, most-recently-updated first.
     */
    async getAutomationsFlows(requestParameters: AutomationsApiGetAutomationsFlowsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlowPage> {
        const response = await this.getAutomationsFlowsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one automation and its latest version. That is the flow record plus the step tree the builder edits; a flow of another org answers not-found.
     * Returns one automation and its latest version.
     */
    async getAutomationsFlowsByIdRaw(requestParameters: AutomationsApiGetAutomationsFlowsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PopulatedFlow>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAutomationsFlowsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PopulatedFlowFromJSON(jsonValue));
    }

    /**
     * Returns one automation and its latest version. That is the flow record plus the step tree the builder edits; a flow of another org answers not-found.
     * Returns one automation and its latest version.
     */
    async getAutomationsFlowsById(requestParameters: AutomationsApiGetAutomationsFlowsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PopulatedFlow> {
        const response = await this.getAutomationsFlowsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one flow\'s versions, newest first. The optional `limit` query bounds the page.
     * Returns one flow\'s versions, newest first.
     */
    async getAutomationsFlowsByIdVersionsRaw(requestParameters: AutomationsApiGetAutomationsFlowsByIdVersionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VersionPage>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAutomationsFlowsByIdVersions().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}/versions`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VersionPageFromJSON(jsonValue));
    }

    /**
     * Returns one flow\'s versions, newest first. The optional `limit` query bounds the page.
     * Returns one flow\'s versions, newest first.
     */
    async getAutomationsFlowsByIdVersions(requestParameters: AutomationsApiGetAutomationsFlowsByIdVersionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VersionPage> {
        const response = await this.getAutomationsFlowsByIdVersionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Pieces is the retired-name alias of the connector catalogue. It serves exactly what GET /v1/automations/connectors serves, under the name this surface used before \"piece\" (the ActivePieces term) became \"connector\", and stays valid for clients pinned to the old path. Prefer /connectors.
     * Pieces is the retired-name alias of the connector catalogue.
     */
    async getAutomationsPiecesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Catalog>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/pieces`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CatalogFromJSON(jsonValue));
    }

    /**
     * Pieces is the retired-name alias of the connector catalogue. It serves exactly what GET /v1/automations/connectors serves, under the name this surface used before \"piece\" (the ActivePieces term) became \"connector\", and stays valid for clients pinned to the old path. Prefer /connectors.
     * Pieces is the retired-name alias of the connector catalogue.
     */
    async getAutomationsPieces(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Catalog> {
        const response = await this.getAutomationsPiecesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s run history, newest first. The optional `flowId` query narrows it to one flow and `limit` bounds the page.
     * Returns the caller org\'s run history, newest first.
     */
    async getAutomationsRunsRaw(requestParameters: AutomationsApiGetAutomationsRunsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RunPage>> {
        const queryParameters: any = {};

        if (requestParameters['flowId'] != null) {
            queryParameters['flowId'] = requestParameters['flowId'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/runs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RunPageFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s run history, newest first. The optional `flowId` query narrows it to one flow and `limit` bounds the page.
     * Returns the caller org\'s run history, newest first.
     */
    async getAutomationsRuns(requestParameters: AutomationsApiGetAutomationsRunsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RunPage> {
        const response = await this.getAutomationsRunsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one run. A run that has not reached a terminal status is refreshed from the durable engine first — scoped to the org\'s own namespace — so the caller sees live progress rather than the last status that happened to be persisted.
     * Returns one run.
     */
    async getAutomationsRunsByIdRaw(requestParameters: AutomationsApiGetAutomationsRunsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlowRun>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAutomationsRunsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/runs/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowRunFromJSON(jsonValue));
    }

    /**
     * Returns one run. A run that has not reached a terminal status is refreshed from the durable engine first — scoped to the org\'s own namespace — so the caller sees live progress rather than the last status that happened to be persisted.
     * Returns one run.
     */
    async getAutomationsRunsById(requestParameters: AutomationsApiGetAutomationsRunsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlowRun> {
        const response = await this.getAutomationsRunsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Updates one automation\'s metadata in place. Every field is optional; a field the request omits is left alone. Publishing a version pins which one runs, and is refused unless that version belongs to this flow.
     * Updates one automation\'s metadata in place.
     */
    async patchAutomationsFlowsByIdRaw(requestParameters: AutomationsApiPatchAutomationsFlowsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Flow>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling patchAutomationsFlowsById().'
            );
        }

        if (requestParameters['patchFlowIn'] == null) {
            throw new runtime.RequiredError(
                'patchFlowIn',
                'Required parameter "patchFlowIn" was null or undefined when calling patchAutomationsFlowsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchFlowInToJSON(requestParameters['patchFlowIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowFromJSON(jsonValue));
    }

    /**
     * Updates one automation\'s metadata in place. Every field is optional; a field the request omits is left alone. Publishing a version pins which one runs, and is refused unless that version belongs to this flow.
     * Updates one automation\'s metadata in place.
     */
    async patchAutomationsFlowsById(requestParameters: AutomationsApiPatchAutomationsFlowsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Flow> {
        const response = await this.patchAutomationsFlowsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Run executes one connector action in-process and answers the outcome. The caller\'s resolved credential travels in `auth`, delivered to the action verbatim — the runtime resolves no credential itself. An action that ran and failed (or an action name the connector does not have) answers ok:false with the failure message, not an HTTP error; an unknown connector is 404 and a missing action 422.
     * Run executes one connector action in-process and answers the outcome.
     */
    async postAutomationsConnectorsByIdRunRaw(requestParameters: AutomationsApiPostAutomationsConnectorsByIdRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RunResp>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsConnectorsByIdRun().'
            );
        }

        if (requestParameters['runIn'] == null) {
            throw new runtime.RequiredError(
                'runIn',
                'Required parameter "runIn" was null or undefined when calling postAutomationsConnectorsByIdRun().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/connectors/{id}/run`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RunInToJSON(requestParameters['runIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RunRespFromJSON(jsonValue));
    }

    /**
     * Run executes one connector action in-process and answers the outcome. The caller\'s resolved credential travels in `auth`, delivered to the action verbatim — the runtime resolves no credential itself. An action that ran and failed (or an action name the connector does not have) answers ok:false with the failure message, not an HTTP error; an unknown connector is 404 and a missing action 422.
     * Run executes one connector action in-process and answers the outcome.
     */
    async postAutomationsConnectorsByIdRun(requestParameters: AutomationsApiPostAutomationsConnectorsByIdRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RunResp> {
        const response = await this.postAutomationsConnectorsByIdRunRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an automation and its initial DRAFT version in one call. The new flow is DISABLED — creating it does not arm its trigger; POST /v1/automations/flows/{id}/enable does that.
     * Creates an automation and its initial DRAFT version in one call.
     */
    async postAutomationsFlowsRaw(requestParameters: AutomationsApiPostAutomationsFlowsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PopulatedFlow>> {
        if (requestParameters['createFlowReq'] == null) {
            throw new runtime.RequiredError(
                'createFlowReq',
                'Required parameter "createFlowReq" was null or undefined when calling postAutomationsFlows().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateFlowReqToJSON(requestParameters['createFlowReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PopulatedFlowFromJSON(jsonValue));
    }

    /**
     * Creates an automation and its initial DRAFT version in one call. The new flow is DISABLED — creating it does not arm its trigger; POST /v1/automations/flows/{id}/enable does that.
     * Creates an automation and its initial DRAFT version in one call.
     */
    async postAutomationsFlows(requestParameters: AutomationsApiPostAutomationsFlowsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PopulatedFlow> {
        const response = await this.postAutomationsFlowsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Disarms a flow\'s trigger and marks it DISABLED. Its schedule and its event subscriptions are dropped, so a disabled flow is never a live target; runs already in flight are unaffected, and it can still be started on demand.
     * Disarms a flow\'s trigger and marks it DISABLED.
     */
    async postAutomationsFlowsByIdDisableRaw(requestParameters: AutomationsApiPostAutomationsFlowsByIdDisableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Flow>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsFlowsByIdDisable().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}/disable`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowFromJSON(jsonValue));
    }

    /**
     * Disarms a flow\'s trigger and marks it DISABLED. Its schedule and its event subscriptions are dropped, so a disabled flow is never a live target; runs already in flight are unaffected, and it can still be started on demand.
     * Disarms a flow\'s trigger and marks it DISABLED.
     */
    async postAutomationsFlowsByIdDisable(requestParameters: AutomationsApiPostAutomationsFlowsByIdDisableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Flow> {
        const response = await this.postAutomationsFlowsByIdDisableRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Arms a flow\'s trigger and marks it ENABLED. A POLLING trigger gets a cron schedule on the durable engine; a WEBHOOK trigger gets a subscription in the routing index, so an inbound event starts it; a MANUAL trigger arms nothing and still runs on demand.
     * Arms a flow\'s trigger and marks it ENABLED.
     */
    async postAutomationsFlowsByIdEnableRaw(requestParameters: AutomationsApiPostAutomationsFlowsByIdEnableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Flow>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsFlowsByIdEnable().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}/enable`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowFromJSON(jsonValue));
    }

    /**
     * Arms a flow\'s trigger and marks it ENABLED. A POLLING trigger gets a cron schedule on the durable engine; a WEBHOOK trigger gets a subscription in the routing index, so an inbound event starts it; a MANUAL trigger arms nothing and still runs on demand.
     * Arms a flow\'s trigger and marks it ENABLED.
     */
    async postAutomationsFlowsByIdEnable(requestParameters: AutomationsApiPostAutomationsFlowsByIdEnableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Flow> {
        const response = await this.postAutomationsFlowsByIdEnableRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies ONE flow operation and answers the thing it changed. The operation is named by `type`, with its arguments under `request`: `CHANGE_NAME`, `UPDATE_TRIGGER`, `ADD_ACTION`, `UPDATE_ACTION`, `MOVE_ACTION`, `DELETE_ACTION` edit the flow\'s LATEST version and answer with that version, and `CHANGE_STATUS` instead enables or disables the flow and answers with the FLOW. Two response shapes on one address is the rule a reader would otherwise get wrong, and it is why this route is not a typed op.  Edits land on the latest version only — the published version a run executes is untouched until it is republished — and the whole resulting step tree is re-validated against the step-count and size caps after every operation, so a long sequence of `ADD_ACTION` calls cannot grow a flow past a bound one step at a time (422 when it would). Org-scoped and fails closed: a validated principal is required (403 without one), the flow and its version are read under the caller\'s OWN org so another tenant\'s id is a 404, and an operation whose `request` does not decode is a 400.
     * Edit a flow — rename it, retarget its trigger, or add, move and delete steps
     */
    async postAutomationsFlowsByIdOperationsRaw(requestParameters: AutomationsApiPostAutomationsFlowsByIdOperationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsFlowsByIdOperations().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}/operations`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Applies ONE flow operation and answers the thing it changed. The operation is named by `type`, with its arguments under `request`: `CHANGE_NAME`, `UPDATE_TRIGGER`, `ADD_ACTION`, `UPDATE_ACTION`, `MOVE_ACTION`, `DELETE_ACTION` edit the flow\'s LATEST version and answer with that version, and `CHANGE_STATUS` instead enables or disables the flow and answers with the FLOW. Two response shapes on one address is the rule a reader would otherwise get wrong, and it is why this route is not a typed op.  Edits land on the latest version only — the published version a run executes is untouched until it is republished — and the whole resulting step tree is re-validated against the step-count and size caps after every operation, so a long sequence of `ADD_ACTION` calls cannot grow a flow past a bound one step at a time (422 when it would). Org-scoped and fails closed: a validated principal is required (403 without one), the flow and its version are read under the caller\'s OWN org so another tenant\'s id is a 404, and an operation whose `request` does not decode is a 400.
     * Edit a flow — rename it, retarget its trigger, or add, move and delete steps
     */
    async postAutomationsFlowsByIdOperations(requestParameters: AutomationsApiPostAutomationsFlowsByIdOperationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postAutomationsFlowsByIdOperationsRaw(requestParameters, initOverrides);
    }

    /**
     * Starts one durable run of a flow now. It runs the flow\'s published version if one is pinned, else its latest, and answers the run record it created. The run is bounded by the org\'s per-minute run-start budget and its in-flight concurrency ceiling; over either, or with the engine not ready, no run is started and no run id is burned.
     * Starts one durable run of a flow now.
     */
    async postAutomationsFlowsByIdRunRaw(requestParameters: AutomationsApiPostAutomationsFlowsByIdRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlowRun>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsFlowsByIdRun().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}/run`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowRunFromJSON(jsonValue));
    }

    /**
     * Starts one durable run of a flow now. It runs the flow\'s published version if one is pinned, else its latest, and answers the run record it created. The run is bounded by the org\'s per-minute run-start budget and its in-flight concurrency ceiling; over either, or with the engine not ready, no run is started and no run id is burned.
     * Starts one durable run of a flow now.
     */
    async postAutomationsFlowsByIdRun(requestParameters: AutomationsApiPostAutomationsFlowsByIdRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlowRun> {
        const response = await this.postAutomationsFlowsByIdRunRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a new DRAFT version to a flow. The version is created invalid unless it carries a trigger, and it does not become the running version until it is published (PATCH the flow\'s publishedVersionId) or becomes the latest.
     * Adds a new DRAFT version to a flow.
     */
    async postAutomationsFlowsByIdVersionsRaw(requestParameters: AutomationsApiPostAutomationsFlowsByIdVersionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FlowVersion>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsFlowsByIdVersions().'
            );
        }

        if (requestParameters['createVersionIn'] == null) {
            throw new runtime.RequiredError(
                'createVersionIn',
                'Required parameter "createVersionIn" was null or undefined when calling postAutomationsFlowsByIdVersions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/flows/{id}/versions`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateVersionInToJSON(requestParameters['createVersionIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FlowVersionFromJSON(jsonValue));
    }

    /**
     * Adds a new DRAFT version to a flow. The version is created invalid unless it carries a trigger, and it does not become the running version until it is published (PATCH the flow\'s publishedVersionId) or becomes the latest.
     * Adds a new DRAFT version to a flow.
     */
    async postAutomationsFlowsByIdVersions(requestParameters: AutomationsApiPostAutomationsFlowsByIdVersionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FlowVersion> {
        const response = await this.postAutomationsFlowsByIdVersionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Delivers one event to the org\'s automation triggers and answers `{matched:n}` — how many enabled flows had a webhook trigger on this `(source, event)` key and were started by it. A zero match is a success, not an error: nothing was subscribed.  The path is the trigger key and the JSON object body is the event payload, threaded into each started run as `{{trigger.*}}` with all of its keys intact — which is why this is not a typed op, since a declared input struct would silently DISCARD every payload key it had no field for. Re-delivery is a no-op: an `X-Idempotency-Key` header dedupes, and with none the body is content-hashed instead, so a hammer of identical posts collapses to ONE run rather than minting a fresh one per post. An in-platform producer may propagate `X-Causation-Depth` so a firing that a flow caused is bounded against a loop; an absent or invalid header reads as depth 0, an external origin.  Authenticated and org-scoped, unlike a provider\'s public webhook URL: a validated principal is required (403 without one) and the org is that principal\'s, never the body\'s, so a producer can only fire into its own tenant\'s flows. Both path segments are required (400) and a payload over the size limit is a 413.
     * Fire an event that starts every enabled flow subscribed to it
     */
    async postAutomationsHooksBySourceByEventRaw(requestParameters: AutomationsApiPostAutomationsHooksBySourceByEventRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['source'] == null) {
            throw new runtime.RequiredError(
                'source',
                'Required parameter "source" was null or undefined when calling postAutomationsHooksBySourceByEvent().'
            );
        }

        if (requestParameters['event'] == null) {
            throw new runtime.RequiredError(
                'event',
                'Required parameter "event" was null or undefined when calling postAutomationsHooksBySourceByEvent().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/hooks/{source}/{event}`;
        urlPath = urlPath.replace(`{${"source"}}`, encodeURIComponent(String(requestParameters['source'])));
        urlPath = urlPath.replace(`{${"event"}}`, encodeURIComponent(String(requestParameters['event'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delivers one event to the org\'s automation triggers and answers `{matched:n}` — how many enabled flows had a webhook trigger on this `(source, event)` key and were started by it. A zero match is a success, not an error: nothing was subscribed.  The path is the trigger key and the JSON object body is the event payload, threaded into each started run as `{{trigger.*}}` with all of its keys intact — which is why this is not a typed op, since a declared input struct would silently DISCARD every payload key it had no field for. Re-delivery is a no-op: an `X-Idempotency-Key` header dedupes, and with none the body is content-hashed instead, so a hammer of identical posts collapses to ONE run rather than minting a fresh one per post. An in-platform producer may propagate `X-Causation-Depth` so a firing that a flow caused is bounded against a loop; an absent or invalid header reads as depth 0, an external origin.  Authenticated and org-scoped, unlike a provider\'s public webhook URL: a validated principal is required (403 without one) and the org is that principal\'s, never the body\'s, so a producer can only fire into its own tenant\'s flows. Both path segments are required (400) and a payload over the size limit is a 413.
     * Fire an event that starts every enabled flow subscribed to it
     */
    async postAutomationsHooksBySourceByEvent(requestParameters: AutomationsApiPostAutomationsHooksBySourceByEventRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postAutomationsHooksBySourceByEventRaw(requestParameters, initOverrides);
    }

    /**
     * Delivers the durable `resume` signal to a run parked on a `wait_for_approval` waitpoint and answers `{resumed:true}` once the engine has taken it.  The body is an ARBITRARY JSON value — object, array, string, number — delivered VERBATIM into the workflow as that waitpoint\'s output, so it is what the steps after the approval read as their input. An empty body resumes with no payload. That open shape is why this route is not a typed op: an operation\'s input can carry the payload or the run address, never both.  Org-scoped and fails closed: a validated principal is required (403 without one), the run is read under the caller\'s OWN org so another tenant\'s run id is a 404, a body that is not JSON is a 400, and a payload over the size limit is a 413 — it becomes durable engine state, so it is bounded here rather than after it lands. The resume is audited as `automations.run.resume`.
     * Release a run waiting at an approval step, with the approval payload
     */
    async postAutomationsRunsByIdResumeRaw(requestParameters: AutomationsApiPostAutomationsRunsByIdResumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postAutomationsRunsByIdResume().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/automations/runs/{id}/resume`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delivers the durable `resume` signal to a run parked on a `wait_for_approval` waitpoint and answers `{resumed:true}` once the engine has taken it.  The body is an ARBITRARY JSON value — object, array, string, number — delivered VERBATIM into the workflow as that waitpoint\'s output, so it is what the steps after the approval read as their input. An empty body resumes with no payload. That open shape is why this route is not a typed op: an operation\'s input can carry the payload or the run address, never both.  Org-scoped and fails closed: a validated principal is required (403 without one), the run is read under the caller\'s OWN org so another tenant\'s run id is a 404, a body that is not JSON is a 400, and a payload over the size limit is a 413 — it becomes durable engine state, so it is bounded here rather than after it lands. The resume is audited as `automations.run.resume`.
     * Release a run waiting at an approval step, with the approval payload
     */
    async postAutomationsRunsByIdResume(requestParameters: AutomationsApiPostAutomationsRunsByIdResumeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postAutomationsRunsByIdResumeRaw(requestParameters, initOverrides);
    }

}
