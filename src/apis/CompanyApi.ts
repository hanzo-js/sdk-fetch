/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AdvanceIn,
  BeginIn,
  DecisionIn,
  DeckOut,
  EsignCompleteIn,
  EsignOut,
  FormationView,
  FoundersIn,
  ImportCapTableIn,
  ImportCapTableOut,
  ImportDocumentsIn,
  ImportDocumentsOut,
  KycRefreshOut,
  KycStartOut,
  RegisterCounts,
  RegisterPage,
  ReviewQueue,
  RoundInput,
  RoundOut,
  SafeIn,
  SafeOut,
  StructureIn,
} from '../models/index.js';
import {
    AdvanceInFromJSON,
    AdvanceInToJSON,
    BeginInFromJSON,
    BeginInToJSON,
    DecisionInFromJSON,
    DecisionInToJSON,
    DeckOutFromJSON,
    DeckOutToJSON,
    EsignCompleteInFromJSON,
    EsignCompleteInToJSON,
    EsignOutFromJSON,
    EsignOutToJSON,
    FormationViewFromJSON,
    FormationViewToJSON,
    FoundersInFromJSON,
    FoundersInToJSON,
    ImportCapTableInFromJSON,
    ImportCapTableInToJSON,
    ImportCapTableOutFromJSON,
    ImportCapTableOutToJSON,
    ImportDocumentsInFromJSON,
    ImportDocumentsInToJSON,
    ImportDocumentsOutFromJSON,
    ImportDocumentsOutToJSON,
    KycRefreshOutFromJSON,
    KycRefreshOutToJSON,
    KycStartOutFromJSON,
    KycStartOutToJSON,
    RegisterCountsFromJSON,
    RegisterCountsToJSON,
    RegisterPageFromJSON,
    RegisterPageToJSON,
    ReviewQueueFromJSON,
    ReviewQueueToJSON,
    RoundInputFromJSON,
    RoundInputToJSON,
    RoundOutFromJSON,
    RoundOutToJSON,
    SafeInFromJSON,
    SafeInToJSON,
    SafeOutFromJSON,
    SafeOutToJSON,
    StructureInFromJSON,
    StructureInToJSON,
} from '../models/index.js';

export interface CompanyApiGetCompanyRegisterRequest {
    stage?: string;
    structure?: string;
    limit?: number;
    offset?: number;
}

export interface CompanyApiGetCompanyReviewRequest {
    limit?: number;
}

export interface CompanyApiPostCompanyRequest {
    beginIn: BeginIn;
}

export interface CompanyApiPostCompanyAdvanceRequest {
    advanceIn: AdvanceIn;
}

export interface CompanyApiPostCompanyEsignCompleteRequest {
    esignCompleteIn: EsignCompleteIn;
}

export interface CompanyApiPostCompanyFoundersRequest {
    foundersIn: FoundersIn;
}

export interface CompanyApiPostCompanyFundraiseDeckRequest {
    body?: Blob;
}

export interface CompanyApiPostCompanyFundraiseRoundRequest {
    roundInput: RoundInput;
}

export interface CompanyApiPostCompanyFundraiseSafeRequest {
    safeIn: SafeIn;
}

export interface CompanyApiPostCompanyImportCaptableRequest {
    importCapTableIn: ImportCapTableIn;
}

export interface CompanyApiPostCompanyImportDocumentsRequest {
    importDocumentsIn: ImportDocumentsIn;
}

export interface CompanyApiPostCompanyKycDecisionRequest {
    decisionIn: DecisionIn;
}

export interface CompanyApiPutCompanyStructureRequest {
    structureIn: StructureIn;
}

/**
 * 
 */
export class CompanyApi extends runtime.BaseAPI {

    /**
     * Get returns the caller org\'s formation and the stages reachable from it, or 404 when the org has not begun one.
     * Get returns the caller org\'s formation and the stages reachable from it, or 404 when the org has not begun one.
     */
    async getCompanyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Get returns the caller org\'s formation and the stages reachable from it, or 404 when the org has not begun one.
     * Get returns the caller org\'s formation and the stages reachable from it, or 404 when the org has not begun one.
     */
    async getCompany(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.getCompanyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the platform\'s whole formation register, newest activity first — every org\'s formation, not the caller\'s. It is a Hanzo platform operation: a caller who is not a platform reviewer gets 403.  Filter by stage and structure, page with limit and offset. An unknown stage is refused with 400 rather than returning a silently empty page.
     * Returns the platform\'s whole formation register, newest activity first — every org\'s formation, not the caller\'s.
     */
    async getCompanyRegisterRaw(requestParameters: CompanyApiGetCompanyRegisterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterPage>> {
        const queryParameters: any = {};

        if (requestParameters['stage'] != null) {
            queryParameters['stage'] = requestParameters['stage'];
        }

        if (requestParameters['structure'] != null) {
            queryParameters['structure'] = requestParameters['structure'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/register`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterPageFromJSON(jsonValue));
    }

    /**
     * Returns the platform\'s whole formation register, newest activity first — every org\'s formation, not the caller\'s. It is a Hanzo platform operation: a caller who is not a platform reviewer gets 403.  Filter by stage and structure, page with limit and offset. An unknown stage is refused with 400 rather than returning a silently empty page.
     * Returns the platform\'s whole formation register, newest activity first — every org\'s formation, not the caller\'s.
     */
    async getCompanyRegister(requestParameters: CompanyApiGetCompanyRegisterRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterPage> {
        const response = await this.getCompanyRegisterRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Counts the platform\'s formations by stage — the register\'s shape in one read, so a queue that is growing is visible as a number rather than inferred by paging the list. A Hanzo platform operation: a caller who is not a platform reviewer gets 403.
     * Counts the platform\'s formations by stage — the register\'s shape in one read, so a queue that is growing is visible as a number rather than inferred by paging the list.
     */
    async getCompanyRegisterSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterCounts>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/register/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterCountsFromJSON(jsonValue));
    }

    /**
     * Counts the platform\'s formations by stage — the register\'s shape in one read, so a queue that is growing is visible as a number rather than inferred by paging the list. A Hanzo platform operation: a caller who is not a platform reviewer gets 403.
     * Counts the platform\'s formations by stage — the register\'s shape in one read, so a queue that is growing is visible as a number rather than inferred by paging the list.
     */
    async getCompanyRegisterSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterCounts> {
        const response = await this.getCompanyRegisterSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports the founders whose KYC is not yet settled, oldest formation first, so the queue drains in the order founders have been waiting. A Hanzo platform operation: a caller who is not a platform reviewer gets 403.  It only says who is waiting; the decision itself is POST /v1/company/kyc/decision.
     * Reports the founders whose KYC is not yet settled, oldest formation first, so the queue drains in the order founders have been waiting.
     */
    async getCompanyReviewRaw(requestParameters: CompanyApiGetCompanyReviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReviewQueue>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/review`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReviewQueueFromJSON(jsonValue));
    }

    /**
     * Reports the founders whose KYC is not yet settled, oldest formation first, so the queue drains in the order founders have been waiting. A Hanzo platform operation: a caller who is not a platform reviewer gets 403.  It only says who is waiting; the decision itself is POST /v1/company/kyc/decision.
     * Reports the founders whose KYC is not yet settled, oldest formation first, so the queue drains in the order founders have been waiting.
     */
    async getCompanyReview(requestParameters: CompanyApiGetCompanyReviewRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReviewQueue> {
        const response = await this.getCompanyReviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Begin starts the org\'s one formation and returns it with the stages reachable from it. It is idempotent: an org that already has a formation gets that one back with 200, while a first call creates it and answers 201.
     * Begin starts the org\'s one formation and returns it with the stages reachable from it.
     */
    async postCompanyRaw(requestParameters: CompanyApiPostCompanyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        if (requestParameters['beginIn'] == null) {
            throw new runtime.RequiredError(
                'beginIn',
                'Required parameter "beginIn" was null or undefined when calling postCompany().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BeginInToJSON(requestParameters['beginIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Begin starts the org\'s one formation and returns it with the stages reachable from it. It is idempotent: an org that already has a formation gets that one back with 200, while a first call creates it and answers 201.
     * Begin starts the org\'s one formation and returns it with the stages reachable from it.
     */
    async postCompany(requestParameters: CompanyApiPostCompanyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Advance runs the ONE guarded transition of the formation machine. It is the only door between stages: the actions populate data, this decides ordering.  An edge the machine does not define answers 409; an edge whose guard is not yet satisfied answers 422 naming what is missing. Reaching the terminal `company` stage also records the incorporation on the canonical cap table, and that must succeed before the transition is persisted.
     * Advance runs the ONE guarded transition of the formation machine.
     */
    async postCompanyAdvanceRaw(requestParameters: CompanyApiPostCompanyAdvanceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        if (requestParameters['advanceIn'] == null) {
            throw new runtime.RequiredError(
                'advanceIn',
                'Required parameter "advanceIn" was null or undefined when calling postCompanyAdvance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/advance`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AdvanceInToJSON(requestParameters['advanceIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Advance runs the ONE guarded transition of the formation machine. It is the only door between stages: the actions populate data, this decides ordering.  An edge the machine does not define answers 409; an edge whose guard is not yet satisfied answers 422 naming what is missing. Reaching the terminal `company` stage also records the incorporation on the canonical cap table, and that must succeed before the transition is persisted.
     * Advance runs the ONE guarded transition of the formation machine.
     */
    async postCompanyAdvance(requestParameters: CompanyApiPostCompanyAdvanceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyAdvanceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Renders the formation documents for the chosen structure and jurisdiction, ingests each into the org\'s data room, and submits the state filing through the filing seam.  With no filing partner wired the filing is recorded honestly as \"manual\" — no filing id is fabricated. Available only at the documents stage.
     * Renders the formation documents for the chosen structure and jurisdiction, ingests each into the org\'s data room, and submits the state filing through the filing seam.
     */
    async postCompanyDocumentsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/documents`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Renders the formation documents for the chosen structure and jurisdiction, ingests each into the org\'s data room, and submits the state filing through the filing seam.  With no filing partner wired the filing is recorded honestly as \"manual\" — no filing id is fabricated. Available only at the documents stage.
     * Renders the formation documents for the chosen structure and jurisdiction, ingests each into the org\'s data room, and submits the state filing through the filing seam.
     */
    async postCompanyDocuments(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyDocumentsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Sends the generated formation documents for signature by every founder and records the provider\'s reference on the formation. Available only at the esign stage.
     * Sends the generated formation documents for signature by every founder and records the provider\'s reference on the formation.
     */
    async postCompanyEsignRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EsignOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/esign`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EsignOutFromJSON(jsonValue));
    }

    /**
     * Sends the generated formation documents for signature by every founder and records the provider\'s reference on the formation. Available only at the esign stage.
     * Sends the generated formation documents for signature by every founder and records the provider\'s reference on the formation.
     */
    async postCompanyEsign(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EsignOut> {
        const response = await this.postCompanyEsignRaw(initOverrides);
        return await response.value();
    }

    /**
     * Records whether the formation documents have been signed. It consults the e-signature provider, which a real provider\'s webhook drives; the signal is idempotent.  An explicit `signed` in the request overrides the provider\'s answer, which is the manual path for the stub provider that never self-completes.
     * Records whether the formation documents have been signed.
     */
    async postCompanyEsignCompleteRaw(requestParameters: CompanyApiPostCompanyEsignCompleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        if (requestParameters['esignCompleteIn'] == null) {
            throw new runtime.RequiredError(
                'esignCompleteIn',
                'Required parameter "esignCompleteIn" was null or undefined when calling postCompanyEsignComplete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/esign/complete`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EsignCompleteInToJSON(requestParameters['esignCompleteIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Records whether the formation documents have been signed. It consults the e-signature provider, which a real provider\'s webhook drives; the signal is idempotent.  An explicit `signed` in the request overrides the provider\'s answer, which is the manual path for the stub provider that never self-completes.
     * Records whether the formation documents have been signed.
     */
    async postCompanyEsignComplete(requestParameters: CompanyApiPostCompanyEsignCompleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyEsignCompleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the formation\'s founders. Each founder needs a name, an email and an equity share in basis points; every founder is (re)set to pending KYC, so a previously settled decision does not survive a change of the list.
     * Replaces the formation\'s founders.
     */
    async postCompanyFoundersRaw(requestParameters: CompanyApiPostCompanyFoundersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        if (requestParameters['foundersIn'] == null) {
            throw new runtime.RequiredError(
                'foundersIn',
                'Required parameter "foundersIn" was null or undefined when calling postCompanyFounders().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/founders`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FoundersInToJSON(requestParameters['foundersIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Replaces the formation\'s founders. Each founder needs a name, an email and an equity share in basis points; every founder is (re)set to pending KYC, so a previously settled decision does not survive a change of the list.
     * Replaces the formation\'s founders.
     */
    async postCompanyFounders(requestParameters: CompanyApiPostCompanyFoundersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyFoundersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stores the request body as a document in the caller org\'s data room and answers with the data room id to reference it by. The deck is RAW BYTES of whatever content type is sent — a PDF, a slide export — not a JSON document: the Content-Type header is carried through to the data room as given, and `?name=` names the document, defaulting to `pitch-deck`.  Scoped to the caller\'s validated org, and only after incorporation: a formation still short of stage `company` is refused 409 and an org that never began one is 404. The route is registered AHEAD of the surface\'s JSON body cap deliberately, so a deck\'s size ceiling is the edge\'s rather than the cap meant for small structured records. An empty body is 400; a data room that will not take the bytes is 502.
     * Share a pitch deck in the org\'s data room
     */
    async postCompanyFundraiseDeckRaw(requestParameters: CompanyApiPostCompanyFundraiseDeckRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeckOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/fundraise/deck`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeckOutFromJSON(jsonValue));
    }

    /**
     * Stores the request body as a document in the caller org\'s data room and answers with the data room id to reference it by. The deck is RAW BYTES of whatever content type is sent — a PDF, a slide export — not a JSON document: the Content-Type header is carried through to the data room as given, and `?name=` names the document, defaulting to `pitch-deck`.  Scoped to the caller\'s validated org, and only after incorporation: a formation still short of stage `company` is refused 409 and an org that never began one is 404. The route is registered AHEAD of the surface\'s JSON body cap deliberately, so a deck\'s size ceiling is the edge\'s rather than the cap meant for small structured records. An empty body is 400; a data room that will not take the bytes is 502.
     * Share a pitch deck in the org\'s data room
     */
    async postCompanyFundraiseDeck(requestParameters: CompanyApiPostCompanyFundraiseDeckRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeckOut> {
        const response = await this.postCompanyFundraiseDeckRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a fundraising round on the org\'s canonical cap table. Available only after incorporation (stage company); roundType defaults to PRICED.
     * Records a fundraising round on the org\'s canonical cap table.
     */
    async postCompanyFundraiseRoundRaw(requestParameters: CompanyApiPostCompanyFundraiseRoundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RoundOut>> {
        if (requestParameters['roundInput'] == null) {
            throw new runtime.RequiredError(
                'roundInput',
                'Required parameter "roundInput" was null or undefined when calling postCompanyFundraiseRound().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/fundraise/round`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RoundInputToJSON(requestParameters['roundInput']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RoundOutFromJSON(jsonValue));
    }

    /**
     * Records a fundraising round on the org\'s canonical cap table. Available only after incorporation (stage company); roundType defaults to PRICED.
     * Records a fundraising round on the org\'s canonical cap table.
     */
    async postCompanyFundraiseRound(requestParameters: CompanyApiPostCompanyFundraiseRoundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RoundOut> {
        const response = await this.postCompanyFundraiseRoundRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Raises an e-signature request over documents already in the org\'s data room — a SAFE, a convertible note, or any other fundraising paper. Available only after incorporation (stage company).
     * Raises an e-signature request over documents already in the org\'s data room — a SAFE, a convertible note, or any other fundraising paper.
     */
    async postCompanyFundraiseSafeRaw(requestParameters: CompanyApiPostCompanyFundraiseSafeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SafeOut>> {
        if (requestParameters['safeIn'] == null) {
            throw new runtime.RequiredError(
                'safeIn',
                'Required parameter "safeIn" was null or undefined when calling postCompanyFundraiseSafe().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/fundraise/safe`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SafeInToJSON(requestParameters['safeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SafeOutFromJSON(jsonValue));
    }

    /**
     * Raises an e-signature request over documents already in the org\'s data room — a SAFE, a convertible note, or any other fundraising paper. Available only after incorporation (stage company).
     * Raises an e-signature request over documents already in the org\'s data room — a SAFE, a convertible note, or any other fundraising paper.
     */
    async postCompanyFundraiseSafe(requestParameters: CompanyApiPostCompanyFundraiseSafeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SafeOut> {
        const response = await this.postCompanyFundraiseSafeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Seeds the canonical cap table with the founding allocation (stakeholders, a common share class, issued shares) and anchors the deterministic equity-genesis root on-chain.  It is idempotent: once a root is recorded the cap table is NOT re-seeded, which would double-issue founder share certificates. The root is persisted even when the on-chain submit fails, because the root is the tamper-evident witness and must not be recomputed on retry. Available only at the genesis stage.
     * Seeds the canonical cap table with the founding allocation (stakeholders, a common share class, issued shares) and anchors the deterministic equity-genesis root on-chain.
     */
    async postCompanyGenesisRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/genesis`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Seeds the canonical cap table with the founding allocation (stakeholders, a common share class, issued shares) and anchors the deterministic equity-genesis root on-chain.  It is idempotent: once a root is recorded the cap table is NOT re-seeded, which would double-issue founder share certificates. The root is persisted even when the on-chain submit fails, because the root is the tamper-evident witness and must not be recomputed on retry. Available only at the genesis stage.
     * Seeds the canonical cap table with the founding allocation (stakeholders, a common share class, issued shares) and anchors the deterministic equity-genesis root on-chain.
     */
    async postCompanyGenesis(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyGenesisRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reads an existing company\'s cap table from a Google Sheet and adds its stakeholders to the canonical cap table.  The first row is a header and columns are matched by name (case-insensitive): name and email are required, type/relationship/institution optional. A sheet without name and email columns, or with no usable data rows, is refused with 400. Available only at the import stage.
     * Reads an existing company\'s cap table from a Google Sheet and adds its stakeholders to the canonical cap table.
     */
    async postCompanyImportCaptableRaw(requestParameters: CompanyApiPostCompanyImportCaptableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ImportCapTableOut>> {
        if (requestParameters['importCapTableIn'] == null) {
            throw new runtime.RequiredError(
                'importCapTableIn',
                'Required parameter "importCapTableIn" was null or undefined when calling postCompanyImportCaptable().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/import/captable`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ImportCapTableInToJSON(requestParameters['importCapTableIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ImportCapTableOutFromJSON(jsonValue));
    }

    /**
     * Reads an existing company\'s cap table from a Google Sheet and adds its stakeholders to the canonical cap table.  The first row is a header and columns are matched by name (case-insensitive): name and email are required, type/relationship/institution optional. A sheet without name and email columns, or with no usable data rows, is refused with 400. Available only at the import stage.
     * Reads an existing company\'s cap table from a Google Sheet and adds its stakeholders to the canonical cap table.
     */
    async postCompanyImportCaptable(requestParameters: CompanyApiPostCompanyImportCaptableRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ImportCapTableOut> {
        const response = await this.postCompanyImportCaptableRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Ingests an existing company\'s corporate documents from a Google Drive folder into the org\'s data room. The import is shallow — sub-folders are skipped, not walked — and available only at the import stage.
     * Ingests an existing company\'s corporate documents from a Google Drive folder into the org\'s data room.
     */
    async postCompanyImportDocumentsRaw(requestParameters: CompanyApiPostCompanyImportDocumentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ImportDocumentsOut>> {
        if (requestParameters['importDocumentsIn'] == null) {
            throw new runtime.RequiredError(
                'importDocumentsIn',
                'Required parameter "importDocumentsIn" was null or undefined when calling postCompanyImportDocuments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/import/documents`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ImportDocumentsInToJSON(requestParameters['importDocumentsIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ImportDocumentsOutFromJSON(jsonValue));
    }

    /**
     * Ingests an existing company\'s corporate documents from a Google Drive folder into the org\'s data room. The import is shallow — sub-folders are skipped, not walked — and available only at the import stage.
     * Ingests an existing company\'s corporate documents from a Google Drive folder into the org\'s data room.
     */
    async postCompanyImportDocuments(requestParameters: CompanyApiPostCompanyImportDocumentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ImportDocumentsOut> {
        const response = await this.postCompanyImportDocumentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * StartKYC opens an identity-verification session for every founder with the wired provider and records each session\'s reference on the formation.  A start is never a decision: any terminal status the provider reports at inquiry time is clamped back to pending, so the payment gate can never open here. A terminal status arrives only from POST /v1/company/kyc/refresh (the provider) or POST /v1/company/kyc/decision (a Hanzo platform reviewer).
     * StartKYC opens an identity-verification session for every founder with the wired provider and records each session\'s reference on the formation.
     */
    async postCompanyKycRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KycStartOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/kyc`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KycStartOutFromJSON(jsonValue));
    }

    /**
     * StartKYC opens an identity-verification session for every founder with the wired provider and records each session\'s reference on the formation.  A start is never a decision: any terminal status the provider reports at inquiry time is clamped back to pending, so the payment gate can never open here. A terminal status arrives only from POST /v1/company/kyc/refresh (the provider) or POST /v1/company/kyc/decision (a Hanzo platform reviewer).
     * StartKYC opens an identity-verification session for every founder with the wired provider and records each session\'s reference on the formation.
     */
    async postCompanyKyc(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KycStartOut> {
        const response = await this.postCompanyKycRaw(initOverrides);
        return await response.value();
    }

    /**
     * DecideKYC records a privileged reviewer\'s MANUAL decision on a founder\'s KYC — the human-in-the-loop path, and the ONLY route to a pass when no real provider is wired. It produces a DISTINCT reviewer_confirmed, never a provider \"verified\".  Because Hanzo forms the entity and carries the formation KYC/AML obligation, the reviewer is a HANZO platform reviewer (SuperAdmin), and the decision is ATTRIBUTED to them.
     * DecideKYC records a privileged reviewer\'s MANUAL decision on a founder\'s KYC — the human-in-the-loop path, and the ONLY route to a pass when no real provider is wired.
     */
    async postCompanyKycDecisionRaw(requestParameters: CompanyApiPostCompanyKycDecisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        if (requestParameters['decisionIn'] == null) {
            throw new runtime.RequiredError(
                'decisionIn',
                'Required parameter "decisionIn" was null or undefined when calling postCompanyKycDecision().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/kyc/decision`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DecisionInToJSON(requestParameters['decisionIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * DecideKYC records a privileged reviewer\'s MANUAL decision on a founder\'s KYC — the human-in-the-loop path, and the ONLY route to a pass when no real provider is wired. It produces a DISTINCT reviewer_confirmed, never a provider \"verified\".  Because Hanzo forms the entity and carries the formation KYC/AML obligation, the reviewer is a HANZO platform reviewer (SuperAdmin), and the decision is ATTRIBUTED to them.
     * DecideKYC records a privileged reviewer\'s MANUAL decision on a founder\'s KYC — the human-in-the-loop path, and the ONLY route to a pass when no real provider is wired.
     */
    async postCompanyKycDecision(requestParameters: CompanyApiPostCompanyKycDecisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyKycDecisionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * RefreshKYC reconciles each pending founder\'s KYC with the WIRED provider — the PULL path to a provider-reported terminal status. For the manual provider the check stays pending; for a real provider it reflects the settled decision, ATTRIBUTED to the provider.  It NEVER trusts a client-asserted status — the status comes from the provider seam — so a client cannot force a pass here, and an already-passing founder (e.g. a reviewer confirmation) is left untouched.
     * RefreshKYC reconciles each pending founder\'s KYC with the WIRED provider — the PULL path to a provider-reported terminal status.
     */
    async postCompanyKycRefreshRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KycRefreshOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/kyc/refresh`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KycRefreshOutFromJSON(jsonValue));
    }

    /**
     * RefreshKYC reconciles each pending founder\'s KYC with the WIRED provider — the PULL path to a provider-reported terminal status. For the manual provider the check stays pending; for a real provider it reflects the settled decision, ATTRIBUTED to the provider.  It NEVER trusts a client-asserted status — the status comes from the provider seam — so a client cannot force a pass here, and an already-passing founder (e.g. a reviewer confirmation) is left untouched.
     * RefreshKYC reconciles each pending founder\'s KYC with the WIRED provider — the PULL path to a provider-reported terminal status.
     */
    async postCompanyKycRefresh(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KycRefreshOut> {
        const response = await this.postCompanyKycRefreshRaw(initOverrides);
        return await response.value();
    }

    /**
     * Bills the caller\'s own org the one-time Hanzo Company formation fee — $999 unless the deployment sets another — and answers with the formation record carrying its paid flag and the charge reference. Takes no body: the org is the validated tenant and the amount is the platform\'s, never the caller\'s to assert.  IDEMPOTENT on the formation rather than on the request: an already-paid formation answers 200 with the same record and is not charged again, so a retry or a double-clicked button costs nothing. Available only at the `payment` stage (409 anywhere else) and only for an org that has begun a formation (404 otherwise).  A refused charge answers the fleet-wide billing contract, not a formation error — 402 when the org cannot pay, 503 when metering is unavailable — which is exactly why this route is not a typed op.
     * Charge the one-time formation fee and mark the formation paid
     */
    async postCompanyPaymentRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/payment`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Bills the caller\'s own org the one-time Hanzo Company formation fee — $999 unless the deployment sets another — and answers with the formation record carrying its paid flag and the charge reference. Takes no body: the org is the validated tenant and the amount is the platform\'s, never the caller\'s to assert.  IDEMPOTENT on the formation rather than on the request: an already-paid formation answers 200 with the same record and is not charged again, so a retry or a double-clicked button costs nothing. Available only at the `payment` stage (409 anywhere else) and only for an org that has begun a formation (404 otherwise).  A refused charge answers the fleet-wide billing contract, not a formation error — 402 when the org cannot pay, 503 when metering is unavailable — which is exactly why this route is not a typed op.
     * Charge the one-time formation fee and mark the formation paid
     */
    async postCompanyPayment(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanyPaymentRaw(initOverrides);
        return await response.value();
    }

    /**
     * Skip marks the org as already incorporated and moves it onto the import path, so an existing company brings its documents and cap table in instead of forming a new entity. Available only at the structure stage.
     * Skip marks the org as already incorporated and moves it onto the import path, so an existing company brings its documents and cap table in instead of forming a new entity.
     */
    async postCompanySkipRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/skip`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Skip marks the org as already incorporated and moves it onto the import path, so an existing company brings its documents and cap table in instead of forming a new entity. Available only at the structure stage.
     * Skip marks the org as already incorporated and moves it onto the import path, so an existing company brings its documents and cap table in instead of forming a new entity.
     */
    async postCompanySkip(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.postCompanySkipRaw(initOverrides);
        return await response.value();
    }

    /**
     * Records the entity kind, the state of formation and the proposed name. Available only at the structure stage; an unknown structure or jurisdiction, or an empty name, is refused with 400.
     * Records the entity kind, the state of formation and the proposed name.
     */
    async putCompanyStructureRaw(requestParameters: CompanyApiPutCompanyStructureRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormationView>> {
        if (requestParameters['structureIn'] == null) {
            throw new runtime.RequiredError(
                'structureIn',
                'Required parameter "structureIn" was null or undefined when calling putCompanyStructure().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/company/structure`;

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: StructureInToJSON(requestParameters['structureIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormationViewFromJSON(jsonValue));
    }

    /**
     * Records the entity kind, the state of formation and the proposed name. Available only at the structure stage; an unknown structure or jurisdiction, or an empty name, is refused with 400.
     * Records the entity kind, the state of formation and the proposed name.
     */
    async putCompanyStructure(requestParameters: CompanyApiPutCompanyStructureRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormationView> {
        const response = await this.putCompanyStructureRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
