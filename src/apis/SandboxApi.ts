/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Blob,
  EndIn,
  ExecRequest,
  ExecResult,
  LeaseIn,
  Leased,
  PathIn,
  Ran,
  RunIn,
  Sandbox,
  SandboxList,
  StopIn,
  Stopped,
  TicketGrant,
  WriteIn,
  Wrote,
} from '../models/index.js';
import {
    BlobFromJSON,
    BlobToJSON,
    EndInFromJSON,
    EndInToJSON,
    ExecRequestFromJSON,
    ExecRequestToJSON,
    ExecResultFromJSON,
    ExecResultToJSON,
    LeaseInFromJSON,
    LeaseInToJSON,
    LeasedFromJSON,
    LeasedToJSON,
    PathInFromJSON,
    PathInToJSON,
    RanFromJSON,
    RanToJSON,
    RunInFromJSON,
    RunInToJSON,
    SandboxFromJSON,
    SandboxToJSON,
    SandboxListFromJSON,
    SandboxListToJSON,
    StopInFromJSON,
    StopInToJSON,
    StoppedFromJSON,
    StoppedToJSON,
    TicketGrantFromJSON,
    TicketGrantToJSON,
    WriteInFromJSON,
    WriteInToJSON,
    WroteFromJSON,
    WroteToJSON,
} from '../models/index.js';

export interface SandboxApiDeleteSandboxByIdRequest {
    id: string;
    purge?: string;
}

export interface SandboxApiEndSandboxRequest {
    endIn: EndIn;
}

export interface SandboxApiGetSandboxRequest {
    project?: string;
    status?: string;
}

export interface SandboxApiGetSandboxByIdRequest {
    id: string;
}

export interface SandboxApiGetSandboxByIdFsRequest {
    id: string;
}

export interface SandboxApiGetSandboxByIdScreenRequest {
    id: string;
}

export interface SandboxApiGetSandboxByIdScreenWsRequest {
    id: string;
}

export interface SandboxApiGetSandboxByIdTerminalRequest {
    id: string;
}

export interface SandboxApiGetSandboxByIdTerminalWsRequest {
    id: string;
}

export interface SandboxApiLeaseSandboxRequest {
    leaseIn: LeaseIn;
}

export interface SandboxApiPostSandboxRequest {
    leaseIn: LeaseIn;
}

export interface SandboxApiPostSandboxByIdExecRequest {
    id: string;
    execRequest: ExecRequest;
}

export interface SandboxApiPostSandboxByIdFsRequest {
    id: string;
}

export interface SandboxApiPostSandboxByIdScreenTicketRequest {
    id: string;
}

export interface SandboxApiPostSandboxByIdTerminalTicketRequest {
    id: string;
}

export interface SandboxApiReadSandboxFileRequest {
    pathIn: PathIn;
}

export interface SandboxApiRunInSandboxRequest {
    runIn: RunIn;
}

export interface SandboxApiStopRunRequest {
    stopIn: StopIn;
}

export interface SandboxApiWriteSandboxFileRequest {
    writeIn: WriteIn;
}

/**
 * 
 */
export class SandboxApi extends runtime.BaseAPI {

    /**
     * Ends a sandbox and releases the compute behind it. Answers 204.  ENDING IS NOT STOPPING. This releases the resource: the pod goes and anything only inside it goes with it. To end what a sandbox is RUNNING while keeping the sandbox — the checkout, the logs, the half-written file — the verb is POST /v1/sandbox/stop.  `?purge=1` additionally removes the record, so the sandbox stops being listed at all rather than being listed as ended.
     * Ends a sandbox and releases the compute behind it.
     */
    async deleteSandboxByIdRaw(requestParameters: SandboxApiDeleteSandboxByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteSandboxById().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['purge'] != null) {
            queryParameters['purge'] = requestParameters['purge'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ends a sandbox and releases the compute behind it. Answers 204.  ENDING IS NOT STOPPING. This releases the resource: the pod goes and anything only inside it goes with it. To end what a sandbox is RUNNING while keeping the sandbox — the checkout, the logs, the half-written file — the verb is POST /v1/sandbox/stop.  `?purge=1` additionally removes the record, so the sandbox stops being listed at all rather than being listed as ended.
     * Ends a sandbox and releases the compute behind it.
     */
    async deleteSandboxById(requestParameters: SandboxApiDeleteSandboxByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSandboxByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
     * End a sandbox and release it
     */
    async endSandboxRaw(requestParameters: SandboxApiEndSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['endIn'] == null) {
            throw new runtime.RequiredError(
                'endIn',
                'Required parameter "endIn" was null or undefined when calling endSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/end`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EndInToJSON(requestParameters['endIn']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
     * End a sandbox and release it
     */
    async endSandbox(requestParameters: SandboxApiEndSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.endSandboxRaw(requestParameters, initOverrides);
    }

    /**
     * Lists the caller org\'s sandboxes, newest first.  `?project=` and `?status=` narrow it. Only the caller\'s org\'s: the store is keyed on the validated org, so another tenant\'s sandbox is not something this operation can return.
     * Lists the caller org\'s sandboxes, newest first.
     */
    async getSandboxRaw(requestParameters: SandboxApiGetSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SandboxList>> {
        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SandboxListFromJSON(jsonValue));
    }

    /**
     * Lists the caller org\'s sandboxes, newest first.  `?project=` and `?status=` narrow it. Only the caller\'s org\'s: the store is keyed on the validated org, so another tenant\'s sandbox is not something this operation can return.
     * Lists the caller org\'s sandboxes, newest first.
     */
    async getSandbox(requestParameters: SandboxApiGetSandboxRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SandboxList> {
        const response = await this.getSandboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.  An id the caller\'s org does not hold is the same 404 an unknown id gives — the store is keyed on the org, so a cross-tenant id simply is not there.
     * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.
     */
    async getSandboxByIdRaw(requestParameters: SandboxApiGetSandboxByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Sandbox>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SandboxFromJSON(jsonValue));
    }

    /**
     * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.  An id the caller\'s org does not hold is the same 404 an unknown id gives — the store is keyed on the org, so a cross-tenant id simply is not there.
     * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.
     */
    async getSandboxById(requestParameters: SandboxApiGetSandboxByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Sandbox> {
        const response = await this.getSandboxByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
     * Read a file, or list a directory
     */
    async getSandboxByIdFsRaw(requestParameters: SandboxApiGetSandboxByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxByIdFs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/fs`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
     * Read a file, or list a directory
     */
    async getSandboxByIdFs(requestParameters: SandboxApiGetSandboxByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxByIdFsRaw(requestParameters, initOverrides);
    }

    /**
     * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
     * The screen, as a page
     */
    async getSandboxByIdScreenRaw(requestParameters: SandboxApiGetSandboxByIdScreenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxByIdScreen().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/screen`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
     * The screen, as a page
     */
    async getSandboxByIdScreen(requestParameters: SandboxApiGetSandboxByIdScreenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxByIdScreenRaw(requestParameters, initOverrides);
    }

    /**
     * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
     * The screen, as a socket
     */
    async getSandboxByIdScreenWsRaw(requestParameters: SandboxApiGetSandboxByIdScreenWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxByIdScreenWs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/screen/ws`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
     * The screen, as a socket
     */
    async getSandboxByIdScreenWs(requestParameters: SandboxApiGetSandboxByIdScreenWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxByIdScreenWsRaw(requestParameters, initOverrides);
    }

    /**
     * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
     * The terminal, as a page
     */
    async getSandboxByIdTerminalRaw(requestParameters: SandboxApiGetSandboxByIdTerminalRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxByIdTerminal().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/terminal`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
     * The terminal, as a page
     */
    async getSandboxByIdTerminal(requestParameters: SandboxApiGetSandboxByIdTerminalRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxByIdTerminalRaw(requestParameters, initOverrides);
    }

    /**
     * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
     * The terminal, as a socket
     */
    async getSandboxByIdTerminalWsRaw(requestParameters: SandboxApiGetSandboxByIdTerminalWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSandboxByIdTerminalWs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/terminal/ws`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
     * The terminal, as a socket
     */
    async getSandboxByIdTerminalWs(requestParameters: SandboxApiGetSandboxByIdTerminalWsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSandboxByIdTerminalWsRaw(requestParameters, initOverrides);
    }

    /**
     * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
     * Lease a sandbox — a real computer — or resume one you hold
     */
    async leaseSandboxRaw(requestParameters: SandboxApiLeaseSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Leased>> {
        if (requestParameters['leaseIn'] == null) {
            throw new runtime.RequiredError(
                'leaseIn',
                'Required parameter "leaseIn" was null or undefined when calling leaseSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/lease`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LeaseInToJSON(requestParameters['leaseIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LeasedFromJSON(jsonValue));
    }

    /**
     * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
     * Lease a sandbox — a real computer — or resume one you hold
     */
    async leaseSandbox(requestParameters: SandboxApiLeaseSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Leased> {
        const response = await this.leaseSandboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Leases a sandbox — a real computer — for the caller\'s org.  The class decides what it is for and therefore its image, working directory and isolation. A dev or desktop sandbox is SINGLE-ATTACH per project, so asking twice for one project resumes the one that exists rather than paying for a second; an exec sandbox carries no project and is bounded per org instead, refused 429 past the ceiling because the caller\'s correct response is to wait.  Answers 201 with the sandbox as leased, which names the runtime it GOT — not the one that was asked for.
     * Leases a sandbox — a real computer — for the caller\'s org.
     */
    async postSandboxRaw(requestParameters: SandboxApiPostSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Sandbox>> {
        if (requestParameters['leaseIn'] == null) {
            throw new runtime.RequiredError(
                'leaseIn',
                'Required parameter "leaseIn" was null or undefined when calling postSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LeaseInToJSON(requestParameters['leaseIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SandboxFromJSON(jsonValue));
    }

    /**
     * Leases a sandbox — a real computer — for the caller\'s org.  The class decides what it is for and therefore its image, working directory and isolation. A dev or desktop sandbox is SINGLE-ATTACH per project, so asking twice for one project resumes the one that exists rather than paying for a second; an exec sandbox carries no project and is bounded per org instead, refused 429 past the ceiling because the caller\'s correct response is to wait.  Answers 201 with the sandbox as leased, which names the runtime it GOT — not the one that was asked for.
     * Leases a sandbox — a real computer — for the caller\'s org.
     */
    async postSandbox(requestParameters: SandboxApiPostSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Sandbox> {
        const response = await this.postSandboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.  Send `argv` — an argument vector cannot be word-split by accident — or `command` for a shell line, which is the only input here that ever reaches a shell. A non-zero exit is a SUCCESSFUL call carrying a failed command: the status is 200 and the exit code is in the answer, because \"the command failed\" and \"the call failed\" are different facts.
     * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.
     */
    async postSandboxByIdExecRaw(requestParameters: SandboxApiPostSandboxByIdExecRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExecResult>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxByIdExec().'
            );
        }

        if (requestParameters['execRequest'] == null) {
            throw new runtime.RequiredError(
                'execRequest',
                'Required parameter "execRequest" was null or undefined when calling postSandboxByIdExec().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/exec`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ExecRequestToJSON(requestParameters['execRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExecResultFromJSON(jsonValue));
    }

    /**
     * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.  Send `argv` — an argument vector cannot be word-split by accident — or `command` for a shell line, which is the only input here that ever reaches a shell. A non-zero exit is a SUCCESSFUL call carrying a failed command: the status is 200 and the exit code is in the answer, because \"the command failed\" and \"the call failed\" are different facts.
     * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.
     */
    async postSandboxByIdExec(requestParameters: SandboxApiPostSandboxByIdExecRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExecResult> {
        const response = await this.postSandboxByIdExecRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
     * Write a file
     */
    async postSandboxByIdFsRaw(requestParameters: SandboxApiPostSandboxByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxByIdFs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/fs`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
     * Write a file
     */
    async postSandboxByIdFs(requestParameters: SandboxApiPostSandboxByIdFsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSandboxByIdFsRaw(requestParameters, initOverrides);
    }

    /**
     * Mints a short-lived grant to open the screen of a desktop sandbox. Same properties as the terminal ticket, for the other endpoint.
     * Mints a short-lived grant to open the screen of a desktop sandbox.
     */
    async postSandboxByIdScreenTicketRaw(requestParameters: SandboxApiPostSandboxByIdScreenTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TicketGrant>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxByIdScreenTicket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/screen/ticket`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TicketGrantFromJSON(jsonValue));
    }

    /**
     * Mints a short-lived grant to open the screen of a desktop sandbox. Same properties as the terminal ticket, for the other endpoint.
     * Mints a short-lived grant to open the screen of a desktop sandbox.
     */
    async postSandboxByIdScreenTicket(requestParameters: SandboxApiPostSandboxByIdScreenTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TicketGrant> {
        const response = await this.postSandboxByIdScreenTicketRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Mints a short-lived grant to open a terminal on a sandbox.  The ticket travels in the query string of the URL it answers with, because a browser cannot set an Authorization header on a WebSocket handshake. It is single-purpose and short-lived for exactly that reason. A sandbox that is not running is 409 rather than a ticket that cannot be used.
     * Mints a short-lived grant to open a terminal on a sandbox.
     */
    async postSandboxByIdTerminalTicketRaw(requestParameters: SandboxApiPostSandboxByIdTerminalTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TicketGrant>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSandboxByIdTerminalTicket().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/{id}/terminal/ticket`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TicketGrantFromJSON(jsonValue));
    }

    /**
     * Mints a short-lived grant to open a terminal on a sandbox.  The ticket travels in the query string of the URL it answers with, because a browser cannot set an Authorization header on a WebSocket handshake. It is single-purpose and short-lived for exactly that reason. A sandbox that is not running is 409 rather than a ticket that cannot be used.
     * Mints a short-lived grant to open a terminal on a sandbox.
     */
    async postSandboxByIdTerminalTicket(requestParameters: SandboxApiPostSandboxByIdTerminalTicketRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TicketGrant> {
        const response = await this.postSandboxByIdTerminalTicketRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
     * Read a file from a sandbox you hold
     */
    async readSandboxFileRaw(requestParameters: SandboxApiReadSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        if (requestParameters['pathIn'] == null) {
            throw new runtime.RequiredError(
                'pathIn',
                'Required parameter "pathIn" was null or undefined when calling readSandboxFile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/read`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PathInToJSON(requestParameters['pathIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BlobFromJSON(jsonValue));
    }

    /**
     * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
     * Read a file from a sandbox you hold
     */
    async readSandboxFile(requestParameters: SandboxApiReadSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.readSandboxFileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
     * Run a command in a sandbox you hold and read its output
     */
    async runInSandboxRaw(requestParameters: SandboxApiRunInSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Ran>> {
        if (requestParameters['runIn'] == null) {
            throw new runtime.RequiredError(
                'runIn',
                'Required parameter "runIn" was null or undefined when calling runInSandbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/run`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RunInToJSON(requestParameters['runIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RanFromJSON(jsonValue));
    }

    /**
     * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
     * Run a command in a sandbox you hold and read its output
     */
    async runInSandbox(requestParameters: SandboxApiRunInSandboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Ran> {
        const response = await this.runInSandboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
     * Stop what a sandbox is running, and keep the sandbox
     */
    async stopRunRaw(requestParameters: SandboxApiStopRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Stopped>> {
        if (requestParameters['stopIn'] == null) {
            throw new runtime.RequiredError(
                'stopIn',
                'Required parameter "stopIn" was null or undefined when calling stopRun().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/stop`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StopInToJSON(requestParameters['stopIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StoppedFromJSON(jsonValue));
    }

    /**
     * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
     * Stop what a sandbox is running, and keep the sandbox
     */
    async stopRun(requestParameters: SandboxApiStopRunRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Stopped> {
        const response = await this.stopRunRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
     * Write a file into a sandbox you hold
     */
    async writeSandboxFileRaw(requestParameters: SandboxApiWriteSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Wrote>> {
        if (requestParameters['writeIn'] == null) {
            throw new runtime.RequiredError(
                'writeIn',
                'Required parameter "writeIn" was null or undefined when calling writeSandboxFile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sandbox/write`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WriteInToJSON(requestParameters['writeIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WroteFromJSON(jsonValue));
    }

    /**
     * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
     * Write a file into a sandbox you hold
     */
    async writeSandboxFile(requestParameters: SandboxApiWriteSandboxFileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Wrote> {
        const response = await this.writeSandboxFileRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
