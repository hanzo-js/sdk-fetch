/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  EntitlementsView,
  MutateReq,
  OnboardReq,
  OnboardResp,
} from '../models/index.js';
import {
    EntitlementsViewFromJSON,
    EntitlementsViewToJSON,
    MutateReqFromJSON,
    MutateReqToJSON,
    OnboardReqFromJSON,
    OnboardReqToJSON,
    OnboardRespFromJSON,
    OnboardRespToJSON,
} from '../models/index.js';

export interface OrgsApiGetOrgsByOrgEntitlementsRequest {
    org: string;
}

export interface OrgsApiPostOrgsRequest {
    onboardReq: OnboardReq;
}

export interface OrgsApiPostOrgsByOrgEntitlementsRequest {
    org: string;
    mutateReq: MutateReq;
}

/**
 * 
 */
export class OrgsApi extends runtime.BaseAPI {

    /**
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show. It is distinct from what the org\'s plan ENTITLES it to (that is GET /v1/entitlements, resolved from commerce).  A caller may only read its OWN org\'s row; a platform super admin may read any.
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show.
     */
    async getOrgsByOrgEntitlementsRaw(requestParameters: OrgsApiGetOrgsByOrgEntitlementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntitlementsView>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling getOrgsByOrgEntitlements().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/orgs/{org}/entitlements`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntitlementsViewFromJSON(jsonValue));
    }

    /**
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show. It is distinct from what the org\'s plan ENTITLES it to (that is GET /v1/entitlements, resolved from commerce).  A caller may only read its OWN org\'s row; a platform super admin may read any.
     * Get lists the products an org has ENABLED — its own intent, which the console\'s paid-product sidebar reads to decide what to show.
     */
    async getOrgsByOrgEntitlements(requestParameters: OrgsApiGetOrgsByOrgEntitlementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntitlementsView> {
        const response = await this.getOrgsByOrgEntitlementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no home org): create + MOVE the user in as admin, so their next     JWT carries the new owner and the cloud scopes everything to it. This is the     path a fresh OAuth sign-up takes, from the sign-up application\'s org.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
     * Onboard creates the caller\'s organization.
     */
    async postOrgsRaw(requestParameters: OrgsApiPostOrgsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OnboardResp>> {
        if (requestParameters['onboardReq'] == null) {
            throw new runtime.RequiredError(
                'onboardReq',
                'Required parameter "onboardReq" was null or undefined when calling postOrgs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/orgs`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OnboardReqToJSON(requestParameters['onboardReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OnboardRespFromJSON(jsonValue));
    }

    /**
     * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no home org): create + MOVE the user in as admin, so their next     JWT carries the new owner and the cloud scopes everything to it. This is the     path a fresh OAuth sign-up takes, from the sign-up application\'s org.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
     * Onboard creates the caller\'s organization.
     */
    async postOrgs(requestParameters: OrgsApiPostOrgsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OnboardResp> {
        const response = await this.postOrgsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Post turns products on or off for an org and returns the enabled set afterwards.  A product may only be ENABLED if the org\'s plan already ENTITLES it, so enabling never spends new money — a product the plan does not grant answers 402 and the console routes that to an upgrade prompt. DISABLING is never gated. A platform super admin bypasses the plan check (operator comp/grant) and may target any org; everyone else may only change their own. Commerce unreachable is a 503, never an implicit yes.
     * Post turns products on or off for an org and returns the enabled set afterwards.
     */
    async postOrgsByOrgEntitlementsRaw(requestParameters: OrgsApiPostOrgsByOrgEntitlementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntitlementsView>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling postOrgsByOrgEntitlements().'
            );
        }

        if (requestParameters['mutateReq'] == null) {
            throw new runtime.RequiredError(
                'mutateReq',
                'Required parameter "mutateReq" was null or undefined when calling postOrgsByOrgEntitlements().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/orgs/{org}/entitlements`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MutateReqToJSON(requestParameters['mutateReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntitlementsViewFromJSON(jsonValue));
    }

    /**
     * Post turns products on or off for an org and returns the enabled set afterwards.  A product may only be ENABLED if the org\'s plan already ENTITLES it, so enabling never spends new money — a product the plan does not grant answers 402 and the console routes that to an upgrade prompt. DISABLING is never gated. A platform super admin bypasses the plan check (operator comp/grant) and may target any org; everyone else may only change their own. Commerce unreachable is a 503, never an implicit yes.
     * Post turns products on or off for an org and returns the enabled set afterwards.
     */
    async postOrgsByOrgEntitlements(requestParameters: OrgsApiPostOrgsByOrgEntitlementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntitlementsView> {
        const response = await this.postOrgsByOrgEntitlementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
