/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  DocumentPage,
  DocumentReply,
  FilingPage,
  FilingReply,
  FilingRequest,
  GenerateRequest,
  LegalHealth,
  SignReply,
  SignRequest,
  TemplateCatalog,
  TemplateOverride,
  TemplateReply,
} from '../models/index.js';
import {
    DocumentPageFromJSON,
    DocumentPageToJSON,
    DocumentReplyFromJSON,
    DocumentReplyToJSON,
    FilingPageFromJSON,
    FilingPageToJSON,
    FilingReplyFromJSON,
    FilingReplyToJSON,
    FilingRequestFromJSON,
    FilingRequestToJSON,
    GenerateRequestFromJSON,
    GenerateRequestToJSON,
    LegalHealthFromJSON,
    LegalHealthToJSON,
    SignReplyFromJSON,
    SignReplyToJSON,
    SignRequestFromJSON,
    SignRequestToJSON,
    TemplateCatalogFromJSON,
    TemplateCatalogToJSON,
    TemplateOverrideFromJSON,
    TemplateOverrideToJSON,
    TemplateReplyFromJSON,
    TemplateReplyToJSON,
} from '../models/index.js';

export interface LegalApiGetLegalDocumentsRequest {
    limit?: number;
}

export interface LegalApiGetLegalDocumentsByIdRequest {
    id: string;
}

export interface LegalApiGetLegalFilingsRequest {
    limit?: number;
}

export interface LegalApiGetLegalTemplatesByIdRequest {
    id: string;
}

export interface LegalApiPostLegalDocumentsRequest {
    generateRequest: GenerateRequest;
}

export interface LegalApiPostLegalDocumentsByIdSignRequest {
    id: string;
    signRequest: SignRequest;
}

export interface LegalApiPostLegalDocumentsByIdSignCompleteRequest {
    id: string;
}

export interface LegalApiPostLegalFilingsRequest {
    filingRequest: FilingRequest;
}

export interface LegalApiPutLegalTemplatesByIdRequest {
    id: string;
    templateOverride: TemplateOverride;
}

/**
 * 
 */
export class LegalApi extends runtime.BaseAPI {

    /**
     * Returns the org\'s generated documents, newest first, WITHOUT their rendered content — fetch one document to read its body.  The response is marked no-store: these records name the counterparties an org is contracting with, and must not sit in a shared cache.
     * Returns the org\'s generated documents, newest first, WITHOUT their rendered content — fetch one document to read its body.
     */
    async getLegalDocumentsRaw(requestParameters: LegalApiGetLegalDocumentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocumentPage>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/documents`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocumentPageFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s generated documents, newest first, WITHOUT their rendered content — fetch one document to read its body.  The response is marked no-store: these records name the counterparties an org is contracting with, and must not sit in a shared cache.
     * Returns the org\'s generated documents, newest first, WITHOUT their rendered content — fetch one document to read its body.
     */
    async getLegalDocuments(requestParameters: LegalApiGetLegalDocumentsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocumentPage> {
        const response = await this.getLegalDocumentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one of the org\'s documents WITH its rendered body. 404 when the org has no document with that id — a document is never readable across orgs.  The response is marked no-store: the body is contract text, sealed at rest and returned only to the owning org, and must not sit in a shared cache.
     * Returns one of the org\'s documents WITH its rendered body.
     */
    async getLegalDocumentsByIdRaw(requestParameters: LegalApiGetLegalDocumentsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocumentReply>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getLegalDocumentsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/documents/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocumentReplyFromJSON(jsonValue));
    }

    /**
     * Returns one of the org\'s documents WITH its rendered body. 404 when the org has no document with that id — a document is never readable across orgs.  The response is marked no-store: the body is contract text, sealed at rest and returned only to the owning org, and must not sit in a shared cache.
     * Returns one of the org\'s documents WITH its rendered body.
     */
    async getLegalDocumentsById(requestParameters: LegalApiGetLegalDocumentsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocumentReply> {
        const response = await this.getLegalDocumentsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s filing records, newest first — which documents were filed where, through which provider, and what the filing\'s honest status is.
     * Returns the org\'s filing records, newest first — which documents were filed where, through which provider, and what the filing\'s honest status is.
     */
    async getLegalFilingsRaw(requestParameters: LegalApiGetLegalFilingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FilingPage>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/filings`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FilingPageFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s filing records, newest first — which documents were filed where, through which provider, and what the filing\'s honest status is.
     * Returns the org\'s filing records, newest first — which documents were filed where, through which provider, and what the filing\'s honest status is.
     */
    async getLegalFilings(requestParameters: LegalApiGetLegalFilingsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FilingPage> {
        const response = await this.getLegalFilingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports that the legal subsystem is serving and how many built-in templates its catalog carries. It reads no tenant, so a liveness prober that sends no principal is answered rather than refused.
     * Reports that the legal subsystem is serving and how many built-in templates its catalog carries.
     */
    async getLegalHealthRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LegalHealth>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/health`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LegalHealthFromJSON(jsonValue));
    }

    /**
     * Reports that the legal subsystem is serving and how many built-in templates its catalog carries. It reads no tenant, so a liveness prober that sends no principal is answered rather than refused.
     * Reports that the legal subsystem is serving and how many built-in templates its catalog carries.
     */
    async getLegalHealth(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LegalHealth> {
        const response = await this.getLegalHealthRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the org\'s effective template catalog: every built-in template, with any the org has overridden replaced by its own latest version.  The listing carries each template\'s metadata and its declared MERGE FIELDS — the keys a document generation must supply — but never the template bodies; fetch one template to get its body. Templates in the formation and equity categories are marked counselReview: every document rendered from them carries a counsel notice, and that posture cannot be dropped by an override.
     * Returns the org\'s effective template catalog: every built-in template, with any the org has overridden replaced by its own latest version.
     */
    async getLegalTemplatesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TemplateCatalog>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/templates`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TemplateCatalogFromJSON(jsonValue));
    }

    /**
     * Returns the org\'s effective template catalog: every built-in template, with any the org has overridden replaced by its own latest version.  The listing carries each template\'s metadata and its declared MERGE FIELDS — the keys a document generation must supply — but never the template bodies; fetch one template to get its body. Templates in the formation and equity categories are marked counselReview: every document rendered from them carries a counsel notice, and that posture cannot be dropped by an override.
     * Returns the org\'s effective template catalog: every built-in template, with any the org has overridden replaced by its own latest version.
     */
    async getLegalTemplates(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TemplateCatalog> {
        const response = await this.getLegalTemplatesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one template resolved for the caller\'s org — the org\'s own override if it has saved one, else the built-in — with its full text/template body and its declared merge fields. 404 when neither exists.
     * Returns one template resolved for the caller\'s org — the org\'s own override if it has saved one, else the built-in — with its full text/template body and its declared merge fields.
     */
    async getLegalTemplatesByIdRaw(requestParameters: LegalApiGetLegalTemplatesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TemplateReply>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getLegalTemplatesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/templates/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TemplateReplyFromJSON(jsonValue));
    }

    /**
     * Returns one template resolved for the caller\'s org — the org\'s own override if it has saved one, else the built-in — with its full text/template body and its declared merge fields. 404 when neither exists.
     * Returns one template resolved for the caller\'s org — the org\'s own override if it has saved one, else the built-in — with its full text/template body and its declared merge fields.
     */
    async getLegalTemplatesById(requestParameters: LegalApiGetLegalTemplatesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TemplateReply> {
        const response = await this.getLegalTemplatesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Renders a document from a template and the caller\'s own merge data, seals it in the org\'s store, and returns it with its rendered body.  The render is PURE and deterministic — no clock, no I/O — so the same template version and the same data always produce identical bytes, which is what makes a generated contract reproducible. It fails CLOSED on a missing merge field: there is no blank-filled contract, only a 400 naming the fields that were absent. When the template is counsel-review the rendered body opens with the counsel notice, which no caller can suppress.  The document is a DRAFT. Hanzo Legal manages documents; it does not give legal advice and does not determine that a document is valid or sufficient.
     * Renders a document from a template and the caller\'s own merge data, seals it in the org\'s store, and returns it with its rendered body.
     */
    async postLegalDocumentsRaw(requestParameters: LegalApiPostLegalDocumentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocumentReply>> {
        if (requestParameters['generateRequest'] == null) {
            throw new runtime.RequiredError(
                'generateRequest',
                'Required parameter "generateRequest" was null or undefined when calling postLegalDocuments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/documents`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GenerateRequestToJSON(requestParameters['generateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocumentReplyFromJSON(jsonValue));
    }

    /**
     * Renders a document from a template and the caller\'s own merge data, seals it in the org\'s store, and returns it with its rendered body.  The render is PURE and deterministic — no clock, no I/O — so the same template version and the same data always produce identical bytes, which is what makes a generated contract reproducible. It fails CLOSED on a missing merge field: there is no blank-filled contract, only a 400 naming the fields that were absent. When the template is counsel-review the rendered body opens with the counsel notice, which no caller can suppress.  The document is a DRAFT. Hanzo Legal manages documents; it does not give legal advice and does not determine that a document is valid or sufficient.
     * Renders a document from a template and the caller\'s own merge data, seals it in the org\'s store, and returns it with its rendered body.
     */
    async postLegalDocuments(requestParameters: LegalApiPostLegalDocumentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocumentReply> {
        const response = await this.postLegalDocumentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Opens an e-signature request over one document and moves it to out_for_signature, returning the provider\'s reference for the request.  The provider is whatever this deployment has wired. The honest default is \"manual\": the request is recorded and the org fulfils it out of band — nothing here fabricates a signature, and the stub never reports itself complete.
     * Opens an e-signature request over one document and moves it to out_for_signature, returning the provider\'s reference for the request.
     */
    async postLegalDocumentsByIdSignRaw(requestParameters: LegalApiPostLegalDocumentsByIdSignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SignReply>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postLegalDocumentsByIdSign().'
            );
        }

        if (requestParameters['signRequest'] == null) {
            throw new runtime.RequiredError(
                'signRequest',
                'Required parameter "signRequest" was null or undefined when calling postLegalDocumentsByIdSign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/documents/{id}/sign`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SignRequestToJSON(requestParameters['signRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SignReplyFromJSON(jsonValue));
    }

    /**
     * Opens an e-signature request over one document and moves it to out_for_signature, returning the provider\'s reference for the request.  The provider is whatever this deployment has wired. The honest default is \"manual\": the request is recorded and the org fulfils it out of band — nothing here fabricates a signature, and the stub never reports itself complete.
     * Opens an e-signature request over one document and moves it to out_for_signature, returning the provider\'s reference for the request.
     */
    async postLegalDocumentsByIdSign(requestParameters: LegalApiPostLegalDocumentsByIdSignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SignReply> {
        const response = await this.postLegalDocumentsByIdSignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records completion of the signature request opened over a generated document and answers the document with a `signed` flag.  The e-sign provider\'s own status is consulted FIRST and is the default answer; an explicit `signed` field in the body overrides it. That override is the whole point: the default `manual` provider never self-completes, so a reviewer (or a real provider\'s webhook) is what moves the document. A completion flips the document to `signed`, stamps `signedAt`, and writes a `legal.document.signed` audit event; a provider still reporting incomplete answers 200 with the document unchanged, so the call is safe to repeat and never fabricates a signature.  Org-scoped and fails closed: a validated principal is required (403 without one), the document is read under the caller\'s OWN org so another tenant\'s id is a 404, a document with no open signature request is a 400, and a provider whose status call errors is a 502.
     * Record that a generated document\'s signature request completed
     */
    async postLegalDocumentsByIdSignCompleteRaw(requestParameters: LegalApiPostLegalDocumentsByIdSignCompleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postLegalDocumentsByIdSignComplete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/documents/{id}/sign/complete`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Records completion of the signature request opened over a generated document and answers the document with a `signed` flag.  The e-sign provider\'s own status is consulted FIRST and is the default answer; an explicit `signed` field in the body overrides it. That override is the whole point: the default `manual` provider never self-completes, so a reviewer (or a real provider\'s webhook) is what moves the document. A completion flips the document to `signed`, stamps `signedAt`, and writes a `legal.document.signed` audit event; a provider still reporting incomplete answers 200 with the document unchanged, so the call is safe to repeat and never fabricates a signature.  Org-scoped and fails closed: a validated principal is required (403 without one), the document is read under the caller\'s OWN org so another tenant\'s id is a 404, a document with no open signature request is a 400, and a provider whose status call errors is a 502.
     * Record that a generated document\'s signature request completed
     */
    async postLegalDocumentsByIdSignComplete(requestParameters: LegalApiPostLegalDocumentsByIdSignCompleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postLegalDocumentsByIdSignCompleteRaw(requestParameters, initOverrides);
    }

    /**
     * Records a filing of one or more of the org\'s documents with a state or agency, and returns the tracking record.  It is a TRACKING record, not an autonomous filing. With no filing partner wired the honest status is \"manual\" and the note says so: the documents were generated for signature, and the org files them through its registered agent. Nothing here invents a filing id it does not have.  Every document id must belong to the caller\'s org; one that does not is a 404 naming it, so a filing can never reach across tenants.
     * Records a filing of one or more of the org\'s documents with a state or agency, and returns the tracking record.
     */
    async postLegalFilingsRaw(requestParameters: LegalApiPostLegalFilingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FilingReply>> {
        if (requestParameters['filingRequest'] == null) {
            throw new runtime.RequiredError(
                'filingRequest',
                'Required parameter "filingRequest" was null or undefined when calling postLegalFilings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/filings`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FilingRequestToJSON(requestParameters['filingRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FilingReplyFromJSON(jsonValue));
    }

    /**
     * Records a filing of one or more of the org\'s documents with a state or agency, and returns the tracking record.  It is a TRACKING record, not an autonomous filing. With no filing partner wired the honest status is \"manual\" and the note says so: the documents were generated for signature, and the org files them through its registered agent. Nothing here invents a filing id it does not have.  Every document id must belong to the caller\'s org; one that does not is a 404 naming it, so a filing can never reach across tenants.
     * Records a filing of one or more of the org\'s documents with a state or agency, and returns the tracking record.
     */
    async postLegalFilings(requestParameters: LegalApiPostLegalFilingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FilingReply> {
        const response = await this.postLegalFilingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Saves the org\'s own version of a template — a custom NDA, a house MSA — and returns it with its new version number. It takes effect for that org only; other orgs keep the built-in.  Two boundaries cannot be crossed here. Overriding a built-in INHERITS its category and its counsel-review posture, which can be raised but never dropped; and a formation or equity template is counsel-review whatever the caller sends, so no org can generate a securities-class document without the notice.  The body is validated on save, not at generation: a template that references an UNDECLARED merge field is refused with 400 rather than stored and rendered blank into a contract months later.
     * Saves the org\'s own version of a template — a custom NDA, a house MSA — and returns it with its new version number.
     */
    async putLegalTemplatesByIdRaw(requestParameters: LegalApiPutLegalTemplatesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TemplateReply>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putLegalTemplatesById().'
            );
        }

        if (requestParameters['templateOverride'] == null) {
            throw new runtime.RequiredError(
                'templateOverride',
                'Required parameter "templateOverride" was null or undefined when calling putLegalTemplatesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/legal/templates/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: TemplateOverrideToJSON(requestParameters['templateOverride']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TemplateReplyFromJSON(jsonValue));
    }

    /**
     * Saves the org\'s own version of a template — a custom NDA, a house MSA — and returns it with its new version number. It takes effect for that org only; other orgs keep the built-in.  Two boundaries cannot be crossed here. Overriding a built-in INHERITS its category and its counsel-review posture, which can be raised but never dropped; and a formation or equity template is counsel-review whatever the caller sends, so no org can generate a securities-class document without the notice.  The body is validated on save, not at generation: a template that references an UNDECLARED merge field is refused with 400 rather than stored and rendered blank into a contract months later.
     * Saves the org\'s own version of a template — a custom NDA, a house MSA — and returns it with its new version number.
     */
    async putLegalTemplatesById(requestParameters: LegalApiPutLegalTemplatesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TemplateReply> {
        const response = await this.putLegalTemplatesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
