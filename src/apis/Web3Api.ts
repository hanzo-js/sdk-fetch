/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  Balances,
  ChainList,
  ChainStatus,
  RpcIn,
  RpcOut,
} from '../models/index.js';
import {
    BalancesFromJSON,
    BalancesToJSON,
    ChainListFromJSON,
    ChainListToJSON,
    ChainStatusFromJSON,
    ChainStatusToJSON,
    RpcInFromJSON,
    RpcInToJSON,
    RpcOutFromJSON,
    RpcOutToJSON,
} from '../models/index.js';

export interface Web3ApiGetWeb3ChainsByChainRequest {
    chain: string;
}

export interface Web3ApiGetWeb3TokensByChainByAddressRequest {
    chain: string;
    address: string;
}

export interface Web3ApiPostWeb3RpcByChainRequest {
    chain: string;
    rpcIn: RpcIn;
}

/**
 * 
 */
export class Web3Api extends runtime.BaseAPI {

    /**
     * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/web3/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
     * Reports the chains this deployment can reach.
     */
    async getWeb3ChainsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChainList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/web3/chains`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChainListFromJSON(jsonValue));
    }

    /**
     * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/web3/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
     * Reports the chains this deployment can reach.
     */
    async getWeb3Chains(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChainList> {
        const response = await this.getWeb3ChainsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
     * Reports one chain and whether its upstream is answering.
     */
    async getWeb3ChainsByChainRaw(requestParameters: Web3ApiGetWeb3ChainsByChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChainStatus>> {
        if (requestParameters['chain'] == null) {
            throw new runtime.RequiredError(
                'chain',
                'Required parameter "chain" was null or undefined when calling getWeb3ChainsByChain().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/web3/chains/{chain}`;
        urlPath = urlPath.replace(`{${"chain"}}`, encodeURIComponent(String(requestParameters['chain'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChainStatusFromJSON(jsonValue));
    }

    /**
     * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
     * Reports one chain and whether its upstream is answering.
     */
    async getWeb3ChainsByChain(requestParameters: Web3ApiGetWeb3ChainsByChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChainStatus> {
        const response = await this.getWeb3ChainsByChainRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reads an address\'s native balance on a chain.  ERC-20 positions are NOT enumerated here: eth_getBalance answers the native one, but \"every token this address holds\" is an indexer question — there is no RPC call that answers it, and walking a token list would return a number that silently omits whatever the list missed. explorer owns the indexer relationship; this returns the balance the chain itself can prove.
     * Reads an address\'s native balance on a chain.
     */
    async getWeb3TokensByChainByAddressRaw(requestParameters: Web3ApiGetWeb3TokensByChainByAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Balances>> {
        if (requestParameters['chain'] == null) {
            throw new runtime.RequiredError(
                'chain',
                'Required parameter "chain" was null or undefined when calling getWeb3TokensByChainByAddress().'
            );
        }

        if (requestParameters['address'] == null) {
            throw new runtime.RequiredError(
                'address',
                'Required parameter "address" was null or undefined when calling getWeb3TokensByChainByAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/web3/tokens/{chain}/{address}`;
        urlPath = urlPath.replace(`{${"chain"}}`, encodeURIComponent(String(requestParameters['chain'])));
        urlPath = urlPath.replace(`{${"address"}}`, encodeURIComponent(String(requestParameters['address'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BalancesFromJSON(jsonValue));
    }

    /**
     * Reads an address\'s native balance on a chain.  ERC-20 positions are NOT enumerated here: eth_getBalance answers the native one, but \"every token this address holds\" is an indexer question — there is no RPC call that answers it, and walking a token list would return a number that silently omits whatever the list missed. explorer owns the indexer relationship; this returns the balance the chain itself can prove.
     * Reads an address\'s native balance on a chain.
     */
    async getWeb3TokensByChainByAddress(requestParameters: Web3ApiGetWeb3TokensByChainByAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Balances> {
        const response = await this.getWeb3TokensByChainByAddressRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Forwards a JSON-RPC call to the named chain and returns its answer unchanged. Only declared chains are reachable, and only to a caller with a validated principal — this is the deployment\'s upstream, not an open relay.
     * Forwards a JSON-RPC call to the named chain and returns its answer unchanged.
     */
    async postWeb3RpcByChainRaw(requestParameters: Web3ApiPostWeb3RpcByChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RpcOut>> {
        if (requestParameters['chain'] == null) {
            throw new runtime.RequiredError(
                'chain',
                'Required parameter "chain" was null or undefined when calling postWeb3RpcByChain().'
            );
        }

        if (requestParameters['rpcIn'] == null) {
            throw new runtime.RequiredError(
                'rpcIn',
                'Required parameter "rpcIn" was null or undefined when calling postWeb3RpcByChain().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/web3/rpc/{chain}`;
        urlPath = urlPath.replace(`{${"chain"}}`, encodeURIComponent(String(requestParameters['chain'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RpcInToJSON(requestParameters['rpcIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RpcOutFromJSON(jsonValue));
    }

    /**
     * Forwards a JSON-RPC call to the named chain and returns its answer unchanged. Only declared chains are reachable, and only to a caller with a validated principal — this is the deployment\'s upstream, not an open relay.
     * Forwards a JSON-RPC call to the named chain and returns its answer unchanged.
     */
    async postWeb3RpcByChain(requestParameters: Web3ApiPostWeb3RpcByChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RpcOut> {
        const response = await this.postWeb3RpcByChainRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
