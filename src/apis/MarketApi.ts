/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  History,
  Pools,
  Roster,
  Survey,
  Tokens,
} from '../models/index.js';
import {
    HistoryFromJSON,
    HistoryToJSON,
    PoolsFromJSON,
    PoolsToJSON,
    RosterFromJSON,
    RosterToJSON,
    SurveyFromJSON,
    SurveyToJSON,
    TokensFromJSON,
    TokensToJSON,
} from '../models/index.js';

export interface MarketApiGetMarketPoolsRequest {
    chain?: string;
}

export interface MarketApiGetMarketSurveyRequest {
    chain?: string;
}

export interface MarketApiGetMarketTokenRequest {
    chain?: string;
    at?: string;
}

export interface MarketApiGetMarketTokensRequest {
    chain?: string;
}

/**
 * 
 */
export class MarketApi extends runtime.BaseAPI {

    /**
     * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.  One call. It reads the chain registry, then every chain\'s indexer for its figures and its most recent active day, all at once — where a client doing it itself makes one registry request and two more per chain.  THE ROW IS THE UNIT OF TRUTH. Each carries its own reach, so one indexer being unreachable costs one row its figures and leaves the rest answered. A chain with no market maker deployed — the registry names no factory for it — answers `read` with totals of nothing, which is a fact about that chain and is not the same as a chain nobody could ask.
     * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.
     */
    async getMarketChainsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Roster>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/market/chains`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RosterFromJSON(jsonValue));
    }

    /**
     * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.  One call. It reads the chain registry, then every chain\'s indexer for its figures and its most recent active day, all at once — where a client doing it itself makes one registry request and two more per chain.  THE ROW IS THE UNIT OF TRUTH. Each carries its own reach, so one indexer being unreachable costs one row its figures and leaves the rest answered. A chain with no market maker deployed — the registry names no factory for it — answers `read` with totals of nothing, which is a fact about that chain and is not the same as a chain nobody could ask.
     * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.
     */
    async getMarketChains(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Roster> {
        const response = await this.getMarketChainsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.  The two price fields on a pool are the ratio its own reserves stand at, as the indexer computed them. They are not a price ON either token and not a mark: nothing here derives one, ranks the pools, or names a route through them.  A chain with no market maker deployed answers `read` with no pools. That is the chain\'s real condition, and it is deliberately not the same answer as an indexer that could not be asked.
     * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.
     */
    async getMarketPoolsRaw(requestParameters: MarketApiGetMarketPoolsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Pools>> {
        const queryParameters: any = {};

        if (requestParameters['chain'] != null) {
            queryParameters['chain'] = requestParameters['chain'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/market/pools`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PoolsFromJSON(jsonValue));
    }

    /**
     * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.  The two price fields on a pool are the ratio its own reserves stand at, as the indexer computed them. They are not a price ON either token and not a mark: nothing here derives one, ranks the pools, or names a route through them.  A chain with no market maker deployed answers `read` with no pools. That is the chain\'s real condition, and it is deliberately not the same answer as an indexer that could not be asked.
     * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.
     */
    async getMarketPools(requestParameters: MarketApiGetMarketPoolsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Pools> {
        const response = await this.getMarketPoolsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers which of the four settlement precompiles carry code on one chain.  An address with no code answers a call with empty data rather than an error, so \"this chain has no view precompile\" and \"this market was never opened\" reach a caller as the same silence — and only the second is a fact about a market. This says which it is, by asking the node for the code at each address.  It reads presence and nothing else. No market, no quote, no depth and no order is requested here, and `eth_getCode` is the only method this operation ever sends.
     * Answers which of the four settlement precompiles carry code on one chain.
     */
    async getMarketSurveyRaw(requestParameters: MarketApiGetMarketSurveyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Survey>> {
        const queryParameters: any = {};

        if (requestParameters['chain'] != null) {
            queryParameters['chain'] = requestParameters['chain'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/market/survey`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyFromJSON(jsonValue));
    }

    /**
     * Answers which of the four settlement precompiles carry code on one chain.  An address with no code answers a call with empty data rather than an error, so \"this chain has no view precompile\" and \"this market was never opened\" reach a caller as the same silence — and only the second is a fact about a market. This says which it is, by asking the node for the code at each address.  It reads presence and nothing else. No market, no quote, no depth and no order is requested here, and `eth_getCode` is the only method this operation ever sends.
     * Answers which of the four settlement precompiles carry code on one chain.
     */
    async getMarketSurvey(requestParameters: MarketApiGetMarketSurveyRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Survey> {
        const response = await this.getMarketSurveyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.  Every figure is the indexer\'s own arithmetic, passed through as the decimal string it computed. Nothing here rounds one, converts one, or fills a gap: a day the indexer holds no figure for arrives with that field absent, which says \"not indexed\" where a zero would say \"worth nothing\".
     * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.
     */
    async getMarketTokenRaw(requestParameters: MarketApiGetMarketTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<History>> {
        const queryParameters: any = {};

        if (requestParameters['chain'] != null) {
            queryParameters['chain'] = requestParameters['chain'];
        }

        if (requestParameters['at'] != null) {
            queryParameters['at'] = requestParameters['at'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/market/token`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HistoryFromJSON(jsonValue));
    }

    /**
     * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.  Every figure is the indexer\'s own arithmetic, passed through as the decimal string it computed. Nothing here rounds one, converts one, or fills a gap: a day the indexer holds no figure for arrives with that field absent, which says \"not indexed\" where a zero would say \"worth nothing\".
     * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.
     */
    async getMarketToken(requestParameters: MarketApiGetMarketTokenRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<History> {
        const response = await this.getMarketTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.  This is what the indexer INGESTED, which is not the same as what exists on the chain: a token nothing has traded has no row here, and this is not a registry of what is permitted or listed.
     * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.
     */
    async getMarketTokensRaw(requestParameters: MarketApiGetMarketTokensRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Tokens>> {
        const queryParameters: any = {};

        if (requestParameters['chain'] != null) {
            queryParameters['chain'] = requestParameters['chain'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/market/tokens`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TokensFromJSON(jsonValue));
    }

    /**
     * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.  This is what the indexer INGESTED, which is not the same as what exists on the chain: a token nothing has traded has no row here, and this is not a registry of what is permitted or listed.
     * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.
     */
    async getMarketTokens(requestParameters: MarketApiGetMarketTokensRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Tokens> {
        const response = await this.getMarketTokensRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
