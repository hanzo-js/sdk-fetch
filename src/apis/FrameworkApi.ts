/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  DocType,
  DocTypeList,
  DocumentList,
  Install,
  ModuleList,
  ModuleState,
  SummaryView,
} from '../models/index.js';
import {
    DocTypeFromJSON,
    DocTypeToJSON,
    DocTypeListFromJSON,
    DocTypeListToJSON,
    DocumentListFromJSON,
    DocumentListToJSON,
    InstallFromJSON,
    InstallToJSON,
    ModuleListFromJSON,
    ModuleListToJSON,
    ModuleStateFromJSON,
    ModuleStateToJSON,
    SummaryViewFromJSON,
    SummaryViewToJSON,
} from '../models/index.js';

export interface FrameworkApiDeleteFrameworkByDoctypeByNameRequest {
    doctype: string;
    name: string;
}

export interface FrameworkApiDeleteFrameworkDoctypesByNameRequest {
    name: string;
}

export interface FrameworkApiGetFrameworkByDoctypeRequest {
    doctype: string;
    filters?: string;
    fields?: string;
    orderBy?: string;
    limit?: string;
}

export interface FrameworkApiGetFrameworkByDoctypeByNameRequest {
    doctype: string;
    name: string;
}

export interface FrameworkApiGetFrameworkDoctypesByNameRequest {
    name: string;
}

export interface FrameworkApiGetFrameworkModulesByModuleRequest {
    module: string;
}

export interface FrameworkApiPostFrameworkByDoctypeRequest {
    doctype: string;
}

export interface FrameworkApiPostFrameworkByDoctypeByNameCancelRequest {
    doctype: string;
    name: string;
}

export interface FrameworkApiPostFrameworkByDoctypeByNameSubmitRequest {
    doctype: string;
    name: string;
}

export interface FrameworkApiPostFrameworkDoctypesRequest {
    docType: DocType;
}

export interface FrameworkApiPostFrameworkModulesByModuleInstallRequest {
    module: string;
}

export interface FrameworkApiPutFrameworkByDoctypeByNameRequest {
    doctype: string;
    name: string;
}

export interface FrameworkApiPutFrameworkDoctypesByNameRequest {
    name: string;
    docType: DocType;
}

/**
 * 
 */
export class FrameworkApi extends runtime.BaseAPI {

    /**
     * Removes one document, after its on_trash hooks agree. A SUBMITTED document cannot be deleted — cancel it first. Answers 204.
     * Removes one document, after its on_trash hooks agree.
     */
    async deleteFrameworkByDoctypeByNameRaw(requestParameters: FrameworkApiDeleteFrameworkByDoctypeByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling deleteFrameworkByDoctypeByName().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteFrameworkByDoctypeByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}/{name}`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one document, after its on_trash hooks agree. A SUBMITTED document cannot be deleted — cancel it first. Answers 204.
     * Removes one document, after its on_trash hooks agree.
     */
    async deleteFrameworkByDoctypeByName(requestParameters: FrameworkApiDeleteFrameworkByDoctypeByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteFrameworkByDoctypeByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Removes a DocType and every document stored under it. The definition and its data go together — a document with no schema can be neither validated nor read back — so there is no undo. Manager-only. Answers 204.
     * Removes a DocType and every document stored under it.
     */
    async deleteFrameworkDoctypesByNameRaw(requestParameters: FrameworkApiDeleteFrameworkDoctypesByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteFrameworkDoctypesByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/doctypes/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a DocType and every document stored under it. The definition and its data go together — a document with no schema can be neither validated nor read back — so there is no undo. Manager-only. Answers 204.
     * Removes a DocType and every document stored under it.
     */
    async deleteFrameworkDoctypesByName(requestParameters: FrameworkApiDeleteFrameworkDoctypesByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteFrameworkDoctypesByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the caller org\'s documents of one DocType, filtered, ordered and projected by the query. The DocType is resolved FIRST — through the same permission gate the list itself uses — because the query is validated against its schema: a filter, sort or field name the DocType does not declare is refused rather than reaching the store.
     * Returns the caller org\'s documents of one DocType, filtered, ordered and projected by the query.
     */
    async getFrameworkByDoctypeRaw(requestParameters: FrameworkApiGetFrameworkByDoctypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocumentList>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling getFrameworkByDoctype().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['filters'] != null) {
            queryParameters['filters'] = requestParameters['filters'];
        }

        if (requestParameters['fields'] != null) {
            queryParameters['fields'] = requestParameters['fields'];
        }

        if (requestParameters['orderBy'] != null) {
            queryParameters['order_by'] = requestParameters['orderBy'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocumentListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s documents of one DocType, filtered, ordered and projected by the query. The DocType is resolved FIRST — through the same permission gate the list itself uses — because the query is validated against its schema: a filter, sort or field name the DocType does not declare is refused rather than reaching the store.
     * Returns the caller org\'s documents of one DocType, filtered, ordered and projected by the query.
     */
    async getFrameworkByDoctype(requestParameters: FrameworkApiGetFrameworkByDoctypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocumentList> {
        const response = await this.getFrameworkByDoctypeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one document by name, with Password fields redacted.
     * Returns one document by name, with Password fields redacted.
     */
    async getFrameworkByDoctypeByNameRaw(requestParameters: FrameworkApiGetFrameworkByDoctypeByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling getFrameworkByDoctypeByName().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getFrameworkByDoctypeByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}/{name}`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns one document by name, with Password fields redacted.
     * Returns one document by name, with Password fields redacted.
     */
    async getFrameworkByDoctypeByName(requestParameters: FrameworkApiGetFrameworkByDoctypeByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.getFrameworkByDoctypeByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every DocType defined in the caller\'s org. Another tenant\'s definitions are never included: the org is part of the store key.
     * Returns every DocType defined in the caller\'s org.
     */
    async getFrameworkDoctypesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocTypeList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/doctypes`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocTypeListFromJSON(jsonValue));
    }

    /**
     * Returns every DocType defined in the caller\'s org. Another tenant\'s definitions are never included: the org is part of the store key.
     * Returns every DocType defined in the caller\'s org.
     */
    async getFrameworkDoctypes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocTypeList> {
        const response = await this.getFrameworkDoctypesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one DocType definition — its fields, naming rule, permissions and lifecycle flags. Scoped to the caller\'s org, so another tenant\'s DocType of the same name is simply not found.
     * Returns one DocType definition — its fields, naming rule, permissions and lifecycle flags.
     */
    async getFrameworkDoctypesByNameRaw(requestParameters: FrameworkApiGetFrameworkDoctypesByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocType>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getFrameworkDoctypesByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/doctypes/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocTypeFromJSON(jsonValue));
    }

    /**
     * Returns one DocType definition — its fields, naming rule, permissions and lifecycle flags. Scoped to the caller\'s org, so another tenant\'s DocType of the same name is simply not found.
     * Returns one DocType definition — its fields, naming rule, permissions and lifecycle flags.
     */
    async getFrameworkDoctypesByName(requestParameters: FrameworkApiGetFrameworkDoctypesByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocType> {
        const response = await this.getFrameworkDoctypesByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every app lane compiled into this deployment and the DocTypes each one installs. It describes the BINARY, not the org: what a given org has actually installed is the per-module state below.
     * Returns every app lane compiled into this deployment and the DocTypes each one installs.
     */
    async getFrameworkModulesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModuleList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/modules`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModuleListFromJSON(jsonValue));
    }

    /**
     * Returns every app lane compiled into this deployment and the DocTypes each one installs. It describes the BINARY, not the org: what a given org has actually installed is the per-module state below.
     * Returns every app lane compiled into this deployment and the DocTypes each one installs.
     */
    async getFrameworkModules(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModuleList> {
        const response = await this.getFrameworkModulesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one app lane\'s install state for the caller\'s org: the DocTypes the lane declares, and which of them already exist in the org. That is the honest \"set up\" versus \"installed\" answer a console renders.
     * Returns one app lane\'s install state for the caller\'s org: the DocTypes the lane declares, and which of them already exist in the org.
     */
    async getFrameworkModulesByModuleRaw(requestParameters: FrameworkApiGetFrameworkModulesByModuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModuleState>> {
        if (requestParameters['module'] == null) {
            throw new runtime.RequiredError(
                'module',
                'Required parameter "module" was null or undefined when calling getFrameworkModulesByModule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/modules/{module}`;
        urlPath = urlPath.replace(`{${"module"}}`, encodeURIComponent(String(requestParameters['module'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModuleStateFromJSON(jsonValue));
    }

    /**
     * Returns one app lane\'s install state for the caller\'s org: the DocTypes the lane declares, and which of them already exist in the org. That is the honest \"set up\" versus \"installed\" answer a console renders.
     * Returns one app lane\'s install state for the caller\'s org: the DocTypes the lane declares, and which of them already exist in the org.
     */
    async getFrameworkModulesByModule(requestParameters: FrameworkApiGetFrameworkModulesByModuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModuleState> {
        const response = await this.getFrameworkModulesByModuleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports how much of the DocType surface the caller\'s org uses: how many DocTypes it has defined, and how many documents exist across them.
     * Reports how much of the DocType surface the caller\'s org uses: how many DocTypes it has defined, and how many documents exist across them.
     */
    async getFrameworkSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SummaryView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SummaryViewFromJSON(jsonValue));
    }

    /**
     * Reports how much of the DocType surface the caller\'s org uses: how many DocTypes it has defined, and how many documents exist across them.
     * Reports how much of the DocType surface the caller\'s org uses: how many DocTypes it has defined, and how many documents exist across them.
     */
    async getFrameworkSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SummaryView> {
        const response = await this.getFrameworkSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * The body is the DOCUMENT\'S field data: a flat JSON object whose properties are the fieldnames the DocType declares, not a fixed envelope. That is why this operation publishes no request schema — the shape is metadata the DocType defines at run time, and no Go struct both accepts it verbatim and describes it, so nothing is asserted rather than something false.  The engine validates and coerces every field against the DocType, runs the before_insert and before_save hooks (either may reject the write), stores the document, then runs the after hooks. It answers 201 with the stored document: the field data plus the managed envelope — `name`, `doctype`, `docstatus`, `createdAt`, `updatedAt`. A Password field comes back as a fixed redaction marker and is dropped when empty; its stored value is never returned by this or any other read on this surface.  `name` in the body is the REQUESTED DOCUMENT NAME, not a data field. A DocType with an autoname rule names the document itself and ignores it; a prompt-named DocType takes it. This collision is also why the two path segments cannot be folded into the body, and so why the route stays untyped.  Scoped to the org of the validated principal, and the engine\'s own permission calculus decides the rest: the caller needs create rights on this DocType through a role it holds, or a platform admin bit. A caller with no validated principal reaches the engine as the zero Caller and is refused before any store is opened — a forged org header alone buys nothing.  A DocType declared Single has exactly ONE document per org, so this writes that one instance instead of adding a row. The body is size-bounded by the engine, the same bound on every host.
     * Create one document of a DocType, from that DocType\'s own fields.
     */
    async postFrameworkByDoctypeRaw(requestParameters: FrameworkApiPostFrameworkByDoctypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling postFrameworkByDoctype().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The body is the DOCUMENT\'S field data: a flat JSON object whose properties are the fieldnames the DocType declares, not a fixed envelope. That is why this operation publishes no request schema — the shape is metadata the DocType defines at run time, and no Go struct both accepts it verbatim and describes it, so nothing is asserted rather than something false.  The engine validates and coerces every field against the DocType, runs the before_insert and before_save hooks (either may reject the write), stores the document, then runs the after hooks. It answers 201 with the stored document: the field data plus the managed envelope — `name`, `doctype`, `docstatus`, `createdAt`, `updatedAt`. A Password field comes back as a fixed redaction marker and is dropped when empty; its stored value is never returned by this or any other read on this surface.  `name` in the body is the REQUESTED DOCUMENT NAME, not a data field. A DocType with an autoname rule names the document itself and ignores it; a prompt-named DocType takes it. This collision is also why the two path segments cannot be folded into the body, and so why the route stays untyped.  Scoped to the org of the validated principal, and the engine\'s own permission calculus decides the rest: the caller needs create rights on this DocType through a role it holds, or a platform admin bit. A caller with no validated principal reaches the engine as the zero Caller and is refused before any store is opened — a forged org header alone buys nothing.  A DocType declared Single has exactly ONE document per org, so this writes that one instance instead of adding a row. The body is size-bounded by the engine, the same bound on every host.
     * Create one document of a DocType, from that DocType\'s own fields.
     */
    async postFrameworkByDoctype(requestParameters: FrameworkApiPostFrameworkByDoctypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postFrameworkByDoctypeRaw(requestParameters, initOverrides);
    }

    /**
     * Moves a submitted document to cancelled (docstatus 1 → 2) after its on_cancel hooks agree. Cancelling is terminal — a cancelled document cannot be re-submitted — but it CAN then be deleted.
     * Moves a submitted document to cancelled (docstatus 1 → 2) after its on_cancel hooks agree.
     */
    async postFrameworkByDoctypeByNameCancelRaw(requestParameters: FrameworkApiPostFrameworkByDoctypeByNameCancelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling postFrameworkByDoctypeByNameCancel().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postFrameworkByDoctypeByNameCancel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}/{name}/cancel`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Moves a submitted document to cancelled (docstatus 1 → 2) after its on_cancel hooks agree. Cancelling is terminal — a cancelled document cannot be re-submitted — but it CAN then be deleted.
     * Moves a submitted document to cancelled (docstatus 1 → 2) after its on_cancel hooks agree.
     */
    async postFrameworkByDoctypeByNameCancel(requestParameters: FrameworkApiPostFrameworkByDoctypeByNameCancelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.postFrameworkByDoctypeByNameCancelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Moves a draft to submitted (docstatus 0 → 1) after its on_submit hooks agree. A submitted document is IMMUTABLE: further writes and deletes are refused until it is cancelled. Only a submittable DocType has this lifecycle; any other docstatus is an illegal transition.
     * Moves a draft to submitted (docstatus 0 → 1) after its on_submit hooks agree.
     */
    async postFrameworkByDoctypeByNameSubmitRaw(requestParameters: FrameworkApiPostFrameworkByDoctypeByNameSubmitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: object; }>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling postFrameworkByDoctypeByNameSubmit().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postFrameworkByDoctypeByNameSubmit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}/{name}/submit`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Moves a draft to submitted (docstatus 0 → 1) after its on_submit hooks agree. A submitted document is IMMUTABLE: further writes and deletes are refused until it is cancelled. Only a submittable DocType has this lifecycle; any other docstatus is an illegal transition.
     * Moves a draft to submitted (docstatus 0 → 1) after its on_submit hooks agree.
     */
    async postFrameworkByDoctypeByNameSubmit(requestParameters: FrameworkApiPostFrameworkByDoctypeByNameSubmitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: object; }> {
        const response = await this.postFrameworkByDoctypeByNameSubmitRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Defines a DocType in the caller\'s org: the metadata that gives a document surface its fields, its naming rule, whether it has a submit/cancel lifecycle, and which role may do what to it. Manager-only — on a fresh org the first caller to administer it is seeded as its System Manager, after which only a System Manager (or a platform admin) may define. Answers 201.
     * Defines a DocType in the caller\'s org: the metadata that gives a document surface its fields, its naming rule, whether it has a submit/cancel lifecycle, and which role may do what to it.
     */
    async postFrameworkDoctypesRaw(requestParameters: FrameworkApiPostFrameworkDoctypesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocType>> {
        if (requestParameters['docType'] == null) {
            throw new runtime.RequiredError(
                'docType',
                'Required parameter "docType" was null or undefined when calling postFrameworkDoctypes().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/doctypes`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DocTypeToJSON(requestParameters['docType']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocTypeFromJSON(jsonValue));
    }

    /**
     * Defines a DocType in the caller\'s org: the metadata that gives a document surface its fields, its naming rule, whether it has a submit/cancel lifecycle, and which role may do what to it. Manager-only — on a fresh org the first caller to administer it is seeded as its System Manager, after which only a System Manager (or a platform admin) may define. Answers 201.
     * Defines a DocType in the caller\'s org: the metadata that gives a document surface its fields, its naming rule, whether it has a submit/cancel lifecycle, and which role may do what to it.
     */
    async postFrameworkDoctypes(requestParameters: FrameworkApiPostFrameworkDoctypesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocType> {
        const response = await this.postFrameworkDoctypesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an app lane\'s DocTypes in the caller\'s org. Idempotent and create-if-absent: a DocType the org already has is reported as existing and never replaced, so re-installing cannot clobber a definition the org has since edited. Manager-only.
     * Creates an app lane\'s DocTypes in the caller\'s org.
     */
    async postFrameworkModulesByModuleInstallRaw(requestParameters: FrameworkApiPostFrameworkModulesByModuleInstallRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Install>> {
        if (requestParameters['module'] == null) {
            throw new runtime.RequiredError(
                'module',
                'Required parameter "module" was null or undefined when calling postFrameworkModulesByModuleInstall().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/modules/{module}/install`;
        urlPath = urlPath.replace(`{${"module"}}`, encodeURIComponent(String(requestParameters['module'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstallFromJSON(jsonValue));
    }

    /**
     * Creates an app lane\'s DocTypes in the caller\'s org. Idempotent and create-if-absent: a DocType the org already has is reported as existing and never replaced, so re-installing cannot clobber a definition the org has since edited. Manager-only.
     * Creates an app lane\'s DocTypes in the caller\'s org.
     */
    async postFrameworkModulesByModuleInstall(requestParameters: FrameworkApiPostFrameworkModulesByModuleInstallRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Install> {
        const response = await this.postFrameworkModulesByModuleInstallRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * PUT semantics: the stored field data BECOMES the body, so a field the body omits is not left at its previous value. The body is the document\'s own field data — the same metadata-defined open object the create takes, and the same reason this operation publishes no request schema.  Only a DRAFT can be edited. A document that has been submitted or cancelled is immutable and the write is refused as a conflict, so the submit lifecycle cannot be bypassed by a plain update — cancel it first, and note that a cancelled document can be deleted but never re-submitted or re-edited. The engine validates the new data against the DocType, runs before_save (which may reject), writes, then runs the after hooks, and answers 200 with the stored document plus its managed envelope, Password fields redacted.  The document name in the path is percent-decoded before it is matched, so a name containing a space is addressed as it is stored. An unknown DocType or document is not found, and the same answer covers a document that exists in another tenant: the org comes from the validated principal and is part of the store key, so a caller cannot learn that another org\'s document exists. Write rights on the DocType are required, decided by the engine\'s permission calculus.  For a Single DocType the path name is ignored — there is one instance per org and this writes it.
     * Replace a draft document\'s field data wholesale.
     */
    async putFrameworkByDoctypeByNameRaw(requestParameters: FrameworkApiPutFrameworkByDoctypeByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['doctype'] == null) {
            throw new runtime.RequiredError(
                'doctype',
                'Required parameter "doctype" was null or undefined when calling putFrameworkByDoctypeByName().'
            );
        }

        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling putFrameworkByDoctypeByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/{doctype}/{name}`;
        urlPath = urlPath.replace(`{${"doctype"}}`, encodeURIComponent(String(requestParameters['doctype'])));
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * PUT semantics: the stored field data BECOMES the body, so a field the body omits is not left at its previous value. The body is the document\'s own field data — the same metadata-defined open object the create takes, and the same reason this operation publishes no request schema.  Only a DRAFT can be edited. A document that has been submitted or cancelled is immutable and the write is refused as a conflict, so the submit lifecycle cannot be bypassed by a plain update — cancel it first, and note that a cancelled document can be deleted but never re-submitted or re-edited. The engine validates the new data against the DocType, runs before_save (which may reject), writes, then runs the after hooks, and answers 200 with the stored document plus its managed envelope, Password fields redacted.  The document name in the path is percent-decoded before it is matched, so a name containing a space is addressed as it is stored. An unknown DocType or document is not found, and the same answer covers a document that exists in another tenant: the org comes from the validated principal and is part of the store key, so a caller cannot learn that another org\'s document exists. Write rights on the DocType are required, decided by the engine\'s permission calculus.  For a Single DocType the path name is ignored — there is one instance per org and this writes it.
     * Replace a draft document\'s field data wholesale.
     */
    async putFrameworkByDoctypeByName(requestParameters: FrameworkApiPutFrameworkByDoctypeByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.putFrameworkByDoctypeByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Replaces a DocType definition wholesale (PUT semantics): the stored definition becomes the body. The name in the URL is authoritative over the body\'s, and documents already stored under the DocType are left intact. Manager-only.
     * Replaces a DocType definition wholesale (PUT semantics): the stored definition becomes the body.
     */
    async putFrameworkDoctypesByNameRaw(requestParameters: FrameworkApiPutFrameworkDoctypesByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocType>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling putFrameworkDoctypesByName().'
            );
        }

        if (requestParameters['docType'] == null) {
            throw new runtime.RequiredError(
                'docType',
                'Required parameter "docType" was null or undefined when calling putFrameworkDoctypesByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/framework/doctypes/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: DocTypeToJSON(requestParameters['docType']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocTypeFromJSON(jsonValue));
    }

    /**
     * Replaces a DocType definition wholesale (PUT semantics): the stored definition becomes the body. The name in the URL is authoritative over the body\'s, and documents already stored under the DocType are left intact. Manager-only.
     * Replaces a DocType definition wholesale (PUT semantics): the stored definition becomes the body.
     */
    async putFrameworkDoctypesByName(requestParameters: FrameworkApiPutFrameworkDoctypesByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocType> {
        const response = await this.putFrameworkDoctypesByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
