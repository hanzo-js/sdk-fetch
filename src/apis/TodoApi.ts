/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  IssueEdit,
  IssueHit,
  IssueHits,
  IssueView,
  NewIssue,
  RoomWork,
  TodoProject,
} from '../models/index.js';
import {
    IssueEditFromJSON,
    IssueEditToJSON,
    IssueHitFromJSON,
    IssueHitToJSON,
    IssueHitsFromJSON,
    IssueHitsToJSON,
    IssueViewFromJSON,
    IssueViewToJSON,
    NewIssueFromJSON,
    NewIssueToJSON,
    RoomWorkFromJSON,
    RoomWorkToJSON,
    TodoProjectFromJSON,
    TodoProjectToJSON,
} from '../models/index.js';

export interface TodoApiDeleteTodoProjectsByKeyRequest {
    key: string;
}

export interface TodoApiGetTodoBoardRequest {
    key?: string;
    status?: string;
    kind?: string;
    repo?: string;
    label?: string;
    source?: string;
    scheduled?: boolean;
}

export interface TodoApiGetTodoIssuesRequest {
    q?: string;
    project?: string;
    status?: string;
    kind?: string;
    repo?: string;
    room?: string;
    source?: string;
    assignee?: string;
    limit?: number;
}

export interface TodoApiGetTodoProjectsByKeyRequest {
    key: string;
}

export interface TodoApiGetTodoProjectsByKeyIssuesRequest {
    key: string;
    status?: string;
    kind?: string;
    repo?: string;
    label?: string;
    source?: string;
    scheduled?: boolean;
}

export interface TodoApiGetTodoProjectsByKeyIssuesByNumRequest {
    key: string;
    num: number;
}

export interface TodoApiGetTodoRoomsByRoomRequest {
    room: string;
}

export interface TodoApiPatchTodoProjectsByKeyRequest {
    key: string;
}

export interface TodoApiPatchTodoProjectsByKeyIssuesByNumRequest {
    key: string;
    num: number;
    issueEdit: IssueEdit;
}

export interface TodoApiPostTodoProjectsByKeyIssuesRequest {
    key: string;
    newIssue: NewIssue;
}

export interface TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest {
    key: string;
    num: number;
}

/**
 * 
 */
export class TodoApi extends runtime.BaseAPI {

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second endpoint on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * Refused — a board is a repository on the forge
     */
    async deleteTodoProjectsByKeyRaw(requestParameters: TodoApiDeleteTodoProjectsByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling deleteTodoProjectsByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second endpoint on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * Refused — a board is a repository on the forge
     */
    async deleteTodoProjectsByKey(requestParameters: TodoApiDeleteTodoProjectsByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTodoProjectsByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
     */
    async getTodoBoardRaw(requestParameters: TodoApiGetTodoBoardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<IssueView>>> {
        const queryParameters: any = {};

        if (requestParameters['key'] != null) {
            queryParameters['key'] = requestParameters['key'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        if (requestParameters['label'] != null) {
            queryParameters['label'] = requestParameters['label'];
        }

        if (requestParameters['source'] != null) {
            queryParameters['source'] = requestParameters['source'];
        }

        if (requestParameters['scheduled'] != null) {
            queryParameters['scheduled'] = requestParameters['scheduled'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/board`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(IssueViewFromJSON));
    }

    /**
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
     */
    async getTodoBoard(requestParameters: TodoApiGetTodoBoardRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<IssueView>> {
        const response = await this.getTodoBoardRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers across every project in the org.  The org comes from the validated principal and never from the request: a caller able to name the org could read another tenant\'s backlog, and a search is exactly the shape that would quietly return it.
     * Answers across every project in the org.
     */
    async getTodoIssuesRaw(requestParameters: TodoApiGetTodoIssuesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IssueHits>> {
        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        if (requestParameters['room'] != null) {
            queryParameters['room'] = requestParameters['room'];
        }

        if (requestParameters['source'] != null) {
            queryParameters['source'] = requestParameters['source'];
        }

        if (requestParameters['assignee'] != null) {
            queryParameters['assignee'] = requestParameters['assignee'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/issues`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IssueHitsFromJSON(jsonValue));
    }

    /**
     * Answers across every project in the org.  The org comes from the validated principal and never from the request: a caller able to name the org could read another tenant\'s backlog, and a search is exactly the shape that would quietly return it.
     * Answers across every project in the org.
     */
    async getTodoIssues(requestParameters: TodoApiGetTodoIssuesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IssueHits> {
        const response = await this.getTodoIssuesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the boards of your org — the places your work actually is. The key addresses the board\'s issues.  A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is assembled from the work itself: the repositories your org has filed issues on, plus the boards the index holds. A repository with nothing on it is not in the list and is still perfectly addressable — GET /projects/<name> reads it and a create files into it — so nothing is lost by leaving it out.  Measured, which is why: reading the forge\'s whole repository inventory put 745 boards here, of which all but a handful were vendored forks and mirrors (.github, .profile, DOMPurify, BoatAttack) that will never carry this org\'s work. A list that long is not a list — the estate\'s real roadmap was in it somewhere and no one could see it.  The forge half is the FORGE\'s answer for your own account, so two people in one org can legitimately see different boards.
     * Returns the boards of your org — the places your work actually is.
     */
    async getTodoProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<TodoProject>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(TodoProjectFromJSON));
    }

    /**
     * Returns the boards of your org — the places your work actually is. The key addresses the board\'s issues.  A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is assembled from the work itself: the repositories your org has filed issues on, plus the boards the index holds. A repository with nothing on it is not in the list and is still perfectly addressable — GET /projects/<name> reads it and a create files into it — so nothing is lost by leaving it out.  Measured, which is why: reading the forge\'s whole repository inventory put 745 boards here, of which all but a handful were vendored forks and mirrors (.github, .profile, DOMPurify, BoatAttack) that will never carry this org\'s work. A list that long is not a list — the estate\'s real roadmap was in it somewhere and no one could see it.  The forge half is the FORGE\'s answer for your own account, so two people in one org can legitimately see different boards.
     * Returns the boards of your org — the places your work actually is.
     */
    async getTodoProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<TodoProject>> {
        const response = await this.getTodoProjectsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one board of your org by its key — the repository name. 404 when your org has no repository under that key, or when your own forge account cannot see it.
     * Returns one board of your org by its key — the repository name.
     */
    async getTodoProjectsByKeyRaw(requestParameters: TodoApiGetTodoProjectsByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TodoProject>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getTodoProjectsByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TodoProjectFromJSON(jsonValue));
    }

    /**
     * Returns one board of your org by its key — the repository name. 404 when your org has no repository under that key, or when your own forge account cannot see it.
     * Returns one board of your org by its key — the repository name.
     */
    async getTodoProjectsByKey(requestParameters: TodoApiGetTodoProjectsByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TodoProject> {
        const response = await this.getTodoProjectsByKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
     */
    async getTodoProjectsByKeyIssuesRaw(requestParameters: TodoApiGetTodoProjectsByKeyIssuesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<IssueView>>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getTodoProjectsByKeyIssues().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        if (requestParameters['repo'] != null) {
            queryParameters['repo'] = requestParameters['repo'];
        }

        if (requestParameters['label'] != null) {
            queryParameters['label'] = requestParameters['label'];
        }

        if (requestParameters['source'] != null) {
            queryParameters['source'] = requestParameters['source'];
        }

        if (requestParameters['scheduled'] != null) {
            queryParameters['scheduled'] = requestParameters['scheduled'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}/issues`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(IssueViewFromJSON));
    }

    /**
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
     */
    async getTodoProjectsByKeyIssues(requestParameters: TodoApiGetTodoProjectsByKeyIssuesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<IssueView>> {
        const response = await this.getTodoProjectsByKeyIssuesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns ONE work item in full — its description included.  The list reads answer a board, and a board is a summary: the description is where the actual content of a work item lives — what an issue asks for, what an epic\'s acceptance criteria are — and no read on this surface returned it. The address is the one PATCH already accepts, so an item you can move is now an item you can read.  It reads the forge directly rather than filtering the org fan-out, then falls back to the index for a board the forge has never heard of — the same order, and the same reason, as GetProject: a row is one kind of thing however it came to exist, so a caller does not have to know which store it is in to fetch it.
     * Returns ONE work item in full — its description included.
     */
    async getTodoProjectsByKeyIssuesByNumRaw(requestParameters: TodoApiGetTodoProjectsByKeyIssuesByNumRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IssueView>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling getTodoProjectsByKeyIssuesByNum().'
            );
        }

        if (requestParameters['num'] == null) {
            throw new runtime.RequiredError(
                'num',
                'Required parameter "num" was null or undefined when calling getTodoProjectsByKeyIssuesByNum().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}/issues/{num}`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));
        urlPath = urlPath.replace(`{${"num"}}`, encodeURIComponent(String(requestParameters['num'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IssueViewFromJSON(jsonValue));
    }

    /**
     * Returns ONE work item in full — its description included.  The list reads answer a board, and a board is a summary: the description is where the actual content of a work item lives — what an issue asks for, what an epic\'s acceptance criteria are — and no read on this surface returned it. The address is the one PATCH already accepts, so an item you can move is now an item you can read.  It reads the forge directly rather than filtering the org fan-out, then falls back to the index for a board the forge has never heard of — the same order, and the same reason, as GetProject: a row is one kind of thing however it came to exist, so a caller does not have to know which store it is in to fetch it.
     * Returns ONE work item in full — its description included.
     */
    async getTodoProjectsByKeyIssuesByNum(requestParameters: TodoApiGetTodoProjectsByKeyIssuesByNumRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IssueView> {
        const response = await this.getTodoProjectsByKeyIssuesByNumRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Summarises one room\'s work.  The room is opaque here and is deliberately not resolved: this package cannot say whether a room exists — apps/team owns that document — so an unknown room answers an EMPTY board rather than a 404. That is the honest answer and the useful one: a channel that has never had an item filed in it and a channel id that was mistyped both have no work, and inventing a distinction would require this surface to hold a second copy of the room list (HIP-0523 §2 forbids it, and it would drift the first time a room was renamed).  Tenancy is the validated principal\'s org and nothing else, so a caller cannot read another tenant\'s channel by naming its room.
     * Summarises one room\'s work.
     */
    async getTodoRoomsByRoomRaw(requestParameters: TodoApiGetTodoRoomsByRoomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RoomWork>> {
        if (requestParameters['room'] == null) {
            throw new runtime.RequiredError(
                'room',
                'Required parameter "room" was null or undefined when calling getTodoRoomsByRoom().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/rooms/{room}`;
        urlPath = urlPath.replace(`{${"room"}}`, encodeURIComponent(String(requestParameters['room'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RoomWorkFromJSON(jsonValue));
    }

    /**
     * Summarises one room\'s work.  The room is opaque here and is deliberately not resolved: this package cannot say whether a room exists — apps/team owns that document — so an unknown room answers an EMPTY board rather than a 404. That is the honest answer and the useful one: a channel that has never had an item filed in it and a channel id that was mistyped both have no work, and inventing a distinction would require this surface to hold a second copy of the room list (HIP-0523 §2 forbids it, and it would drift the first time a room was renamed).  Tenancy is the validated principal\'s org and nothing else, so a caller cannot read another tenant\'s channel by naming its room.
     * Summarises one room\'s work.
     */
    async getTodoRoomsByRoom(requestParameters: TodoApiGetTodoRoomsByRoomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RoomWork> {
        const response = await this.getTodoRoomsByRoomRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second endpoint on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * Refused — a board is a repository on the forge
     */
    async patchTodoProjectsByKeyRaw(requestParameters: TodoApiPatchTodoProjectsByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling patchTodoProjectsByKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second endpoint on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * Refused — a board is a repository on the forge
     */
    async patchTodoProjectsByKey(requestParameters: TodoApiPatchTodoProjectsByKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchTodoProjectsByKeyRaw(requestParameters, initOverrides);
    }

    /**
     * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it. Absent fields are left alone.  MOVING A CARD IS A RELABEL. The column lives in the forge\'s label set, so the move replaces that set rather than writing a status column here that a forge-side change could contradict. Moving to `done` also CLOSES the issue on the forge, because a done card and an open issue are a contradiction.
     * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it.
     */
    async patchTodoProjectsByKeyIssuesByNumRaw(requestParameters: TodoApiPatchTodoProjectsByKeyIssuesByNumRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IssueView>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling patchTodoProjectsByKeyIssuesByNum().'
            );
        }

        if (requestParameters['num'] == null) {
            throw new runtime.RequiredError(
                'num',
                'Required parameter "num" was null or undefined when calling patchTodoProjectsByKeyIssuesByNum().'
            );
        }

        if (requestParameters['issueEdit'] == null) {
            throw new runtime.RequiredError(
                'issueEdit',
                'Required parameter "issueEdit" was null or undefined when calling patchTodoProjectsByKeyIssuesByNum().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}/issues/{num}`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));
        urlPath = urlPath.replace(`{${"num"}}`, encodeURIComponent(String(requestParameters['num'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: IssueEditToJSON(requestParameters['issueEdit']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IssueViewFromJSON(jsonValue));
    }

    /**
     * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it. Absent fields are left alone.  MOVING A CARD IS A RELABEL. The column lives in the forge\'s label set, so the move replaces that set rather than writing a status column here that a forge-side change could contradict. Moving to `done` also CLOSES the issue on the forge, because a done card and an open issue are a contradiction.
     * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it.
     */
    async patchTodoProjectsByKeyIssuesByNum(requestParameters: TodoApiPatchTodoProjectsByKeyIssuesByNumRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IssueView> {
        const response = await this.patchTodoProjectsByKeyIssuesByNumRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second endpoint on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * Refused — a board is a repository on the forge
     */
    async postTodoProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second endpoint on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * Refused — a board is a repository on the forge
     */
    async postTodoProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postTodoProjectsRaw(initOverrides);
    }

    /**
     * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.  The column and priority are written as LABELS, which is what makes the card and the forge issue the same object: someone relabelling in the forge web UI has moved your card.
     * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.
     */
    async postTodoProjectsByKeyIssuesRaw(requestParameters: TodoApiPostTodoProjectsByKeyIssuesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IssueView>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling postTodoProjectsByKeyIssues().'
            );
        }

        if (requestParameters['newIssue'] == null) {
            throw new runtime.RequiredError(
                'newIssue',
                'Required parameter "newIssue" was null or undefined when calling postTodoProjectsByKeyIssues().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}/issues`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NewIssueToJSON(requestParameters['newIssue']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IssueViewFromJSON(jsonValue));
    }

    /**
     * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.  The column and priority are written as LABELS, which is what makes the card and the forge issue the same object: someone relabelling in the forge web UI has moved your card.
     * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.
     */
    async postTodoProjectsByKeyIssues(requestParameters: TodoApiPostTodoProjectsByKeyIssuesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IssueView> {
        const response = await this.postTodoProjectsByKeyIssuesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Takes an issue: it becomes yours and it moves to in_progress.  The holder is the CALLER, never an argument. \"Assign this to someone else\" is a different act with different authority, and it already exists as a PATCH; conflating them would let anyone hand work to anyone by naming them.  Claiming something already held by someone else is refused rather than silently taken — two agents on one issue is the failure this prevents, and a claim that quietly wins a race is worse than one that says no.
     * Takes an issue: it becomes yours and it moves to in_progress.
     */
    async postTodoProjectsByKeyIssuesByNumClaimRaw(requestParameters: TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IssueHit>> {
        if (requestParameters['key'] == null) {
            throw new runtime.RequiredError(
                'key',
                'Required parameter "key" was null or undefined when calling postTodoProjectsByKeyIssuesByNumClaim().'
            );
        }

        if (requestParameters['num'] == null) {
            throw new runtime.RequiredError(
                'num',
                'Required parameter "num" was null or undefined when calling postTodoProjectsByKeyIssuesByNumClaim().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/todo/projects/{key}/issues/{num}/claim`;
        urlPath = urlPath.replace(`{${"key"}}`, encodeURIComponent(String(requestParameters['key'])));
        urlPath = urlPath.replace(`{${"num"}}`, encodeURIComponent(String(requestParameters['num'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IssueHitFromJSON(jsonValue));
    }

    /**
     * Takes an issue: it becomes yours and it moves to in_progress.  The holder is the CALLER, never an argument. \"Assign this to someone else\" is a different act with different authority, and it already exists as a PATCH; conflating them would let anyone hand work to anyone by naming them.  Claiming something already held by someone else is refused rather than silently taken — two agents on one issue is the failure this prevents, and a claim that quietly wins a race is worse than one that says no.
     * Takes an issue: it becomes yours and it moves to in_progress.
     */
    async postTodoProjectsByKeyIssuesByNumClaim(requestParameters: TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IssueHit> {
        const response = await this.postTodoProjectsByKeyIssuesByNumClaimRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
