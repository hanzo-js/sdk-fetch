/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  HelpArticle,
  HelpArticleList,
  HelpCategoryList,
  HelpTicketFiled,
  HelpTicketIntake,
} from '../models/index.js';
import {
    HelpArticleFromJSON,
    HelpArticleToJSON,
    HelpArticleListFromJSON,
    HelpArticleListToJSON,
    HelpCategoryListFromJSON,
    HelpCategoryListToJSON,
    HelpTicketFiledFromJSON,
    HelpTicketFiledToJSON,
    HelpTicketIntakeFromJSON,
    HelpTicketIntakeToJSON,
} from '../models/index.js';

export interface HelpApiGetHelpArticlesRequest {
    category?: string;
    limit?: number;
}

export interface HelpApiGetHelpArticlesBySlugRequest {
    slug: string;
}

export interface HelpApiPostHelpTicketsRequest {
    helpTicketIntake: HelpTicketIntake;
}

/**
 * 
 */
export class HelpApi extends runtime.BaseAPI {

    /**
     * Returns the public knowledge base: the help center\'s Published, publicly-visible articles as cards. The org is server-fixed and the status/is_public filter is server-set, so neither the tenant nor the visibility can be widened by the caller. A deployment with no help center answers 404.
     * Returns the public knowledge base: the help center\'s Published, publicly-visible articles as cards.
     */
    async getHelpArticlesRaw(requestParameters: HelpApiGetHelpArticlesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HelpArticleList>> {
        const queryParameters: any = {};

        if (requestParameters['category'] != null) {
            queryParameters['category'] = requestParameters['category'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/help/articles`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HelpArticleListFromJSON(jsonValue));
    }

    /**
     * Returns the public knowledge base: the help center\'s Published, publicly-visible articles as cards. The org is server-fixed and the status/is_public filter is server-set, so neither the tenant nor the visibility can be widened by the caller. A deployment with no help center answers 404.
     * Returns the public knowledge base: the help center\'s Published, publicly-visible articles as cards.
     */
    async getHelpArticles(requestParameters: HelpApiGetHelpArticlesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HelpArticleList> {
        const response = await this.getHelpArticlesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one public article by slug, with its body. A missing, Draft, or internal (non-public) article is 404 — fail-closed, so this route is no existence oracle for anything beyond \"published and public\".
     * Returns one public article by slug, with its body.
     */
    async getHelpArticlesBySlugRaw(requestParameters: HelpApiGetHelpArticlesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HelpArticle>> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling getHelpArticlesBySlug().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/help/articles/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HelpArticleFromJSON(jsonValue));
    }

    /**
     * Returns one public article by slug, with its body. A missing, Draft, or internal (non-public) article is 404 — fail-closed, so this route is no existence oracle for anything beyond \"published and public\".
     * Returns one public article by slug, with its body.
     */
    async getHelpArticlesBySlug(requestParameters: HelpApiGetHelpArticlesBySlugRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HelpArticle> {
        const response = await this.getHelpArticlesBySlugRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the knowledge-base sections for the public center\'s navigation — but ONLY the sections that front at least one Published, public article, so an internal (agent-only) category name or description never leaks. A section with no public article is invisible; a center with no public articles has no sections, which is an empty list rather than an error.
     * Returns the knowledge-base sections for the public center\'s navigation — but ONLY the sections that front at least one Published, public article, so an internal (agent-only) category name or description never leaks.
     */
    async getHelpCategoriesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HelpCategoryList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/help/categories`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HelpCategoryListFromJSON(jsonValue));
    }

    /**
     * Returns the knowledge-base sections for the public center\'s navigation — but ONLY the sections that front at least one Published, public article, so an internal (agent-only) category name or description never leaks. A section with no public article is invisible; a center with no public articles has no sections, which is an empty list rather than an error.
     * Returns the knowledge-base sections for the public center\'s navigation — but ONLY the sections that front at least one Published, public article, so an internal (agent-only) category name or description never leaks.
     */
    async getHelpCategories(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HelpCategoryList> {
        const response = await this.getHelpCategoriesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Files a customer support ticket into the public help center. It creates the ticket (status Open, source portal) with the customer\'s message on the description, then records that same message as the opening entry of the ticket\'s conversation thread; the description carries it regardless, so failing to write that entry loses nothing. Answers 201 with an opaque reference.  A deployment with no help center answers 404, one whose center has not installed the Help model answers 503, and a body over 64 KiB answers 413 — in that order, which is the order the route has always decided them in.
     * Files a customer support ticket into the public help center.
     */
    async postHelpTicketsRaw(requestParameters: HelpApiPostHelpTicketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HelpTicketFiled>> {
        if (requestParameters['helpTicketIntake'] == null) {
            throw new runtime.RequiredError(
                'helpTicketIntake',
                'Required parameter "helpTicketIntake" was null or undefined when calling postHelpTickets().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/help/tickets`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: HelpTicketIntakeToJSON(requestParameters['helpTicketIntake']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HelpTicketFiledFromJSON(jsonValue));
    }

    /**
     * Files a customer support ticket into the public help center. It creates the ticket (status Open, source portal) with the customer\'s message on the description, then records that same message as the opening entry of the ticket\'s conversation thread; the description carries it regardless, so failing to write that entry loses nothing. Answers 201 with an opaque reference.  A deployment with no help center answers 404, one whose center has not installed the Help model answers 503, and a body over 64 KiB answers 413 — in that order, which is the order the route has always decided them in.
     * Files a customer support ticket into the public help center.
     */
    async postHelpTickets(requestParameters: HelpApiPostHelpTicketsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HelpTicketFiled> {
        const response = await this.postHelpTicketsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
