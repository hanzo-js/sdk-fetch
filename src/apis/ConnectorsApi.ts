/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ConnectorProvidersOut,
  ConnectorTokenOut,
  ConnectorsOut,
  CredentialIn,
  CredentialOut,
  DevicePollOut,
  DeviceStartIn,
  DeviceStartOut,
  DisconnectOut,
  RefreshOut,
} from '../models/index.js';
import {
    ConnectorProvidersOutFromJSON,
    ConnectorProvidersOutToJSON,
    ConnectorTokenOutFromJSON,
    ConnectorTokenOutToJSON,
    ConnectorsOutFromJSON,
    ConnectorsOutToJSON,
    CredentialInFromJSON,
    CredentialInToJSON,
    CredentialOutFromJSON,
    CredentialOutToJSON,
    DevicePollOutFromJSON,
    DevicePollOutToJSON,
    DeviceStartInFromJSON,
    DeviceStartInToJSON,
    DeviceStartOutFromJSON,
    DeviceStartOutToJSON,
    DisconnectOutFromJSON,
    DisconnectOutToJSON,
    RefreshOutFromJSON,
    RefreshOutToJSON,
} from '../models/index.js';

export interface ConnectorsApiDeleteConnectorsByIdRequest {
    id: string;
}

export interface ConnectorsApiGetConnectorsByIdTokenRequest {
    id: string;
}

export interface ConnectorsApiPostConnectorsByIdRefreshRequest {
    id: string;
}

export interface ConnectorsApiPostConnectorsByProviderCredentialRequest {
    provider: string;
    credentialIn: CredentialIn;
}

export interface ConnectorsApiPostConnectorsByProviderDeviceRequest {
    provider: string;
    deviceStartIn: DeviceStartIn;
}

export interface ConnectorsApiPostConnectorsByProviderDeviceByFlowPollRequest {
    provider: string;
    flow: string;
}

/**
 * 
 */
export class ConnectorsApi extends runtime.BaseAPI {

    /**
     * Forgets a connector: every custodied secret, then the row. Idempotent — dropping a never-connected id still answers {disconnected:true} (disconnect() parity). No provider Revoke: none of the user-plane providers exposes a revoke endpoint.
     * Forgets a connector: every custodied secret, then the row.
     */
    async deleteConnectorsByIdRaw(requestParameters: ConnectorsApiDeleteConnectorsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DisconnectOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteConnectorsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DisconnectOutFromJSON(jsonValue));
    }

    /**
     * Forgets a connector: every custodied secret, then the row. Idempotent — dropping a never-connected id still answers {disconnected:true} (disconnect() parity). No provider Revoke: none of the user-plane providers exposes a revoke endpoint.
     * Forgets a connector: every custodied secret, then the row.
     */
    async deleteConnectorsById(requestParameters: ConnectorsApiDeleteConnectorsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DisconnectOut> {
        const response = await this.deleteConnectorsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller\'s OWN connectors across every provider — the set `hanzo connector ls` prints. Rows are keyed (org,user), so this can never surface another user\'s connector, and no secret is in the view.
     * Lists the caller\'s OWN connectors across every provider — the set `hanzo connector ls` prints.
     */
    async getConnectorsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConnectorsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConnectorsOutFromJSON(jsonValue));
    }

    /**
     * Lists the caller\'s OWN connectors across every provider — the set `hanzo connector ls` prints. Rows are keyed (org,user), so this can never surface another user\'s connector, and no secret is in the view.
     * Lists the caller\'s OWN connectors across every provider — the set `hanzo connector ls` prints.
     */
    async getConnectors(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConnectorsOut> {
        const response = await this.getConnectorsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Hands the custodied access token to its owner — the ONE place custody exits. The (org,user)-keyed row IS the same-user gate: another user\'s id is simply \"no row\" → 404. fresh() auto-rotates within the refreshSkew window; static providers degenerate to a plain kmsGet of Secrets[0]. Refresh tokens are NEVER returned — custody keeps the sink. The token is never logged.
     * Hands the custodied access token to its owner — the ONE place custody exits.
     */
    async getConnectorsByIdTokenRaw(requestParameters: ConnectorsApiGetConnectorsByIdTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConnectorTokenOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getConnectorsByIdToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/{id}/token`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConnectorTokenOutFromJSON(jsonValue));
    }

    /**
     * Hands the custodied access token to its owner — the ONE place custody exits. The (org,user)-keyed row IS the same-user gate: another user\'s id is simply \"no row\" → 404. fresh() auto-rotates within the refreshSkew window; static providers degenerate to a plain kmsGet of Secrets[0]. Refresh tokens are NEVER returned — custody keeps the sink. The token is never logged.
     * Hands the custodied access token to its owner — the ONE place custody exits.
     */
    async getConnectorsByIdToken(requestParameters: ConnectorsApiGetConnectorsByIdTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConnectorTokenOut> {
        const response = await this.getConnectorsByIdTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the user-scoped provider cards — the catalog of what a user can connect, and how. Methods derive from capabilities (Device/Adopt/Verify — Mount asserts at least one), never from a parallel kind enum.
     * Lists the user-scoped provider cards — the catalog of what a user can connect, and how.
     */
    async getConnectorsProvidersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConnectorProvidersOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/providers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConnectorProvidersOutFromJSON(jsonValue));
    }

    /**
     * Lists the user-scoped provider cards — the catalog of what a user can connect, and how. Methods derive from capabilities (Device/Adopt/Verify — Mount asserts at least one), never from a parallel kind enum.
     * Lists the user-scoped provider cards — the catalog of what a user can connect, and how.
     */
    async getConnectorsProviders(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConnectorProvidersOut> {
        const response = await this.getConnectorsProvidersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Forces a token rotation for a connected connector, ahead of the automatic rotation a token read would do inside the expiry window. Only providers that declare a Refresh support it.
     * Forces a token rotation for a connected connector, ahead of the automatic rotation a token read would do inside the expiry window.
     */
    async postConnectorsByIdRefreshRaw(requestParameters: ConnectorsApiPostConnectorsByIdRefreshRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefreshOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postConnectorsByIdRefresh().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/{id}/refresh`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefreshOutFromJSON(jsonValue));
    }

    /**
     * Forces a token rotation for a connected connector, ahead of the automatic rotation a token read would do inside the expiry window. Only providers that declare a Refresh support it.
     * Forces a token rotation for a connected connector, ahead of the automatic rotation a token read would do inside the expiry window.
     */
    async postConnectorsByIdRefresh(requestParameters: ConnectorsApiPostConnectorsByIdRefreshRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefreshOut> {
        const response = await this.postConnectorsByIdRefreshRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the direct intake path: a customer-held token/setup-token (Verify) or an externally obtained OAuth bundle from the CLI\'s local PKCE (Adopt). ALWAYS verify-before-store: a bad credential is refused and NOTHING is persisted (connectByCredential\'s fail-closed order).
     * Is the direct intake path: a customer-held token/setup-token (Verify) or an externally obtained OAuth bundle from the CLI\'s local PKCE (Adopt).
     */
    async postConnectorsByProviderCredentialRaw(requestParameters: ConnectorsApiPostConnectorsByProviderCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CredentialOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling postConnectorsByProviderCredential().'
            );
        }

        if (requestParameters['credentialIn'] == null) {
            throw new runtime.RequiredError(
                'credentialIn',
                'Required parameter "credentialIn" was null or undefined when calling postConnectorsByProviderCredential().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/{provider}/credential`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CredentialInToJSON(requestParameters['credentialIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CredentialOutFromJSON(jsonValue));
    }

    /**
     * Is the direct intake path: a customer-held token/setup-token (Verify) or an externally obtained OAuth bundle from the CLI\'s local PKCE (Adopt). ALWAYS verify-before-store: a bad credential is refused and NOTHING is persisted (connectByCredential\'s fail-closed order).
     * Is the direct intake path: a customer-held token/setup-token (Verify) or an externally obtained OAuth bundle from the CLI\'s local PKCE (Adopt).
     */
    async postConnectorsByProviderCredential(requestParameters: ConnectorsApiPostConnectorsByProviderCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CredentialOut> {
        const response = await this.postConnectorsByProviderCredentialRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Begins a device sign-in and returns the code to show the user plus how to poll for completion. KMS readiness is checked NOW rather than dead-ending the user at poll-done (connect() parity), and the per-provider connector cap is checked before the provider is called. The provider\'s device code is persisted only in the encrypted grants table and is NEVER returned.
     * Begins a device sign-in and returns the code to show the user plus how to poll for completion.
     */
    async postConnectorsByProviderDeviceRaw(requestParameters: ConnectorsApiPostConnectorsByProviderDeviceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeviceStartOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling postConnectorsByProviderDevice().'
            );
        }

        if (requestParameters['deviceStartIn'] == null) {
            throw new runtime.RequiredError(
                'deviceStartIn',
                'Required parameter "deviceStartIn" was null or undefined when calling postConnectorsByProviderDevice().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/{provider}/device`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DeviceStartInToJSON(requestParameters['deviceStartIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeviceStartOutFromJSON(jsonValue));
    }

    /**
     * Begins a device sign-in and returns the code to show the user plus how to poll for completion. KMS readiness is checked NOW rather than dead-ending the user at poll-done (connect() parity), and the per-provider connector cap is checked before the provider is called. The provider\'s device code is persisted only in the encrypted grants table and is NEVER returned.
     * Begins a device sign-in and returns the code to show the user plus how to poll for completion.
     */
    async postConnectorsByProviderDevice(requestParameters: ConnectorsApiPostConnectorsByProviderDeviceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeviceStartOut> {
        const response = await this.postConnectorsByProviderDeviceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Advances a device sign-in. Terminal outcomes are DATA, not errors (verifyConn {active:false} discipline) — the status set is closed: pending|connected|denied|expired. pollSlow collapses to \"pending\" on the wire; the raised cadence rides interval.
     * Advances a device sign-in.
     */
    async postConnectorsByProviderDeviceByFlowPollRaw(requestParameters: ConnectorsApiPostConnectorsByProviderDeviceByFlowPollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DevicePollOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling postConnectorsByProviderDeviceByFlowPoll().'
            );
        }

        if (requestParameters['flow'] == null) {
            throw new runtime.RequiredError(
                'flow',
                'Required parameter "flow" was null or undefined when calling postConnectorsByProviderDeviceByFlowPoll().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/connectors/{provider}/device/{flow}/poll`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));
        urlPath = urlPath.replace(`{${"flow"}}`, encodeURIComponent(String(requestParameters['flow'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DevicePollOutFromJSON(jsonValue));
    }

    /**
     * Advances a device sign-in. Terminal outcomes are DATA, not errors (verifyConn {active:false} discipline) — the status set is closed: pending|connected|denied|expired. pollSlow collapses to \"pending\" on the wire; the raised cadence rides interval.
     * Advances a device sign-in.
     */
    async postConnectorsByProviderDeviceByFlowPoll(requestParameters: ConnectorsApiPostConnectorsByProviderDeviceByFlowPollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DevicePollOut> {
        const response = await this.postConnectorsByProviderDeviceByFlowPollRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
