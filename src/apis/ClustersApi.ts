/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ClusterAttach,
  ClusterDetached,
  ClusterList,
  ClusterView,
  NodePoolView,
  PoolCreate,
  PoolScale,
} from '../models/index.js';
import {
    ClusterAttachFromJSON,
    ClusterAttachToJSON,
    ClusterDetachedFromJSON,
    ClusterDetachedToJSON,
    ClusterListFromJSON,
    ClusterListToJSON,
    ClusterViewFromJSON,
    ClusterViewToJSON,
    NodePoolViewFromJSON,
    NodePoolViewToJSON,
    PoolCreateFromJSON,
    PoolCreateToJSON,
    PoolScaleFromJSON,
    PoolScaleToJSON,
} from '../models/index.js';

export interface ClustersApiAttachClusterRequest {
    clusterAttach: ClusterAttach;
}

export interface ClustersApiCreateNodePoolRequest {
    clusterId: string;
    poolCreate: PoolCreate;
}

export interface ClustersApiDeleteNodePoolRequest {
    clusterId: string;
    poolId: string;
    provider?: string;
}

export interface ClustersApiDetachClusterRequest {
    id: string;
}

export interface ClustersApiScaleNodePoolRequest {
    clusterId: string;
    poolId: string;
    poolScale: PoolScale;
}

/**
 * 
 */
export class ClustersApi extends runtime.BaseAPI {

    /**
     * Attaches a BYO cluster to the caller\'s org — the kubeconfig is validated, KMS-sealed and added to the fleet — and answers 201 with the cluster as it now appears on GET /v1/clusters. Billed the nominal management fee: the customer brings the compute, Hanzo meters the management plane.
     * Attaches a BYO cluster to the caller\'s org — the kubeconfig is validated, KMS-sealed and added to the fleet — and answers 201 with the cluster as it now appears on GET /v1/clusters.
     */
    async attachClusterRaw(requestParameters: ClustersApiAttachClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ClusterView>> {
        if (requestParameters['clusterAttach'] == null) {
            throw new runtime.RequiredError(
                'clusterAttach',
                'Required parameter "clusterAttach" was null or undefined when calling attachCluster().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/clusters`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ClusterAttachToJSON(requestParameters['clusterAttach']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ClusterViewFromJSON(jsonValue));
    }

    /**
     * Attaches a BYO cluster to the caller\'s org — the kubeconfig is validated, KMS-sealed and added to the fleet — and answers 201 with the cluster as it now appears on GET /v1/clusters. Billed the nominal management fee: the customer brings the compute, Hanzo meters the management plane.
     * Attaches a BYO cluster to the caller\'s org — the kubeconfig is validated, KMS-sealed and added to the fleet — and answers 201 with the cluster as it now appears on GET /v1/clusters.
     */
    async attachCluster(requestParameters: ClustersApiAttachClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ClusterView> {
        const response = await this.attachClusterRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a node pool to one of the caller org\'s clusters and answers 201 with the created pool. Only the CreateNodePoolSpec fields are forwarded; owner/provider/clusterId ride in the query exactly as Visor expects them.
     * Adds a node pool to one of the caller org\'s clusters and answers 201 with the created pool.
     */
    async createNodePoolRaw(requestParameters: ClustersApiCreateNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NodePoolView>> {
        if (requestParameters['clusterId'] == null) {
            throw new runtime.RequiredError(
                'clusterId',
                'Required parameter "clusterId" was null or undefined when calling createNodePool().'
            );
        }

        if (requestParameters['poolCreate'] == null) {
            throw new runtime.RequiredError(
                'poolCreate',
                'Required parameter "poolCreate" was null or undefined when calling createNodePool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/clusters/{clusterId}/pools`;
        urlPath = urlPath.replace(`{${"clusterId"}}`, encodeURIComponent(String(requestParameters['clusterId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PoolCreateToJSON(requestParameters['poolCreate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NodePoolViewFromJSON(jsonValue));
    }

    /**
     * Adds a node pool to one of the caller org\'s clusters and answers 201 with the created pool. Only the CreateNodePoolSpec fields are forwarded; owner/provider/clusterId ride in the query exactly as Visor expects them.
     * Adds a node pool to one of the caller org\'s clusters and answers 201 with the created pool.
     */
    async createNodePool(requestParameters: ClustersApiCreateNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NodePoolView> {
        const response = await this.createNodePoolRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes a node pool from one of the caller org\'s clusters. The owner scopes the delete to the caller\'s tenant; provider+clusterId drive the provider-side removal. Answers 204.
     * Removes a node pool from one of the caller org\'s clusters.
     */
    async deleteNodePoolRaw(requestParameters: ClustersApiDeleteNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['clusterId'] == null) {
            throw new runtime.RequiredError(
                'clusterId',
                'Required parameter "clusterId" was null or undefined when calling deleteNodePool().'
            );
        }

        if (requestParameters['poolId'] == null) {
            throw new runtime.RequiredError(
                'poolId',
                'Required parameter "poolId" was null or undefined when calling deleteNodePool().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/clusters/{clusterId}/pools/{poolId}`;
        urlPath = urlPath.replace(`{${"clusterId"}}`, encodeURIComponent(String(requestParameters['clusterId'])));
        urlPath = urlPath.replace(`{${"poolId"}}`, encodeURIComponent(String(requestParameters['poolId'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a node pool from one of the caller org\'s clusters. The owner scopes the delete to the caller\'s tenant; provider+clusterId drive the provider-side removal. Answers 204.
     * Removes a node pool from one of the caller org\'s clusters.
     */
    async deleteNodePool(requestParameters: ClustersApiDeleteNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteNodePoolRaw(requestParameters, initOverrides);
    }

    /**
     * Removes a BYO cluster from the caller org\'s fleet. It only ever touches BYO clusters — a managed cluster\'s nodes are removed through the node-pool routes — and answers 404 when the name is not in this org\'s fleet.
     * Removes a BYO cluster from the caller org\'s fleet.
     */
    async detachClusterRaw(requestParameters: ClustersApiDetachClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ClusterDetached>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling detachCluster().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/clusters/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ClusterDetachedFromJSON(jsonValue));
    }

    /**
     * Removes a BYO cluster from the caller org\'s fleet. It only ever touches BYO clusters — a managed cluster\'s nodes are removed through the node-pool routes — and answers 404 when the name is not in this org\'s fleet.
     * Removes a BYO cluster from the caller org\'s fleet.
     */
    async detachCluster(requestParameters: ClustersApiDetachClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ClusterDetached> {
        const response = await this.detachClusterRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s clusters from both sources: the managed clusters projected from Visor\'s node pools, and the BYO clusters attached to the caller\'s project. A Visor outage costs the managed half only — the BYO half still lists, because a page that 502s on an optional provider is worse than a page that shows what it can.
     * Returns the caller org\'s clusters from both sources: the managed clusters projected from Visor\'s node pools, and the BYO clusters attached to the caller\'s project.
     */
    async listClustersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ClusterList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/clusters`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ClusterListFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s clusters from both sources: the managed clusters projected from Visor\'s node pools, and the BYO clusters attached to the caller\'s project. A Visor outage costs the managed half only — the BYO half still lists, because a page that 502s on an optional provider is worse than a page that shows what it can.
     * Returns the caller org\'s clusters from both sources: the managed clusters projected from Visor\'s node pools, and the BYO clusters attached to the caller\'s project.
     */
    async listClusters(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ClusterList> {
        const response = await this.listClustersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Resizes a node pool to an absolute node count and returns the pool as Visor reports it after the change.
     * Resizes a node pool to an absolute node count and returns the pool as Visor reports it after the change.
     */
    async scaleNodePoolRaw(requestParameters: ClustersApiScaleNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NodePoolView>> {
        if (requestParameters['clusterId'] == null) {
            throw new runtime.RequiredError(
                'clusterId',
                'Required parameter "clusterId" was null or undefined when calling scaleNodePool().'
            );
        }

        if (requestParameters['poolId'] == null) {
            throw new runtime.RequiredError(
                'poolId',
                'Required parameter "poolId" was null or undefined when calling scaleNodePool().'
            );
        }

        if (requestParameters['poolScale'] == null) {
            throw new runtime.RequiredError(
                'poolScale',
                'Required parameter "poolScale" was null or undefined when calling scaleNodePool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/clusters/{clusterId}/pools/{poolId}/scale`;
        urlPath = urlPath.replace(`{${"clusterId"}}`, encodeURIComponent(String(requestParameters['clusterId'])));
        urlPath = urlPath.replace(`{${"poolId"}}`, encodeURIComponent(String(requestParameters['poolId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PoolScaleToJSON(requestParameters['poolScale']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NodePoolViewFromJSON(jsonValue));
    }

    /**
     * Resizes a node pool to an absolute node count and returns the pool as Visor reports it after the change.
     * Resizes a node pool to an absolute node count and returns the pool as Visor reports it after the change.
     */
    async scaleNodePool(requestParameters: ClustersApiScaleNodePoolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NodePoolView> {
        const response = await this.scaleNodePoolRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
