/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  BlobJSON,
  CommitsJSON,
  CreateReq,
  FilesJSON,
  GcOut,
  KeyList,
  KeyView,
  MirrorList,
  MirrorReq,
  MirrorTargetReq,
  MirrorTargetView,
  OpenReq,
  PatchIn,
  PullList,
  PullView,
  PushReq,
  PushResp,
  ReadmeJSON,
  RefsJSON,
  RegisterKeyReq,
  RepoList,
  RepoView,
  SubscribeReq,
  SubscriptionList,
  SubscriptionView,
  TreeJSON,
  UsageView,
  ZapProcReq,
} from '../models/index.js';
import {
    BlobJSONFromJSON,
    BlobJSONToJSON,
    CommitsJSONFromJSON,
    CommitsJSONToJSON,
    CreateReqFromJSON,
    CreateReqToJSON,
    FilesJSONFromJSON,
    FilesJSONToJSON,
    GcOutFromJSON,
    GcOutToJSON,
    KeyListFromJSON,
    KeyListToJSON,
    KeyViewFromJSON,
    KeyViewToJSON,
    MirrorListFromJSON,
    MirrorListToJSON,
    MirrorReqFromJSON,
    MirrorReqToJSON,
    MirrorTargetReqFromJSON,
    MirrorTargetReqToJSON,
    MirrorTargetViewFromJSON,
    MirrorTargetViewToJSON,
    OpenReqFromJSON,
    OpenReqToJSON,
    PatchInFromJSON,
    PatchInToJSON,
    PullListFromJSON,
    PullListToJSON,
    PullViewFromJSON,
    PullViewToJSON,
    PushReqFromJSON,
    PushReqToJSON,
    PushRespFromJSON,
    PushRespToJSON,
    ReadmeJSONFromJSON,
    ReadmeJSONToJSON,
    RefsJSONFromJSON,
    RefsJSONToJSON,
    RegisterKeyReqFromJSON,
    RegisterKeyReqToJSON,
    RepoListFromJSON,
    RepoListToJSON,
    RepoViewFromJSON,
    RepoViewToJSON,
    SubscribeReqFromJSON,
    SubscribeReqToJSON,
    SubscriptionListFromJSON,
    SubscriptionListToJSON,
    SubscriptionViewFromJSON,
    SubscriptionViewToJSON,
    TreeJSONFromJSON,
    TreeJSONToJSON,
    UsageViewFromJSON,
    UsageViewToJSON,
    ZapProcReqFromJSON,
    ZapProcReqToJSON,
} from '../models/index.js';

export interface GitApiDeleteGitKeysByIdRequest {
    id: string;
}

export interface GitApiDeleteGitReposByNameRequest {
    name: string;
}

export interface GitApiDeleteGitReposByNameMirrorsByIdRequest {
    name: string;
    id: string;
}

export interface GitApiDeleteGitReposByNameSubscriptionsByIdRequest {
    name: string;
    id: string;
}

export interface GitApiGetGitByOrgByProjectByRepoInfoRefsRequest {
    org: string;
    project: string;
    repo: string;
}

export interface GitApiGetGitByOrgByRepoInfoRefsRequest {
    org: string;
    repo: string;
}

export interface GitApiGetGitReposByNameRequest {
    name: string;
}

export interface GitApiGetGitReposByNameBlobRequest {
    name: string;
    ref?: string;
    path?: string;
}

export interface GitApiGetGitReposByNameCommitsRequest {
    name: string;
    ref?: string;
    path?: string;
    limit?: number;
}

export interface GitApiGetGitReposByNameFilesRequest {
    name: string;
    ref?: string;
    glob?: string;
}

export interface GitApiGetGitReposByNameMirrorsRequest {
    name: string;
}

export interface GitApiGetGitReposByNamePullsRequest {
    name: string;
    state?: string;
}

export interface GitApiGetGitReposByNamePullsByNumberRequest {
    name: string;
    number: number;
}

export interface GitApiGetGitReposByNameReadmeRequest {
    name: string;
    ref?: string;
}

export interface GitApiGetGitReposByNameRefsRequest {
    name: string;
}

export interface GitApiGetGitReposByNameSubscriptionsRequest {
    name: string;
}

export interface GitApiGetGitReposByNameTreeRequest {
    name: string;
    ref?: string;
    path?: string;
}

export interface GitApiPatchGitReposByNameRequest {
    name: string;
    patchIn: PatchIn;
}

export interface GitApiPostGitByOrgByProjectByRepoGitReceivePackRequest {
    org: string;
    project: string;
    repo: string;
    body?: Blob;
}

export interface GitApiPostGitByOrgByProjectByRepoGitUploadPackRequest {
    org: string;
    project: string;
    repo: string;
    body?: Blob;
}

export interface GitApiPostGitByOrgByRepoGitReceivePackRequest {
    org: string;
    repo: string;
    body?: Blob;
}

export interface GitApiPostGitByOrgByRepoGitUploadPackRequest {
    org: string;
    repo: string;
    body?: Blob;
}

export interface GitApiPostGitKeysRequest {
    registerKeyReq: RegisterKeyReq;
}

export interface GitApiPostGitReposRequest {
    createReq: CreateReq;
}

export interface GitApiPostGitReposByNameGcRequest {
    name: string;
}

export interface GitApiPostGitReposByNameMirrorRequest {
    name: string;
    mirrorReq: MirrorReq;
}

export interface GitApiPostGitReposByNameMirrorsRequest {
    name: string;
    mirrorTargetReq: MirrorTargetReq;
}

export interface GitApiPostGitReposByNamePullsRequest {
    name: string;
    openReq: OpenReq;
}

export interface GitApiPostGitReposByNamePullsByNumberMergeRequest {
    name: string;
    number: number;
}

export interface GitApiPostGitReposByNamePushRequest {
    name: string;
    pushReq: PushReq;
}

export interface GitApiPostGitReposByNameSubscriptionsRequest {
    name: string;
    subscribeReq: SubscribeReq;
}

export interface GitApiPostGitZapCreaterepoRequest {
    zapProcReq?: ZapProcReq;
}

export interface GitApiPostGitZapDeleterepoRequest {
    zapProcReq?: ZapProcReq;
}

export interface GitApiPostGitZapGetrepoRequest {
    zapProcReq?: ZapProcReq;
}

/**
 * 
 */
export class GitApi extends runtime.BaseAPI {

    /**
     * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found. Answers 204 with no body. Once removed the key no longer authenticates any SSH git access.
     * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found.
     */
    async deleteGitKeysByIdRaw(requestParameters: GitApiDeleteGitKeysByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteGitKeysById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/keys/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found. Answers 204 with no body. Once removed the key no longer authenticates any SSH git access.
     * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found.
     */
    async deleteGitKeysById(requestParameters: GitApiDeleteGitKeysByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteGitKeysByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes a repo\'s metadata and purges its storage. Answers 204 with no body. The metadata row is the source of truth for existence, so a storage purge that fails is logged and the delete still succeeds — and a second call is a 404, not a second delete.
     * Removes a repo\'s metadata and purges its storage.
     */
    async deleteGitReposByNameRaw(requestParameters: GitApiDeleteGitReposByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteGitReposByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a repo\'s metadata and purges its storage. Answers 204 with no body. The metadata row is the source of truth for existence, so a storage purge that fails is logged and the delete still succeeds — and a second call is a 404, not a second delete.
     * Removes a repo\'s metadata and purges its storage.
     */
    async deleteGitReposByName(requestParameters: GitApiDeleteGitReposByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteGitReposByNameRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one outbound mirror target; later pushes stop being forwarded to it. Answers 204 with no body. Nothing is done to the downstream remote itself — only this repo\'s intent to push there is dropped.
     * Removes one outbound mirror target; later pushes stop being forwarded to it.
     */
    async deleteGitReposByNameMirrorsByIdRaw(requestParameters: GitApiDeleteGitReposByNameMirrorsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteGitReposByNameMirrorsById().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteGitReposByNameMirrorsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/mirrors/{id}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one outbound mirror target; later pushes stop being forwarded to it. Answers 204 with no body. Nothing is done to the downstream remote itself — only this repo\'s intent to push there is dropped.
     * Removes one outbound mirror target; later pushes stop being forwarded to it.
     */
    async deleteGitReposByNameMirrorsById(requestParameters: GitApiDeleteGitReposByNameMirrorsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteGitReposByNameMirrorsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel. Answers 204 with no body. An id that is not this repo\'s subscription is not found.
     * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel.
     */
    async deleteGitReposByNameSubscriptionsByIdRaw(requestParameters: GitApiDeleteGitReposByNameSubscriptionsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteGitReposByNameSubscriptionsById().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteGitReposByNameSubscriptionsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/subscriptions/{id}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel. Answers 204 with no body. An id that is not this repo\'s subscription is not found.
     * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel.
     */
    async deleteGitReposByNameSubscriptionsById(requestParameters: GitApiDeleteGitReposByNameSubscriptionsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteGitReposByNameSubscriptionsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Advertise a repository\'s refs to a git client
     */
    async getGitByOrgByProjectByRepoInfoRefsRaw(requestParameters: GitApiGetGitByOrgByProjectByRepoInfoRefsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling getGitByOrgByProjectByRepoInfoRefs().'
            );
        }

        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getGitByOrgByProjectByRepoInfoRefs().'
            );
        }

        if (requestParameters['repo'] == null) {
            throw new runtime.RequiredError(
                'repo',
                'Required parameter "repo" was null or undefined when calling getGitByOrgByProjectByRepoInfoRefs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/{org}/{project}/{repo}/info/refs`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"repo"}}`, encodeURIComponent(String(requestParameters['repo'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Advertise a repository\'s refs to a git client
     */
    async getGitByOrgByProjectByRepoInfoRefs(requestParameters: GitApiGetGitByOrgByProjectByRepoInfoRefsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getGitByOrgByProjectByRepoInfoRefsRaw(requestParameters, initOverrides);
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Advertise a repository\'s refs to a git client
     */
    async getGitByOrgByRepoInfoRefsRaw(requestParameters: GitApiGetGitByOrgByRepoInfoRefsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling getGitByOrgByRepoInfoRefs().'
            );
        }

        if (requestParameters['repo'] == null) {
            throw new runtime.RequiredError(
                'repo',
                'Required parameter "repo" was null or undefined when calling getGitByOrgByRepoInfoRefs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/{org}/{repo}/info/refs`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"repo"}}`, encodeURIComponent(String(requestParameters['repo'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Advertise a repository\'s refs to a git client
     */
    async getGitByOrgByRepoInfoRefs(requestParameters: GitApiGetGitByOrgByRepoInfoRefsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getGitByOrgByRepoInfoRefsRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped on read even though the fingerprint index is global, so one org never sees another\'s.
     * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`.
     */
    async getGitKeysRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KeyList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/keys`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KeyListFromJSON(jsonValue));
    }

    /**
     * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped on read even though the fingerprint index is global, so one org never sees another\'s.
     * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`.
     */
    async getGitKeys(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KeyList> {
        const response = await this.getGitKeysRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the repos in the caller\'s scope, most recently updated first. The scope is the request principal\'s — the gateway-minted org and its optional project — never anything off the wire, so a caller only ever sees its own. Rows carry no branches or HEAD; read one repo for those.
     * Returns the repos in the caller\'s scope, most recently updated first.
     */
    async getGitReposRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RepoList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RepoListFromJSON(jsonValue));
    }

    /**
     * Returns the repos in the caller\'s scope, most recently updated first. The scope is the request principal\'s — the gateway-minted org and its optional project — never anything off the wire, so a caller only ever sees its own. Rows carry no branches or HEAD; read one repo for those.
     * Returns the repos in the caller\'s scope, most recently updated first.
     */
    async getGitRepos(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RepoList> {
        const response = await this.getGitReposRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one repo with its live ref state: every branch name and the resolved HEAD commit. Both are read from the object store on each call, so an empty repo reports no branches and an empty head rather than failing. A repo outside the caller\'s scope is not found.
     * Returns one repo with its live ref state: every branch name and the resolved HEAD commit.
     */
    async getGitReposByNameRaw(requestParameters: GitApiGetGitReposByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RepoView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RepoViewFromJSON(jsonValue));
    }

    /**
     * Returns one repo with its live ref state: every branch name and the resolved HEAD commit. Both are read from the object store on each call, so an empty repo reports no branches and an empty head rather than failing. A repo outside the caller\'s scope is not found.
     * Returns one repo with its live ref state: every branch name and the resolved HEAD commit.
     */
    async getGitReposByName(requestParameters: GitApiGetGitReposByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RepoView> {
        const response = await this.getGitReposByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one file\'s bytes at one revision. Text comes back verbatim, binary comes back base64, and a file past the 1 MiB view cap comes back marked truncated with NO content — the client is expected to clone instead.
     * Returns one file\'s bytes at one revision.
     */
    async getGitReposByNameBlobRaw(requestParameters: GitApiGetGitReposByNameBlobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BlobJSON>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameBlob().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['ref'] != null) {
            queryParameters['ref'] = requestParameters['ref'];
        }

        if (requestParameters['path'] != null) {
            queryParameters['path'] = requestParameters['path'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/blob`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BlobJSONFromJSON(jsonValue));
    }

    /**
     * Returns one file\'s bytes at one revision. Text comes back verbatim, binary comes back base64, and a file past the 1 MiB view cap comes back marked truncated with NO content — the client is expected to clone instead.
     * Returns one file\'s bytes at one revision.
     */
    async getGitReposByNameBlob(requestParameters: GitApiGetGitReposByNameBlobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BlobJSON> {
        const response = await this.getGitReposByNameBlobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Walks a ref\'s history newest first, or one path\'s history when a path is given. There is no cursor: the page is the newest `limit` commits.
     * Walks a ref\'s history newest first, or one path\'s history when a path is given.
     */
    async getGitReposByNameCommitsRaw(requestParameters: GitApiGetGitReposByNameCommitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CommitsJSON>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameCommits().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['ref'] != null) {
            queryParameters['ref'] = requestParameters['ref'];
        }

        if (requestParameters['path'] != null) {
            queryParameters['path'] = requestParameters['path'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/commits`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CommitsJSONFromJSON(jsonValue));
    }

    /**
     * Walks a ref\'s history newest first, or one path\'s history when a path is given. There is no cursor: the page is the newest `limit` commits.
     * Walks a ref\'s history newest first, or one path\'s history when a path is given.
     */
    async getGitReposByNameCommits(requestParameters: GitApiGetGitReposByNameCommitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CommitsJSON> {
        const response = await this.getGitReposByNameCommitsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from. It is the read a delivery generator makes: one call answers \"what is the inventory at this commit, and what does it say\", where listing and then fetching would be a request per file.  Returning the resolved revision matters as much as the bytes. A generator that lists at `main` and then reads at `main` can straddle a push and assemble half its inventory from one commit and half from the next; resolving once makes the whole read consistent by construction.  A file past the read cap comes back Truncated with no content rather than being dropped. A caller building a desired set has to know the difference between \"this file is empty\" and \"this file was not read\" — silently omitting it is how a pruning reconcile deletes what the missing file declared.
     * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from.
     */
    async getGitReposByNameFilesRaw(requestParameters: GitApiGetGitReposByNameFilesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FilesJSON>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameFiles().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['ref'] != null) {
            queryParameters['ref'] = requestParameters['ref'];
        }

        if (requestParameters['glob'] != null) {
            queryParameters['glob'] = requestParameters['glob'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/files`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FilesJSONFromJSON(jsonValue));
    }

    /**
     * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from. It is the read a delivery generator makes: one call answers \"what is the inventory at this commit, and what does it say\", where listing and then fetching would be a request per file.  Returning the resolved revision matters as much as the bytes. A generator that lists at `main` and then reads at `main` can straddle a push and assemble half its inventory from one commit and half from the next; resolving once makes the whole read consistent by construction.  A file past the read cap comes back Truncated with no content rather than being dropped. A caller building a desired set has to know the difference between \"this file is empty\" and \"this file was not read\" — silently omitting it is how a pruning reconcile deletes what the missing file declared.
     * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from.
     */
    async getGitReposByNameFiles(requestParameters: GitApiGetGitReposByNameFilesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FilesJSON> {
        const response = await this.getGitReposByNameFilesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
     * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
     */
    async getGitReposByNameMirrorsRaw(requestParameters: GitApiGetGitReposByNameMirrorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MirrorList>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameMirrors().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/mirrors`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MirrorListFromJSON(jsonValue));
    }

    /**
     * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
     * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
     */
    async getGitReposByNameMirrors(requestParameters: GitApiGetGitReposByNameMirrorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MirrorList> {
        const response = await this.getGitReposByNameMirrorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a repo\'s pull requests, newest number first — what is waiting to be reviewed, and what has already landed. Narrow it with ?state=open or ?state=merged; omit state for every proposal.
     * Returns a repo\'s pull requests, newest number first — what is waiting to be reviewed, and what has already landed.
     */
    async getGitReposByNamePullsRaw(requestParameters: GitApiGetGitReposByNamePullsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PullList>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNamePulls().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/pulls`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PullListFromJSON(jsonValue));
    }

    /**
     * Returns a repo\'s pull requests, newest number first — what is waiting to be reviewed, and what has already landed. Narrow it with ?state=open or ?state=merged; omit state for every proposal.
     * Returns a repo\'s pull requests, newest number first — what is waiting to be reviewed, and what has already landed.
     */
    async getGitReposByNamePulls(requestParameters: GitApiGetGitReposByNamePullsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PullList> {
        const response = await this.getGitReposByNamePullsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one pull request by its per-repo number. A number belonging to another tenant\'s repo is not found, exactly as the repo itself is not.
     * Returns one pull request by its per-repo number.
     */
    async getGitReposByNamePullsByNumberRaw(requestParameters: GitApiGetGitReposByNamePullsByNumberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PullView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNamePullsByNumber().'
            );
        }

        if (requestParameters['number'] == null) {
            throw new runtime.RequiredError(
                'number',
                'Required parameter "number" was null or undefined when calling getGitReposByNamePullsByNumber().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/pulls/{number}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));
        urlPath = urlPath.replace(`{${"number"}}`, encodeURIComponent(String(requestParameters['number'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PullViewFromJSON(jsonValue));
    }

    /**
     * Returns one pull request by its per-repo number. A number belonging to another tenant\'s repo is not found, exactly as the repo itself is not.
     * Returns one pull request by its per-repo number.
     */
    async getGitReposByNamePullsByNumber(requestParameters: GitApiGetGitReposByNamePullsByNumberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PullView> {
        const response = await this.getGitReposByNamePullsByNumberRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it. A repo with no README is not found.
     * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it.
     */
    async getGitReposByNameReadmeRaw(requestParameters: GitApiGetGitReposByNameReadmeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReadmeJSON>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameReadme().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['ref'] != null) {
            queryParameters['ref'] = requestParameters['ref'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/readme`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReadmeJSONFromJSON(jsonValue));
    }

    /**
     * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it. A repo with no README is not found.
     * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it.
     */
    async getGitReposByNameReadme(requestParameters: GitApiGetGitReposByNameReadmeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReadmeJSON> {
        const response = await this.getGitReposByNameReadmeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call. Unlike the other read ops it tolerates a repo with no commits: the ref sets come back empty and the default branch is still named.
     * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call.
     */
    async getGitReposByNameRefsRaw(requestParameters: GitApiGetGitReposByNameRefsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefsJSON>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameRefs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/refs`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefsJSONFromJSON(jsonValue));
    }

    /**
     * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call. Unlike the other read ops it tolerates a repo with no commits: the ref sets come back empty and the default branch is still named.
     * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call.
     */
    async getGitReposByNameRefs(requestParameters: GitApiGetGitReposByNameRefsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefsJSON> {
        const response = await this.getGitReposByNameRefsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
     * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
     */
    async getGitReposByNameSubscriptionsRaw(requestParameters: GitApiGetGitReposByNameSubscriptionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubscriptionList>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameSubscriptions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/subscriptions`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubscriptionListFromJSON(jsonValue));
    }

    /**
     * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
     * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
     */
    async getGitReposByNameSubscriptions(requestParameters: GitApiGetGitReposByNameSubscriptionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubscriptionList> {
        const response = await this.getGitReposByNameSubscriptionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the immediate children of one directory at one revision, directories before files. It does not recurse — walk down a level at a time.
     * Lists the immediate children of one directory at one revision, directories before files.
     */
    async getGitReposByNameTreeRaw(requestParameters: GitApiGetGitReposByNameTreeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TreeJSON>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getGitReposByNameTree().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['ref'] != null) {
            queryParameters['ref'] = requestParameters['ref'];
        }

        if (requestParameters['path'] != null) {
            queryParameters['path'] = requestParameters['path'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/tree`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TreeJSONFromJSON(jsonValue));
    }

    /**
     * Lists the immediate children of one directory at one revision, directories before files. It does not recurse — walk down a level at a time.
     * Lists the immediate children of one directory at one revision, directories before files.
     */
    async getGitReposByNameTree(requestParameters: GitApiGetGitReposByNameTreeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TreeJSON> {
        const response = await this.getGitReposByNameTreeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on. It spans EVERY project sub-scope, unlike the repo list, so a billing consumer sees the whole tenant footprint in one call. Sizes are last-measured values (create, push, mirror and gc each re-measure), not a live walk of the disk.
     * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on.
     */
    async getGitUsageRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageView>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/usage`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageViewFromJSON(jsonValue));
    }

    /**
     * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on. It spans EVERY project sub-scope, unlike the repo list, so a billing consumer sees the whole tenant footprint in one call. Sizes are last-measured values (create, push, mirror and gc each re-measure), not a live walk of the disk.
     * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on.
     */
    async getGitUsage(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageView> {
        const response = await this.getGitUsageRaw(initOverrides);
        return await response.value();
    }

    /**
     * Flips a repo\'s public bit, the one mutable repo setting today. Public grants ANONYMOUS fetch only; push and the whole control plane stay org-authed. Returns the updated repo.
     * Flips a repo\'s public bit, the one mutable repo setting today.
     */
    async patchGitReposByNameRaw(requestParameters: GitApiPatchGitReposByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RepoView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling patchGitReposByName().'
            );
        }

        if (requestParameters['patchIn'] == null) {
            throw new runtime.RequiredError(
                'patchIn',
                'Required parameter "patchIn" was null or undefined when calling patchGitReposByName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchInToJSON(requestParameters['patchIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RepoViewFromJSON(jsonValue));
    }

    /**
     * Flips a repo\'s public bit, the one mutable repo setting today. Public grants ANONYMOUS fetch only; push and the whole control plane stay org-authed. Returns the updated repo.
     * Flips a repo\'s public bit, the one mutable repo setting today.
     */
    async patchGitReposByName(requestParameters: GitApiPatchGitReposByNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RepoView> {
        const response = await this.patchGitReposByNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Accept a push, and turn it into a build
     */
    async postGitByOrgByProjectByRepoGitReceivePackRaw(requestParameters: GitApiPostGitByOrgByProjectByRepoGitReceivePackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling postGitByOrgByProjectByRepoGitReceivePack().'
            );
        }

        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postGitByOrgByProjectByRepoGitReceivePack().'
            );
        }

        if (requestParameters['repo'] == null) {
            throw new runtime.RequiredError(
                'repo',
                'Required parameter "repo" was null or undefined when calling postGitByOrgByProjectByRepoGitReceivePack().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/{org}/{project}/{repo}/git-receive-pack`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"repo"}}`, encodeURIComponent(String(requestParameters['repo'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Accept a push, and turn it into a build
     */
    async postGitByOrgByProjectByRepoGitReceivePack(requestParameters: GitApiPostGitByOrgByProjectByRepoGitReceivePackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitByOrgByProjectByRepoGitReceivePackRaw(requestParameters, initOverrides);
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Serve a clone or fetch
     */
    async postGitByOrgByProjectByRepoGitUploadPackRaw(requestParameters: GitApiPostGitByOrgByProjectByRepoGitUploadPackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling postGitByOrgByProjectByRepoGitUploadPack().'
            );
        }

        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postGitByOrgByProjectByRepoGitUploadPack().'
            );
        }

        if (requestParameters['repo'] == null) {
            throw new runtime.RequiredError(
                'repo',
                'Required parameter "repo" was null or undefined when calling postGitByOrgByProjectByRepoGitUploadPack().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/{org}/{project}/{repo}/git-upload-pack`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));
        urlPath = urlPath.replace(`{${"repo"}}`, encodeURIComponent(String(requestParameters['repo'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Serve a clone or fetch
     */
    async postGitByOrgByProjectByRepoGitUploadPack(requestParameters: GitApiPostGitByOrgByProjectByRepoGitUploadPackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitByOrgByProjectByRepoGitUploadPackRaw(requestParameters, initOverrides);
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Accept a push, and turn it into a build
     */
    async postGitByOrgByRepoGitReceivePackRaw(requestParameters: GitApiPostGitByOrgByRepoGitReceivePackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling postGitByOrgByRepoGitReceivePack().'
            );
        }

        if (requestParameters['repo'] == null) {
            throw new runtime.RequiredError(
                'repo',
                'Required parameter "repo" was null or undefined when calling postGitByOrgByRepoGitReceivePack().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/{org}/{repo}/git-receive-pack`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"repo"}}`, encodeURIComponent(String(requestParameters['repo'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Accept a push, and turn it into a build
     */
    async postGitByOrgByRepoGitReceivePack(requestParameters: GitApiPostGitByOrgByRepoGitReceivePackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitByOrgByRepoGitReceivePackRaw(requestParameters, initOverrides);
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Serve a clone or fetch
     */
    async postGitByOrgByRepoGitUploadPackRaw(requestParameters: GitApiPostGitByOrgByRepoGitUploadPackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['org'] == null) {
            throw new runtime.RequiredError(
                'org',
                'Required parameter "org" was null or undefined when calling postGitByOrgByRepoGitUploadPack().'
            );
        }

        if (requestParameters['repo'] == null) {
            throw new runtime.RequiredError(
                'repo',
                'Required parameter "repo" was null or undefined when calling postGitByOrgByRepoGitUploadPack().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/octet-stream';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/{org}/{repo}/git-upload-pack`;
        urlPath = urlPath.replace(`{${"org"}}`, encodeURIComponent(String(requestParameters['org'])));
        urlPath = urlPath.replace(`{${"repo"}}`, encodeURIComponent(String(requestParameters['repo'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * Serve a clone or fetch
     */
    async postGitByOrgByRepoGitUploadPack(requestParameters: GitApiPostGitByOrgByRepoGitUploadPackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitByOrgByRepoGitUploadPackRaw(requestParameters, initOverrides);
    }

    /**
     * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org. The key line is parsed and canonicalized before storage, its SHA256 fingerprint becomes the auth lookup handle, and the full public key round-trips (it is public). Answers 201. Fingerprints are globally unique, so a key already registered — to this org or any other — is a 409: one key belongs to exactly one org.
     * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org.
     */
    async postGitKeysRaw(requestParameters: GitApiPostGitKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KeyView>> {
        if (requestParameters['registerKeyReq'] == null) {
            throw new runtime.RequiredError(
                'registerKeyReq',
                'Required parameter "registerKeyReq" was null or undefined when calling postGitKeys().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/keys`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RegisterKeyReqToJSON(requestParameters['registerKeyReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KeyViewFromJSON(jsonValue));
    }

    /**
     * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org. The key line is parsed and canonicalized before storage, its SHA256 fingerprint becomes the auth lookup handle, and the full public key round-trips (it is public). Answers 201. Fingerprints are globally unique, so a key already registered — to this org or any other — is a 409: one key belongs to exactly one org.
     * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org.
     */
    async postGitKeys(requestParameters: GitApiPostGitKeysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KeyView> {
        const response = await this.postGitKeysRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs. Answers 201. The name must be unique within the scope — a repeat is a 409, never a silent overwrite of an existing repo. The org comes from the validated principal, so a repo is always born owned by the caller\'s own tenant.
     * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs.
     */
    async postGitReposRaw(requestParameters: GitApiPostGitReposRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RepoView>> {
        if (requestParameters['createReq'] == null) {
            throw new runtime.RequiredError(
                'createReq',
                'Required parameter "createReq" was null or undefined when calling postGitRepos().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateReqToJSON(requestParameters['createReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RepoViewFromJSON(jsonValue));
    }

    /**
     * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs. Answers 201. The name must be unique within the scope — a repeat is a 409, never a silent overwrite of an existing repo. The org comes from the validated principal, so a repo is always born owned by the caller\'s own tenant.
     * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs.
     */
    async postGitRepos(requestParameters: GitApiPostGitReposRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RepoView> {
        const response = await this.postGitReposRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph. Idempotent, and safe to interrupt — git swaps both artifacts atomically. It runs under one pack slot with the same memory bounds as a clone, so it can block behind heavy pack traffic rather than compete with it. Storage usage is re-measured afterwards, since a repack reclaims space.
     * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph.
     */
    async postGitReposByNameGcRaw(requestParameters: GitApiPostGitReposByNameGcRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GcOut>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNameGc().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/gc`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GcOutFromJSON(jsonValue));
    }

    /**
     * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph. Idempotent, and safe to interrupt — git swaps both artifacts atomically. It runs under one pack slot with the same memory bounds as a clone, so it can block behind heavy pack traffic rather than compete with it. Storage usage is re-measured afterwards, since a repack reclaims space.
     * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph.
     */
    async postGitReposByNameGc(requestParameters: GitApiPostGitReposByNameGcRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GcOut> {
        const response = await this.postGitReposByNameGcRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Imports an external git repository into the caller\'s repo, provisioning it on first use. Fetch is FORCED and covers every ref, so a first call clones the source and a repeat call re-syncs it — the endpoint is idempotent by mirror semantics. Mirrored bytes are metered exactly like a push, and a push.landed event is emitted for the default branch so the code index picks the repo up.
     * Imports an external git repository into the caller\'s repo, provisioning it on first use.
     */
    async postGitReposByNameMirrorRaw(requestParameters: GitApiPostGitReposByNameMirrorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RepoView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNameMirror().'
            );
        }

        if (requestParameters['mirrorReq'] == null) {
            throw new runtime.RequiredError(
                'mirrorReq',
                'Required parameter "mirrorReq" was null or undefined when calling postGitReposByNameMirror().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/mirror`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MirrorReqToJSON(requestParameters['mirrorReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RepoViewFromJSON(jsonValue));
    }

    /**
     * Imports an external git repository into the caller\'s repo, provisioning it on first use. Fetch is FORCED and covers every ref, so a first call clones the source and a repeat call re-syncs it — the endpoint is idempotent by mirror semantics. Mirrored bytes are metered exactly like a push, and a push.landed event is emitted for the default branch so the code index picks the repo up.
     * Imports an external git repository into the caller\'s repo, provisioning it on first use.
     */
    async postGitReposByNameMirror(requestParameters: GitApiPostGitReposByNameMirrorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RepoView> {
        const response = await this.postGitReposByNameMirrorRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here. Answers 201. The URL must be https to a host on the mirror allowlist (github.com / gitlab.com): the same set the mirror credential may be sent to, so a target can never capture the shared token or point the push at an internal service. Any embedded userinfo is stripped — credentials ride env-only at push time and never enter the stored URL. One mirror per host per repo; a second is a 409.
     * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here.
     */
    async postGitReposByNameMirrorsRaw(requestParameters: GitApiPostGitReposByNameMirrorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MirrorTargetView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNameMirrors().'
            );
        }

        if (requestParameters['mirrorTargetReq'] == null) {
            throw new runtime.RequiredError(
                'mirrorTargetReq',
                'Required parameter "mirrorTargetReq" was null or undefined when calling postGitReposByNameMirrors().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/mirrors`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MirrorTargetReqToJSON(requestParameters['mirrorTargetReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MirrorTargetViewFromJSON(jsonValue));
    }

    /**
     * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here. Answers 201. The URL must be https to a host on the mirror allowlist (github.com / gitlab.com): the same set the mirror credential may be sent to, so a target can never capture the shared token or point the push at an internal service. Any embedded userinfo is stripped — credentials ride env-only at push time and never enter the stored URL. One mirror per host per repo; a second is a 409.
     * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here.
     */
    async postGitReposByNameMirrors(requestParameters: GitApiPostGitReposByNameMirrorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MirrorTargetView> {
        const response = await this.postGitReposByNameMirrorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Proposes a branch for merging and returns it with its number. Answers 201. Both branches must already exist — a proposal naming a branch nobody pushed is a typo, not a plan — and base defaults to the repo\'s default branch.  Proposing the same head into the same base twice is a 409 while the first proposal is still open, so a retried agent run leaves ONE thing to review rather than a pile of identical ones. A repo outside the caller\'s scope is a 404, exactly as reading it is.
     * Proposes a branch for merging and returns it with its number.
     */
    async postGitReposByNamePullsRaw(requestParameters: GitApiPostGitReposByNamePullsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PullView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNamePulls().'
            );
        }

        if (requestParameters['openReq'] == null) {
            throw new runtime.RequiredError(
                'openReq',
                'Required parameter "openReq" was null or undefined when calling postGitReposByNamePulls().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/pulls`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OpenReqToJSON(requestParameters['openReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PullViewFromJSON(jsonValue));
    }

    /**
     * Proposes a branch for merging and returns it with its number. Answers 201. Both branches must already exist — a proposal naming a branch nobody pushed is a typo, not a plan — and base defaults to the repo\'s default branch.  Proposing the same head into the same base twice is a 409 while the first proposal is still open, so a retried agent run leaves ONE thing to review rather than a pile of identical ones. A repo outside the caller\'s scope is a 404, exactly as reading it is.
     * Proposes a branch for merging and returns it with its number.
     */
    async postGitReposByNamePulls(requestParameters: GitApiPostGitReposByNamePullsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PullView> {
        const response = await this.postGitReposByNamePullsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Merges an open pull request by FAST-FORWARDING base to head, and answers the proposal in its merged state with the revision base now points at.  It merges only when base is already an ancestor of head — the case where head contains every commit base has, so moving the branch loses nothing and invents nothing. When base has moved on independently, this REFUSES with 409 and says so: a real three-way merge is not implemented here, and reporting one would claim a result these bytes do not produce. Rebase head onto base and merge again.  The move is judged by the same ref policy a `git push` of it would face, and fires the same build and notify reactions, so merging is not a way around either. Merging an already-merged proposal is a 409.
     * Merges an open pull request by FAST-FORWARDING base to head, and answers the proposal in its merged state with the revision base now points at.
     */
    async postGitReposByNamePullsByNumberMergeRaw(requestParameters: GitApiPostGitReposByNamePullsByNumberMergeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PullView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNamePullsByNumberMerge().'
            );
        }

        if (requestParameters['number'] == null) {
            throw new runtime.RequiredError(
                'number',
                'Required parameter "number" was null or undefined when calling postGitReposByNamePullsByNumberMerge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/pulls/{number}/merge`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));
        urlPath = urlPath.replace(`{${"number"}}`, encodeURIComponent(String(requestParameters['number'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PullViewFromJSON(jsonValue));
    }

    /**
     * Merges an open pull request by FAST-FORWARDING base to head, and answers the proposal in its merged state with the revision base now points at.  It merges only when base is already an ancestor of head — the case where head contains every commit base has, so moving the branch loses nothing and invents nothing. When base has moved on independently, this REFUSES with 409 and says so: a real three-way merge is not implemented here, and reporting one would claim a result these bytes do not produce. Rebase head onto base and merge again.  The move is judged by the same ref policy a `git push` of it would face, and fires the same build and notify reactions, so merging is not a way around either. Merging an already-merged proposal is a 409.
     * Merges an open pull request by FAST-FORWARDING base to head, and answers the proposal in its merged state with the revision base now points at.
     */
    async postGitReposByNamePullsByNumberMerge(requestParameters: GitApiPostGitReposByNamePullsByNumberMergeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PullView> {
        const response = await this.postGitReposByNamePullsByNumberMergeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push. The repo is CREATED on first push, the files are merged onto the branch tip (unlisted files survive), and the same push-to-deploy hook a real receive-pack fires is fired, so downstream this is indistinguishable from a `git push`.
     * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push.
     */
    async postGitReposByNamePushRaw(requestParameters: GitApiPostGitReposByNamePushRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PushResp>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNamePush().'
            );
        }

        if (requestParameters['pushReq'] == null) {
            throw new runtime.RequiredError(
                'pushReq',
                'Required parameter "pushReq" was null or undefined when calling postGitReposByNamePush().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/push`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PushReqToJSON(requestParameters['pushReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PushRespFromJSON(jsonValue));
    }

    /**
     * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push. The repo is CREATED on first push, the files are merged onto the branch tip (unlisted files survive), and the same push-to-deploy hook a real receive-pack fires is fired, so downstream this is indistinguishable from a `git push`.
     * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push.
     */
    async postGitReposByNamePush(requestParameters: GitApiPostGitReposByNamePushRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PushResp> {
        const response = await this.postGitReposByNamePushRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there. Answers 201. The same channel twice on one repo is a 409; a repo outside the caller\'s scope is a 404, exactly as reading it is.
     * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there.
     */
    async postGitReposByNameSubscriptionsRaw(requestParameters: GitApiPostGitReposByNameSubscriptionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubscriptionView>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling postGitReposByNameSubscriptions().'
            );
        }

        if (requestParameters['subscribeReq'] == null) {
            throw new runtime.RequiredError(
                'subscribeReq',
                'Required parameter "subscribeReq" was null or undefined when calling postGitReposByNameSubscriptions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/repos/{name}/subscriptions`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubscribeReqToJSON(requestParameters['subscribeReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubscriptionViewFromJSON(jsonValue));
    }

    /**
     * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there. Answers 201. The same channel twice on one repo is a 409; a repo outside the caller\'s scope is a 404, exactly as reading it is.
     * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there.
     */
    async postGitReposByNameSubscriptions(requestParameters: GitApiPostGitReposByNameSubscriptionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubscriptionView> {
        const response = await this.postGitReposByNameSubscriptionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * GONE (410). This was the canonical forge\'s push-to-deploy door, and it never dispatched a build in its life.  It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge\'s hook page, and nothing built.  Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.  The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as \'the API is switched off\' — the wrong conclusion this estate has already drawn twice.
     * Retired — forge pushes build via platform.hanzo.ai
     */
    async postGitWebhookRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/webhook`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * GONE (410). This was the canonical forge\'s push-to-deploy door, and it never dispatched a build in its life.  It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge\'s hook page, and nothing built.  Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.  The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as \'the API is switched off\' — the wrong conclusion this estate has already drawn twice.
     * Retired — forge pushes build via platform.hanzo.ai
     */
    async postGitWebhook(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitWebhookRaw(initOverrides);
    }

    /**
     * Creates a repository in the caller\'s org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Create a repository over the ZAP transport
     */
    async postGitZapCreaterepoRaw(requestParameters: GitApiPostGitZapCreaterepoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/zap/createRepo`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ZapProcReqToJSON(requestParameters['zapProcReq']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Creates a repository in the caller\'s org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Create a repository over the ZAP transport
     */
    async postGitZapCreaterepo(requestParameters: GitApiPostGitZapCreaterepoRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitZapCreaterepoRaw(requestParameters, initOverrides);
    }

    /**
     * Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller\'s org and project scope is a 404 envelope, so a delete can never reach another tenant\'s repository.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Delete a repository over the ZAP transport
     */
    async postGitZapDeleterepoRaw(requestParameters: GitApiPostGitZapDeleterepoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/zap/deleteRepo`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ZapProcReqToJSON(requestParameters['zapProcReq']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller\'s org and project scope is a 404 envelope, so a delete can never reach another tenant\'s repository.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Delete a repository over the ZAP transport
     */
    async postGitZapDeleterepo(requestParameters: GitApiPostGitZapDeleterepoRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitZapDeleterepoRaw(requestParameters, initOverrides);
    }

    /**
     * Answers a single repository\'s record, named by `name`. A repository outside the caller\'s org and project scope is a 404 envelope, the same answer one that does not exist gets.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Read one repository over the ZAP transport
     */
    async postGitZapGetrepoRaw(requestParameters: GitApiPostGitZapGetrepoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/zap/getRepo`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ZapProcReqToJSON(requestParameters['zapProcReq']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers a single repository\'s record, named by `name`. A repository outside the caller\'s org and project scope is a 404 envelope, the same answer one that does not exist gets.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Read one repository over the ZAP transport
     */
    async postGitZapGetrepo(requestParameters: GitApiPostGitZapGetrepoRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitZapGetrepoRaw(requestParameters, initOverrides);
    }

    /**
     * Answers every repository in the caller\'s org and project scope. It reads NO body — the scope is entirely the caller\'s identity — so a request with an empty object is correct.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * List your repositories over the ZAP transport
     */
    async postGitZapListreposRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/zap/listRepos`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers every repository in the caller\'s org and project scope. It reads NO body — the scope is entirely the caller\'s identity — so a request with an empty object is correct.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * List your repositories over the ZAP transport
     */
    async postGitZapListrepos(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitZapListreposRaw(initOverrides);
    }

    /**
     * Answers every repository in the caller\'s org with its size in bytes, plus the org\'s total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller\'s own org, so it is that org\'s footprint and never the fleet\'s.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Report your org\'s git storage footprint over the ZAP transport
     */
    async postGitZapUsageRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/git/zap/usage`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Answers every repository in the caller\'s org with its size in bytes, plus the org\'s total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller\'s own org, so it is that org\'s footprint and never the fleet\'s.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * Report your org\'s git storage footprint over the ZAP transport
     */
    async postGitZapUsage(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postGitZapUsageRaw(initOverrides);
    }

}
