/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  ClearReferenceOut,
  ReferenceOut,
  ReferenceSetsOut,
  RefreshReferenceIn,
  RefreshReferenceOut,
  ResolveReferenceIn,
  ResolveReferenceOut,
  SetReferenceIn,
  SetReferenceOut,
} from '../models/index.js';
import {
    ClearReferenceOutFromJSON,
    ClearReferenceOutToJSON,
    ReferenceOutFromJSON,
    ReferenceOutToJSON,
    ReferenceSetsOutFromJSON,
    ReferenceSetsOutToJSON,
    RefreshReferenceInFromJSON,
    RefreshReferenceInToJSON,
    RefreshReferenceOutFromJSON,
    RefreshReferenceOutToJSON,
    ResolveReferenceInFromJSON,
    ResolveReferenceInToJSON,
    ResolveReferenceOutFromJSON,
    ResolveReferenceOutToJSON,
    SetReferenceInFromJSON,
    SetReferenceInToJSON,
    SetReferenceOutFromJSON,
    SetReferenceOutToJSON,
} from '../models/index.js';

export interface ReferenceApiRiskClearReferenceRequest {
    set: string;
    key?: string;
}

export interface ReferenceApiRiskReferenceRequest {
    set: string;
    after?: string;
    limit?: number;
}

export interface ReferenceApiRiskRefreshReferenceRequest {
    refreshReferenceIn: RefreshReferenceIn;
}

export interface ReferenceApiRiskResolveReferenceRequest {
    resolveReferenceIn: ResolveReferenceIn;
}

export interface ReferenceApiRiskSetReferenceRequest {
    set: string;
    setReferenceIn: SetReferenceIn;
}

/**
 * 
 */
export class ReferenceApi extends runtime.BaseAPI {

    /**
     * Removes one of your organisation\'s overrides.  It removes an entry your organisation wrote, never a baseline member: the published set is not writable from here, so a removal can only ever restore the baseline\'s own answer.
     * Removes one of your organisation\'s overrides.
     */
    async riskClearReferenceRaw(requestParameters: ReferenceApiRiskClearReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ClearReferenceOut>> {
        if (requestParameters['set'] == null) {
            throw new runtime.RequiredError(
                'set',
                'Required parameter "set" was null or undefined when calling riskClearReference().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['key'] != null) {
            queryParameters['key'] = requestParameters['key'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/reference/{set}`;
        urlPath = urlPath.replace(`{${"set"}}`, encodeURIComponent(String(requestParameters['set'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ClearReferenceOutFromJSON(jsonValue));
    }

    /**
     * Removes one of your organisation\'s overrides.  It removes an entry your organisation wrote, never a baseline member: the published set is not writable from here, so a removal can only ever restore the baseline\'s own answer.
     * Removes one of your organisation\'s overrides.
     */
    async riskClearReference(requestParameters: ReferenceApiRiskClearReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ClearReferenceOut> {
        const response = await this.riskClearReferenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reference describes one set and lists your org\'s overrides in it.  The set half is public data about a published list — its version, its publishers, their licences and how current each one is. The overrides half is yours alone: it is read from your organisation\'s own store, and no other organisation\'s entries can appear in it.
     * Reference describes one set and lists your org\'s overrides in it.
     */
    async riskReferenceRaw(requestParameters: ReferenceApiRiskReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReferenceOut>> {
        if (requestParameters['set'] == null) {
            throw new runtime.RequiredError(
                'set',
                'Required parameter "set" was null or undefined when calling riskReference().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['after'] != null) {
            queryParameters['after'] = requestParameters['after'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/reference/{set}`;
        urlPath = urlPath.replace(`{${"set"}}`, encodeURIComponent(String(requestParameters['set'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReferenceOutFromJSON(jsonValue));
    }

    /**
     * Reference describes one set and lists your org\'s overrides in it.  The set half is public data about a published list — its version, its publishers, their licences and how current each one is. The overrides half is yours alone: it is read from your organisation\'s own store, and no other organisation\'s entries can appear in it.
     * Reference describes one set and lists your org\'s overrides in it.
     */
    async riskReference(requestParameters: ReferenceApiRiskReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReferenceOut> {
        const response = await this.riskReferenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every set this plane publishes, with its version and how fresh it is.  Read the Stale and Refused lists first: they are the two ways this plane can be quietly wrong, and they are reported rather than inferred. A set in Refused answers nothing — it has never loaded, it is held by another component, or it names a source we hold no licence for.
     * Lists every set this plane publishes, with its version and how fresh it is.
     */
    async riskReferenceSetsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReferenceSetsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/reference`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReferenceSetsOutFromJSON(jsonValue));
    }

    /**
     * Lists every set this plane publishes, with its version and how fresh it is.  Read the Stale and Refused lists first: they are the two ways this plane can be quietly wrong, and they are reported rather than inferred. A set in Refused answers nothing — it has never loaded, it is held by another component, or it names a source we hold no licence for.
     * Lists every set this plane publishes, with its version and how fresh it is.
     */
    async riskReferenceSets(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReferenceSetsOut> {
        const response = await this.riskReferenceSetsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Takes a new version of one set. SuperAdmin only.  It is platform work, not tenant work: it writes the shared baseline every organisation reads, so it is gated to the platform\'s own identity. Nothing here can write an organisation\'s overrides, and nothing an organisation sends can reach this route.  Idempotent. A version is the content digest of what was taken, so refreshing an unchanged publisher writes no rows and reports unchanged. Resumable: a run that died half-way is continued from where it stopped rather than restarted.  A set whose source needs a licence we do not hold is refused with the reason, rather than being quietly skipped.
     * Takes a new version of one set.
     */
    async riskRefreshReferenceRaw(requestParameters: ReferenceApiRiskRefreshReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefreshReferenceOut>> {
        if (requestParameters['refreshReferenceIn'] == null) {
            throw new runtime.RequiredError(
                'refreshReferenceIn',
                'Required parameter "refreshReferenceIn" was null or undefined when calling riskRefreshReference().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/reference/refresh`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RefreshReferenceInToJSON(requestParameters['refreshReferenceIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefreshReferenceOutFromJSON(jsonValue));
    }

    /**
     * Takes a new version of one set. SuperAdmin only.  It is platform work, not tenant work: it writes the shared baseline every organisation reads, so it is gated to the platform\'s own identity. Nothing here can write an organisation\'s overrides, and nothing an organisation sends can reach this route.  Idempotent. A version is the content digest of what was taken, so refreshing an unchanged publisher writes no rows and reports unchanged. Resumable: a run that died half-way is continued from where it stopped rather than restarted.  A set whose source needs a licence we do not hold is refused with the reason, rather than being quietly skipped.
     * Takes a new version of one set.
     */
    async riskRefreshReference(requestParameters: ReferenceApiRiskRefreshReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefreshReferenceOut> {
        const response = await this.riskRefreshReferenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Looks keys up against the reference plane.  Your organisation\'s own overrides are consulted FIRST and win outright; the shared baseline answers everything they do not cover. Every answer names the version that produced it, when that version was current and whether it is stale, so a decision can record exactly what it consulted.  Read Refusal before reading Hit. A set that has never loaded, one held by the component that screens against it, and one whose source needs a licence we do not hold all answer with a refusal — and a miss on a refusing set means nothing is known, not that the key is clean.
     * Looks keys up against the reference plane.
     */
    async riskResolveReferenceRaw(requestParameters: ReferenceApiRiskResolveReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResolveReferenceOut>> {
        if (requestParameters['resolveReferenceIn'] == null) {
            throw new runtime.RequiredError(
                'resolveReferenceIn',
                'Required parameter "resolveReferenceIn" was null or undefined when calling riskResolveReference().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/reference/resolve`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResolveReferenceInToJSON(requestParameters['resolveReferenceIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResolveReferenceOutFromJSON(jsonValue));
    }

    /**
     * Looks keys up against the reference plane.  Your organisation\'s own overrides are consulted FIRST and win outright; the shared baseline answers everything they do not cover. Every answer names the version that produced it, when that version was current and whether it is stale, so a decision can record exactly what it consulted.  Read Refusal before reading Hit. A set that has never loaded, one held by the component that screens against it, and one whose source needs a licence we do not hold all answer with a refusal — and a miss on a refusing set means nothing is known, not that the key is clean.
     * Looks keys up against the reference plane.
     */
    async riskResolveReference(requestParameters: ReferenceApiRiskResolveReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResolveReferenceOut> {
        const response = await this.riskResolveReferenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes your organisation\'s own allow and deny entries over a set.  Idempotent on the key: writing the same entry twice is one entry, and writing it again replaces the verdict and the note. The whole batch is one transaction, so a batch that would cross the per-set bound writes nothing rather than half of itself — a half-applied deny list is worse than a refused one, because nobody can tell which half applied.  Your entries are held in your organisation\'s own store and are never visible to another organisation, and they never change what any other organisation sees. The shared baseline is not writable from here at all.
     * Writes your organisation\'s own allow and deny entries over a set.
     */
    async riskSetReferenceRaw(requestParameters: ReferenceApiRiskSetReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SetReferenceOut>> {
        if (requestParameters['set'] == null) {
            throw new runtime.RequiredError(
                'set',
                'Required parameter "set" was null or undefined when calling riskSetReference().'
            );
        }

        if (requestParameters['setReferenceIn'] == null) {
            throw new runtime.RequiredError(
                'setReferenceIn',
                'Required parameter "setReferenceIn" was null or undefined when calling riskSetReference().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/reference/{set}`;
        urlPath = urlPath.replace(`{${"set"}}`, encodeURIComponent(String(requestParameters['set'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SetReferenceInToJSON(requestParameters['setReferenceIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SetReferenceOutFromJSON(jsonValue));
    }

    /**
     * Writes your organisation\'s own allow and deny entries over a set.  Idempotent on the key: writing the same entry twice is one entry, and writing it again replaces the verdict and the note. The whole batch is one transaction, so a batch that would cross the per-set bound writes nothing rather than half of itself — a half-applied deny list is worse than a refused one, because nobody can tell which half applied.  Your entries are held in your organisation\'s own store and are never visible to another organisation, and they never change what any other organisation sees. The shared baseline is not writable from here at all.
     * Writes your organisation\'s own allow and deny entries over a set.
     */
    async riskSetReference(requestParameters: ReferenceApiRiskSetReferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SetReferenceOut> {
        const response = await this.riskSetReferenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
