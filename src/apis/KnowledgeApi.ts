/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  CatalogOut,
  ConnectionOut,
  GraphOut,
  KbAuthorizeOut,
  KbConnectorsOut,
  KbSyncOut,
  SearchIn,
  SearchOut,
} from '../models/index.js';
import {
    CatalogOutFromJSON,
    CatalogOutToJSON,
    ConnectionOutFromJSON,
    ConnectionOutToJSON,
    GraphOutFromJSON,
    GraphOutToJSON,
    KbAuthorizeOutFromJSON,
    KbAuthorizeOutToJSON,
    KbConnectorsOutFromJSON,
    KbConnectorsOutToJSON,
    KbSyncOutFromJSON,
    KbSyncOutToJSON,
    SearchInFromJSON,
    SearchInToJSON,
    SearchOutFromJSON,
    SearchOutToJSON,
} from '../models/index.js';

export interface KnowledgeApiDeleteKnowledgeConnectorsByProviderRequest {
    provider: string;
}

export interface KnowledgeApiGetKnowledgeConnectorsByProviderCallbackRequest {
    provider: string;
    code?: string;
    state?: string;
    error?: string;
}

export interface KnowledgeApiGetKnowledgeConnectorsByProviderConnectRequest {
    provider: string;
}

export interface KnowledgeApiGetKnowledgeGraphRequest {
    project?: string;
}

export interface KnowledgeApiPostKnowledgeConnectorsByProviderSyncRequest {
    provider: string;
}

export interface KnowledgeApiPostKnowledgeSearchRequest {
    searchIn: SearchIn;
}

/**
 * 
 */
export class KnowledgeApi extends runtime.BaseAPI {

    /**
     * Revokes a connection: it tombstones the stored credential so a later sync cannot reuse it, purges this provider\'s points from the org\'s vector namespace, and marks the connector disconnected. The documents already ingested stay in the org\'s store — they are the org\'s own data — but stop being retrievable by search; a caller deletes them through the document surface.
     * Revokes a connection: it tombstones the stored credential so a later sync cannot reuse it, purges this provider\'s points from the org\'s vector namespace, and marks the connector disconnected.
     */
    async deleteKnowledgeConnectorsByProviderRaw(requestParameters: KnowledgeApiDeleteKnowledgeConnectorsByProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConnectionOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling deleteKnowledgeConnectorsByProvider().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/connectors/{provider}`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConnectionOutFromJSON(jsonValue));
    }

    /**
     * Revokes a connection: it tombstones the stored credential so a later sync cannot reuse it, purges this provider\'s points from the org\'s vector namespace, and marks the connector disconnected. The documents already ingested stay in the org\'s store — they are the org\'s own data — but stop being retrievable by search; a caller deletes them through the document surface.
     * Revokes a connection: it tombstones the stored credential so a later sync cannot reuse it, purges this provider\'s points from the org\'s vector namespace, and marks the connector disconnected.
     */
    async deleteKnowledgeConnectorsByProvider(requestParameters: KnowledgeApiDeleteKnowledgeConnectorsByProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConnectionOut> {
        const response = await this.deleteKnowledgeConnectorsByProviderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every supported knowledge connector with THIS org\'s connection state and the REAL number of documents each has ingested into the org\'s store. A provider that is configured for the deployment but not yet connected appears as disconnected, so the console can offer a Connect button. No secret is ever returned.
     * Returns every supported knowledge connector with THIS org\'s connection state and the REAL number of documents each has ingested into the org\'s store.
     */
    async getKnowledgeConnectorsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KbConnectorsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/connectors`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KbConnectorsOutFromJSON(jsonValue));
    }

    /**
     * Returns every supported knowledge connector with THIS org\'s connection state and the REAL number of documents each has ingested into the org\'s store. A provider that is configured for the deployment but not yet connected appears as disconnected, so the console can offer a Connect button. No secret is ever returned.
     * Returns every supported knowledge connector with THIS org\'s connection state and the REAL number of documents each has ingested into the org\'s store.
     */
    async getKnowledgeConnectors(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KbConnectorsOut> {
        const response = await this.getKnowledgeConnectorsRaw(initOverrides);
        return await response.value();
    }

    /**
     * CompleteConnectorOAuth finishes an OAuth connection: it exchanges the provider\'s code for a token, seals that token in KMS, and records the connection. THE ORG COMES FROM THE SIGNED STATE, not from a header and not from the provider, so an attacker cannot bind their own account to someone else\'s org — a tampered, expired or foreign-provider state is refused outright. The token itself is never returned, never written into the document, and never logged; the document holds only its KMS path.
     * CompleteConnectorOAuth finishes an OAuth connection: it exchanges the provider\'s code for a token, seals that token in KMS, and records the connection.
     */
    async getKnowledgeConnectorsByProviderCallbackRaw(requestParameters: KnowledgeApiGetKnowledgeConnectorsByProviderCallbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConnectionOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling getKnowledgeConnectorsByProviderCallback().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['code'] != null) {
            queryParameters['code'] = requestParameters['code'];
        }

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        if (requestParameters['error'] != null) {
            queryParameters['error'] = requestParameters['error'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/connectors/{provider}/callback`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConnectionOutFromJSON(jsonValue));
    }

    /**
     * CompleteConnectorOAuth finishes an OAuth connection: it exchanges the provider\'s code for a token, seals that token in KMS, and records the connection. THE ORG COMES FROM THE SIGNED STATE, not from a header and not from the provider, so an attacker cannot bind their own account to someone else\'s org — a tampered, expired or foreign-provider state is refused outright. The token itself is never returned, never written into the document, and never logged; the document holds only its KMS path.
     * CompleteConnectorOAuth finishes an OAuth connection: it exchanges the provider\'s code for a token, seals that token in KMS, and records the connection.
     */
    async getKnowledgeConnectorsByProviderCallback(requestParameters: KnowledgeApiGetKnowledgeConnectorsByProviderCallbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConnectionOut> {
        const response = await this.getKnowledgeConnectorsByProviderCallbackRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * StartConnectorOAuth returns the provider authorize URL the console opens to connect this org\'s account. There is no server-side redirect — the console stays in control of the navigation. The URL carries a state this server SIGNED over the caller\'s validated org, so the connection the callback completes can only ever land in that org.
     * StartConnectorOAuth returns the provider authorize URL the console opens to connect this org\'s account.
     */
    async getKnowledgeConnectorsByProviderConnectRaw(requestParameters: KnowledgeApiGetKnowledgeConnectorsByProviderConnectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KbAuthorizeOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling getKnowledgeConnectorsByProviderConnect().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/connectors/{provider}/connect`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KbAuthorizeOutFromJSON(jsonValue));
    }

    /**
     * StartConnectorOAuth returns the provider authorize URL the console opens to connect this org\'s account. There is no server-side redirect — the console stays in control of the navigation. The URL carries a state this server SIGNED over the caller\'s validated org, so the connection the callback completes can only ever land in that org.
     * StartConnectorOAuth returns the provider authorize URL the console opens to connect this org\'s account.
     */
    async getKnowledgeConnectorsByProviderConnect(requestParameters: KnowledgeApiGetKnowledgeConnectorsByProviderConnectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KbAuthorizeOut> {
        const response = await this.getKnowledgeConnectorsByProviderConnectRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the ONE catalog of everything a caller can connect: every first-party connector and every long-tail one, in a single list sorted by provider. `configured` reports whether this deployment holds OAuth credentials for a source, so the console can show Connect rather than a dead button, and `kind` is a badge only — the connect and sync lifecycle is identical for both. The catalog itself is org-independent; a validated principal is still required. It is metadata only: no secret is ever returned.
     * Returns the ONE catalog of everything a caller can connect: every first-party connector and every long-tail one, in a single list sorted by provider.
     */
    async getKnowledgeConnectorsCatalogRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CatalogOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/connectors/catalog`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CatalogOutFromJSON(jsonValue));
    }

    /**
     * Returns the ONE catalog of everything a caller can connect: every first-party connector and every long-tail one, in a single list sorted by provider. `configured` reports whether this deployment holds OAuth credentials for a source, so the console can show Connect rather than a dead button, and `kind` is a badge only — the connect and sync lifecycle is identical for both. The catalog itself is org-independent; a validated principal is still required. It is metadata only: no secret is ever returned.
     * Returns the ONE catalog of everything a caller can connect: every first-party connector and every long-tail one, in a single list sorted by provider.
     */
    async getKnowledgeConnectorsCatalog(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CatalogOut> {
        const response = await this.getKnowledgeConnectorsCatalogRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller org\'s knowledge as a node/edge graph shaped for a force-directed renderer: pages, memories and synced sources as nodes; the page parent tree, the wikilinks between pages, and each source\'s connector provenance as edges. Wikilink targets are resolved HERE by title or slug, so a rename never needs an edge rewrite and a link that matches no page renders as its own \"unresolved\" node instead of vanishing. ?project= narrows it. A store outage degrades to an honest empty graph, never a 5xx.
     * Returns the caller org\'s knowledge as a node/edge graph shaped for a force-directed renderer: pages, memories and synced sources as nodes; the page parent tree, the wikilinks between pages, and each source\'s connector provenance as edges.
     */
    async getKnowledgeGraphRaw(requestParameters: KnowledgeApiGetKnowledgeGraphRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GraphOut>> {
        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/graph`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GraphOutFromJSON(jsonValue));
    }

    /**
     * Returns the caller org\'s knowledge as a node/edge graph shaped for a force-directed renderer: pages, memories and synced sources as nodes; the page parent tree, the wikilinks between pages, and each source\'s connector provenance as edges. Wikilink targets are resolved HERE by title or slug, so a rename never needs an edge rewrite and a link that matches no page renders as its own \"unresolved\" node instead of vanishing. ?project= narrows it. A store outage degrades to an honest empty graph, never a 5xx.
     * Returns the caller org\'s knowledge as a node/edge graph shaped for a force-directed renderer: pages, memories and synced sources as nodes; the page parent tree, the wikilinks between pages, and each source\'s connector provenance as edges.
     */
    async getKnowledgeGraph(requestParameters: KnowledgeApiGetKnowledgeGraphRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GraphOut> {
        const response = await this.getKnowledgeGraphRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Pulls the provider\'s documents for the caller\'s org and files them as knowledge sources, which the store\'s own hook then indexes — so a synced document is retrievable exactly like a hand-written page. The org is the validated tenant and the credential is read from KMS, so an org can only ever sync its own connection. A provider failure is reported honestly (502) and recorded on the connector rather than silently swallowed.
     * Pulls the provider\'s documents for the caller\'s org and files them as knowledge sources, which the store\'s own hook then indexes — so a synced document is retrievable exactly like a hand-written page.
     */
    async postKnowledgeConnectorsByProviderSyncRaw(requestParameters: KnowledgeApiPostKnowledgeConnectorsByProviderSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<KbSyncOut>> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling postKnowledgeConnectorsByProviderSync().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/connectors/{provider}/sync`;
        urlPath = urlPath.replace(`{${"provider"}}`, encodeURIComponent(String(requestParameters['provider'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => KbSyncOutFromJSON(jsonValue));
    }

    /**
     * Pulls the provider\'s documents for the caller\'s org and files them as knowledge sources, which the store\'s own hook then indexes — so a synced document is retrievable exactly like a hand-written page. The org is the validated tenant and the credential is read from KMS, so an org can only ever sync its own connection. A provider failure is reported honestly (502) and recorded on the connector rather than silently swallowed.
     * Pulls the provider\'s documents for the caller\'s org and files them as knowledge sources, which the store\'s own hook then indexes — so a synced document is retrievable exactly like a hand-written page.
     */
    async postKnowledgeConnectorsByProviderSync(requestParameters: KnowledgeApiPostKnowledgeConnectorsByProviderSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<KbSyncOut> {
        const response = await this.postKnowledgeConnectorsByProviderSyncRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Ingests an uploaded export as a tree of kb-page documents with its link structure intact. `?format=` picks the normalizer — obsidian, notion, roam or evernote — and the export arrives as a multipart `file` part, or as the raw request body when there is no multipart part: an Obsidian or Notion vault zip, a Roam JSON (raw or inside the zip Roam downloads), or an Evernote .enex.  The pages are filed through the SAME ingest path a connector sync uses, so the kb-page hook indexes each one for retrieval AND extracts its `[[wikilinks]]` into kb-link edges — the imported vault is searchable and its graph is navigable without a second pass. Parents are filed before their children, and each page takes a slug unique within the org (suffixed -2, -3, … on collision), so a re-import adds pages rather than overwriting the ones already there.  Scoped to the caller\'s validated org; `?project=` narrows every imported page to one project. No validated principal is 403, and an org that has not installed the kb module is refused with the install call to make first. The bounds are 64 MB per upload, 5000 pages and 8 MB per archive entry: pages past the five-thousandth are dropped and a larger entry is truncated at its bound, and a page the store rejects is skipped — so the answer\'s `imported` count is what was actually filed, not what was sent.
     * Import an Obsidian, Notion, Roam or Evernote export into the org\'s knowledge base
     */
    async postKnowledgeImportRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/import`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ingests an uploaded export as a tree of kb-page documents with its link structure intact. `?format=` picks the normalizer — obsidian, notion, roam or evernote — and the export arrives as a multipart `file` part, or as the raw request body when there is no multipart part: an Obsidian or Notion vault zip, a Roam JSON (raw or inside the zip Roam downloads), or an Evernote .enex.  The pages are filed through the SAME ingest path a connector sync uses, so the kb-page hook indexes each one for retrieval AND extracts its `[[wikilinks]]` into kb-link edges — the imported vault is searchable and its graph is navigable without a second pass. Parents are filed before their children, and each page takes a slug unique within the org (suffixed -2, -3, … on collision), so a re-import adds pages rather than overwriting the ones already there.  Scoped to the caller\'s validated org; `?project=` narrows every imported page to one project. No validated principal is 403, and an org that has not installed the kb module is refused with the install call to make first. The bounds are 64 MB per upload, 5000 pages and 8 MB per archive entry: pages past the five-thousandth are dropped and a larger entry is truncated at its bound, and a page the store rejects is skipped — so the answer\'s `imported` count is what was actually filed, not what was sent.
     * Import an Obsidian, Notion, Roam or Evernote export into the org\'s knowledge base
     */
    async postKnowledgeImport(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postKnowledgeImportRaw(initOverrides);
    }

    /**
     * Runs a semantic search over the caller org\'s own knowledge — its wiki pages, its agent memories and everything its connectors have synced — and returns the matching passages. This is the RAG entry point: an agent asks \"what does this org know about X\" and the org\'s OWN vector namespace answers. The org comes from the validated principal, and both the collection and the payload filter are pinned to it, so cross-tenant retrieval is impossible. An unreachable index returns an honest empty result set with degraded=true, never a 5xx.
     * Runs a semantic search over the caller org\'s own knowledge — its wiki pages, its agent memories and everything its connectors have synced — and returns the matching passages.
     */
    async postKnowledgeSearchRaw(requestParameters: KnowledgeApiPostKnowledgeSearchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchOut>> {
        if (requestParameters['searchIn'] == null) {
            throw new runtime.RequiredError(
                'searchIn',
                'Required parameter "searchIn" was null or undefined when calling postKnowledgeSearch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/knowledge/search`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SearchInToJSON(requestParameters['searchIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchOutFromJSON(jsonValue));
    }

    /**
     * Runs a semantic search over the caller org\'s own knowledge — its wiki pages, its agent memories and everything its connectors have synced — and returns the matching passages. This is the RAG entry point: an agent asks \"what does this org know about X\" and the org\'s OWN vector namespace answers. The org comes from the validated principal, and both the collection and the payload filter are pinned to it, so cross-tenant retrieval is impossible. An unreachable index returns an honest empty result set with degraded=true, never a 5xx.
     * Runs a semantic search over the caller org\'s own knowledge — its wiki pages, its agent memories and everything its connectors have synced — and returns the matching passages.
     */
    async postKnowledgeSearch(requestParameters: KnowledgeApiPostKnowledgeSearchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchOut> {
        const response = await this.postKnowledgeSearchRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
