/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  IndexersOut,
} from '../models/index.js';
import {
    IndexersOutFromJSON,
    IndexersOutToJSON,
} from '../models/index.js';

/**
 * 
 */
export class IndexersApi extends runtime.BaseAPI {

    /**
     * Reports the deployment\'s chain indexer(s) and how far each has indexed. Identity and health come from the indexer\'s /health; the latest indexed block (height + time) from its /v1/explorer/blocks. The row EXISTS if EITHER call reaches the indexer; when the indexer is entirely unreachable the answer degrades to an honest-EMPTY list at 200, not a 502. No chain HEAD is exposed by the indexer REST, so `lag` is honestly omitted rather than fabricated.
     * Reports the deployment\'s chain indexer(s) and how far each has indexed.
     */
    async getIndexersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IndexersOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/indexers`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IndexersOutFromJSON(jsonValue));
    }

    /**
     * Reports the deployment\'s chain indexer(s) and how far each has indexed. Identity and health come from the indexer\'s /health; the latest indexed block (height + time) from its /v1/explorer/blocks. The row EXISTS if EITHER call reaches the indexer; when the indexer is entirely unreachable the answer degrades to an honest-EMPTY list at 200, not a 502. No chain HEAD is exposed by the indexer REST, so `lag` is honestly omitted rather than fabricated.
     * Reports the deployment\'s chain indexer(s) and how far each has indexed.
     */
    async getIndexers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IndexersOut> {
        const response = await this.getIndexersRaw(initOverrides);
        return await response.value();
    }

}
