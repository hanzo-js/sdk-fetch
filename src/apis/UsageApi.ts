/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  DashResp,
  ReportReq,
  ReportResp,
  UsageAnalyticsAccess,
  UsageAnalyticsView,
  UsageSummary,
} from '../models/index.js';
import {
    DashRespFromJSON,
    DashRespToJSON,
    ReportReqFromJSON,
    ReportReqToJSON,
    ReportRespFromJSON,
    ReportRespToJSON,
    UsageAnalyticsAccessFromJSON,
    UsageAnalyticsAccessToJSON,
    UsageAnalyticsViewFromJSON,
    UsageAnalyticsViewToJSON,
    UsageSummaryFromJSON,
    UsageSummaryToJSON,
} from '../models/index.js';

export interface UsageApiGetUsageAnalyticsRequest {
    end?: string;
    plan?: string;
    range?: string;
    start?: string;
}

export interface UsageApiGetUsageAnalyticsAccessRequest {
    plan?: string;
}

export interface UsageApiGetUsageSamplesRequest {
    account?: string;
    provider?: string;
    range?: string;
    window?: string;
}

export interface UsageApiGetUsageSummaryRequest {
    range?: string;
    start?: string;
    end?: string;
}

export interface UsageApiPostUsageRequest {
    reportReq: ReportReq;
}

/**
 * 
 */
export class UsageApi extends runtime.BaseAPI {

    /**
     * Is the entitlement-GATED per-provider breakdown of the caller org\'s LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads its totals from. Basic own-org usage stays ungated at /v1/usage/summary.  A plan that does not grant the analytics datastore is refused with 402, and an unresolvable plan fails closed to the free floor, which does not grant it. The window is clamped forward to the plan\'s retention entitlement, so a tenant can never read older than its plan allows even with a custom start. The response is marked no-store.  INTERIM (mirrors apps/world\'s limits echo): no org→plan resolver exists in cloud yet — the subscription lookup is owned by the billing plane and the gateway principal carries no plan claim — so the caller passes the plan and the gate resolves THAT plan\'s access.
     * Is the entitlement-GATED per-provider breakdown of the caller org\'s LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads its totals from.
     */
    async getUsageAnalyticsRaw(requestParameters: UsageApiGetUsageAnalyticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageAnalyticsView>> {
        const queryParameters: any = {};

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        if (requestParameters['plan'] != null) {
            queryParameters['plan'] = requestParameters['plan'];
        }

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/usage/analytics`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageAnalyticsViewFromJSON(jsonValue));
    }

    /**
     * Is the entitlement-GATED per-provider breakdown of the caller org\'s LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads its totals from. Basic own-org usage stays ungated at /v1/usage/summary.  A plan that does not grant the analytics datastore is refused with 402, and an unresolvable plan fails closed to the free floor, which does not grant it. The window is clamped forward to the plan\'s retention entitlement, so a tenant can never read older than its plan allows even with a custom start. The response is marked no-store.  INTERIM (mirrors apps/world\'s limits echo): no org→plan resolver exists in cloud yet — the subscription lookup is owned by the billing plane and the gateway principal carries no plan claim — so the caller passes the plan and the gate resolves THAT plan\'s access.
     * Is the entitlement-GATED per-provider breakdown of the caller org\'s LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads its totals from.
     */
    async getUsageAnalytics(requestParameters: UsageApiGetUsageAnalyticsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageAnalyticsView> {
        const response = await this.getUsageAnalyticsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Echoes a plan\'s resolved analytics entitlement so a dashboard can configure itself against the LIVE catalog instead of hardcoding tier numbers. An empty plan resolves the free floor, and a catalog resolution failure serves that same floor rather than erroring — so this always answers 200. It is a read-only contract echo and carries no tenant data.
     * Echoes a plan\'s resolved analytics entitlement so a dashboard can configure itself against the LIVE catalog instead of hardcoding tier numbers.
     */
    async getUsageAnalyticsAccessRaw(requestParameters: UsageApiGetUsageAnalyticsAccessRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageAnalyticsAccess>> {
        const queryParameters: any = {};

        if (requestParameters['plan'] != null) {
            queryParameters['plan'] = requestParameters['plan'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/usage/analytics/access`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageAnalyticsAccessFromJSON(jsonValue));
    }

    /**
     * Echoes a plan\'s resolved analytics entitlement so a dashboard can configure itself against the LIVE catalog instead of hardcoding tier numbers. An empty plan resolves the free floor, and a catalog resolution failure serves that same floor rather than erroring — so this always answers 200. It is a read-only contract echo and carries no tenant data.
     * Echoes a plan\'s resolved analytics entitlement so a dashboard can configure itself against the LIVE catalog instead of hardcoding tier numbers.
     */
    async getUsageAnalyticsAccess(requestParameters: UsageApiGetUsageAnalyticsAccessRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageAnalyticsAccess> {
        const response = await this.getUsageAnalyticsAccessRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Is the PER-PROVIDER view: one connected account\'s own consumption of its own plan — \"my plan is 47% through its 6h window, resets at 14:20\".  `current` is the newest instance of each lane (the headline); `windows` is the history behind it. Both come from ONE deduped read, so they can never disagree. The rows are the caller\'s OWN linked accounts, scoped to the validated principal and its subject — never another user\'s, and never another org\'s.
     * Is the PER-PROVIDER view: one connected account\'s own consumption of its own plan — \"my plan is 47% through its 6h window, resets at 14:20\".
     */
    async getUsageSamplesRaw(requestParameters: UsageApiGetUsageSamplesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DashResp>> {
        const queryParameters: any = {};

        if (requestParameters['account'] != null) {
            queryParameters['account'] = requestParameters['account'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        if (requestParameters['window'] != null) {
            queryParameters['window'] = requestParameters['window'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/usage/samples`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DashRespFromJSON(jsonValue));
    }

    /**
     * Is the PER-PROVIDER view: one connected account\'s own consumption of its own plan — \"my plan is 47% through its 6h window, resets at 14:20\".  `current` is the newest instance of each lane (the headline); `windows` is the history behind it. Both come from ONE deduped read, so they can never disagree. The rows are the caller\'s OWN linked accounts, scoped to the validated principal and its subject — never another user\'s, and never another org\'s.
     * Is the PER-PROVIDER view: one connected account\'s own consumption of its own plan — \"my plan is 47% through its 6h window, resets at 14:20\".
     */
    async getUsageSamples(requestParameters: UsageApiGetUsageSamplesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DashResp> {
        const response = await this.getUsageSamplesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers GET /v1/usage/summary: the caller\'s own usage footprint over one window — the categorized spend roll-up from the commerce ledger, the org\'s LLM usage totals from the warehouse, and the caller\'s OWN linked provider accounts beside the org\'s Hanzo-routed usage.  Every source degrades INDEPENDENTLY to honest zeros and says so in `sources` and in its own `available` flag, so a partial deploy reports \"no data\" rather than fabricating spend. The account rows and the Hanzo rows are concatenated and never summed: a plan\'s percent is not money.  The response is org-scoped from the validated principal and marked no-store — a signed-out caller is refused.
     * Answers GET /v1/usage/summary: the caller\'s own usage footprint over one window — the categorized spend roll-up from the commerce ledger, the org\'s LLM usage totals from the warehouse, and the caller\'s OWN linked provider accounts beside the org\'s Hanzo-routed usage.
     */
    async getUsageSummaryRaw(requestParameters: UsageApiGetUsageSummaryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageSummary>> {
        const queryParameters: any = {};

        if (requestParameters['range'] != null) {
            queryParameters['range'] = requestParameters['range'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/usage/summary`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageSummaryFromJSON(jsonValue));
    }

    /**
     * Answers GET /v1/usage/summary: the caller\'s own usage footprint over one window — the categorized spend roll-up from the commerce ledger, the org\'s LLM usage totals from the warehouse, and the caller\'s OWN linked provider accounts beside the org\'s Hanzo-routed usage.  Every source degrades INDEPENDENTLY to honest zeros and says so in `sources` and in its own `available` flag, so a partial deploy reports \"no data\" rather than fabricating spend. The account rows and the Hanzo rows are concatenated and never summed: a plan\'s percent is not money.  The response is org-scoped from the validated principal and marked no-store — a signed-out caller is refused.
     * Answers GET /v1/usage/summary: the caller\'s own usage footprint over one window — the categorized spend roll-up from the commerce ledger, the org\'s LLM usage totals from the warehouse, and the caller\'s OWN linked provider accounts beside the org\'s Hanzo-routed usage.
     */
    async getUsageSummary(requestParameters: UsageApiGetUsageSummaryRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageSummary> {
        const response = await this.getUsageSummaryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Ingests a batch of account-usage samples — what a developer\'s OWN AI accounts have consumed of their OWN plans, metered from each provider\'s own login — and appends them to the warehouse series. Answers 202.  Send either a `samples` array or one sample\'s fields at the top level. Every sample needs a provider, a machine and a known window class; an unknown window or kind is refused rather than silently rewritten, because a dash filled with a class nobody reported is worse than an error. There is no timestamp field: the server owns the observation clock, and a sample says which window it measured with windowStart or resetsAt.  It is FAIL-SOFT on storage: a warehouse outage costs a poll of history (stored:false), never a failed request. It records usage ONLY — the link registry is refreshed separately via POST /v1/link, so there is one and only one way to update an account row.
     * Ingests a batch of account-usage samples — what a developer\'s OWN AI accounts have consumed of their OWN plans, metered from each provider\'s own login — and appends them to the warehouse series.
     */
    async postUsageRaw(requestParameters: UsageApiPostUsageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReportResp>> {
        if (requestParameters['reportReq'] == null) {
            throw new runtime.RequiredError(
                'reportReq',
                'Required parameter "reportReq" was null or undefined when calling postUsage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/usage`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ReportReqToJSON(requestParameters['reportReq']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReportRespFromJSON(jsonValue));
    }

    /**
     * Ingests a batch of account-usage samples — what a developer\'s OWN AI accounts have consumed of their OWN plans, metered from each provider\'s own login — and appends them to the warehouse series. Answers 202.  Send either a `samples` array or one sample\'s fields at the top level. Every sample needs a provider, a machine and a known window class; an unknown window or kind is refused rather than silently rewritten, because a dash filled with a class nobody reported is worse than an error. There is no timestamp field: the server owns the observation clock, and a sample says which window it measured with windowStart or resetsAt.  It is FAIL-SOFT on storage: a warehouse outage costs a poll of history (stored:false), never a failed request. It records usage ONLY — the link registry is refreshed separately via POST /v1/link, so there is one and only one way to update an account row.
     * Ingests a batch of account-usage samples — what a developer\'s OWN AI accounts have consumed of their OWN plans, metered from each provider\'s own login — and appends them to the warehouse series.
     */
    async postUsage(requestParameters: UsageApiPostUsageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReportResp> {
        const response = await this.postUsageRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
