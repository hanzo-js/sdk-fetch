/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  AccountsOut,
  TreasuryReport,
} from '../models/index.js';
import {
    AccountsOutFromJSON,
    AccountsOutToJSON,
    TreasuryReportFromJSON,
    TreasuryReportToJSON,
} from '../models/index.js';

export interface TreasuryApiGetTreasuryAccountsRequest {
    scope?: string;
    org?: string;
}

/**
 * 
 */
export class TreasuryApi extends runtime.BaseAPI {

    /**
     * Returns the reserve fund\'s health and the current revenue-share policy for any validated caller. It is a TRANSPARENCY view — a partner or author can see that the pool backing their payouts is solvent — and NOT per-org money, which is the customer\'s own commerce balance at /v1/billing/balance. The policy is read-only here; only a SuperAdmin sets it.
     * Returns the reserve fund\'s health and the current revenue-share policy for any validated caller.
     */
    async getTreasuryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TreasuryReport>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/treasury`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TreasuryReportFromJSON(jsonValue));
    }

    /**
     * Returns the reserve fund\'s health and the current revenue-share policy for any validated caller. It is a TRANSPARENCY view — a partner or author can see that the pool backing their payouts is solvent — and NOT per-org money, which is the customer\'s own commerce balance at /v1/billing/balance. The policy is read-only here; only a SuperAdmin sets it.
     * Returns the reserve fund\'s health and the current revenue-share policy for any validated caller.
     */
    async getTreasury(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TreasuryReport> {
        const response = await this.getTreasuryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the ledger accounts the caller may see, with their balances. It is tenant-isolated SERVER-SIDE: an ordinary caller sees ONLY accounts under its own \"org:<tenant>:\" prefix, never house accounts and never another tenant\'s. A SuperAdmin may widen with ?scope=house (the reserve, revenue and payout house accounts) or ?org=<tenant> — the only way to cross the tenant boundary, and only for platform sudo. The answer is honestly empty until a tenant has ledger postings.
     * Returns the ledger accounts the caller may see, with their balances.
     */
    async getTreasuryAccountsRaw(requestParameters: TreasuryApiGetTreasuryAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountsOut>> {
        const queryParameters: any = {};

        if (requestParameters['scope'] != null) {
            queryParameters['scope'] = requestParameters['scope'];
        }

        if (requestParameters['org'] != null) {
            queryParameters['org'] = requestParameters['org'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/treasury/accounts`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountsOutFromJSON(jsonValue));
    }

    /**
     * Returns the ledger accounts the caller may see, with their balances. It is tenant-isolated SERVER-SIDE: an ordinary caller sees ONLY accounts under its own \"org:<tenant>:\" prefix, never house accounts and never another tenant\'s. A SuperAdmin may widen with ?scope=house (the reserve, revenue and payout house accounts) or ?org=<tenant> — the only way to cross the tenant boundary, and only for platform sudo. The answer is honestly empty until a tenant has ledger postings.
     * Returns the ledger accounts the caller may see, with their balances.
     */
    async getTreasuryAccounts(requestParameters: TreasuryApiGetTreasuryAccountsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountsOut> {
        const response = await this.getTreasuryAccountsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
