/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  O11yO11yDiscoverIn,
  O11yO11yDiscoverOut,
  O11yO11yErrorGettableIssueOut,
  O11yO11yErrorIssueOut,
  O11yO11yErrorIssuesOut,
  O11yO11yLogsOut,
  O11yO11ySentryEventOut,
  O11yO11ySentryIssueEventsOut,
  O11yO11ySentryPostableProject,
  O11yO11ySentryProjectOut,
  O11yO11ySentryProjectsOut,
  O11yO11ySentryUpdateIssueIn,
  O11yO11yStatsOut,
  O11yO11yTraceOut,
  O11yO11yTracesOut,
} from '../models/index.js';
import {
    O11yO11yDiscoverInFromJSON,
    O11yO11yDiscoverInToJSON,
    O11yO11yDiscoverOutFromJSON,
    O11yO11yDiscoverOutToJSON,
    O11yO11yErrorGettableIssueOutFromJSON,
    O11yO11yErrorGettableIssueOutToJSON,
    O11yO11yErrorIssueOutFromJSON,
    O11yO11yErrorIssueOutToJSON,
    O11yO11yErrorIssuesOutFromJSON,
    O11yO11yErrorIssuesOutToJSON,
    O11yO11yLogsOutFromJSON,
    O11yO11yLogsOutToJSON,
    O11yO11ySentryEventOutFromJSON,
    O11yO11ySentryEventOutToJSON,
    O11yO11ySentryIssueEventsOutFromJSON,
    O11yO11ySentryIssueEventsOutToJSON,
    O11yO11ySentryPostableProjectFromJSON,
    O11yO11ySentryPostableProjectToJSON,
    O11yO11ySentryProjectOutFromJSON,
    O11yO11ySentryProjectOutToJSON,
    O11yO11ySentryProjectsOutFromJSON,
    O11yO11ySentryProjectsOutToJSON,
    O11yO11ySentryUpdateIssueInFromJSON,
    O11yO11ySentryUpdateIssueInToJSON,
    O11yO11yStatsOutFromJSON,
    O11yO11yStatsOutToJSON,
    O11yO11yTraceOutFromJSON,
    O11yO11yTraceOutToJSON,
    O11yO11yTracesOutFromJSON,
    O11yO11yTracesOutToJSON,
} from '../models/index.js';

export interface SentryApiDeleteSentryByWildcard1Request {
    wildcard1: string;
}

export interface SentryApiDeleteSentryProjectsByIdRequest {
    id: string;
}

export interface SentryApiGetSentryByWildcard1Request {
    wildcard1: string;
}

export interface SentryApiGetSentryEventsByIdRequest {
    id: string;
    project: string;
}

export interface SentryApiGetSentryIssuesRequest {
    status?: string;
    level?: string;
    environment?: string;
    serviceName?: string;
    query?: string;
    sort?: string;
    offset?: number;
    limit?: number;
    project?: string;
    period?: string;
}

export interface SentryApiGetSentryIssuesByIdRequest {
    id: string;
}

export interface SentryApiGetSentryIssuesByIdEventsRequest {
    id: string;
    project: string;
    limit?: number;
}

export interface SentryApiGetSentryLogsRequest {
    project: string;
    query?: string;
    period?: string;
    limit?: number;
}

export interface SentryApiGetSentryProjectsByIdRequest {
    id: string;
}

export interface SentryApiGetSentryStatsRequest {
    project: string;
    field?: string;
    period?: string;
}

export interface SentryApiGetSentryTracesRequest {
    project: string;
    period?: string;
    limit?: number;
}

export interface SentryApiGetSentryTracesByIdRequest {
    id: string;
    project: string;
}

export interface SentryApiPatchSentryByWildcard1Request {
    wildcard1: string;
}

export interface SentryApiPostSentryByProjectEnvelopeRequest {
    project: string;
}

export interface SentryApiPostSentryByProjectStoreRequest {
    project: string;
}

export interface SentryApiPostSentryByWildcard1Request {
    wildcard1: string;
}

export interface SentryApiPostSentryDiscoverRequest {
    o11yO11yDiscoverIn: O11yO11yDiscoverIn;
}

export interface SentryApiPostSentryProjectsRequest {
    o11yO11ySentryPostableProject: O11yO11ySentryPostableProject;
}

export interface SentryApiPostSentryProjectsByIdKeysRotateRequest {
    id: string;
}

export interface SentryApiPutSentryByWildcard1Request {
    wildcard1: string;
}

export interface SentryApiPutSentryIssuesByIdRequest {
    id: string;
    o11yO11ySentryUpdateIssueIn: O11yO11ySentryUpdateIssueIn;
}

/**
 * 
 */
export class SentryApi extends runtime.BaseAPI {

    /**
     * The one delete on the Sentry surface: removing a PROJECT, answering 204. Error issues, events and traces are not individually deletable — they are append-only telemetry, and their lifetime is retention\'s business, not an API call\'s.  Requires a validated, org-scoped principal with edit rights; a viewer is refused. The delete is confined to the org minted from that principal\'s claim, so a project id belonging to another tenant is not found rather than removed. Deleting a project retires the DSN that fed it, so any SDK still pointed at that key stops being accepted. Before the runtime is initialized, 503.
     * Delete a Sentry project
     */
    async deleteSentryByWildcard1Raw(requestParameters: SentryApiDeleteSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling deleteSentryByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The one delete on the Sentry surface: removing a PROJECT, answering 204. Error issues, events and traces are not individually deletable — they are append-only telemetry, and their lifetime is retention\'s business, not an API call\'s.  Requires a validated, org-scoped principal with edit rights; a viewer is refused. The delete is confined to the org minted from that principal\'s claim, so a project id belonging to another tenant is not found rather than removed. Deleting a project retires the DSN that fed it, so any SDK still pointed at that key stops being accepted. Before the runtime is initialized, 503.
     * Delete a Sentry project
     */
    async deleteSentryByWildcard1(requestParameters: SentryApiDeleteSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSentryByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * Deletes one Sentry project of the caller\'s org. Its DSN stops resolving immediately, so ingest for that id fails closed exactly as an unknown project does; retained events are not touched. Answers 204.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Deletes one Sentry project of the caller\'s org.
     */
    async deleteSentryProjectsByIdRaw(requestParameters: SentryApiDeleteSentryProjectsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteSentryProjectsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/projects/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one Sentry project of the caller\'s org. Its DSN stops resolving immediately, so ingest for that id fails closed exactly as an unknown project does; retained events are not touched. Answers 204.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Deletes one Sentry project of the caller\'s org.
     */
    async deleteSentryProjectsById(requestParameters: SentryApiDeleteSentryProjectsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSentryProjectsByIdRaw(requestParameters, initOverrides);
    }

    /**
     * Serves the Sentry-compatible read surface — projects, error issues and one issue\'s occurrences, a single event, error logs, error-correlated traces and one trace\'s waterfall, and the event-rate stats — so a Sentry client or the error console reads its errors at the paths it already speaks.  It is the SAME runtime the observability surface serves, reached under a second path family, and there is NO rewrite: the runtime carries these routes literally. That is what makes this a product face rather than a translation layer. One runtime, two path families.  A validated principal is required and the read is scoped to that principal\'s own org. Errors are a tenant\'s OWN data, so org membership is the whole admission test and there is deliberately no admin term on it — gating the product on platform sudo would make the only way to see your own errors a scope that shows you everyone\'s. Before the runtime is initialized, 503.
     * Read the caller org\'s errors on the Sentry surface
     */
    async getSentryByWildcard1Raw(requestParameters: SentryApiGetSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling getSentryByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Serves the Sentry-compatible read surface — projects, error issues and one issue\'s occurrences, a single event, error logs, error-correlated traces and one trace\'s waterfall, and the event-rate stats — so a Sentry client or the error console reads its errors at the paths it already speaks.  It is the SAME runtime the observability surface serves, reached under a second path family, and there is NO rewrite: the runtime carries these routes literally. That is what makes this a product face rather than a translation layer. One runtime, two path families.  A validated principal is required and the read is scoped to that principal\'s own org. Errors are a tenant\'s OWN data, so org membership is the whole admission test and there is deliberately no admin term on it — gating the product on platform sudo would make the only way to see your own errors a scope that shows you everyone\'s. Before the runtime is initialized, 503.
     * Read the caller org\'s errors on the Sentry surface
     */
    async getSentryByWildcard1(requestParameters: SentryApiGetSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getSentryByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * Returns one captured error event of a project, by its id.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Returns one captured error event of a project, by its id.
     */
    async getSentryEventsByIdRaw(requestParameters: SentryApiGetSentryEventsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11ySentryEventOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSentryEventsById().'
            );
        }

        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getSentryEventsById().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/events/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11ySentryEventOutFromJSON(jsonValue));
    }

    /**
     * Returns one captured error event of a project, by its id.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Returns one captured error event of a project, by its id.
     */
    async getSentryEventsById(requestParameters: SentryApiGetSentryEventsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11ySentryEventOut> {
        const response = await this.getSentryEventsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller\'s org\'s grouped error issues, optionally narrowed to one project and one time window, and filtered by status, level, environment, service, a free-text query and a sort.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Lists the caller\'s org\'s grouped error issues, optionally narrowed to one project and one time window, and filtered by status, level, environment, service, a free-text query and a sort.
     */
    async getSentryIssuesRaw(requestParameters: SentryApiGetSentryIssuesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yErrorIssuesOut>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['level'] != null) {
            queryParameters['level'] = requestParameters['level'];
        }

        if (requestParameters['environment'] != null) {
            queryParameters['environment'] = requestParameters['environment'];
        }

        if (requestParameters['serviceName'] != null) {
            queryParameters['serviceName'] = requestParameters['serviceName'];
        }

        if (requestParameters['query'] != null) {
            queryParameters['query'] = requestParameters['query'];
        }

        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['period'] != null) {
            queryParameters['period'] = requestParameters['period'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/issues`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yErrorIssuesOutFromJSON(jsonValue));
    }

    /**
     * Lists the caller\'s org\'s grouped error issues, optionally narrowed to one project and one time window, and filtered by status, level, environment, service, a free-text query and a sort.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Lists the caller\'s org\'s grouped error issues, optionally narrowed to one project and one time window, and filtered by status, level, environment, service, a free-text query and a sort.
     */
    async getSentryIssues(requestParameters: SentryApiGetSentryIssuesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yErrorIssuesOut> {
        const response = await this.getSentryIssuesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one grouped issue of the caller\'s org with its latest occurrence sample.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Returns one grouped issue of the caller\'s org with its latest occurrence sample.
     */
    async getSentryIssuesByIdRaw(requestParameters: SentryApiGetSentryIssuesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yErrorGettableIssueOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSentryIssuesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/issues/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yErrorGettableIssueOutFromJSON(jsonValue));
    }

    /**
     * Returns one grouped issue of the caller\'s org with its latest occurrence sample.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Returns one grouped issue of the caller\'s org with its latest occurrence sample.
     */
    async getSentryIssuesById(requestParameters: SentryApiGetSentryIssuesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yErrorGettableIssueOut> {
        const response = await this.getSentryIssuesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists one issue\'s captured occurrences, scoped to a project — a project is an isolation unit, so the caller declares which project\'s occurrences to read.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Lists one issue\'s captured occurrences, scoped to a project — a project is an isolation unit, so the caller declares which project\'s occurrences to read.
     */
    async getSentryIssuesByIdEventsRaw(requestParameters: SentryApiGetSentryIssuesByIdEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11ySentryIssueEventsOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSentryIssuesByIdEvents().'
            );
        }

        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getSentryIssuesByIdEvents().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/issues/{id}/events`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11ySentryIssueEventsOutFromJSON(jsonValue));
    }

    /**
     * Lists one issue\'s captured occurrences, scoped to a project — a project is an isolation unit, so the caller declares which project\'s occurrences to read.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Lists one issue\'s captured occurrences, scoped to a project — a project is an isolation unit, so the caller declares which project\'s occurrences to read.
     */
    async getSentryIssuesByIdEvents(requestParameters: SentryApiGetSentryIssuesByIdEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11ySentryIssueEventsOut> {
        const response = await this.getSentryIssuesByIdEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists a project\'s captured error events, newest first, optionally narrowed to those whose message or exception text contains a search string.
     * Lists a project\'s captured error events, newest first, optionally narrowed to those whose message or exception text contains a search string.
     */
    async getSentryLogsRaw(requestParameters: SentryApiGetSentryLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yLogsOut>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getSentryLogs().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['query'] != null) {
            queryParameters['query'] = requestParameters['query'];
        }

        if (requestParameters['period'] != null) {
            queryParameters['period'] = requestParameters['period'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/logs`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yLogsOutFromJSON(jsonValue));
    }

    /**
     * Lists a project\'s captured error events, newest first, optionally narrowed to those whose message or exception text contains a search string.
     * Lists a project\'s captured error events, newest first, optionally narrowed to those whose message or exception text contains a search string.
     */
    async getSentryLogs(requestParameters: SentryApiGetSentryLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yLogsOut> {
        const response = await this.getSentryLogsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the caller\'s org\'s Sentry projects, each with its freshly-derived DSN.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Lists the caller\'s org\'s Sentry projects, each with its freshly-derived DSN.
     */
    async getSentryProjectsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11ySentryProjectsOut>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11ySentryProjectsOutFromJSON(jsonValue));
    }

    /**
     * Lists the caller\'s org\'s Sentry projects, each with its freshly-derived DSN.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Lists the caller\'s org\'s Sentry projects, each with its freshly-derived DSN.
     */
    async getSentryProjects(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11ySentryProjectsOut> {
        const response = await this.getSentryProjectsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one Sentry project of the caller\'s org, DSN included.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Returns one Sentry project of the caller\'s org, DSN included.
     */
    async getSentryProjectsByIdRaw(requestParameters: SentryApiGetSentryProjectsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11ySentryProjectOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSentryProjectsById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/projects/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11ySentryProjectOutFromJSON(jsonValue));
    }

    /**
     * Returns one Sentry project of the caller\'s org, DSN included.  Callers need the viewer role; the runtime\'s own gate enforces it.
     * Returns one Sentry project of the caller\'s org, DSN included.
     */
    async getSentryProjectsById(requestParameters: SentryApiGetSentryProjectsByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11ySentryProjectOut> {
        const response = await this.getSentryProjectsByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a project\'s event-rate timeseries: one bucket per interval over the requested period, counting the events in it.
     * Returns a project\'s event-rate timeseries: one bucket per interval over the requested period, counting the events in it.
     */
    async getSentryStatsRaw(requestParameters: SentryApiGetSentryStatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yStatsOut>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getSentryStats().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['field'] != null) {
            queryParameters['field'] = requestParameters['field'];
        }

        if (requestParameters['period'] != null) {
            queryParameters['period'] = requestParameters['period'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/stats`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yStatsOutFromJSON(jsonValue));
    }

    /**
     * Returns a project\'s event-rate timeseries: one bucket per interval over the requested period, counting the events in it.
     * Returns a project\'s event-rate timeseries: one bucket per interval over the requested period, counting the events in it.
     */
    async getSentryStats(requestParameters: SentryApiGetSentryStatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yStatsOut> {
        const response = await this.getSentryStatsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the traces a project\'s captured errors reference, each with how many errors landed on it, when they started and stopped, and the latest message seen — the entry point for \"which requests are failing\".
     * Lists the traces a project\'s captured errors reference, each with how many errors landed on it, when they started and stopped, and the latest message seen — the entry point for \"which requests are failing\".
     */
    async getSentryTracesRaw(requestParameters: SentryApiGetSentryTracesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yTracesOut>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getSentryTraces().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['period'] != null) {
            queryParameters['period'] = requestParameters['period'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/traces`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yTracesOutFromJSON(jsonValue));
    }

    /**
     * Lists the traces a project\'s captured errors reference, each with how many errors landed on it, when they started and stopped, and the latest message seen — the entry point for \"which requests are failing\".
     * Lists the traces a project\'s captured errors reference, each with how many errors landed on it, when they started and stopped, and the latest message seen — the entry point for \"which requests are failing\".
     */
    async getSentryTraces(requestParameters: SentryApiGetSentryTracesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yTracesOut> {
        const response = await this.getSentryTracesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one trace\'s captured errors for a project — every error event that carried the trace id, in the order the events plane holds them.
     * Returns one trace\'s captured errors for a project — every error event that carried the trace id, in the order the events plane holds them.
     */
    async getSentryTracesByIdRaw(requestParameters: SentryApiGetSentryTracesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yTraceOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSentryTracesById().'
            );
        }

        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling getSentryTracesById().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/traces/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yTraceOutFromJSON(jsonValue));
    }

    /**
     * Returns one trace\'s captured errors for a project — every error event that carried the trace id, in the order the events plane holds them.
     * Returns one trace\'s captured errors for a project — every error event that carried the trace id, in the order the events plane holds them.
     */
    async getSentryTracesById(requestParameters: SentryApiGetSentryTracesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yTraceOut> {
        const response = await this.getSentryTracesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The Sentry face carries NO route for a partial update. The wildcard admits every method, so this operation exists as an address, but nothing behind it answers and a request lands on the runtime as an unrouted path.  It is documented rather than silently omitted because the useful thing to say is where to go instead: an issue\'s lifecycle — resolve, ignore, assign — is a REPLACE on that issue, not a patch, and it is the only mutable state on this surface. A client that reaches for a partial update here is looking for that call.
     * Not served — the Sentry surface has no partial update
     */
    async patchSentryByWildcard1Raw(requestParameters: SentryApiPatchSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling patchSentryByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The Sentry face carries NO route for a partial update. The wildcard admits every method, so this operation exists as an address, but nothing behind it answers and a request lands on the runtime as an unrouted path.  It is documented rather than silently omitted because the useful thing to say is where to go instead: an issue\'s lifecycle — resolve, ignore, assign — is a REPLACE on that issue, not a patch, and it is the only mutable state on this surface. A client that reaches for a partial update here is looking for that call.
     * Not served — the Sentry surface has no partial update
     */
    async patchSentryByWildcard1(requestParameters: SentryApiPatchSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchSentryByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * The same envelope ingest as the DSN path, spelled the way this platform names things: one /v1/, the product, the project. Point an SDK\'s DSN here and the wire is identical.  AUTHENTICATED BY THE DSN PUBLIC KEY and exempt from the principal gate for the same reason — a Sentry SDK has no Hanzo session to present. The project segment is a UUID enforced by the route, and the exemption matches method plus prefix plus suffix, so every Sentry READ (issues, discover, events, logs, traces, stats) stays gated.
     * Receive a Sentry envelope on the clean root
     */
    async postSentryByProjectEnvelopeRaw(requestParameters: SentryApiPostSentryByProjectEnvelopeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postSentryByProjectEnvelope().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{project}/envelope/`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The same envelope ingest as the DSN path, spelled the way this platform names things: one /v1/, the product, the project. Point an SDK\'s DSN here and the wire is identical.  AUTHENTICATED BY THE DSN PUBLIC KEY and exempt from the principal gate for the same reason — a Sentry SDK has no Hanzo session to present. The project segment is a UUID enforced by the route, and the exemption matches method plus prefix plus suffix, so every Sentry READ (issues, discover, events, logs, traces, stats) stays gated.
     * Receive a Sentry envelope on the clean root
     */
    async postSentryByProjectEnvelope(requestParameters: SentryApiPostSentryByProjectEnvelopeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSentryByProjectEnvelopeRaw(requestParameters, initOverrides);
    }

    /**
     * The legacy single-event ingest on the clean /v1/sentry root — one JSON event rather than a framed batch. Same DSN-key authentication, same gate exemption, same UUID-enforced project segment as the envelope route beside it.
     * Receive a single Sentry event on the clean root
     */
    async postSentryByProjectStoreRaw(requestParameters: SentryApiPostSentryByProjectStoreRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling postSentryByProjectStore().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{project}/store/`;
        urlPath = urlPath.replace(`{${"project"}}`, encodeURIComponent(String(requestParameters['project'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The legacy single-event ingest on the clean /v1/sentry root — one JSON event rather than a framed batch. Same DSN-key authentication, same gate exemption, same UUID-enforced project segment as the envelope route beside it.
     * Receive a single Sentry event on the clean root
     */
    async postSentryByProjectStore(requestParameters: SentryApiPostSentryByProjectStoreRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSentryByProjectStoreRaw(requestParameters, initOverrides);
    }

    /**
     * Carries every write on the Sentry-compatible surface: the SDK\'s error ingest, and the authenticated writes the console makes — creating a project, rotating a project\'s DSN key, and running a discover query over the events plane.  THE TWO ARE AUTHENTICATED DIFFERENTLY, and that is the rule to get right. An envelope or store submission presents a DSN public key, never a Hanzo session, so it is exempt from the principal gate and verified by the ingest key check instead — which derives the org from the DSN and fails closed. A keyless submission is a 401 from that verifier, not a 403 from the gate, and telling those two apart is how you tell the hops apart. Every other write here needs a validated, org-scoped principal, and creating or rotating requires an editor rather than a viewer.  The ingest exemption is matched by method plus prefix plus suffix, never a bare prefix, and the project segment must be a UUID — so no read is reachable through it. Before the runtime is initialized, 503.
     * Send events to the Sentry surface, or write on it
     */
    async postSentryByWildcard1Raw(requestParameters: SentryApiPostSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling postSentryByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Carries every write on the Sentry-compatible surface: the SDK\'s error ingest, and the authenticated writes the console makes — creating a project, rotating a project\'s DSN key, and running a discover query over the events plane.  THE TWO ARE AUTHENTICATED DIFFERENTLY, and that is the rule to get right. An envelope or store submission presents a DSN public key, never a Hanzo session, so it is exempt from the principal gate and verified by the ingest key check instead — which derives the org from the DSN and fails closed. A keyless submission is a 401 from that verifier, not a 403 from the gate, and telling those two apart is how you tell the hops apart. Every other write here needs a validated, org-scoped principal, and creating or rotating requires an editor rather than a viewer.  The ingest exemption is matched by method plus prefix plus suffix, never a bare prefix, and the project segment must be a UUID — so no read is reachable through it. Before the runtime is initialized, 503.
     * Send events to the Sentry surface, or write on it
     */
    async postSentryByWildcard1(requestParameters: SentryApiPostSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.postSentryByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * Aggregates a project\'s captured errors into a table — the caller names the filters, the groupings and the aggregations, and gets back the columns and rows they asked for.  The project is mandatory and is checked against the caller\'s own org before it scopes anything, so a project id belonging to someone else reads as absent rather than as data.
     * Aggregates a project\'s captured errors into a table — the caller names the filters, the groupings and the aggregations, and gets back the columns and rows they asked for.
     */
    async postSentryDiscoverRaw(requestParameters: SentryApiPostSentryDiscoverRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yDiscoverOut>> {
        if (requestParameters['o11yO11yDiscoverIn'] == null) {
            throw new runtime.RequiredError(
                'o11yO11yDiscoverIn',
                'Required parameter "o11yO11yDiscoverIn" was null or undefined when calling postSentryDiscover().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/discover`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: O11yO11yDiscoverInToJSON(requestParameters['o11yO11yDiscoverIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yDiscoverOutFromJSON(jsonValue));
    }

    /**
     * Aggregates a project\'s captured errors into a table — the caller names the filters, the groupings and the aggregations, and gets back the columns and rows they asked for.  The project is mandatory and is checked against the caller\'s own org before it scopes anything, so a project id belonging to someone else reads as absent rather than as data.
     * Aggregates a project\'s captured errors into a table — the caller names the filters, the groupings and the aggregations, and gets back the columns and rows they asked for.
     */
    async postSentryDiscover(requestParameters: SentryApiPostSentryDiscoverRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yDiscoverOut> {
        const response = await this.postSentryDiscoverRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a Sentry project under the caller\'s org and returns it, DSN included. Only the name, and optionally a slug and platform, are the caller\'s to set; the org, id and key are server-assigned.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Creates a Sentry project under the caller\'s org and returns it, DSN included.
     */
    async postSentryProjectsRaw(requestParameters: SentryApiPostSentryProjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11ySentryProjectOut>> {
        if (requestParameters['o11yO11ySentryPostableProject'] == null) {
            throw new runtime.RequiredError(
                'o11yO11ySentryPostableProject',
                'Required parameter "o11yO11ySentryPostableProject" was null or undefined when calling postSentryProjects().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/projects`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: O11yO11ySentryPostableProjectToJSON(requestParameters['o11yO11ySentryPostableProject']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11ySentryProjectOutFromJSON(jsonValue));
    }

    /**
     * Creates a Sentry project under the caller\'s org and returns it, DSN included. Only the name, and optionally a slug and platform, are the caller\'s to set; the org, id and key are server-assigned.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Creates a Sentry project under the caller\'s org and returns it, DSN included.
     */
    async postSentryProjects(requestParameters: SentryApiPostSentryProjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11ySentryProjectOut> {
        const response = await this.postSentryProjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rotates a project\'s DSN key — bumping its rotation watermark so keys below it stop verifying — and returns the project with its new DSN.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Rotates a project\'s DSN key — bumping its rotation watermark so keys below it stop verifying — and returns the project with its new DSN.
     */
    async postSentryProjectsByIdKeysRotateRaw(requestParameters: SentryApiPostSentryProjectsByIdKeysRotateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11ySentryProjectOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling postSentryProjectsByIdKeysRotate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/projects/{id}/keys/rotate`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11ySentryProjectOutFromJSON(jsonValue));
    }

    /**
     * Rotates a project\'s DSN key — bumping its rotation watermark so keys below it stop verifying — and returns the project with its new DSN.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Rotates a project\'s DSN key — bumping its rotation watermark so keys below it stop verifying — and returns the project with its new DSN.
     */
    async postSentryProjectsByIdKeysRotate(requestParameters: SentryApiPostSentryProjectsByIdKeysRotateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11ySentryProjectOut> {
        const response = await this.postSentryProjectsByIdKeysRotateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The one replace on the Sentry surface: updating an error ISSUE — resolving it, ignoring it, or assigning it — and answering the updated issue.  Nothing else here takes a replace. A project is created and deleted but never replaced, and the event and trace planes are append-only telemetry, so an issue\'s lifecycle is the only mutable state this face exposes.  Requires a validated, org-scoped principal with edit rights; a viewer is refused. The write is confined to the org minted from that principal\'s claim, so an issue id belonging to another tenant is simply not found. Before the runtime is initialized, 503.
     * Move an error issue through its lifecycle
     */
    async putSentryByWildcard1Raw(requestParameters: SentryApiPutSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['wildcard1'] == null) {
            throw new runtime.RequiredError(
                'wildcard1',
                'Required parameter "wildcard1" was null or undefined when calling putSentryByWildcard1().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/{wildcard1}`;
        urlPath = urlPath.replace(`{${"wildcard1"}}`, encodeURIComponent(String(requestParameters['wildcard1'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The one replace on the Sentry surface: updating an error ISSUE — resolving it, ignoring it, or assigning it — and answering the updated issue.  Nothing else here takes a replace. A project is created and deleted but never replaced, and the event and trace planes are append-only telemetry, so an issue\'s lifecycle is the only mutable state this face exposes.  Requires a validated, org-scoped principal with edit rights; a viewer is refused. The write is confined to the org minted from that principal\'s claim, so an issue id belonging to another tenant is simply not found. Before the runtime is initialized, 503.
     * Move an error issue through its lifecycle
     */
    async putSentryByWildcard1(requestParameters: SentryApiPutSentryByWildcard1Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.putSentryByWildcard1Raw(requestParameters, initOverrides);
    }

    /**
     * Changes an issue\'s lifecycle — resolve, ignore, reopen or assign — and returns the updated issue. Fields left unset are left unchanged.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Changes an issue\'s lifecycle — resolve, ignore, reopen or assign — and returns the updated issue.
     */
    async putSentryIssuesByIdRaw(requestParameters: SentryApiPutSentryIssuesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<O11yO11yErrorIssueOut>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putSentryIssuesById().'
            );
        }

        if (requestParameters['o11yO11ySentryUpdateIssueIn'] == null) {
            throw new runtime.RequiredError(
                'o11yO11ySentryUpdateIssueIn',
                'Required parameter "o11yO11ySentryUpdateIssueIn" was null or undefined when calling putSentryIssuesById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/sentry/issues/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: O11yO11ySentryUpdateIssueInToJSON(requestParameters['o11yO11ySentryUpdateIssueIn']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => O11yO11yErrorIssueOutFromJSON(jsonValue));
    }

    /**
     * Changes an issue\'s lifecycle — resolve, ignore, reopen or assign — and returns the updated issue. Fields left unset are left unchanged.  Callers need the editor role; the runtime\'s own gate enforces it.
     * Changes an issue\'s lifecycle — resolve, ignore, reopen or assign — and returns the updated issue.
     */
    async putSentryIssuesById(requestParameters: SentryApiPutSentryIssuesByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<O11yO11yErrorIssueOut> {
        const response = await this.putSentryIssuesByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
