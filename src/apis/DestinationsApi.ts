/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime.js';
import type {
  DestinationDisconnected,
  DestinationList,
  DestinationStatus,
  DestinationTest,
} from '../models/index.js';
import {
    DestinationDisconnectedFromJSON,
    DestinationDisconnectedToJSON,
    DestinationListFromJSON,
    DestinationListToJSON,
    DestinationStatusFromJSON,
    DestinationStatusToJSON,
    DestinationTestFromJSON,
    DestinationTestToJSON,
} from '../models/index.js';

export interface DestinationsApiDeleteDestinationsByPlatformRequest {
    platform: string;
}

export interface DestinationsApiGetDestinationsByPlatformRequest {
    platform: string;
}

export interface DestinationsApiPostDestinationsByPlatformRequest {
    platform: string;
    requestBody?: { [key: string]: any; };
}

export interface DestinationsApiPostDestinationsByPlatformTestRequest {
    platform: string;
}

/**
 * 
 */
export class DestinationsApi extends runtime.BaseAPI {

    /**
     * Forgets a destination for the caller\'s org: every credential held in KMS, then the stored config. Idempotent, and it requires org admin.
     * Forgets a destination for the caller\'s org: every credential held in KMS, then the stored config.
     */
    async deleteDestinationsByPlatformRaw(requestParameters: DestinationsApiDeleteDestinationsByPlatformRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DestinationDisconnected>> {
        if (requestParameters['platform'] == null) {
            throw new runtime.RequiredError(
                'platform',
                'Required parameter "platform" was null or undefined when calling deleteDestinationsByPlatform().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/destinations/{platform}`;
        urlPath = urlPath.replace(`{${"platform"}}`, encodeURIComponent(String(requestParameters['platform'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DestinationDisconnectedFromJSON(jsonValue));
    }

    /**
     * Forgets a destination for the caller\'s org: every credential held in KMS, then the stored config. Idempotent, and it requires org admin.
     * Forgets a destination for the caller\'s org: every credential held in KMS, then the stored config.
     */
    async deleteDestinationsByPlatform(requestParameters: DestinationsApiDeleteDestinationsByPlatformRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DestinationDisconnected> {
        const response = await this.deleteDestinationsByPlatformRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports every destination this deployment can forward to, each with the caller org\'s connection state: whether it is connected, whether it is enabled, whether a credential resolves right now, and the config fields the console renders for it.
     * Reports every destination this deployment can forward to, each with the caller org\'s connection state: whether it is connected, whether it is enabled, whether a credential resolves right now, and the config fields the console renders for it.
     */
    async getDestinationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DestinationList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/destinations`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DestinationListFromJSON(jsonValue));
    }

    /**
     * Reports every destination this deployment can forward to, each with the caller org\'s connection state: whether it is connected, whether it is enabled, whether a credential resolves right now, and the config fields the console renders for it.
     * Reports every destination this deployment can forward to, each with the caller org\'s connection state: whether it is connected, whether it is enabled, whether a credential resolves right now, and the config fields the console renders for it.
     */
    async getDestinations(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DestinationList> {
        const response = await this.getDestinationsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports one destination\'s card for the caller\'s org — its config fields, its connection state, and whether a credential resolves right now. A platform this deployment does not carry is not found.
     * Reports one destination\'s card for the caller\'s org — its config fields, its connection state, and whether a credential resolves right now.
     */
    async getDestinationsByPlatformRaw(requestParameters: DestinationsApiGetDestinationsByPlatformRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DestinationStatus>> {
        if (requestParameters['platform'] == null) {
            throw new runtime.RequiredError(
                'platform',
                'Required parameter "platform" was null or undefined when calling getDestinationsByPlatform().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/destinations/{platform}`;
        urlPath = urlPath.replace(`{${"platform"}}`, encodeURIComponent(String(requestParameters['platform'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DestinationStatusFromJSON(jsonValue));
    }

    /**
     * Reports one destination\'s card for the caller\'s org — its config fields, its connection state, and whether a credential resolves right now. A platform this deployment does not carry is not found.
     * Reports one destination\'s card for the caller\'s org — its config fields, its connection state, and whether a credential resolves right now.
     */
    async getDestinationsByPlatform(requestParameters: DestinationsApiGetDestinationsByPlatformRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DestinationStatus> {
        const response = await this.getDestinationsByPlatformRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stores the addressed platform\'s non-secret ids (its measurement, pixel or dataset ids) and seals its API credential into KMS under a path scoped to the caller\'s own org, then answers the same status card the read routes do — with live telling you whether the credential actually resolves right now. The body\'s property NAMES are the platform\'s own: each field the platform declares, plus each secret under its camelCase name, so the accepted keys differ per platform and a missing REQUIRED field is refused. Connecting is an ORG ADMIN action — a validated member without the admin bit gets 403 — and it fails closed with 503 when the KMS master key is unavailable rather than persisting a destination whose secret was never sealed. The secret itself never appears in the response, in the store, or in a log line; only its NAME is ever published. Set enabled to false to keep the connection but stop the analytics fan-out to it.
     * Connect one conversion destination for your org, or update the one you have
     */
    async postDestinationsByPlatformRaw(requestParameters: DestinationsApiPostDestinationsByPlatformRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DestinationStatus>> {
        if (requestParameters['platform'] == null) {
            throw new runtime.RequiredError(
                'platform',
                'Required parameter "platform" was null or undefined when calling postDestinationsByPlatform().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/destinations/{platform}`;
        urlPath = urlPath.replace(`{${"platform"}}`, encodeURIComponent(String(requestParameters['platform'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['requestBody'],
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DestinationStatusFromJSON(jsonValue));
    }

    /**
     * Stores the addressed platform\'s non-secret ids (its measurement, pixel or dataset ids) and seals its API credential into KMS under a path scoped to the caller\'s own org, then answers the same status card the read routes do — with live telling you whether the credential actually resolves right now. The body\'s property NAMES are the platform\'s own: each field the platform declares, plus each secret under its camelCase name, so the accepted keys differ per platform and a missing REQUIRED field is refused. Connecting is an ORG ADMIN action — a validated member without the admin bit gets 403 — and it fails closed with 503 when the KMS master key is unavailable rather than persisting a destination whose secret was never sealed. The secret itself never appears in the response, in the store, or in a log line; only its NAME is ever published. Set enabled to false to keep the connection but stop the analytics fan-out to it.
     * Connect one conversion destination for your org, or update the one you have
     */
    async postDestinationsByPlatform(requestParameters: DestinationsApiPostDestinationsByPlatformRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DestinationStatus> {
        const response = await this.postDestinationsByPlatformRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sends ONE synthetic pageview through the connected destination end to end and reports what the platform said. A send the platform refuses is reported as data — {\"ok\": false, \"error\": …} at 200 — so the console shows the platform\'s own words rather than an error about Hanzo. It requires org admin.
     * Sends ONE synthetic pageview through the connected destination end to end and reports what the platform said.
     */
    async postDestinationsByPlatformTestRaw(requestParameters: DestinationsApiPostDestinationsByPlatformTestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DestinationTest>> {
        if (requestParameters['platform'] == null) {
            throw new runtime.RequiredError(
                'platform',
                'Required parameter "platform" was null or undefined when calling postDestinationsByPlatformTest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/destinations/{platform}/test`;
        urlPath = urlPath.replace(`{${"platform"}}`, encodeURIComponent(String(requestParameters['platform'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DestinationTestFromJSON(jsonValue));
    }

    /**
     * Sends ONE synthetic pageview through the connected destination end to end and reports what the platform said. A send the platform refuses is reported as data — {\"ok\": false, \"error\": …} at 200 — so the console shows the platform\'s own words rather than an error about Hanzo. It requires org admin.
     * Sends ONE synthetic pageview through the connected destination end to end and reports what the platform said.
     */
    async postDestinationsByPlatformTest(requestParameters: DestinationsApiPostDestinationsByPlatformTestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DestinationTest> {
        const response = await this.postDestinationsByPlatformTestRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
