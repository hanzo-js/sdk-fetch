/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface CartItemSet
 */
export interface CartItemSet {
    /**
     * ID is the cart to amend, from the path.
     * @type {string}
     * @memberof CartItemSet
     */
    id?: string;
    /**
     * Product names the catalog product to set, by its id or its URL slug. Give
     * this or Variant, never both; a request naming neither is refused.
     * @type {string}
     * @memberof CartItemSet
     */
    product?: string;
    /**
     * Quantity is how many of that item the cart should hold AFTER this call — it
     * is the resulting count, not a delta, so sending 3 twice leaves 3 and not 6.
     * ZERO REMOVES the line, which is the only way to take an item out.
     * @type {number}
     * @memberof CartItemSet
     */
    quantity?: number;
    /**
     * Variant names the specific sellable variant to set, by its id or its SKU.
     * Prefer it over Product for anything sold in sizes, colours or tiers — the
     * price and the stock are the variant's, not the product's.
     * @type {string}
     * @memberof CartItemSet
     */
    variant?: string;
}

/**
 * Check if a given object implements the CartItemSet interface.
 */
export function instanceOfCartItemSet(value: object): value is CartItemSet {
    return true;
}

export function CartItemSetFromJSON(json: any): CartItemSet {
    return CartItemSetFromJSONTyped(json, false);
}

export function CartItemSetFromJSONTyped(json: any, ignoreDiscriminator: boolean): CartItemSet {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'] == null ? undefined : json['id'],
        'product': json['product'] == null ? undefined : json['product'],
        'quantity': json['quantity'] == null ? undefined : json['quantity'],
        'variant': json['variant'] == null ? undefined : json['variant'],
    };
}

export function CartItemSetToJSON(json: any): CartItemSet {
    return CartItemSetToJSONTyped(json, false);
}

export function CartItemSetToJSONTyped(value?: CartItemSet | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'product': value['product'],
        'quantity': value['quantity'],
        'variant': value['variant'],
    };
}

