/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface NotifySend
 */
export interface NotifySend {
    /**
     * Body is the message text, sent verbatim when present — the no-template path.
     * @type {string}
     * @memberof NotifySend
     */
    body?: string;
    /**
     * Channel selects the delivery channel, sms or email. The per-channel routes
     * (/send/sms, /send/email) pin it, overriding whatever the body names; on the
     * generic route it is required.
     * @type {string}
     * @memberof NotifySend
     */
    channel?: string;
    /**
     * Event is the event name, which doubles as the template id when TemplateID is
     * empty — the IAM OTP path sends event=iam.otp_sent and nothing else.
     * @type {string}
     * @memberof NotifySend
     */
    event?: string;
    /**
     * Provider pins a provider service name (twilio, plivo, twilio_email, mail).
     * Empty picks the one whose org credentials are actually configured in KMS.
     * @type {string}
     * @memberof NotifySend
     */
    provider?: string;
    /**
     * Subject is the message subject, carried on the email channel only.
     * @type {string}
     * @memberof NotifySend
     */
    subject?: string;
    /**
     * Sync must be exactly "true": delivery here is synchronous, and anything else
     * answers 503 because the queue plane that would run an async dispatch is owned
     * elsewhere. Over REST it rides as ?sync=true (the URL binds over the body); a
     * by-name call states it in its arguments.
     * @type {string}
     * @memberof NotifySend
     */
    sync?: string;
    /**
     * TemplateID selects a built-in template when Body is empty.
     * @type {string}
     * @memberof NotifySend
     */
    templateId?: string;
    /**
     * 
     * @type {any}
     * @memberof NotifySend
     */
    templateVars?: any | null;
    /**
     * To is the destination address per recipient — a phone number for sms, an
     * email address for email. Several recipients fan out into one provider call
     * each, and the response shape follows the count (see the items field).
     * @type {Array<string>}
     * @memberof NotifySend
     */
    to?: Array<string>;
}

/**
 * Check if a given object implements the NotifySend interface.
 */
export function instanceOfNotifySend(value: object): value is NotifySend {
    return true;
}

export function NotifySendFromJSON(json: any): NotifySend {
    return NotifySendFromJSONTyped(json, false);
}

export function NotifySendFromJSONTyped(json: any, ignoreDiscriminator: boolean): NotifySend {
    if (json == null) {
        return json;
    }
    return {
        
        'body': json['body'] == null ? undefined : json['body'],
        'channel': json['channel'] == null ? undefined : json['channel'],
        'event': json['event'] == null ? undefined : json['event'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'subject': json['subject'] == null ? undefined : json['subject'],
        'sync': json['sync'] == null ? undefined : json['sync'],
        'templateId': json['template_id'] == null ? undefined : json['template_id'],
        'templateVars': json['template_vars'] == null ? undefined : json['template_vars'],
        'to': json['to'] == null ? undefined : json['to'],
    };
}

export function NotifySendToJSON(json: any): NotifySend {
    return NotifySendToJSONTyped(json, false);
}

export function NotifySendToJSONTyped(value?: NotifySend | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'body': value['body'],
        'channel': value['channel'],
        'event': value['event'],
        'provider': value['provider'],
        'subject': value['subject'],
        'sync': value['sync'],
        'template_id': value['templateId'],
        'template_vars': value['templateVars'],
        'to': value['to'],
    };
}

