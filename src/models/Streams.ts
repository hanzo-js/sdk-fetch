/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Stream } from './Stream.js';
import {
    StreamFromJSON,
    StreamFromJSONTyped,
    StreamToJSON,
    StreamToJSONTyped,
} from './Stream.js';

/**
 * 
 * @export
 * @interface Streams
 */
export interface Streams {
    /**
     * Streams is the page, ordered by name.
     * @type {Array<Stream>}
     * @memberof Streams
     */
    streams?: Array<Stream>;
    /**
     * Total is the org's stream count before paging.
     * @type {number}
     * @memberof Streams
     */
    total?: number;
}

/**
 * Check if a given object implements the Streams interface.
 */
export function instanceOfStreams(value: object): value is Streams {
    return true;
}

export function StreamsFromJSON(json: any): Streams {
    return StreamsFromJSONTyped(json, false);
}

export function StreamsFromJSONTyped(json: any, ignoreDiscriminator: boolean): Streams {
    if (json == null) {
        return json;
    }
    return {
        
        'streams': json['streams'] == null ? undefined : ((json['streams'] as Array<any>).map(StreamFromJSON)),
        'total': json['total'] == null ? undefined : json['total'],
    };
}

export function StreamsToJSON(json: any): Streams {
    return StreamsToJSONTyped(json, false);
}

export function StreamsToJSONTyped(value?: Streams | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'streams': value['streams'] == null ? undefined : ((value['streams'] as Array<any>).map(StreamToJSON)),
        'total': value['total'],
    };
}

