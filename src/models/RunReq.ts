/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { EnvVarJSON } from './EnvVarJSON.js';
import {
    EnvVarJSONFromJSON,
    EnvVarJSONFromJSONTyped,
    EnvVarJSONToJSON,
    EnvVarJSONToJSONTyped,
} from './EnvVarJSON.js';

/**
 * 
 * @export
 * @interface RunReq
 */
export interface RunReq {
    /**
     * Env is the run's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`;
     * a variable marked `secret: true` is sealed into KMS.
     * @type {Array<EnvVarJSON>}
     * @memberof RunReq
     */
    env?: Array<EnvVarJSON>;
    /**
     * GPU is how many GPUs the run asks for; a negative value is 400.
     * @type {number}
     * @memberof RunReq
     */
    gpu?: number;
    /**
     * Image is the container image to run. Required.
     * @type {string}
     * @memberof RunReq
     */
    image?: string;
    /**
     * MaxScale above the floor declares an autoscaling ceiling; 0 means no
     * autoscaler at all — a fixed run at the floor.
     * @type {number}
     * @memberof RunReq
     */
    maxScale?: number;
    /**
     * MinScale is the replica floor, clamped to the deployment's limit.
     * @type {number}
     * @memberof RunReq
     */
    minScale?: number;
    /**
     * Name is the run's name, and the slug is derived from it. Required, and it
     * must resolve to `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`. Re-running the same
     * name updates that run in place.
     * @type {string}
     * @memberof RunReq
     */
    name?: string;
    /**
     * Port is the container port the run listens on.
     * @type {number}
     * @memberof RunReq
     */
    port?: number;
    /**
     * Runtime is accepted for the client contract and echoed nowhere: the image
     * IS the runtime unit.
     * @type {string}
     * @memberof RunReq
     */
    runtime?: string;
    /**
     * Shape is a compute size label, echoed back; sizing is the operator's
     * default. Defaults to "auto".
     * @type {string}
     * @memberof RunReq
     */
    shape?: string;
}

/**
 * Check if a given object implements the RunReq interface.
 */
export function instanceOfRunReq(value: object): value is RunReq {
    return true;
}

export function RunReqFromJSON(json: any): RunReq {
    return RunReqFromJSONTyped(json, false);
}

export function RunReqFromJSONTyped(json: any, ignoreDiscriminator: boolean): RunReq {
    if (json == null) {
        return json;
    }
    return {
        
        'env': json['env'] == null ? undefined : ((json['env'] as Array<any>).map(EnvVarJSONFromJSON)),
        'gpu': json['gpu'] == null ? undefined : json['gpu'],
        'image': json['image'] == null ? undefined : json['image'],
        'maxScale': json['maxScale'] == null ? undefined : json['maxScale'],
        'minScale': json['minScale'] == null ? undefined : json['minScale'],
        'name': json['name'] == null ? undefined : json['name'],
        'port': json['port'] == null ? undefined : json['port'],
        'runtime': json['runtime'] == null ? undefined : json['runtime'],
        'shape': json['shape'] == null ? undefined : json['shape'],
    };
}

export function RunReqToJSON(json: any): RunReq {
    return RunReqToJSONTyped(json, false);
}

export function RunReqToJSONTyped(value?: RunReq | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'env': value['env'] == null ? undefined : ((value['env'] as Array<any>).map(EnvVarJSONToJSON)),
        'gpu': value['gpu'],
        'image': value['image'],
        'maxScale': value['maxScale'],
        'minScale': value['minScale'],
        'name': value['name'],
        'port': value['port'],
        'runtime': value['runtime'],
        'shape': value['shape'],
    };
}

