/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { EventView } from './EventView.js';
import {
    EventViewFromJSON,
    EventViewFromJSONTyped,
    EventViewToJSON,
    EventViewToJSONTyped,
} from './EventView.js';

/**
 * 
 * @export
 * @interface ControlResult
 */
export interface ControlResult {
    /**
     * Command is the verb that was recorded: pause, resume, stop or message.
     * @type {string}
     * @memberof ControlResult
     */
    command?: string;
    /**
     * Event is the durable control event the command became. The intent is
     * recorded whether or not it reached an engine, which is what makes a
     * stream-consuming surface able to act on it.
     * @type {EventView}
     * @memberof ControlResult
     */
    event?: EventView;
    /**
     * Forwarded is whether the command also reached the durable-execution engine.
     * FALSE IS NOT A FAILURE: a session with no workflow link, or a deployment
     * with no tasks backend, is record-only by design. A forward that was
     * attempted and failed is a 502, never a false here.
     * @type {boolean}
     * @memberof ControlResult
     */
    forwarded?: boolean;
}

/**
 * Check if a given object implements the ControlResult interface.
 */
export function instanceOfControlResult(value: object): value is ControlResult {
    return true;
}

export function ControlResultFromJSON(json: any): ControlResult {
    return ControlResultFromJSONTyped(json, false);
}

export function ControlResultFromJSONTyped(json: any, ignoreDiscriminator: boolean): ControlResult {
    if (json == null) {
        return json;
    }
    return {
        
        'command': json['command'] == null ? undefined : json['command'],
        'event': json['event'] == null ? undefined : EventViewFromJSON(json['event']),
        'forwarded': json['forwarded'] == null ? undefined : json['forwarded'],
    };
}

export function ControlResultToJSON(json: any): ControlResult {
    return ControlResultToJSONTyped(json, false);
}

export function ControlResultToJSONTyped(value?: ControlResult | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'command': value['command'],
        'event': EventViewToJSON(value['event']),
        'forwarded': value['forwarded'],
    };
}

