/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface IdentityIn
 */
export interface IdentityIn {
    /**
     * Name is the device's name within the org — a DNS label. The fabric knows
     * the identity as "<name>.<org>"; every answer here uses the caller's name.
     * @type {string}
     * @memberof IdentityIn
     */
    name?: string;
    /**
     * Roles are extra role attributes for the identity, each scoped to the
     * caller's org on the way in ("k3s-host" is written as "k3s-host.<org>") so
     * no caller can claim an attribute another tenant's policy selects. A role
     * of the form "<service>-host" makes this identity a HOST of that published
     * service — the bind policy from POST /v1/network/services selects exactly
     * that attribute — and is refused when the org has no such service.
     * @type {Array<string>}
     * @memberof IdentityIn
     */
    roles?: Array<string>;
}

/**
 * Check if a given object implements the IdentityIn interface.
 */
export function instanceOfIdentityIn(value: object): value is IdentityIn {
    return true;
}

export function IdentityInFromJSON(json: any): IdentityIn {
    return IdentityInFromJSONTyped(json, false);
}

export function IdentityInFromJSONTyped(json: any, ignoreDiscriminator: boolean): IdentityIn {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'] == null ? undefined : json['name'],
        'roles': json['roles'] == null ? undefined : json['roles'],
    };
}

export function IdentityInToJSON(json: any): IdentityIn {
    return IdentityInToJSONTyped(json, false);
}

export function IdentityInToJSONTyped(value?: IdentityIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'roles': value['roles'],
    };
}

