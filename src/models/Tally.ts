/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface Tally
 */
export interface Tally {
    /**
     * AccruedLifetimeCents is all commission ever accrued, summed across every
     * affiliate, in cents. It only grows; a payout does not reduce it.
     * @type {number}
     * @memberof Tally
     */
    accruedLifetimeCents?: number;
    /**
     * Affiliates is how many affiliate rows the board read, at every status. The
     * read is bounded at 1000 rows, so a larger fleet reports the bound.
     * @type {number}
     * @memberof Tally
     */
    affiliates?: number;
    /**
     * Approved is how many of those rows are approved — the only ones whose code
     * resolves for attribution and whose balance can grow.
     * @type {number}
     * @memberof Tally
     */
    approved?: number;
    /**
     * PaidLifetimeCents is all commission ever paid out, in cents: credits grants
     * plus record-only cash disbursements.
     * @type {number}
     * @memberof Tally
     */
    paidLifetimeCents?: number;
    /**
     * PendingLiabilityCents is accrued minus paid across every affiliate, in cents.
     * Read it as money OWED and not yet disbursed — a liability, not spend.
     * @type {number}
     * @memberof Tally
     */
    pendingLiabilityCents?: number;
}

/**
 * Check if a given object implements the Tally interface.
 */
export function instanceOfTally(value: object): value is Tally {
    return true;
}

export function TallyFromJSON(json: any): Tally {
    return TallyFromJSONTyped(json, false);
}

export function TallyFromJSONTyped(json: any, ignoreDiscriminator: boolean): Tally {
    if (json == null) {
        return json;
    }
    return {
        
        'accruedLifetimeCents': json['accruedLifetimeCents'] == null ? undefined : json['accruedLifetimeCents'],
        'affiliates': json['affiliates'] == null ? undefined : json['affiliates'],
        'approved': json['approved'] == null ? undefined : json['approved'],
        'paidLifetimeCents': json['paidLifetimeCents'] == null ? undefined : json['paidLifetimeCents'],
        'pendingLiabilityCents': json['pendingLiabilityCents'] == null ? undefined : json['pendingLiabilityCents'],
    };
}

export function TallyToJSON(json: any): Tally {
    return TallyToJSONTyped(json, false);
}

export function TallyToJSONTyped(value?: Tally | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'accruedLifetimeCents': value['accruedLifetimeCents'],
        'affiliates': value['affiliates'],
        'approved': value['approved'],
        'paidLifetimeCents': value['paidLifetimeCents'],
        'pendingLiabilityCents': value['pendingLiabilityCents'],
    };
}

