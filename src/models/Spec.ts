/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { GPU } from './GPU.js';
import {
    GPUFromJSON,
    GPUFromJSONTyped,
    GPUToJSON,
    GPUToJSONTyped,
} from './GPU.js';

/**
 * 
 * @export
 * @interface Spec
 */
export interface Spec {
    /**
     * amd64 | arm64 | ...
     * @type {string}
     * @memberof Spec
     */
    arch?: string;
    /**
     * logical cores
     * @type {number}
     * @memberof Spec
     */
    cpus?: number;
    /**
     * GPUs is every accelerator the machine advertises, one entry each, capped at
     * 32 on write. Empty means the probe found none — and that is the answer a
     * Need is checked against, so a machine with no entry here clears no
     * accelerator floor. The list is not vendor-filtered: what satisfies a job is
     * counts and VRAM, never a brand (see Need).
     * @type {Array<GPU>}
     * @memberof Spec
     */
    gpus?: Array<GPU>;
    /**
     * total RAM, bytes
     * @type {number}
     * @memberof Spec
     */
    memory?: number;
    /**
     * linux | darwin | windows
     * @type {string}
     * @memberof Spec
     */
    os?: string;
}

/**
 * Check if a given object implements the Spec interface.
 */
export function instanceOfSpec(value: object): value is Spec {
    return true;
}

export function SpecFromJSON(json: any): Spec {
    return SpecFromJSONTyped(json, false);
}

export function SpecFromJSONTyped(json: any, ignoreDiscriminator: boolean): Spec {
    if (json == null) {
        return json;
    }
    return {
        
        'arch': json['arch'] == null ? undefined : json['arch'],
        'cpus': json['cpus'] == null ? undefined : json['cpus'],
        'gpus': json['gpus'] == null ? undefined : ((json['gpus'] as Array<any>).map(GPUFromJSON)),
        'memory': json['memory'] == null ? undefined : json['memory'],
        'os': json['os'] == null ? undefined : json['os'],
    };
}

export function SpecToJSON(json: any): Spec {
    return SpecToJSONTyped(json, false);
}

export function SpecToJSONTyped(value?: Spec | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'arch': value['arch'],
        'cpus': value['cpus'],
        'gpus': value['gpus'] == null ? undefined : ((value['gpus'] as Array<any>).map(GPUToJSON)),
        'memory': value['memory'],
        'os': value['os'],
    };
}

