/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Strategy } from './Strategy.js';
import {
    StrategyFromJSON,
    StrategyFromJSONTyped,
    StrategyToJSON,
    StrategyToJSONTyped,
} from './Strategy.js';
import type { Page } from './Page.js';
import {
    PageFromJSON,
    PageFromJSONTyped,
    PageToJSON,
    PageToJSONTyped,
} from './Page.js';
import type { JourneyStep } from './JourneyStep.js';
import {
    JourneyStepFromJSON,
    JourneyStepFromJSONTyped,
    JourneyStepToJSON,
    JourneyStepToJSONTyped,
} from './JourneyStep.js';
import type { Principle } from './Principle.js';
import {
    PrincipleFromJSON,
    PrincipleFromJSONTyped,
    PrincipleToJSON,
    PrincipleToJSONTyped,
} from './Principle.js';
import type { Section } from './Section.js';
import {
    SectionFromJSON,
    SectionFromJSONTyped,
    SectionToJSON,
    SectionToJSONTyped,
} from './Section.js';

/**
 * 
 * @export
 * @interface Blueprint
 */
export interface Blueprint {
    /**
     * Brand is the white-label key this playbook serves. Empty is the shared
     * default every unbranded deployment falls back to.
     * @type {string}
     * @memberof Blueprint
     */
    brand?: string;
    /**
     * Enabled is the whole-playbook lever. Absent reads as ON; an explicit false
     * makes resolution skip this playbook entirely and fall through to the next
     * tier, rather than serving an empty journey.
     * @type {boolean}
     * @memberof Blueprint
     */
    enabled?: boolean;
    /**
     * Principles are the fixed 64-archetype spine a tactic files under. It is
     * authored data an operator organises the corpus by; nothing in the checklist
     * engine reads it.
     * @type {Array<Principle>}
     * @memberof Blueprint
     */
    principles?: Array<Principle>;
    /**
     * Sections are the journey's ordered phases.
     * @type {Array<Section>}
     * @memberof Blueprint
     */
    sections?: Array<Section>;
    /**
     * Steps are every checklist item, disabled ones included — this is the authored
     * document, not the projection an org runs.
     * @type {Array<JourneyStep>}
     * @memberof Blueprint
     */
    steps?: Array<JourneyStep>;
    /**
     * Strategies are the tactics corpus the recommendation reads narrow.
     * @type {Array<Strategy>}
     * @memberof Blueprint
     */
    strategies?: Array<Strategy>;
    /**
     * Templates are the reusable prompts and snippets steps reference by id.
     * @type {Array<Page>}
     * @memberof Blueprint
     */
    templates?: Array<Page>;
    /**
     * Title is the playbook's name as a person reads it.
     * @type {string}
     * @memberof Blueprint
     */
    title?: string;
    /**
     * Version is the playbook's own name for this edition of its content, chosen by
     * whoever authored it. It travels onto every journey projected from it. The
     * store's numeric revision is a separate value and lives beside it.
     * @type {string}
     * @memberof Blueprint
     */
    version?: string;
}

/**
 * Check if a given object implements the Blueprint interface.
 */
export function instanceOfBlueprint(value: object): value is Blueprint {
    return true;
}

export function BlueprintFromJSON(json: any): Blueprint {
    return BlueprintFromJSONTyped(json, false);
}

export function BlueprintFromJSONTyped(json: any, ignoreDiscriminator: boolean): Blueprint {
    if (json == null) {
        return json;
    }
    return {
        
        'brand': json['brand'] == null ? undefined : json['brand'],
        'enabled': json['enabled'] == null ? undefined : json['enabled'],
        'principles': json['principles'] == null ? undefined : ((json['principles'] as Array<any>).map(PrincipleFromJSON)),
        'sections': json['sections'] == null ? undefined : ((json['sections'] as Array<any>).map(SectionFromJSON)),
        'steps': json['steps'] == null ? undefined : ((json['steps'] as Array<any>).map(JourneyStepFromJSON)),
        'strategies': json['strategies'] == null ? undefined : ((json['strategies'] as Array<any>).map(StrategyFromJSON)),
        'templates': json['templates'] == null ? undefined : ((json['templates'] as Array<any>).map(PageFromJSON)),
        'title': json['title'] == null ? undefined : json['title'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function BlueprintToJSON(json: any): Blueprint {
    return BlueprintToJSONTyped(json, false);
}

export function BlueprintToJSONTyped(value?: Blueprint | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'brand': value['brand'],
        'enabled': value['enabled'],
        'principles': value['principles'] == null ? undefined : ((value['principles'] as Array<any>).map(PrincipleToJSON)),
        'sections': value['sections'] == null ? undefined : ((value['sections'] as Array<any>).map(SectionToJSON)),
        'steps': value['steps'] == null ? undefined : ((value['steps'] as Array<any>).map(JourneyStepToJSON)),
        'strategies': value['strategies'] == null ? undefined : ((value['strategies'] as Array<any>).map(StrategyToJSON)),
        'templates': value['templates'] == null ? undefined : ((value['templates'] as Array<any>).map(PageToJSON)),
        'title': value['title'],
        'version': value['version'],
    };
}

