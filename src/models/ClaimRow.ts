/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface ClaimRow
 */
export interface ClaimRow {
    /**
     * At is when a stored row was recorded. Zero for a seed row.
     * @type {Date}
     * @memberof ClaimRow
     */
    at?: Date;
    /**
     * Benchmark is the canonical test id the claim is about, from /catalog.
     * @type {string}
     * @memberof ClaimRow
     */
    benchmark?: string;
    /**
     * By is who recorded it, when the caller said.
     * @type {string}
     * @memberof ClaimRow
     */
    by?: string;
    /**
     * Model is the system the score is claimed for.
     * @type {string}
     * @memberof ClaimRow
     */
    model?: string;
    /**
     * Origin is "seed" for a compiled row and "stored" for one written through
     * this surface. It is the difference between what we shipped and what an
     * operator has since corrected.
     * @type {string}
     * @memberof ClaimRow
     */
    origin?: string;
    /**
     * Protocol records HOW it was scored — provider-reported, agentic,
     * third-party-leaderboard — so a provider card is never read as a measurement.
     * @type {string}
     * @memberof ClaimRow
     */
    protocol?: string;
    /**
     * Provider is who the claim belongs to — the lab or leaderboard whose number
     * this is.
     * @type {string}
     * @memberof ClaimRow
     */
    provider?: string;
    /**
     * Score is the reported aggregate, as a percentage.
     * @type {number}
     * @memberof ClaimRow
     */
    score?: number;
    /**
     * Source is the citation the row was read from.
     * @type {string}
     * @memberof ClaimRow
     */
    source?: string;
}

/**
 * Check if a given object implements the ClaimRow interface.
 */
export function instanceOfClaimRow(value: object): value is ClaimRow {
    return true;
}

export function ClaimRowFromJSON(json: any): ClaimRow {
    return ClaimRowFromJSONTyped(json, false);
}

export function ClaimRowFromJSONTyped(json: any, ignoreDiscriminator: boolean): ClaimRow {
    if (json == null) {
        return json;
    }
    return {
        
        'at': json['at'] == null ? undefined : (new Date(json['at'])),
        'benchmark': json['benchmark'] == null ? undefined : json['benchmark'],
        'by': json['by'] == null ? undefined : json['by'],
        'model': json['model'] == null ? undefined : json['model'],
        'origin': json['origin'] == null ? undefined : json['origin'],
        'protocol': json['protocol'] == null ? undefined : json['protocol'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'score': json['score'] == null ? undefined : json['score'],
        'source': json['source'] == null ? undefined : json['source'],
    };
}

export function ClaimRowToJSON(json: any): ClaimRow {
    return ClaimRowToJSONTyped(json, false);
}

export function ClaimRowToJSONTyped(value?: ClaimRow | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'at': value['at'] == null ? undefined : ((value['at']).toISOString()),
        'benchmark': value['benchmark'],
        'by': value['by'],
        'model': value['model'],
        'origin': value['origin'],
        'protocol': value['protocol'],
        'provider': value['provider'],
        'score': value['score'],
        'source': value['source'],
    };
}

