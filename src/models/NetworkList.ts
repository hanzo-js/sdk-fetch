/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { NetworkView } from './NetworkView.js';
import {
    NetworkViewFromJSON,
    NetworkViewFromJSONTyped,
    NetworkViewToJSON,
    NetworkViewToJSONTyped,
} from './NetworkView.js';

/**
 * 
 * @export
 * @interface NetworkList
 */
export interface NetworkList {
    /**
     * Networks holds the org's overlay network, or is empty when the org has no
     * edge-routers on the fabric (no nodes → no network, never a fabricated one).
     * @type {Array<NetworkView>}
     * @memberof NetworkList
     */
    networks?: Array<NetworkView>;
}

/**
 * Check if a given object implements the NetworkList interface.
 */
export function instanceOfNetworkList(value: object): value is NetworkList {
    return true;
}

export function NetworkListFromJSON(json: any): NetworkList {
    return NetworkListFromJSONTyped(json, false);
}

export function NetworkListFromJSONTyped(json: any, ignoreDiscriminator: boolean): NetworkList {
    if (json == null) {
        return json;
    }
    return {
        
        'networks': json['networks'] == null ? undefined : ((json['networks'] as Array<any>).map(NetworkViewFromJSON)),
    };
}

export function NetworkListToJSON(json: any): NetworkList {
    return NetworkListToJSONTyped(json, false);
}

export function NetworkListToJSONTyped(value?: NetworkList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'networks': value['networks'] == null ? undefined : ((value['networks'] as Array<any>).map(NetworkViewToJSON)),
    };
}

