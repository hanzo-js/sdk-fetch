/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { CodeFile } from './CodeFile.js';
import {
    CodeFileFromJSON,
    CodeFileFromJSONTyped,
    CodeFileToJSON,
    CodeFileToJSONTyped,
} from './CodeFile.js';

/**
 * 
 * @export
 * @interface CodeResult
 */
export interface CodeResult {
    /**
     * Files are what this run CREATED OR CHANGED, decided by mtime against a marker
     * taken before the program started — so it is the run's output, not a listing of
     * the directory. Fetch each from GET /v1/download/{session}/{id}.
     * @type {Array<CodeFile>}
     * @memberof CodeResult
     */
    files?: Array<CodeFile>;
    /**
     * SessionID is the sandbox this run used — the one that was passed in, or the
     * fresh one that was leased. Pass it to the next run to keep the filesystem.
     * @type {string}
     * @memberof CodeResult
     */
    sessionId?: string;
    /**
     * Stderr is what the program wrote to standard error, INCLUDING a compiler's
     * diagnostics and the trace of a program that exited non-zero. Its presence is
     * not a failed call.
     * @type {string}
     * @memberof CodeResult
     */
    stderr?: string;
    /**
     * Stdout is what the program wrote to standard output.
     * @type {string}
     * @memberof CodeResult
     */
    stdout?: string;
}

/**
 * Check if a given object implements the CodeResult interface.
 */
export function instanceOfCodeResult(value: object): value is CodeResult {
    return true;
}

export function CodeResultFromJSON(json: any): CodeResult {
    return CodeResultFromJSONTyped(json, false);
}

export function CodeResultFromJSONTyped(json: any, ignoreDiscriminator: boolean): CodeResult {
    if (json == null) {
        return json;
    }
    return {
        
        'files': json['files'] == null ? undefined : ((json['files'] as Array<any>).map(CodeFileFromJSON)),
        'sessionId': json['session_id'] == null ? undefined : json['session_id'],
        'stderr': json['stderr'] == null ? undefined : json['stderr'],
        'stdout': json['stdout'] == null ? undefined : json['stdout'],
    };
}

export function CodeResultToJSON(json: any): CodeResult {
    return CodeResultToJSONTyped(json, false);
}

export function CodeResultToJSONTyped(value?: CodeResult | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'files': value['files'] == null ? undefined : ((value['files'] as Array<any>).map(CodeFileToJSON)),
        'session_id': value['sessionId'],
        'stderr': value['stderr'],
        'stdout': value['stdout'],
    };
}

