/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Spend } from './Spend.js';
import {
    SpendFromJSON,
    SpendFromJSONTyped,
    SpendToJSON,
    SpendToJSONTyped,
} from './Spend.js';
import type { LLM } from './LLM.js';
import {
    LLMFromJSON,
    LLMFromJSONTyped,
    LLMToJSON,
    LLMToJSONTyped,
} from './LLM.js';
import type { Sources } from './Sources.js';
import {
    SourcesFromJSON,
    SourcesFromJSONTyped,
    SourcesToJSON,
    SourcesToJSONTyped,
} from './Sources.js';
import type { UsageScope } from './UsageScope.js';
import {
    UsageScopeFromJSON,
    UsageScopeFromJSONTyped,
    UsageScopeToJSON,
    UsageScopeToJSONTyped,
} from './UsageScope.js';
import type { Accounts } from './Accounts.js';
import {
    AccountsFromJSON,
    AccountsFromJSONTyped,
    AccountsToJSON,
    AccountsToJSONTyped,
} from './Accounts.js';

/**
 * 
 * @export
 * @interface UsageSummary
 */
export interface UsageSummary {
    /**
     * Accounts is the caller's own linked provider accounts beside the org's
     * Hanzo-routed usage, labelled row by row and never summed together.
     * @type {Accounts}
     * @memberof UsageSummary
     */
    accounts?: Accounts;
    /**
     * End is the window's exclusive end, RFC3339 UTC.
     * @type {string}
     * @memberof UsageSummary
     */
    end?: string;
    /**
     * Interval is the bucket width the spend series is gap-filled at.
     * @type {string}
     * @memberof UsageSummary
     */
    interval?: string;
    /**
     * LLM is the org's Hanzo-routed inference totals from the warehouse.
     * @type {LLM}
     * @memberof UsageSummary
     */
    llm?: LLM;
    /**
     * Range is the window label that was served.
     * @type {string}
     * @memberof UsageSummary
     */
    range?: string;
    /**
     * Scope is the tenant and subject the roll-up was answered for.
     * @type {UsageScope}
     * @memberof UsageSummary
     */
    scope?: UsageScope;
    /**
     * Sources says which upstreams actually answered, so a zero can be read as
     * "no data yet" rather than as a measurement.
     * @type {Sources}
     * @memberof UsageSummary
     */
    sources?: Sources;
    /**
     * Spend is the categorized cost roll-up from the billing ledger.
     * @type {Spend}
     * @memberof UsageSummary
     */
    spend?: Spend;
    /**
     * Start is the window's inclusive start, RFC3339 UTC.
     * @type {string}
     * @memberof UsageSummary
     */
    start?: string;
}

/**
 * Check if a given object implements the UsageSummary interface.
 */
export function instanceOfUsageSummary(value: object): value is UsageSummary {
    return true;
}

export function UsageSummaryFromJSON(json: any): UsageSummary {
    return UsageSummaryFromJSONTyped(json, false);
}

export function UsageSummaryFromJSONTyped(json: any, ignoreDiscriminator: boolean): UsageSummary {
    if (json == null) {
        return json;
    }
    return {
        
        'accounts': json['accounts'] == null ? undefined : AccountsFromJSON(json['accounts']),
        'end': json['end'] == null ? undefined : json['end'],
        'interval': json['interval'] == null ? undefined : json['interval'],
        'llm': json['llm'] == null ? undefined : LLMFromJSON(json['llm']),
        'range': json['range'] == null ? undefined : json['range'],
        'scope': json['scope'] == null ? undefined : UsageScopeFromJSON(json['scope']),
        'sources': json['sources'] == null ? undefined : SourcesFromJSON(json['sources']),
        'spend': json['spend'] == null ? undefined : SpendFromJSON(json['spend']),
        'start': json['start'] == null ? undefined : json['start'],
    };
}

export function UsageSummaryToJSON(json: any): UsageSummary {
    return UsageSummaryToJSONTyped(json, false);
}

export function UsageSummaryToJSONTyped(value?: UsageSummary | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'accounts': AccountsToJSON(value['accounts']),
        'end': value['end'],
        'interval': value['interval'],
        'llm': LLMToJSON(value['llm']),
        'range': value['range'],
        'scope': UsageScopeToJSON(value['scope']),
        'sources': SourcesToJSON(value['sources']),
        'spend': SpendToJSON(value['spend']),
        'start': value['start'],
    };
}

