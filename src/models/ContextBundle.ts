/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Span } from './Span.js';
import {
    SpanFromJSON,
    SpanFromJSONTyped,
    SpanToJSON,
    SpanToJSONTyped,
} from './Span.js';

/**
 * 
 * @export
 * @interface ContextBundle
 */
export interface ContextBundle {
    /**
     * BudgetTokens is the ceiling the caller asked for. Packing stops under it, so
     * this is a bound and not a target.
     * @type {number}
     * @memberof ContextBundle
     */
    budgetTokens?: number;
    /**
     * Query is the ask this bundle was packed for, echoed back so a cached or
     * forwarded bundle still says what it answers.
     * @type {string}
     * @memberof ContextBundle
     */
    query?: string;
    /**
     * Repo narrows the retrieval to one repository. Absent means every indexed repo
     * was searched.
     * @type {string}
     * @memberof ContextBundle
     */
    repo?: string;
    /**
     * Spans are the packed chunks, most relevant first, each expanded with the
     * definitions it calls and its notable callers. The top match is always present
     * even if it had to be truncated to fit, so a matched query never comes back
     * with nothing.
     * @type {Array<Span>}
     * @memberof ContextBundle
     */
    spans?: Array<Span>;
    /**
     * UsedTokens is what the returned spans actually cost, by the same estimate the
     * packer used (roughly one token per four characters — an estimate, not a
     * tokenizer's count, so size a real window with headroom).
     * @type {number}
     * @memberof ContextBundle
     */
    usedTokens?: number;
}

/**
 * Check if a given object implements the ContextBundle interface.
 */
export function instanceOfContextBundle(value: object): value is ContextBundle {
    return true;
}

export function ContextBundleFromJSON(json: any): ContextBundle {
    return ContextBundleFromJSONTyped(json, false);
}

export function ContextBundleFromJSONTyped(json: any, ignoreDiscriminator: boolean): ContextBundle {
    if (json == null) {
        return json;
    }
    return {
        
        'budgetTokens': json['budgetTokens'] == null ? undefined : json['budgetTokens'],
        'query': json['query'] == null ? undefined : json['query'],
        'repo': json['repo'] == null ? undefined : json['repo'],
        'spans': json['spans'] == null ? undefined : ((json['spans'] as Array<any>).map(SpanFromJSON)),
        'usedTokens': json['usedTokens'] == null ? undefined : json['usedTokens'],
    };
}

export function ContextBundleToJSON(json: any): ContextBundle {
    return ContextBundleToJSONTyped(json, false);
}

export function ContextBundleToJSONTyped(value?: ContextBundle | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'budgetTokens': value['budgetTokens'],
        'query': value['query'],
        'repo': value['repo'],
        'spans': value['spans'] == null ? undefined : ((value['spans'] as Array<any>).map(SpanToJSON)),
        'usedTokens': value['usedTokens'],
    };
}

