/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Reach } from './Reach.js';
import {
    ReachFromJSON,
    ReachFromJSONTyped,
    ReachToJSON,
    ReachToJSONTyped,
} from './Reach.js';
import type { Market } from './Market.js';
import {
    MarketFromJSON,
    MarketFromJSONTyped,
    MarketToJSON,
    MarketToJSONTyped,
} from './Market.js';

/**
 * 
 * @export
 * @interface Roster
 */
export interface Roster {
    /**
     * Chains is `[]` where the registry answered and named none, and `null` where
     * it did not answer — never absent, because a missing key and an empty list
     * read alike and only one of them means "there are none". Each row carries its
     * OWN reach for its figures, so a chain whose indexer is down is one row
     * saying so.
     * @type {Array<Market>}
     * @memberof Roster
     */
    chains?: Array<Market>;
    /**
     * Reach is how far the read of the REGISTRY got. It governs the list: a
     * registry that did not answer yields no rows, and the reason it did not is
     * here rather than in an empty array a caller would read as "no chains exist".
     * @type {Reach}
     * @memberof Roster
     */
    reach?: Reach;
}

/**
 * Check if a given object implements the Roster interface.
 */
export function instanceOfRoster(value: object): value is Roster {
    return true;
}

export function RosterFromJSON(json: any): Roster {
    return RosterFromJSONTyped(json, false);
}

export function RosterFromJSONTyped(json: any, ignoreDiscriminator: boolean): Roster {
    if (json == null) {
        return json;
    }
    return {
        
        'chains': json['chains'] == null ? undefined : ((json['chains'] as Array<any>).map(MarketFromJSON)),
        'reach': json['reach'] == null ? undefined : ReachFromJSON(json['reach']),
    };
}

export function RosterToJSON(json: any): Roster {
    return RosterToJSONTyped(json, false);
}

export function RosterToJSONTyped(value?: Roster | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'chains': value['chains'] == null ? undefined : ((value['chains'] as Array<any>).map(MarketToJSON)),
        'reach': ReachToJSON(value['reach']),
    };
}

