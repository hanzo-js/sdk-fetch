/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface BoardItem
 */
export interface BoardItem {
    /**
     * DocType is which content type the row came from: Campaign, SocialPost or
     * Asset. The board spans all three at once, so this is what tells them apart.
     * @type {string}
     * @memberof BoardItem
     */
    doctype?: string;
    /**
     * Name is the document within that type. (doctype, name) is the pair every
     * /v1/content write addresses an item by.
     * @type {string}
     * @memberof BoardItem
     */
    name?: string;
    /**
     * Project is the brand/site sub-scope within the org. Absent for an item held
     * at org level rather than under one brand.
     * @type {string}
     * @memberof BoardItem
     */
    project?: string;
    /**
     * Status is the lifecycle state: draft, in_review, approved, queued, published
     * or archived. It decides what a reader may see — the public site pulls
     * exactly "published" and nothing else — so it is a visibility fact, not a
     * workflow label.
     * @type {string}
     * @memberof BoardItem
     */
    status?: string;
    /**
     * Title is the item's headline, read from its type's own title field. Empty
     * for a document that has none.
     * @type {string}
     * @memberof BoardItem
     */
    title?: string;
    /**
     * UpdatedAt is unix seconds of the document's last write, and the key the
     * board sorts on, newest first.
     * @type {number}
     * @memberof BoardItem
     */
    updatedAt?: number;
}

/**
 * Check if a given object implements the BoardItem interface.
 */
export function instanceOfBoardItem(value: object): value is BoardItem {
    return true;
}

export function BoardItemFromJSON(json: any): BoardItem {
    return BoardItemFromJSONTyped(json, false);
}

export function BoardItemFromJSONTyped(json: any, ignoreDiscriminator: boolean): BoardItem {
    if (json == null) {
        return json;
    }
    return {
        
        'doctype': json['doctype'] == null ? undefined : json['doctype'],
        'name': json['name'] == null ? undefined : json['name'],
        'project': json['project'] == null ? undefined : json['project'],
        'status': json['status'] == null ? undefined : json['status'],
        'title': json['title'] == null ? undefined : json['title'],
        'updatedAt': json['updatedAt'] == null ? undefined : json['updatedAt'],
    };
}

export function BoardItemToJSON(json: any): BoardItem {
    return BoardItemToJSONTyped(json, false);
}

export function BoardItemToJSONTyped(value?: BoardItem | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'doctype': value['doctype'],
        'name': value['name'],
        'project': value['project'],
        'status': value['status'],
        'title': value['title'],
        'updatedAt': value['updatedAt'],
    };
}

