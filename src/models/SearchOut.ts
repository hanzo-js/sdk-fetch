/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Hit } from './Hit.js';
import {
    HitFromJSON,
    HitFromJSONTyped,
    HitToJSON,
    HitToJSONTyped,
} from './Hit.js';

/**
 * 
 * @export
 * @interface SearchOut
 */
export interface SearchOut {
    /**
     * Degraded is true when the index was unreachable and this answer is honestly
     * empty rather than wrong — a RAG caller continues with no context instead of
     * failing the turn. Absent on a normal answer.
     * @type {boolean}
     * @memberof SearchOut
     */
    degraded?: boolean;
    /**
     * Hits are the matching passages, most relevant first.
     * @type {Array<Hit>}
     * @memberof SearchOut
     */
    hits?: Array<Hit>;
}

/**
 * Check if a given object implements the SearchOut interface.
 */
export function instanceOfSearchOut(value: object): value is SearchOut {
    return true;
}

export function SearchOutFromJSON(json: any): SearchOut {
    return SearchOutFromJSONTyped(json, false);
}

export function SearchOutFromJSONTyped(json: any, ignoreDiscriminator: boolean): SearchOut {
    if (json == null) {
        return json;
    }
    return {
        
        'degraded': json['degraded'] == null ? undefined : json['degraded'],
        'hits': json['hits'] == null ? undefined : ((json['hits'] as Array<any>).map(HitFromJSON)),
    };
}

export function SearchOutToJSON(json: any): SearchOut {
    return SearchOutToJSONTyped(json, false);
}

export function SearchOutToJSONTyped(value?: SearchOut | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'degraded': value['degraded'],
        'hits': value['hits'] == null ? undefined : ((value['hits'] as Array<any>).map(HitToJSON)),
    };
}

