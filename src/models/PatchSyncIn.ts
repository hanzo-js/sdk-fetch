/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { EndpointReq } from './EndpointReq.js';
import {
    EndpointReqFromJSON,
    EndpointReqFromJSONTyped,
    EndpointReqToJSON,
    EndpointReqToJSONTyped,
} from './EndpointReq.js';

/**
 * 
 * @export
 * @interface PatchSyncIn
 */
export interface PatchSyncIn {
    /**
     * Actor is the loop-guard identity the sync writes as. Omitted, the stored actor
     * stands.
     * @type {string}
     * @memberof PatchSyncIn
     */
    actor?: string;
    /**
     * Direction is both, pull, push or off. Omitted, the stored direction stands.
     * @type {string}
     * @memberof PatchSyncIn
     */
    direction?: string;
    /**
     * ID is the sync to update, from the path.
     * @type {string}
     * @memberof PatchSyncIn
     */
    id?: string;
    /**
     * Kind names a different kind of sync, and is refused, for the same reason.
     * @type {string}
     * @memberof PatchSyncIn
     */
    kind?: string;
    /**
     * Source, Target and Kind are DECLARED HERE IN ORDER TO BE REFUSED.
     * 
     * They are immutable by design — re-pointing a sync is a delete and a create, so
     * a link can never silently start syncing somewhere else — but an UNDECLARED
     * field is dropped by the binder before the handler sees it, so a request asking
     * to repoint answered 200, changed nothing, and said nothing. The operator then
     * believes a moved repository has been repointed and it has not.
     * 
     * Live: a sync still naming github.com/hanzoai/cloud after the repository moved
     * to hanzo-inc/cloud failed every reconcile with "Repository not found", and the
     * PATCH that appeared to fix it did nothing at all. Declaring the fields is what
     * lets the documented immutability actually answer.
     * Source names a new upstream, and is refused. Delete this sync and create the
     * one you want.
     * @type {EndpointReq}
     * @memberof PatchSyncIn
     */
    source?: EndpointReq;
    /**
     * Target names a new native repository, and is refused, for the same reason.
     * @type {EndpointReq}
     * @memberof PatchSyncIn
     */
    target?: EndpointReq;
    /**
     * Trigger is webhook, poll or manual. Omitted, the stored trigger stands.
     * @type {string}
     * @memberof PatchSyncIn
     */
    trigger?: string;
}

/**
 * Check if a given object implements the PatchSyncIn interface.
 */
export function instanceOfPatchSyncIn(value: object): value is PatchSyncIn {
    return true;
}

export function PatchSyncInFromJSON(json: any): PatchSyncIn {
    return PatchSyncInFromJSONTyped(json, false);
}

export function PatchSyncInFromJSONTyped(json: any, ignoreDiscriminator: boolean): PatchSyncIn {
    if (json == null) {
        return json;
    }
    return {
        
        'actor': json['actor'] == null ? undefined : json['actor'],
        'direction': json['direction'] == null ? undefined : json['direction'],
        'id': json['id'] == null ? undefined : json['id'],
        'kind': json['kind'] == null ? undefined : json['kind'],
        'source': json['source'] == null ? undefined : EndpointReqFromJSON(json['source']),
        'target': json['target'] == null ? undefined : EndpointReqFromJSON(json['target']),
        'trigger': json['trigger'] == null ? undefined : json['trigger'],
    };
}

export function PatchSyncInToJSON(json: any): PatchSyncIn {
    return PatchSyncInToJSONTyped(json, false);
}

export function PatchSyncInToJSONTyped(value?: PatchSyncIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'actor': value['actor'],
        'direction': value['direction'],
        'id': value['id'],
        'kind': value['kind'],
        'source': EndpointReqToJSON(value['source']),
        'target': EndpointReqToJSON(value['target']),
        'trigger': value['trigger'],
    };
}

