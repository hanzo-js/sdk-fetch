/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface CreateWalletIn
 */
export interface CreateWalletIn {
    /**
     * AccountID is the account this wallet belongs to. Required, and it must be
     * an account of the caller's own org — an unknown one is a 404.
     * @type {string}
     * @memberof CreateWalletIn
     */
    accountId?: string;
    /**
     * Agent optionally narrows the wallet to one agent within the org. It becomes
     * a segment of the key ref, so it must be a url-safe segment with no slash.
     * @type {string}
     * @memberof CreateWalletIn
     */
    agent?: string;
    /**
     * Chain is the EVM chain this wallet is for, as "eip155:<n>" or a bare
     * decimal chain id. Optional; a Safe defaults to the Hanzo L1 (36963).
     * @type {string}
     * @memberof CreateWalletIn
     */
    chain?: string;
    /**
     * Custody selects the signing backend: "kms" (in-process, always available),
     * "mpc" or "treasury" (the deployed MPC ring), or "safe" (a Safe smart wallet
     * owned by an MPC key). Empty uses the deployment's default. A backend that is
     * not configured fails CLOSED with 503 rather than fabricating a signature.
     * @type {string}
     * @memberof CreateWalletIn
     */
    custody?: string;
    /**
     * Name is the wallet's display label. Optional.
     * @type {string}
     * @memberof CreateWalletIn
     */
    name?: string;
    /**
     * Tier is the MPC wallet tier: hot, warm, cold, gas, bridge, contract_admin,
     * validator, quarantine or disaster_recovery. Empty defaults to hot.
     * @type {string}
     * @memberof CreateWalletIn
     */
    tier?: string;
}

/**
 * Check if a given object implements the CreateWalletIn interface.
 */
export function instanceOfCreateWalletIn(value: object): value is CreateWalletIn {
    return true;
}

export function CreateWalletInFromJSON(json: any): CreateWalletIn {
    return CreateWalletInFromJSONTyped(json, false);
}

export function CreateWalletInFromJSONTyped(json: any, ignoreDiscriminator: boolean): CreateWalletIn {
    if (json == null) {
        return json;
    }
    return {
        
        'accountId': json['accountId'] == null ? undefined : json['accountId'],
        'agent': json['agent'] == null ? undefined : json['agent'],
        'chain': json['chain'] == null ? undefined : json['chain'],
        'custody': json['custody'] == null ? undefined : json['custody'],
        'name': json['name'] == null ? undefined : json['name'],
        'tier': json['tier'] == null ? undefined : json['tier'],
    };
}

export function CreateWalletInToJSON(json: any): CreateWalletIn {
    return CreateWalletInToJSONTyped(json, false);
}

export function CreateWalletInToJSONTyped(value?: CreateWalletIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'accountId': value['accountId'],
        'agent': value['agent'],
        'chain': value['chain'],
        'custody': value['custody'],
        'name': value['name'],
        'tier': value['tier'],
    };
}

