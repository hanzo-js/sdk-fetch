/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { RiskModelValue } from './RiskModelValue.js';
import {
    RiskModelValueFromJSON,
    RiskModelValueFromJSONTyped,
    RiskModelValueToJSON,
    RiskModelValueToJSONTyped,
} from './RiskModelValue.js';
import type { RiskTrial } from './RiskTrial.js';
import {
    RiskTrialFromJSON,
    RiskTrialFromJSONTyped,
    RiskTrialToJSON,
    RiskTrialToJSONTyped,
} from './RiskTrial.js';

/**
 * 
 * @export
 * @interface RiskSearchReport
 */
export interface RiskSearchReport {
    /**
     * Done is false while the run is still going; the trials below are then the
     * ones finished so far.
     * @type {boolean}
     * @memberof RiskSearchReport
     */
    done?: boolean;
    /**
     * Ended is when it finished, RFC 3339. Absent while it is still going.
     * @type {string}
     * @memberof RiskSearchReport
     */
    ended?: string;
    /**
     * Events is how much of this organisation's history was replayed.
     * @type {number}
     * @memberof RiskSearchReport
     */
    events?: number;
    /**
     * Fitted is the winning shape FITTED over your own history and published as one of
     * your organisation's own model values. Name its address on PUT
     * /v1/risk/state/model and the winning shape becomes the model you are running.
     * 
     * It is why this op answers something you can act on. A trial keeps counts and not
     * the model that produced them, so a report without this named a shape nobody could
     * install — and the adoption path refused a shape change besides. Fitting the winner
     * once is a sixty-fifth pass over the same history; keeping all sixty-four fitted
     * models resident instead would cost a measured 21 MiB per run for sixty-three
     * shapes nobody adopts.
     * 
     * Two things about it are worth knowing before you adopt it. Its realised rate can
     * differ from the winner's above, because the ranking measures every candidate under
     * one fixed reference geometry so the comparison is a comparison, while this is
     * fitted under YOUR geometry — the one an outsider cannot predict. And it has
     * learned the window this search replayed and nothing older, so adopting it trades
     * history for fit.
     * @type {RiskModelValue}
     * @memberof RiskSearchReport
     */
    fitted?: RiskModelValue;
    /**
     * Gap says why the winning shape could not be fitted into an adoptable value, when
     * it could not. It is separate from Refusal because they are different facts: a
     * refusal means the ranking below proves nothing, a gap means the ranking stands and
     * only the value is missing.
     * @type {string}
     * @memberof RiskSearchReport
     */
    gap?: string;
    /**
     * ID is the run.
     * @type {string}
     * @memberof RiskSearchReport
     */
    id?: string;
    /**
     * Refusal says why the run proves nothing, when it does. An empty history is
     * REFUSED rather than reported as zero alerts: "no alerts" is exactly what a
     * quiet model looks like, and choosing a shape on the strength of an empty
     * replay is the failure a sandbox exists to prevent.
     * @type {string}
     * @memberof RiskSearchReport
     */
    refusal?: string;
    /**
     * Started is when the run was accepted, RFC 3339.
     * @type {string}
     * @memberof RiskSearchReport
     */
    started?: string;
    /**
     * Trials is every shape tried, best first.
     * @type {Array<RiskTrial>}
     * @memberof RiskSearchReport
     */
    trials?: Array<RiskTrial>;
    /**
     * Winner is the best-fitting shape, absent when nothing fit.
     * @type {RiskTrial}
     * @memberof RiskSearchReport
     */
    winner?: RiskTrial;
}

/**
 * Check if a given object implements the RiskSearchReport interface.
 */
export function instanceOfRiskSearchReport(value: object): value is RiskSearchReport {
    return true;
}

export function RiskSearchReportFromJSON(json: any): RiskSearchReport {
    return RiskSearchReportFromJSONTyped(json, false);
}

export function RiskSearchReportFromJSONTyped(json: any, ignoreDiscriminator: boolean): RiskSearchReport {
    if (json == null) {
        return json;
    }
    return {
        
        'done': json['done'] == null ? undefined : json['done'],
        'ended': json['ended'] == null ? undefined : json['ended'],
        'events': json['events'] == null ? undefined : json['events'],
        'fitted': json['fitted'] == null ? undefined : RiskModelValueFromJSON(json['fitted']),
        'gap': json['gap'] == null ? undefined : json['gap'],
        'id': json['id'] == null ? undefined : json['id'],
        'refusal': json['refusal'] == null ? undefined : json['refusal'],
        'started': json['started'] == null ? undefined : json['started'],
        'trials': json['trials'] == null ? undefined : ((json['trials'] as Array<any>).map(RiskTrialFromJSON)),
        'winner': json['winner'] == null ? undefined : RiskTrialFromJSON(json['winner']),
    };
}

export function RiskSearchReportToJSON(json: any): RiskSearchReport {
    return RiskSearchReportToJSONTyped(json, false);
}

export function RiskSearchReportToJSONTyped(value?: RiskSearchReport | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'done': value['done'],
        'ended': value['ended'],
        'events': value['events'],
        'fitted': RiskModelValueToJSON(value['fitted']),
        'gap': value['gap'],
        'id': value['id'],
        'refusal': value['refusal'],
        'started': value['started'],
        'trials': value['trials'] == null ? undefined : ((value['trials'] as Array<any>).map(RiskTrialToJSON)),
        'winner': RiskTrialToJSON(value['winner']),
    };
}

