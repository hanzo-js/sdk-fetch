/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ArgoHealth } from './ArgoHealth.js';
import {
    ArgoHealthFromJSON,
    ArgoHealthFromJSONTyped,
    ArgoHealthToJSON,
    ArgoHealthToJSONTyped,
} from './ArgoHealth.js';
import type { ArgoInfoItem } from './ArgoInfoItem.js';
import {
    ArgoInfoItemFromJSON,
    ArgoInfoItemFromJSONTyped,
    ArgoInfoItemToJSON,
    ArgoInfoItemToJSONTyped,
} from './ArgoInfoItem.js';
import type { ArgoResourceRef } from './ArgoResourceRef.js';
import {
    ArgoResourceRefFromJSON,
    ArgoResourceRefFromJSONTyped,
    ArgoResourceRefToJSON,
    ArgoResourceRefToJSONTyped,
} from './ArgoResourceRef.js';

/**
 * 
 * @export
 * @interface ArgoNode
 */
export interface ArgoNode {
    /**
     * CreatedAt is the object's creationTimestamp, RFC 3339 UTC to the second.
     * Absent when the object carries none.
     * @type {string}
     * @memberof ArgoNode
     */
    createdAt?: string;
    /**
     * 
     * @type {string}
     * @memberof ArgoNode
     */
    group?: string;
    /**
     * Health is the node's own derived health. Always present on a node of this
     * tree; a kind with no health signal of its own reports Healthy, since a
     * ConfigMap existing IS its healthy state.
     * @type {ArgoHealth}
     * @memberof ArgoNode
     */
    health?: ArgoHealth;
    /**
     * Images are the container images running on this node. Always absent — the tag
     * travels as the "Image Tag" chip in Info instead, which is where the SPA reads
     * it on a node.
     * @type {Array<string>}
     * @memberof ArgoNode
     */
    images?: Array<string>;
    /**
     * Info are the chips shown on the node. At most one: the image tag — the
     * RUNNING tag on a Deployment, ReplicaSet or Pod, and the DECLARED tag on the
     * App CR at the root. Absent on a node that carries no image at all.
     * @type {Array<ArgoInfoItem>}
     * @memberof ArgoNode
     */
    info?: Array<ArgoInfoItem>;
    /**
     * 
     * @type {string}
     * @memberof ArgoNode
     */
    kind?: string;
    /**
     * 
     * @type {string}
     * @memberof ArgoNode
     */
    name?: string;
    /**
     * 
     * @type {string}
     * @memberof ArgoNode
     */
    namespace?: string;
    /**
     * ParentRefs are the node's edges UPWARD, which is how the SPA draws the DAG
     * from this flat list. Exactly one entry where present: a depth-1 object points
     * at the App CR, a ReplicaSet at its Deployment, a Pod at its ReplicaSet (or at
     * the Deployment whose selector matches it, when the ReplicaSet is gone).
     * Absent on the root.
     * @type {Array<ArgoResourceRef>}
     * @memberof ArgoNode
     */
    parentRefs?: Array<ArgoResourceRef>;
    /**
     * ResourceVersion is the k8s version a watch would resume from. Always empty:
     * the tree is rebuilt from live reads on every request, including on every
     * frame of the SSE stream, so there is no revision to resume from.
     * @type {string}
     * @memberof ArgoNode
     */
    resourceVersion?: string;
    /**
     * 
     * @type {string}
     * @memberof ArgoNode
     */
    uid?: string;
    /**
     * 
     * @type {string}
     * @memberof ArgoNode
     */
    version?: string;
}

/**
 * Check if a given object implements the ArgoNode interface.
 */
export function instanceOfArgoNode(value: object): value is ArgoNode {
    return true;
}

export function ArgoNodeFromJSON(json: any): ArgoNode {
    return ArgoNodeFromJSONTyped(json, false);
}

export function ArgoNodeFromJSONTyped(json: any, ignoreDiscriminator: boolean): ArgoNode {
    if (json == null) {
        return json;
    }
    return {
        
        'createdAt': json['createdAt'] == null ? undefined : json['createdAt'],
        'group': json['group'] == null ? undefined : json['group'],
        'health': json['health'] == null ? undefined : ArgoHealthFromJSON(json['health']),
        'images': json['images'] == null ? undefined : json['images'],
        'info': json['info'] == null ? undefined : ((json['info'] as Array<any>).map(ArgoInfoItemFromJSON)),
        'kind': json['kind'] == null ? undefined : json['kind'],
        'name': json['name'] == null ? undefined : json['name'],
        'namespace': json['namespace'] == null ? undefined : json['namespace'],
        'parentRefs': json['parentRefs'] == null ? undefined : ((json['parentRefs'] as Array<any>).map(ArgoResourceRefFromJSON)),
        'resourceVersion': json['resourceVersion'] == null ? undefined : json['resourceVersion'],
        'uid': json['uid'] == null ? undefined : json['uid'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function ArgoNodeToJSON(json: any): ArgoNode {
    return ArgoNodeToJSONTyped(json, false);
}

export function ArgoNodeToJSONTyped(value?: ArgoNode | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'createdAt': value['createdAt'],
        'group': value['group'],
        'health': ArgoHealthToJSON(value['health']),
        'images': value['images'],
        'info': value['info'] == null ? undefined : ((value['info'] as Array<any>).map(ArgoInfoItemToJSON)),
        'kind': value['kind'],
        'name': value['name'],
        'namespace': value['namespace'],
        'parentRefs': value['parentRefs'] == null ? undefined : ((value['parentRefs'] as Array<any>).map(ArgoResourceRefToJSON)),
        'resourceVersion': value['resourceVersion'],
        'uid': value['uid'],
        'version': value['version'],
    };
}

