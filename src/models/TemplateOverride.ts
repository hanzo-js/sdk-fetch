/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Field } from './Field.js';
import {
    FieldFromJSON,
    FieldFromJSONTyped,
    FieldToJSON,
    FieldToJSONTyped,
} from './Field.js';

/**
 * 
 * @export
 * @interface TemplateOverride
 */
export interface TemplateOverride {
    /**
     * Body is the text/template source. Required. Every {{.key}} it references must
     * be declared in Fields, or the save is refused rather than rendering a blank
     * into a contract later.
     * @type {string}
     * @memberof TemplateOverride
     */
    body?: string;
    /**
     * Category groups the template: formation, equity, ops or sales. Optional when
     * overriding a built-in, which supplies its own.
     * @type {string}
     * @memberof TemplateOverride
     */
    category?: string;
    /**
     * CounselReview marks a template whose documents must carry the counsel notice.
     * It can be raised but never lowered: a formation or equity template is always
     * counsel-review, and an override of a counsel-review built-in stays one.
     * @type {boolean}
     * @memberof TemplateOverride
     */
    counselReview?: boolean;
    /**
     * Fields declares the merge fields the body consumes. Every declared field is
     * REQUIRED at generation — the engine fails closed on a missing one.
     * @type {Array<Field>}
     * @memberof TemplateOverride
     */
    fields?: Array<Field>;
    /**
     * ID is the template to override, from the path. Overriding a built-in id
     * inherits that built-in's category, title and counsel-review posture.
     * @type {string}
     * @memberof TemplateOverride
     */
    id?: string;
    /**
     * Title is the template's display name. Required unless a built-in supplies it.
     * @type {string}
     * @memberof TemplateOverride
     */
    title?: string;
}

/**
 * Check if a given object implements the TemplateOverride interface.
 */
export function instanceOfTemplateOverride(value: object): value is TemplateOverride {
    return true;
}

export function TemplateOverrideFromJSON(json: any): TemplateOverride {
    return TemplateOverrideFromJSONTyped(json, false);
}

export function TemplateOverrideFromJSONTyped(json: any, ignoreDiscriminator: boolean): TemplateOverride {
    if (json == null) {
        return json;
    }
    return {
        
        'body': json['body'] == null ? undefined : json['body'],
        'category': json['category'] == null ? undefined : json['category'],
        'counselReview': json['counselReview'] == null ? undefined : json['counselReview'],
        'fields': json['fields'] == null ? undefined : ((json['fields'] as Array<any>).map(FieldFromJSON)),
        'id': json['id'] == null ? undefined : json['id'],
        'title': json['title'] == null ? undefined : json['title'],
    };
}

export function TemplateOverrideToJSON(json: any): TemplateOverride {
    return TemplateOverrideToJSONTyped(json, false);
}

export function TemplateOverrideToJSONTyped(value?: TemplateOverride | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'body': value['body'],
        'category': value['category'],
        'counselReview': value['counselReview'],
        'fields': value['fields'] == null ? undefined : ((value['fields'] as Array<any>).map(FieldToJSON)),
        'id': value['id'],
        'title': value['title'],
    };
}

