/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Arm } from './Arm.js';
import {
    ArmFromJSON,
    ArmFromJSONTyped,
    ArmToJSON,
    ArmToJSONTyped,
} from './Arm.js';

/**
 * 
 * @export
 * @interface CreateBody
 */
export interface CreateBody {
    /**
     * ExposureEvent is the event that marks a subject as enrolled — the analysis
     * denominator — defaulting to the SDK's $feature_flag_called marker.
     * @type {string}
     * @memberof CreateBody
     */
    exposureEvent?: string;
    /**
     * FlagKey names the assignment flag this experiment writes, defaulting to
     * exp_<id>. It must be a slug.
     * @type {string}
     * @memberof CreateBody
     */
    flagKey?: string;
    /**
     * ID is the experiment's slug, claimed once per project. It must match
     * [A-Za-z0-9][A-Za-z0-9._-]{0,127}.
     * @type {string}
     * @memberof CreateBody
     */
    id: string;
    /**
     * MetricEvent is the event that counts as a conversion — the analysis
     * numerator.
     * @type {string}
     * @memberof CreateBody
     */
    metricEvent: string;
    /**
     * Name is free text for a reader; the id is what addresses the experiment.
     * @type {string}
     * @memberof CreateBody
     */
    name?: string;
    /**
     * SubjectKind is the unit assigned and measured: user (the default), org,
     * session or audience.
     * @type {string}
     * @memberof CreateBody
     */
    subjectKind?: string;
    /**
     * Arms are the arms, at least two. Weights that are all zero become an
     * even split; otherwise they must sum to 100, and at most one arm may be
     * flagged control.
     * @type {Array<Arm>}
     * @memberof CreateBody
     */
    variants: Array<Arm>;
}

/**
 * Check if a given object implements the CreateBody interface.
 */
export function instanceOfCreateBody(value: object): value is CreateBody {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('metricEvent' in value) || value['metricEvent'] === undefined) return false;
    if (!('variants' in value) || value['variants'] === undefined) return false;
    return true;
}

export function CreateBodyFromJSON(json: any): CreateBody {
    return CreateBodyFromJSONTyped(json, false);
}

export function CreateBodyFromJSONTyped(json: any, ignoreDiscriminator: boolean): CreateBody {
    if (json == null) {
        return json;
    }
    return {
        
        'exposureEvent': json['exposureEvent'] == null ? undefined : json['exposureEvent'],
        'flagKey': json['flagKey'] == null ? undefined : json['flagKey'],
        'id': json['id'],
        'metricEvent': json['metricEvent'],
        'name': json['name'] == null ? undefined : json['name'],
        'subjectKind': json['subjectKind'] == null ? undefined : json['subjectKind'],
        'variants': ((json['variants'] as Array<any>).map(ArmFromJSON)),
    };
}

export function CreateBodyToJSON(json: any): CreateBody {
    return CreateBodyToJSONTyped(json, false);
}

export function CreateBodyToJSONTyped(value?: CreateBody | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'exposureEvent': value['exposureEvent'],
        'flagKey': value['flagKey'],
        'id': value['id'],
        'metricEvent': value['metricEvent'],
        'name': value['name'],
        'subjectKind': value['subjectKind'],
        'variants': ((value['variants'] as Array<any>).map(ArmToJSON)),
    };
}

