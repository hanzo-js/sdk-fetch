/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { UsageWindowView } from './UsageWindowView.js';
import {
    UsageWindowViewFromJSON,
    UsageWindowViewFromJSONTyped,
    UsageWindowViewToJSON,
    UsageWindowViewToJSONTyped,
} from './UsageWindowView.js';

/**
 * 
 * @export
 * @interface DashResp
 */
export interface DashResp {
    /**
     * Account is the linked account that was asked about, when one was named.
     * @type {string}
     * @memberof DashResp
     */
    account?: string;
    /**
     * Available is false when the warehouse could not be read. That means "no
     * answer", NOT "no usage" — the two lists below are then empty for a reason.
     * @type {boolean}
     * @memberof DashResp
     */
    available?: boolean;
    /**
     * Current is the newest window instance of each lane — the dash headline.
     * @type {Array<UsageWindowView>}
     * @memberof DashResp
     */
    current?: Array<UsageWindowView>;
    /**
     * From is the inclusive start of that window, RFC3339 UTC.
     * @type {string}
     * @memberof DashResp
     */
    from?: string;
    /**
     * Provider is the upstream that was asked about, echoed back.
     * @type {string}
     * @memberof DashResp
     */
    provider?: string;
    /**
     * Range is the window that was served: 1h, 24h, 7d or 30d.
     * @type {string}
     * @memberof DashResp
     */
    range?: string;
    /**
     * Scope says whose rows these are: the caller's own linked accounts.
     * @type {string}
     * @memberof DashResp
     */
    scope?: string;
    /**
     * Source names the meter of record — the provider's own login, not Hanzo.
     * @type {string}
     * @memberof DashResp
     */
    source?: string;
    /**
     * To is the exclusive end of that window, RFC3339 UTC.
     * @type {string}
     * @memberof DashResp
     */
    to?: string;
    /**
     * Windows is every instance in range, newest first — the history behind it.
     * @type {Array<UsageWindowView>}
     * @memberof DashResp
     */
    windows?: Array<UsageWindowView>;
}

/**
 * Check if a given object implements the DashResp interface.
 */
export function instanceOfDashResp(value: object): value is DashResp {
    return true;
}

export function DashRespFromJSON(json: any): DashResp {
    return DashRespFromJSONTyped(json, false);
}

export function DashRespFromJSONTyped(json: any, ignoreDiscriminator: boolean): DashResp {
    if (json == null) {
        return json;
    }
    return {
        
        'account': json['account'] == null ? undefined : json['account'],
        'available': json['available'] == null ? undefined : json['available'],
        'current': json['current'] == null ? undefined : ((json['current'] as Array<any>).map(UsageWindowViewFromJSON)),
        'from': json['from'] == null ? undefined : json['from'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'range': json['range'] == null ? undefined : json['range'],
        'scope': json['scope'] == null ? undefined : json['scope'],
        'source': json['source'] == null ? undefined : json['source'],
        'to': json['to'] == null ? undefined : json['to'],
        'windows': json['windows'] == null ? undefined : ((json['windows'] as Array<any>).map(UsageWindowViewFromJSON)),
    };
}

export function DashRespToJSON(json: any): DashResp {
    return DashRespToJSONTyped(json, false);
}

export function DashRespToJSONTyped(value?: DashResp | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'account': value['account'],
        'available': value['available'],
        'current': value['current'] == null ? undefined : ((value['current'] as Array<any>).map(UsageWindowViewToJSON)),
        'from': value['from'],
        'provider': value['provider'],
        'range': value['range'],
        'scope': value['scope'],
        'source': value['source'],
        'to': value['to'],
        'windows': value['windows'] == null ? undefined : ((value['windows'] as Array<any>).map(UsageWindowViewToJSON)),
    };
}

