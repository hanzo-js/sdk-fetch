/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { BalanceLine } from './BalanceLine.js';
import {
    BalanceLineFromJSON,
    BalanceLineFromJSONTyped,
    BalanceLineToJSON,
    BalanceLineToJSONTyped,
} from './BalanceLine.js';

/**
 * 
 * @export
 * @interface BalanceSheet
 */
export interface BalanceSheet {
    /**
     * AsOf is the posting time the statement is taken at, inclusive. A balance sheet
     * is a snapshot, not a window, so there is no From. Absent means as of now.
     * @type {string}
     * @memberof BalanceSheet
     */
    asOf?: string;
    /**
     * Assets are what the org OWNS at that instant, one line per account that has a
     * balance. Cash, receivables, funds captured but not yet settled.
     * @type {Array<BalanceLine>}
     * @memberof BalanceSheet
     */
    assets?: Array<BalanceLine>;
    /**
     * Balanced is whether assets equal liabilities plus equity — the accounting
     * equation, computed from the totals above rather than assumed. False means the
     * ledger is broken, not that the statement is.
     * @type {boolean}
     * @memberof BalanceSheet
     */
    balanced?: boolean;
    /**
     * Equity is what is left over for the owners. It carries a DERIVED retained
     * earnings line holding cumulative income minus expense, because this ledger has
     * no period close that sweeps the P&L into equity — without that line the
     * equation would not close.
     * @type {Array<BalanceLine>}
     * @memberof BalanceSheet
     */
    equity?: Array<BalanceLine>;
    /**
     * Liabilities are what the org OWES — including customers' unspent prepaid
     * credit, which is their money until it is consumed and so is carried here
     * rather than counted as revenue.
     * @type {Array<BalanceLine>}
     * @memberof BalanceSheet
     */
    liabilities?: Array<BalanceLine>;
    /**
     * TotalAssets is the sum of the asset lines, in cents.
     * @type {number}
     * @memberof BalanceSheet
     */
    totalAssets?: number;
    /**
     * TotalEquity is the sum of the equity lines including retained earnings, in cents.
     * @type {number}
     * @memberof BalanceSheet
     */
    totalEquity?: number;
    /**
     * TotalLiabilities is the sum of the liability lines, in cents.
     * @type {number}
     * @memberof BalanceSheet
     */
    totalLiabilities?: number;
}

/**
 * Check if a given object implements the BalanceSheet interface.
 */
export function instanceOfBalanceSheet(value: object): value is BalanceSheet {
    return true;
}

export function BalanceSheetFromJSON(json: any): BalanceSheet {
    return BalanceSheetFromJSONTyped(json, false);
}

export function BalanceSheetFromJSONTyped(json: any, ignoreDiscriminator: boolean): BalanceSheet {
    if (json == null) {
        return json;
    }
    return {
        
        'asOf': json['asOf'] == null ? undefined : json['asOf'],
        'assets': json['assets'] == null ? undefined : ((json['assets'] as Array<any>).map(BalanceLineFromJSON)),
        'balanced': json['balanced'] == null ? undefined : json['balanced'],
        'equity': json['equity'] == null ? undefined : ((json['equity'] as Array<any>).map(BalanceLineFromJSON)),
        'liabilities': json['liabilities'] == null ? undefined : ((json['liabilities'] as Array<any>).map(BalanceLineFromJSON)),
        'totalAssets': json['totalAssets'] == null ? undefined : json['totalAssets'],
        'totalEquity': json['totalEquity'] == null ? undefined : json['totalEquity'],
        'totalLiabilities': json['totalLiabilities'] == null ? undefined : json['totalLiabilities'],
    };
}

export function BalanceSheetToJSON(json: any): BalanceSheet {
    return BalanceSheetToJSONTyped(json, false);
}

export function BalanceSheetToJSONTyped(value?: BalanceSheet | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'asOf': value['asOf'],
        'assets': value['assets'] == null ? undefined : ((value['assets'] as Array<any>).map(BalanceLineToJSON)),
        'balanced': value['balanced'],
        'equity': value['equity'] == null ? undefined : ((value['equity'] as Array<any>).map(BalanceLineToJSON)),
        'liabilities': value['liabilities'] == null ? undefined : ((value['liabilities'] as Array<any>).map(BalanceLineToJSON)),
        'totalAssets': value['totalAssets'],
        'totalEquity': value['totalEquity'],
        'totalLiabilities': value['totalLiabilities'],
    };
}

