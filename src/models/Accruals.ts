/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface Accruals
 */
export interface Accruals {
    /**
     * Accrued is how many NEW commission accruals this run created, counted across
     * every upline level. The accrual is latched at most once per (affiliate, source
     * org, period), so a re-run inside the same month reports 0 having changed
     * nothing — 0 means "already accrued", not "failed".
     * @type {number}
     * @memberof Accruals
     */
    accrued?: number;
    /**
     * RoyaltiesAccrued is how many OSS-author royalty accruals the SAME spend read
     * produced in the sibling authors program. One read drives both.
     * @type {number}
     * @memberof Accruals
     */
    royaltiesAccrued?: number;
    /**
     * RoyaltyFailures is reported, not swallowed: a sweep that could not reach
     * the royalty store must not read as one that found nothing owed. The count
     * was already computed and then dropped on the floor, which is the same
     * silence the typed leg was added to end.
     * @type {number}
     * @memberof Accruals
     */
    royaltyFailures?: number;
    /**
     * Swept is how many source (referred) orgs the run visited, bounded at 500 per
     * run. A source with no spend this period, or one whose spend could not be read,
     * still counts as swept.
     * @type {number}
     * @memberof Accruals
     */
    swept?: number;
}

/**
 * Check if a given object implements the Accruals interface.
 */
export function instanceOfAccruals(value: object): value is Accruals {
    return true;
}

export function AccrualsFromJSON(json: any): Accruals {
    return AccrualsFromJSONTyped(json, false);
}

export function AccrualsFromJSONTyped(json: any, ignoreDiscriminator: boolean): Accruals {
    if (json == null) {
        return json;
    }
    return {
        
        'accrued': json['accrued'] == null ? undefined : json['accrued'],
        'royaltiesAccrued': json['royaltiesAccrued'] == null ? undefined : json['royaltiesAccrued'],
        'royaltyFailures': json['royaltyFailures'] == null ? undefined : json['royaltyFailures'],
        'swept': json['swept'] == null ? undefined : json['swept'],
    };
}

export function AccrualsToJSON(json: any): Accruals {
    return AccrualsToJSONTyped(json, false);
}

export function AccrualsToJSONTyped(value?: Accruals | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'accrued': value['accrued'],
        'royaltiesAccrued': value['royaltiesAccrued'],
        'royaltyFailures': value['royaltyFailures'],
        'swept': value['swept'],
    };
}

