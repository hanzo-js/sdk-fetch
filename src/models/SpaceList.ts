/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { SpaceItem } from './SpaceItem.js';
import {
    SpaceItemFromJSON,
    SpaceItemFromJSONTyped,
    SpaceItemToJSON,
    SpaceItemToJSONTyped,
} from './SpaceItem.js';

/**
 * 
 * @export
 * @interface SpaceList
 */
export interface SpaceList {
    /**
     * Spaces are the caller org's spaces, oldest first as the store returns them.
     * @type {Array<SpaceItem>}
     * @memberof SpaceList
     */
    spaces?: Array<SpaceItem>;
    /**
     * Total is how many spaces this org has. It equals len(spaces): the listing is
     * not paged, because one bucket per (org, space) keeps an org's count small by
     * construction, which is the whole reason a drive is a prefix and not a bucket.
     * @type {number}
     * @memberof SpaceList
     */
    total?: number;
}

/**
 * Check if a given object implements the SpaceList interface.
 */
export function instanceOfSpaceList(value: object): value is SpaceList {
    return true;
}

export function SpaceListFromJSON(json: any): SpaceList {
    return SpaceListFromJSONTyped(json, false);
}

export function SpaceListFromJSONTyped(json: any, ignoreDiscriminator: boolean): SpaceList {
    if (json == null) {
        return json;
    }
    return {
        
        'spaces': json['spaces'] == null ? undefined : ((json['spaces'] as Array<any>).map(SpaceItemFromJSON)),
        'total': json['total'] == null ? undefined : json['total'],
    };
}

export function SpaceListToJSON(json: any): SpaceList {
    return SpaceListToJSONTyped(json, false);
}

export function SpaceListToJSONTyped(value?: SpaceList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'spaces': value['spaces'] == null ? undefined : ((value['spaces'] as Array<any>).map(SpaceItemToJSON)),
        'total': value['total'],
    };
}

