/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface TrustSettings
 */
export interface TrustSettings {
    /**
     * Name is the centre's display name. Required to publish.
     * @type {string}
     * @memberof TrustSettings
     */
    name?: string;
    /**
     * Nda is the text a party must accept before asking for a document. Optional;
     * empty asks for no acceptance. The text in force is copied onto each request as
     * it is accepted, so editing it never changes what anyone already agreed to.
     * @type {string}
     * @memberof TrustSettings
     */
    nda?: string;
    /**
     * Publish makes the centre answer at its public address. False withdraws it: the
     * address stops answering while every item, grant and record stays exactly as it
     * was, so withdrawing is reversible and loses nothing.
     * @type {boolean}
     * @memberof TrustSettings
     */
    publish?: boolean;
    /**
     * Slug is the public address to answer at — a lowercase label of letters,
     * digits and hyphens. Required to publish, unique across the deployment, and one
     * org holds one: publishing under a new address MOVES the centre rather than
     * leaving the old one answering.
     * @type {string}
     * @memberof TrustSettings
     */
    slug?: string;
}

/**
 * Check if a given object implements the TrustSettings interface.
 */
export function instanceOfTrustSettings(value: object): value is TrustSettings {
    return true;
}

export function TrustSettingsFromJSON(json: any): TrustSettings {
    return TrustSettingsFromJSONTyped(json, false);
}

export function TrustSettingsFromJSONTyped(json: any, ignoreDiscriminator: boolean): TrustSettings {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'] == null ? undefined : json['name'],
        'nda': json['nda'] == null ? undefined : json['nda'],
        'publish': json['publish'] == null ? undefined : json['publish'],
        'slug': json['slug'] == null ? undefined : json['slug'],
    };
}

export function TrustSettingsToJSON(json: any): TrustSettings {
    return TrustSettingsToJSONTyped(json, false);
}

export function TrustSettingsToJSONTyped(value?: TrustSettings | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'nda': value['nda'],
        'publish': value['publish'],
        'slug': value['slug'],
    };
}

