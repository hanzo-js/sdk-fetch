/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { LinkView } from './LinkView.js';
import {
    LinkViewFromJSON,
    LinkViewFromJSONTyped,
    LinkViewToJSON,
    LinkViewToJSONTyped,
} from './LinkView.js';
import type { DeviceView } from './DeviceView.js';
import {
    DeviceViewFromJSON,
    DeviceViewFromJSONTyped,
    DeviceViewToJSON,
    DeviceViewToJSONTyped,
} from './DeviceView.js';

/**
 * 
 * @export
 * @interface LinkList
 */
export interface LinkList {
    /**
     * Devices is the same rows folded per machine — the cross-machine "AI
     * Providers / Accounts" view.
     * @type {Array<DeviceView>}
     * @memberof LinkList
     */
    devices?: Array<DeviceView>;
    /**
     * Links is every link the caller registered, newest first. Revoked links are
     * INCLUDED rather than dropped, because a logged-out account keeps its usage
     * history and audit trail.
     * @type {Array<LinkView>}
     * @memberof LinkList
     */
    links?: Array<LinkView>;
}

/**
 * Check if a given object implements the LinkList interface.
 */
export function instanceOfLinkList(value: object): value is LinkList {
    return true;
}

export function LinkListFromJSON(json: any): LinkList {
    return LinkListFromJSONTyped(json, false);
}

export function LinkListFromJSONTyped(json: any, ignoreDiscriminator: boolean): LinkList {
    if (json == null) {
        return json;
    }
    return {
        
        'devices': json['devices'] == null ? undefined : ((json['devices'] as Array<any>).map(DeviceViewFromJSON)),
        'links': json['links'] == null ? undefined : ((json['links'] as Array<any>).map(LinkViewFromJSON)),
    };
}

export function LinkListToJSON(json: any): LinkList {
    return LinkListToJSONTyped(json, false);
}

export function LinkListToJSONTyped(value?: LinkList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'devices': value['devices'] == null ? undefined : ((value['devices'] as Array<any>).map(DeviceViewToJSON)),
        'links': value['links'] == null ? undefined : ((value['links'] as Array<any>).map(LinkViewToJSON)),
    };
}

