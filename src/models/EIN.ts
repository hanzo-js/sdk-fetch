/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Form } from './Form.js';
import {
    FormFromJSON,
    FormFromJSONTyped,
    FormToJSON,
    FormToJSONTyped,
} from './Form.js';
import type { Responsible } from './Responsible.js';
import {
    ResponsibleFromJSON,
    ResponsibleFromJSONTyped,
    ResponsibleToJSON,
    ResponsibleToJSONTyped,
} from './Responsible.js';

/**
 * 
 * @export
 * @interface EIN
 */
export interface EIN {
    /**
     * Expedited reports that prioritised handling was asked for.
     * @type {boolean}
     * @memberof EIN
     */
    expedited?: boolean;
    /**
     * Forms are the forms this application owes, with what each is for.
     * @type {Array<Form>}
     * @memberof EIN
     */
    forms?: Array<Form>;
    /**
     * NAICS is the six-digit code for what the business does. The SS-4 asks it
     * and the IRS will not process an application without one.
     * @type {string}
     * @memberof EIN
     */
    naics?: string;
    /**
     * Number is the issued EIN, absent until the IRS issues it.
     * @type {string}
     * @memberof EIN
     */
    number?: string;
    /**
     * Online reports that this application can be filed with the IRS online and
     * issued in a sitting, rather than signed and posted. It is the single fact
     * that decides how long a customer waits, so it is answered rather than
     * implied by the absence of forms.
     * @type {boolean}
     * @memberof EIN
     */
    online?: boolean;
    /**
     * Responsible is the person the IRS holds answerable.
     * @type {Responsible}
     * @memberof EIN
     */
    responsible?: Responsible;
    /**
     * Status is how far it has got.
     * @type {string}
     * @memberof EIN
     */
    status?: string;
}

/**
 * Check if a given object implements the EIN interface.
 */
export function instanceOfEIN(value: object): value is EIN {
    return true;
}

export function EINFromJSON(json: any): EIN {
    return EINFromJSONTyped(json, false);
}

export function EINFromJSONTyped(json: any, ignoreDiscriminator: boolean): EIN {
    if (json == null) {
        return json;
    }
    return {
        
        'expedited': json['expedited'] == null ? undefined : json['expedited'],
        'forms': json['forms'] == null ? undefined : ((json['forms'] as Array<any>).map(FormFromJSON)),
        'naics': json['naics'] == null ? undefined : json['naics'],
        'number': json['number'] == null ? undefined : json['number'],
        'online': json['online'] == null ? undefined : json['online'],
        'responsible': json['responsible'] == null ? undefined : ResponsibleFromJSON(json['responsible']),
        'status': json['status'] == null ? undefined : json['status'],
    };
}

export function EINToJSON(json: any): EIN {
    return EINToJSONTyped(json, false);
}

export function EINToJSONTyped(value?: EIN | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'expedited': value['expedited'],
        'forms': value['forms'] == null ? undefined : ((value['forms'] as Array<any>).map(FormToJSON)),
        'naics': value['naics'],
        'number': value['number'],
        'online': value['online'],
        'responsible': ResponsibleToJSON(value['responsible']),
        'status': value['status'],
    };
}

