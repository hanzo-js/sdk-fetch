/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Wire } from './Wire.js';
import {
    WireFromJSON,
    WireFromJSONTyped,
    WireToJSON,
    WireToJSONTyped,
} from './Wire.js';

/**
 * 
 * @export
 * @interface TrailPage
 */
export interface TrailPage {
    /**
     * Data is one page of the org's events, newest first. Empty, never null.
     * @type {Array<Wire>}
     * @memberof TrailPage
     */
    data?: Array<Wire>;
    /**
     * Msg is the envelope's message slot, empty on success.
     * @type {string}
     * @memberof TrailPage
     */
    msg?: string;
    /**
     * Status is the envelope's status slot, "ok" on success.
     * @type {string}
     * @memberof TrailPage
     */
    status?: string;
    /**
     * Total is how many events match the filter, across all pages — what a pager
     * needs to size itself.
     * @type {number}
     * @memberof TrailPage
     */
    total?: number;
}

/**
 * Check if a given object implements the TrailPage interface.
 */
export function instanceOfTrailPage(value: object): value is TrailPage {
    return true;
}

export function TrailPageFromJSON(json: any): TrailPage {
    return TrailPageFromJSONTyped(json, false);
}

export function TrailPageFromJSONTyped(json: any, ignoreDiscriminator: boolean): TrailPage {
    if (json == null) {
        return json;
    }
    return {
        
        'data': json['data'] == null ? undefined : ((json['data'] as Array<any>).map(WireFromJSON)),
        'msg': json['msg'] == null ? undefined : json['msg'],
        'status': json['status'] == null ? undefined : json['status'],
        'total': json['total'] == null ? undefined : json['total'],
    };
}

export function TrailPageToJSON(json: any): TrailPage {
    return TrailPageToJSONTyped(json, false);
}

export function TrailPageToJSONTyped(value?: TrailPage | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'data': value['data'] == null ? undefined : ((value['data'] as Array<any>).map(WireToJSON)),
        'msg': value['msg'],
        'status': value['status'],
        'total': value['total'],
    };
}

