/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Status } from './Status.js';
import {
    StatusFromJSON,
    StatusFromJSONTyped,
    StatusToJSON,
    StatusToJSONTyped,
} from './Status.js';

/**
 * 
 * @export
 * @interface Host
 */
export interface Host {
    /**
     * 
     * @type {string}
     * @memberof Host
     */
    addr?: string;
    /**
     * Err is set when a peer could not be reached. Its plugins are then
     * unknown, which is NOT the same as none, so the list stays empty and the
     * drift below refuses to conclude anything from it.
     * @type {string}
     * @memberof Host
     */
    error?: string;
    /**
     * Host is the pod's stable id, and Addr where it was reached. Self is true
     * for the host that answered the request.
     * @type {string}
     * @memberof Host
     */
    host?: string;
    /**
     * 
     * @type {Array<Status>}
     * @memberof Host
     */
    plugins?: Array<Status>;
    /**
     * 
     * @type {boolean}
     * @memberof Host
     */
    self?: boolean;
}

/**
 * Check if a given object implements the Host interface.
 */
export function instanceOfHost(value: object): value is Host {
    return true;
}

export function HostFromJSON(json: any): Host {
    return HostFromJSONTyped(json, false);
}

export function HostFromJSONTyped(json: any, ignoreDiscriminator: boolean): Host {
    if (json == null) {
        return json;
    }
    return {
        
        'addr': json['addr'] == null ? undefined : json['addr'],
        'error': json['error'] == null ? undefined : json['error'],
        'host': json['host'] == null ? undefined : json['host'],
        'plugins': json['plugins'] == null ? undefined : ((json['plugins'] as Array<any>).map(StatusFromJSON)),
        'self': json['self'] == null ? undefined : json['self'],
    };
}

export function HostToJSON(json: any): Host {
    return HostToJSONTyped(json, false);
}

export function HostToJSONTyped(value?: Host | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'addr': value['addr'],
        'error': value['error'],
        'host': value['host'],
        'plugins': value['plugins'] == null ? undefined : ((value['plugins'] as Array<any>).map(StatusToJSON)),
        'self': value['self'],
    };
}

