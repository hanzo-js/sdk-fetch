/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface ReloadIn
 */
export interface ReloadIn {
    /**
     * Name is the app, from the path. It must be one the manifest declares.
     * @type {string}
     * @memberof ReloadIn
     */
    name?: string;
    /**
     * Scope "host" applies here only. Default "fleet" rolls it out one host at
     * a time, halting on the first host that fails to come up.
     * @type {string}
     * @memberof ReloadIn
     */
    scope?: string;
    /**
     * 
     * @type {string}
     * @memberof ReloadIn
     */
    sum?: string;
    /**
     * URL is the artifact directly, for an origin with no index. Sum is its hex
     * SHA-256 and is REQUIRED with it: zip refuses an unverified download, and
     * so does this.
     * @type {string}
     * @memberof ReloadIn
     */
    url?: string;
    /**
     * Version is a release tag, resolved to a URL and digest through the
     * origin's binaries.json index — the same index CI publishes, so there is
     * no second table mapping versions to digests.
     * @type {string}
     * @memberof ReloadIn
     */
    version?: string;
}

/**
 * Check if a given object implements the ReloadIn interface.
 */
export function instanceOfReloadIn(value: object): value is ReloadIn {
    return true;
}

export function ReloadInFromJSON(json: any): ReloadIn {
    return ReloadInFromJSONTyped(json, false);
}

export function ReloadInFromJSONTyped(json: any, ignoreDiscriminator: boolean): ReloadIn {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'] == null ? undefined : json['name'],
        'scope': json['scope'] == null ? undefined : json['scope'],
        'sum': json['sum'] == null ? undefined : json['sum'],
        'url': json['url'] == null ? undefined : json['url'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function ReloadInToJSON(json: any): ReloadIn {
    return ReloadInToJSONTyped(json, false);
}

export function ReloadInToJSONTyped(value?: ReloadIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'scope': value['scope'],
        'sum': value['sum'],
        'url': value['url'],
        'version': value['version'],
    };
}

