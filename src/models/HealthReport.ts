/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Loss } from './Loss.js';
import {
    LossFromJSON,
    LossFromJSONTyped,
    LossToJSON,
    LossToJSONTyped,
} from './Loss.js';
import type { HealthPlane } from './HealthPlane.js';
import {
    HealthPlaneFromJSON,
    HealthPlaneFromJSONTyped,
    HealthPlaneToJSON,
    HealthPlaneToJSONTyped,
} from './HealthPlane.js';
import type { HealthLenses } from './HealthLenses.js';
import {
    HealthLensesFromJSON,
    HealthLensesFromJSONTyped,
    HealthLensesToJSON,
    HealthLensesToJSONTyped,
} from './HealthLenses.js';

/**
 * 
 * @export
 * @interface HealthReport
 */
export interface HealthReport {
    /**
     * Datastore reports whether the shared warehouse client has a live connection.
     * It is load-bearing for the READ path: false is one of the two ways this
     * answers 503.
     * @type {boolean}
     * @memberof HealthReport
     */
    datastore?: boolean;
    /**
     * Lenses is per-lens table availability, probed only when connected — so it is
     * absent from a degraded report, which has nothing to say about tables it could
     * not reach.
     * @type {HealthLenses}
     * @memberof HealthReport
     */
    lenses?: HealthLenses;
    /**
     * Lost is the count of facts the sink irrecoverably dropped since boot
     * (warehouse.go). It is reported on the DEGRADED report too, and deliberately: a
     * warehouse that is unreachable is exactly when facts start failing their
     * deliveries, so suppressing the number here would hide it precisely when it
     * moves. ANY NON-ZERO VALUE IS AN ALARM — it counts data the endpoint already
     * answered 200 for.
     * @type {Loss}
     * @memberof HealthReport
     */
    lost?: Loss;
    /**
     * Plane reports the event plane — the bus and the stream every accepted event is
     * published to BEFORE any of it reaches the warehouse. It is load-bearing for the
     * WRITE path, and it is here because its absence was a real outage: this endpoint
     * answered 200/ok on warehouse connectivity alone while every POST /v1/event 503'd
     * on a stream that could not bind, so 100% ingest loss was invisible to monitoring.
     * A probe that cannot see the write path cannot report the write path.
     * @type {HealthPlane}
     * @memberof HealthReport
     */
    plane?: HealthPlane;
    /**
     * Reason is the human-readable cause, present only on a degraded report.
     * @type {string}
     * @memberof HealthReport
     */
    reason?: string;
    /**
     * Service names the subsystem answering, so a probe aggregating several health
     * endpoints can attribute a degraded one.
     * @type {string}
     * @memberof HealthReport
     */
    service?: string;
    /**
     * Status is ok or degraded. Degraded is the 503 and means EITHER load-bearing
     * dependency is down — the warehouse this subsystem reads, or the event plane it
     * writes. It is not moved by a missing lens table, which is honest-empty.
     * @type {string}
     * @memberof HealthReport
     */
    status?: string;
    /**
     * Warehouse names the datastore database every lens reads.
     * @type {string}
     * @memberof HealthReport
     */
    warehouse?: string;
}

/**
 * Check if a given object implements the HealthReport interface.
 */
export function instanceOfHealthReport(value: object): value is HealthReport {
    return true;
}

export function HealthReportFromJSON(json: any): HealthReport {
    return HealthReportFromJSONTyped(json, false);
}

export function HealthReportFromJSONTyped(json: any, ignoreDiscriminator: boolean): HealthReport {
    if (json == null) {
        return json;
    }
    return {
        
        'datastore': json['datastore'] == null ? undefined : json['datastore'],
        'lenses': json['lenses'] == null ? undefined : HealthLensesFromJSON(json['lenses']),
        'lost': json['lost'] == null ? undefined : LossFromJSON(json['lost']),
        'plane': json['plane'] == null ? undefined : HealthPlaneFromJSON(json['plane']),
        'reason': json['reason'] == null ? undefined : json['reason'],
        'service': json['service'] == null ? undefined : json['service'],
        'status': json['status'] == null ? undefined : json['status'],
        'warehouse': json['warehouse'] == null ? undefined : json['warehouse'],
    };
}

export function HealthReportToJSON(json: any): HealthReport {
    return HealthReportToJSONTyped(json, false);
}

export function HealthReportToJSONTyped(value?: HealthReport | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'datastore': value['datastore'],
        'lenses': HealthLensesToJSON(value['lenses']),
        'lost': LossToJSON(value['lost']),
        'plane': HealthPlaneToJSON(value['plane']),
        'reason': value['reason'],
        'service': value['service'],
        'status': value['status'],
        'warehouse': value['warehouse'],
    };
}

