/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ScoreView } from './ScoreView.js';
import {
    ScoreViewFromJSON,
    ScoreViewFromJSONTyped,
    ScoreViewToJSON,
    ScoreViewToJSONTyped,
} from './ScoreView.js';

/**
 * 
 * @export
 * @interface ScoreList
 */
export interface ScoreList {
    /**
     * Data is the caller org's score events matching the filters, bounded by limit.
     * @type {Array<ScoreView>}
     * @memberof ScoreList
     */
    data?: Array<ScoreView>;
}

/**
 * Check if a given object implements the ScoreList interface.
 */
export function instanceOfScoreList(value: object): value is ScoreList {
    return true;
}

export function ScoreListFromJSON(json: any): ScoreList {
    return ScoreListFromJSONTyped(json, false);
}

export function ScoreListFromJSONTyped(json: any, ignoreDiscriminator: boolean): ScoreList {
    if (json == null) {
        return json;
    }
    return {
        
        'data': json['data'] == null ? undefined : ((json['data'] as Array<any>).map(ScoreViewFromJSON)),
    };
}

export function ScoreListToJSON(json: any): ScoreList {
    return ScoreListToJSONTyped(json, false);
}

export function ScoreListToJSONTyped(value?: ScoreList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'data': value['data'] == null ? undefined : ((value['data'] as Array<any>).map(ScoreViewToJSON)),
    };
}

