/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface WebQuestion
 */
export interface WebQuestion {
    /**
     * Language narrows the search to a locale, BCP-47-ish ("en", "ja"). Empty
     * means no narrowing.
     * @type {string}
     * @memberof WebQuestion
     */
    language?: string;
    /**
     * MaxSources caps how many pages are read. Empty means the mode's own budget.
     * @type {number}
     * @memberof WebQuestion
     */
    maxSources?: number;
    /**
     * Mode is how much work to do: `search` (fast, one pass), `news` (recency
     * biased), `research` (a plan and several rounds) or `deep` (the widest
     * survey). Empty means research.
     * @type {string}
     * @memberof WebQuestion
     */
    mode?: string;
    /**
     * Q is the question, in plain language. Required.
     * @type {string}
     * @memberof WebQuestion
     */
    q?: string;
    /**
     * Sources narrows where the evidence comes from: any of `web`, `news`,
     * `academic`, `github`, `reddit`, `x`. Each becomes a site-scoped search, so
     * `["x"]` researches X/Twitter posts rather than the open web.
     * @type {Array<string>}
     * @memberof WebQuestion
     */
    sources?: Array<string>;
}

/**
 * Check if a given object implements the WebQuestion interface.
 */
export function instanceOfWebQuestion(value: object): value is WebQuestion {
    return true;
}

export function WebQuestionFromJSON(json: any): WebQuestion {
    return WebQuestionFromJSONTyped(json, false);
}

export function WebQuestionFromJSONTyped(json: any, ignoreDiscriminator: boolean): WebQuestion {
    if (json == null) {
        return json;
    }
    return {
        
        'language': json['language'] == null ? undefined : json['language'],
        'maxSources': json['max_sources'] == null ? undefined : json['max_sources'],
        'mode': json['mode'] == null ? undefined : json['mode'],
        'q': json['q'] == null ? undefined : json['q'],
        'sources': json['sources'] == null ? undefined : json['sources'],
    };
}

export function WebQuestionToJSON(json: any): WebQuestion {
    return WebQuestionToJSONTyped(json, false);
}

export function WebQuestionToJSONTyped(value?: WebQuestion | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'language': value['language'],
        'max_sources': value['maxSources'],
        'mode': value['mode'],
        'q': value['q'],
        'sources': value['sources'],
    };
}

