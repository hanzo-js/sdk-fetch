/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { CodeFile } from './CodeFile.js';
import {
    CodeFileFromJSON,
    CodeFileFromJSONTyped,
    CodeFileToJSON,
    CodeFileToJSONTyped,
} from './CodeFile.js';

/**
 * 
 * @export
 * @interface CodeRun
 */
export interface CodeRun {
    /**
     * Args become the PROGRAM's argv, never the compiler's. For the compiled
     * languages the toolchain builds first and these are passed to the binary it
     * produced.
     * @type {Array<string>}
     * @memberof CodeRun
     */
    args?: Array<string>;
    /**
     * Code is the WHOLE program, not a fragment: it is written to a single file and
     * that file is what runs, so a compiled language needs its entry point and an
     * interpreted one runs top to bottom.
     * @type {string}
     * @memberof CodeRun
     */
    code: string;
    /**
     * Files are inputs the host already put in some session. Each names the session
     * its bytes live in, which is usually — and ideally — the session this run wants.
     * @type {Array<CodeFile>}
     * @memberof CodeRun
     */
    files?: Array<CodeFile>;
    /**
     * Lang selects the toolchain, and with it the filename the code is written to
     * and the line that runs it: py, js, ts, bash, r, php, go, rs, c, cpp, java, d,
     * f90. Anything else is refused rather than guessed at — a run in the wrong
     * language fails somewhere deep in a compiler, which reads as an outage.
     * @type {string}
     * @memberof CodeRun
     */
    lang: string;
    /**
     * RuntimeSessionHint is the stateful-session hint. It is carried so a client
     * that sends it is not silently misread, and it selects nothing here: every
     * session in this implementation is already a warm sandbox, so there is no
     * second kind of runtime for a hint to choose between.
     * @type {string}
     * @memberof CodeRun
     */
    runtimeSessionHint?: string;
    /**
     * SessionID continues an EXISTING sandbox, which is what makes runs stateful:
     * the same filesystem, so one run's output file is the next run's input. Empty
     * leases a fresh sandbox and the id it got comes back on the result.
     * @type {string}
     * @memberof CodeRun
     */
    sessionId?: string;
    /**
     * UserID attributes the run inside the caller's org. It is a label, never a
     * tenant: the org is resolved from the validated principal and a value here
     * cannot widen what the run may reach.
     * @type {string}
     * @memberof CodeRun
     */
    userId?: string;
}

/**
 * Check if a given object implements the CodeRun interface.
 */
export function instanceOfCodeRun(value: object): value is CodeRun {
    if (!('code' in value) || value['code'] === undefined) return false;
    if (!('lang' in value) || value['lang'] === undefined) return false;
    return true;
}

export function CodeRunFromJSON(json: any): CodeRun {
    return CodeRunFromJSONTyped(json, false);
}

export function CodeRunFromJSONTyped(json: any, ignoreDiscriminator: boolean): CodeRun {
    if (json == null) {
        return json;
    }
    return {
        
        'args': json['args'] == null ? undefined : json['args'],
        'code': json['code'],
        'files': json['files'] == null ? undefined : ((json['files'] as Array<any>).map(CodeFileFromJSON)),
        'lang': json['lang'],
        'runtimeSessionHint': json['runtime_session_hint'] == null ? undefined : json['runtime_session_hint'],
        'sessionId': json['session_id'] == null ? undefined : json['session_id'],
        'userId': json['user_id'] == null ? undefined : json['user_id'],
    };
}

export function CodeRunToJSON(json: any): CodeRun {
    return CodeRunToJSONTyped(json, false);
}

export function CodeRunToJSONTyped(value?: CodeRun | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'args': value['args'],
        'code': value['code'],
        'files': value['files'] == null ? undefined : ((value['files'] as Array<any>).map(CodeFileToJSON)),
        'lang': value['lang'],
        'runtime_session_hint': value['runtimeSessionHint'],
        'session_id': value['sessionId'],
        'user_id': value['userId'],
    };
}

