/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ReferenceSet } from './ReferenceSet.js';
import {
    ReferenceSetFromJSON,
    ReferenceSetFromJSONTyped,
    ReferenceSetToJSON,
    ReferenceSetToJSONTyped,
} from './ReferenceSet.js';

/**
 * 
 * @export
 * @interface ReferenceSetsOut
 */
export interface ReferenceSetsOut {
    /**
     * Refused names the sets that cannot be consulted at all. A key checked
     * against one of these is UNKNOWN, not clean.
     * @type {Array<string>}
     * @memberof ReferenceSetsOut
     */
    refused?: Array<string>;
    /**
     * Sets is the whole catalog, in a stable order.
     * @type {Array<ReferenceSet>}
     * @memberof ReferenceSetsOut
     */
    sets?: Array<ReferenceSet>;
    /**
     * Stale names the sets past their freshness bound — the list to alarm on.
     * @type {Array<string>}
     * @memberof ReferenceSetsOut
     */
    stale?: Array<string>;
}

/**
 * Check if a given object implements the ReferenceSetsOut interface.
 */
export function instanceOfReferenceSetsOut(value: object): value is ReferenceSetsOut {
    return true;
}

export function ReferenceSetsOutFromJSON(json: any): ReferenceSetsOut {
    return ReferenceSetsOutFromJSONTyped(json, false);
}

export function ReferenceSetsOutFromJSONTyped(json: any, ignoreDiscriminator: boolean): ReferenceSetsOut {
    if (json == null) {
        return json;
    }
    return {
        
        'refused': json['refused'] == null ? undefined : json['refused'],
        'sets': json['sets'] == null ? undefined : ((json['sets'] as Array<any>).map(ReferenceSetFromJSON)),
        'stale': json['stale'] == null ? undefined : json['stale'],
    };
}

export function ReferenceSetsOutToJSON(json: any): ReferenceSetsOut {
    return ReferenceSetsOutToJSONTyped(json, false);
}

export function ReferenceSetsOutToJSONTyped(value?: ReferenceSetsOut | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'refused': value['refused'],
        'sets': value['sets'] == null ? undefined : ((value['sets'] as Array<any>).map(ReferenceSetToJSON)),
        'stale': value['stale'],
    };
}

