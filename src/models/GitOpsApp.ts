/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { GitOpsDeploy } from './GitOpsDeploy.js';
import {
    GitOpsDeployFromJSON,
    GitOpsDeployFromJSONTyped,
    GitOpsDeployToJSON,
    GitOpsDeployToJSONTyped,
} from './GitOpsDeploy.js';
import type { GitOpsOperation } from './GitOpsOperation.js';
import {
    GitOpsOperationFromJSON,
    GitOpsOperationFromJSONTyped,
    GitOpsOperationToJSON,
    GitOpsOperationToJSONTyped,
} from './GitOpsOperation.js';

/**
 * 
 * @export
 * @interface GitOpsApp
 */
export interface GitOpsApp {
    /**
     * Automated is whether CD applies new commits without being asked. It reads the
     * PRESENCE of spec.syncPolicy.automated, which is a block rather than a
     * boolean; false means drift is reported and nothing moves.
     * @type {boolean}
     * @memberof GitOpsApp
     */
    automated?: boolean;
    /**
     * Health is CD's verdict on the objects it manages, verbatim: Healthy,
     * Progressing, Degraded, Suspended, Missing or Unknown.
     * @type {string}
     * @memberof GitOpsApp
     */
    health?: string;
    /**
     * History is the recent deploy log, NEWEST FIRST and capped at ten. CD appends
     * oldest-first and bounds the list itself; the reversal happens here so a
     * caller never has to know the storage order to show what shipped last. Empty
     * (never null) for an Application that has deployed nothing.
     * @type {Array<GitOpsDeploy>}
     * @memberof GitOpsApp
     */
    history?: Array<GitOpsDeploy>;
    /**
     * Name is what CD calls this tracked source, not the workload it deploys —
     * the Application CR's own metadata.name. The fleet ApplicationSet mints these
     * as <namespace>-<app>.
     * @type {string}
     * @memberof GitOpsApp
     */
    name?: string;
    /**
     * Namespace is where the Application OBJECT lives: CD's own controller
     * namespace, which is the same one for every row here. It is NOT the
     * destination the workloads land in — this endpoint lists cluster-wide and
     * never reads spec.destination.
     * @type {string}
     * @memberof GitOpsApp
     */
    namespace?: string;
    /**
     * Operation is the last sync attempt and how it ended. Absent when CD has run
     * none, which is the honest gap between "never tried" and "tried and failed".
     * @type {GitOpsOperation}
     * @memberof GitOpsApp
     */
    operation?: GitOpsOperation;
    /**
     * Path is the directory inside that repository CD renders, relative to its root.
     * @type {string}
     * @memberof GitOpsApp
     */
    path?: string;
    /**
     * Project is the AppProject fence the sync is admitted under: which repos this
     * Application may pull from and which destinations it may write to. Empty when
     * the CR declares none.
     * @type {string}
     * @memberof GitOpsApp
     */
    project?: string;
    /**
     * ReconciledAt is when CD last COMPARED this Application against git, RFC 3339.
     * It moves on every comparison, including ones that applied nothing.
     * @type {string}
     * @memberof GitOpsApp
     */
    reconciledAt?: string;
    /**
     * RepoURL is the git repository CD polls for this Application's desired state.
     * @type {string}
     * @memberof GitOpsApp
     */
    repoURL?: string;
    /**
     * Resources is how MANY objects CD manages for this Application
     * (len(status.resources)) — a count, not the objects. Zero for an Application
     * CD has not reconciled.
     * @type {number}
     * @memberof GitOpsApp
     */
    resources?: number;
    /**
     * Revision is the commit CD last APPLIED (status.sync.revision). Empty means it
     * has applied none — never read that as the head of TargetRevision.
     * @type {string}
     * @memberof GitOpsApp
     */
    revision?: string;
    /**
     * SelfHeal is whether CD also reverts changes made directly in the cluster
     * (syncPolicy.automated.selfHeal). Meaningless unless Automated.
     * @type {boolean}
     * @memberof GitOpsApp
     */
    selfHeal?: boolean;
    /**
     * Sync is CD's verdict on git versus cluster, verbatim: Synced, OutOfSync or
     * Unknown. It is about the applied REVISION, so an Application can be Synced to
     * a commit that is several behind the branch it tracks.
     * @type {string}
     * @memberof GitOpsApp
     */
    sync?: string;
    /**
     * TargetRevision is the git ref CD TRACKS — usually a branch such as "main".
     * It is what CD aims at; Revision is what it has reached.
     * @type {string}
     * @memberof GitOpsApp
     */
    targetRevision?: string;
}

/**
 * Check if a given object implements the GitOpsApp interface.
 */
export function instanceOfGitOpsApp(value: object): value is GitOpsApp {
    return true;
}

export function GitOpsAppFromJSON(json: any): GitOpsApp {
    return GitOpsAppFromJSONTyped(json, false);
}

export function GitOpsAppFromJSONTyped(json: any, ignoreDiscriminator: boolean): GitOpsApp {
    if (json == null) {
        return json;
    }
    return {
        
        'automated': json['automated'] == null ? undefined : json['automated'],
        'health': json['health'] == null ? undefined : json['health'],
        'history': json['history'] == null ? undefined : ((json['history'] as Array<any>).map(GitOpsDeployFromJSON)),
        'name': json['name'] == null ? undefined : json['name'],
        'namespace': json['namespace'] == null ? undefined : json['namespace'],
        'operation': json['operation'] == null ? undefined : GitOpsOperationFromJSON(json['operation']),
        'path': json['path'] == null ? undefined : json['path'],
        'project': json['project'] == null ? undefined : json['project'],
        'reconciledAt': json['reconciledAt'] == null ? undefined : json['reconciledAt'],
        'repoURL': json['repoURL'] == null ? undefined : json['repoURL'],
        'resources': json['resources'] == null ? undefined : json['resources'],
        'revision': json['revision'] == null ? undefined : json['revision'],
        'selfHeal': json['selfHeal'] == null ? undefined : json['selfHeal'],
        'sync': json['sync'] == null ? undefined : json['sync'],
        'targetRevision': json['targetRevision'] == null ? undefined : json['targetRevision'],
    };
}

export function GitOpsAppToJSON(json: any): GitOpsApp {
    return GitOpsAppToJSONTyped(json, false);
}

export function GitOpsAppToJSONTyped(value?: GitOpsApp | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'automated': value['automated'],
        'health': value['health'],
        'history': value['history'] == null ? undefined : ((value['history'] as Array<any>).map(GitOpsDeployToJSON)),
        'name': value['name'],
        'namespace': value['namespace'],
        'operation': GitOpsOperationToJSON(value['operation']),
        'path': value['path'],
        'project': value['project'],
        'reconciledAt': value['reconciledAt'],
        'repoURL': value['repoURL'],
        'resources': value['resources'],
        'revision': value['revision'],
        'selfHeal': value['selfHeal'],
        'sync': value['sync'],
        'targetRevision': value['targetRevision'],
    };
}

