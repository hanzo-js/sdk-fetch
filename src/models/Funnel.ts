/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface Funnel
 */
export interface Funnel {
    /**
     * ConvertedOrgs is how many distinct referred orgs have produced positive
     * commission at least once — a referral that actually spent.
     * @type {number}
     * @memberof Funnel
     */
    convertedOrgs?: number;
    /**
     * RatePct is convertedOrgs over referredOrgs as a PERCENTAGE, 0–100, and the one
     * non-integer figure on this board. It is 0 when nothing has been referred yet,
     * not undefined.
     * @type {number}
     * @memberof Funnel
     */
    ratePct?: number;
    /**
     * ReferredOrgs is how many attribution edges exist fleet-wide — one per referred
     * org, first-touch, so it is also the count of distinct referred orgs.
     * @type {number}
     * @memberof Funnel
     */
    referredOrgs?: number;
}

/**
 * Check if a given object implements the Funnel interface.
 */
export function instanceOfFunnel(value: object): value is Funnel {
    return true;
}

export function FunnelFromJSON(json: any): Funnel {
    return FunnelFromJSONTyped(json, false);
}

export function FunnelFromJSONTyped(json: any, ignoreDiscriminator: boolean): Funnel {
    if (json == null) {
        return json;
    }
    return {
        
        'convertedOrgs': json['convertedOrgs'] == null ? undefined : json['convertedOrgs'],
        'ratePct': json['ratePct'] == null ? undefined : json['ratePct'],
        'referredOrgs': json['referredOrgs'] == null ? undefined : json['referredOrgs'],
    };
}

export function FunnelToJSON(json: any): Funnel {
    return FunnelToJSONTyped(json, false);
}

export function FunnelToJSONTyped(value?: Funnel | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'convertedOrgs': value['convertedOrgs'],
        'ratePct': value['ratePct'],
        'referredOrgs': value['referredOrgs'],
    };
}

