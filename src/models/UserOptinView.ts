/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface UserOptinView
 */
export interface UserOptinView {
    /**
     * CanSet is false when the caller's ledger identity cannot be resolved (no user
     * name on the principal). Writing the preference would fail, so hide the control.
     * @type {boolean}
     * @memberof UserOptinView
     */
    canSet?: boolean;
    /**
     * Handle is the display name on the caller's listed row. Empty when they never
     * chose one; opting in without a handle sets it to their username, so a listed row
     * is never blank.
     * @type {string}
     * @memberof UserOptinView
     */
    handle?: string;
    /**
     * Listed is true when the caller's board row is published under Handle to other
     * viewers. False — the default for anyone who never opted in — anonymizes the row;
     * the metric still counts, only the name is withheld.
     * @type {boolean}
     * @memberof UserOptinView
     */
    listed?: boolean;
}

/**
 * Check if a given object implements the UserOptinView interface.
 */
export function instanceOfUserOptinView(value: object): value is UserOptinView {
    return true;
}

export function UserOptinViewFromJSON(json: any): UserOptinView {
    return UserOptinViewFromJSONTyped(json, false);
}

export function UserOptinViewFromJSONTyped(json: any, ignoreDiscriminator: boolean): UserOptinView {
    if (json == null) {
        return json;
    }
    return {
        
        'canSet': json['canSet'] == null ? undefined : json['canSet'],
        'handle': json['handle'] == null ? undefined : json['handle'],
        'listed': json['listed'] == null ? undefined : json['listed'],
    };
}

export function UserOptinViewToJSON(json: any): UserOptinView {
    return UserOptinViewToJSONTyped(json, false);
}

export function UserOptinViewToJSONTyped(value?: UserOptinView | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'canSet': value['canSet'],
        'handle': value['handle'],
        'listed': value['listed'],
    };
}

