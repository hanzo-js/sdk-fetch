/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface RoomWork
 */
export interface RoomWork {
    /**
     * Open is how many items are still work: everything whose status does not
     * end it. It is the number a channel header shows.
     * @type {number}
     * @memberof RoomWork
     */
    open?: number;
    /**
     * Room is the room these counts are for, echoed back as it was resolved.
     * @type {string}
     * @memberof RoomWork
     */
    room?: string;
    /**
     * Status is the count per board column, carrying EVERY column this surface
     * knows — an empty column reads 0 rather than being absent, so a caller can
     * render the board without inventing the vocabulary. The keys are the same
     * closed set every other operation here validates against.
     * @type {{ [key: string]: number; }}
     * @memberof RoomWork
     */
    status?: { [key: string]: number; };
    /**
     * Total is every item bound to this room, settled ones included, so Total
     * minus Open is what the room has finished.
     * @type {number}
     * @memberof RoomWork
     */
    total?: number;
    /**
     * Updated is when anything in this room's work last moved, in unix seconds.
     * ABSENT when the room has no work at all: zero would read as the epoch, and
     * a room nobody has filed anything in has no last activity rather than an
     * infinitely old one. Total is 0 in exactly that case.
     * @type {number}
     * @memberof RoomWork
     */
    updated?: number;
}

/**
 * Check if a given object implements the RoomWork interface.
 */
export function instanceOfRoomWork(value: object): value is RoomWork {
    return true;
}

export function RoomWorkFromJSON(json: any): RoomWork {
    return RoomWorkFromJSONTyped(json, false);
}

export function RoomWorkFromJSONTyped(json: any, ignoreDiscriminator: boolean): RoomWork {
    if (json == null) {
        return json;
    }
    return {
        
        'open': json['open'] == null ? undefined : json['open'],
        'room': json['room'] == null ? undefined : json['room'],
        'status': json['status'] == null ? undefined : json['status'],
        'total': json['total'] == null ? undefined : json['total'],
        'updated': json['updated'] == null ? undefined : json['updated'],
    };
}

export function RoomWorkToJSON(json: any): RoomWork {
    return RoomWorkToJSONTyped(json, false);
}

export function RoomWorkToJSONTyped(value?: RoomWork | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'open': value['open'],
        'room': value['room'],
        'status': value['status'],
        'total': value['total'],
        'updated': value['updated'],
    };
}

