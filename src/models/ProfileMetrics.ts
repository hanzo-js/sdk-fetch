/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Funnel } from './Funnel.js';
import {
    FunnelFromJSON,
    FunnelFromJSONTyped,
    FunnelToJSON,
    FunnelToJSONTyped,
} from './Funnel.js';
import type { ProgressView } from './ProgressView.js';
import {
    ProgressViewFromJSON,
    ProgressViewFromJSONTyped,
    ProgressViewToJSON,
    ProgressViewToJSONTyped,
} from './ProgressView.js';

/**
 * 
 * @export
 * @interface ProfileMetrics
 */
export interface ProfileMetrics {
    /**
     * Funnel is what the org's analytics observed over the trailing window. Read
     * its `available` first: an org with no analytics reports zeros here, and zero
     * traffic and no measurement are different facts.
     * @type {Funnel}
     * @memberof ProfileMetrics
     */
    funnel?: Funnel;
    /**
     * LaunchProgress is the org's own position in the launch checklist, folded in
     * so a profile carries both what the org has BUILT and what it has DONE.
     * @type {ProgressView}
     * @memberof ProfileMetrics
     */
    launchProgress?: ProgressView;
    /**
     * Records is how many business records the org holds — the volume that tells a
     * real book of customers from an empty account. It feeds the `customers`
     * signal, which crosses at a threshold rather than at one row.
     * @type {number}
     * @memberof ProfileMetrics
     */
    records?: number;
    /**
     * RevenueCents is the org's money OF RECORD — what its books say, in whole
     * cents, never a float and never a display string. This is the number the
     * scaling stage is decided on; funnel.revenue is the beacon's separate,
     * unreconciled view of the same business. Zero when the org has none, and also
     * zero when the books could not be read, which is why the `revenue` signal
     * beside it is the thing to trust.
     * @type {number}
     * @memberof ProfileMetrics
     */
    revenueCents?: number;
}

/**
 * Check if a given object implements the ProfileMetrics interface.
 */
export function instanceOfProfileMetrics(value: object): value is ProfileMetrics {
    return true;
}

export function ProfileMetricsFromJSON(json: any): ProfileMetrics {
    return ProfileMetricsFromJSONTyped(json, false);
}

export function ProfileMetricsFromJSONTyped(json: any, ignoreDiscriminator: boolean): ProfileMetrics {
    if (json == null) {
        return json;
    }
    return {
        
        'funnel': json['funnel'] == null ? undefined : FunnelFromJSON(json['funnel']),
        'launchProgress': json['launchProgress'] == null ? undefined : ProgressViewFromJSON(json['launchProgress']),
        'records': json['records'] == null ? undefined : json['records'],
        'revenueCents': json['revenueCents'] == null ? undefined : json['revenueCents'],
    };
}

export function ProfileMetricsToJSON(json: any): ProfileMetrics {
    return ProfileMetricsToJSONTyped(json, false);
}

export function ProfileMetricsToJSONTyped(value?: ProfileMetrics | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'funnel': FunnelToJSON(value['funnel']),
        'launchProgress': ProgressViewToJSON(value['launchProgress']),
        'records': value['records'],
        'revenueCents': value['revenueCents'],
    };
}

