/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ItemView } from './ItemView.js';
import {
    ItemViewFromJSON,
    ItemViewFromJSONTyped,
    ItemViewToJSON,
    ItemViewToJSONTyped,
} from './ItemView.js';

/**
 * 
 * @export
 * @interface ItemList
 */
export interface ItemList {
    /**
     * Data is the examples of the one dataset named in the path, archived ones
     * included, so the caller sees the whole set rather than what a run would use.
     * @type {Array<ItemView>}
     * @memberof ItemList
     */
    data?: Array<ItemView>;
}

/**
 * Check if a given object implements the ItemList interface.
 */
export function instanceOfItemList(value: object): value is ItemList {
    return true;
}

export function ItemListFromJSON(json: any): ItemList {
    return ItemListFromJSONTyped(json, false);
}

export function ItemListFromJSONTyped(json: any, ignoreDiscriminator: boolean): ItemList {
    if (json == null) {
        return json;
    }
    return {
        
        'data': json['data'] == null ? undefined : ((json['data'] as Array<any>).map(ItemViewFromJSON)),
    };
}

export function ItemListToJSON(json: any): ItemList {
    return ItemListToJSONTyped(json, false);
}

export function ItemListToJSONTyped(value?: ItemList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'data': value['data'] == null ? undefined : ((value['data'] as Array<any>).map(ItemViewToJSON)),
    };
}

