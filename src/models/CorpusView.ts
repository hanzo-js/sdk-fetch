/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { StrategyView } from './StrategyView.js';
import {
    StrategyViewFromJSON,
    StrategyViewFromJSONTyped,
    StrategyViewToJSON,
    StrategyViewToJSONTyped,
} from './StrategyView.js';

/**
 * 
 * @export
 * @interface CorpusView
 */
export interface CorpusView {
    /**
     * Count is how many tactics survived every filter.
     * @type {number}
     * @memberof CorpusView
     */
    count?: number;
    /**
     * Stage is the growth stage the tag join ran at — the org's observed stage, or
     * the one ?stage= previewed.
     * @type {string}
     * @memberof CorpusView
     */
    stage?: string;
    /**
     * Strategies are the surviving tactics, in corpus authoring order.
     * @type {Array<StrategyView>}
     * @memberof CorpusView
     */
    strategies?: Array<StrategyView>;
}

/**
 * Check if a given object implements the CorpusView interface.
 */
export function instanceOfCorpusView(value: object): value is CorpusView {
    return true;
}

export function CorpusViewFromJSON(json: any): CorpusView {
    return CorpusViewFromJSONTyped(json, false);
}

export function CorpusViewFromJSONTyped(json: any, ignoreDiscriminator: boolean): CorpusView {
    if (json == null) {
        return json;
    }
    return {
        
        'count': json['count'] == null ? undefined : json['count'],
        'stage': json['stage'] == null ? undefined : json['stage'],
        'strategies': json['strategies'] == null ? undefined : ((json['strategies'] as Array<any>).map(StrategyViewFromJSON)),
    };
}

export function CorpusViewToJSON(json: any): CorpusView {
    return CorpusViewToJSONTyped(json, false);
}

export function CorpusViewToJSONTyped(value?: CorpusView | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'count': value['count'],
        'stage': value['stage'],
        'strategies': value['strategies'] == null ? undefined : ((value['strategies'] as Array<any>).map(StrategyViewToJSON)),
    };
}

