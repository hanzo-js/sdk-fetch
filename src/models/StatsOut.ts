/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { StatsSessions } from './StatsSessions.js';
import {
    StatsSessionsFromJSON,
    StatsSessionsFromJSONTyped,
    StatsSessionsToJSON,
    StatsSessionsToJSONTyped,
} from './StatsSessions.js';

/**
 * 
 * @export
 * @interface StatsOut
 */
export interface StatsOut {
    /**
     * Admin is the upstream service's server-panel flag, always false here.
     * @type {boolean}
     * @memberof StatsOut
     */
    admin?: boolean;
    /**
     * Metrics is the upstream transactor's metrics block. This server does not
     * populate it, so it is always the empty object — the front reads the key,
     * not its contents.
     * @type {object}
     * @memberof StatsOut
     */
    metrics?: object;
    /**
     * Statistics carries the live sessions.
     * @type {StatsSessions}
     * @memberof StatsOut
     */
    statistics?: StatsSessions;
}

/**
 * Check if a given object implements the StatsOut interface.
 */
export function instanceOfStatsOut(value: object): value is StatsOut {
    return true;
}

export function StatsOutFromJSON(json: any): StatsOut {
    return StatsOutFromJSONTyped(json, false);
}

export function StatsOutFromJSONTyped(json: any, ignoreDiscriminator: boolean): StatsOut {
    if (json == null) {
        return json;
    }
    return {
        
        'admin': json['admin'] == null ? undefined : json['admin'],
        'metrics': json['metrics'] == null ? undefined : json['metrics'],
        'statistics': json['statistics'] == null ? undefined : StatsSessionsFromJSON(json['statistics']),
    };
}

export function StatsOutToJSON(json: any): StatsOut {
    return StatsOutToJSONTyped(json, false);
}

export function StatsOutToJSONTyped(value?: StatsOut | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'admin': value['admin'],
        'metrics': value['metrics'],
        'statistics': StatsSessionsToJSON(value['statistics']),
    };
}

