/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface FleetSpec
 */
export interface FleetSpec {
    /**
     * Arch is the CPU architecture, amd64 or arm64, and it is what decides whether
     * a binary built for the fleet will run here. Only the sources that report one
     * carry it (a linked run-target, a BYO worker).
     * @type {string}
     * @memberof FleetSpec
     */
    arch?: string;
    /**
     * CPUs is logical cores on the unit.
     * @type {number}
     * @memberof FleetSpec
     */
    cpus?: number;
    /**
     * GPUModel names the FIRST accelerator ("NVIDIA GB10") as the representative of
     * the set; GPUs carries how many. Empty for a cluster, whose cards are counted
     * rather than modelled, and for a unit with none.
     * @type {string}
     * @memberof FleetSpec
     */
    gpuModel?: string;
    /**
     * GPUs is how many accelerators the unit has. For a cluster it is the vendor
     * totals summed across every node, so it counts cards, not machines.
     * @type {number}
     * @memberof FleetSpec
     */
    gpus?: number;
    /**
     * Memory is total system RAM in BYTES — not GB, and not what is free right now
     * (fleetMetrics carries that). Absent when the source reports no RAM figure.
     * @type {number}
     * @memberof FleetSpec
     */
    memory?: number;
    /**
     * OS is the operating system the unit runs: linux, darwin or windows. Empty
     * when the source does not report one — a cluster row does not.
     * @type {string}
     * @memberof FleetSpec
     */
    os?: string;
}

/**
 * Check if a given object implements the FleetSpec interface.
 */
export function instanceOfFleetSpec(value: object): value is FleetSpec {
    return true;
}

export function FleetSpecFromJSON(json: any): FleetSpec {
    return FleetSpecFromJSONTyped(json, false);
}

export function FleetSpecFromJSONTyped(json: any, ignoreDiscriminator: boolean): FleetSpec {
    if (json == null) {
        return json;
    }
    return {
        
        'arch': json['arch'] == null ? undefined : json['arch'],
        'cpus': json['cpus'] == null ? undefined : json['cpus'],
        'gpuModel': json['gpuModel'] == null ? undefined : json['gpuModel'],
        'gpus': json['gpus'] == null ? undefined : json['gpus'],
        'memory': json['memory'] == null ? undefined : json['memory'],
        'os': json['os'] == null ? undefined : json['os'],
    };
}

export function FleetSpecToJSON(json: any): FleetSpec {
    return FleetSpecToJSONTyped(json, false);
}

export function FleetSpecToJSONTyped(value?: FleetSpec | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'arch': value['arch'],
        'cpus': value['cpus'],
        'gpuModel': value['gpuModel'],
        'gpus': value['gpus'],
        'memory': value['memory'],
        'os': value['os'],
    };
}

