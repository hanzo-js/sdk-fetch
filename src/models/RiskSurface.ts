/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface RiskSurface
 */
export interface RiskSurface {
    /**
     * Folded is how many buckets of the tenant's own feature surface were folded
     * into the model when it became resident.
     * @type {number}
     * @memberof RiskSurface
     */
    folded?: number;
    /**
     * Gap says why the fold did not happen or did not complete, when that is the
     * case. An empty surface and an unreachable warehouse are different facts and
     * a model must not report them as the same one.
     * @type {string}
     * @memberof RiskSurface
     */
    gap?: string;
    /**
     * Refused is how many buckets of this organisation's own surface the fold
     * could not fold, because a subject on them is longer than this plane's own
     * field bound. It is history the model does not have, said out loud.
     * @type {number}
     * @memberof RiskSurface
     */
    refused?: number;
    /**
     * Replayed is how many of this organisation's own recorded observations
     * rebuilt its sliding aggregates when the model became resident. It is what
     * says a rollout was a rebuild rather than a blindness: the aggregates are a
     * projection of a durable record, so a restart costs a replay and not a
     * control.
     * @type {number}
     * @memberof RiskSurface
     */
    replayed?: number;
    /**
     * Rolled is how many windows of this organisation's own source planes —
     * product events, captured failures, metered inference — were rolled up into
     * its feature surface before that fold. Zero with no gap means the surface was
     * already current, which is a different fact from the rollup never running.
     * @type {number}
     * @memberof RiskSurface
     */
    rolled?: number;
    /**
     * Window is the lookback the fold covered.
     * @type {string}
     * @memberof RiskSurface
     */
    window?: string;
}

/**
 * Check if a given object implements the RiskSurface interface.
 */
export function instanceOfRiskSurface(value: object): value is RiskSurface {
    return true;
}

export function RiskSurfaceFromJSON(json: any): RiskSurface {
    return RiskSurfaceFromJSONTyped(json, false);
}

export function RiskSurfaceFromJSONTyped(json: any, ignoreDiscriminator: boolean): RiskSurface {
    if (json == null) {
        return json;
    }
    return {
        
        'folded': json['folded'] == null ? undefined : json['folded'],
        'gap': json['gap'] == null ? undefined : json['gap'],
        'refused': json['refused'] == null ? undefined : json['refused'],
        'replayed': json['replayed'] == null ? undefined : json['replayed'],
        'rolled': json['rolled'] == null ? undefined : json['rolled'],
        'window': json['window'] == null ? undefined : json['window'],
    };
}

export function RiskSurfaceToJSON(json: any): RiskSurface {
    return RiskSurfaceToJSONTyped(json, false);
}

export function RiskSurfaceToJSONTyped(value?: RiskSurface | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'folded': value['folded'],
        'gap': value['gap'],
        'refused': value['refused'],
        'replayed': value['replayed'],
        'rolled': value['rolled'],
        'window': value['window'],
    };
}

