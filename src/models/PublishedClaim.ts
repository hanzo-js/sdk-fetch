/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface PublishedClaim
 */
export interface PublishedClaim {
    /**
     * Benchmark is the canonical test id the claim is about, from /catalog.
     * @type {string}
     * @memberof PublishedClaim
     */
    benchmark?: string;
    /**
     * Model is the system the score is claimed for.
     * @type {string}
     * @memberof PublishedClaim
     */
    model?: string;
    /**
     * Protocol records HOW it was scored — provider-reported, agentic,
     * third-party-leaderboard — because a provider card and a third party running
     * its own harness are different kinds of number and must not be blended.
     * @type {string}
     * @memberof PublishedClaim
     */
    protocol?: string;
    /**
     * Provider is who the claim belongs to — the lab or leaderboard whose number
     * this is. It joins a claim to the attempts measured for that same model.
     * @type {string}
     * @memberof PublishedClaim
     */
    provider?: string;
    /**
     * Score is the reported aggregate, as a percentage.
     * @type {number}
     * @memberof PublishedClaim
     */
    score?: number;
    /**
     * Source is the citation the row was read from. A claim without one is a
     * number nobody can check, so every write requires it.
     * @type {string}
     * @memberof PublishedClaim
     */
    source?: string;
}

/**
 * Check if a given object implements the PublishedClaim interface.
 */
export function instanceOfPublishedClaim(value: object): value is PublishedClaim {
    return true;
}

export function PublishedClaimFromJSON(json: any): PublishedClaim {
    return PublishedClaimFromJSONTyped(json, false);
}

export function PublishedClaimFromJSONTyped(json: any, ignoreDiscriminator: boolean): PublishedClaim {
    if (json == null) {
        return json;
    }
    return {
        
        'benchmark': json['benchmark'] == null ? undefined : json['benchmark'],
        'model': json['model'] == null ? undefined : json['model'],
        'protocol': json['protocol'] == null ? undefined : json['protocol'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'score': json['score'] == null ? undefined : json['score'],
        'source': json['source'] == null ? undefined : json['source'],
    };
}

export function PublishedClaimToJSON(json: any): PublishedClaim {
    return PublishedClaimToJSONTyped(json, false);
}

export function PublishedClaimToJSONTyped(value?: PublishedClaim | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'benchmark': value['benchmark'],
        'model': value['model'],
        'protocol': value['protocol'],
        'provider': value['provider'],
        'score': value['score'],
        'source': value['source'],
    };
}

