/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { LevelView } from './LevelView.js';
import {
    LevelViewFromJSON,
    LevelViewFromJSONTyped,
    LevelViewToJSON,
    LevelViewToJSONTyped,
} from './LevelView.js';
import type { Remittance } from './Remittance.js';
import {
    RemittanceFromJSON,
    RemittanceFromJSONTyped,
    RemittanceToJSON,
    RemittanceToJSONTyped,
} from './Remittance.js';

/**
 * 
 * @export
 * @interface AffiliateSelf
 */
export interface AffiliateSelf {
    /**
     * AccruedCents is lifetime commission accrued, in cents. It only grows — a
     * payout is recorded against paidCents and never reduces this.
     * @type {number}
     * @memberof AffiliateSelf
     */
    accruedCents?: number;
    /**
     * Code is the minted referral code, the slug the ?aff link carries. Absent until
     * staff approve; codes live in ONE global namespace across all affiliates.
     * @type {string}
     * @memberof AffiliateSelf
     */
    code?: string;
    /**
     * DefaultRateBps is the direct rate a new affiliate starts at, in basis points
     * of margin (2000 = 20%). Answered ONLY to a caller that has not applied, as the
     * quote beside `schedule`.
     * @type {number}
     * @memberof AffiliateSelf
     */
    defaultRateBps?: number;
    /**
     * DownlineTotal counts every org in the caller's downline across the levels.
     * @type {number}
     * @memberof AffiliateSelf
     */
    downlineTotal?: number;
    /**
     * Handle is the opt-in public leaderboard name. Empty means opted out: the
     * caller keeps its rank and still sees its own row, it is just not listed.
     * @type {string}
     * @memberof AffiliateSelf
     */
    handle?: string;
    /**
     * ID is the affiliate's server-minted handle, "aff_"-prefixed. Absent until the
     * org applies.
     * @type {string}
     * @memberof AffiliateSelf
     */
    id?: string;
    /**
     * IsAffiliate says whether the caller org has an affiliate record. On false the
     * answer carries the rate SCHEDULE and the default rate instead of a downline,
     * so the console can show what the caller would earn.
     * @type {boolean}
     * @memberof AffiliateSelf
     */
    isAffiliate?: boolean;
    /**
     * Levels is the caller's downline per upline level, with the rate paid there.
     * @type {Array<LevelView>}
     * @memberof AffiliateSelf
     */
    levels?: Array<LevelView>;
    /**
     * Link is the shareable ?aff URL built from the code. Empty until a code is
     * minted, since there is nothing to share before approval.
     * @type {string}
     * @memberof AffiliateSelf
     */
    link?: string;
    /**
     * MarginBps is the platform gross-margin fraction, in basis points, that every
     * rate here is a rate OF. Read live per request, so it is the value in force
     * now, not the one that applied to commission already accrued.
     * @type {number}
     * @memberof AffiliateSelf
     */
    marginBps?: number;
    /**
     * PaidCents is lifetime commission already paid out, in cents — credits grants
     * and record-only cash disbursements alike.
     * @type {number}
     * @memberof AffiliateSelf
     */
    paidCents?: number;
    /**
     * Payouts is the payout history, newest first, bounded to the last 100 rows.
     * @type {Array<Remittance>}
     * @memberof AffiliateSelf
     */
    payouts?: Array<Remittance>;
    /**
     * PendingCents is accrued minus paid, in cents — what the platform still owes
     * and the ceiling on the next payout. Never negative.
     * @type {number}
     * @memberof AffiliateSelf
     */
    pendingCents?: number;
    /**
     * RateBps is the caller's OWN direct (level 1) commission rate, in basis points
     * of margin. Levels 2 and 3 are platform-wide and appear in `levels`.
     * @type {number}
     * @memberof AffiliateSelf
     */
    rateBps?: number;
    /**
     * Schedule is the rate schedule quoted to a caller that has not applied.
     * @type {Array<LevelView>}
     * @memberof AffiliateSelf
     */
    schedule?: Array<LevelView>;
    /**
     * Status is "applied", "approved" or "suspended"; absent for a caller that never
     * applied. Only "approved" mints links and accrues.
     * @type {string}
     * @memberof AffiliateSelf
     */
    status?: string;
}

/**
 * Check if a given object implements the AffiliateSelf interface.
 */
export function instanceOfAffiliateSelf(value: object): value is AffiliateSelf {
    return true;
}

export function AffiliateSelfFromJSON(json: any): AffiliateSelf {
    return AffiliateSelfFromJSONTyped(json, false);
}

export function AffiliateSelfFromJSONTyped(json: any, ignoreDiscriminator: boolean): AffiliateSelf {
    if (json == null) {
        return json;
    }
    return {
        
        'accruedCents': json['accruedCents'] == null ? undefined : json['accruedCents'],
        'code': json['code'] == null ? undefined : json['code'],
        'defaultRateBps': json['defaultRateBps'] == null ? undefined : json['defaultRateBps'],
        'downlineTotal': json['downlineTotal'] == null ? undefined : json['downlineTotal'],
        'handle': json['handle'] == null ? undefined : json['handle'],
        'id': json['id'] == null ? undefined : json['id'],
        'isAffiliate': json['isAffiliate'] == null ? undefined : json['isAffiliate'],
        'levels': json['levels'] == null ? undefined : ((json['levels'] as Array<any>).map(LevelViewFromJSON)),
        'link': json['link'] == null ? undefined : json['link'],
        'marginBps': json['marginBps'] == null ? undefined : json['marginBps'],
        'paidCents': json['paidCents'] == null ? undefined : json['paidCents'],
        'payouts': json['payouts'] == null ? undefined : ((json['payouts'] as Array<any>).map(RemittanceFromJSON)),
        'pendingCents': json['pendingCents'] == null ? undefined : json['pendingCents'],
        'rateBps': json['rateBps'] == null ? undefined : json['rateBps'],
        'schedule': json['schedule'] == null ? undefined : ((json['schedule'] as Array<any>).map(LevelViewFromJSON)),
        'status': json['status'] == null ? undefined : json['status'],
    };
}

export function AffiliateSelfToJSON(json: any): AffiliateSelf {
    return AffiliateSelfToJSONTyped(json, false);
}

export function AffiliateSelfToJSONTyped(value?: AffiliateSelf | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'accruedCents': value['accruedCents'],
        'code': value['code'],
        'defaultRateBps': value['defaultRateBps'],
        'downlineTotal': value['downlineTotal'],
        'handle': value['handle'],
        'id': value['id'],
        'isAffiliate': value['isAffiliate'],
        'levels': value['levels'] == null ? undefined : ((value['levels'] as Array<any>).map(LevelViewToJSON)),
        'link': value['link'],
        'marginBps': value['marginBps'],
        'paidCents': value['paidCents'],
        'payouts': value['payouts'] == null ? undefined : ((value['payouts'] as Array<any>).map(RemittanceToJSON)),
        'pendingCents': value['pendingCents'],
        'rateBps': value['rateBps'],
        'schedule': value['schedule'] == null ? undefined : ((value['schedule'] as Array<any>).map(LevelViewToJSON)),
        'status': value['status'],
    };
}

