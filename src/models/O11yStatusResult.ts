/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { O11yDeployment } from './O11yDeployment.js';
import {
    O11yDeploymentFromJSON,
    O11yDeploymentFromJSONTyped,
    O11yDeploymentToJSON,
    O11yDeploymentToJSONTyped,
} from './O11yDeployment.js';

/**
 * 
 * @export
 * @interface O11yStatusResult
 */
export interface O11yStatusResult {
    /**
     * CheckedAt is when this answer was measured, RFC3339 UTC.
     * @type {string}
     * @memberof O11yStatusResult
     */
    checkedAt?: string;
    /**
     * Deployments is the per-replica inventory behind the verdict. Empty means the
     * telemetry store reported none, not that the service runs on none.
     * @type {Array<O11yDeployment>}
     * @memberof O11yStatusResult
     */
    deployments?: Array<O11yDeployment>;
    /**
     * LatencyMs is the health probe's round trip in MILLISECONDS, time-boxed at two
     * seconds. It is 0 when no probe answered, which is not a fast service.
     * @type {number}
     * @memberof O11yStatusResult
     */
    latencyMs?: number;
    /**
     * Product is the service this answer is about, echoed back.
     * @type {string}
     * @memberof O11yStatusResult
     */
    product?: string;
    /**
     * Source is where the verdict came from: "probe" (we asked and it answered),
     * "datastore" (the probe did not answer and the replica inventory decided it),
     * "unreachable" (neither), or "unknown-service" for a well-formed product name
     * nothing backs — which is answered without probing, since dialling an
     * arbitrary host on a caller's say-so is the request forgery this refuses.
     * @type {string}
     * @memberof O11yStatusResult
     */
    source?: string;
    /**
     * Up is true when the health probe succeeded OR any replica reports up, so a
     * service reachable by either route reads up. Read Source to know which.
     * @type {boolean}
     * @memberof O11yStatusResult
     */
    up?: boolean;
}

/**
 * Check if a given object implements the O11yStatusResult interface.
 */
export function instanceOfO11yStatusResult(value: object): value is O11yStatusResult {
    return true;
}

export function O11yStatusResultFromJSON(json: any): O11yStatusResult {
    return O11yStatusResultFromJSONTyped(json, false);
}

export function O11yStatusResultFromJSONTyped(json: any, ignoreDiscriminator: boolean): O11yStatusResult {
    if (json == null) {
        return json;
    }
    return {
        
        'checkedAt': json['checkedAt'] == null ? undefined : json['checkedAt'],
        'deployments': json['deployments'] == null ? undefined : ((json['deployments'] as Array<any>).map(O11yDeploymentFromJSON)),
        'latencyMs': json['latencyMs'] == null ? undefined : json['latencyMs'],
        'product': json['product'] == null ? undefined : json['product'],
        'source': json['source'] == null ? undefined : json['source'],
        'up': json['up'] == null ? undefined : json['up'],
    };
}

export function O11yStatusResultToJSON(json: any): O11yStatusResult {
    return O11yStatusResultToJSONTyped(json, false);
}

export function O11yStatusResultToJSONTyped(value?: O11yStatusResult | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'checkedAt': value['checkedAt'],
        'deployments': value['deployments'] == null ? undefined : ((value['deployments'] as Array<any>).map(O11yDeploymentToJSON)),
        'latencyMs': value['latencyMs'],
        'product': value['product'],
        'source': value['source'],
        'up': value['up'],
    };
}

