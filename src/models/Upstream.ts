/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Backend } from './Backend.js';
import {
    BackendFromJSON,
    BackendFromJSONTyped,
    BackendToJSON,
    BackendToJSONTyped,
} from './Backend.js';

/**
 * 
 * @export
 * @interface Upstream
 */
export interface Upstream {
    /**
     * Backends are the upstream servers to balance across: 1..32 of them.
     * @type {Array<Backend>}
     * @memberof Upstream
     */
    backends?: Array<Backend>;
    /**
     * ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.
     * A create that omits it gets a generated one. Routes reference it by this id.
     * @type {string}
     * @memberof Upstream
     */
    id?: string;
    /**
     * PassHostHeader forwards the client's original Host header upstream instead
     * of rewriting it to the backend's.
     * @type {boolean}
     * @memberof Upstream
     */
    passHostHeader?: boolean;
}

/**
 * Check if a given object implements the Upstream interface.
 */
export function instanceOfUpstream(value: object): value is Upstream {
    return true;
}

export function UpstreamFromJSON(json: any): Upstream {
    return UpstreamFromJSONTyped(json, false);
}

export function UpstreamFromJSONTyped(json: any, ignoreDiscriminator: boolean): Upstream {
    if (json == null) {
        return json;
    }
    return {
        
        'backends': json['backends'] == null ? undefined : ((json['backends'] as Array<any>).map(BackendFromJSON)),
        'id': json['id'] == null ? undefined : json['id'],
        'passHostHeader': json['passHostHeader'] == null ? undefined : json['passHostHeader'],
    };
}

export function UpstreamToJSON(json: any): Upstream {
    return UpstreamToJSONTyped(json, false);
}

export function UpstreamToJSONTyped(value?: Upstream | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'backends': value['backends'] == null ? undefined : ((value['backends'] as Array<any>).map(BackendToJSON)),
        'id': value['id'],
        'passHostHeader': value['passHostHeader'],
    };
}

