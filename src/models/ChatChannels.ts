/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ChannelView } from './ChannelView.js';
import {
    ChannelViewFromJSON,
    ChannelViewFromJSONTyped,
    ChannelViewToJSON,
    ChannelViewToJSONTyped,
} from './ChannelView.js';

/**
 * 
 * @export
 * @interface ChatChannels
 */
export interface ChatChannels {
    /**
     * Channels is every chat transport this deployment supports, in a fixed
     * order, each carrying whether the org has connected it, the account behind
     * the connection, what the transport can do, the org's DM/group access
     * policies for it, and how many pairing requests are waiting.
     * @type {Array<ChannelView>}
     * @memberof ChatChannels
     */
    channels?: Array<ChannelView>;
}

/**
 * Check if a given object implements the ChatChannels interface.
 */
export function instanceOfChatChannels(value: object): value is ChatChannels {
    return true;
}

export function ChatChannelsFromJSON(json: any): ChatChannels {
    return ChatChannelsFromJSONTyped(json, false);
}

export function ChatChannelsFromJSONTyped(json: any, ignoreDiscriminator: boolean): ChatChannels {
    if (json == null) {
        return json;
    }
    return {
        
        'channels': json['channels'] == null ? undefined : ((json['channels'] as Array<any>).map(ChannelViewFromJSON)),
    };
}

export function ChatChannelsToJSON(json: any): ChatChannels {
    return ChatChannelsToJSONTyped(json, false);
}

export function ChatChannelsToJSONTyped(value?: ChatChannels | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'channels': value['channels'] == null ? undefined : ((value['channels'] as Array<any>).map(ChannelViewToJSON)),
    };
}

