/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Reach } from './Reach.js';
import {
    ReachFromJSON,
    ReachFromJSONTyped,
    ReachToJSON,
    ReachToJSONTyped,
} from './Reach.js';
import type { Pool } from './Pool.js';
import {
    PoolFromJSON,
    PoolFromJSONTyped,
    PoolToJSON,
    PoolToJSONTyped,
} from './Pool.js';

/**
 * 
 * @export
 * @interface Pools
 */
export interface Pools {
    /**
     * 
     * @type {string}
     * @memberof Pools
     */
    chain?: string;
    /**
     * Pools is `[]` where the chain has none and `null` where the read failed.
     * 
     * The two are different sentences and the wire says which: an empty ARRAY is the
     * indexer answering that nothing is deployed there, and `null` is nobody having
     * answered. `omitempty` would collapse both to an absent key — which is the
     * exact flattening the reach beside it exists to prevent, reintroduced one
     * struct tag lower down.
     * @type {Array<Pool>}
     * @memberof Pools
     */
    pools?: Array<Pool>;
    /**
     * 
     * @type {Reach}
     * @memberof Pools
     */
    reach?: Reach;
}

/**
 * Check if a given object implements the Pools interface.
 */
export function instanceOfPools(value: object): value is Pools {
    return true;
}

export function PoolsFromJSON(json: any): Pools {
    return PoolsFromJSONTyped(json, false);
}

export function PoolsFromJSONTyped(json: any, ignoreDiscriminator: boolean): Pools {
    if (json == null) {
        return json;
    }
    return {
        
        'chain': json['chain'] == null ? undefined : json['chain'],
        'pools': json['pools'] == null ? undefined : ((json['pools'] as Array<any>).map(PoolFromJSON)),
        'reach': json['reach'] == null ? undefined : ReachFromJSON(json['reach']),
    };
}

export function PoolsToJSON(json: any): Pools {
    return PoolsToJSONTyped(json, false);
}

export function PoolsToJSONTyped(value?: Pools | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'chain': value['chain'],
        'pools': value['pools'] == null ? undefined : ((value['pools'] as Array<any>).map(PoolToJSON)),
        'reach': ReachToJSON(value['reach']),
    };
}

