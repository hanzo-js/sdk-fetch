/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface PromptMeta
 */
export interface PromptMeta {
    /**
     * Labels is the creator's free-form taxonomy, stored as given after trimming and
     * de-duplication. Always present, `[]` when none — never null.
     * @type {Array<string>}
     * @memberof PromptMeta
     */
    labels?: Array<string>;
    /**
     * LastUpdatedAt is when the newest version was appended, RFC 3339 UTC. Empty
     * only if the record carries no timestamp at all.
     * @type {string}
     * @memberof PromptMeta
     */
    lastUpdatedAt?: string;
    /**
     * Name is the prompt's org-unique handle and the URL segment it is fetched by:
     * GET /v1/prompt/<name>.
     * @type {string}
     * @memberof PromptMeta
     */
    name?: string;
    /**
     * Tags is the second free-form taxonomy under the same rules as Labels. Nothing
     * in this service interprets either; they are yours to organize by.
     * @type {Array<string>}
     * @memberof PromptMeta
     */
    tags?: Array<string>;
    /**
     * Type labels the template's kind, "text" unless the creator said otherwise. It
     * is the CURRENT version's type; earlier versions may carry a different one.
     * @type {string}
     * @memberof PromptMeta
     */
    type?: string;
    /**
     * Versions lists every version NUMBER this prompt has, newest first, capped at
     * the last 100. The highest is the current one. (On a metrics row the same key
     * is a count, not a list.)
     * @type {Array<number>}
     * @memberof PromptMeta
     */
    versions?: Array<number>;
}

/**
 * Check if a given object implements the PromptMeta interface.
 */
export function instanceOfPromptMeta(value: object): value is PromptMeta {
    return true;
}

export function PromptMetaFromJSON(json: any): PromptMeta {
    return PromptMetaFromJSONTyped(json, false);
}

export function PromptMetaFromJSONTyped(json: any, ignoreDiscriminator: boolean): PromptMeta {
    if (json == null) {
        return json;
    }
    return {
        
        'labels': json['labels'] == null ? undefined : json['labels'],
        'lastUpdatedAt': json['lastUpdatedAt'] == null ? undefined : json['lastUpdatedAt'],
        'name': json['name'] == null ? undefined : json['name'],
        'tags': json['tags'] == null ? undefined : json['tags'],
        'type': json['type'] == null ? undefined : json['type'],
        'versions': json['versions'] == null ? undefined : json['versions'],
    };
}

export function PromptMetaToJSON(json: any): PromptMeta {
    return PromptMetaToJSONTyped(json, false);
}

export function PromptMetaToJSONTyped(value?: PromptMeta | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'labels': value['labels'],
        'lastUpdatedAt': value['lastUpdatedAt'],
        'name': value['name'],
        'tags': value['tags'],
        'type': value['type'],
        'versions': value['versions'],
    };
}

