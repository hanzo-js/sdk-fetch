/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ReferenceVersion } from './ReferenceVersion.js';
import {
    ReferenceVersionFromJSON,
    ReferenceVersionFromJSONTyped,
    ReferenceVersionToJSON,
    ReferenceVersionToJSONTyped,
} from './ReferenceVersion.js';
import type { ReferenceAnswer } from './ReferenceAnswer.js';
import {
    ReferenceAnswerFromJSON,
    ReferenceAnswerFromJSONTyped,
    ReferenceAnswerToJSON,
    ReferenceAnswerToJSONTyped,
} from './ReferenceAnswer.js';

/**
 * 
 * @export
 * @interface ResolveReferenceOut
 */
export interface ResolveReferenceOut {
    /**
     * Answers is one entry per (set, key) consulted.
     * @type {Array<ReferenceAnswer>}
     * @memberof ResolveReferenceOut
     */
    answers?: Array<ReferenceAnswer>;
    /**
     * Consulted names the version of every set that took part, so a decision can
     * record precisely what it leaned on. Record this with the decision: it is
     * what makes the decision reproducible a year later.
     * @type {Array<ReferenceVersion>}
     * @memberof ResolveReferenceOut
     */
    consulted?: Array<ReferenceVersion>;
    /**
     * Refused names the consulted sets that could not answer at all. A key that
     * missed in one of these is UNKNOWN, not clean.
     * @type {Array<string>}
     * @memberof ResolveReferenceOut
     */
    refused?: Array<string>;
    /**
     * Stale names the consulted sets past their freshness bound. Staleness is
     * itself a risk signal — a decision taken against a three-week-old list is a
     * weaker decision, and this is how it knows.
     * @type {Array<string>}
     * @memberof ResolveReferenceOut
     */
    stale?: Array<string>;
}

/**
 * Check if a given object implements the ResolveReferenceOut interface.
 */
export function instanceOfResolveReferenceOut(value: object): value is ResolveReferenceOut {
    return true;
}

export function ResolveReferenceOutFromJSON(json: any): ResolveReferenceOut {
    return ResolveReferenceOutFromJSONTyped(json, false);
}

export function ResolveReferenceOutFromJSONTyped(json: any, ignoreDiscriminator: boolean): ResolveReferenceOut {
    if (json == null) {
        return json;
    }
    return {
        
        'answers': json['answers'] == null ? undefined : ((json['answers'] as Array<any>).map(ReferenceAnswerFromJSON)),
        'consulted': json['consulted'] == null ? undefined : ((json['consulted'] as Array<any>).map(ReferenceVersionFromJSON)),
        'refused': json['refused'] == null ? undefined : json['refused'],
        'stale': json['stale'] == null ? undefined : json['stale'],
    };
}

export function ResolveReferenceOutToJSON(json: any): ResolveReferenceOut {
    return ResolveReferenceOutToJSONTyped(json, false);
}

export function ResolveReferenceOutToJSONTyped(value?: ResolveReferenceOut | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'answers': value['answers'] == null ? undefined : ((value['answers'] as Array<any>).map(ReferenceAnswerToJSON)),
        'consulted': value['consulted'] == null ? undefined : ((value['consulted'] as Array<any>).map(ReferenceVersionToJSON)),
        'refused': value['refused'],
        'stale': value['stale'],
    };
}

