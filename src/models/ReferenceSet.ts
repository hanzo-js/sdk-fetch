/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ReferenceSource } from './ReferenceSource.js';
import {
    ReferenceSourceFromJSON,
    ReferenceSourceFromJSONTyped,
    ReferenceSourceToJSON,
    ReferenceSourceToJSONTyped,
} from './ReferenceSource.js';

/**
 * 
 * @export
 * @interface ReferenceSet
 */
export interface ReferenceSet {
    /**
     * Age is how long ago that was.
     * @type {string}
     * @memberof ReferenceSet
     */
    age?: string;
    /**
     * AsOf is when the OLDEST contributing publisher was current, RFC 3339. The
     * oldest and not the newest: a set is exactly as fresh as its weakest source.
     * @type {string}
     * @memberof ReferenceSet
     */
    asOf?: string;
    /**
     * Keys is how many members the baseline carries.
     * @type {number}
     * @memberof ReferenceSet
     */
    keys?: number;
    /**
     * Kind is how the baseline comes to exist: fetch (downloaded from a
     * publisher), local (computed here), attest (held by the component that
     * screens against it, freshness reported), or seam (declared and NOT held,
     * because the source needs a licence we do not have).
     * @type {string}
     * @memberof ReferenceSet
     */
    kind?: string;
    /**
     * Match is how a key is tested: exact, domain, net, digits, pattern or range.
     * @type {string}
     * @memberof ReferenceSet
     */
    match?: string;
    /**
     * MaxAge is how old this set may be before it is stale.
     * @type {string}
     * @memberof ReferenceSet
     */
    maxAge?: string;
    /**
     * Overrides is how many entries YOUR org has laid over this baseline.
     * @type {number}
     * @memberof ReferenceSet
     */
    overrides?: number;
    /**
     * Refusal names why the set cannot be relied on, when it cannot: never
     * loaded, held elsewhere, or a licence we do not hold. Non-empty means a
     * lookup against this set will not answer, rather than answering clean.
     * @type {string}
     * @memberof ReferenceSet
     */
    refusal?: string;
    /**
     * Set is the name this set is addressed by.
     * @type {string}
     * @memberof ReferenceSet
     */
    set?: string;
    /**
     * Sources is each contributing publisher, its licence and its own freshness.
     * @type {Array<ReferenceSource>}
     * @memberof ReferenceSet
     */
    sources?: Array<ReferenceSource>;
    /**
     * Stale is whether it is past that bound. A stale set still answers and says
     * so, because yesterday's list beats none.
     * @type {boolean}
     * @memberof ReferenceSet
     */
    stale?: boolean;
    /**
     * Version is the exact baseline consulted — every contributing publisher and
     * its content digest. A decision records this and an auditor resolves it back.
     * @type {string}
     * @memberof ReferenceSet
     */
    version?: string;
    /**
     * What the set holds, in one sentence.
     * @type {string}
     * @memberof ReferenceSet
     */
    what?: string;
}

/**
 * Check if a given object implements the ReferenceSet interface.
 */
export function instanceOfReferenceSet(value: object): value is ReferenceSet {
    return true;
}

export function ReferenceSetFromJSON(json: any): ReferenceSet {
    return ReferenceSetFromJSONTyped(json, false);
}

export function ReferenceSetFromJSONTyped(json: any, ignoreDiscriminator: boolean): ReferenceSet {
    if (json == null) {
        return json;
    }
    return {
        
        'age': json['age'] == null ? undefined : json['age'],
        'asOf': json['asOf'] == null ? undefined : json['asOf'],
        'keys': json['keys'] == null ? undefined : json['keys'],
        'kind': json['kind'] == null ? undefined : json['kind'],
        'match': json['match'] == null ? undefined : json['match'],
        'maxAge': json['maxAge'] == null ? undefined : json['maxAge'],
        'overrides': json['overrides'] == null ? undefined : json['overrides'],
        'refusal': json['refusal'] == null ? undefined : json['refusal'],
        'set': json['set'] == null ? undefined : json['set'],
        'sources': json['sources'] == null ? undefined : ((json['sources'] as Array<any>).map(ReferenceSourceFromJSON)),
        'stale': json['stale'] == null ? undefined : json['stale'],
        'version': json['version'] == null ? undefined : json['version'],
        'what': json['what'] == null ? undefined : json['what'],
    };
}

export function ReferenceSetToJSON(json: any): ReferenceSet {
    return ReferenceSetToJSONTyped(json, false);
}

export function ReferenceSetToJSONTyped(value?: ReferenceSet | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'age': value['age'],
        'asOf': value['asOf'],
        'keys': value['keys'],
        'kind': value['kind'],
        'match': value['match'],
        'maxAge': value['maxAge'],
        'overrides': value['overrides'],
        'refusal': value['refusal'],
        'set': value['set'],
        'sources': value['sources'] == null ? undefined : ((value['sources'] as Array<any>).map(ReferenceSourceToJSON)),
        'stale': value['stale'],
        'version': value['version'],
        'what': value['what'],
    };
}

