/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Hit } from './Hit.js';
import {
    HitFromJSON,
    HitFromJSONTyped,
    HitToJSON,
    HitToJSONTyped,
} from './Hit.js';
import type { BackendStatus } from './BackendStatus.js';
import {
    BackendStatusFromJSON,
    BackendStatusFromJSONTyped,
    BackendStatusToJSON,
    BackendStatusToJSONTyped,
} from './BackendStatus.js';

/**
 * 
 * @export
 * @interface Fusion
 */
export interface Fusion {
    /**
     * Backends is the per-leg report. Always populated.
     * @type {Array<BackendStatus>}
     * @memberof Fusion
     */
    backends?: Array<BackendStatus>;
    /**
     * Hits is the fused, ranked result set.
     * @type {Array<Hit>}
     * @memberof Fusion
     */
    hits?: Array<Hit>;
    /**
     * Mode is the mode actually used after `auto` resolution.
     * @type {string}
     * @memberof Fusion
     */
    mode?: string;
    /**
     * Status is the query's overall honesty signal:
     *   ok          every consulted leg answered.
     *   partial     at least one leg failed; Hits holds the survivors' results.
     *   unavailable every consulted leg failed; Hits is empty AND that is stated.
     * @type {string}
     * @memberof Fusion
     */
    status?: string;
    /**
     * TookMS is the whole query's wall time in milliseconds — every leg it
     * consulted, plus fusion and paging. Each leg's own share is in
     * Backends[].TookMS; the legs run in sequence, so this is at least their sum.
     * @type {number}
     * @memberof Fusion
     */
    tookMs?: number;
}

/**
 * Check if a given object implements the Fusion interface.
 */
export function instanceOfFusion(value: object): value is Fusion {
    return true;
}

export function FusionFromJSON(json: any): Fusion {
    return FusionFromJSONTyped(json, false);
}

export function FusionFromJSONTyped(json: any, ignoreDiscriminator: boolean): Fusion {
    if (json == null) {
        return json;
    }
    return {
        
        'backends': json['backends'] == null ? undefined : ((json['backends'] as Array<any>).map(BackendStatusFromJSON)),
        'hits': json['hits'] == null ? undefined : ((json['hits'] as Array<any>).map(HitFromJSON)),
        'mode': json['mode'] == null ? undefined : json['mode'],
        'status': json['status'] == null ? undefined : json['status'],
        'tookMs': json['took_ms'] == null ? undefined : json['took_ms'],
    };
}

export function FusionToJSON(json: any): Fusion {
    return FusionToJSONTyped(json, false);
}

export function FusionToJSONTyped(value?: Fusion | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'backends': value['backends'] == null ? undefined : ((value['backends'] as Array<any>).map(BackendStatusToJSON)),
        'hits': value['hits'] == null ? undefined : ((value['hits'] as Array<any>).map(HitToJSON)),
        'mode': value['mode'],
        'status': value['status'],
        'took_ms': value['tookMs'],
    };
}

