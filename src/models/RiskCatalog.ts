/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { RiskOrgFeature } from './RiskOrgFeature.js';
import {
    RiskOrgFeatureFromJSON,
    RiskOrgFeatureFromJSONTyped,
    RiskOrgFeatureToJSON,
    RiskOrgFeatureToJSONTyped,
} from './RiskOrgFeature.js';
import type { RiskModelFeature } from './RiskModelFeature.js';
import {
    RiskModelFeatureFromJSON,
    RiskModelFeatureFromJSONTyped,
    RiskModelFeatureToJSON,
    RiskModelFeatureToJSONTyped,
} from './RiskModelFeature.js';
import type { RiskBand } from './RiskBand.js';
import {
    RiskBandFromJSON,
    RiskBandFromJSONTyped,
    RiskBandToJSON,
    RiskBandToJSONTyped,
} from './RiskBand.js';

/**
 * 
 * @export
 * @interface RiskCatalog
 */
export interface RiskCatalog {
    /**
     * Gap says why a lens could not be measured, when that is the case. Each
     * reason names its own lens, because "the surface is unreadable" and "the
     * network baseline is unreadable" are different facts.
     * @type {string}
     * @memberof RiskCatalog
     */
    gap?: string;
    /**
     * Model is the governed inventory: one entry per dimension of the model
     * space, each carrying the typology it serves and the published standard that
     * asks for it. It is the same for every organisation, because it is the
     * model's shape.
     * @type {Array<RiskModelFeature>}
     * @memberof RiskCatalog
     */
    model?: Array<RiskModelFeature>;
    /**
     * Network is the published cross-organisation baseline over the same window,
     * so the surface above has something to be read AGAINST. It is the same for
     * every caller and it names nobody.
     * 
     * It carries no tenant and cannot be made to: the table it reads has no org
     * column, every figure is a quantile over at least kAnonOrgs organisations
     * weighted one vote each, and a band that does not meet that floor is dropped
     * on the way out.
     * @type {Array<RiskBand>}
     * @memberof RiskCatalog
     */
    network?: Array<RiskBand>;
    /**
     * Surface is what this organisation's own event surface carries, per
     * dimension, measured over the window. A dimension present in no bucket is
     * blind here — the model reads its neutral value and a reviewer has to be able
     * to see that.
     * @type {Array<RiskOrgFeature>}
     * @memberof RiskCatalog
     */
    surface?: Array<RiskOrgFeature>;
    /**
     * Tenant is whose surface was measured.
     * @type {string}
     * @memberof RiskCatalog
     */
    tenant?: string;
}

/**
 * Check if a given object implements the RiskCatalog interface.
 */
export function instanceOfRiskCatalog(value: object): value is RiskCatalog {
    return true;
}

export function RiskCatalogFromJSON(json: any): RiskCatalog {
    return RiskCatalogFromJSONTyped(json, false);
}

export function RiskCatalogFromJSONTyped(json: any, ignoreDiscriminator: boolean): RiskCatalog {
    if (json == null) {
        return json;
    }
    return {
        
        'gap': json['gap'] == null ? undefined : json['gap'],
        'model': json['model'] == null ? undefined : ((json['model'] as Array<any>).map(RiskModelFeatureFromJSON)),
        'network': json['network'] == null ? undefined : ((json['network'] as Array<any>).map(RiskBandFromJSON)),
        'surface': json['surface'] == null ? undefined : ((json['surface'] as Array<any>).map(RiskOrgFeatureFromJSON)),
        'tenant': json['tenant'] == null ? undefined : json['tenant'],
    };
}

export function RiskCatalogToJSON(json: any): RiskCatalog {
    return RiskCatalogToJSONTyped(json, false);
}

export function RiskCatalogToJSONTyped(value?: RiskCatalog | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'gap': value['gap'],
        'model': value['model'] == null ? undefined : ((value['model'] as Array<any>).map(RiskModelFeatureToJSON)),
        'network': value['network'] == null ? undefined : ((value['network'] as Array<any>).map(RiskBandToJSON)),
        'surface': value['surface'] == null ? undefined : ((value['surface'] as Array<any>).map(RiskOrgFeatureToJSON)),
        'tenant': value['tenant'],
    };
}

