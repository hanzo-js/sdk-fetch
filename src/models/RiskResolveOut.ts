/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { RiskResolved } from './RiskResolved.js';
import {
    RiskResolvedFromJSON,
    RiskResolvedFromJSONTyped,
    RiskResolvedToJSON,
    RiskResolvedToJSONTyped,
} from './RiskResolved.js';

/**
 * 
 * @export
 * @interface RiskResolveOut
 */
export interface RiskResolveOut {
    /**
     * Horizon is the maturity horizon this answer was computed under, IN DAYS — the
     * caller's, or 120 when it stated none. Each event's as-of is its own `at` plus
     * this many days, and that as-of is what decides which assertions were visible
     * to it; an event whose as-of falls after Now is not resolved at all and is
     * counted in Unmatured instead.
     * @type {number}
     * @memberof RiskResolveOut
     */
    horizon?: number;
    /**
     * Labels is one entry per named event that BOTH matured and had at least one
     * assertion knowable by its own as-of, in the order the events were named. The
     * three outcomes partition the ask: len(labels) + Unmatured + Unlabelled is the
     * number of DISTINCT events named, an event named twice having been answered
     * once.
     * @type {Array<RiskResolved>}
     * @memberof RiskResolveOut
     */
    labels?: Array<RiskResolved>;
    /**
     * Now and Horizon echo the observation this answer was computed under. A
     * resolved label without them is a claim nobody can check.
     * @type {string}
     * @memberof RiskResolveOut
     */
    now?: string;
    /**
     * Unlabelled is how many matured events had no assertion knowable by their
     * own as-of. That is the ordinary state of most traffic and it is reported
     * rather than answered as unproductive: manufacturing negatives is how a
     * fraud model comes to describe the incumbent block list.
     * @type {number}
     * @memberof RiskResolveOut
     */
    unlabelled?: number;
    /**
     * Unmatured is how many named events had not aged past the horizon. They are
     * not unlabelled — they are not yet ASKABLE, and a supervised training set
     * must exclude them rather than treat them as negatives.
     * @type {number}
     * @memberof RiskResolveOut
     */
    unmatured?: number;
}

/**
 * Check if a given object implements the RiskResolveOut interface.
 */
export function instanceOfRiskResolveOut(value: object): value is RiskResolveOut {
    return true;
}

export function RiskResolveOutFromJSON(json: any): RiskResolveOut {
    return RiskResolveOutFromJSONTyped(json, false);
}

export function RiskResolveOutFromJSONTyped(json: any, ignoreDiscriminator: boolean): RiskResolveOut {
    if (json == null) {
        return json;
    }
    return {
        
        'horizon': json['horizon'] == null ? undefined : json['horizon'],
        'labels': json['labels'] == null ? undefined : ((json['labels'] as Array<any>).map(RiskResolvedFromJSON)),
        'now': json['now'] == null ? undefined : json['now'],
        'unlabelled': json['unlabelled'] == null ? undefined : json['unlabelled'],
        'unmatured': json['unmatured'] == null ? undefined : json['unmatured'],
    };
}

export function RiskResolveOutToJSON(json: any): RiskResolveOut {
    return RiskResolveOutToJSONTyped(json, false);
}

export function RiskResolveOutToJSONTyped(value?: RiskResolveOut | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'horizon': value['horizon'],
        'labels': value['labels'] == null ? undefined : ((value['labels'] as Array<any>).map(RiskResolvedToJSON)),
        'now': value['now'],
        'unlabelled': value['unlabelled'],
        'unmatured': value['unmatured'],
    };
}

