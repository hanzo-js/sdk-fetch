/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { RiskDatasetSpec } from './RiskDatasetSpec.js';
import {
    RiskDatasetSpecFromJSON,
    RiskDatasetSpecFromJSONTyped,
    RiskDatasetSpecToJSON,
    RiskDatasetSpecToJSONTyped,
} from './RiskDatasetSpec.js';
import type { RiskSplitCounts } from './RiskSplitCounts.js';
import {
    RiskSplitCountsFromJSON,
    RiskSplitCountsFromJSONTyped,
    RiskSplitCountsToJSON,
    RiskSplitCountsToJSONTyped,
} from './RiskSplitCounts.js';

/**
 * 
 * @export
 * @interface RiskDataset
 */
export interface RiskDataset {
    /**
     * At is when this version last changed state, and By who.
     * @type {string}
     * @memberof RiskDataset
     */
    at?: string;
    /**
     * 
     * @type {string}
     * @memberof RiskDataset
     */
    by?: string;
    /**
     * Counts is how the rows fall across the splits.
     * @type {RiskSplitCounts}
     * @memberof RiskDataset
     */
    counts?: RiskSplitCounts;
    /**
     * Digest fingerprints the SPEC and the ROWS together. Two materialisations of
     * one spec agree on it or the plane says they do not.
     * @type {string}
     * @memberof RiskDataset
     */
    digest?: string;
    /**
     * Name and Version identify the version.
     * @type {string}
     * @memberof RiskDataset
     */
    name?: string;
    /**
     * Oversize is how many of the window's subjects this version could NOT carry
     * because their subject identity exceeds the plane's per-subject byte bound.
     * 
     * It is on the wire, not only in a log, because it is the one degradation a
     * caller cannot otherwise detect: the rows that are here look complete, and a
     * dataset silently missing a population is a model silently blind to it.
     * Non-zero does not make a version invalid — it makes it a version whose
     * coverage is STATED. Zero is the normal case and omits.
     * @type {number}
     * @memberof RiskDataset
     */
    oversize?: number;
    /**
     * Refusal names why there are no bytes, when there are none.
     * @type {string}
     * @memberof RiskDataset
     */
    refusal?: string;
    /**
     * Running is true while THIS process is materialising the version. A version
     * that is `materializing` and not running was started by a process that is
     * gone — two states the register cannot tell apart, because a register cannot
     * know which processes are alive.
     * @type {boolean}
     * @memberof RiskDataset
     */
    running?: boolean;
    /**
     * Share is the fraction of the window's subjects admitted, in thousandths.
     * 1000 means the whole window fitted under the cap; anything less means the
     * version is a reproducible sample and says by how much.
     * @type {number}
     * @memberof RiskDataset
     */
    share?: number;
    /**
     * Spec is the bound query this version was built from, exactly as recorded.
     * @type {RiskDatasetSpec}
     * @memberof RiskDataset
     */
    spec?: RiskDatasetSpec;
    /**
     * Status is declared, materializing, ready or refused. Only `ready` has bytes,
     * and `ready` is terminal: a published version is never rewritten.
     * @type {string}
     * @memberof RiskDataset
     */
    status?: string;
    /**
     * Truncated is true when the row cap bound before the window ran out. The
     * trailing subject is dropped whole when that happens, because half a subject
     * on one side of a split is exactly the leak the grouping prevents.
     * @type {boolean}
     * @memberof RiskDataset
     */
    truncated?: boolean;
    /**
     * 
     * @type {number}
     * @memberof RiskDataset
     */
    version?: number;
}

/**
 * Check if a given object implements the RiskDataset interface.
 */
export function instanceOfRiskDataset(value: object): value is RiskDataset {
    return true;
}

export function RiskDatasetFromJSON(json: any): RiskDataset {
    return RiskDatasetFromJSONTyped(json, false);
}

export function RiskDatasetFromJSONTyped(json: any, ignoreDiscriminator: boolean): RiskDataset {
    if (json == null) {
        return json;
    }
    return {
        
        'at': json['at'] == null ? undefined : json['at'],
        'by': json['by'] == null ? undefined : json['by'],
        'counts': json['counts'] == null ? undefined : RiskSplitCountsFromJSON(json['counts']),
        'digest': json['digest'] == null ? undefined : json['digest'],
        'name': json['name'] == null ? undefined : json['name'],
        'oversize': json['oversize'] == null ? undefined : json['oversize'],
        'refusal': json['refusal'] == null ? undefined : json['refusal'],
        'running': json['running'] == null ? undefined : json['running'],
        'share': json['share'] == null ? undefined : json['share'],
        'spec': json['spec'] == null ? undefined : RiskDatasetSpecFromJSON(json['spec']),
        'status': json['status'] == null ? undefined : json['status'],
        'truncated': json['truncated'] == null ? undefined : json['truncated'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function RiskDatasetToJSON(json: any): RiskDataset {
    return RiskDatasetToJSONTyped(json, false);
}

export function RiskDatasetToJSONTyped(value?: RiskDataset | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'at': value['at'],
        'by': value['by'],
        'counts': RiskSplitCountsToJSON(value['counts']),
        'digest': value['digest'],
        'name': value['name'],
        'oversize': value['oversize'],
        'refusal': value['refusal'],
        'running': value['running'],
        'share': value['share'],
        'spec': RiskDatasetSpecToJSON(value['spec']),
        'status': value['status'],
        'truncated': value['truncated'],
        'version': value['version'],
    };
}

