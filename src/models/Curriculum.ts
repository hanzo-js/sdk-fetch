/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { JourneyStep } from './JourneyStep.js';
import {
    JourneyStepFromJSON,
    JourneyStepFromJSONTyped,
    JourneyStepToJSON,
    JourneyStepToJSONTyped,
} from './JourneyStep.js';

/**
 * 
 * @export
 * @interface Curriculum
 */
export interface Curriculum {
    /**
     * Steps are the enabled steps in authoring order. Order is the tiebreak the
     * next-step logic walks, so it is part of the contract rather than cosmetic.
     * @type {Array<JourneyStep>}
     * @memberof Curriculum
     */
    steps?: Array<JourneyStep>;
    /**
     * Title is the playbook's name as it heads the checklist.
     * @type {string}
     * @memberof Curriculum
     */
    title?: string;
    /**
     * Version identifies the authored playbook this journey was projected from, so
     * two orgs on different playbooks can be told apart. It is the blueprint's own
     * `version` string, not the store's numeric revision.
     * @type {string}
     * @memberof Curriculum
     */
    version?: string;
}

/**
 * Check if a given object implements the Curriculum interface.
 */
export function instanceOfCurriculum(value: object): value is Curriculum {
    return true;
}

export function CurriculumFromJSON(json: any): Curriculum {
    return CurriculumFromJSONTyped(json, false);
}

export function CurriculumFromJSONTyped(json: any, ignoreDiscriminator: boolean): Curriculum {
    if (json == null) {
        return json;
    }
    return {
        
        'steps': json['steps'] == null ? undefined : ((json['steps'] as Array<any>).map(JourneyStepFromJSON)),
        'title': json['title'] == null ? undefined : json['title'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function CurriculumToJSON(json: any): Curriculum {
    return CurriculumToJSONTyped(json, false);
}

export function CurriculumToJSONTyped(value?: Curriculum | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'steps': value['steps'] == null ? undefined : ((value['steps'] as Array<any>).map(JourneyStepToJSON)),
        'title': value['title'],
        'version': value['version'],
    };
}

