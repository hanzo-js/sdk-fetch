/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Endpoint } from './Endpoint.js';
import {
    EndpointFromJSON,
    EndpointFromJSONTyped,
    EndpointToJSON,
    EndpointToJSONTyped,
} from './Endpoint.js';

/**
 * 
 * @export
 * @interface EndpointList
 */
export interface EndpointList {
    /**
     * Data is the org's endpoints, newest first, each with its signing secret
     * REDACTED — the secret leaves the server only on create and on rotate.
     * @type {Array<Endpoint>}
     * @memberof EndpointList
     */
    data?: Array<Endpoint>;
}

/**
 * Check if a given object implements the EndpointList interface.
 */
export function instanceOfEndpointList(value: object): value is EndpointList {
    return true;
}

export function EndpointListFromJSON(json: any): EndpointList {
    return EndpointListFromJSONTyped(json, false);
}

export function EndpointListFromJSONTyped(json: any, ignoreDiscriminator: boolean): EndpointList {
    if (json == null) {
        return json;
    }
    return {
        
        'data': json['data'] == null ? undefined : ((json['data'] as Array<any>).map(EndpointFromJSON)),
    };
}

export function EndpointListToJSON(json: any): EndpointList {
    return EndpointListToJSONTyped(json, false);
}

export function EndpointListToJSONTyped(value?: EndpointList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'data': value['data'] == null ? undefined : ((value['data'] as Array<any>).map(EndpointToJSON)),
    };
}

