/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { AppliedResource } from './AppliedResource.js';
import {
    AppliedResourceFromJSON,
    AppliedResourceFromJSONTyped,
    AppliedResourceToJSON,
    AppliedResourceToJSONTyped,
} from './AppliedResource.js';
import type { ReconcileSource } from './ReconcileSource.js';
import {
    ReconcileSourceFromJSON,
    ReconcileSourceFromJSONTyped,
    ReconcileSourceToJSON,
    ReconcileSourceToJSONTyped,
} from './ReconcileSource.js';

/**
 * 
 * @export
 * @interface ReconcileReport
 */
export interface ReconcileReport {
    /**
     * Declared is how many objects the rendered source declares — the denominator
     * the three outcome counts below are read against. Zero means the render
     * produced nothing, which trips the prune fuse rather than sweeping the fleet.
     * @type {number}
     * @memberof ReconcileReport
     */
    declared?: number;
    /**
     * Failed is how many objects the apply could not reconcile. Non-zero is a
     * PARTIAL run reported at 200: the engine applied what it could and each
     * failure names itself in Results, so a caller reads this number rather than
     * the status code to learn whether the fleet matches the source.
     * @type {number}
     * @memberof ReconcileReport
     */
    failed?: number;
    /**
     * Instance is the tracking id this run stamps on everything it manages, so a
     * later run can tell the objects it owns from objects another instance
     * declares. DEPLOY_ENGINE_INSTANCE names it; the default is `universe`.
     * @type {string}
     * @memberof ReconcileReport
     */
    instance?: string;
    /**
     * Prune reports whether DELETION was enabled for this run. False means an
     * object the source no longer declares was left alone rather than removed, so
     * a zero Pruned below means "nothing to delete" only when this is true.
     * @type {boolean}
     * @memberof ReconcileReport
     */
    prune?: boolean;
    /**
     * Pruned is how many live objects this run DELETED because the source no
     * longer declares them. Always 0 when Prune is false.
     * @type {number}
     * @memberof ReconcileReport
     */
    pruned?: number;
    /**
     * Results is one entry per object the run acted on, in the order the engine
     * applied them. Empty (never null) when the run reconciled nothing.
     * @type {Array<AppliedResource>}
     * @memberof ReconcileReport
     */
    results?: Array<AppliedResource>;
    /**
     * Revision is the source commit this run applied, as the source resolved it —
     * a git commit SHA, not an image tag. It is what an operator cites when asking
     * what the cluster was last made to match.
     * @type {string}
     * @memberof ReconcileReport
     */
    revision?: string;
    /**
     * Source is the git coordinate the run rendered. It is this deployment's own
     * configuration echoed back, never a request parameter, and it is reported so
     * a reader of the answer knows WHICH tree the revision names.
     * @type {ReconcileSource}
     * @memberof ReconcileReport
     */
    source?: ReconcileSource;
    /**
     * Synced is how many objects the run applied successfully.
     * @type {number}
     * @memberof ReconcileReport
     */
    synced?: number;
}

/**
 * Check if a given object implements the ReconcileReport interface.
 */
export function instanceOfReconcileReport(value: object): value is ReconcileReport {
    return true;
}

export function ReconcileReportFromJSON(json: any): ReconcileReport {
    return ReconcileReportFromJSONTyped(json, false);
}

export function ReconcileReportFromJSONTyped(json: any, ignoreDiscriminator: boolean): ReconcileReport {
    if (json == null) {
        return json;
    }
    return {
        
        'declared': json['declared'] == null ? undefined : json['declared'],
        'failed': json['failed'] == null ? undefined : json['failed'],
        'instance': json['instance'] == null ? undefined : json['instance'],
        'prune': json['prune'] == null ? undefined : json['prune'],
        'pruned': json['pruned'] == null ? undefined : json['pruned'],
        'results': json['results'] == null ? undefined : ((json['results'] as Array<any>).map(AppliedResourceFromJSON)),
        'revision': json['revision'] == null ? undefined : json['revision'],
        'source': json['source'] == null ? undefined : ReconcileSourceFromJSON(json['source']),
        'synced': json['synced'] == null ? undefined : json['synced'],
    };
}

export function ReconcileReportToJSON(json: any): ReconcileReport {
    return ReconcileReportToJSONTyped(json, false);
}

export function ReconcileReportToJSONTyped(value?: ReconcileReport | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'declared': value['declared'],
        'failed': value['failed'],
        'instance': value['instance'],
        'prune': value['prune'],
        'pruned': value['pruned'],
        'results': value['results'] == null ? undefined : ((value['results'] as Array<any>).map(AppliedResourceToJSON)),
        'revision': value['revision'],
        'source': ReconcileSourceToJSON(value['source']),
        'synced': value['synced'],
    };
}

