/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface AuthoredPlugin
 */
export interface AuthoredPlugin {
    /**
     * CreatedAt is when the plugin was last built, Unix seconds.
     * @type {number}
     * @memberof AuthoredPlugin
     */
    createdAt?: number;
    /**
     * ID is the plugin's id within the org, and the id a delete addresses.
     * @type {string}
     * @memberof AuthoredPlugin
     */
    id?: string;
    /**
     * Name is the plugin's name: one lowercase path segment, the id it runs by.
     * @type {string}
     * @memberof AuthoredPlugin
     */
    name?: string;
    /**
     * Org is the org that built the plugin — the validated caller's.
     * @type {string}
     * @memberof AuthoredPlugin
     */
    org?: string;
    /**
     * Provider is the connectors provider whose credential this plugin uses at
     * run time. Absent for a plugin that needs none. The credential itself is
     * never here — it stays under KMS custody in the connectors plane.
     * @type {string}
     * @memberof AuthoredPlugin
     */
    provider?: string;
    /**
     * Source is the TypeScript as authored (or as generated from a spec).
     * @type {string}
     * @memberof AuthoredPlugin
     */
    source?: string;
}

/**
 * Check if a given object implements the AuthoredPlugin interface.
 */
export function instanceOfAuthoredPlugin(value: object): value is AuthoredPlugin {
    return true;
}

export function AuthoredPluginFromJSON(json: any): AuthoredPlugin {
    return AuthoredPluginFromJSONTyped(json, false);
}

export function AuthoredPluginFromJSONTyped(json: any, ignoreDiscriminator: boolean): AuthoredPlugin {
    if (json == null) {
        return json;
    }
    return {
        
        'createdAt': json['createdAt'] == null ? undefined : json['createdAt'],
        'id': json['id'] == null ? undefined : json['id'],
        'name': json['name'] == null ? undefined : json['name'],
        'org': json['org'] == null ? undefined : json['org'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'source': json['source'] == null ? undefined : json['source'],
    };
}

export function AuthoredPluginToJSON(json: any): AuthoredPlugin {
    return AuthoredPluginToJSONTyped(json, false);
}

export function AuthoredPluginToJSONTyped(value?: AuthoredPlugin | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'createdAt': value['createdAt'],
        'id': value['id'],
        'name': value['name'],
        'org': value['org'],
        'provider': value['provider'],
        'source': value['source'],
    };
}

