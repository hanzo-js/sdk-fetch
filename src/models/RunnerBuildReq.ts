/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { BinarySpec } from './BinarySpec.js';
import {
    BinarySpecFromJSON,
    BinarySpecFromJSONTyped,
    BinarySpecToJSON,
    BinarySpecToJSONTyped,
} from './BinarySpec.js';

/**
 * 
 * @export
 * @interface RunnerBuildReq
 */
export interface RunnerBuildReq {
    /**
     * Arch is the target architecture for the artifact lane.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    arch?: string;
    /**
     * Args are --build-arg values. They are what lets several images off ONE
     * Dockerfile mean different things — the sandbox classes are three entries
     * differing only by STAGE. Validated at the k8s choke point, with VERSION and
     * REVISION taking precedence: those are receipts the builder derives from the
     * tag and the commit, and a caller that could overwrite them could make an
     * image lie about which commit it is.
     * @type {{ [key: string]: string; }}
     * @memberof RunnerBuildReq
     */
    args?: { [key: string]: string; };
    /**
     * Binaries selects the ARTIFACT lane (artifact.go): build what the repo's
     * hanzo.yml `binaries:` block declares — a Go binary, an npm tarball, a Rust
     * binary — and publish it to hanzoai/s3 instead of pushing an image. It is the
     * same recipe hanzoai/ci reads, sent verbatim, so `image` is meaningless here
     * and must be absent.
     * @type {Array<BinarySpec>}
     * @memberof RunnerBuildReq
     */
    binaries?: Array<BinarySpec>;
    /**
     * Branch is the branch to build when no SHA or Ref is given.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    branch?: string;
    /**
     * Bucket mirrors hanzo.yml's `bucket:` — where the artifact lane publishes.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    bucket?: string;
    /**
     * Context is the build context path within the repo.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    context?: string;
    /**
     * DockerTarget is the multi-stage build target to stop at.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    dockerTarget?: string;
    /**
     * Dockerfile is the path to build from; empty uses the zero-config frontend.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    dockerfile?: string;
    /**
     * Image is the output image ref to push. Required on the image lane, and it
     * must target a registry namespace the caller's org owns.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    image?: string;
    /**
     * OS is the target operating system for the artifact lane.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    os?: string;
    /**
     * Ref is the git ref to build when no SHA is given.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    ref?: string;
    /**
     * Repo is the repository clone URL to build. Required on the image lane.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    repo?: string;
    /**
     * SHA is the commit to pin; it wins over Ref and Branch.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    sha?: string;
    /**
     * Tag is the publish path segment, so both entry points write ONE index at ONE
     * URL. It defaults to the pinned ref, and must be named explicitly for a
     * branch.
     * @type {string}
     * @memberof RunnerBuildReq
     */
    tag?: string;
}

/**
 * Check if a given object implements the RunnerBuildReq interface.
 */
export function instanceOfRunnerBuildReq(value: object): value is RunnerBuildReq {
    return true;
}

export function RunnerBuildReqFromJSON(json: any): RunnerBuildReq {
    return RunnerBuildReqFromJSONTyped(json, false);
}

export function RunnerBuildReqFromJSONTyped(json: any, ignoreDiscriminator: boolean): RunnerBuildReq {
    if (json == null) {
        return json;
    }
    return {
        
        'arch': json['arch'] == null ? undefined : json['arch'],
        'args': json['args'] == null ? undefined : json['args'],
        'binaries': json['binaries'] == null ? undefined : ((json['binaries'] as Array<any>).map(BinarySpecFromJSON)),
        'branch': json['branch'] == null ? undefined : json['branch'],
        'bucket': json['bucket'] == null ? undefined : json['bucket'],
        'context': json['context'] == null ? undefined : json['context'],
        'dockerTarget': json['dockerTarget'] == null ? undefined : json['dockerTarget'],
        'dockerfile': json['dockerfile'] == null ? undefined : json['dockerfile'],
        'image': json['image'] == null ? undefined : json['image'],
        'os': json['os'] == null ? undefined : json['os'],
        'ref': json['ref'] == null ? undefined : json['ref'],
        'repo': json['repo'] == null ? undefined : json['repo'],
        'sha': json['sha'] == null ? undefined : json['sha'],
        'tag': json['tag'] == null ? undefined : json['tag'],
    };
}

export function RunnerBuildReqToJSON(json: any): RunnerBuildReq {
    return RunnerBuildReqToJSONTyped(json, false);
}

export function RunnerBuildReqToJSONTyped(value?: RunnerBuildReq | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'arch': value['arch'],
        'args': value['args'],
        'binaries': value['binaries'] == null ? undefined : ((value['binaries'] as Array<any>).map(BinarySpecToJSON)),
        'branch': value['branch'],
        'bucket': value['bucket'],
        'context': value['context'],
        'dockerTarget': value['dockerTarget'],
        'dockerfile': value['dockerfile'],
        'image': value['image'],
        'os': value['os'],
        'ref': value['ref'],
        'repo': value['repo'],
        'sha': value['sha'],
        'tag': value['tag'],
    };
}

