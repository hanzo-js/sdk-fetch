/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { InvoiceLine } from './InvoiceLine.js';
import {
    InvoiceLineFromJSON,
    InvoiceLineFromJSONTyped,
    InvoiceLineToJSON,
    InvoiceLineToJSONTyped,
} from './InvoiceLine.js';

/**
 * 
 * @export
 * @interface RaiseIn
 */
export interface RaiseIn {
    /**
     * Currency is the ISO 4217 code, lower-cased. Empty means usd.
     * @type {string}
     * @memberof RaiseIn
     */
    currency?: string;
    /**
     * CustomerEmail is where the invoice is sent. Optional.
     * @type {string}
     * @memberof RaiseIn
     */
    customerEmail?: string;
    /**
     * Lines are the charges. The invoice subtotal and amount due are COMPUTED
     * from these — there is no total field to send, because a total that
     * disagreed with its own lines would bill a number nobody could derive.
     * @type {Array<InvoiceLine>}
     * @memberof RaiseIn
     */
    lines?: Array<InvoiceLine>;
    /**
     * UserID identifies the customer being billed, within the caller's own org.
     * Required — an invoice with no addressee is not an invoice.
     * @type {string}
     * @memberof RaiseIn
     */
    userId?: string;
}

/**
 * Check if a given object implements the RaiseIn interface.
 */
export function instanceOfRaiseIn(value: object): value is RaiseIn {
    return true;
}

export function RaiseInFromJSON(json: any): RaiseIn {
    return RaiseInFromJSONTyped(json, false);
}

export function RaiseInFromJSONTyped(json: any, ignoreDiscriminator: boolean): RaiseIn {
    if (json == null) {
        return json;
    }
    return {
        
        'currency': json['currency'] == null ? undefined : json['currency'],
        'customerEmail': json['customerEmail'] == null ? undefined : json['customerEmail'],
        'lines': json['lines'] == null ? undefined : ((json['lines'] as Array<any>).map(InvoiceLineFromJSON)),
        'userId': json['userId'] == null ? undefined : json['userId'],
    };
}

export function RaiseInToJSON(json: any): RaiseIn {
    return RaiseInToJSONTyped(json, false);
}

export function RaiseInToJSONTyped(value?: RaiseIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'currency': value['currency'],
        'customerEmail': value['customerEmail'],
        'lines': value['lines'] == null ? undefined : ((value['lines'] as Array<any>).map(InvoiceLineToJSON)),
        'userId': value['userId'],
    };
}

