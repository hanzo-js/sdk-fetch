/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface ProjectsUploadGrant
 */
export interface ProjectsUploadGrant {
    /**
     * ExpiresAt is when the grant stops being accepted, as Unix seconds. It is
     * short-lived by design and is handed out ONCE, on the response that queues the
     * deployment — a later read of that deployment does not carry it, so a grant
     * cannot be fetched again after the build it was minted for.
     * @type {number}
     * @memberof ProjectsUploadGrant
     */
    expiresAt?: number;
    /**
     * Fields are form values every POST must carry VERBATIM, alongside `key` and
     * `file`. The signature covers them, so altering any one of them — including
     * widening the key to reach outside the prefix — invalidates the grant rather
     * than extending it.
     * @type {{ [key: string]: string; }}
     * @memberof ProjectsUploadGrant
     */
    fields?: { [key: string]: string; };
    /**
     * MaxBytes bounds ONE object, not the upload as a whole.
     * @type {number}
     * @memberof ProjectsUploadGrant
     */
    maxBytes?: number;
    /**
     * Prefix is the only place this grant can write: the deployment's own key
     * prefix. It authorizes WRITES ONLY, which is why completing a deployment
     * reconciles the prefix against a manifest instead of letting CI delete.
     * @type {string}
     * @memberof ProjectsUploadGrant
     */
    prefix?: string;
    /**
     * URL is the address to POST each object to. It is signed for the PUBLIC
     * endpoint, because the signature covers the host and CI posts from outside the
     * cluster.
     * @type {string}
     * @memberof ProjectsUploadGrant
     */
    url?: string;
}

/**
 * Check if a given object implements the ProjectsUploadGrant interface.
 */
export function instanceOfProjectsUploadGrant(value: object): value is ProjectsUploadGrant {
    return true;
}

export function ProjectsUploadGrantFromJSON(json: any): ProjectsUploadGrant {
    return ProjectsUploadGrantFromJSONTyped(json, false);
}

export function ProjectsUploadGrantFromJSONTyped(json: any, ignoreDiscriminator: boolean): ProjectsUploadGrant {
    if (json == null) {
        return json;
    }
    return {
        
        'expiresAt': json['expiresAt'] == null ? undefined : json['expiresAt'],
        'fields': json['fields'] == null ? undefined : json['fields'],
        'maxBytes': json['maxBytes'] == null ? undefined : json['maxBytes'],
        'prefix': json['prefix'] == null ? undefined : json['prefix'],
        'url': json['url'] == null ? undefined : json['url'],
    };
}

export function ProjectsUploadGrantToJSON(json: any): ProjectsUploadGrant {
    return ProjectsUploadGrantToJSONTyped(json, false);
}

export function ProjectsUploadGrantToJSONTyped(value?: ProjectsUploadGrant | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'expiresAt': value['expiresAt'],
        'fields': value['fields'],
        'maxBytes': value['maxBytes'],
        'prefix': value['prefix'],
        'url': value['url'],
    };
}

