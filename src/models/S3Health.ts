/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface S3Health
 */
export interface S3Health {
    /**
     * Error is why the probe is degraded, in plain words. Absent when it is not.
     * @type {string}
     * @memberof S3Health
     */
    error?: string;
    /**
     * Presign is whether presigned upload and download URLs can be minted, which
     * needs a PUBLIC endpoint on top of the credentials. False does not make the
     * surface degraded — listing and creating still work.
     * 
     * NOT omitempty. On the ready path the untyped probe wrote this key
     * unconditionally, and omitting a false one would turn what a
     * healthy-but-presignless deployment reports from "presign: false" into
     * silence — two different facts. The degraded body now carries it too, which is
     * the one delta of typing this: the probe answers ONE shape under both of its
     * statuses, which is what makes a single declared Out honest.
     * @type {boolean}
     * @memberof S3Health
     */
    presign?: boolean;
    /**
     * Ready is whether this deployment can serve object operations at all: true
     * only when admin credentials are configured.
     * @type {boolean}
     * @memberof S3Health
     */
    ready?: boolean;
    /**
     * Service names the subsystem this probe is for. Always "s3".
     * @type {string}
     * @memberof S3Health
     */
    service?: string;
    /**
     * Status is "ok" when the store is reachable in principle, "degraded" when it
     * is not. It is the field to read; the HTTP status carries the same fact for a
     * caller that only looks at the code.
     * @type {string}
     * @memberof S3Health
     */
    status?: string;
}

/**
 * Check if a given object implements the S3Health interface.
 */
export function instanceOfS3Health(value: object): value is S3Health {
    return true;
}

export function S3HealthFromJSON(json: any): S3Health {
    return S3HealthFromJSONTyped(json, false);
}

export function S3HealthFromJSONTyped(json: any, ignoreDiscriminator: boolean): S3Health {
    if (json == null) {
        return json;
    }
    return {
        
        'error': json['error'] == null ? undefined : json['error'],
        'presign': json['presign'] == null ? undefined : json['presign'],
        'ready': json['ready'] == null ? undefined : json['ready'],
        'service': json['service'] == null ? undefined : json['service'],
        'status': json['status'] == null ? undefined : json['status'],
    };
}

export function S3HealthToJSON(json: any): S3Health {
    return S3HealthToJSONTyped(json, false);
}

export function S3HealthToJSONTyped(value?: S3Health | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'error': value['error'],
        'presign': value['presign'],
        'ready': value['ready'],
        'service': value['service'],
        'status': value['status'],
    };
}

