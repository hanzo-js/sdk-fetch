/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface BackfillQuery
 */
export interface BackfillQuery {
    /**
     * Before bounds the seed to ledger rows written before this RFC3339 instant.
     * Defaults to now, and is snapped down to UTC midnight — the rollup's grain, so
     * the seeded days and the guarded days are the same set. Pass the day the
     * incremental view started capturing, so seed and view never share a day.
     * @type {string}
     * @memberof BackfillQuery
     */
    before?: string;
    /**
     * Force must be exactly "true" to seed a rollup that already holds rows. It is
     * spelled as a string, not a flag, because the guard has always compared this
     * value literally — "1" and "yes" do NOT force.
     * @type {string}
     * @memberof BackfillQuery
     */
    force?: string;
}

/**
 * Check if a given object implements the BackfillQuery interface.
 */
export function instanceOfBackfillQuery(value: object): value is BackfillQuery {
    return true;
}

export function BackfillQueryFromJSON(json: any): BackfillQuery {
    return BackfillQueryFromJSONTyped(json, false);
}

export function BackfillQueryFromJSONTyped(json: any, ignoreDiscriminator: boolean): BackfillQuery {
    if (json == null) {
        return json;
    }
    return {
        
        'before': json['before'] == null ? undefined : json['before'],
        'force': json['force'] == null ? undefined : json['force'],
    };
}

export function BackfillQueryToJSON(json: any): BackfillQuery {
    return BackfillQueryToJSONTyped(json, false);
}

export function BackfillQueryToJSONTyped(value?: BackfillQuery | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'before': value['before'],
        'force': value['force'],
    };
}

