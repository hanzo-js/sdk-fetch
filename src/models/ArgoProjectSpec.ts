/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ArgoDestination } from './ArgoDestination.js';
import {
    ArgoDestinationFromJSON,
    ArgoDestinationFromJSONTyped,
    ArgoDestinationToJSON,
    ArgoDestinationToJSONTyped,
} from './ArgoDestination.js';
import type { ArgoGroupKind } from './ArgoGroupKind.js';
import {
    ArgoGroupKindFromJSON,
    ArgoGroupKindFromJSONTyped,
    ArgoGroupKindToJSON,
    ArgoGroupKindToJSONTyped,
} from './ArgoGroupKind.js';

/**
 * 
 * @export
 * @interface ArgoProjectSpec
 */
export interface ArgoProjectSpec {
    /**
     * ClusterResourceWhitelist are the cluster-scoped kinds it may create —
     * [{group:"*", kind:"*"}] on a synthesized project.
     * @type {Array<ArgoGroupKind>}
     * @memberof ArgoProjectSpec
     */
    clusterResourceWhitelist?: Array<ArgoGroupKind>;
    /**
     * Description is the project's human label: the IAM project's display name, or
     * its description when it has no display name. Absent when IAM carries neither.
     * @type {string}
     * @memberof ArgoProjectSpec
     */
    description?: string;
    /**
     * Destinations are the cluster/namespace pairs it may write to — a single
     * {server:"*", namespace:"*"} on a synthesized project, for the same reason.
     * @type {Array<ArgoDestination>}
     * @memberof ArgoProjectSpec
     */
    destinations?: Array<ArgoDestination>;
    /**
     * SourceRepos are the git repos applications in this project may pull from.
     * ["*"] for every project this plane synthesizes or reflects from IAM: the
     * boundary that actually holds on this platform is the IAM org, resolved before
     * a row is ever projected, so the projected fence is deliberately permissive
     * and is NOT an authorization statement.
     * @type {Array<string>}
     * @memberof ArgoProjectSpec
     */
    sourceRepos?: Array<string>;
}

/**
 * Check if a given object implements the ArgoProjectSpec interface.
 */
export function instanceOfArgoProjectSpec(value: object): value is ArgoProjectSpec {
    return true;
}

export function ArgoProjectSpecFromJSON(json: any): ArgoProjectSpec {
    return ArgoProjectSpecFromJSONTyped(json, false);
}

export function ArgoProjectSpecFromJSONTyped(json: any, ignoreDiscriminator: boolean): ArgoProjectSpec {
    if (json == null) {
        return json;
    }
    return {
        
        'clusterResourceWhitelist': json['clusterResourceWhitelist'] == null ? undefined : ((json['clusterResourceWhitelist'] as Array<any>).map(ArgoGroupKindFromJSON)),
        'description': json['description'] == null ? undefined : json['description'],
        'destinations': json['destinations'] == null ? undefined : ((json['destinations'] as Array<any>).map(ArgoDestinationFromJSON)),
        'sourceRepos': json['sourceRepos'] == null ? undefined : json['sourceRepos'],
    };
}

export function ArgoProjectSpecToJSON(json: any): ArgoProjectSpec {
    return ArgoProjectSpecToJSONTyped(json, false);
}

export function ArgoProjectSpecToJSONTyped(value?: ArgoProjectSpec | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'clusterResourceWhitelist': value['clusterResourceWhitelist'] == null ? undefined : ((value['clusterResourceWhitelist'] as Array<any>).map(ArgoGroupKindToJSON)),
        'description': value['description'],
        'destinations': value['destinations'] == null ? undefined : ((value['destinations'] as Array<any>).map(ArgoDestinationToJSON)),
        'sourceRepos': value['sourceRepos'],
    };
}

