/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface AudiencePreview
 */
export interface AudiencePreview {
    /**
     * Available is false when the roster or the warehouse could not be read; the
     * counts are then zero because nothing was measured, not because the cohort
     * is empty, and Reason says which read failed.
     * @type {boolean}
     * @memberof AudiencePreview
     */
    available?: boolean;
    /**
     * Count is the cohort size: distinct warehouse identifiers for an event
     * audience, mailable customers for an event-less (whole-org) one.
     * @type {number}
     * @memberof AudiencePreview
     */
    count?: number;
    /**
     * Deliverable is how many de-duplicated mailboxes a send would reach. Two
     * customers sharing an address count once, so it is <= Count.
     * @type {number}
     * @memberof AudiencePreview
     */
    deliverable?: number;
    /**
     * Reason is the error text of the read that failed: the org's roster could
     * not be loaded ("identity store unavailable…"), or the cohort query had no
     * warehouse to run against ("analytics warehouse not configured"). Absent
     * when the evaluation succeeded, so its presence and Available=false are one
     * fact seen twice.
     * @type {string}
     * @memberof AudiencePreview
     */
    reason?: string;
    /**
     * Sample is up to 1000 cohort IDENTIFIERS — never addresses, which product
     * analytics does not hold. Empty for an event-less (whole-org) audience.
     * @type {Array<string>}
     * @memberof AudiencePreview
     */
    sample?: Array<string>;
    /**
     * Source names where the cohort was read: the events table for an event
     * audience, "iam:<org>" for the whole-org one.
     * @type {string}
     * @memberof AudiencePreview
     */
    source?: string;
    /**
     * Unmatched is how many cohort identifiers named nobody on the org's roster
     * and so have no address to mail. It is reported rather than hidden: it is
     * the honest explanation for a cohort of 500 that mails 3. Always 0 for an
     * event-less (whole-org) audience, which starts from the roster and has
     * nothing to match.
     * @type {number}
     * @memberof AudiencePreview
     */
    unmatched?: number;
}

/**
 * Check if a given object implements the AudiencePreview interface.
 */
export function instanceOfAudiencePreview(value: object): value is AudiencePreview {
    return true;
}

export function AudiencePreviewFromJSON(json: any): AudiencePreview {
    return AudiencePreviewFromJSONTyped(json, false);
}

export function AudiencePreviewFromJSONTyped(json: any, ignoreDiscriminator: boolean): AudiencePreview {
    if (json == null) {
        return json;
    }
    return {
        
        'available': json['available'] == null ? undefined : json['available'],
        'count': json['count'] == null ? undefined : json['count'],
        'deliverable': json['deliverable'] == null ? undefined : json['deliverable'],
        'reason': json['reason'] == null ? undefined : json['reason'],
        'sample': json['sample'] == null ? undefined : json['sample'],
        'source': json['source'] == null ? undefined : json['source'],
        'unmatched': json['unmatched'] == null ? undefined : json['unmatched'],
    };
}

export function AudiencePreviewToJSON(json: any): AudiencePreview {
    return AudiencePreviewToJSONTyped(json, false);
}

export function AudiencePreviewToJSONTyped(value?: AudiencePreview | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'available': value['available'],
        'count': value['count'],
        'deliverable': value['deliverable'],
        'reason': value['reason'],
        'sample': value['sample'],
        'source': value['source'],
        'unmatched': value['unmatched'],
    };
}

