/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { RiskLabelEvent } from './RiskLabelEvent.js';
import {
    RiskLabelEventFromJSON,
    RiskLabelEventFromJSONTyped,
    RiskLabelEventToJSON,
    RiskLabelEventToJSONTyped,
} from './RiskLabelEvent.js';

/**
 * 
 * @export
 * @interface RiskResolveIn
 */
export interface RiskResolveIn {
    /**
     * Horizon is how many days an event must age before it may be resolved at
     * all, and it is the whole of the no-leakage rule. 120 for the payment lane
     * (past the Visa and Mastercard dispute windows), 14 for signup abuse.
     * Unstated takes 120.
     * @type {number}
     * @memberof RiskResolveIn
     */
    horizon?: number;
    /**
     * Now moves the observation instant BACKWARDS, RFC 3339. It exists so a
     * BACKTEST can resolve labels as the plane stood at a past moment; without it,
     * every backtest would score a model against knowledge that arrived after the
     * decision it is being scored on. An instant after the server clock is
     * refused: a backtest resolves the past, and a future one would declare
     * unmatured events matured and hand a training set negatives for rows whose
     * chargeback has not had time to arrive.
     * @type {string}
     * @memberof RiskResolveIn
     */
    now?: string;
    /**
     * Subjects are the exact events being judged. Each carries its own event
     * time, because the as-of that keeps the future out is derived from that
     * instant plus the horizon — one as-of over a whole batch would give a
     * January row six extra months of hindsight.
     * 
     * One entry per DISTINCT (kind, subject, at): naming an event twice answers
     * once, because an event resolved twice would list its own winner as a
     * contrary claim and would hand a materialiser duplicate training rows.
     * @type {Array<RiskLabelEvent>}
     * @memberof RiskResolveIn
     */
    subjects?: Array<RiskLabelEvent>;
}

/**
 * Check if a given object implements the RiskResolveIn interface.
 */
export function instanceOfRiskResolveIn(value: object): value is RiskResolveIn {
    return true;
}

export function RiskResolveInFromJSON(json: any): RiskResolveIn {
    return RiskResolveInFromJSONTyped(json, false);
}

export function RiskResolveInFromJSONTyped(json: any, ignoreDiscriminator: boolean): RiskResolveIn {
    if (json == null) {
        return json;
    }
    return {
        
        'horizon': json['horizon'] == null ? undefined : json['horizon'],
        'now': json['now'] == null ? undefined : json['now'],
        'subjects': json['subjects'] == null ? undefined : ((json['subjects'] as Array<any>).map(RiskLabelEventFromJSON)),
    };
}

export function RiskResolveInToJSON(json: any): RiskResolveIn {
    return RiskResolveInToJSONTyped(json, false);
}

export function RiskResolveInToJSONTyped(value?: RiskResolveIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'horizon': value['horizon'],
        'now': value['now'],
        'subjects': value['subjects'] == null ? undefined : ((value['subjects'] as Array<any>).map(RiskLabelEventToJSON)),
    };
}

