/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface ProjectsPublish
 */
export interface ProjectsPublish {
    /**
     * Slug is the site to publish, from the path.
     * @type {string}
     * @memberof ProjectsPublish
     */
    slug?: string;
    /**
     * Source is the build output to promote, as a path RELATIVE to your org's own
     * storage space — never a URL and never a bucket. The org segment is prepended
     * server-side from the validated principal, so the worst a hostile source can
     * address is something your own org already owns.
     * @type {string}
     * @memberof ProjectsPublish
     */
    source?: string;
}

/**
 * Check if a given object implements the ProjectsPublish interface.
 */
export function instanceOfProjectsPublish(value: object): value is ProjectsPublish {
    return true;
}

export function ProjectsPublishFromJSON(json: any): ProjectsPublish {
    return ProjectsPublishFromJSONTyped(json, false);
}

export function ProjectsPublishFromJSONTyped(json: any, ignoreDiscriminator: boolean): ProjectsPublish {
    if (json == null) {
        return json;
    }
    return {
        
        'slug': json['slug'] == null ? undefined : json['slug'],
        'source': json['source'] == null ? undefined : json['source'],
    };
}

export function ProjectsPublishToJSON(json: any): ProjectsPublish {
    return ProjectsPublishToJSONTyped(json, false);
}

export function ProjectsPublishToJSONTyped(value?: ProjectsPublish | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'slug': value['slug'],
        'source': value['source'],
    };
}

