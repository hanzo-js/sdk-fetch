/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { CategorySpend } from './CategorySpend.js';
import {
    CategorySpendFromJSON,
    CategorySpendFromJSONTyped,
    CategorySpendToJSON,
    CategorySpendToJSONTyped,
} from './CategorySpend.js';
import type { SpendPoint } from './SpendPoint.js';
import {
    SpendPointFromJSON,
    SpendPointFromJSONTyped,
    SpendPointToJSON,
    SpendPointToJSONTyped,
} from './SpendPoint.js';

/**
 * 
 * @export
 * @interface Spend
 */
export interface Spend {
    /**
     * Available is false when the commerce ledger was unconfigured or
     * unreachable. Every number below is then an honest zero, NOT a measured one.
     * @type {boolean}
     * @memberof Spend
     */
    available?: boolean;
    /**
     * AvailableCents is what of that balance is still spendable.
     * @type {number}
     * @memberof Spend
     */
    availableCents?: number;
    /**
     * BalanceCents is the prepaid wallet's balance, in US cents.
     * @type {number}
     * @memberof Spend
     */
    balanceCents?: number;
    /**
     * ByCategory is the window's spend split by ledger category, largest first.
     * @type {Array<CategorySpend>}
     * @memberof Spend
     */
    byCategory?: Array<CategorySpend>;
    /**
     * MTDCents is commerce's authoritative month-to-date consumed figure, which
     * is a different period from the window and is not derived from it.
     * @type {number}
     * @memberof Spend
     */
    mtdCents?: number;
    /**
     * OverageCents is month-to-date consumption beyond the plan's allowance.
     * @type {number}
     * @memberof Spend
     */
    overageCents?: number;
    /**
     * Series is the window's spend over time, gap-filled at the window's
     * interval.
     * @type {Array<SpendPoint>}
     * @memberof Spend
     */
    series?: Array<SpendPoint>;
    /**
     * Source names where the roll-up came from.
     * @type {string}
     * @memberof Spend
     */
    source?: string;
    /**
     * TotalCents is consumption over the requested window, in US cents. It is
     * self-consistent with ByCategory and Series.
     * @type {number}
     * @memberof Spend
     */
    totalCents?: number;
}

/**
 * Check if a given object implements the Spend interface.
 */
export function instanceOfSpend(value: object): value is Spend {
    return true;
}

export function SpendFromJSON(json: any): Spend {
    return SpendFromJSONTyped(json, false);
}

export function SpendFromJSONTyped(json: any, ignoreDiscriminator: boolean): Spend {
    if (json == null) {
        return json;
    }
    return {
        
        'available': json['available'] == null ? undefined : json['available'],
        'availableCents': json['availableCents'] == null ? undefined : json['availableCents'],
        'balanceCents': json['balanceCents'] == null ? undefined : json['balanceCents'],
        'byCategory': json['byCategory'] == null ? undefined : ((json['byCategory'] as Array<any>).map(CategorySpendFromJSON)),
        'mtdCents': json['mtdCents'] == null ? undefined : json['mtdCents'],
        'overageCents': json['overageCents'] == null ? undefined : json['overageCents'],
        'series': json['series'] == null ? undefined : ((json['series'] as Array<any>).map(SpendPointFromJSON)),
        'source': json['source'] == null ? undefined : json['source'],
        'totalCents': json['totalCents'] == null ? undefined : json['totalCents'],
    };
}

export function SpendToJSON(json: any): Spend {
    return SpendToJSONTyped(json, false);
}

export function SpendToJSONTyped(value?: Spend | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'available': value['available'],
        'availableCents': value['availableCents'],
        'balanceCents': value['balanceCents'],
        'byCategory': value['byCategory'] == null ? undefined : ((value['byCategory'] as Array<any>).map(CategorySpendToJSON)),
        'mtdCents': value['mtdCents'],
        'overageCents': value['overageCents'],
        'series': value['series'] == null ? undefined : ((value['series'] as Array<any>).map(SpendPointToJSON)),
        'source': value['source'],
        'totalCents': value['totalCents'],
    };
}

