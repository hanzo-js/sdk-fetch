/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface ArgoSource
 */
export interface ArgoSource {
    /**
     * Path is the directory within RepoURL. Display-only alongside a display-only
     * RepoURL; CD's own value for a CD row.
     * @type {string}
     * @memberof ArgoSource
     */
    path?: string;
    /**
     * RepoURL is the git repository the desired state comes from. For an
     * application projected from an App CR it is the fleet manifest repo and is
     * DISPLAY ONLY — an App CR pins an image, and nothing is rendered from this
     * repo to produce it. For a CD row it is the repo CD actually polls.
     * @type {string}
     * @memberof ArgoSource
     */
    repoURL?: string;
    /**
     * TargetRevision is the git ref tracked there — a branch such as "main".
     * Display-only for a projected App CR; the ref CD tracks for a CD row.
     * @type {string}
     * @memberof ArgoSource
     */
    targetRevision?: string;
}

/**
 * Check if a given object implements the ArgoSource interface.
 */
export function instanceOfArgoSource(value: object): value is ArgoSource {
    return true;
}

export function ArgoSourceFromJSON(json: any): ArgoSource {
    return ArgoSourceFromJSONTyped(json, false);
}

export function ArgoSourceFromJSONTyped(json: any, ignoreDiscriminator: boolean): ArgoSource {
    if (json == null) {
        return json;
    }
    return {
        
        'path': json['path'] == null ? undefined : json['path'],
        'repoURL': json['repoURL'] == null ? undefined : json['repoURL'],
        'targetRevision': json['targetRevision'] == null ? undefined : json['targetRevision'],
    };
}

export function ArgoSourceToJSON(json: any): ArgoSource {
    return ArgoSourceToJSONTyped(json, false);
}

export function ArgoSourceToJSONTyped(value?: ArgoSource | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'path': value['path'],
        'repoURL': value['repoURL'],
        'targetRevision': value['targetRevision'],
    };
}

