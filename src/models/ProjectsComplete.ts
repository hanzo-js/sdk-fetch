/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface ProjectsComplete
 */
export interface ProjectsComplete {
    /**
     * Bytes is their total size in bytes.
     * @type {number}
     * @memberof ProjectsComplete
     */
    bytes?: number;
    /**
     * Commit is the revision that was built, recorded on the deployment.
     * @type {string}
     * @memberof ProjectsComplete
     */
    commit?: string;
    /**
     * Files is how many objects CI published.
     * @type {number}
     * @memberof ProjectsComplete
     */
    files?: number;
    /**
     * ID is the queued deployment to complete, from the path.
     * @type {string}
     * @memberof ProjectsComplete
     */
    id?: string;
    /**
     * Keys is the manifest CI just uploaded, RELATIVE to the deployment prefix. It
     * is what replaces `aws s3 sync --delete`: an upload grant authorizes writes
     * only, so CI cannot remove a file, and cloud reconciles the prefix against
     * this list instead (grant.go). Omit it and nothing is deleted — the prefix
     * only grows, which is the old pre-grant behaviour and a safe default.
     * @type {Array<string>}
     * @memberof ProjectsComplete
     */
    keys?: Array<string>;
    /**
     * LiveURL is a HINT at the address the site should serve at. The public host is
     * claimed by cloud first, so this can refine the URL a deployment reports but
     * can never assert a subdomain another tenant holds.
     * @type {string}
     * @memberof ProjectsComplete
     */
    liveUrl?: string;
    /**
     * Message is what happened, in words — on an error completion, why it failed.
     * @type {string}
     * @memberof ProjectsComplete
     */
    message?: string;
    /**
     * Slug is the project the deployment belongs to, from the path.
     * @type {string}
     * @memberof ProjectsComplete
     */
    slug?: string;
    /**
     * Status is how the build ended: `live` if it succeeded, `error` if it did not.
     * Nothing else is accepted.
     * @type {string}
     * @memberof ProjectsComplete
     */
    status?: string;
}

/**
 * Check if a given object implements the ProjectsComplete interface.
 */
export function instanceOfProjectsComplete(value: object): value is ProjectsComplete {
    return true;
}

export function ProjectsCompleteFromJSON(json: any): ProjectsComplete {
    return ProjectsCompleteFromJSONTyped(json, false);
}

export function ProjectsCompleteFromJSONTyped(json: any, ignoreDiscriminator: boolean): ProjectsComplete {
    if (json == null) {
        return json;
    }
    return {
        
        'bytes': json['bytes'] == null ? undefined : json['bytes'],
        'commit': json['commit'] == null ? undefined : json['commit'],
        'files': json['files'] == null ? undefined : json['files'],
        'id': json['id'] == null ? undefined : json['id'],
        'keys': json['keys'] == null ? undefined : json['keys'],
        'liveUrl': json['liveUrl'] == null ? undefined : json['liveUrl'],
        'message': json['message'] == null ? undefined : json['message'],
        'slug': json['slug'] == null ? undefined : json['slug'],
        'status': json['status'] == null ? undefined : json['status'],
    };
}

export function ProjectsCompleteToJSON(json: any): ProjectsComplete {
    return ProjectsCompleteToJSONTyped(json, false);
}

export function ProjectsCompleteToJSONTyped(value?: ProjectsComplete | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'bytes': value['bytes'],
        'commit': value['commit'],
        'files': value['files'],
        'id': value['id'],
        'keys': value['keys'],
        'liveUrl': value['liveUrl'],
        'message': value['message'],
        'slug': value['slug'],
        'status': value['status'],
    };
}

