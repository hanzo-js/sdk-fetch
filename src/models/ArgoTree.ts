/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ArgoNode } from './ArgoNode.js';
import {
    ArgoNodeFromJSON,
    ArgoNodeFromJSONTyped,
    ArgoNodeToJSON,
    ArgoNodeToJSONTyped,
} from './ArgoNode.js';

/**
 * 
 * @export
 * @interface ArgoTree
 */
export interface ArgoTree {
    /**
     * Hosts is ArgoCD's per-node machine inventory. Always empty: this plane
     * projects applications and serves no cluster-node view.
     * @type {Array<object>}
     * @memberof ArgoTree
     */
    hosts?: Array<object>;
    /**
     * Nodes is the FLAT node list, root first: the App CR, then the objects the
     * operator owns, then their ReplicaSets and Pods. The hierarchy is in
     * ParentRefs, not in the ordering.
     * @type {Array<ArgoNode>}
     * @memberof ArgoTree
     */
    nodes?: Array<ArgoNode>;
    /**
     * OrphanedNodes are objects in the namespace belonging to no application.
     * Always empty: this walk reaches an object only THROUGH ownership from the App
     * CR, so it can never hold one that is orphaned.
     * @type {Array<ArgoNode>}
     * @memberof ArgoTree
     */
    orphanedNodes?: Array<ArgoNode>;
}

/**
 * Check if a given object implements the ArgoTree interface.
 */
export function instanceOfArgoTree(value: object): value is ArgoTree {
    return true;
}

export function ArgoTreeFromJSON(json: any): ArgoTree {
    return ArgoTreeFromJSONTyped(json, false);
}

export function ArgoTreeFromJSONTyped(json: any, ignoreDiscriminator: boolean): ArgoTree {
    if (json == null) {
        return json;
    }
    return {
        
        'hosts': json['hosts'] == null ? undefined : json['hosts'],
        'nodes': json['nodes'] == null ? undefined : ((json['nodes'] as Array<any>).map(ArgoNodeFromJSON)),
        'orphanedNodes': json['orphanedNodes'] == null ? undefined : ((json['orphanedNodes'] as Array<any>).map(ArgoNodeFromJSON)),
    };
}

export function ArgoTreeToJSON(json: any): ArgoTree {
    return ArgoTreeToJSONTyped(json, false);
}

export function ArgoTreeToJSONTyped(value?: ArgoTree | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'hosts': value['hosts'],
        'nodes': value['nodes'] == null ? undefined : ((value['nodes'] as Array<any>).map(ArgoNodeToJSON)),
        'orphanedNodes': value['orphanedNodes'] == null ? undefined : ((value['orphanedNodes'] as Array<any>).map(ArgoNodeToJSON)),
    };
}

