/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface Remittance
 */
export interface Remittance {
    /**
     * AmountCents is the amount disbursed, in cents. It was reserved against pending
     * commission atomically when recorded, so it never exceeds what was owed.
     * @type {number}
     * @memberof Remittance
     */
    amountCents?: number;
    /**
     * CreatedAt is when the payout was recorded, Unix seconds UTC — when the balance
     * moved, not necessarily when the cash landed.
     * @type {number}
     * @memberof Remittance
     */
    createdAt?: number;
    /**
     * ID is the payout row's server-minted handle, "apo_"-prefixed.
     * @type {string}
     * @memberof Remittance
     */
    id?: string;
    /**
     * Method is how it was settled. "credits" issued a commerce grant into the
     * affiliate org's own wallet; any other value (wire, paypal, check, …) is a
     * RECORD of cash a human moved out of band.
     * @type {string}
     * @memberof Remittance
     */
    method?: string;
    /**
     * Reference is the operator's settlement note — a bank id, a ledger ref. Free
     * text, absent when none was given.
     * @type {string}
     * @memberof Remittance
     */
    reference?: string;
    /**
     * Txn is the commerce ledger transaction id, set ONLY where a "credits" payout
     * actually issued the grant. Absent for cash methods, which write no ledger row.
     * @type {string}
     * @memberof Remittance
     */
    txn?: string;
}

/**
 * Check if a given object implements the Remittance interface.
 */
export function instanceOfRemittance(value: object): value is Remittance {
    return true;
}

export function RemittanceFromJSON(json: any): Remittance {
    return RemittanceFromJSONTyped(json, false);
}

export function RemittanceFromJSONTyped(json: any, ignoreDiscriminator: boolean): Remittance {
    if (json == null) {
        return json;
    }
    return {
        
        'amountCents': json['amountCents'] == null ? undefined : json['amountCents'],
        'createdAt': json['createdAt'] == null ? undefined : json['createdAt'],
        'id': json['id'] == null ? undefined : json['id'],
        'method': json['method'] == null ? undefined : json['method'],
        'reference': json['reference'] == null ? undefined : json['reference'],
        'txn': json['txn'] == null ? undefined : json['txn'],
    };
}

export function RemittanceToJSON(json: any): Remittance {
    return RemittanceToJSONTyped(json, false);
}

export function RemittanceToJSONTyped(value?: Remittance | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'amountCents': value['amountCents'],
        'createdAt': value['createdAt'],
        'id': value['id'],
        'method': value['method'],
        'reference': value['reference'],
        'txn': value['txn'],
    };
}

