/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ByoGPU } from './ByoGPU.js';
import {
    ByoGPUFromJSON,
    ByoGPUFromJSONTyped,
    ByoGPUToJSON,
    ByoGPUToJSONTyped,
} from './ByoGPU.js';
import type { EngineAdvertisement } from './EngineAdvertisement.js';
import {
    EngineAdvertisementFromJSON,
    EngineAdvertisementFromJSONTyped,
    EngineAdvertisementToJSON,
    EngineAdvertisementToJSONTyped,
} from './EngineAdvertisement.js';

/**
 * 
 * @export
 * @interface ByoWorker
 */
export interface ByoWorker {
    /**
     * Arch/CPUs/Memory are the connecting host's static CPU spec, mirrored from the
     * registration: Arch is runtime.GOARCH (amd64 | arm64), Memory is total RAM in
     * BYTES — the same fields a code-linked run-target carries, so the /v1/visor/fleet
     * board renders a linked node's arch + cores + RAM like any other unit.
     * @type {string}
     * @memberof ByoWorker
     */
    arch?: string;
    /**
     * Capabilities is what this worker offers the org: "studio.render" when the
     * node can render, "engine.serve" when it serves a model endpoint. A node
     * advertises one only once it can honour it, so an absent list means a node
     * that has dialed in but is not ready to serve any of them yet.
     * @type {Array<string>}
     * @memberof ByoWorker
     */
    capabilities?: Array<string>;
    /**
     * CPUModel is the processor as the host names it ("Apple M3 Max"), for display.
     * @type {string}
     * @memberof ByoWorker
     */
    cpuModel?: string;
    /**
     * CPUs is the host's logical core count.
     * @type {number}
     * @memberof ByoWorker
     */
    cpus?: number;
    /**
     * Cuda is the host's CUDA toolkit version. NVIDIA hosts report it.
     * @type {string}
     * @memberof ByoWorker
     */
    cuda?: string;
    /**
     * Driver is the host's NVIDIA kernel driver version — distinct from Cuda, and
     * the one that bounds which CUDA versions can run on this box.
     * @type {string}
     * @memberof ByoWorker
     */
    driver?: string;
    /**
     * Engine is the hanzo-engine model server this node runs, when it runs one
     * (`hanzo link --serve-engine`). Absent means the node takes jobs but serves
     * no model endpoint.
     * @type {EngineAdvertisement}
     * @memberof ByoWorker
     */
    engine?: EngineAdvertisement;
    /**
     * FirstSeen is when this node first dialed in, RFC 3339 — the start of its
     * presence record, which `hanzo unlink` ends.
     * @type {string}
     * @memberof ByoWorker
     */
    firstSeen?: string;
    /**
     * GPUs are the accelerators the host found on itself. Empty is a real answer:
     * a CPU-only machine can dial in and take non-GPU work.
     * @type {Array<ByoGPU>}
     * @memberof ByoWorker
     */
    gpus?: Array<ByoGPU>;
    /**
     * Hip is the host's HIP runtime version, the AMD counterpart to Cuda.
     * @type {string}
     * @memberof ByoWorker
     */
    hip?: string;
    /**
     * Hostname is what the host calls itself. It equals ID for any hostname already
     * in the [a-z0-9-] alphabet, and differs when sanitizing had to change it.
     * @type {string}
     * @memberof ByoWorker
     */
    hostname?: string;
    /**
     * ID is the node's id in the fleet — the sanitized hostname it registered
     * under, which is also the `unit` its samples and its gpu-jobs lane key on.
     * This is the id to use everywhere else on the compute surface.
     * @type {string}
     * @memberof ByoWorker
     */
    id?: string;
    /**
     * JobQueue is the tasks NAMESPACE this worker claims render jobs out of —
     * "gpu-jobs" unless `hanzo link` was pointed at another. Within it, a job aimed
     * at this node alone rides the task-queue value "gpu:<id>".
     * @type {string}
     * @memberof ByoWorker
     */
    jobQueue?: string;
    /**
     * LastHeartbeat is the most recent beat this node sent, RFC 3339. It is what
     * Status is computed from, so a reader can check the judgement.
     * @type {string}
     * @memberof ByoWorker
     */
    lastHeartbeat?: string;
    /**
     * Location is always "on-prem" — a machine that dialed in has no cloud region,
     * and inventing one would put it somewhere it is not.
     * @type {string}
     * @memberof ByoWorker
     */
    location?: string;
    /**
     * Memory is the host's total RAM in BYTES.
     * @type {number}
     * @memberof ByoWorker
     */
    memory?: number;
    /**
     * Os is the host's operating system: linux, darwin or windows.
     * @type {string}
     * @memberof ByoWorker
     */
    os?: string;
    /**
     * Provider is always "byo": this machine is the operator's, not one Hanzo
     * provisioned. It exists so a fold into the machines/GPUs pages says which
     * rows are rented and which are the customer's own.
     * @type {string}
     * @memberof ByoWorker
     */
    provider?: string;
    /**
     * Rocm is the host's ROCm version. AMD hosts report it; empty otherwise.
     * @type {string}
     * @memberof ByoWorker
     */
    rocm?: string;
    /**
     * Status is "online" when the last heartbeat landed within 90s, else
     * "offline" — so it is a fact about heartbeat freshness, not about the box
     * being powered on. A worker that has never beaten reads offline.
     * @type {string}
     * @memberof ByoWorker
     */
    status?: string;
    /**
     * Version is the `hanzo` CLI version running on the node. It is what to check
     * when a worker is missing a field a newer registration reports.
     * @type {string}
     * @memberof ByoWorker
     */
    version?: string;
}

/**
 * Check if a given object implements the ByoWorker interface.
 */
export function instanceOfByoWorker(value: object): value is ByoWorker {
    return true;
}

export function ByoWorkerFromJSON(json: any): ByoWorker {
    return ByoWorkerFromJSONTyped(json, false);
}

export function ByoWorkerFromJSONTyped(json: any, ignoreDiscriminator: boolean): ByoWorker {
    if (json == null) {
        return json;
    }
    return {
        
        'arch': json['arch'] == null ? undefined : json['arch'],
        'capabilities': json['capabilities'] == null ? undefined : json['capabilities'],
        'cpuModel': json['cpuModel'] == null ? undefined : json['cpuModel'],
        'cpus': json['cpus'] == null ? undefined : json['cpus'],
        'cuda': json['cuda'] == null ? undefined : json['cuda'],
        'driver': json['driver'] == null ? undefined : json['driver'],
        'engine': json['engine'] == null ? undefined : EngineAdvertisementFromJSON(json['engine']),
        'firstSeen': json['firstSeen'] == null ? undefined : json['firstSeen'],
        'gpus': json['gpus'] == null ? undefined : ((json['gpus'] as Array<any>).map(ByoGPUFromJSON)),
        'hip': json['hip'] == null ? undefined : json['hip'],
        'hostname': json['hostname'] == null ? undefined : json['hostname'],
        'id': json['id'] == null ? undefined : json['id'],
        'jobQueue': json['jobQueue'] == null ? undefined : json['jobQueue'],
        'lastHeartbeat': json['lastHeartbeat'] == null ? undefined : json['lastHeartbeat'],
        'location': json['location'] == null ? undefined : json['location'],
        'memory': json['memory'] == null ? undefined : json['memory'],
        'os': json['os'] == null ? undefined : json['os'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'rocm': json['rocm'] == null ? undefined : json['rocm'],
        'status': json['status'] == null ? undefined : json['status'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function ByoWorkerToJSON(json: any): ByoWorker {
    return ByoWorkerToJSONTyped(json, false);
}

export function ByoWorkerToJSONTyped(value?: ByoWorker | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'arch': value['arch'],
        'capabilities': value['capabilities'],
        'cpuModel': value['cpuModel'],
        'cpus': value['cpus'],
        'cuda': value['cuda'],
        'driver': value['driver'],
        'engine': EngineAdvertisementToJSON(value['engine']),
        'firstSeen': value['firstSeen'],
        'gpus': value['gpus'] == null ? undefined : ((value['gpus'] as Array<any>).map(ByoGPUToJSON)),
        'hip': value['hip'],
        'hostname': value['hostname'],
        'id': value['id'],
        'jobQueue': value['jobQueue'],
        'lastHeartbeat': value['lastHeartbeat'],
        'location': value['location'],
        'memory': value['memory'],
        'os': value['os'],
        'provider': value['provider'],
        'rocm': value['rocm'],
        'status': value['status'],
        'version': value['version'],
    };
}

