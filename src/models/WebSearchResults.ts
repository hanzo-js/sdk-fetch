/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { WebResult } from './WebResult.js';
import {
    WebResultFromJSON,
    WebResultFromJSONTyped,
    WebResultToJSON,
    WebResultToJSONTyped,
} from './WebResult.js';
import type { WebEngine } from './WebEngine.js';
import {
    WebEngineFromJSON,
    WebEngineFromJSONTyped,
    WebEngineToJSON,
    WebEngineToJSONTyped,
} from './WebEngine.js';

/**
 * 
 * @export
 * @interface WebSearchResults
 */
export interface WebSearchResults {
    /**
     * Engines is one entry per engine asked, in the order they were asked. It is
     * ADDITIVE to the SearXNG contract, which the LibreChat client ignores as an
     * unknown field exactly as it ignores `engine` on a result.
     * @type {Array<WebEngine>}
     * @memberof WebSearchResults
     */
    engines?: Array<WebEngine>;
    /**
     * NumberOfResults is len(results) — what this answer carries, never an
     * estimate of what the web holds.
     * @type {number}
     * @memberof WebSearchResults
     */
    numberOfResults?: number;
    /**
     * Query is the query that ran, echoed back.
     * @type {string}
     * @memberof WebSearchResults
     */
    query?: string;
    /**
     * Results are the merged hits, deduplicated by normalised URL and capped at
     * 30. Always an array and never null: no hits is an ANSWER, not a fault.
     * @type {Array<WebResult>}
     * @memberof WebSearchResults
     */
    results?: Array<WebResult>;
}

/**
 * Check if a given object implements the WebSearchResults interface.
 */
export function instanceOfWebSearchResults(value: object): value is WebSearchResults {
    return true;
}

export function WebSearchResultsFromJSON(json: any): WebSearchResults {
    return WebSearchResultsFromJSONTyped(json, false);
}

export function WebSearchResultsFromJSONTyped(json: any, ignoreDiscriminator: boolean): WebSearchResults {
    if (json == null) {
        return json;
    }
    return {
        
        'engines': json['engines'] == null ? undefined : ((json['engines'] as Array<any>).map(WebEngineFromJSON)),
        'numberOfResults': json['number_of_results'] == null ? undefined : json['number_of_results'],
        'query': json['query'] == null ? undefined : json['query'],
        'results': json['results'] == null ? undefined : ((json['results'] as Array<any>).map(WebResultFromJSON)),
    };
}

export function WebSearchResultsToJSON(json: any): WebSearchResults {
    return WebSearchResultsToJSONTyped(json, false);
}

export function WebSearchResultsToJSONTyped(value?: WebSearchResults | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'engines': value['engines'] == null ? undefined : ((value['engines'] as Array<any>).map(WebEngineToJSON)),
        'number_of_results': value['numberOfResults'],
        'query': value['query'],
        'results': value['results'] == null ? undefined : ((value['results'] as Array<any>).map(WebResultToJSON)),
    };
}

