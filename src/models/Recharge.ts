/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { Recharged } from './Recharged.js';
import {
    RechargedFromJSON,
    RechargedFromJSONTyped,
    RechargedToJSON,
    RechargedToJSONTyped,
} from './Recharged.js';

/**
 * 
 * @export
 * @interface Recharge
 */
export interface Recharge {
    /**
     * Charged is how many of them were actually charged. It is at most Orgs, and
     * the difference is orgs whose balance was already above their threshold.
     * @type {number}
     * @memberof Recharge
     */
    charged?: number;
    /**
     * Orgs is how many orgs the sweep considered — every org with auto-recharge
     * armed, whether or not it needed charging.
     * @type {number}
     * @memberof Recharge
     */
    orgs?: number;
    /**
     * Results is one row per org considered, so a sweep that charged nobody is
     * still explainable. Never null.
     * @type {Array<Recharged>}
     * @memberof Recharge
     */
    results?: Array<Recharged>;
}

/**
 * Check if a given object implements the Recharge interface.
 */
export function instanceOfRecharge(value: object): value is Recharge {
    return true;
}

export function RechargeFromJSON(json: any): Recharge {
    return RechargeFromJSONTyped(json, false);
}

export function RechargeFromJSONTyped(json: any, ignoreDiscriminator: boolean): Recharge {
    if (json == null) {
        return json;
    }
    return {
        
        'charged': json['charged'] == null ? undefined : json['charged'],
        'orgs': json['orgs'] == null ? undefined : json['orgs'],
        'results': json['results'] == null ? undefined : ((json['results'] as Array<any>).map(RechargedFromJSON)),
    };
}

export function RechargeToJSON(json: any): Recharge {
    return RechargeToJSONTyped(json, false);
}

export function RechargeToJSONTyped(value?: Recharge | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'charged': value['charged'],
        'orgs': value['orgs'],
        'results': value['results'] == null ? undefined : ((value['results'] as Array<any>).map(RechargedToJSON)),
    };
}

