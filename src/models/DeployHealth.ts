/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface DeployHealth
 */
export interface DeployHealth {
    /**
     * CRD reports whether the App custom resource is served and listable. Absent
     * when the apiserver was unreachable, because then it is unknown rather than
     * false.
     * @type {boolean}
     * @memberof DeployHealth
     */
    crd?: boolean;
    /**
     * K8s reports whether the Kubernetes API is reachable. Absent when the probe
     * did not get far enough to find out.
     * @type {boolean}
     * @memberof DeployHealth
     */
    k8s?: boolean;
    /**
     * Service names the subsystem answering, so a probe response is attributable
     * when several are collected together.
     * @type {string}
     * @memberof DeployHealth
     */
    service?: string;
    /**
     * Status is `ok` when this deployment can serve the delivery plane, and
     * `degraded` otherwise. It agrees with the HTTP status by construction — see
     * StatusCode.
     * @type {string}
     * @memberof DeployHealth
     */
    status?: string;
}

/**
 * Check if a given object implements the DeployHealth interface.
 */
export function instanceOfDeployHealth(value: object): value is DeployHealth {
    return true;
}

export function DeployHealthFromJSON(json: any): DeployHealth {
    return DeployHealthFromJSONTyped(json, false);
}

export function DeployHealthFromJSONTyped(json: any, ignoreDiscriminator: boolean): DeployHealth {
    if (json == null) {
        return json;
    }
    return {
        
        'crd': json['crd'] == null ? undefined : json['crd'],
        'k8s': json['k8s'] == null ? undefined : json['k8s'],
        'service': json['service'] == null ? undefined : json['service'],
        'status': json['status'] == null ? undefined : json['status'],
    };
}

export function DeployHealthToJSON(json: any): DeployHealth {
    return DeployHealthToJSONTyped(json, false);
}

export function DeployHealthToJSONTyped(value?: DeployHealth | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'crd': value['crd'],
        'k8s': value['k8s'],
        'service': value['service'],
        'status': value['status'],
    };
}

