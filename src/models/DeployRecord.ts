/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface DeployRecord
 */
export interface DeployRecord {
    /**
     * Created reports whether this call recorded a new attribution edge (201) or
     * found an existing one (200). Absent when nothing was recorded.
     * @type {boolean}
     * @memberof DeployRecord
     */
    created?: boolean;
    /**
     * CreatedAt is when the edge was first recorded, in unix seconds. Absent when
     * nothing was recorded.
     * @type {number}
     * @memberof DeployRecord
     */
    createdAt?: number;
    /**
     * DeployID is the attribution edge's handle. Absent when nothing was recorded.
     * @type {string}
     * @memberof DeployRecord
     */
    deployId?: string;
    /**
     * Reason says why nothing was attributed. Present only when recorded is false.
     * @type {string}
     * @memberof DeployRecord
     */
    reason?: string;
    /**
     * Recorded reports whether the deploy was attributed to an author at all. False
     * is the ordinary answer for a project built from no repository, or from one no
     * author has verified — never an error, so a deploy path can fire this
     * unconditionally.
     * @type {boolean}
     * @memberof DeployRecord
     */
    recorded?: boolean;
    /**
     * Self reports that the deploying org IS the author's org. Such a deploy is
     * recorded for provenance but excluded from accrual. Absent when nothing was
     * recorded.
     * @type {boolean}
     * @memberof DeployRecord
     */
    self?: boolean;
}

/**
 * Check if a given object implements the DeployRecord interface.
 */
export function instanceOfDeployRecord(value: object): value is DeployRecord {
    return true;
}

export function DeployRecordFromJSON(json: any): DeployRecord {
    return DeployRecordFromJSONTyped(json, false);
}

export function DeployRecordFromJSONTyped(json: any, ignoreDiscriminator: boolean): DeployRecord {
    if (json == null) {
        return json;
    }
    return {
        
        'created': json['created'] == null ? undefined : json['created'],
        'createdAt': json['createdAt'] == null ? undefined : json['createdAt'],
        'deployId': json['deployId'] == null ? undefined : json['deployId'],
        'reason': json['reason'] == null ? undefined : json['reason'],
        'recorded': json['recorded'] == null ? undefined : json['recorded'],
        'self': json['self'] == null ? undefined : json['self'],
    };
}

export function DeployRecordToJSON(json: any): DeployRecord {
    return DeployRecordToJSONTyped(json, false);
}

export function DeployRecordToJSONTyped(value?: DeployRecord | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'created': value['created'],
        'createdAt': value['createdAt'],
        'deployId': value['deployId'],
        'reason': value['reason'],
        'recorded': value['recorded'],
        'self': value['self'],
    };
}

